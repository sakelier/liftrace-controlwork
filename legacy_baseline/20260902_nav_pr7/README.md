# patrol_control 基线快照

- 基线提交：`d95377c`
- 快照范围：`patrol_uav_ws-patrol_planner/src/patrol_control` 的 27 个受 Git 跟踪文件
- 用途：在移植 VCL06 正式任务闭环前保留旧控制包原貌；本目录不参与 Catkin 编译
- `FILES.txt`：相对路径清单
- `SHA256SUMS`：规则要求的快照校验值

本快照只记录导航 PR6 后继分支的直接基线，不包含视觉集成仓此前生成的 foreign
`legacy_baseline`，也不包含旧临时 `coverage_search_manager`。
