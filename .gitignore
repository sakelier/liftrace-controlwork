# --- ROS / catkin build artifacts (any nesting level) ---
build/
devel/
install/
logs/

# --- Compiled binaries / libraries / python caches ---
*.so
*.o
*.a
*.pyc
__pycache__/

# --- Prebuilt odom_visualization executables (rebuild from src/) ---
**/Fast-Planner/uav_simulator/Utils/odom_visualization/bin/

# --- catkin workspace toplevel symlink (auto-generated by catkin_init_workspace) ---
**/src/CMakeLists.txt

# --- Dereferenced symlink loop: catkin_simple test fixture recurses into a
#     full copy of itself (~40 levels). ---
**/tool/catkin_simple/test/scenarios/hello_world/catkin_simple/
