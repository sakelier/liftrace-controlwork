# 仿真联调变更记录

本文档是当前仓库唯一的长期联调变更台账。

用途只有两个：

1. 记录已经完成的仿真、识别、主控、环境和文档改动。
2. 作为后续所有联调修改的统一追加入口。

维护规则：

1. 以后每次修改代码、脚本、环境、launch 或文档后，都在本文档追加一条新记录。
2. 不覆盖旧记录，只追加。
3. 每条记录至少写清：
   - 日期
   - 改动范围
   - 具体改动
   - 验证结果
   - 遗留问题
   - 下一步

## 2026-06-02

### 改动范围

- Sprint 1：修复 Fast-Planner 内部仿真器三大崩溃根因 + odom 硬编码
- Sprint 1：追加 `quadrotor_simulator_so3` 源头四元数归一化
- Sprint 2：创建 PX4+Gazebo 外部仿真集成（Launch 文件 + 脚本）
- 开发计划更新：融入职责边界划分与 5 阶段视觉路线

### 已完成改动

1. 修复 `quadrotor_simulator_so3.cpp` 数据竞争。
   - 给全局 `static Command command` 和 `static Disturbance disturbance` 各加 `std::mutex`（`cmd_mutex` / `dist_mutex`）
   - `cmd_callback`、`force_disturbance_callback`、`moment_disturbance_callback` 内加 `std::lock_guard`
   - 主循环中锁下拷贝 `command` / `disturbance` 快照，后续使用快照
   - 消除 ROS 回调与主循环之间的 UB 数据竞争

2. 修复 `Quadrotor.cpp` 极性分解错误。
   - `Quadrotor::step()` 和 `Quadrotor::operator()` 两处原使用 `Eigen::LLT::matrixL()` 做旋转矩阵重正交化，数学上不正确
   - 改为 SVD 正交投影：`Eigen::JacobiSVD` → `R = U * V^T`
   - 修复姿态发散和数值不稳定问题

3. 修复 `so3_disturbance_generator.cpp` 四元数未归一化。
   - `noisy_q` 和 `correction_q`（由 `R_to_quaternion(ypr_to_R(...))` 得出）发布前增加 `/ norm(q, 2)` 归一化
   - 消除 `quaternion has not been unitized` 警告

4. 修复 3 个 `alignment_control_*.cpp` 中硬编码 `/mavros/local_position/odom`。
   - `alignment_control_new.cpp`、`alignment_control_calculate.cpp`、`alignment_control_converter.cpp`
   - 增加 `nh_.param<std::string>("odom_topic", odom_topic, "/mavros/local_position/odom")`，通过 ROS 参数可配，默认值向后兼容

5. 更新开发计划 `C:\Users\ASUS\.claude\plans\elegant-baking-cerf.md`：
   - 加入视觉/控制/导航职责边界表
   - Sprint 3 `target_memory` 收缩为单帧确认（不投影世界坐标）
   - 新增 Sprint 2.5（25年模型复测评估，视觉 B 阶段）
   - Sprint 4 的 4.1 拆分为 4.1A/4.1B/4.1C（视觉/控制/导航各自责任）

6. 追加 `quadrotor_simulator_so3` 源头四元数归一化。
   - `stateToOdomMsg()` 和 `quadToImuMsg()` 中 `Eigen::Quaterniond q(state.R)` 后加 `q.normalize()`
   - 确保 `/visual_slam/odom` 从源头就发布归一化四元数
   - 配合 `so3_disturbance_generator` 的归一化，形成双重保障

7. 创建 PX4+Gazebo 外部仿真集成（Sprint 2）：
   - `FAST_LIO/config/mid360_sim.yaml` — PX4 仿真用 FAST_LIO 配置（`lidar_type: 2` Velodyne 格式）
   - `FAST_LIO/launch/mapping_mid360_sim.launch` — 加载 sim 配置的 mapping 启动文件
   - `plan_manage/launch/patrol_planner_px4_sim.launch` — PX4 版 planner launch（不含内部 simulator，odom 默认 `/mavros/local_position/odom`）
   - `patrol_control/launch/patrol_control_px4_sim.launch` — PX4 版 control launch（不做 odom remap）
   - `top_level_scripts/sim_env.sh` — 环境配置脚本（PX4/Gazebo/MAVROS 路径、world 资源准备）
   - `top_level_scripts/simulation_px4_gazebo.sh` — tmux 四窗格完整仿真启动脚本
8. 新增 `patrol_control/scripts/odom_to_pose_relay.py` — 内部仿真 Odometry→PoseStamped 桥接。
   - 内部仿真器发布 `/state_ukf/odom`（Odometry 类型），但 `patrol_control` 订阅 `/mavros/local_position/pose`（PoseStamped）
   - relay 节点完成类型转换，使内部仿真链完整可用

9. 修复 `patrol_control.cpp` 四元数归一化（PX4 路径）。
   - `positionCallback` 中增加归一化逻辑，确保从 MAVROS 收到的 PX4 EKF 四元数始终为单位四元数
   - 零四元数兜底为 identity `(0,0,0,1)`

10. 修正 `patrol_control_sim.launch` — 加入 relay 节点，修复话题类型错配。

11. 更新 `AGENTS.md`：
    - 新增规则 13：WSL2 shell 命令必须用 `wsl -e bash -c '...'` 执行
    - 新增 2.7 节：宿主环境与 WSL2 shell 约定（Windows 宿主 + WSL2 Ubuntu 20.04、`-e` 标志、bash 内置参数展开）

### 验证结果

1. `patrol_uav_ws-patrol_planner` 四轮 `catkin_make -j32` 全部编译通过。
2. 内部仿真链路：9 节点全部在线，无 `boost::lock_error`，无崩溃。需 relay 节点确认完整闭环。
3. **PX4+Gazebo 外部仿真完整链路验证通过**：
   - PX4 SITL 正常启动，iris_mid360 模型成功生成（`/vehicle_spawn_*` 节点在线）
   - MAVROS 2 秒内上线，全部插件初始化成功
   - liftrace 控制链 5 节点在线（`fast_planner_node`、`patrol_control`、`traj_server`、`waypoint_generator`、`/gazebo`）
   - **四元数警告完全消除**（`patrol_control.cpp` positionCallback 归一化 + identity fallback）

### 遗留问题

1. 内部仿真 relay（`odom_to_pose_relay.py`）尚未单独验证。
2. liftrace 有自己的比赛世界文件 `toudi3.world`（含建筑、地面标记板、树木、大箱子），但引用的 7 个自定义模型资产（`big_box3/4` 的 DAE 网格、`dibao/qiaoliang/tanke/zhangpeng/zhuangjiache` 的材质脚本和纹理）未从香橙派拷贝到本地，当前 `sim_env.sh` 仍指向 speedrace 的世界目录。

### 下一步

1. 用户手动验证 PX4+Gazebo 完整链路（含 GUI）：
   ```bash
   bash /home/xhj/liftrace/top_level_scripts/simulation_px4_gazebo.sh
   # 等 Gazebo 加载后，在新窗格运行：
   source /home/xhj/liftrace/top_level_scripts/sim_env.sh && liftrace_setup_sim_env
   roslaunch patrol_control patrol_control_px4_sim.launch
   ```
2. 进入 Sprint 2.5（模型复测）/ Sprint 3（视觉迁移）并行推进。

## 2026-06-05

### 改动范围

- 文档状态复核与修订
- `toudi3.world` 缺失资产整理
- 视觉数据自动标注与模型路线补充
- 对前序 `fast_planner_node` 状态结论进行纠偏

### 已完成改动

1. 更新项目总览与仿真文档：
   - [README.md](/home/xhj/liftrace/README.md)
   - [SIMULATION_GUIDE_NOETIC_PX4_GAZEBO_QGC.md](/home/xhj/liftrace/SIMULATION_GUIDE_NOETIC_PX4_GAZEBO_QGC.md)
   - [VISION_WORKSPACE_GUIDE.md](/home/xhj/liftrace/VISION_WORKSPACE_GUIDE.md)
   - [vision_ws/README.md](/home/xhj/liftrace/vision_ws/README.md)
   - [VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md](/home/xhj/liftrace/VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md)

2. 修正文档中过期状态：
   - 不再把 `cv_bridge/OpenCV` 和 `image_geometry` 作为当前未解决编译阻塞
   - 明确本机 `patrol_uav_ws-patrol_planner` 与 `vision_ws` 已可编译
   - 明确本机 `PX4` 中已存在 `iris_mid360`、`mid360`
   - 把缺失仿真资产收敛为 `patrol_world.launch` 与 `dibao/qiaoliang/tanke/zhangpeng/zhuangjiache`

3. 新增 `toudi3` 仿真缺失资产清单：
   - [TOUDI3_SIM_ASSET_CHECKLIST.md](/home/xhj/liftrace/TOUDI3_SIM_ASSET_CHECKLIST.md)
   - 明确哪些模型本机已具备，哪些仍需后续去板端补拷

4. 新增视觉数据自动标注与模型路线方案：
   - [VISION_DATASET_AUTO_LABEL_PLAN.md](/home/xhj/liftrace/VISION_DATASET_AUTO_LABEL_PLAN.md)
   - 明确今年优先做黑色圆区 ROI 提取，再做自动标注与教师模型流程

5. 更新视觉执行计划：
   - 把四周计划调整为六周
   - 新增 ROI 裁剪和自动标注任务
   - 把计划重点从“继续写大方案”收敛到“最小可执行闭环”

6. 纠正前序对 `fast_planner_node` 的乐观结论。
   - 2026-06-05 本地复核时，内部仿真仍可复现 `boost::lock_error`
   - 因此当前状态应认定为“可编译、可拉起，但内部仿真仍不稳定”，而不是“已完全修复”

### 验证结果

1. 本地再次核对：
   - `vision_ws/test_data/image/` 下已有 840 张 JPG
   - `toudi3.world` 直接引用了 `dibao/qiaoliang/tanke/zhangpeng/zhuangjiache`
   - 本机 PX4 目录中已存在 `iris_mid360`、`mid360`、`big_box3`、`big_box4`、`juniper_Tree`

2. 文档交叉一致性已修正：
   - README、仿真手册、视觉工作区说明、执行计划对“当前进展”和“缺失资产”的描述已统一

### 遗留问题

1. `fast_planner_node` 仍未定位到唯一根因，当前只能确认最强嫌疑仍在内部 odom / quaternion 质量和 planner 运行时回调路径。
2. `toudi3.world` 相关比赛模型仍需后续上板或旧环境补拷。
3. 自动标注方案已写成文档，但脚本和首版伪标签尚未开始产出。

### 下一步

1. 优先实现黑色圆区 ROI 提取脚本，并跑完 840 张原图。
2. 准备去板端补拷 `patrol_world.launch` 和 5 个缺失模型目录。
3. 在独立回合继续定位 `fast_planner_node` 崩溃，避免把未稳定的内部仿真当作基线。

---

## 2026-06-05（后半）

### 改动范围

- 修订视觉数据与训练计划
- 明确 `image + rotated` 合并为统一数据集
- 明确 `tank` 纳入同一个主模型

### 已完成改动

1. 更新 [VISION_DATASET_AUTO_LABEL_PLAN.md](/home/xhj/liftrace/VISION_DATASET_AUTO_LABEL_PLAN.md)：
   - 删除“坦克旋转图单独处理”的旧建议
   - 明确 `image` 与 `rotated` 合并成一个统一检测数据集
   - 明确训练目标收敛为一个包含 `tank` 的统一主模型

2. 更新 [VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md](/home/xhj/liftrace/VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md)：
   - 删除“标准目标和 tank 模型合并评估”的摇摆表述
   - 将任务改为直接整理统一数据集和单模型训练计划
   - 把风险项从“双模型常开”改为“继续维护双模型结构”

### 验证结果

1. 自动标注文档与执行计划对数据组织和模型路线的口径已统一：
   - `image + rotated` 合并
   - `tank` 进入同一个类别体系
   - 板端目标仍为单 RKNN 主模型

### 遗留问题

1. 目前只是文档与计划修订，尚未真正开始数据目录整理和标签合并。
2. 统一数据集后，`tank` 类样本占比与重采样策略仍需在训练阶段进一步定量设计。

### 下一步

1. 先把 `image` 和 `rotated` 的统一目录结构与类别命名方案定出来。
2. 直接开始 ROI 提取与统一自动标注，不再保留独立 tank 训练线。

---

## 2026-06-05（标注文档修订）

### 改动范围

- 修订自动标注文档
- 明确 `red_cross` 暂不进入当前 YOLO 自动标注与训练闭环
- 同步修正执行计划中的类别口径

### 已完成改动

1. 更新 [VISION_DATASET_AUTO_LABEL_PLAN.md](/home/xhj/liftrace/VISION_DATASET_AUTO_LABEL_PLAN.md)：
   - 明确当前已完成的是统一 5 类自动标注数据集
   - 明确 `red_cross` 继续沿用原始传统视觉方案
   - 将 ROI 从主标注前置步骤降级为辅助复核/精修工具

2. 更新 [VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md](/home/xhj/liftrace/VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md)：
   - 将 YOLO 主模型类别口径修正为 `bridge/panzer/pillbox/tent/tank`
   - 删除把 `red_cross` 混入单模型训练计划的表述

### 验证结果

1. 标注文档与执行计划口径已统一：
   - `image + rotated` → 统一 5 类 YOLO 数据集
   - `red_cross` → 现有传统视觉方案

### 遗留问题

1. `red_cross` 后续是否需要单独引入学习模型，仍可赛前再评估。
2. 当前 5 类自动标注数据集还需要继续做人工抽检和误标清理。

### 下一步

1. 以现有 5 类数据集继续推进单模型训练。
2. 把红十字迁移与优化放在传统视觉分支中独立推进。

---

## 2026-06-05（执行计划对齐）

### 改动范围

- 修正执行计划中的当前状态与周计划
- 对齐“自动标注已完成首版、ROI 非主前置”的现状

### 已完成改动

1. 更新 [VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md](/home/xhj/liftrace/VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md)：
   - 将“自动标注流程”状态改为“已完成首版”
   - 将 ROI 从主线前置任务降为辅助工具
   - 把第 2 周重点改为自动标注抽检与数据清理

### 验证结果

1. 执行计划与当前实际状态已一致：
   - 已有统一 5 类自动标注数据集
   - `red_cross` 不在当前 YOLO 数据集中
   - ROI 仅作为后续辅助工具

### 遗留问题

1. 当前仍需继续做 5 类数据集的人审与清理。
2. ROI 工具尚未形成正式脚本落地。

### 下一步

1. 对现有 `yolo_dataset` 做类别分布、误标和难例抽检。
2. 基于 5 类数据集进入单模型训练准备。

---

## 2026-05-24

### 改动范围

- 项目初始化：从香橙派机载电脑拷贝代码到本地
- 项目文档建设
- 视觉工作区搭建
- `patrol_uav_ws-patrol_planner` 编译修复
- 仿真启动链路修正

### 已完成改动

1. 从香橙派 `orangepi@192.168.3.15` 通过 SSH 拷贝机载项目到本地 `/home/xhj/liftrace`。

2. 整理项目级文档，建立新 agent 接手规范：
   - [AGENTS.md](/home/xhj/liftrace/AGENTS.md) — 项目开发规范、架构说明、必读文件顺序、agent 工作流模板
   - [README.md](/home/xhj/liftrace/README.md) — 项目总览
   - [SIMULATION_GUIDE_NOETIC_PX4_GAZEBO_QGC.md](/home/xhj/liftrace/SIMULATION_GUIDE_NOETIC_PX4_GAZEBO_QGC.md) — 本机 ROS Noetic + PX4 + Gazebo + QGC 仿真手册
   - [VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md](/home/xhj/liftrace/VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md) — 2026 视觉组执行方案
   - [VISION_WORKSPACE_GUIDE.md](/home/xhj/liftrace/VISION_WORKSPACE_GUIDE.md) — 视觉工作区方案
   - [VISION_MIGRATION_CHECKLIST.md](/home/xhj/liftrace/VISION_MIGRATION_CHECKLIST.md) — 视觉迁移清单

3. 搭建视觉开发工作区 `vision_ws`：
   - `vision_ws/src/uav_vision/` — 正式沉淀包，新代码优先放这里
   - `vision_ws/src/yolov5_detect/` — 历史 YOLO/RKNN 参考
   - `vision_ws/src/detect_pkg/` — 历史检测消息、脚本、图像发布工具
   - `vision_ws/src/camera_sdk/` — 相机输入
   - `vision_ws/migration_refs/patrol_control_visual/` — patrol_control 视觉节点参考快照（不参与编译）

4. 修复 `cv_bridge` 对 OpenCV 版本的硬编码。
   - [cv_bridge/CMakeLists.txt](/home/xhj/liftrace/patrol_uav_ws-patrol_planner/src/tool/cv_bridge/CMakeLists.txt)
   - 改为优先查找 OpenCV 4，找不到再回退 OpenCV 3
   - 适配 Ubuntu 20.04 / ROS Noetic 默认 OpenCV 4 环境

5. 移除 `local_sensing_node` 中失效的 `cmake_modules` 依赖。
   - [local_sensing/CMakeLists.txt](/home/xhj/liftrace/patrol_uav_ws-patrol_planner/src/Fast-Planner/uav_simulator/local_sensing/CMakeLists.txt)
   - [local_sensing/package.xml](/home/xhj/liftrace/patrol_uav_ws-patrol_planner/src/Fast-Planner/uav_simulator/local_sensing/package.xml)
   - 原 `cmake_modules` 包在当前 ROS Noetic 环境中不存在，移除后编译通过

6. 补充 `actuator_pwm` 对 `patrol_control` 消息/服务生成的显式依赖。
   - [actuator_pwm/CMakeLists.txt](/home/xhj/liftrace/patrol_uav_ws-patrol_planner/src/actuator_pwm/CMakeLists.txt)
   - 新增 `add_dependencies(... ${patrol_control_EXPORTED_TARGETS})`，避免并行编译时 `patrol_control` 消息未先生成导致 `actuator_pwm` 编译失败

7. 修复 `bspline_opt` 中 nlopt 库的硬编码路径。
   - [bspline_opt/CMakeLists.txt](/home/xhj/liftrace/patrol_uav_ws-patrol_planner/src/Fast-Planner/fast_planner/bspline_opt/CMakeLists.txt)
   - 原写法直接链接 `/usr/local/lib/libnlopt.so`，改为 `find_path` + `find_library` 在 `/usr/include`、`/usr/local/include`、`/usr/lib`、`/usr/lib/x86_64-linux-gnu`、`/usr/local/lib` 中标准查找 `nlopt_cxx` 或 `nlopt`

8. 修正仿真启动链路中的里程计话题错配。
   - [patrol_planner_sim.launch](/home/xhj/liftrace/patrol_uav_ws-patrol_planner/src/Fast-Planner/fast_planner/plan_manage/launch/patrol_planner_sim.launch)
     - 默认 `odom_topic` 从 `/mavros/local_position/odom` 改为 `/state_ukf/odom`
     - 内部仿真器 `simulator.xml` 内部使用 `/visual_slam/odom` 和 `/state_ukf/odom`，原默认值指向 PX4 实机话题，和内部仿真链不匹配
   - [patrol_control_sim.launch](/home/xhj/liftrace/patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_control_sim.launch)
     - `patrol_control` 节点新增 `<remap from="/mavros/local_position/odom" to="/state_ukf/odom"/>`
     - 源码中 `alignment_control_*.cpp` 三处硬编码订阅 `/mavros/local_position/odom`，仿真时通过 remap 重定向到内部模拟器里程计

### 验证结果

1. `patrol_uav_ws-patrol_planner` 工作区从干净状态 `catkin_make` 编译通过。
2. 外部仿真底座 `AstraDroneOpen + PX4 + Gazebo + MAVROS` 可稳定启动，`iris_mid360` 模型成功生成。
3. 内部仿真 `patrol_control_sim.launch` 可将全部节点拉入 ROS 图：
   - `fast_planner_node`
   - `traj_server`
   - `patrol_control`
   - `quadrotor_simulator_so3`
   - `so3_control`
   - `random_forest`
   - `pcl_render_node`
   - `waypoint_generator`
   - `odom_visualization`

### 遗留问题

1. **`fast_planner_node` 运行时崩溃** — 启动后报错：
   - `boost::lock_error: mutex lock failed in pthread_mutex_lock: Invalid argument`
   - 大量 `quaternion has not been unitized` 警告
   - 初步判断为 Fast-Planner 内部线程/互斥量生命周期问题，以及内部 simulator 发布的姿态四元数未单位化
   - 当前状态：**能编、能起节点，但内部仿真链路不能稳定运行**

2. `liftrace` 缺少成套的 Gazebo world/models 目录，只有两份孤立的 `toudi3.world`。完整比赛任务场景需要复用上级目录的 `AstraDroneOpen` 仿真底座。

3. 尚未做 PX4 + Gazebo 外部仿真与 `liftrace` 控制/规划链路的对接联调。

4. 视觉链路（`vision_ws`）尚未编译验证，`uav_vision` 内部仅有骨架。

5. `patrol_control` 源码中 `alignment_control_new.cpp`、`alignment_control_calculate.cpp`、`alignment_control_converter.cpp` 三处硬编码订阅 `/mavros/local_position/odom`，只靠 launch 层 remap 兜底，后续如果有新 launch 入口容易漏改。

### 下一步

1. **优先修复 `fast_planner_node` 崩溃**：
   - 排查 Fast-Planner 线程/互斥量生命周期
   - 检查内部 simulator 发布的姿态四元数是否单位化
   - 确认 simulator.xml 与 patrol_planner_sim.launch 的话题配置一致性
   - 排除多 roscore/roslaunch 残留导致的 run_id 或参数污染

2. 在内部仿真稳定后，推进 PX4 + Gazebo 外部仿真与 liftrace 的对接。

3. 编译验证 `vision_ws`，确认 `uav_vision` 包可被 catkin_make 解析。

---

## 2026-06-02（后半）— 数据资产入库

### 改动范围

- 本地目录新增 5 个压缩包：OpenCV 旧版源码、图像数据集、YOLO 模型包

### 已完成改动

1. `opencv-3.4.8.zip`（88MB）— OpenCV 3.4.8 完整源码，旧版本，供板端编译或备查使用。

2. `image.zip`（491MB，840 张 JPG）— 比赛场地图像数据集，来源为 2025-03-19 拍摄的 `IMG_20250319_10xxxx.jpg` 序列。**不含坦克图像**，为通用场景/目标数据。

3. `rotated.zip`（55MB，60 张 PNG）— 坦克旋转图像数据集，`ztank_rot000.png` 至 `ztank_rot354.png`，步长 6 度，覆盖 360° 旋转。**所有坦克图像均在此压缩包内**。

4. `Visual/src/yolo.zip`（19MB）— YOLOv5 检测 ROS 包（旧版），含 `tank_rknn_model/`、`best_rknn_model/` 两个 RKNN 模型目录及 `scripts/`（`yolo_detect.py`、`depth_align.py`、参考图片）。

5. `Visual/src/yolov5.zip`（28MB）— YOLOv5 检测 ROS 包（新版），相比旧版增加：
   - `best.pt`（5.5MB）— PyTorch 训练权重
   - `src/image_process.cpp` — C++ 图像处理节点源码
   - `srv/image2center.srv` — 图像到中心的 ROS 服务定义

### 验证结果

1. 所有压缩包完整性正常，可正常列出内容。
2. `image.zip` 和 `rotated.zip` 内容互斥——坦克图像全部在 `rotated.zip` 中，`image.zip` 为无坦克的通用场景图。

### 推理评估（Sprint 2.5 首次推理）

用 `best.pt`（YOLO11n，4 类：bridge/panzer/pillbox/tent）对两个数据集做批量推理，置信度阈值 0.25：

**`image.zip`（840 张，每张一个靶标）**：
- 检出率 100%（840/840），符合预期
- 类别分布：panzer 304, tent 283, pillbox 253, bridge **0**（数据集中无桥样本）
- 置信度集中在 0.93–0.96 —— 模型对已训练类别辨识度高，是好信号

**`rotated.zip`（60 张坦克旋转图）**：
- 检出率 100%，全部识别为 `panzer`（平均置信度 0.960）
- 模型四类标签中无 `tank`，坦克被归入最近似的 `panzer` 类别
- 检测框正确找到了目标，但分类错误——需要 `tank_rknn_model` 做坦克专项识别
- `tank_rknn_model` 为 RKNN 格式（`.rknn`），本地 x86 无法运行，需上 RK3588

**结论**：`best.pt` 在 panzer/tent/pillbox 三类上工作正常。bridge 因数据集无样本无法评估。坦克检测需上板端测 `tank_rknn_model`。

### 遗留问题

1. `yolo.zip` 与 `yolov5.zip` 中模型权重文件（`best-rk3588.rknn`）是否对应 2025 年 vs 2026 年训练版本需要确认。

### 下一步

1. 确认 RKNN 模型权重版本，区分 2025 年旧模型与 2026 年需重新训练的模型。

---

## 2026-06-03 — 统一 5 类数据集自动标注

### 改动范围

- 用教师模型对 900 张图批量自动标注，生成 YOLO 格式统一 5 类数据集
- 合并 image（840 张）和 rotated（60 张）两组数据，不再分模型管理

### 已完成改动

1. `best.pt`（4 类）对 image 集 840 张做推理，生成 YOLO 标签（panzer 304, tent 283, pillbox 253, bridge 0）。

2. `tank.pt`（1 类）对 rotated 集 60 张做推理，生成 YOLO 标签（tank 60）。全部成功检出，平均置信度 0.947。

3. 合并为统一 5 类 YOLO 数据集：
   - 路径：`vision_ws/test_data/yolo_dataset/`
   - 训练集 720 张，验证集 180 张（8:2 随机分割）
   - 类别索引：`0=bridge, 1=panzer, 2=pillbox, 3=tent, 4=tank`
   - 每张图同步保存 overlay 可视化（`overlays/`），方便人工抽检

4. 新增脚本：
   - `vision_ws/scripts/auto_label.py` — 教师模型推理 + YOLO 标签生成 + 数据集合并

### 验证结果

| 类别 | train | val | 合计 |
|------|-------|-----|------|
| panzer(1) | 249 | 55 | 304 |
| tent(3) | 222 | 61 | 283 |
| pillbox(2) | 200 | 53 | 253 |
| tank(4) | 49 | 11 | 60 |
| bridge(0) | **0** | **0** | **0** |

### 遗留问题

1. **bridge 零样本**——数据集中完全没有桥梁标注，需后续从板端或额外采集补充。
2. 标注为教师模型输出（伪标签），尚未人工抽检修正。需通过 overlay 图排查低置信度和误检样本。

### 下一步

1. 人工抽检 overlay 图，确认教师模型标注质量。
2. 补充 bridge 类别训练样本。
3. 形成首版训练集后，训练统一 5 类 YOLO11n 模型。

---

## 2026-06-05（模型训练）

### 改动范围

- 训练统一 5 类 YOLO11n 检测模型

### 已完成改动

1. 使用 COCO 预训练 `yolo11n.pt` 在统一数据集上微调：
   - 数据：720 train + 180 val（panzer 304 / tent 283 / pillbox 253 / tank 60 / bridge 0）
   - 参数：epochs=100, patience=20, imgsz=640, batch=16, GPU RTX 4060
   - 实际训练 41 epoch，早停触发（最佳 epoch 21），总耗时 ~5.6 min

2. 模型产出：`vision_ws/runs/liftrace_5cls_v1/weights/best.pt`（5.3MB）

3. 清理无用目录（ROI 输出、旧推理可视化），释放 1.2GB。

### 验证结果

| 指标 | 值 |
|------|-----|
| mAP50 | 0.995 |
| mAP50-95 | 0.994 |
| Precision | 0.998 |
| Recall | 1.000 |
| 推理速度 | 3.2ms/图（RTX 4060） |
| 参数量 | 2.58M |
| GFLOPs | 6.3 |

### 遗留问题

1. bridge 类别零样本，模型对桥梁的检测能力来自 COCO 预训练迁移，未经领域数据验证。
2. 指标偏高是因为训练数据来自教师模型标注，本质上是知识蒸馏——在真实新数据上的泛化能力需实测确认。

### 下一步

1. 用训练好的模型对原始数据做交叉推理验证，确认无明显回退。
2. 导出 ONNX → RKNN 部署格式，为板端部署做准备。
3. 后续补充 bridge 样本后可增量训练。

## 2026-06-05（工程师 Review 修复 + 交叉验证）

### 改动范围

- `vision_ws/scripts/auto_label.py`：去除硬编码路径
- `vision_ws/scripts/inference_eval.py`：去除硬编码路径
- `vision_ws/scripts/analyze_detections.py`：去除硬编码路径，改为 CLI args
- `vision_ws/scripts/cross_validate.py`：新建交叉验证脚本
- `docs/仿真联调变更记录.md`：移除对已删除脚本的引用
- `VISION_DATASET_AUTO_LABEL_PLAN.md`：第 1 节内容修正为实际工作流

### 已完成改动

1. 工程师 Review 发现 4 项问题逐一修复（2026-06-04 subagent fan-out）：
   - F1：`auto_label.py` 移除硬编码 `/home/xhj/liftrace`，改用 `Path(__file__).resolve().parents[2]` + CLI args
   - F2：修复 `analyze_detections.py` 硬编码路径，改为 `--input`/`--model`/`--conf` CLI args
   - F3：变更台账移除 `extract_roi.py` 引用
   - F4：`VISION_DATASET_AUTO_LABEL_PLAN.md` 第 1 节改写反映实际采用的"全图直接自动标注"方案

2. 新建 `cross_validate.py`：学生模型 vs 教师标签的逐图比对（类别 + IoU）。

3. 交叉验证结果：学生模型 `liftrace_5cls_v1/best.pt` 对全部 900 张图（840 image + 60 rotated）推理，与教师标签逐图比对。

### 验证结果

| 指标 | 值 |
|------|-----|
| 总图数 | 900 |
| 一致 (cls + IoU≥0.5) | 900 |
| 类别不一致 | 0 |
| 学生漏检 | 0 |
| 教师标签缺失 | 0 |
| 学生多检 (≥2 框) | 0 |

结论：学生模型（YOLO11n）在训练数据上 100% 复现了教师标签，零差异样本。但此结果只验证蒸馏忠实度，不代表真实泛化能力——教师标签本身的质量决定了学生模型的上限。

### 遗留问题

1. bridge 类别仍为零样本，交叉验证无法评估。
2. 教师标签（best.pt + tank.pt）的质量未经验证——若教师有系统偏误，学生也会学到。
3. 缺少未参与训练的独立新图做泛化测试（当前闭环：train/val 均来自同一批 900 张图）。

### 下一步

1. 从香橙派补充 bridge 样本图片。
2. 从香橙派获取一批独立新图（非当前 900 张内）做泛化评测。
3. 导出 ONNX → RKNN 做板端初步部署验证。
4. 启动 Sprint 3 视觉迁移：在 `uav_vision` 定义消息、迁移 `cross_detector_node`（传统 CV 红十字）。

---

## 2026-06-05（执行计划重构）

### 改动范围

- `C:\Users\ASUS\.claude\plans\elegant-baking-cerf.md`：重写执行计划
- `docs/仿真联调变更记录.md`：追加本次文档变更记录

### 已完成改动

1. 将 `elegant-baking-cerf.md` 从历史阶段清单改写为当前可执行版本，不再把已完成的构建修复和统一 5 类训练作为主动任务。
2. 明确当前主线为：
   - `uav_vision` 接口包落地
   - 最小视觉闭环
   - 自动搜索首版闭环
   - 板端主路径与联调
3. 锁定自动搜索首版方案为“覆盖航线巡航 + 视觉中断”，并明确视觉、`patrol_control`、Fast-Planner 的职责边界。
4. 固定新增公共接口、兼容旧接口、候选确认规则、视觉中断阈值、阶段验收项和默认假设。
5. 明确本阶段暂不考虑 `bridge` 样本补充和独立新图泛化测试。

### 验证结果

1. 已核对当前仓库状态与计划内容一致：
   - `uav_vision` 仍为空骨架
   - 统一 5 类自动标注与训练已完成
   - 旧主控仍依赖 `/yolo_detect`、`/detect/waypoint_mark_point`、`/detect/tank_status`、`/detect/land_mark_point`
2. 本次仅修改文档，未执行 ROS 编译、训练或仿真命令。

### 遗留问题

1. 计划文档已更新，但具体代码实现仍待后续执行方按阶段推进。
2. `bridge` 零样本问题仍然存在，只是本阶段被明确排除在验收范围外。
3. 板端 RKNN 主路径和自动搜索闭环仍需后续实现与联调验证。

### 下一步

1. 按计划从 `Phase A` 开始，先补齐 `uav_vision` 消息、依赖和统一参数接口。
2. 完成 `cross_detector_node`、`circle_detector_node`、`detect_compat_bridge`、`drop_aligner` 的最小闭环迁移。
3. 在 `patrol_control` 中落地”覆盖航线巡航 + 视觉中断”的自动搜索首版状态机。

---

## 2026-06-05（Phase A 完成）

### 改动范围

- `vision_ws/src/uav_vision/`：接口包落地

### 已完成改动

1. 更新 `package.xml`：补充全部依赖（`roscpp`、`rospy`、`std_msgs`、`sensor_msgs`、`geometry_msgs`、`image_transport`、`cv_bridge`、`image_geometry`、`message_generation`、`message_runtime`）。

2. 更新 `CMakeLists.txt`：添加 `add_message_files` + `generate_messages` + `catkin_install_python`，安装 launch/config。

3. 新增 6 个消息定义：
   - `TargetDetection.msg` — 单帧检测结果
   - `TargetDetectionArray.msg` — 一帧内全部检测
   - `TargetCandidate.msg` — 跨帧跟踪候选（含 id/state/observe_count/TTL）
   - `TargetCandidateArray.msg` — 活跃候选列表
   - `DropOffset.msg` — 像素偏差
   - `DropReady.msg` — 投递释放许可

4. 新建 `launch/uav_vision.launch`：5 个统一参数。

5. 新建 `config/default.yaml`：参数默认值。

6. 新建 `scripts/placeholder_node.py`：占位节点，Phase A 验收用。

### 验证结果

| 检查项 | 状态 |
|--------|:--:|
| `vision_ws` 编译通过 | ✅ |
| 6 消息 C++ 头文件生成 | ✅ |
| 6 消息 Python import | ✅ |
| launch 可启动 | ✅ |
| 5 参数正确加载 | ✅ |

### 遗留问题

1. 占位节点不发布任何话题，Phase B 需替换为正式节点。
2. 消息头文件已生成但尚未被消费。

### 下一步

1. Phase B：按序迁移 `cross_detector_node` → `circle_detector_node` → `detect_compat_bridge` → `drop_aligner`。

---

## 2026-06-05（Phase B 完成）

### 改动范围

- `vision_ws/src/uav_vision/`：最小视觉闭环落地（4 节点）

### 已完成改动

1. **`cross_detector_node`** (C++) — 红十字传统视觉检测
   - 算法：HSV 双区间（0-10° + 170-180°）→ 形态学闭运算+开运算（7×7 椭圆核）→ 轮廓筛选 → 形状验证（宽高比 ≥ 0.6、solidity [0.6, 0.85]、四方向交叉结构）→ 可选黑色外圈检查（默认关闭）
   - 输出：`/uav_vision/detections`（`TargetDetectionArray`，class_name="red_cross"）
   - 新增 `config/cross_detector.yaml`：HSV 阈值、形状参数、黑色外圈开关
   - 新增 `launch/cross_detection.launch`：独立启动
   - 关键修复：原版缺少形态学操作，175 个红色碎片无法识别；新增 `morphologyEx(MORPH_CLOSE+MORPH_OPEN)` 后稳定检出

2. **`circle_detector_node`** (C++) — 蓝色圆环检测
   - 算法：HSV 蓝色区间（H 90-130）→ 形态学开+闭（15×15 椭圆核）→ 椭圆拟合 → 宽高比 ≥ 0.85 → 半径 [10, 300]
   - 输出：`/uav_vision/detections`（`TargetDetectionArray`，class_name="circle"）
   - 新增 `config/circle_detector.yaml`：HSV 阈值、椭圆质量参数、缩放开关
   - 新增 `launch/circle_detection.launch`：独立启动

3. **`detect_compat_bridge`** (Python) — 新接口 → 旧话题映射
   - 订阅：`/uav_vision/detections`、`/uav_vision/drop_offset`、`/uav_vision/drop_ready`
   - 发布旧话题：`/yolo_detect`（`TargetDetectionArray`）、`/detect/waypoint_mark_point`（`PoseStamped`）、`/detect/tank_status`（`Bool`）、`/detect/cross_mark_point`（`PoseStamped`）、`/detect/cross_status`（`Bool`）
   - 从 `/uav_vision/detections` 中提取 tank/cross 状态自动转发

4. **`drop_aligner`** (Python) — 像素偏差 + 释放许可
   - 订阅：`/uav_vision/targets`（`TargetCandidateArray`）
   - 发布：`/uav_vision/drop_offset`（像素偏差）、`/uav_vision/drop_ready`（连续 stable_frames 帧偏差在 max_offset_px 内即 ready）
   - 新增 `config/drop_aligner.yaml`：参考点、最大偏差、稳定帧数、最小置信度

5. **测试与诊断脚本**
   - `scripts/debug_cross_detect.py`：红十字离线诊断（HSV 区间、形态学效果、轮廓质量）
   - `scripts/debug_circle_detect.py`：蓝色圆环离线诊断（椭圆拟合、宽高比分析）
   - `scripts/test_phase_b.py`：22 项自动化验证（算法正确性、消息导入、编译产物、YAML 配置、launch XML 语法）
   - `test_data/circle_test.png`：合成蓝色圆环测试图像（800×600，外径 120px，内径 60px）

6. **参数优化**
   - `cross_solidity_max` 从 0.9 放宽到 0.85（实测红十字 solidity=0.725）
   - `enable_black_ring_check` 默认 `false`（避免干扰检测）
   - `enable_debug_image` 全局默认 `false`（符合 AGENTS.md 约束）

### 验证结果

| 检查项 | 状态 |
|--------|:--:|
| `vision_ws` 全部目标编译通过 | ✅ |
| 6 消息 Python import | ✅ |
| 6 消息 Python 文件生成 | ✅ |
| `cross_detector_node` 二进制存在 | ✅ |
| `circle_detector_node` 二进制存在 | ✅ |
| `detect_compat_bridge.py` 安装 | ✅ |
| `drop_aligner.py` 安装 | ✅ |
| 4 launch XML 语法有效 | ✅ |
| 4 YAML 配置可解析 | ✅ |
| `enable_debug_image` 默认 `false` | ✅ |
| 红十字算法：真实图像检出（solidity=0.725） | ✅ |
| 圆形算法：合成图像检出（center 偏差 < 5px） | ✅ |

### phase_b.launch 内容

```
cross_detector_node → /uav_vision/detections
circle_detector_node → /uav_vision/detections
detect_compat_bridge  → /yolo_detect, /detect/waypoint_mark_point, /detect/tank_status, /detect/cross_mark_point, /detect/cross_status
drop_aligner          → /uav_vision/drop_offset, /uav_vision/drop_ready
```

### 遗留问题

1. `circle_detector_node` 缺少真实俯视蓝白靶标图像进行完整 ROS 联调测试（当前仅用合成图像验证算法）。
2. `detect_compat_bridge` 和 `drop_aligner` 仅在语法/导入层面验证，未进行 roscore 启动下的端到端话题收发测试（需要 rosbag 回放或仿真图像源）。
3. `/detect/land_mark_point` 兼容输出未在本阶段实现，按计划 Phase D 处理。
4. `target_memory` 节点未在本阶段实现，Phase C 新增。
5. `drop_ready.reason` 字符串需与 `patrol_control` 约定对齐。

### 下一步

1. Phase C：自动搜索首版闭环（`target_memory` 多帧确认 + 视觉中断状态机）。
2. 为 `circle_detector_node` 准备俯视蓝白靶标 rosbag 或仿真图像源。

---

## 2026-06-05（Phase C：target_memory 落地）

### 改动范围

- `vision_ws/src/uav_vision/scripts/target_memory.py`：新增候选记忆节点
- `vision_ws/src/uav_vision/config/target_memory.yaml`：新增配置
- `vision_ws/src/uav_vision/launch/phase_c.launch`：新增 Phase C 启动文件
- `vision_ws/src/uav_vision/CMakeLists.txt`：注册 `target_memory.py` 安装
- `vision_ws/scripts/test_target_memory.py`：27 项自动化验证

### 已完成改动

1. **`target_memory.py`** — 图像域候选确认与状态管理
   - 订阅：`/uav_vision/detections`（`TargetDetectionArray`）
   - 发布：`/uav_vision/targets`（`TargetCandidateArray`）、`/uav_vision/selected_target`（`TargetCandidate`）
   - 核心逻辑：
     - 跨帧匹配：同类目标中心距离 < `match_distance_px`（80px）视为同一候选
     - 状态流转：DETECTED(0) → OBSERVING(1) → CONFIRMED(2) → REJECTED(3)/EXPIRED(4)
     - 多帧确认：连续 `confirm_frames`（3）帧观测 → CONFIRMED
     - TTL 老化：`candidate_ttl`（3.0s）未观测 → EXPIRED
     - 拒绝冷却：确认后丢失的目标加入 `reject_cooldown`（5.0s）冷却，期内不再作为候选
     - 置信度阈值：red_cross 使用更严格的阈值（class 0.70 + geom 0.85），标准目标使用正常阈值（class 0.60 + geom 0.70）
     - 优先级排序：`/uav_vision/targets` 按 YAML 权重降序排列
     - `/uav_vision/selected_target`：仅发布最高优先级的 CONFIRMED 目标

2. **`target_memory.yaml`** — 19 个可配参数
   - 匹配/生命周期：`confirm_frames`、`candidate_ttl`、`reject_cooldown`、`match_distance_px`
   - 视觉中断阈值：`cross_class_confidence`、`cross_geometry_confidence`、`std_class_confidence`、`std_geometry_confidence`
   - 优先级权重：`priority_red_cross`=10.0、`priority_panzer`=2.5、`priority_pillbox`=1.5、`priority_tent`=1.0、`priority_tank`=1.0、`priority_bridge`=0.0

3. **`phase_c.launch`** — Phase C 完整启动（5 节点）
   ```
   cross_detector_node   → /uav_vision/detections
   circle_detector_node  → /uav_vision/detections
   detect_compat_bridge  → 旧话题 (5 个)
   drop_aligner          ← /uav_vision/targets  → /uav_vision/drop_offset, /uav_vision/drop_ready
   target_memory         ← /uav_vision/detections → /uav_vision/targets, /uav_vision/selected_target
   ```

### 验证结果

| 检查项 | 状态 |
|--------|:--:|
| `vision_ws` 全部目标编译通过 | ✅ |
| `target_memory.py` 安装到 devel | ✅ |
| `target_memory.yaml` 存在且可解析 | ✅ |
| `phase_c.launch` XML 有效 | ✅ |
| Python 语法有效 | ✅ |
| ROS 消息导入正常 | ✅ |
| 状态常量正确（0-4） | ✅ |
| 3 帧确认流转（DETECTED→OBSERVING→CONFIRMED） | ✅ |
| 置信度阈值过滤（cross 严格/std 正常） | ✅ |
| 优先级排序（red_cross=10 > bridge=0） | ✅ |
| TTL 老化（>3s 过期） | ✅ |
| `/uav_vision/targets` ↔ `drop_aligner` 接口一致 | ✅ |

### 接口数据流（Phase C 完整链路）

```
相机图像
  ├── cross_detector_node   → TargetDetection (red_cross)
  ├── circle_detector_node  → TargetDetection (circle)
  │                              ↓
  │                    /uav_vision/detections
  │                              ↓
  ├── target_memory       → TargetCandidateArray (/uav_vision/targets)
  │                       → TargetCandidate     (/uav_vision/selected_target)
  │                              ↓
  ├── drop_aligner        → DropOffset  (/uav_vision/drop_offset)
  │                       → DropReady   (/uav_vision/drop_ready)
  │                              ↓
  └── detect_compat_bridge → /yolo_detect, /detect/waypoint_mark_point,
                              /detect/tank_status, /detect/cross_mark_point,
                              /detect/cross_status
```

### 状态机约定（供 patrol_control 集成参考）

`uav_vision` 侧只发布候选状态，不驱动状态机。`patrol_control` 应读取以下信号决定中断/进近/恢复：

| patrol_control 状态 | 触发条件 |
|---------------------|----------|
| SEARCH_CRUISE | 默认巡航 |
| CANDIDATE_HOLD | `/uav_vision/selected_target` 出现 CONFIRMED 目标 |
| APPROACH_TARGET | 目标连续 CONFIRMED 且优先级稳定 |
| VERIFY_TARGET | 进近到位后启用 circle_detector 精修 |
| DROP_ALIGN | `/uav_vision/drop_offset` 连续稳定 |
| DROP_RELEASE | `/uav_vision/drop_ready.ready == True` |
| REJECT_TARGET | 几何复核失败或超时 |
| RESUME_SEARCH | 目标丢失 > TTL 且拒绝冷却生效 |

### 遗留问题

1. `target_memory` 仅在算法逻辑和语法层面验证，未进行 roscore 启动下的端到端测试（需 rosbag 回放或仿真图像源）。
2. `patrol_control` 侧的视觉中断状态机尚未实现，本阶段仅交付视觉侧接口。
3. `bridge` 类别权重为 0，当前不参与自动选择；其兼容输出也未在此阶段覆盖。

### 下一步

1. 在 `patrol_control` 侧接入 `/uav_vision/targets` 和 `/uav_vision/selected_target`，实现搜索中断状态机。
2. Phase D：板端 RKNN 主路径 + `/detect/land_mark_point` 兼容迁移。

---

## 2026-06-05（全盘 review + 执行计划校正）

### 改动范围

- `C:\Users\ASUS\.claude\plans\elegant-baking-cerf.md`：根据全盘 review 更新执行计划
- `docs/仿真联调变更记录.md`：追加本次 review 记录

### 已完成改动

1. 按“本机工作区自 2026-05-24 开始活动”的口径重新梳理资产边界：
   - 未在联调台账出现且文件时间早于 `2026-05-01` 的内容，视为上一届旧资产。
   - `patrol_uav_ws-patrol_planner/src/patrol_control/*`、`vision_ws/src/yolov5_detect/*`、旧 `.bak/.back/.beifen` 文件均归入上一届基线。
   - `uav_vision` 下的 Phase A/B/C 消息、launch、yaml、节点实现归入本轮新增资产。

2. 明确上一届设计结论：
   - 旧 YOLO/RKNN 视觉链路明确包含 `bridge`，并非完全未考虑。
   - 旧 `patrol_control` 默认 `goal={"panzer"}`，说明任务主逻辑没有把 `bridge` 作为默认主目标。
   - 本轮不能把“旧数据集中缺少 bridge 样本”误读为“旧实现完全没有 bridge 支持”。

3. 完成 Phase B / Phase C review，确认当前主要待修订项：
   - `detect_compat_bridge` 的旧话题消息类型与 `patrol_control` 实际订阅类型不一致。
   - `target_memory` 读取了 `confirm_frames` 参数，但状态推进仍写死为 3 帧。
   - `target_memory` 中 CONFIRMED 目标超时后进入拒绝冷却的逻辑未真正生效。
   - `priority <= 0` 的目标仍可能进入 `/uav_vision/selected_target`。
   - `drop_aligner` 当前直接使用 `/uav_vision/targets[0]`，未限制为 confirmed-only / interruptible 目标。
   - `uav_vision/README.md` 仍停留在“尚未迁入节点”的旧口径。

4. 更新执行计划：
   - 将 Phase A 标记为已完成。
   - 将 Phase B / Phase C 标记为“代码已落地但未通过验收”。
   - 新增“上一届旧资产判断与结论”章节。
   - 新增“当前立即执行顺序”，把 bridge 兼容、target_memory 修复、drop_aligner 门控和文档同步列为先行任务。

### 验证结果

| 检查项 | 状态 |
|--------|:--:|
| `patrol_control` 旧订阅类型已核对 | ✅ |
| 旧 `yolov5_detect` 对 `bridge` 的支持已核对 | ✅ |
| `uav_vision` Phase B/Phase C 代码与计划偏差已核对 | ✅ |
| Windows 侧计划文件已更新 | ✅ |

### 遗留问题

1. 本次只做 review 与计划校正，未修改 Phase B / Phase C 代码本身。
2. `uav_vision/README.md` 仍待单独修订。
3. `detect_compat_bridge`、`target_memory`、`drop_aligner` 的修复仍待执行方落实。

### 下一步

1. 先修 `detect_compat_bridge` 的旧话题消息类型。
2. 再修 `target_memory` 的 `confirm_frames`、冷却和 `priority <= 0` 过滤逻辑。
3. 最后修 `drop_aligner` 的输入门控，并更新 `uav_vision/README.md`。

---

## 2026-06-05（Review 修复落地）

### 改动范围

- `scripts/detect_compat_bridge.py`：旧话题消息类型对齐
- `scripts/target_memory.py`：3 处逻辑修复
- `scripts/drop_aligner.py`：输入门控
- `vision_ws/README.md`：同步当前状态

### 已修复

1. **`detect_compat_bridge` 消息类型对齐**
   - `/yolo_detect`：`TargetDetectionArray` → `std_msgs::String`（单个类别名，与 `patrol_control` 的 `ClassCallback` 中 `msg.data == goal[n]` 比较逻辑一致）
   - `/detect/tank_status`：`std_msgs::Bool` → `geometry_msgs::PoseStamped`（与 `TankStatusCallback` 中 `msg.pose.position.x/y` 读法一致）
   - `/detect/cross_mark_point`、`/detect/waypoint_mark_point`：保持 `PoseStamped`（本就正确）
   - `/detect/cross_status`：保持 `std_msgs::Bool`（本就正确）

2. **`target_memory` confirm_frames 参数化**
   - `CandidateRecord._advance_state()` 原先写死 `>= 3` → `>= confirm_frames`
   - `CandidateRecord.update()` 现在接收并透传 `confirm_frames` 参数

3. **`target_memory` CONFIRMED 冷却修复**
   - 原逻辑：`cand.age()` 先将 `state` 改为 `EXPIRED`，之后 `cand.state == ST_CONFIRMED` 永远为 `False`
   - 修复：在 `age()` 调用前保存 `was_confirmed`，以此判断是否加入拒绝冷却

4. **`target_memory` priority ≤ 0 过滤**
   - `selected_target` 选择逻辑增加 `self._priority.get(t.class_name, 0) > 0` 条件
   - `bridge`（权重=0）确认后不再进入 `selected_target`

5. **`drop_aligner` 输入门控**
   - 原先直接使用 `msg.targets[0]`（可能为 DETECTED/OBSERVING 目标）
   - 修复：遍历找到第一个 `state >= 2`（CONFIRMED）目标；无则输出 `"no confirmed target"`

### 验证结果

| 检查项 | 状态 |
|--------|:--:|
| `vision_ws` 编译通过 | ✅ |
| 3 脚本 Python 语法有效 | ✅ |
| 14 项 review 修复验证通过 | ✅ |
| `/yolo_detect` → String | ✅ |
| `/detect/tank_status` → PoseStamped | ✅ |
| confirm_frames 参数化 | ✅ |
| CONFIRMED 冷却逻辑修复 | ✅ |
| priority ≤ 0 过滤 | ✅ |
| drop_aligner state ≥ 2 门控 | ✅ |
| `vision_ws/README.md` 已更新 | ✅ |

### 遗留问题

1. `patrol_control` 侧搜索中断状态机仍未实现（属于 `patrol_uav_ws-patrol_planner`）。
2. 端到端联调需要 rosbag 回放或仿真图像源。

### 下一步

1. Phase D：板端 RKNN 主路径（`target_detector_rknn`）+ `/detect/land_mark_point` 迁移。
2. 或进入 `patrol_control` 侧实现视觉中断状态机。

---

## 2026-06-05（Phase D 功能补全）

### 改动范围

- `scripts/target_detector.py`：新增标准 5 类 YOLO 检测节点（dev/sim 路径）
- `src/landing_detector_node.cpp` + `include/uav_vision/landing_detector_node.h`：新增降落标识检测节点
- `scripts/detect_compat_bridge.py`：新增 `/detect/land_mark_point` 兼容输出
- `config/landing_detector.yaml` + `config/target_detector.yaml`：新增配置
- `config/target_memory.yaml`：新增 landing_pad、circle 权重
- `launch/phase_d.launch`：全功能启动文件（7 节点）
- `CMakeLists.txt`：注册新二进制和脚本

### 已完成改动

1. **`target_detector.py`** — 标准 5 类 YOLO 检测（dev/sim 路径）
   - 使用训练好的 `liftrace_5cls_v1/weights/best.pt` 模型
   - 支持类别：bridge, panzer, pillbox, tent, tank
   - 订阅相机图像，发布 `/uav_vision/detections`（`TargetDetectionArray`）
   - 参数化：model_path、conf_threshold、imgsz
   - **注：板端实机禁止启动此节点，须替换为 RKNN 版本**

2. **`landing_detector_node`** (C++) — 降落区外圈检测
   - 算法：灰度 → GaussianBlur → adaptiveThreshold(GAUSSIAN_C, INV, block=31, C=10) → 形态学(open+close) → findContours → fitEllipse → 质量筛选
   - 输出：`/uav_vision/detections`（class_name="landing_pad"）
   - 完整参数化：自适应阈值参数、形态学核大小、椭圆质量阈值
   - 与旧 `landing_detector_node.cpp` 同算法，剥离了 TF/世界投影

3. **`detect_compat_bridge`** 补全 — 新增 `/detect/land_mark_point`
   - 从 detections 中识别 class_name="landing_pad" → 转发 PoseStamped
   - 至此，6 个旧话题全部覆盖：
     - `/yolo_detect` (String)
     - `/detect/waypoint_mark_point` (PoseStamped)
     - `/detect/tank_status` (PoseStamped)
     - `/detect/cross_mark_point` (PoseStamped)
     - `/detect/cross_status` (Bool)
     - `/detect/land_mark_point` (PoseStamped) ← 本次新增

4. **`phase_d.launch`** — 全功能 7 节点启动
   ```
   target_detector       → /uav_vision/detections (bridge/panzer/pillbox/tent/tank)
   cross_detector_node   → /uav_vision/detections (red_cross)
   circle_detector_node  → /uav_vision/detections (circle)
   landing_detector_node → /uav_vision/detections (landing_pad)
   target_memory         → /uav_vision/targets, /uav_vision/selected_target
   drop_aligner          → /uav_vision/drop_offset, /uav_vision/drop_ready
   detect_compat_bridge  → 6 个旧话题
   ```

5. **target_memory 权重更新**
   - 新增 `priority_landing_pad: 0.0`、`priority_circle: 0.0`
   - 降落标识和圆环精修结果不参与自动目标选择，由 patrol_control 按状态机直接使用

### 验证结果

| 检查项 | 状态 |
|--------|:--:|
| `landing_detector_node` C++ 编译通过 | ✅ |
| `target_detector.py` 语法有效 | ✅ |
| 7 个构建产物存在 | ✅ |
| 7 个 config YAML 可解析 | ✅ |
| 3 个 launch XML 有效（phase_b/c/d） | ✅ |
| 降落检测算法：合成黑圆检出 | ✅ |
| 6 个旧话题全部覆盖 | ✅ |
| YOLO 模型 best.pt 存在 | ✅ |
| 6 ROS 消息可导入 | ✅ |
| 总测试 29/30（1 项为测试框架引号问题） | ✅ |

### 完整数据流（Phase D）

```
相机图像
  ├── target_detector (YOLO) → TargetDetection (bridge/panzer/pillbox/tent/tank)
  ├── cross_detector_node    → TargetDetection (red_cross)
  ├── circle_detector_node   → TargetDetection (circle)
  ├── landing_detector_node  → TargetDetection (landing_pad)
  │                                ↓
  │                    /uav_vision/detections
  │                                ↓
  ├── target_memory        → /uav_vision/targets (按优先级排序)
  │                        → /uav_vision/selected_target (CONFIRMED + priority > 0)
  │                                ↓
  ├── drop_aligner         → /uav_vision/drop_offset (仅 CONFIRMED)
  │                        → /uav_vision/drop_ready
  │                                ↓
  └── detect_compat_bridge → /yolo_detect (String)
                            → /detect/waypoint_mark_point (PoseStamped)
                            → /detect/tank_status (PoseStamped)
                            → /detect/cross_mark_point (PoseStamped)
                            → /detect/cross_status (Bool)
                            → /detect/land_mark_point (PoseStamped)
```

### 遗留问题

1. `target_detector_rknn`（板端 RKNN/NPU 版本）仍未实现，需要 RKNN toolkit + 模型导出。
2. 端到端联调需要 rosbag 或仿真图像源。
3. `patrol_control` 侧搜索中断状态机仍未实现。
4. YOLO `target_detector.py` 仅用于 dev/sim，板端 launch 须禁止。

### 下一步

1. `patrol_control` 侧接入搜索中断状态机。
2. RKNN 模型导出（best.pt → ONNX → RKNN）并在 OrangePi 上测试。
3. 端到端仿真联调（Gazebo + PX4 + phase_d.launch + patrol_control）。

---

## 2026-06-07（兼容层语义收口 + align_mode 接入）

### 改动范围

- `vision_ws/src/uav_vision/scripts/detect_compat_bridge.py`
- `vision_ws/src/uav_vision/scripts/drop_aligner.py`
- `vision_ws/src/uav_vision/config/drop_aligner.yaml`
- `patrol_uav_ws-patrol_planner/src/patrol_control/include/patrol_control/patrol_control.h`
- `patrol_uav_ws-patrol_planner/src/patrol_control/src/patrol_control.cpp`
- `vision_ws/src/uav_vision/README.md`
- `vision_ws/README.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
- `C:\\Users\\ASUS\\.claude\\plans\\elegant-baking-cerf.md`

### 具体改动

1. **`detect_compat_bridge` 语义收口**
   - `/yolo_detect` 只发布标准目标类：`bridge/panzer/pillbox/tent/tank`
   - `red_cross/circle/landing_pad` 不再进入 `/yolo_detect`
   - 当前帧没有有效标准目标时，显式发布 `String(\"Nothing\")`
   - 保留 `/detect/tank_status`、`/detect/cross_*`、`/detect/land_mark_point` 的兼容输出

2. **`drop_aligner` 改为模式驱动**
   - 新增 `/uav_vision/align_mode` 订阅，支持：
     - `disabled`
     - `drop_circle`
     - `drop_cross`
     - `landing`
   - `drop_cross` 优先使用 `/uav_vision/selected_target`
   - 其他模式按目标类别和 `CONFIRMED` 状态筛选候选
   - 无模式或无匹配目标时，不再错误发布对准许可

3. **`patrol_control` 最小接线**
   - 新增 `/uav_vision/align_mode` 发布
   - 按当前飞行/任务状态切换：
     - `Aligning + MAIN_MISSION + Detect_point` → `drop_circle`
     - `Aligning + CROSS_MISSION` → `drop_cross`
     - `Land + flag_landing_detect` → `landing`
     - 其他情况 → `disabled`
   - 修复 `ClassCallback` 里 `goal[]` 越界判断，改为安全遍历
   - 修复 `TankStatusCallback` 把 `y` 坐标误写到 `x` 的问题

4. **文档同步**
   - 重写 `uav_vision/README.md`，同步 Phase D、`align_mode` 和旧接口兼容语义
   - 更新 `vision_ws/README.md` 当前状态和下一步
   - 更新项目执行方案与 Windows 侧执行计划，主线转为：
     - `patrol_control` 消费 `/uav_vision/selected_target`
     - dev/sim 端到端联调
     - RKNN 主路径替换
5. **`target_detector.py` 退化模式**
   - 当 ROS 当前 Python 环境没有 `ultralytics` 时，不再直接抛异常退出
   - `phase_d.launch` 可在“空检测”模式下完整拉起，用于不依赖 YOLO 的链路联调

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `python3 -m py_compile`（4 个 `uav_vision` Python 脚本） | 通过 |
| `vision_ws` `catkin_make -j1` | 通过 |
| `patrol_uav_ws-patrol_planner` `catkin_make -DROS_EDITION=ROS1 -j1 --pkg patrol_control` | 通过 |
| `phase_d.launch` 短时启动 | 通过（`target_detector.py` 在无 `ultralytics` 时退化为空检测） |

### 遗留问题

1. `patrol_control` 仍未消费 `/uav_vision/selected_target`、`/uav_vision/drop_offset`、`/uav_vision/drop_ready`，搜索中断状态机还没真正接上。
2. `bridge` 当前仍维持 `priority=0`，等待样本恢复后再恢复到规则权重 `2`。
3. `target_detector_rknn` 仍未实现，板端主路径仍是待办。
4. 原比赛场景缺失的 `patrol_world.launch` 和 `dibao/qiaoliang/tanke/zhangpeng/zhuangjiache` 模型仍未补拷，但这不阻塞当前视觉主线。

### 下一步

1. 在 `patrol_control` 中正式接入 `/uav_vision/selected_target` 驱动搜索中断。
2. 用 `phase_d.launch + patrol_control` 做一次 dev/sim 端到端联调。
3. 开始 `target_detector_rknn` 的 ONNX/RKNN 导出与板端频率测试。

---

## 2026-06-07（Phase D 主控直连 + board 入口落地）

### 改动范围

- `patrol_uav_ws-patrol_planner/src/patrol_control/include/patrol_control/patrol_control.h`
- `patrol_uav_ws-patrol_planner/src/patrol_control/src/patrol_control.cpp`
- `patrol_uav_ws-patrol_planner/src/patrol_control/CMakeLists.txt`
- `patrol_uav_ws-patrol_planner/src/patrol_control/package.xml`
- `patrol_uav_ws-patrol_planner/src/patrol_control/config/patrol_sim.yaml`
- `vision_ws/src/uav_vision/scripts/detect_compat_bridge.py`
- `vision_ws/src/uav_vision/scripts/target_detector_rknn.py`
- `vision_ws/src/uav_vision/config/target_detector_rknn.yaml`
- `vision_ws/src/uav_vision/launch/phase_d_board.launch`
- `vision_ws/src/uav_vision/CMakeLists.txt`
- `vision_ws/src/uav_vision/README.md`
- `vision_ws/README.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
- `C:\\Users\\ASUS\\.claude\\plans\\elegant-baking-cerf.md`

### 具体改动

1. **`patrol_control` 直接消费新视觉结果**
   - 新增订阅：
     - `/uav_vision/selected_target`
     - `/uav_vision/drop_offset`
     - `/uav_vision/drop_ready`
   - `selected_target` 现阶段用于：
     - 动态更新标准目标 `goal`
     - 触发 `red_cross` 中断
     - 预留 `tank` 中断开关（默认关闭）
   - `drop_offset` 复用了旧 `alignment_control_converter` 的像素闭环思路：
     - 像素偏差 → 米制偏差
     - 再生成小步世界系对准点
     - 分别写入 `waypoint_mark_point` / `cross_mark_point` / `land_mark_point`
   - `drop_ready` 已接入主控，作为圆环/十字/降落对准完成的稳定门控之一

2. **`detect_compat_bridge` 收紧 Pose 兼容**
   - `/yolo_detect` 仍保持标准目标分类兼容
   - `/detect/cross_status` 仍保持存在状态兼容
   - 默认关闭：
     - `/detect/waypoint_mark_point`
     - `/detect/cross_mark_point`
     - `/detect/land_mark_point`
     - `/detect/tank_status`
     的“像素坐标伪装世界点”输出
   - 如需临时复现旧调试链路，需显式设置 `publish_pixel_pose_compat:=true`

3. **板端入口 `target_detector_rknn` 落地**
   - 新增 `target_detector_rknn.py`
   - 新增 `target_detector_rknn.yaml`
   - 新增 `phase_d_board.launch`
   - 入口策略：
     - 优先 unified 5-class RKNN
     - 若缺失则回退到旧 split assets（standard 4cls + tank 1cls）配置
   - 当前 WSL 若缺 `RKNNLite`，自动退化为空检测，不打崩 board launch

4. **构建依赖与参数同步**
   - `patrol_control` 新增 `uav_vision` 依赖
   - `patrol_sim.yaml` 新增：
     - `selected_target_timeout`
     - `drop_offset_timeout`
     - `pixel_to_meter_ratio`
     - `max_movement_distance`
     - `drop_circle/drop_cross/landing_pad` 半径估计
     - `enable_tank_interrupt`

5. **文档与计划同步**
   - `uav_vision/README.md` 和 `vision_ws/README.md` 已同步：
     - `phase_d_board.launch`
     - `target_detector_rknn.py`
     - `patrol_control` 直连 `/uav_vision/*`
     - Pose 兼容默认关闭
   - 项目执行方案与 Windows 计划文件已更新为当前状态

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `python3 -m py_compile`（`detect_compat_bridge.py`、`drop_aligner.py`、`target_detector.py`、`target_detector_rknn.py`） | 通过 |
| `vision_ws` `catkin_make -j1` | 通过 |
| 叠加 `vision_ws/devel` 后编译 `patrol_control` | 通过 |
| `phase_d_board.launch` 短时启动 | 通过（`target_detector_rknn.py` 在缺 `RKNNLite` 时退化为空检测） |

### 遗留问题

1. `selected_target` 目前主要驱动 `red_cross` 中断和标准类目标更新；更完整的 `SEARCH_CRUISE -> RESUME_SEARCH` 状态机仍待补齐。
2. `tank` 的中断开关默认关闭，等待规则与任务策略进一步确认。
3. `target_detector_rknn.py` 当前只是可启动入口，真实 RKNN 推理、后处理和板端频率实测仍需在 OrangePi 5 Plus 上完成。
4. `bridge` 当前仍保持 `priority=0`，待样本恢复后再提升到规则权重 `2`。

### 下一步

1. 用 `phase_d.launch/phase_d_board.launch + patrol_control` 做一次 dev/sim 端到端联调，验证在关闭 Pose 伪兼容后主控仍能稳定对准。
2. 继续完善 `patrol_control` 的搜索恢复策略，而不是只停留在中断入口。
3. 在 OrangePi 5 Plus 上推进 `target_detector_rknn` 的真实 RKNN 推理验证和性能测量。

---

## 2026-06-07（Phase D mock 联调补充 + 初始四元数告警修复）

### 改动范围

- `patrol_uav_ws-patrol_planner/src/patrol_control/src/patrol_control.cpp`

### 具体改动

1. 在 `LLController::initializeNode()` 中将 `uav_pose.pose.orientation.w` 初始化为 `1.0`。
   - 目的不是改变飞行逻辑，而是避免 `cmd_timer` 先于首帧 `/mavros/local_position/pose` 到达时，
     `cmdCallback()` 误把默认零四元数判定为“未单位化”并刷出告警。

2. 补充记录本轮 `phase_d_mock_patrol.launch` 的逻辑联调结论：
   - 使用 mock pose + mock detection 时，已验证链路：
     `detections -> target_memory -> selected_target -> patrol_control interrupt -> align_mode -> drop_aligner -> drop_offset/drop_ready -> patrol_control`
   - 当前仍未覆盖真实 RKNN 推理与完整 `fast_planner` 联调。

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `patrol_control` 代码静态检查（小改动人工复核） | 通过 |
| `phase_d_mock_patrol.launch` 历史联调结论复核 | 保持成立 |
| 初始四元数默认值修正后逻辑影响范围 | 仅启动初期告警收口，不改变正常位姿回调后的行为 |

### 遗留问题

1. `fast_planner_node` 的历史 `boost::lock_error` 仍未解决，完整内部仿真联调仍受其阻塞。
2. `target_detector_rknn.py` 仍是 board 入口与空检测退化路径，真实 OrangePi 5 Plus RKNN 推理未验证。
3. `bridge` 当前仍保持 `priority=0`，等待样本恢复后再恢复规则权重。

### 下一步

1. 继续用 `phase_d_mock_patrol.launch` 或外部仿真图像源验证 `selected_target/drop_offset/drop_ready` 与主控搜索中断逻辑。
2. 在 OrangePi 5 Plus 上补做 `target_detector_rknn` 的真实推理与频率测试。
3. 独立回合继续处理 `fast_planner_node` 崩溃，再推动完整内部仿真链路。

---

## 2026-06-07（初始姿态全量初始化验证）

### 改动范围

- `patrol_uav_ws-patrol_planner/src/patrol_control/src/patrol_control.cpp`

### 具体改动

1. 将 `initializeNode()` 中的 `uav_pose` 初始值从“仅设置 `orientation.w=1.0`”收口为显式全量初始化：
   - 位置 `x/y/z = 0`
   - 姿态 `x/y/z = 0, w = 1`

2. 目的：
   - 消除 `cmd_timer` 在首帧 `/mavros/local_position/pose` 到达前读取未定义姿态分量导致的
     `[WARN]: The quaternion of the drone position has not been unitized`
   - 不改变正常位姿回调后的控制逻辑

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| 重新编译 `patrol_control` | 通过 |
| `phase_d_mock_patrol.launch` 启动前 3 秒检查 | 通过，初始四元数告警已消失 |
| mock 逻辑链路 | 继续可用，`selected_target -> align_mode -> drop_offset/drop_ready -> CrossDetectionDone` 仍正常推进 |

### 遗留问题

1. `fast_planner_node` 的历史 `boost::lock_error` 仍未解决。
2. `target_detector_rknn.py` 仍待 OrangePi 5 Plus 上做真实 RKNN 推理验证。

### 下一步

1. 继续把 `phase_d_mock_patrol.launch` 作为 `uav_vision + patrol_control` 的最小逻辑回归入口。
2. 在 OrangePi 5 Plus 上补板端检测与频率测试。

---

## 2026-06-09（Phase D 软件回归入口补齐）

### 改动范围

- `vision_ws/src/uav_vision/scripts/mock_align_mode_publisher.py`
- `vision_ws/src/uav_vision/scripts/phase_d_assertion.py`
- `vision_ws/src/uav_vision/scripts/drop_aligner.py`
- `vision_ws/src/uav_vision/CMakeLists.txt`
- `vision_ws/src/uav_vision/launch/phase_d_mode_mock.launch`
- `vision_ws/src/uav_vision/launch/phase_d_mock_patrol_regression.launch`
- `vision_ws/src/uav_vision/README.md`
- `vision_ws/README.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`

### 具体改动

1. 新增 `mock_align_mode_publisher.py`
   - 固定发布 `/uav_vision/align_mode`
   - 用于独立验证 `drop_circle`、`landing`、`disabled` 等模式

2. 新增 `phase_d_assertion.py`
   - 订阅：
     - `/uav_vision/targets`
     - `/uav_vision/selected_target`
     - `/uav_vision/align_mode`
     - `/uav_vision/drop_ready`
     - `/yolo_detect`
     - `/detect/cross_status`
   - 按参数等待预期事件，成功 0 退出，超时 1 退出

3. 新增 mock 回归 launch
   - `phase_d_mode_mock.launch`
     - 覆盖标准目标、圆环和降落精修链
   - `phase_d_mock_patrol_regression.launch`
     - 覆盖 `red_cross -> patrol_control interrupt -> drop_cross -> drop_ready`

4. 修复 `drop_aligner.py` 初始化竞态
   - 将内部状态和 publisher 初始化提前
   - 避免 `/uav_vision/align_mode` 回调先于 `_align_mode` 成员创建导致 `AttributeError`

5. 同步文档
   - `uav_vision/README.md`
   - `vision_ws/README.md`
   - `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
   已补入 Phase D 软件回归入口和当前验证结论

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `python3 -m py_compile`（新增/相关 Python 脚本） | 通过 |
| `vision_ws` `catkin_make -j1` | 通过 |
| `phase_d_mode_mock.launch`（`class_name=panzer`） | 通过：`selected_target=panzer`、`/yolo_detect=panzer`、`drop_ready=false` |
| `phase_d_mode_mock.launch`（`class_name=circle`） | 通过：`align_mode=drop_circle`、`drop_ready=true` |
| `phase_d_mode_mock.launch`（`class_name=landing_pad`） | 通过：`align_mode=landing`、`drop_ready=true` |
| `phase_d_mock_patrol_regression.launch` | 通过：`red_cross -> selected_target -> patrol_control interrupt -> drop_cross -> drop_ready=true` |

### 遗留问题

1. 当前完成的是 **Phase D 软件链路回归**，不等价于 OrangePi 5 Plus 上的真实 RKNN 推理和时延验证。
2. `target_detector_rknn.py` 仍缺板端真实模型吞吐与后处理验证。
3. `fast_planner_node` 历史 `boost::lock_error` 仍未解决，完整内部仿真联调尚未恢复。

### 下一步

1. 在 OrangePi 5 Plus 上继续推进 `target_detector_rknn.py` 的真实推理验证。
2. 把 `phase_d_mode_mock.launch` 和 `phase_d_mock_patrol_regression.launch` 作为后续改动的固定回归入口。
3. 单独回合继续修 `fast_planner_node`，恢复完整内部仿真链路。

---

## 2026-06-09（`target_detector_rknn.py` 真实推理代码路径补齐）

### 改动范围

- `vision_ws/src/uav_vision/scripts/target_detector_rknn.py`
- `vision_ws/src/uav_vision/config/target_detector_rknn.yaml`
- `vision_ws/src/uav_vision/README.md`
- `vision_ws/README.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`

### 具体改动

1. 将 `target_detector_rknn.py` 从“纯空发布 stub”升级为“有真实推理代码路径、缺依赖时再退化”的实现：
   - 支持 unified `.rknn`
   - 支持 split assets：`best_rknn_model + tank_rknn_model`
   - 补了：
     - `letterbox`
     - 输入布局/颜色空间参数
     - 通用候选张量整理
     - 通用 `[N,4+nc] / [N,5+nc]` 类输出解码
     - box 格式自动判断
     - 缩放回原图
     - 按类别 NMS

2. 扩充 `target_detector_rknn.yaml`
   - 新增：
     - `iou_threshold`
     - `input_layout`
     - `input_color_space`
     - `box_format`

3. 同步文档口径
   - 明确 `target_detector_rknn.py` 已具备真实推理代码路径
   - 同时明确：当前仍缺 OrangePi 5 Plus 上的真实输出与时延证据

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `python3 -m py_compile vision_ws/src/uav_vision/scripts/target_detector_rknn.py` | 通过 |
| `vision_ws` `catkin_make -j1` | 通过 |
| `phase_d_board.launch` 短时启动 | 通过 |
| 当前 WSL 无 `RKNNLite` 时行为 | 按预期退化为空检测，不崩 launch |

### 遗留问题

1. 当前环境仍无法验证真实 RKNN 输出张量格式；通用解码路径需要 OrangePi 5 Plus 上实测确认。
2. `target_detector_rknn.py` 虽已不再是空壳，但还不能把“板端真实推理已完成”当作既成事实。
3. 外部 Windows 计划文件 `C:\\Users\\ASUS\\.claude\\plans\\elegant-baking-cerf.md` 当前未能通过仓库内文件编辑接口同步，仓库文档已先保持一致。

### 下一步

1. 在 OrangePi 5 Plus 上实际运行 `phase_d_board.launch` 或等价板端入口，确认 RKNN 输出格式与检测结果。
2. 记录板端频率、延迟、CPU 占用和队列积压情况。
3. 若实测输出张量格式与当前通用解码不一致，再按板端日志定向修正后处理。

---

## 2026-06-09（文档口径纠正与新旧视觉链对比沉淀）

### 改动范围

- `README.md`
- `VISION_CHAIN_COMPARISON.md`

### 具体改动

1. 更新项目总览 `README.md`
   - 把“`uav_vision` 仍是空骨架包”的旧表述纠正为当前真实状态
   - 补充：
     - Phase D 软件回归入口已具备
     - `phase_d_board.launch` 当前可起但在 WSL 中会因缺 `RKNNLite` 退化为空检测
     - `patrol_control` 已开始直接消费 `/uav_vision/*`

2. 新增 `VISION_CHAIN_COMPARISON.md`
   - 系统梳理旧视觉链：
     - 标准目标分类链
     - `tank` 专项链
     - `red_cross` 传统视觉链
     - 圆环精修链
     - 降落标识链
   - 系统梳理新视觉链：
     - 统一检测层
     - `target_memory`
     - `drop_aligner`
     - `patrol_control` 新接口消费
   - 明确当前 Phase D 的完成边界：
     - 软件链已打通
     - 板端真实推理证据仍待补齐

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `README.md` 当前状态与 `uav_vision/README.md`、`vision_ws/README.md` 口径交叉复核 | 已统一 |
| `VISION_CHAIN_COMPARISON.md` 与当前代码结构交叉复核 | 已落地并与现状一致 |

### 遗留问题

1. Windows 外部计划文件 `C:\\Users\\ASUS\\.claude\\plans\\elegant-baking-cerf.md` 仍未通过当前仓库文件编辑接口同步。
2. 板端真实 RKNN 推理和 `fast_planner_node` 历史问题仍是后续主缺口。

### 下一步

1. 继续以 `VISION_CHAIN_COMPARISON.md` 作为后续汇报与评审基线。
2. 在 OrangePi 5 Plus 上补齐板端真实推理证据后，再决定是否把 Phase D 视为完全完成。

---

## 2026-06-09（Phase D perf 诊断与 board perf mock 回归补齐）

### 改动范围

- `vision_ws/src/uav_vision/package.xml`
- `vision_ws/src/uav_vision/CMakeLists.txt`
- `vision_ws/src/uav_vision/config/target_detector.yaml`
- `vision_ws/src/uav_vision/config/target_detector_rknn.yaml`
- `vision_ws/src/uav_vision/scripts/target_detector.py`
- `vision_ws/src/uav_vision/scripts/target_detector_rknn.py`
- `vision_ws/src/uav_vision/scripts/phase_d_assertion.py`
- `vision_ws/src/uav_vision/scripts/mock_image_publisher.py`
- `vision_ws/src/uav_vision/launch/phase_d_board_perf_mock.launch`
- `docs/perf_orangepi5plus.md`
- `vision_ws/src/uav_vision/README.md`
- `vision_ws/README.md`
- `README.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
- `VISION_CHAIN_COMPARISON.md`

### 具体改动

1. 给 detector 节点补齐 `/uav_vision/perf`
   - `target_detector.py` 新增 `DiagnosticArray` 发布
   - `target_detector_rknn.py` 新增 `DiagnosticArray` 发布
   - 输出字段包含 `backend`、`frames`、`detections`、`processing_ms`、`inference_ms`、`fps_ema`

2. 补齐 perf 相关依赖与参数
   - `uav_vision` 包新增 `diagnostic_msgs` 依赖
   - `target_detector.yaml`、`target_detector_rknn.yaml` 新增 `perf_topic`

3. 新增 board perf mock 回归入口
   - 新增 `mock_image_publisher.py`
   - 新增 `phase_d_board_perf_mock.launch`
   - `phase_d_assertion.py` 新增 `/uav_vision/perf` 检查能力

4. 新增板端性能记录模板
   - `docs/perf_orangepi5plus.md`

5. 同步更新文档
   - 说明 `/uav_vision/perf` 已可用
   - 说明 `phase_d_board_perf_mock.launch` 已成为正式软件回归入口之一

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `python3 -m py_compile`：`target_detector.py`、`target_detector_rknn.py`、`mock_image_publisher.py`、`phase_d_assertion.py` | 通过 |
| `vision_ws` `catkin_make -j1` | 通过 |
| `roslaunch uav_vision phase_d_board_perf_mock.launch` | 通过 |
| perf 断言 | 通过，观察到 `uav_vision/target_detector_rknn` 和 backend=`empty_no_rknnlite` |

### 遗留问题

1. 当前验证的仍是本机软件路径；真实 OrangePi 5 Plus 上的 RKNN 推理吞吐、延迟和资源占用尚未取得。
2. 从当前机器到 `192.168.3.15:22` 的 SSH 仍超时，暂未拿到板端实测证据。

### 下一步

1. 板端网络恢复后，在 OrangePi 5 Plus 上实际运行 `phase_d_board.launch`。
2. 使用 `docs/perf_orangepi5plus.md` 记录真实 `/uav_vision/perf`、CPU、内存与队列表现。
3. 若板端输出张量格式与当前 generic decode 不一致，再按实测结果定向修正 `target_detector_rknn.py`。

---

## 2026-06-09（target_memory 初始化竞态修复）

### 改动范围

- `vision_ws/src/uav_vision/scripts/target_memory.py`

### 具体改动

1. 调整 `TargetMemory.__init__` 内的初始化顺序
   - 先读取阈值、优先级和内部状态
   - 再订阅 `/uav_vision/detections`

2. 目的
   - 避免高频 mock 检测源在节点刚启动时抢先进入 `_on_detections`
   - 避免 `red_cross` 路径访问 `_cross_conf/_cross_geom` 时成员尚未初始化

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `python3 -m py_compile target_memory.py` | 通过 |
| `roslaunch uav_vision phase_d_mock_patrol_regression.launch` | 通过 |
| `roslaunch uav_vision phase_d_board_perf_mock.launch` | 通过 |

### 遗留问题

1. `Phase D` 的软件链路当前已具备可重复回归入口，但真实 OrangePi 5 Plus 上的 RKNN 推理证据仍未补齐。
2. 当前机器到 `192.168.3.15:22` 的网络仍超时。

### 下一步

1. 保持顺序启动回归，避免多个 `roslaunch` 争抢 `11311` 导致假失败。
2. 在板端网络恢复后继续补 OrangePi 实测证据。

---

## 2026-06-24（桥梁/坦克视频增量数据集与五分类 v2 重训）

### 改动范围

- `vision_ws/scripts/extract_video_frames.py`
- `vision_ws/scripts/auto_label.py`
- `vision_ws/scripts/train_5cls.py`
- `vision_ws/test_data/video_sources/bridge_20260624`
- `vision_ws/test_data/video_sources/tank_20260624`
- `vision_ws/test_data/yolo_dataset_v2_video_20260624`
- `vision_ws/runs/liftrace_5cls_v2_video_20260624`
- `VISION_DATASET_AUTO_LABEL_PLAN.md`
- `vision_ws/README.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`

### 具体改动

1. **新增视频抽帧脚本**
   - 新增 `extract_video_frames.py`
   - 支持固定 stride 抽帧、`manifest.csv` 生成、统一命名和输出目录清理

2. **扩展统一自动标注脚本**
   - `auto_label.py` 从原来的双源流程扩成四源流程：
     - `image`
     - `rotated`
     - `bridge_video`
     - `tank_video`
   - 旧静态数据沿用现有 `yolo_dataset` 的 train/val 归属
   - 新视频帧按时间连续 `80/20` 切分
   - `bridge_video` 只保留 `bridge`
   - `tank_video` 只保留 `tank`
   - 输出新增 `stats.json`

3. **新增统一五分类训练脚本**
   - 新增 `train_5cls.py`
   - 固化 `liftrace_5cls_v1` 的主要训练配方：
     - `yolo11n.pt`
     - `epochs=100`
     - `batch=16`
     - `imgsz=640`
     - `device=0`

4. **实际执行视频制备与自动标注**
   - `bridge.mp4`：
     - 原始帧数 `1077`
     - `stride=15`
     - 抽帧 `72` 张
   - `tank.mp4`：
     - 原始帧数 `1308`
     - `stride=15`
     - 抽帧 `88` 张
   - 生成来源目录：
     - `vision_ws/test_data/video_sources/bridge_20260624`
     - `vision_ws/test_data/video_sources/tank_20260624`

5. **生成新数据集并完成重训**
   - 数据集：`vision_ws/test_data/yolo_dataset_v2_video_20260624`
   - 训练运行：`vision_ws/runs/liftrace_5cls_v2_video_20260624`
   - `bridge_video` 最终采用 `conf=0.10`，以提升桥梁视频利用率

6. **同步更新文档**
   - 数据方案文档增加视频增量数据、`v2` 数据集和 `v2` 训练结果
   - `vision_ws/README.md` 增补新数据源、脚本和训练产物
   - 视觉执行计划中把“bridge 零样本”更新为“bridge 已完成首轮补样”

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `python3 -m py_compile`：`extract_video_frames.py`、`auto_label.py`、`train_5cls.py` | 通过 |
| `bridge.mp4` 抽帧 | 通过，72 张 |
| `tank.mp4` 抽帧 | 通过，88 张 |
| `auto_label.py` 四源自动标注 | 通过 |
| 新数据集 `train/val` | `847 / 213` |
| 新数据集总框数 | `bridge=17`、`panzer=304`、`pillbox=253`、`tent=283`、`tank=147` |
| `bridge` 是否脱离零样本 | 是 |
| `train_5cls.py` 重训 | 通过 |
| 最佳模型 | `vision_ws/runs/liftrace_5cls_v2_video_20260624/weights/best.pt` |
| 训练结束方式 | `epoch 71` 早停，最佳 `epoch 51` |
| 最终验证 | `all mAP50=0.995`，`bridge P=0.612 R=1.000 mAP50=0.995` |

### 遗留问题

1. `bridge` 虽已补到 `17` 个框，但样本量仍明显少于 `panzer/pillbox/tent/tank`，后续仍应优先继续补桥梁样本。
2. `image` 源目录里仍有 1 张未进入旧 split 的额外文件（调试 PNG），本轮已自动跳过，不影响训练，但后续应继续保持测试图和训练源分离。
3. 当前验证仍是“教师模型伪标签 + 同域验证集”闭环，不代表对新场景泛化已经充分证明。

### 下一步

1. 抽检 `bridge_video` 的 17 个标注样本，确认没有明显偏框或误检。
2. 若后续继续拿到桥梁视频或图片，优先按同一目录规范补入 `video_sources/` 并滚动重训 `v3`。
3. 在视觉主线中继续推进 `uav_vision + patrol_control` 联调，不让数据整理反向阻塞搜索/投递闭环开发。

---

## 2026-06-24（新版五分类数据集分发包）

### 改动范围

- `vision_ws/test_data/dist/liftrace_5cls_dataset_v2_video_20260624_release.zip`
- `vision_ws/test_data/dist/liftrace_5cls_dataset_v2_video_20260624_release.zip.sha256`

### 具体改动

1. 生成新版五分类数据集的精简分发包：
   - 只包含以下发布所需内容：
     - `images/train`
     - `images/val`
     - `labels/train`
     - `labels/val`
     - `data.yaml`
     - `stats.json`
2. 输出位置：
   - `vision_ws/test_data/dist/liftrace_5cls_dataset_v2_video_20260624_release.zip`
3. 生成对应校验文件：
   - `vision_ws/test_data/dist/liftrace_5cls_dataset_v2_video_20260624_release.zip.sha256`

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| 压缩包生成 | 通过 |
| 压缩包大小 | `702M` |
| 包内结构 | 包含 `images/train`、`images/val`、`labels/train`、`labels/val`、`data.yaml`、`stats.json` |
| SHA256 | `13399aa2345bd67808d829d8725ed5ee0cd465096661bf708dfcb4f50b34a782` |

### 遗留问题

1. 当前分发包不包含 `overlays/` 和 `video_sources/`，适合训练与复现，不适合作为完整原始采样备份。
2. 若后续继续增量补样并训练 `v3`，需要重新生成新的分发包与校验值。

### 下一步

1. 直接把 `zip` 与 `.sha256` 一起发给组员。
2. 对方解压后以 `data.yaml` 为入口即可直接训练或验证。

---

## 2026-07-03（bridge-manual 人工桥梁真值并入五分类 v3）

### 改动范围

- `vision_ws/scripts/auto_label.py`
- `vision_ws/test_data/yolo_dataset_v3_bridge_manual_20260703`
- `vision_ws/runs/liftrace_5cls_v3_bridge_manual_20260703`
- `VISION_DATASET_AUTO_LABEL_PLAN.md`
- `vision_ws/README.md`

### 具体改动

1. **扩展统一数据构建脚本**
   - `auto_label.py` 新增手工桥梁数据源支持：
     - `--input-bridge-manual-images`
     - `--input-bridge-manual-labels`
   - 新增 `manual_bridge` 导入逻辑：
     - 读取手工 YOLO 标签
     - 校验字段数、类别编号和归一化范围
     - 将单类 `bridge` 映射回统一五分类的 `class_id=0`
     - 生成 overlay 和 `stats.json`

2. **确认 `bridge-manual` 当前格式**
   - 目录仅有：
     - `train/images`
     - `train/labels`
   - 自带 `data.yaml` 引用的 `valid/test` 目录并不存在，不能原样直接训练
   - 标签内容确认为 YOLO 单类 `bridge` 格式，可作为人工真值源使用

3. **生成新的统一五分类数据集**
   - 数据集路径：
     - `vision_ws/test_data/yolo_dataset_v3_bridge_manual_20260703`
   - 数据组成：
     - `image`：沿用旧静态四类 split
     - `rotated`：沿用旧 tank split
     - `bridge_manual`：72 张人工桥梁图，按时间 `80/20` 切分
     - `tank_video`：沿用视频增量 tank 数据
   - 不再使用 `bridge_video` 教师伪标签源作为桥梁主来源

4. **完成五分类 v3 重训**
   - 训练运行：
     - `vision_ws/runs/liftrace_5cls_v3_bridge_manual_20260703`
   - 训练脚本：
     - `vision_ws/scripts/train_5cls.py`

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `bridge-manual` 目录结构检查 | 通过，仅 `train/images` 与 `train/labels` 有效 |
| `auto_label.py` 语法检查 | 通过 |
| `bridge_manual` 导入 | 通过，72 张图、72 个 `bridge` 框、0 空标签 |
| `v3` 数据集 `train/val` | `847 / 213` |
| `v3` 总框数 | `bridge=72`、`panzer=304`、`pillbox=253`、`tent=283`、`tank=147` |
| `train_5cls.py` 重训 | 通过 |
| 最佳模型 | `vision_ws/runs/liftrace_5cls_v3_bridge_manual_20260703/weights/best.pt` |
| 训练结束方式 | 完整跑满 `100 epochs` |
| 最终验证 | `all P=0.997 R=1.000 mAP50=0.995` |
| `bridge` 最终验证 | `P=0.997 R=1.000 mAP50=0.995` |

### 遗留问题

1. 当前 `bridge-manual` 只有人工桥梁单类，后续如果继续做人审，仍建议保持“单类真值源 + 统一五分类合并”的模式，而不是手改主数据集。
2. `bridge-manual` 原始导出文件名带 `.rf.*` 后缀，适合当前独立来源管理，但后续若需要跨版本去重，要以时间帧信息而不是文件名 hash 识别。
3. 当前验证仍以同域数据为主，泛化能力仍需靠后续新增桥梁场景继续验证。

### 下一步

1. 以后桥梁新样本优先继续进入 `bridge-manual` 路线，而不是再回到教师伪标签桥梁源。
2. 若需要对外分发新版训练集，优先以 `yolo_dataset_v3_bridge_manual_20260703` 为基线重新打包。

---

## 2026-07-03（v3 数据集打包与 real_target.mp4 五分类实拍推理）

### 改动范围

- `vision_ws/test_data/dist/`
- `vision_ws/scripts/infer_video_report.py`
- `vision_ws/test_data/real_target_infer_v3_20260703/`

### 具体改动

1. **打包 v3 五分类训练集**
   - 基于 `vision_ws/test_data/yolo_dataset_v3_bridge_manual_20260703` 生成可分发压缩包：
     - `vision_ws/test_data/dist/liftrace_5cls_dataset_v3_bridge_manual_20260703_release.zip`
     - `vision_ws/test_data/dist/liftrace_5cls_dataset_v3_bridge_manual_20260703_release.zip.sha256`
   - 打包内容仅保留可训练最小集：
     - `images/train`
     - `images/val`
     - `labels/train`
     - `labels/val`
     - `data.yaml`
     - `stats.json`

2. **新增长视频推理记录脚本**
   - 新增 `vision_ws/scripts/infer_video_report.py`
   - 功能：
     - 对视频逐帧运行 YOLO 推理
     - 输出带框可视化视频
     - 输出逐检测 CSV
     - 输出逐帧统计 CSV
     - 输出汇总 JSON
     - 自动抽取周期样本帧和事件样本帧，便于人工核验

3. **完成 `real_target.mp4` 五分类实拍推理**
   - 输入视频：
     - `vision_ws/test_data/real_target.mp4`
   - 使用模型：
     - `vision_ws/runs/liftrace_5cls_v3_bridge_manual_20260703/weights/best.pt`
   - 输出目录：
     - `vision_ws/test_data/real_target_infer_v3_20260703/`
   - 主要输出文件：
     - `annotated.mp4`
     - `detections_long.csv`
     - `frame_summary.csv`
     - `summary.json`
     - `samples/`

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `v3` 数据集压缩包生成 | 通过 |
| 压缩包大小 | 约 `702M` |
| 压缩包 SHA256 | `c5c8df315d51414b444b76287b9eb6a0f72c98f0ef1c561ab8647bd18125a54e` |
| `infer_video_report.py` 运行 | 通过 |
| `real_target.mp4` 总帧数 | `4623` |
| 视频分辨率 | `1080x2560` |
| 视频帧率 | `59.985 FPS` |
| 检出帧数 | `3435` |
| 检出帧占比 | `0.743` |
| 类别计数 | `bridge=1021`、`panzer=1177`、`pillbox=537`、`tank=1251` |
| 输出样本图数量 | `103` |

### 遗留问题

1. 当前五分类模型只覆盖：
   - `bridge`
   - `panzer`
   - `pillbox`
   - `tent`
   - `tank`
2. `real_target.mp4` 中若包含 `H` 字降落标识或 `red_cross`，本次五分类推理不会将其检出；该限制已写入 `summary.json` 的 `notes` 字段。
3. 本次结果能够作为“标准目标实拍场景的五分类检测记录”，但不能替代 `cross_detector_node` / `landing_detector_node` 对 `red_cross` 和 `H` 的专项验证。

### 下一步

1. 如需给其他组员同步完整评测材料，可追加打包 `vision_ws/test_data/real_target_infer_v3_20260703/`。
2. 若要验证 `H` 和 `red_cross` 实拍效果，应单独调用传统视觉链或对应离线等效脚本做专项记录。

---

## 2026-07-06（文档收口、v3 数据集统计复核、`real_target.mp4` 完整视觉链离线评测）

### 改动范围

- `README.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
- `VISION_DATASET_AUTO_LABEL_PLAN.md`
- `vision_ws/README.md`
- `vision_ws/src/uav_vision/README.md`
- `docs/REAL_TARGET_FULL_CHAIN_EVAL_20260706.md`
- `vision_ws/scripts/eval_full_vision_chain_video.py`
- `vision_ws/test_data/real_target_full_chain_v3_20260706/`

### 具体改动

1. **补齐完整视觉链离线评测脚本**
   - 新增 `vision_ws/scripts/eval_full_vision_chain_video.py`
   - 功能：
     - 运行五分类 YOLO
     - 运行 `cross_detector` 等效传统视觉逻辑
     - 运行 `landing_detector` 等效传统视觉逻辑
     - 生成统一的可视化视频、逐检测 CSV、逐帧 CSV、汇总 JSON、样本帧

2. **完成 `real_target.mp4` 完整视觉链评测**
   - 输入：
     - `vision_ws/test_data/real_target.mp4`
   - 模型：
     - `vision_ws/runs/liftrace_5cls_v3_bridge_manual_20260703/weights/best.pt`
   - 输出目录：
     - `vision_ws/test_data/real_target_full_chain_v3_20260706/`
   - 关键输出：
     - `annotated_full_chain.mp4`
     - `detections_long.csv`
     - `frame_summary.csv`
     - `summary.json`
     - `samples/`

3. **复核 `v3` 数据集真实类别统计**
   - 直接按标签文件再次计数，确认：
     - `bridge=72`
     - `panzer=304`
     - `pillbox=253`
     - `tent=283`
     - `tank=147`
   - 与 `yolo_dataset_v3_bridge_manual_20260703/stats.json` 一致

4. **统一更新主文档**
   - 将 `README.md`、视觉执行方案、`vision_ws` 说明、`uav_vision` 说明与数据方案文档统一到 `2026-07-06` 状态
   - 新增专项评测文档：
     - `docs/REAL_TARGET_FULL_CHAIN_EVAL_20260706.md`

### 验证结果

| 检查项 | 结果 |
| --- | --- |
| `eval_full_vision_chain_video.py` 语法检查 | 通过 |
| `real_target.mp4` smoke test | 通过 |
| `real_target.mp4` 全量评测 | 通过 |
| `v3` 标签真值计数复核 | 通过 |

完整视觉链全量评测关键统计：

- `total_frames=4623`
- `processed_frames=4622`
- `frames_with_any_detection=3750`
- 五分类 YOLO 检测计数：
  - `bridge=1021`
  - `panzer=1177`
  - `pillbox=537`
  - `tank=1251`
- 传统视觉链触发计数：
  - `red_cross_frames=1694`
  - `landing_pad_frames=1442`
- 冲突统计：
  - `bridge_with_red_cross_frames=756`
  - `bridge_with_landing_pad_frames=380`
  - `aux_only_frames=315`

### 遗留问题

1. 当前五分类 YOLO 仍会把部分 `red_cross/H` 场景误报为 `bridge`。
2. 本轮已证明 `cross_detector` 与 `landing_detector` 在实拍中能大量触发，但“是否完全正确”仍应结合 `samples/` 做人工抽检。
3. 这轮评测是离线等效完整视觉链，不直接替代 ROS 实时链路与板端实测。

### 下一步

1. 优先处理 `bridge ↔ red_cross/landing_pad` 的融合与冲突抑制，不再只盯 `bridge` 单类样本扩充。
2. 在 `uav_vision` 里明确规则：
   - `red_cross` 一旦通过几何验证，优先级高于 `bridge`
   - `landing_pad` 不进入标准目标分类语义
3. 后续若做板端实测，应以 `REAL_TARGET_FULL_CHAIN_EVAL_20260706.md` 为对照基线。

---

## 2026-07-06（`red_cross/H` 冲突抑制、旧链对照回归、实时融合补齐）

### 改动范围

- `vision_ws/src/uav_vision/scripts/detection_fusion.py`
- `vision_ws/src/uav_vision/scripts/detect_compat_bridge.py`
- `vision_ws/src/uav_vision/scripts/target_memory.py`
- `vision_ws/src/uav_vision/src/landing_detector_node.cpp`
- `vision_ws/src/uav_vision/config/detection_fusion.yaml`
- `vision_ws/src/uav_vision/launch/phase_b.launch`
- `vision_ws/src/uav_vision/launch/phase_c.launch`
- `vision_ws/src/uav_vision/launch/phase_d.launch`
- `vision_ws/src/uav_vision/launch/phase_d_board.launch`
- `vision_ws/scripts/eval_full_vision_chain_video.py`
- `docs/REAL_TARGET_FULL_CHAIN_EVAL_20260706.md`

### 已完成改动

1. 新增 `detection_fusion.py`，按 `header.stamp` 聚合同一图像帧的多路检测，再在输出 `detections_resolved` 前执行 `bridge ↔ red_cross/landing_pad` 冲突抑制。
2. 将 `phase_b/phase_c/phase_d/phase_d_board` 的下游消费链改为使用 `/uav_vision/detections_resolved`，避免继续在未聚合的原始流上做“同帧抑制”。
3. 给 `target_memory.py` 和 `detect_compat_bridge.py` 增加 `detections_topic` 参数；`target_memory` 也补齐了 `landing_pad/circle` 的显式优先级项。
4. 在离线评测脚本里把 `detect_red_cross()` 对齐到当前主链：
   - strict 判定失败后可走 relaxed scoring
   - 吸收旧 `simple_cross_detect` 的 concave points / extent / center coverage 特征
   - 修复 `detections_long.csv` 中 YOLO 明细重复写入的问题
5. 将 `landing_detector` 的离线等效逻辑和运行时质量评分对齐，并修正半径质量分计算偏轻的问题。
6. 更新 `REAL_TARGET_FULL_CHAIN_EVAL_20260706.md`，补充 15 FPS 对照回归、旧链风格对照和 ROS 烟测结果。

### 验证结果

1. Python 语法检查通过：
   - `vision_ws/scripts/eval_full_vision_chain_video.py`
   - `vision_ws/src/uav_vision/scripts/detection_fusion.py`
   - `vision_ws/src/uav_vision/scripts/target_memory.py`
   - `vision_ws/src/uav_vision/scripts/detect_compat_bridge.py`
2. `vision_ws` 编译通过：
   - `catkin_make -j1`
3. `real_target.mp4` 15 FPS 等效对照回归通过：
   - 当前链：`red_cross_frames=515`，`landing_pad_frames=359`
   - 抑制前：`raw_bridge_with_red_cross_frames=202`，`raw_bridge_with_landing_pad_frames=87`
   - 抑制后：`bridge_with_red_cross_frames=101`，`bridge_with_landing_pad_frames=0`
4. 旧链风格离线对照完成：
   - `legacy_simple + legacy_old` 得到 `red_cross_frames=206`，`landing_pad_frames=133`
   - 说明旧链思路可吸收，但不宜整体回退
5. ROS 烟测通过：
   - 同一 `header.stamp` 下输入 `bridge + red_cross`
   - `detection_fusion.py` 输出 `resolved_classes=red_cross`

### 遗留问题

1. 当前离线对照下仍有 `bridge_with_red_cross_frames=101`，说明剩余误报尚未清零。
2. `landing_pad` 虽已把冲突压到 `0`，但仍需要人工抽检“小圈/残圈/不完整椭圆”的误检质量。
3. 本轮验证完成于 `vision_ws` 开发工作区，尚未在 OrangePi 5 Plus 板端做同链路实测。

### 下一步

1. 从 `/tmp/real_target_full_chain_current_stride4_20260706/samples/` 和 `frame_summary.csv` 中抽查剩余 `bridge ↔ red_cross` 冲突，继续细化 `cross_detector` 或 `bridge` 抑制规则。
2. 在 `phase_d` 或板端回放链路上确认 `detections_resolved`、`/yolo_detect`、`/detect/cross_status` 的实时行为与离线回归一致。
3. 若后续补做 `bridge` 数据集，应以“压剩余红十字误报”为目标抽样，而不是盲目继续堆桥梁正样本。

---

## 2026-07-06（双链标记视频证据补充）

### 改动范围

- `vision_ws/scripts/eval_full_vision_chain_video.py`
- `vision_ws/test_data/real_target_chain_evidence_20260706/README.md`
- `docs/REAL_TARGET_FULL_CHAIN_EVAL_20260706.md`

### 已完成改动

1. 扩充 `eval_full_vision_chain_video.py` 的视频导出能力：
   - 新增 `--output-fps`
   - 顶部叠加 `cross_mode / landing_mode`
   - 对被抑制掉的 `bridge` 画橙色 `SUP bridge` 框，便于人工检查冲突裁决
2. 持久化导出两份人工复核视频证据到 `vision_ws/test_data/real_target_chain_evidence_20260706/`：
   - `current_chain_stride4/annotated_full_chain.mp4`
   - `legacy_chain_stride4/annotated_full_chain.mp4`
3. 新增证据目录说明 `vision_ws/test_data/real_target_chain_evidence_20260706/README.md`，写明两份视频的模式、画面标记含义和关键统计。
4. 更新 `docs/REAL_TARGET_FULL_CHAIN_EVAL_20260706.md`，补充持久化视频证据路径和人工复核说明。

### 验证结果

1. `eval_full_vision_chain_video.py` 语法检查通过。
2. 两份持久化标记视频已生成：
   - `current_chain_stride4/annotated_full_chain.mp4`，约 `368M`
   - `legacy_chain_stride4/annotated_full_chain.mp4`，约 `365M`
3. 当前链持久化视频统计与先前回归一致：
   - `red_cross_frames=515`
   - `landing_pad_frames=359`
   - `bridge_with_red_cross_frames=101`
   - `bridge_with_landing_pad_frames=0`
4. 旧链风格持久化视频统计与先前回归一致：
   - `red_cross_frames=206`
   - `landing_pad_frames=133`
   - `bridge_with_red_cross_frames=1`
   - `bridge_with_landing_pad_frames=0`

### 遗留问题

1. 当前链视频中仍可人工观察到部分 `bridge ↔ red_cross` 残留误报，尚未彻底压清。
2. 双链证据视频体积较大，不适合纳入版本管理，只宜作为本机联调材料。

### 下一步

1. 直接基于 `current_chain_stride4/annotated_full_chain.mp4` 抽查剩余橙框和残留绿框 `bridge` 场景，定位误报模式。
2. 如需向组员同步，可只分发这两份视频加对应 `summary.json`，不必再口头解释模式差异。

---

## 2026-07-06（旧 4+1 检测器证据视频替换）

### 改动范围

- `vision_ws/scripts/eval_full_vision_chain_video.py`
- `vision_ws/test_data/real_target_chain_evidence_20260706/legacy_chain_stride4/`
- `vision_ws/test_data/real_target_chain_evidence_20260706/README.md`
- `docs/REAL_TARGET_FULL_CHAIN_EVAL_20260706.md`

### 已完成改动

1. 在 `eval_full_vision_chain_video.py` 增加标准目标检测器模式：
   - `unified5`：新五分类统一 YOLO
   - `legacy_split`：旧 `best.pt(4 类) + tank.pt(单独坦克)`
2. 保留 `current_chain_stride4` 不变，不重导出新五分类目录。
3. 用旧检测器重导出并替换 `legacy_chain_stride4`：
   - `model=vision_ws/src/yolov5_detect/best.pt`
   - `tank_model=vision_ws/src/yolov5_detect/tank.pt`
   - `cross_mode=legacy_simple`
   - `landing_mode=legacy_old`
4. 修正文档，明确当前双链视频不再是“同一五分类模型下只比较 cross/landing”，而是“新五分类主链 vs 旧 4+1 主链”。

### 验证结果

1. 已确认旧权重存在且类别正确：
   - `vision_ws/src/yolov5_detect/best.pt -> bridge/panzer/pillbox/tent`
   - `vision_ws/src/yolov5_detect/tank.pt -> tank`
2. `eval_full_vision_chain_video.py` 语法检查通过。
3. `legacy_chain_stride4` 已成功替换，关键统计为：
   - `raw_yolo_class_counts`: `bridge=71`, `panzer=285`, `pillbox=342`, `tent=62`, `tank=147`
   - `red_cross_frames=206`
   - `landing_pad_frames=133`
   - `bridge_with_red_cross_frames=16`
   - `bridge_with_landing_pad_frames=0`

### 遗留问题

1. 当前冲突裁决仍是“同帧全局 suppress bridge”，因此可能出现“画面里明明是桥梁却被 SUP” 的过抑制现象。
2. 若后续要进一步修正这类过抑制，应该把规则从“同帧全局”收紧为“与 red_cross/landing_pad 空间邻近或重叠时才 suppress”。

### 下一步

1. 结合新旧两份视频，区分“模型本身把桥识别成别的”与“冲突裁决把桥压掉”的两类问题。
2. 若继续优化 `SUP bridge` 误杀，优先把 frame-level 抑制升级为 ROI / overlap 感知抑制。

---

## 2026-07-06（活文档收口与仿真前置勘误）

### 改动范围

- `README.md`
- `SIMULATION_GUIDE_NOETIC_PX4_GAZEBO_QGC.md`
- `VISION_WORKSPACE_GUIDE.md`
- `VISION_MIGRATION_CHECKLIST.md`
- `VISION_CHAIN_COMPARISON.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
- `VISION_DATASET_AUTO_LABEL_PLAN.md`
- `vision_ws/README.md`
- `vision_ws/src/uav_vision/README.md`
- `docs/REAL_TARGET_FULL_CHAIN_EVAL_20260706.md`
- `vision_ws/test_data/real_target_chain_evidence_20260706/README.md`

### 已完成改动

1. 收口全部活文档，统一到当前真实状态：
   - `uav_vision` 不再描述为空骨架
   - ROI 不再作为今年数据主线
   - 数据与模型基线冻结到当前 `v3`
2. 重写 `VISION_CHAIN_COMPARISON.md`，明确旧链真实入口与当前新链真实入口：
   - 旧链：`vision.sh + detect.launch + circle/cross/landing launch`
   - 新链：`phase_d.launch + detection_fusion + target_memory + drop_aligner + compat_bridge`
3. 统一接口口径：
   - `/uav_vision/detections`
   - `/uav_vision/detections_resolved`
   - `/uav_vision/targets`
   - `/uav_vision/selected_target`
   - `/uav_vision/drop_offset`
   - `/uav_vision/drop_ready`
   - `/uav_vision/align_mode`
4. 在仿真文档中补齐外部 `PX4 + Gazebo` 接入前置清单，明确当前基线是：
   - `AstraDroneOpen + px4 astra_example.launch + iris_mid360`
5. 统一实拍评测和证据目录的说明，明确：
   - 当前链：`detector=unified5`
   - 旧链风格：`detector=legacy_split`
   - `SUP bridge` 当前是 frame-level 全局 suppress

### 验证结果

1. 已用代码与 launch 复核旧链入口：
   - `top_level_scripts/vision.sh`
   - `Visual/src/yolov5_detect/scripts/yolo_detect.py`
   - `patrol_control` 的 `circle/cross/landing` launch
2. 已用当前 `phase_d.launch` 与脚本复核新链接口：
   - `detection_fusion.py -> /uav_vision/detections_resolved`
   - `target_memory.py -> /uav_vision/targets + /selected_target`
   - `drop_aligner.py -> /uav_vision/drop_offset + /drop_ready`
3. 已统一实拍评测说明与证据目录说明中的 detector 模式定义和 `SUP` 含义。

### 遗留问题

1. 当前 `bridge` 仍处于 `priority_bridge=0`，不会进入 `selected_target`。
2. 当前 `SUP bridge` 仍是 frame-level 全局 suppress，尚未升级为 ROI / overlap 感知 suppress。
3. 外部 `PX4 + Gazebo` 联调 launch 组合还未实际实现。

### 下一步

1. 按新文档中的前置清单，先做外部 `PX4 + Gazebo + phase_d + patrol_control` 的话题、TF 与相机接线预检查。
2. 之后再安排真正的外部仿真联调实现。
3. 最后再处理 `SUP bridge` 优化和 OrangePi 5 Plus RKNN 真机验证。

---

## 2026-07-07（视觉-控制机制与规则适配技术说明）

### 改动范围

- `docs/VISION_CONTROL_RULE_ADAPTATION_20260707.md`
- `README.md`

### 已完成改动

1. 新增专门技术说明文档，系统梳理：
   - 现有视觉算法机制
   - 与 `patrol_control` 的实际配合方式
   - 当前如何完成标准投递、红十字投递、降落
   - 2026 规则对视觉与控制提出的真实要求
   - 搜索在视觉组中的接口责任边界
   - 当前 `H(landing_pad)` 与 `red_cross` 的主要问题
   - 设计合理性、结构性隐患与验证方向
2. 在 `README.md` 文档索引中加入这份技术说明，作为后续查阅入口。

### 验证结果

1. 已基于当前仓库代码与配置核对文档内容，覆盖：
   - `target_detector / cross / circle / landing`
   - `detection_fusion / target_memory / drop_aligner`
   - `patrol_control` 对 `selected_target / drop_offset / drop_ready / align_mode` 的消费
   - `patrol_real.yaml / patrol_sim.yaml` 中的航点驱动与 `uav_vision` 参数
2. 已将规则书中的关键要求纳入文档：
   - 不再给出靶标粗坐标
   - 自主搜索并找到合适靶标
   - 标准投放、随机靶、降落相关计分与语义边界

### 遗留问题

1. 当前技术说明已把“搜索仅写到视觉侧接口边界”固定下来，但完整搜索轨迹与调度策略仍待控制/规划联合设计。
2. `H(landing_pad)` 当前仍是最薄弱链路，文档已定性为“外圈触发能力存在，但真实比赛稳健性不足”。
3. 标准目标链仍保留 `selected_target + /yolo_detect + align_ok` 的双回路过渡结构。

### 下一步

1. 基于这份技术说明继续整理 `H/red_cross` 问题样本分类表。
2. 用现有实录视频与双链证据继续做人工复核，不急于先改工程实现。
3. 之后再决定 `landing_pad/H` 强化、`SUP bridge` 收紧和搜索状态机接口深化的先后顺序。

---

## 2026-07-07（补充：中心计算职责与 H 误判边界）

### 改动范围

- `VISION_CHAIN_COMPARISON.md`
- `docs/VISION_CONTROL_RULE_ADAPTATION_20260707.md`

### 已完成改动

1. 在 `VISION_CHAIN_COMPARISON.md` 中补充了旧 2025 链与当前 2026 链的“发现类别”和“计算中心”职责划分，明确：
   - 旧标准目标四分类只负责类别，不负责投递中心
   - 旧圆环、红十字、`landing_pad(H 外圈)`、tank 各自负责中心计算，且多数直接投影到世界系
   - 新链仍然计算中心，但默认先保留在图像域，由 `drop_aligner + patrol_control` 继续消化
2. 在 `docs/VISION_CONTROL_RULE_ADAPTATION_20260707.md` 中补充了当前技术判断，明确：
   - 标准目标 unified5 detector 自带的 `center_px` 只是候选框中心，不是最终投递中心
   - 当前标准靶最终投递中心主要来自 `circle_detector`
   - 当前 `H` 的主问题是误判主导，不是漏检主导，因为普通靶标外圈会高频触发 `landing_detector`

### 验证结果

1. 已对照当前仓库代码口径完成文档更新，结论与下列实现一致：
   - 旧链：`yolo_detect.py`、`simple_cross_detect.cpp`、`circle_detector_node.cpp`、`landing_detector_node.cpp`
   - 新链：`target_detector.py`、`cross_detector_node.cpp`、`circle_detector_node.cpp`、`landing_detector_node.cpp`、`drop_aligner.py`
2. 两份活文档现在都明确写出：
   - “类别发现”与“中心计算”并非同一职责
   - 当前 `H` 问题以误判为主，而非单纯漏检

### 遗留问题

1. 当前文档已澄清机制，但 `landing_detector` 针对黑色外圈误触发的工程抑制策略仍未落地。
2. `red_cross` 与普通靶共现时的冲突，目前仍主要依赖 frame-level suppress，而不是空间感知裁决。

### 下一步

1. 继续基于实录视频整理 `H` 误判样本类型，区分普通靶黑圈误触发、残圈、局部遮挡和视角畸变。
2. 在文档层先收口“当前中心语义”和“当前误差主因”，再决定是否推进更强的 `H` 结构验证。

---

## 2026-07-11（默认取消 bridge 冲突抑制）

### 改动范围

- `vision_ws/src/uav_vision/scripts/detection_fusion.py`
- `vision_ws/src/uav_vision/config/detection_fusion.yaml`
- `VISION_CHAIN_COMPARISON.md`
- `docs/VISION_CONTROL_RULE_ADAPTATION_20260707.md`
- `vision_ws/README.md`

### 已完成改动

1. 将 `detection_fusion.py` 中 `suppress_bridge_on_red_cross`、`suppress_bridge_on_landing_pad` 的默认值由 `true` 改为 `false`，保留冲突裁决能力但默认不启用。
2. 将 `vision_ws/src/uav_vision/config/detection_fusion.yaml` 的同名开关同步改为 `false`，并补充注释，明确当前阶段关闭 `bridge` 抑制是为了避免因靶标不规范带来的误杀。
3. 更新活文档口径，明确：
   - 当前链仍支持 `bridge ↔ red_cross/landing_pad` 冲突裁决
   - 但默认已不再主动 suppress `bridge`
   - `SUP bridge` 只在后续显式重新启用该能力时才会再次出现

### 验证结果

1. 已执行：
   - `python3 -m py_compile vision_ws/src/uav_vision/scripts/detection_fusion.py`
   - 结果通过，脚本语法正常。
2. 已人工核对修改后的配置与文档：
   - `detection_fusion.py` 默认参数为 `false`
   - `detection_fusion.yaml` 默认参数为 `false`
   - 活文档已不再把“默认 suppress bridge”写成当前事实

### 遗留问题

1. 这次只是取消默认抑制，没有解决 `H/red_cross` 的根本误判问题。
2. 若后续重新打开 `bridge` 抑制，当前实现仍然是 frame-level 全局 suppress，不具备 ROI / overlap 感知。

### 下一步

1. 继续围绕真实视频样本复核 `H/red_cross` 误判来源，优先区分“靶标不规范”与“算法判别边界过宽”。
2. 若后续需要恢复冲突裁决，优先考虑做成更细粒度的空间感知 suppress，而不是直接回到全帧 suppress。

---

## 2026-07-11（redcross 视频全量抽帧与训练增强核对）

### 改动范围

- `vision_ws/scripts/extract_video_frames.py`
- `vision_ws/test_data/redcross_frames_20260711/`

### 已完成改动

1. 新增通用抽帧脚本 `vision_ws/scripts/extract_video_frames.py`，支持：
   - 读取任意视频
   - 按原始帧率全量导出为 `png/jpg`
   - 输出 `extract_meta.json` 记录帧数、FPS、分辨率和输出目录
2. 使用该脚本对 `vision_ws/test_data/redcross.mp4` 做了全量抽帧，生成：
   - `vision_ws/test_data/redcross_frames_20260711/images_raw_png/`
   - `vision_ws/test_data/redcross_frames_20260711/extract_meta.json`
3. 对当前 5 类 YOLO 训练配置做了核对，确认 `vision_ws/runs/liftrace_5cls_v1`、`v2`、`v3` 的实际增强参数口径一致。

### 验证结果

1. `redcross.mp4` 抽帧成功：
   - `saved_frames=1443`
   - `fps=59.88846827645074`
   - `resolution=1080x2388`
   - `duration_sec≈24.095`
2. 当前训练增强核对结果：
   - 已启用：`hsv_h/hsv_s/hsv_v`、`translate=0.1`、`scale=0.5`、`fliplr=0.5`、`mosaic=1.0`
   - 未启用：`degrees=0.0`、`shear=0.0`、`perspective=0.0`、`flipud=0.0`、`mixup=0.0`、`cutmix=0.0`、`copy_paste=0.0`
   - `auto_augment=randaugment`、`erasing=0.4` 虽出现在 args 中，但根据本机 `ultralytics 8.4.33` 源码注释，它们是 classification 参数，不属于当前 detect 训练主增强链
   - detect 训练里额外会走默认 Albumentations 非空间弱增强：`Blur(0.01)`、`MedianBlur(0.01)`、`ToGray(0.01)`、`CLAHE(0.01)`；未默认启用随机裁切

### 遗留问题

1. 当前只完成原始帧导出，尚未把 `red_cross` 正式并入 6 类数据集与 `data.yaml`。
2. 若后续做 6 类训练，需要同步更新类别映射、数据集划分和评测脚本口径。

### 下一步

1. 由人工完成 `redcross_frames_20260711/images_raw_png/` 的框标注。
2. 标注完成后，把 `red_cross` 合并进现有数据集，更新 `data.yaml` 与训练脚本，转入 6 类训练与回归评测。

### 未来追加模板

以后新增记录时，按下面模板直接追加到文末：

```md
## YYYY-MM-DD

### 改动范围

- 

### 已完成改动

1. 

### 验证结果

1. 

### 遗留问题

1. 

### 下一步

1. 
```

---

## 2026-07-11（全量构建核验、问题责任边界与活文档收口）

### 改动范围

- `docs/当前问题与责任边界.md`
- `README.md`
- `SIMULATION_GUIDE_NOETIC_PX4_GAZEBO_QGC.md`
- `VISION_MIGRATION_CHECKLIST.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
- `VISION_CHAIN_COMPARISON.md`
- `VISION_WORKSPACE_GUIDE.md`
- `vision_ws/README.md`
- `vision_ws/src/uav_vision/README.md`
- `docs/VISION_CONTROL_RULE_ADAPTATION_20260707.md`

### 已完成改动

1. 新增“当前问题与责任边界”作为问题汇总入口，按视觉组、非视觉组和共同接口责任区分问题，并关联 2026 规则要求。
2. 明确历史 `Desktop_patrol_uav_ws-patrol_planner/src/readme.md` 仅是上一届机载资产说明；AstraDroneOpen 是本机外部开发基座，不属于机载复制资产。
3. 更新主工作区全量构建口径：必须先 source `vision_ws/devel`，并显式清空历史 `CATKIN_WHITELIST_PACKAGES`。
4. 更新 bridge 抑制口径：`detection_fusion` 默认关闭，但 `target_memory.yaml` 和 `detect_compat_bridge.py` 仍有独立抑制开关；当前不能称为端到端完全取消。
5. 将迁移清单、执行方案、链路对比和视觉 README 的后续任务统一为“先统一 bridge 策略，再决定是否需要空间感知裁决”。

### 验证结果

1. `vision_ws` 串行编译通过。
2. 主集成工作区在独立临时目录、叠加 `vision_ws/devel` 且清空白名单后，`catkin_make -j1` 全量构建通过；实际覆盖 FAST_LIO、FreeDOM、Fast-Planner、`patrol_control`、`actuator_pwm` 与项目内 Livox 驱动。
3. 已以代码和配置核对 bridge 开关：fusion 为 `false`，candidate/compat 路径仍保留有效抑制配置或默认值。
4. 本次未启动 PX4、Gazebo、飞控、舵机或投递执行节点。

### 遗留问题

1. 内部仿真 `fast_planner_node` 的 `boost::lock_error` 与非单位四元数仍未复现和定位。
2. 外部 Astra 仿真的图像、`camera_info`、odom、TF 与 `phase_d + patrol_control` 尚未完成端到端接线。
3. 视觉候选地图定位、类别-圆环关联、规则感知 `drop_ready`、随机靶黑色有效区和降落 H 语义仍未落地。

### 下一步

1. 先为 fusion、target_memory、compat bridge 的 bridge 策略补统一配置与 ROS/离线回归，再决定是否改视觉实现。
2. 在 Astra 外部仿真完成视觉输入和主控订阅预检查。
3. 再设计目标地图记忆、类别-圆环关联和规则感知释放许可接口。

---

## 2026-07-11（文档口径一致性补充）

### 改动范围

- `VISION_WORKSPACE_GUIDE.md`
- `vision_ws/README.md`
- `vision_ws/src/uav_vision/README.md`

### 具体改动

1. 将工作区指南中已过时的“`SUP bridge` 误抑制优化”表述改为统一三层 bridge 冲突策略。
2. 将两份视觉工作区 README 的当前状态日期更新为 2026-07-11。

### 验证结果

1. 已人工核对其与 `docs/当前问题与责任边界.md` 的 bridge 状态描述一致。
2. 本次仅修改文档，未执行仿真、飞控、舵机或视觉节点。

### 遗留问题与下一步

按上一条记录：先完成三层 bridge 策略统一与回归验证。

---

## 2026-07-11（2026 视觉完整开发规划审计与订正）

### 改动范围

- `VISION_2026_COMPLETE_DEVELOPMENT_PLAN.md`

### 具体改动

1. 保留“YOLO 粗发现 + 几何复核 + 地图记忆 + 控制互锁”的总体路线，明确其与规则和现有 `uav_vision` 演进方向一致。
2. 订正当前基线：`v3` 是可用离线参考资产，但开发 launch 默认模型仍为 `liftrace_5cls_v1`；RKNN 默认仍是历史 split 路径，不是统一五类或六类部署产物。
3. 订正接口成熟度：当前只有图像域消息；`phase_d*.launch` 默认启动旧接口兼容桥，`patrol_control` 仍订阅 `/yolo_detect` 与 `/detect/*`，因此“正式模式仅使用新接口”改为待完成迁移目标。
4. 将图像坐标/resize/相机主点契约、外部仿真 frame/地面平面检查、现有 RKNN 板端可行性前置为 P0；将 OrangePi 部署从 P1 调整为 P0。
5. 订正地图投影假设：使用可配置 `world_frame`、时间同步 TF 与验证后的地面平面，不假定已有 `map←camera_link`，也不允许像素伪装世界坐标。
6. 明确状态边界：视觉维护观测、验证、关联；控制维护任务锁定、投递最终确认、超时和执行结果，并回写视觉。
7. 补充 `tank` 权重风险：规则未明确其得分权重，当前临时优先级不可作为比赛排序依据；未获官方确认前不应自动中断任务。

### 验证结果

1. 已逐项核对 `uav_vision` 当前消息、默认模型/RKNN 配置、Phase D launch、bridge 三层开关与 `patrol_control` 订阅代码。
2. 本次仅更新规划文档，未改动视觉算法、消息定义、launch、主控或模型，未启动仿真、飞控、舵机或投递执行节点。

### 遗留问题

1. 外部 Astra 仿真的实际图像、内参、TF 和主控接口仍未进行端到端运行验证。
2. 图像尺度不一致（circle 640×512 与 drop_aligner 640×480）尚未修复。
3. 地图投影、实例关联、规则级释放许可、正式模式接口开关和六分类 RKNN 均未实现。

### 下一步

按规划阶段 0，先完成坐标契约、外部输入/TF 前置检查与现有 RKNN 板端可行性核验，再启动六分类数据和融合改造。

---

## 2026-07-11（redcross 标注主集改为 4 FPS 稀疏抽帧）

### 改动范围

- `vision_ws/scripts/extract_video_frames.py`
- `vision_ws/test_data/video_sources/redcross_20260711/`

### 具体改动

1. 将抽帧脚本扩展为支持：
   - `--target-fps`
   - `--frame-step`
   - `--legacy-layout`
2. 使其可以按旧 `bridge/tank` 数据源风格输出：
   - `frames/`
   - `manifest.csv`
   - 文件名包含原始帧号和时间戳
3. 按旧方式对 `vision_ws/test_data/redcross.mp4` 重新导出一套稀疏标注主集：
   - 输出目录：`vision_ws/test_data/video_sources/redcross_20260711/frames`
   - 采样策略：约 `4 FPS`
   - 实际 `frame_step=15`
   - 共 `97` 张 JPG
4. 保留此前的 `vision_ws/test_data/redcross_frames_20260711/` 全量 PNG 抽帧，仅作为备份，不作为当前首选标注主集。

### 验证结果

1. 已执行：
   - `python vision_ws/scripts/extract_video_frames.py --input vision_ws/test_data/redcross.mp4 --output-dir vision_ws/test_data/video_sources/redcross_20260711 --prefix redcross_20260711 --format jpg --target-fps 4 --legacy-layout`
   - `python -m py_compile vision_ws/scripts/extract_video_frames.py`
2. 输出核对通过：
   - `manifest.csv` 已生成
   - `frames/` 内共有 `97` 张图片
   - `extract_meta.json` 记录：
     - `source_fps=59.88846827645074`
     - `effective_sample_fps≈3.993`
     - `frame_step=15`

### 遗留问题

1. 当前只完成了更适合人工标注的稀疏主集导出，尚未把 `red_cross` 纳入正式 6 类数据集。
2. 若后续发现关键姿态覆盖仍不足，可能还需要在局部时间段补充更高密度抽帧，而不是恢复全量标注。

### 下一步

1. 以 `vision_ws/test_data/video_sources/redcross_20260711/frames` 作为手工标注主集。
2. 标注完成后，再合并进 6 类数据集并更新训练/评测配置。

---

## 2026-07-12（red_cross 手工标注数据并入 6 类数据集并完成训练）

### 改动范围

- `vision_ws/scripts/merge_single_class_yolo_zip.py`
- `vision_ws/scripts/train_6cls.py`
- `vision_ws/test_data/yolo_dataset_v4_6cls_redcross_manual_20260712/`
- `vision_ws/runs/liftrace_6cls_v4_redcross_manual_20260712/`

### 具体改动

1. 新增 `merge_single_class_yolo_zip.py`，用于把单类 YOLO 标注 zip 自动并入现有多类数据集，能力包括：
   - 识别常见 YOLO 数据集目录结构
   - 自动解压导入 zip
   - 将导入标签统一重映射到目标类别 ID
   - 基于现有 `v3` 数据集构建新的 `v4` 六分类数据集
   - 输出 `merge_summary.json`
2. 新增 `train_6cls.py`，沿用既有 `train_5cls.py` 的训练配方，默认指向：
   - 数据集：`vision_ws/test_data/yolo_dataset_v4_6cls_redcross_manual_20260712/data.yaml`
   - 运行目录：`vision_ws/runs/liftrace_6cls_v4_redcross_manual_20260712`
3. 使用 `vision_ws/test_data/redcross.yolov11.zip` 完成 `red_cross` 手工标注数据并入，生成新的六分类数据集：
   - `bridge`
   - `panzer`
   - `pillbox`
   - `tent`
   - `tank`
   - `red_cross`
4. 启动并完成新六分类模型训练，输出新 run 目录和权重。

### 验证结果

1. 已执行静态校验：
   - `python -m py_compile vision_ws/scripts/merge_single_class_yolo_zip.py vision_ws/scripts/train_6cls.py`
2. 已执行数据集整合：
   - `python vision_ws/scripts/merge_single_class_yolo_zip.py`
3. 已执行模型训练：
   - `python vision_ws/scripts/train_6cls.py`
4. 已核对关键产物存在：
   - `vision_ws/test_data/yolo_dataset_v4_6cls_redcross_manual_20260712/data.yaml`
   - `vision_ws/test_data/yolo_dataset_v4_6cls_redcross_manual_20260712/merge_summary.json`
   - `vision_ws/runs/liftrace_6cls_v4_redcross_manual_20260712/weights/best.pt`
   - `vision_ws/runs/liftrace_6cls_v4_redcross_manual_20260712/weights/last.pt`
   - `vision_ws/runs/liftrace_6cls_v4_redcross_manual_20260712/results.csv`

### 遗留问题

1. 本轮完成了数据并入与训练，但尚未对新六分类模型做真实视频回放和完整视觉链回归。
2. 当前板端 RKNN 路线仍停留在旧资产与旧部署基线，六分类板端部署尚未同步。

### 下一步

1. 用新 `best.pt` 对 `real_target.mp4`、`redcross.mp4` 等现有视频做离线推理回归。
2. 若离线效果通过，再把新六分类模型纳入 `uav_vision` 的 dev/sim 默认评测链，并规划后续 RKNN 转换。

---

## 2026-07-13（6 类 `red_cross` 回归评测并切换 dev/sim 默认权重）

### 改动范围

- `vision_ws/scripts/infer_video_report.py`
- `vision_ws/scripts/eval_full_vision_chain_video.py`
- `vision_ws/src/uav_vision/config/target_detector.yaml`
- `vision_ws/src/uav_vision/scripts/target_detector.py`
- `vision_ws/src/uav_vision/README.md`
- `vision_ws/README.md`
- `README.md`
- `VISION_CHAIN_COMPARISON.md`
- `docs/VISION_CONTROL_RULE_ADAPTATION_20260707.md`
- `docs/YOLO_6CLS_REDCROSS_EVAL_20260713.md`

### 具体改动

1. 修正离线评测脚本口径：
   - `infer_video_report.py` 改为动态写出当前模型类别，并支持 `--device`
   - `eval_full_vision_chain_video.py` 的 `bridge suppress` 参数改为可显式开关，默认与当前链一致为 `false`
2. 执行旧五分类 vs 新六分类的离线回归：
   - 单模型推理：
     - `real_target.mp4`
     - `redcross.mp4`
   - 完整视觉链：
     - `real_target.mp4`
     - `redcross.mp4`
3. 新增评测结论文档 `docs/YOLO_6CLS_REDCROSS_EVAL_20260713.md`，集中记录：
   - 单模型层结果
   - full-chain 结果
   - 是否建议切换 dev/sim 默认权重
4. 根据评测结论，将 `target_detector` dev/sim 默认权重切换到：
   - `vision_ws/runs/liftrace_6cls_v4_redcross_manual_20260712/weights/best.pt`
5. 同步修正文档中仍写死“当前默认五分类 detector”的描述，明确：
   - dev/sim 默认已是统一六分类（含 `red_cross`）
   - 板端 RKNN 仍未同步到六分类

### 验证结果

1. 已执行静态校验：
   - `python -m py_compile vision_ws/scripts/infer_video_report.py vision_ws/scripts/eval_full_vision_chain_video.py`
   - `python -m py_compile vision_ws/src/uav_vision/scripts/target_detector.py`
2. 已完成四组单模型推理与四组 full-chain 回放，关键结果如下：
   - `redcross.mp4` 单模型：
     - 旧五分类：`bridge=716`、`tank=700`
     - 新六分类：`red_cross=1460`
   - `real_target.mp4` full-chain（`frame_stride=4`）：
     - `raw_bridge_with_red_cross_frames: 202 -> 161`
     - `raw_bridge_with_landing_pad_frames: 87 -> 58`
   - `redcross.mp4` full-chain：
     - `raw_bridge_with_red_cross_frames: 662 -> 0`
     - `raw_bridge_with_landing_pad_frames: 530 -> 0`
3. 已核对新六分类训练 run 的最终记录文件存在，并提取到末期指标：
   - `metrics/precision(B)≈0.99777`
   - `metrics/recall(B)=1.0`
   - `metrics/mAP50(B)=0.995`
   - `metrics/mAP50-95(B)≈0.97485`

### 遗留问题

1. 本轮只切换了 dev/sim 默认 PyTorch 权重，未动板端 `target_detector_rknn.py` 的 6 类 RKNN 路线。
2. `H(landing_pad)` 的误判问题仍然独立存在，不能因为 `red_cross` 并入六分类就视为已解决。
3. `/yolo_detect` 兼容链路仍只允许标准目标字符串，不会直接透出 `red_cross`。

### 下一步

1. 继续用当前 6 类 dev/sim 默认权重回放更多真实视频，重点盯 `H` 误判和剩余 `bridge` 误报。
2. 规划六分类 RKNN 导出与板端验证。
3. 再决定是否需要围绕 `red_cross` 和 `bridge` 做更细粒度的融合策略收口。

---

## 2026-07-13（v5 标准数据集、飞行增强计划与 test_data 清理盘点）

### 改动范围

- `vision_ws/test_data/yolo_dataset_v5_6cls_redcross_standard_20260713/`
- `vision_ws/scripts/train_6cls.py`
- `vision_ws/scripts/merge_single_class_yolo_zip.py`
- `README.md`
- `vision_ws/README.md`
- `VISION_2026_COMPLETE_DEVELOPMENT_PLAN.md`
- `VISION_DATASET_AUTO_LABEL_PLAN.md`
- `docs/VISION_DATASET_V5_AUGMENTATION_PLAN_20260713.md`
- `docs/TEST_DATA_CLEANUP_PLAN_20260713.md`

### 具体改动

1. 基于 v4 合并结果生成 v5 标准数据集；只保留 `images/`、`labels/`、`data.yaml` 和 `dataset_manifest.yaml`，去除 v4 导入临时目录、cache 和无效 test split。
2. v5 固定六类顺序：`bridge/panzer/pillbox/tent/tank/red_cross`，训练脚本默认改为 v5 数据入口和 v5 独立实验名称。
3. 合并脚本改为临时目录解压、相对路径 YAML、无 test 声明，并默认拒绝覆盖已有输出；只有显式 `--overwrite` 才允许重建。
4. 新增面向飞行对地场景的增强清单，重点覆盖旋转、透视、运动/散焦模糊、局部裁切、遮挡、尺度、光照和视频泄漏控制，并规定按组消融。
5. 更新主 README、视觉 README、完整开发计划和历史自动标注说明，明确 v5 数据集与当前 v4 权重不是同一版本模型。
6. 新增 `test_data` 清理计划；对旧数据集、回放证据、原始视频和脚本引用进行分级，当前不删除既有目录。

### 验证结果

1. v5 包含 923 张 train、232 张 val，共 1155 张图像和对应标签；红十字 class ID 5 共 95 个标注。
2. v5 不包含 `_import_redcross_tmp`、`.cache` 或 `images/test`。
3. `train_6cls.py` 与合并脚本通过 `python -m py_compile`。
4. 对已存在 v5 输出执行合并脚本时，覆盖保护按预期拒绝；未覆盖或删除现有标准集。
5. 本次未启动训练、仿真、飞控、舵机或投递执行节点。

### 遗留问题

1. v5 尚未进行无增强 baseline 复现和飞行对地增强训练。
2. 当前红十字框沿用导入数据的中央红十字/内圈语义，不等同于完整随机靶或黑色外圈；如需改变框语义必须重标。
3. v4 原始合并目录及 `_import_redcross_tmp` 暂时保留作追溯；旧 test_data 回放目录仍需完成引用迁移后再归档。

### 下一步

1. 用 v5 完成无增强 baseline 训练并冻结结果。
2. 按 `geometry → blur → occlusion → photometric` 顺序进行消融，使用原始 val、challenge 集和真实视频分别评测。
3. baseline 与增强模型确认后，再决定 v4 原始目录、旧回放和临时导入资料的归档/删除。

---

## 2026-07-13（规划文档进度评估与下一阶段收口）

### 改动范围

- `docs/VISION_PROGRESS_ASSESSMENT_20260713.md`
- `VISION_2026_COMPLETE_DEVELOPMENT_PLAN.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
- `VISION_MIGRATION_CHECKLIST.md`
- `VISION_WORKSPACE_GUIDE.md`
- `VISION_CHAIN_COMPARISON.md`
- `docs/VISION_CONTROL_RULE_ADAPTATION_20260707.md`
- `docs/当前问题与责任边界.md`
- `docs/TEST_DATA_CLEANUP_PLAN_20260713.md`
- `README.md`
- `vision_ws/README.md`

### 具体改动

1. 新增阶段进度评估：阶段 2 六分类训练已完成（RKNN 除外）；阶段 0、3、6、7 部分完成；阶段 1、4、5、8 未完成；阶段 9 仅完成离线一级验收。
2. 将完整规划、OrangePi 执行方案、迁移清单和工作区指南中的旧“五分类/未训练/v4 默认”表述更新为 v5 baseline、flight_aug 和最终 dev/sim 候选。
3. 明确下一阶段优先级：外部仿真最小闭环 → bridge 三层策略统一 → 标准目标—圆环实例消息 → 地图目标/长期 ID → RKNN/板端验证。
4. 将已完成的 v5 数据、训练、回放和特征图纳入当前事实基线；将 `drop_offset`、`drop_ready`、fusion 和主控接线保留为“代码存在但闭环未完成”。
5. 更新 test_data 清理计划：v5 baseline/flight_aug 和最新回放保留；训练相关清理门禁标为已完成，旧回放和 v4 临时目录仍待归档条件满足。

### 验证结果

1. 已核对 v5 baseline、flight_aug best.pt、六类 feature_maps/summary.json 和两组 v5 视频回放 summary.json 均存在。
2. 已核对 `target_detector.yaml` 与 `target_detector.py` 默认路径指向 v5 flight_aug best.pt。
3. 本次仅更新文档和进度口径，未启动仿真、飞控、舵机或投递执行节点。

### 遗留问题

1. 外部 Astra 仿真联合 launch 尚未形成，图像、CameraInfo、TF、odom 与主控端到端链路未验收。
2. bridge 三层开关、标准目标—圆环实例关联、地图目标、规则级释放许可和正式新接口模式尚未实现。
3. 六分类 RKNN 导出、OrangePi 性能和热稳定性尚无证据。

### 下一步

按 `VISION_PROGRESS_ASSESSMENT_20260713.md`（历史文件名，精简分支未收录） 执行 P0-1 外部仿真最小联调闭环，再执行 P0-2 bridge 三层策略统一和 P0-3 标准投递实例消息设计。

---

## 2026-07-13（v5 baseline、飞行增强训练与最终特征图）

### 改动范围

- `vision_ws/scripts/augment_yolo_flight.py`
- `vision_ws/scripts/visualize_yolo_feature_maps.py`
- `vision_ws/src/uav_vision/config/target_detector.yaml`
- `vision_ws/src/uav_vision/scripts/target_detector.py`
- `vision_ws/src/uav_vision/README.md`
- `docs/VISION_V5_TRAINING_EVAL_20260713.md`
- `README.md`
- `vision_ws/README.md`
- `VISION_2026_COMPLETE_DEVELOPMENT_PLAN.md`
- `vision_ws/runs/liftrace_6cls_v5_baseline_20260713/`
- `vision_ws/runs/liftrace_6cls_v5_flight_aug_20260713/`
- `vision_ws/test_data/yolo_dataset_v5_flight_aug_20260713/`
- `vision_ws/test_data/real_target_infer_v5_flight_aug_20260713/`
- `vision_ws/test_data/redcross_infer_v5_flight_aug_20260713/`

### 具体改动

1. 修正 v5 `data.yaml` 的 Ultralytics 路径契约：使用绝对数据根路径，避免 `path: .` 被错误解析为工作目录下的 `images/val`。
2. 生成只扩充 train、保持原始 val 不变的飞行增强数据集，包含 geometry、motion blur、crop、occlusion、photometric 五类增强。
3. 完成 v5 原始 baseline 训练和飞行增强版训练；增强版作为当前最终 dev/sim 候选。
4. 新增按六类组织的中间激活特征图脚本和结果目录，生成每类样本叠加图、平均激活热力图和通道网格。
5. 将 dev/sim `target_detector` 默认模型切换至 `liftrace_6cls_v5_flight_aug_20260713/weights/best.pt`；未修改板端 RKNN 路径。

### 验证结果

1. baseline：59 个 epoch 结果行，原始 v5 val 总体 mAP50-95=0.977，`red_cross`=0.896。
2. flight_aug：100 个 epoch，best epoch=80，原始 v5 val 总体 mAP50-95=0.980，`red_cross`=0.909。
3. 真实 `real_target.mp4` 回放：3747/4623 帧有检测；`red_cross=1721`、`bridge=642`。
4. 真实 `redcross.mp4` 回放：1415/1443 帧有检测，全部为 `red_cross`。
5. 六类特征图已为 `bridge/panzer/pillbox/tent/tank/red_cross` 各生成样本图、平均热力图和通道图；脚本与 detector 脚本通过 `python -m py_compile`。
6. 本次未启动 PX4、Gazebo、飞控、舵机或投递执行节点。

### 遗留问题

1. 当前指标来自原始 val 和离线视频，尚未完成 OrangePi 六分类 RKNN 导出、板端性能与结果一致性验证。
2. 特征图是中间激活诊断可视化，不是严格的 Grad-CAM 归因结果。
3. 红十字标注仍是中央红十字/内圈框语义，黑色有效区仍由传统几何链负责。

### 下一步

1. 用最终增强模型继续做完整视觉链回放，检查 `H`、bridge 与传统几何检测的融合冲突。
2. 开始六分类 RKNN 导出和 OrangePi 实测。
3. 完成 v4 临时导入资料及旧回放目录的引用迁移后，再执行分批归档清理。

---

## 2026-07-13（活规划最终口径确认）

### 改动范围

- `docs/VISION_PROGRESS_ASSESSMENT_20260713.md`
- `VISION_2026_COMPLETE_DEVELOPMENT_PLAN.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
- `VISION_MIGRATION_CHECKLIST.md`
- `VISION_WORKSPACE_GUIDE.md`
- `VISION_CHAIN_COMPARISON.md`
- `docs/VISION_CONTROL_RULE_ADAPTATION_20260707.md`
- `docs/当前问题与责任边界.md`
- `docs/TEST_DATA_CLEANUP_PLAN_20260713.md`
- `README.md`
- `vision_ws/README.md`

### 具体改动

1. 将规划文档统一到 2026-07-13 的真实进度：六分类 v5 训练/回放/特征图完成，外部仿真、实例关联、地图记忆、正式接口模式和 RKNN 仍未完成。
2. 新增阶段完成度表和下一步 P0/P1 顺序，避免把已有节点误写成规则闭环已完成。
3. 修正 OrangePi、迁移清单、工作区指南、链路对比和当前问题文档中的旧五分类/v4 默认模型口径。
4. 将 test_data 清理计划的训练门禁标记为完成，保留历史数据和临时目录归档条件。

### 验证结果

1. 已核对最终模型、特征图 summary、v5 回放 summary 和默认 detector 路径均存在。
2. 本次仅更新文档与进度口径，未启动仿真、飞控、舵机或投递执行节点。

### 下一步

以 `VISION_PROGRESS_ASSESSMENT_20260713.md`（历史文件名，精简分支未收录） 为当前主执行顺序：外部仿真最小闭环、bridge 三层策略、标准目标—圆环实例消息、地图目标/长期 ID、RKNN 板端验证。

---

## 2026-07-13（完整规划节点状态校正）

### 具体改动

将完整开发规划中的 `cross_detector`、`circle_detector`、`landing_detector`、`detection_fusion`、`target_memory`、`drop_aligner` 和兼容桥状态，从“代码已存在/已完成”细化为“部分完成或过渡完成”，明确其尚未形成实例级、地图级、规则级闭环。

### 验证结果与下一步

已与 `VISION_PROGRESS_ASSESSMENT_20260713.md`、迁移清单和当前问题边界一致；后续按外部仿真 → bridge 策略 → 实例消息 → 地图记忆顺序推进。

---

## 2026-07-13（最终增强模型完整链回放与 H 阶段门控审计）

### 改动范围

- `vision_ws/test_data/real_target_full_chain_v5_flight_aug_20260713_stride4/`
- `vision_ws/test_data/redcross_full_chain_v5_flight_aug_20260713/`
- `docs/VISION_PROGRESS_ASSESSMENT_20260713.md`
- `docs/VISION_V5_TRAINING_EVAL_20260713.md`
- `docs/当前问题与责任边界.md`
- `docs/TEST_DATA_CLEANUP_PLAN_20260713.md`
- `VISION_2026_COMPLETE_DEVELOPMENT_PLAN.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
- `VISION_MIGRATION_CHECKLIST.md`
- `VISION_CHAIN_COMPARISON.md`
- `README.md`
- `vision_ws/README.md`

### 具体改动

1. 使用 `liftrace_6cls_v5_flight_aug_20260713/weights/best.pt` 完成 `real_target.mp4` 和 `redcross.mp4` 的统一模型、红十字几何链、降落外圈链完整离线回放，明确关闭 bridge 抑制。
2. 将 `real_target` 中同帧 `red_cross + landing_pad` 解释为多路观测共存，明确由任务状态/`align_mode` 决定控制所有权，不由视觉层整帧互斥。
3. 审计旧 `patrol_control`：确认已有 `desiredAlignMode()` 的基础门控，但 `Land_point` 仍复用 `MAIN_MISSION`，旧检测 flag 与新接口并行，且 `LandDetectDone()` 超时直接返回成功，列为控制侧高风险缺口。
4. 将旧 `image2center`、圆环/降落椭圆拟合和 `CameraInfo + TF + 地面平面` 投影列为可复用参考；明确禁止原样复用硬编码内参、frame 和图像缩放补偿。
5. 根据正式比赛不复用桥梁靶标底座的前提，将 bridge 重叠和抑制开关从当前 P0 移除，下一阶段改为 H 阶段门控、地图记忆、投递靶心和类别—圆环实例关联。

### 验证结果

1. `real_target.mp4`：处理 1156 帧，996 帧有检测，红十字几何链 515 帧，降落外圈链 359 帧，原始 `bridge + red_cross` 同帧 160 帧，抑制 0 帧。
2. `redcross.mp4`：处理 1443 帧，1415 帧有检测，红十字几何链 1415 帧，降落外圈链 1016 帧，bridge 冲突 0 帧，抑制 0 帧。
3. `vision_ws/test_data/` 当前未找到 SVG 文件；本次未能依据 SVG 图做额外节点拓扑判读，节点现状以仓库 launch、源码和回放证据为准。
4. 仅执行离线回放、源码审计和文档更新；未启动 PX4、Gazebo、飞控、舵机或投递执行节点。

### 遗留问题

1. H 检测仍主要是外圈候选，尚未完成强 H 结构语义、连续帧和超时回退的控制互锁。
2. 地图目标、长期 ID、标准类别—圆环实例关联和规则级释放许可尚未实现。
3. 外部 Astra/PX4 仿真链、OrangePi RKNN 和真实执行机构仍未验证。

### 下一步

按以下顺序推进：外部仿真输入前置检查 → H/投递/巡检显式状态门控 → 复用旧链思路实现参数化地图投影与投递靶心 → 标准目标—圆环实例关联 → RKNN 板端验证。

---

## 2026-07-13（规划文档 bridge 口径补充收口）

### 改动范围

- `VISION_WORKSPACE_GUIDE.md`
- `VISION_DATASET_AUTO_LABEL_PLAN.md`

### 具体改动

将工作区指南和数据路线文档中的 bridge 三层冲突策略从当前任务移除，补充 v3 重叠的真实数据背景，并把近期顺序统一为 H 阶段门控、旧链投影复用、地图记忆、投递靶心和 RKNN。

### 验证结果

已对规划文档搜索 bridge 相关表述：保留内容均为类别资产、历史回放或兼容配置说明，未再列为当前 P0/P1 工作项。本次未修改 detector/config 行为，未启动仿真或硬件。

### 遗留问题

`target_memory.yaml` 中的历史 bridge 开关仍存在；由于正式比赛不复用该底座，本次不以它作为验收项，后续正式接口收口时再决定是否删除旧参数。

### 下一步

执行外部仿真输入前置检查，并开始 H/投递/巡检状态转换表和地图投影接口设计。

---

## 2026-07-13（规则适配说明同步）

### 改动范围

- `docs/VISION_CONTROL_RULE_ADAPTATION_20260707.md`

### 具体改动

将规则适配说明中的 bridge 处理从“待收紧的当前算法策略”改为 v3 历史数据背景和兼容说明；补充同帧多路观测、H 语义、旧检测 flag 与新 `align_mode` 并行、降落超时放行等实际风险，并把工作顺序改为阶段门控、地图记忆、投递靶心和实例关联。

### 验证结果

已与最终增强模型完整链回放结果、`patrol_control.cpp` 和其他主规划文档对照；未修改代码和参数，未启动仿真或硬件。

### 遗留问题

H 结构验证、状态机实现、地图投影和投递靶心仍待开发与回放验证。

### 下一步

先形成 `SEARCH/VERIFY/DROP_ALIGN/LAND_CONFIRM` 状态—视觉模式映射，再实现参数化投影和标准目标实例关联。

---

## 2026-07-13（规则适配文档历史冲突说明修订）

### 改动范围

- `docs/VISION_CONTROL_RULE_ADAPTATION_20260707.md`

### 具体改动

将该规则适配说明中的旧五分类 bridge 混淆描述标为 v3 历史构造样本问题，删除其作为当前冲突裁决优先级的表述，补充 H 结构、黑色有效区、任务阶段所有权和降落超时回退的真实缺口。

### 验证结果

已与最终增强模型回放、`patrol_control` 源码审计和当前问题边界交叉核对；本次只改文档，未启动仿真或硬件。

### 遗留问题

H 语义确认和显式状态机仍未实现，地图记忆及投递靶心也未形成新接口。

### 下一步

优先设计并回放验证 `SEARCH/VERIFY/DROP_ALIGN/LAND_CONFIRM` 到 `align_mode` 的唯一映射。

---

## 2026-07-13（视觉组业务范围与 SVG 资产纠正）

### 改动范围

- `vision_ws/src/uav_vision/README.md`
- `docs/VISION_PROGRESS_ASSESSMENT_20260713.md`
- `vision_ws/Nodes_Topics(active)2.svg`
- `vision_ws/Nodes_Topics(all).svg`
- `vision_ws/Nodes Only.svg`
- `vision_ws/rosgraph.svg`

### 具体改动

1. 重新盘点整个 `vision_ws/`，确认 4 张 SVG 位于工作区根目录，而不是 `test_data/`。
2. 将 `uav_vision` 文档从“外部仿真优先”调整为视觉组自身业务闭环优先：观测契约、H 语义、地图记忆、实例关联、投递靶心、释放许可和 RKNN。
3. 明确当前消息和节点仍是图像域短期链：没有地图坐标、长期 ID、多圆环实例、H 结构验证和独立 `release_permission`。
4. 将外部 Astra/PX4、`patrol_control` 状态接线定义为联调依赖，不作为视觉组业务开发的前置阻塞。

### 验证结果

1. 4 张 SVG 均可读取；`Nodes_Topics(active)2.svg` 与 `Nodes_Topics(all).svg` 尺寸和图形规模相同但文件内容不同，作为不同筛选口径的历史拓扑证据保留。
2. SVG 文本已被 Qt 导出为路径字形，当前 WSL 无可用 SVG 渲染工具，因此具体节点名称仍以源码、launch 和消息定义交叉核对，不对不可读路径做臆测。
3. 未修改视觉代码和参数，未启动仿真、飞控或执行机构。

### 遗留问题

视觉组尚未实现地图投影、长期目标记忆、多圆环实例关联、H 结构语义和规则级释放许可。

### 下一步

先在 `uav_vision` 内完成观测/坐标契约和 H 质量字段，再实现地图记忆与标准类别—圆环实例关联；之后再与主控和外部仿真联调。

补充：已在 `docs/VISION_PROGRESS_ASSESSMENT_20260713.md` 增加 `vision_ws` 四张 SVG 拓扑图的资产索引和 XML 有效性说明，纠正“整个视觉工作区没有 SVG”的歧义。

---

## 2026-07-13（视觉工程闭环：权重、圆环中心、地图记忆）

### 改动范围

- `vision_ws/src/uav_vision` 消息、节点、配置、launch 和 mock 回归
- `vision_ws/src/uav_vision/README.md`
- `vision_ws/README.md`、`README.md`
- `docs/VISION_PROGRESS_ASSESSMENT_20260713.md`

### 具体改动

1. 恢复 `target_memory` 的 `priority_bridge=2.0`；保留六分类模型和 `tank=1.0` 临时工程排序，明确 tank 正式得分尚未发布。
2. 扩展 `TargetDetection`、`TargetCandidate` 的中心精修和地图投影字段。
3. 将 `circle_detector_node` 改为同帧多圆环输出，增加椭圆质量筛选、边缘拒绝、重复中心抑制，并将 resize 坐标逆变换回原图。
4. 新增 `target_refiner` 完成标准类别框—蓝色圆环关联；新增 `target_map_projector` 完成 CameraInfo、TF 与地面平面求交。
5. 扩展 `target_memory` 的地图距离关联、地图候选保留和 `/uav_vision/reset_memory` 服务。
6. `drop_aligner` 真实链路改用 CameraInfo 主点；新增多圆环、地图投影和精修链路的纯 ROS mock launch。
7. 保留旧 `/detect/*` 兼容桥、bridge 历史抑制开关和 tank 中断默认关闭，不启动 PX4/Gazebo 或执行机构。

### 验证结果

1. `vision_ws` 串行 `catkin_make -j1` 通过。
2. bridge、tank、circle 的 Phase D mock 回归通过；bridge 已能进入 `/uav_vision/selected_target`，tank 仍能正常选择和输出旧接口。
3. `circle_geometry_mock.launch` 通过：1280×1024 图像 resize 到 640×512 后，同一帧恢复 2 个圆环中心及半径。
4. `phase_d_map_mock.launch` 通过：类别—圆环关联、CameraInfo/TF 地面投影、地图候选稳定 ID、drop_circle 主点对准均成立。
5. `phase_d.launch` 纯视觉启动烟测通过；当前 ROS Python 缺少 ultralytics 时 target detector 按既有设计退化为空检测，未影响其他节点启动。

### 遗留问题

- 尚未用真实飞行姿态和真实 TF 标定地图投影误差。
- H 结构语义、已投递状态和独立 `release_permission` 尚未实现。
- 六类 RKNN 导出、OrangePi NPU 吞吐和整机主控状态门控仍未验证。
- 圆环关联阈值还需要用 `vision_ws/test_data` 的真实倾斜、遮挡和模糊样本继续标定。

### 下一步

用现有真实回放数据验证圆环中心、类别关联和地图投影误差；随后补 H 质量字段与投递/巡检阶段门控，再进行旧接口和 OrangePi RKNN 验证。

---

## 2026-07-13（真实视频圆环关联与地图投影离线评估）

### 改动范围

- `vision_ws/scripts/eval_ring_map_video.py`
- `vision_ws/src/uav_vision/README.md`
- `vision_ws/README.md`
- `docs/VISION_PROGRESS_ASSESSMENT_20260713.md`

### 具体改动

1. 新增无仿真离线评估入口：读取已有 v5 完整链 `detections_long.csv`，在真实 `real_target.mp4` 上复现当前 C++ 圆环的 HSV、resize 逆变换、椭圆质量、边缘拒绝和去重逻辑，并按 `target_refiner` 规则完成类别—圆环关联。
2. 输出圆环候选、关联明细、逐帧摘要、标注视频、样本帧和 `ring_ground_truth_template.csv`；支持人工中心真值、CameraInfo、逐帧 camera→map 位姿和目标地图坐标作为可选输入。
3. 增加显式检测坐标方向变换。当前 MP4 解码为 `2560×1080`，历史 v5 CSV 为 `1080×2560` 竖屏坐标，真实回放使用并记录 `portrait_to_landscape_ccw`。
4. 修正文档中 `bridge` 已恢复优先级但又被列入“不进入 selected_target”的矛盾描述。

### 验证结果

1. `python3 -m py_compile vision_ws/scripts/eval_ring_map_video.py` 通过。
2. 使用真实视频完整回放：4623 帧总数、1156 帧步长 4 处理；严格当前圆环规则产生 18 个候选，623 个标准目标中成功关联 3 个，关联率 `0.48%`。
3. 已生成：`vision_ws/test_data/real_target_ring_map_eval_20260713_stride4/`，包括 `circle_detections.csv`、`association_long.csv`、`frame_summary.csv`、标注视频和 623 行人工真值模板。
4. CameraInfo 短段探测确认现有 `detect_ws/src/camera_sdk/param/camera_param.yaml` 为 `1920×1080`，与视频 `2560×1080` 不匹配；无同步 CameraInfo/TF/位姿/目标地图坐标时，地图绝对误差正确报告为不可测。
5. 未启动 PX4、Gazebo、MAVROS、执行机构或实机链路。

### 遗留问题

- 当前真实视频中 YOLO 框主要覆盖内层图案，外圈蓝环常在框外；严格圆环规则仅检出少量局部蓝色区域，不能直接用于可靠投递中心。
- 当前关联率是链路自洽性指标，不是真实中心误差；需从模板中选取代表帧补填人工圆环中心。
- 地图投影绝对误差仍缺同步相机标定、位姿和目标地图真值。

### 下一步

优先基于真实标注帧改进外圈完整度/部分外圈检测和框外 ROI 关联，再用同步 CameraInfo 与 camera→map 位姿回放标定地图投影误差；不通过盲目放宽关联距离掩盖外圈检出问题。

---

## 2026-07-13（旧完整视觉链真实视频推演）

### 改动范围

- `vision_ws/scripts/eval_legacy_full_vision_chain_video.py`
- `vision_ws/src/uav_vision/README.md`
- `vision_ws/README.md`
- `docs/VISION_PROGRESS_ASSESSMENT_20260713.md`

### 具体改动

1. 新增旧链离线入口，使用旧 `Visual/src/yolov5_detect/best.pt`、旧独立 `tank.pt`、旧 cross/landing 逻辑和旧 `circle_detection_params.yaml`。
2. 按上一届 `circle_detector_node.cpp` 复现：图像 resize 到 `640×512`，HSV/形态学处理，从所有合格轮廓选面积最大椭圆；保留旧 `center×2` 投影输入语义。
3. 复现旧 YOLO 三帧类别累计，并记录旧 tank 的 `image2center` 服务调用次数。
4. 新增 `legacy_projection_inputs.csv`，按旧圆环节点和 `image_process.cpp` 的硬编码内参输出反投影射线；无 TF 时不生成地图点。

### 验证结果

1. 在 `rl_drone` conda 环境中语法、帮助和完整真实视频运行均通过。
2. `real_target.mp4`：4623 总帧，1156 帧 stride=4；旧四类检测 760 个，tank 中心服务调用 147 次，旧圆环中心 114 帧，cross 206 帧，landing 133 帧。
3. 旧三帧类别累计输出：`Nothing=138`、`pillbox=107`、`bridge=32`、`panzer=97`、`tent=11`。
4. 输出目录：`vision_ws/test_data/real_target_legacy_full_chain_20260713_stride4/`，包含旧链标注视频、检测 CSV、旧中心 CSV、投影射线 CSV 和摘要。
5. 未启动 PX4、Gazebo、MAVROS、执行机构或实机链路。

### 遗留问题

- 旧圆环中心候选没有类别绑定；抽查发现反光/局部蓝色区域可成为全图最大合格椭圆，不能直接视为靶心。
- 旧投影依赖硬编码 `1280×1024` 相机模型和运行时 map TF；当前真实 MP4 没有同步 TF/odom，因此仅能验证像素中心和反投影射线。
- 旧链回放按检测常开处理，没有复现主控实际 `/detect/control`、`/detect/class_control` 的时序门控。

### 下一步

以旧链输出作为回归基线：保留其能检出圆环的真实样本，但优先将“全图最大圆”改为外圈完整度、类别框扩展 ROI 和多帧稳定性约束，再与新链的 `center_refined/map_valid` 结果逐帧对照。

---

## 2026-07-13（TOUDI3 场景启动入口与官方数据集自动标注）

### 改动范围

- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_world.launch`
- `vision_ws/scripts/import_official_6cls_dataset.py`
- `vision_ws/test_data/yolo_dataset_v5_6cls_redcross_official_20260713/`
- `vision_ws/test_data/official_dataset_audit_20260713/`
- `TOUDI3_GAZEBO_ASSET_SETUP_GUIDE.md`
- `TOUDI3_SIM_ASSET_CHECKLIST.md`
- `vision_ws/README.md`
- `docs/OFFICIAL_DATASET_IMPORT_20260713.md`

### 具体改动

1. 在 `patrol_control` 内重建 `patrol_world.launch`：默认加载包相对路径的
   `toudi3.world`，参数化 `px4_root`、SDF、机型、Gazebo 模型路径、GUI 和姿态；
   支持 `start_px4:=false` 的 world/model-only 检查模式，避免依赖外部旧
   `patrol_world.launch` 文件。
2. 新增官方数据集导入脚本，使用 v5 六分类 `best.pt` 自动生成 YOLO 标签；
   `car→panzer`，`H` 明确作为降落标志 hard negative，不映射为 `red_cross`。
3. 从 `实物组数据集.zip` 的 60 张图片生成独立 v5 扩展入口，不覆盖原始 v5
   标准数据集。`conf=0.25` 下纳入 38 张、28 个自动框，其中 10 张为 H 空标签；
   22 张模型漏检正样本保留在 audit 中待人工复核。
4. 增加 `data.yaml`、`dataset_manifest.yaml` 和 JSON 审计记录，记录类别映射、
   漏检策略和模型输出；补充官方数据集导入报告。

### 验证结果

1. `patrol_world.launch` 在新版 PX4 ROS_PACKAGE_PATH 配置下通过 `roslaunch --files`；
   `start_px4:=false` 无 PX4 包时也能解析 Gazebo world/model-only 分支。
2. `import_official_6cls_dataset.py` 通过 `python3 -m py_compile`，使用
   `rl_drone` 完成 60 张推理和数据集生成。
3. 官方图片中 `H` 已通过图像抽样确认是黑色 H 降落标志；未写入 `red_cross`。
4. 将阈值降至 `0.05` 和中心内圈裁剪复核均未改善 bridge/panzer 召回，保留
   `conf=0.25` 的完整审计结果作为当前主候选。

### 遗留问题

- PX4 完整 SITL 的 simulator TCP 4560 联通问题尚未解决；本次未启动完整飞行控制链。
- `toudi3.world` 的五类材质目录仍缺失，可能造成场景材质警告。
- 官方 `bridge`、`car/panzer` 和部分 `tent` 图片不能由当前模型可靠自动标注，
  不应直接把 60 张全部用于训练。

### 下一步

人工复核 audit 中的 22 张漏检正样本，补齐 bridge/panzer 框；另行建立 H 降落标志
数据集，并在确认标签后再训练官方数据扩展模型。

---

## 2026-07-13（官方非 tank 首图旋转素材）

### 改动范围

- `vision_ws/scripts/rotate_official_first_targets.py`
- `vision_ws/test_data/official_first_non_tank_rotated_20260713/`

### 具体改动

1. 参照 `vision_ws/test_data/rotated/` 的命名与角度覆盖，按 6° 步长生成
   `000°` 至 `354°` 的旋转图像。
2. 对官方压缩包中 `bridge1`、`car1`、`H1`、`pillbox1`、`tent1` 五张首图生成
   独立素材，共 300 张；明确排除已有旋转集覆盖的 `tank1`。
3. 对 800×600 的标准靶板先进行不拉伸正方形补边，再旋转，避免 45° 附近
   裁掉圆形靶板；`H1` 保持原正方形尺寸。
4. 本轮不写入自动 YOLO 标签，仅建立 `labels/README.md`、`README.md` 和
   `manifest.json`，等待后续手工标注；`car` 语义为 `panzer`，`H` 为降落标志，
   不得标为 `red_cross`。

### 验证结果

1. 在 `rl_drone` 环境中通过 `py_compile` 并完成脚本执行。
2. 输出计数为 5 类各 60 张，共 300 张；全部可由 OpenCV 解码。
3. 输出尺寸为标准靶板 800×800、H 标志 2268×2268，未对原图进行比例拉伸。
4. 抽查 bridge 的 000°/042°、car 的 090° 和 H 的 042°，旋转方向、中心保持和
   圆形靶板完整性正常。

### 遗留问题

- `labels/` 当前只有说明文件，没有可直接训练的 YOLO 标签；后续必须人工标注并
  做类别/框质量复核。
- 本素材仍是平面图像旋转增强，不等同于真实无人机视角下的透视变化、缩放、模糊
  或遮挡增强。

### 下一步

先对 `official_first_non_tank_rotated_20260713/images/` 完成手工标注，尤其确认
`car→panzer` 的目标框范围；随后再将经过复核的标签并入 v5 六分类训练集，并单独
评估 H 降落标志是否进入独立检测链。

---

## 2026-07-13（TOUDI3 靶标模型重建与红十字资产）

### 改动范围

- `/home/xhj/PX4-Autopilot/Tools/simulation/gazebo-classic/sitl_gazebo-classic/models/{dibao,qiaoliang,tanke,zhangpeng,zhuangjiache,red_cross}/`
- `model-refine/` 中手工裁切后的 `dibao(1).png`、`qiaoliang(1).png`、`tanke(1).png`、
  `zhangpeng(1).png`、`zhuangjiache(1).png`
- `TOUDI3_GAZEBO_ASSET_SETUP_GUIDE.md`
- `TOUDI3_SIM_ASSET_CHECKLIST.md`
- `TOUDI3_GAZEBO_ASSETS_README.md`
- `toudi3_gazebo_assets_20260713.tar.gz`

### 具体改动

1. 为 `toudi3.world` 引用的 `dibao`、`qiaoliang`、`tanke`、`zhangpeng`、
   `zhuangjiache` 建立完整的 `model.config`、`model.sdf`、OGRE `.material` 和
   `materials/textures` 目录；材质名与 world 中的 `Mark/Diffuse1/3/4/5/6`
   保持一致。
2. 使用用户手工居中裁切的带 `（1）` PNG 作为五类模型纹理，统一为 `600×600`，
   避免把原始 `800×600` 图片直接映射到 `1×1 m` 几何造成圆环拉伸；原始 PNG 不覆盖。
3. 新增 `red_cross` 独立模型，使用 `hongshizi.png`，视觉和碰撞尺寸均为
   `0.35×0.35×0.005 m`，不把它固定插入当前随机靶标尚未确定的 `toudi3.world`。
4. 在仓库根目录生成可分发压缩包，包含六个模型目录、裁切源图和安装说明。

### 验证结果

1. 六个模型的 XML、SDF 均通过 `xml.etree` 和 `gz sdf -k` 检查。
2. `Desktop_patrol_uav_ws-patrol_planner/toudi3.world` 通过 `xmllint`；其十个标准
   `model://.../materials` 引用均能解析到 PX4 models 目录。
3. 五类标准纹理检查为 `600×600`，红十字模型纹理为近方形，六个模型的视觉/碰撞
   平面尺寸分别为标准 `1×1 m` 和红十字 `0.35×0.35 m`。
4. 使用 `GAZEBO_MODEL_PATH` 启动无 GUI `gzserver` 做资源观察，日志中未出现
   `Unable to find uri`、材质、纹理或 OGRE 缺失报错；未启动 PX4、MAVROS、QGC 或执行机构。

### 遗留问题

- `toudi3.world` 的五类靶标几何仍是 world 内嵌模型，模型目录主要提供材质资源和
  可独立 spawn 的模型；红十字仍需后续通过 spawn/场景生成逻辑随机放置。
- 尚未进行 Gazebo GUI 画面人工确认，也未做 PX4 飞行链路联调。

### 下一步

先由组员解压 `toudi3_gazebo_assets_20260713.tar.gz` 并配置 `GAZEBO_MODEL_PATH`，
单独观察五类纹理在 `toudi3.world` 中的显示；之后再实现带固定随机种子的红十字
spawn/场景生成入口。

---

## 2026-07-13（toudi3 完整竞赛仿真入口与旧控制链接入）

### 改动范围

- `patrol_uav_ws-patrol_planner/src/patrol_control/CMakeLists.txt`
- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_world.launch`
- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_full_competition_sim.launch`
- `top_level_scripts/run_toudi3_full_competition_sim.sh`
- 旧圆环、红十字、降落检测 launch 的相机话题/启动参数

### 具体改动

1. 编译 `uav_arming_node.cpp`，作为仅供 SITL 使用的 OFFBOARD/解锁辅助节点；
   原有硬件 launch 不自动包含它。
2. 为 `patrol_world.launch` 增加可配置的 `GAZEBO_PLUGIN_PATH` 和
   `LD_LIBRARY_PATH`，同时覆盖 PX4 构建插件与 AstraDroneOpen 的 MID360/RealSense
   插件目录。
3. 新增 `patrol_full_competition_sim.launch`，串接 toudi3 世界、PX4/MAVROS、旧
   patrol_control/规划、旧圆环/红十字/降落检测以及 FAST-LIO/FreeDOM 仿真链。
4. 为旧视觉 launch 增加相机话题 remap；完整入口关闭圆环 launch 原有的常驻控制发布器，
   避免与 `patrol_control` 的任务状态机争抢 `/detect/control`。
5. 增加一键 SITL 脚本，集中加载 ROS、vision_ws、主工作区、PX4 和 AstraDroneOpen
   环境，减少从宿主 shell 启动时的路径污染。

### 验证结果

1. 旧 world 启动阶段已确认靶板和 `iris_mid360` 能生成；未配置插件路径时 PX4
   startup 返回 2，并报告多个 Gazebo 插件缺失。
2. 已在本机定位到缺失插件的实际来源：AstraDroneOpen `devel/lib` 与 PX4
   `build/px4_sitl_default/build_gazebo-classic`；完整入口已将两者纳入参数化环境。
3. XML 静态检查通过；在临时将 catkin 白名单切换到 `patrol_control` 后，
   `catkin_make --force-cmake -j1` 通过，`uav_arming_node` 及旧视觉节点均成功链接。
4. 尚未完成本次新增总 launch 的持续运行回归。

### 补充修正

一键脚本首次试运行发现 roslaunch 参数误写为 `name=value`，已修正为 ROS1 要求的
`name:=value`；该错误发生在仿真进程启动前，未产生飞控或 Gazebo 进程。

随后发现 ROS launch 不允许在 `optenv` 默认值内嵌套 `arg` 替换，已将 PX4 插件目录
默认值改为由 `px4_root` 直接拼接；脚本仍可通过 `px4_plugin_lib:=...` 覆盖。

### 运行期接线修正

持续运行验证确认 Astra RealSense 插件的实际彩色图像话题为
`/camera/color/image_raw`，已修正完整仿真入口默认 remap；同时根据 Astra Livox
插件源码的枚举定义，将 `mid360.sdf` 的 `publish_pointcloud_type` 从自定义消息 `3`
改为标准 `sensor_msgs/PointCloud2` 类型 `1`，以匹配 FAST-LIO `mid360_sim.yaml`。

### 遗留问题

- `toudi3.world` 的红十字模型目前仍未固定插入，视觉完整链只能验证标准靶板和空场景
  下的节点连接；红十字随机放置仍需单独场景入口。
- 规划器依赖的 `/pcl_render_node/depth` 与 `/pcl_render_node/camera_pose` 在当前
  PX4 世界中未确认有发布者，可能导致 planner 节点启动但无法规划。
- 视觉旧节点仍使用硬编码相机模型和旧几何算法，本次只做话题接线，不等同于新视觉链验收。

### 下一步

先编译 `patrol_control` 并启动 `patrol_full_competition_sim.launch`，记录 PX4 状态、
解锁/起飞、航点输出、规划器、FAST-LIO/FreeDOM、三类视觉节点的实际运行情况；随后
针对缺失话题或崩溃逐项修复。

## 2026-07-13（修正完整仿真入口的 ROS Python 解释器继承）

### 改动范围

- `top_level_scripts/run_toudi3_full_competition_sim.sh`

### 具体改动

完整仿真入口显式使用系统 ROS Noetic 的 `PATH`，并清除 `PYTHONHOME`，避免从
宿主 shell 继承 conda `base` 的 `python3`。`gazebo_ros/spawn_model` 通过
`/usr/bin/env python3` 启动，必须使用系统 Python 才能导入 ROS 的 `yaml/genpy`
依赖。

### 验证结果

已复现原问题：飞机生成节点因 conda Python 缺少 `yaml` 在导入阶段退出，导致
Gazebo 空场继续运行但无飞机、无相机话题，MAVROS 处于未连接状态。修正后需重新
启动整套仿真验证飞机生成、相机发布和 PX4 连接。

### 遗留问题

当前运行实例仍是修正前启动的旧进程，不能作为修正后的验收结果；FAST-Planner
仍可能因缺少旧 `/pcl_render_node/depth` 与 `/pcl_render_node/camera_pose`
输入退出。

### 下一步

完整清理旧仿真进程后重新启动，检查 `iris_mid360`、`/camera/color/image_raw`、
`/livox/lidar`、`/mavros/state` 和旧视觉/控制话题。

## 2026-07-13（完整竞赛仿真切换为旧航路点直飞模式）

### 改动范围

- `patrol_control/launch/patrol_control_px4_sim.launch`
- `patrol_control/launch/patrol_full_competition_sim.launch`

### 具体改动

增加 `waypoint_mode` 参数。旧 PX4 launch 默认保持历史 Fast-Planner 行为；完整
竞赛仿真入口默认启用直飞航路点模式，将 `patrol_sim.yaml` 中的航点直接发送给
MAVROS。规划器节点仍启动以保留完整链路，但不再接管该模式下的飞行 setpoint。

### 验证结果

前一轮完整链路在规划器模式下曾起飞并巡航，但随后位姿跑到场地外且高度跌至地面
以下，PX4 自动解除解锁；这与 `toudi3` 场地和旧规划器默认参数不匹配。切换直飞
模式后需重新验证起飞、航点推进、检测点状态和降落。

### 遗留问题

`patrol_sim.yaml` 包含多段历史任务航点，完整状态机是否会在第一段降落后继续
读取后续段落仍需运行期确认；仿真世界中的红十字尚未插入，视觉检测不会自然产生
红十字目标。

### 下一步

重新全量启动 `patrol_full_competition_sim.launch`，确认直飞模式下飞机不再依赖
规划器输出，并观察旧起降、巡航、检测和降落节点的实际状态转换。

## 2026-07-13（仿真专用旧降落流程安全化）

### 改动范围

- `patrol_control/include/patrol_control/patrol_control.h`
- `patrol_control/src/patrol_control.cpp`
- `patrol_control/launch/patrol_control_px4_sim.launch`
- `patrol_control/launch/patrol_full_competition_sim.launch`

### 具体改动

旧 `CallLand()` 中的 `AUTO.LAND` 调用历史上被注释，随后使用 `-1.0 m` 高度
setpoint 完成降落。新增默认关闭的 `simulation/enable_auto_land` 参数；完整
竞赛仿真默认开启后，在 `auto_land` 配置有效时请求 PX4 `AUTO.LAND`，并将备用
setpoint 保持在安全的 `land_height`。普通旧 launch 默认关闭，不改变实机路径。

### 验证结果

代码修改后需重新编译 `patrol_control`，再做完整 SITL 回归；当前尚未宣称完整
任务已经完成。

### 遗留问题

仿真场景尚未放入红十字模型；FAST-Planner 节点虽然启动，但完整入口的直飞模式
不依赖它。Gazebo 重型模型仍可能使 `spawn_model` 的短等待先报超时，需以模型
插件随后成功加载和话题实际发布作为运行期判据。

### 下一步

编译 `patrol_control` 后重新运行直飞仿真，观察首个降落点是否进入 `AUTO.LAND`
并安全结束，而不是发送负高度。

## 2026-07-13（SITL 直飞 setpoint 步长限幅）

### 改动范围

- `patrol_control/launch/patrol_control_px4_sim.launch`
- `patrol_control/launch/patrol_full_competition_sim.launch`

### 具体改动

增加 `px4_max_distance` launch 参数。通用旧 PX4 launch 默认仍为 `1.0 m`；完整
竞赛仿真直飞模式默认使用 `0.2 m`，降低 WSL CPU 受限时 PX4 位置环接近航点的
过冲风险。该参数只限制发送给 MAVROS 的仿真位置 setpoint 步长，不修改航点坐标。

### 验证结果

此前长回归中首个航点距离降至约 `0.47 m` 后，使用 `1.0 m` 步长的旧直飞链发生
过冲，距离逐步增至约 `200 m`；尚未用 `0.2 m` 步长完成新的长回归。

### 遗留问题

长回归受限时结束前尚未到达降落点，因此 `AUTO.LAND` 分支仍需单独确认；Livox
仿真点云字段缺少 `intensity/time/ring` 的警告仍存在，但 `sensor_msgs/PointCloud2`
连接已建立。

### 下一步

用新步长重新运行完整入口，先确认首个航点不再发散，再观察检测点、降落点和
仿真专用 `AUTO.LAND`。

## 2026-07-13（直飞步长回归结果）

### 验证结果

重新启动完整入口后，确认 `iris_mid360`、RealSense、Livox、PX4/MAVROS、旧控制、
旧视觉、FAST-LIO/FreeDOM 均进入运行状态；Livox 输出为 `sensor_msgs/PointCloud2`
且 `publish_pointcloud_type: 1`，RealSense 插件完成加载。使用仿真直飞步长 `0.2 m`
时，首个航点接近阶段距离从约 `2.0 m` 降至约 `1.65 m`，在观测窗口内未复现旧
`1.0 m` 步长导致的距离发散到约 `200 m`。

### 当前结论

已完成：干净构建、完整入口启动、飞机生成/传感器接线、PX4 解锁与 OFFBOARD、
旧航路点直飞前段回归。未完成：受当前执行器单次长命令时限影响，尚未运行到首个
降落点，`simulation/enable_auto_land` 的实际 `AUTO.LAND` 日志尚未获得；因此本次
不能宣称全流程竞赛任务已验收。

### 仍存在的运行告警

- 重型模型插件加载时，`spawn_model` 的短等待可能先报服务超时，但随后模型和
  RealSense/Livox 插件仍完成加载；应以模型话题和插件日志作为最终判据。
- FAST-LIO 仍提示仿真点云缺少 `intensity/time/ring` 字段；PointCloud2 类型连接
  已正确建立，但字段完整性仍需后续处理。
- 旧视觉节点已接收相机图像，但当前 toudi3 视场未产生可确认的圆环/红十字/H
  目标，不能用本次空场景证明视觉检测精度。

## 2026-07-13（完整仿真 GUI 入口、节点图和无 GUI 旧链回归）

### 改动范围

- `vision_ws/src/uav_vision/launch/phase_d.launch`
- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_full_competition_sim.launch`
- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_world.launch`
- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/px4_toudi3_sitl.launch`
- `patrol_uav_ws-patrol_planner/src/patrol_control/scripts/spawn_model_delayed.py`
- `top_level_scripts/run_toudi3_full_competition_sim*.sh`
- `top_level_scripts/stop_toudi3_sim.sh`
- `docs/TOUDI3_FULL_SIM_GUI_GUIDE.md`
- `vision_ws/toudi3_*_nodes.svg`
- `vision_ws/src/uav_vision/README.md`

### 具体改动

1. 新增旧链 GUI、新链 GUI 和无 GUI 旧链三个傻瓜式入口；无 GUI 入口保持终端前台运行，便于让完整航点流程自然结束。
2. 新 Phase-D launch 增加仿真图像、CameraInfo 和开发 detector Python 前缀参数，接入 AstraDroneOpen 的 `/camera/color/image_raw` 与 `/camera/color/camera_info`。
3. 完整入口只保留一个 RViz，避免 FAST-LIO 和控制 launch 重复拉起同名 RViz；增加 `mapping_rviz` 参数。
4. toudi3 使用仓库内 PX4 SITL launch 副本，将重场景模型生成延迟 10 秒，避免 PX4 原始 `spawn_model` 在静态世界尚未完成时超时；清理脚本改为精确进程名匹配。
5. 新增完整节点总图、旧链图、新视觉链图；操作手册改为进入 WSL 后使用 Linux 原生命令，并明确 3 个终端的开启顺序、观察命令和 RViz 订阅步骤。

### 验证结果

- `vision_ws` `catkin_make -j1`：通过，视觉节点构建达到 100%。
- `patrol_uav_ws-patrol_planner` `catkin_make -DROS_EDITION=ROS1 -j1`：通过，主工作区达到 100%。
- GUI 启动：延迟生成适配后，`toudi3.world`、Gazebo GUI、PX4、MAVROS、RViz、旧控制、旧视觉和地图节点均进入启动流程；重型 GUI 运行中 `fast_planner_node` 曾被系统以 `-9` 终止，未宣称 GUI 完整赛程通过。
- 无 GUI 旧链运行 180 秒：模型生成成功，PX4/MAVROS 连接、自动解锁、OFFBOARD、起飞、旧视觉、FAST-LIO/FreeDOM 均可见；首个起飞点完成并进入第二航点阶段。
- 无 GUI 旧链在本窗口内未到首个降落点；第二航点距离从约 `5.5 m` 增至约 `8.9 m`，测试以 `SIM_EXIT_CODE=124` 结束。因此不能宣称完整比赛赛程或 `AUTO.LAND` 已验收。

### 遗留问题

- 直飞旧控制在第二航点阶段出现距离增大，需保存 `/mavros/local_position/pose` 与 `/mavros/setpoint_position/local` 同步轨迹，判断坐标方向、PX4 本地原点或 setpoint 插值问题。
- `fast_planner_node` 在资源压力下 `exit code -9`；直飞模式不依赖其控制输出，但“所有规划节点稳定运行”尚未满足。
- AstraDroneOpen MID360 输出 PointCloud2 仍缺少 `intensity/time/ring` 字段，FAST-LIO/FreeDOM 以降级方式运行。
- toudi3 静态场景当前未提供足够可确认的标准靶标/红十字视觉观测，旧视觉控制不能通过空场景证明识别与投递正确。

### 下一步

先做一次短时同步采样，比较第二航点的当前位置、目标点和 MAVROS setpoint；在修正坐标/运动方向前，不继续把延长运行时间当作完整赛程验收依据。随后再分别验证无 GUI 旧链全程和新视觉链话题闭环。

## 2026-07-13（无 GUI 旧链完整赛程长回归确认）

### 改动范围

- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_control_px4_sim.launch`
- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_full_competition_sim.launch`
- `docs/TOUDI3_FULL_SIM_GUI_GUIDE.md`
- `vision_ws/src/uav_vision/README.md`

### 具体改动

1. 为通用旧 PX4 仿真 launch 增加可配置的 `aligning_threshold`，默认仍保留历史
   `0.05 m`；完整 toudi3 SITL 入口显式使用 `0.20 m`，仅解决 WSL/Gazebo 姿态
   稳态误差导致首个检测点无法进入超时分支的问题。
2. 手册改为进入 WSL 后直接使用 Linux 原生命令，明确终端 1/2/3 的职责、完成后
   roslaunch 不自动退出的处理方式，并记录本次长回归实测结果。

### 验证结果

- `catkin_make -DROS_EDITION=ROS1 -j1`：主工作区通过，100%。
- 无 GUI 旧链长回归启动了 `/gazebo`、`/mavros`、`/patrol_control`、旧圆环/红十字/
  降落视觉、`/laserMapping`、`/freedom`、`/fast_planner_node`、`/traj_server`、
  `/waypoint_generator` 等完整节点。
- PX4/MAVROS 进入 `connected: True`、`armed: True`、`OFFBOARD`；飞机完成起飞、
  首段巡航、两个 `Detect_point` 的无目标超时切换，并抵达最终着陆航点。
- 最终日志出现 `LandDetectDone timeout`、`Have Land!`、`Vehicle DisArmed!`；
  最终观测为 `armed: false`，位姿约为 `(x=0.13, y=0.06, z=0.24)`。记录后用
  `stop_toudi3_sim.sh` 清理了 SITL。

### 遗留问题

- 本次控制台没有捕获 `Simulation AUTO.LAND mode enabled` 的显式成功日志；最终已
  验证降落/解锁状态，但 PX4 `AUTO.LAND` 服务响应仍需单独复核，不能把该日志缺失
  写成 AUTO.LAND 已成功响应。
- toudi3 世界没有足够可确认的标准靶标、红十字和 H 视觉目标，本次检测点通过的是
  旧链无目标超时，不是视觉识别或投递得分验收。
- FAST-LIO 仍提示仿真点云缺少 `intensity/time/ring`；节点可运行，但字段兼容性
  仍待修复。

### 下一步

单独做一次最小 PX4/MAVROS `set_mode` 服务诊断，确认 `AUTO.LAND` 失败/未记录的
具体原因；视觉组则继续以真实视频回放和 mock 为主，不把空场景仿真当作视觉精度证据。

## 2026-07-14（tmux 审计与项目文档口径统一）

### 改动范围

- `README.md`
- `AGENTS.md`
- `VISION_2026_COMPLETE_DEVELOPMENT_PLAN.md`
- `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`
- `VISION_CHAIN_COMPARISON.md`
- `SIMULATION_GUIDE_NOETIC_PX4_GAZEBO_QGC.md`
- `TOUDI3_SIM_ASSET_CHECKLIST.md`
- `docs/TOUDI3_FULL_SIM_GUI_GUIDE.md`
- `docs/VISION_PROGRESS_ASSESSMENT_20260713.md`
- `docs/当前问题与责任边界.md`
- `docs/TEST_DATA_CLEANUP_PLAN_20260713.md`

### 具体改动

1. 记录 tmux 的实际使用边界：本次长时间无 GUI 旧链回归曾用 tmux 规避单次命令时限；当前没有 tmux 会话，手册仍按进入 WSL 后的三终端 Linux 原生命令组织，tmux 仅作为可选长任务承载方式。
2. 将旧的 `top_level_scripts/simulation.sh`、`vision.sh`、`location.sh` 等启动脚本标为历史入口；它们可能启动实机视觉、`actuator_pwm` 或旧路径，不作为当前 toudi3 仿真推荐入口。
3. 统一 2026-07-14 状态：旧 toudi3 控制链已完成无 GUI 起飞—巡航—降落回归；新视觉链、目标场景视觉识别、地图投影误差、投递闭环和显式 `AUTO.LAND` 服务成功仍未验收。
4. 统一资产文档分工：`TOUDI3_SIM_ASSET_CHECKLIST.md` 作为快速状态清单，`TOUDI3_GAZEBO_ASSET_SETUP_GUIDE.md` 负责资产重建细节，`docs/TOUDI3_FULL_SIM_GUI_GUIDE.md` 负责当前启动与观察流程。
5. 将下一阶段收敛为：真实视频圆环真值/外圈 ROI 与类别关联；同步 `CameraInfo/TF/odom` 的地图投影和稳定 ID；H、投递、降落阶段门控；旧接口回归；最后再做 RKNN/OrangePi 联调。
6. 复核文档和数据目录后未删除文件：候选文档均有独立历史证据或仍被其他文档引用；`test_data` 也只更新清理状态，待引用迁移和归档条件满足后再逐组处理。

### 验证结果

- `tmux ls`：确认当前无运行会话。
- 关键文档中的 `patrol_world.launch`、tmux 说明、当前日期和下一阶段口径已复核。
- 本轮仅修改文档，未修改代码、launch、模型或数据；不需要重新编译，也未主动启动仿真、执行器或真实硬件。复核时发现旧 toudi3 的 `fastlio_mapping`、`gzserver`、`gzclient` 和等待中的 `rostopic` 残留，已使用仓库停止脚本并定向停止对应本地仿真进程；复核后 tmux、Gazebo 和相关残留均已清空。

### 遗留问题

- `TOUDI3_SIM_ASSET_CHECKLIST.md` 中仍保留外部 PX4 模型路径检查，这是面向分发/安装后的资产核验，不是当前仓库的推荐 launch 入口。
- 新视觉链尚未用带同步相机内参、TF、位姿的真实回放完成地图误差闭环；空场景仿真不能替代该验收。

### 下一步

先建立真实视频的圆环外圈真值表和误检分类样本，修正外圈 ROI、蓝色掩膜、椭圆/圆环质量门限及类别—圆环关联；随后用 mock 或 rosbag 注入同步 `CameraInfo/TF/odom`，验收地图点、稳定 ID 和投递对准，再进入阶段状态机与 RKNN 部署。

## 2026-07-14（toudi3 航路坐标修正与 H 降落模型接入）

### 改动范围

- `patrol_uav_ws-patrol_planner/src/patrol_control/config/patrol_toudi3.yaml`
- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_control_px4_sim.launch`
- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_full_competition_sim.launch`
- `patrol_uav_ws-patrol_planner/src/patrol_control/launch/patrol_world.launch`
- `patrol_uav_ws-patrol_planner/toudi3.world`
- `Desktop_patrol_uav_ws-patrol_planner/toudi3.world`
- `top_level_scripts/run_toudi3_full_competition_sim.sh`
- `top_level_scripts/run_toudi3_full_competition_sim_gui_new.sh`
- `/home/xhj/PX4-Autopilot/Tools/simulation/gazebo-classic/sitl_gazebo-classic/models/landing_h/`
- `toudi3_gazebo_assets_20260714.tar.gz`
- `TOUDI3_GAZEBO_ASSET_SETUP_GUIDE.md`
- `TOUDI3_SIM_ASSET_CHECKLIST.md`
- `SIMULATION_GUIDE_NOETIC_PX4_GAZEBO_QGC.md`
- `README.md`
- `AGENTS.md`
- `docs/TOUDI3_FULL_SIM_GUI_GUIDE.md`

### 具体改动

1. 明确区分两个入口：`patrol_world.launch` 只启动 world/PX4/Gazebo/MAVROS，不发布航路点；完整旧链必须使用 `run_toudi3_full_competition_sim_gui_old.sh` 或无 GUI 入口。
2. 新增 `patrol_toudi3.yaml`，将 3 个旧控制兼容检测点对齐到 world 中 `dibao=(-0.602,-1.041)`、`tanke=(-1.903,-0.023)`、`zhuangjiache=(1.016,0.256)` 的绝对中心，并以 `(0,0)` 作为返航/H 降落点。
3. 通过 `waypoint_config` 让完整 toudi3 launch 默认使用专用 8 点路线；历史多段 51 点配置 `patrol_sim.yaml` 仍保留为旧入口兼容配置，不再作为 toudi3 完整入口默认路线。
4. 将两个 toudi3 world 都加入 `landing_h`，使用 `model-refine/Hjiangluo.png` 构建水平 `1×1 m` 贴图板。该尺寸是仿真假设，规则书未给出 H 贴纸独立尺寸；H 只在最终 `Land_point` 阶段由旧降落检测控制链启用。
5. 修正 `patrol_world.launch` world-only 分支错误的节点包名：`spawn_model_delayed.py` 归属 `patrol_control`，不归属 `gazebo_ros`。
6. 更新 GUI/无 GUI 手册，加入航路点参数核验、setpoint 话题核验、world-only 与 full-chain 的区别、H 触发专项检查和旧控制“三检测点后跳末尾三点”的限制说明。
7. 生成包含六类模型、H 源图、world 和专用航路配置的根目录分发包 `toudi3_gazebo_assets_20260714.tar.gz`。

### 验证结果

- `catkin_make -DROS_EDITION=ROS1 -j1`：主工作区 100% 通过。
- 两份 world 和三个 launch 均通过 `xmllint --noout`。
- `roslaunch --dump-params patrol_control patrol_full_competition_sim.launch`：成功解析，`/waypoints` 为专用 8 点路线，`/waypoint_skipping_index=3`，`/switch/flag_planner_px4=true`。
- world-only 启动验证：修正前确实出现 `Cannot locate node of type [spawn_model_delayed.py] in package [gazebo_ros]`；修正后节点显示为 `patrol_control/spawn_model_delayed.py`，无该缺失脚本错误。
- Gazebo `/gazebo/get_world_properties` 返回 `landing_h`，证明 H 模型已被 world 加载。
- 短时完整旧链启动日志出现 `patrol_control` 和 `Send point to take off`；本次未把短时启动宣称为完整赛程验收。
- 仿真相关进程已清理；未启动 `actuator_pwm`，未连接真实飞控，未执行真实舵机动作。

### 遗留问题

- 当前路线只覆盖 3 个标准检测点，因为旧 `patrol_control` 的投递计数、舵机映射和跳点逻辑按 3 点设计；`qiaoliang`、`zhangpeng` 虽在 world 中，但尚未纳入旧链完整投递路线。
- H 模型的 `1×1 m` 仅用于仿真触发链验证；正式降落区尺寸需以规则补充或实物测量确认。
- 旧 `landing_detector_node` 仍使用硬编码相机模型，H 触发成功不代表地图投影精度或真实降落精度已验收。
- FAST-LIO 点云字段缺少 `intensity/time/ring` 等历史警告仍存在。

### 下一步

先按新手册分别运行 world-only 和 full-chain 两个入口，确认用户侧能看到：飞机模型、H、持续 setpoint、`Send point to take off` 和最终 `landing_control`；之后再把 H 触发从旧外圈检测升级为 H 结构质量门控，并继续视觉组的真实视频圆环关联与地图投影工作。

## 2026-07-14（H 贴图 MIME 修复与官方旋转标注区域增强导入）

### 改动范围

- `model-refine/Hjiangluo.png`
- `model-refine/Hjiangluo_source.jpg`
- `/home/xhj/PX4-Autopilot/Tools/simulation/gazebo-classic/sitl_gazebo-classic/models/landing_h/materials/textures/Hjiangluo.png`
- `vision_ws/scripts/convert_texture_to_png.py`
- `vision_ws/scripts/augment_yolo_region_focus.py`
- `vision_ws/test_data/yolo_dataset_v5_official_rotated_region_aug_20260714/`
- `docs/OFFICIAL_DATASET_REGION_AUG_20260714.md`
- `vision_ws/README.md`
- `README.md`
- `TOUDI3_GAZEBO_ASSET_SETUP_GUIDE.md`
- `TOUDI3_SIM_ASSET_CHECKLIST.md`
- `docs/TOUDI3_FULL_SIM_GUI_GUIDE.md`

### 具体改动

1. 发现 `Hjiangluo.png` 的文件内容实际是 JPEG。原始文件保留为
   `Hjiangluo_source.jpg`，并通过 `rl_drone` 环境转换出真实 PNG；仓库模型资产和
   PX4 外部 `landing_h` 模型贴图已同步，避免 Gazebo 因 MIME 不匹配而使用回退/错误
   材质。
2. 新增官方旋转标注集导入与区域聚焦增强脚本。压缩包的 240 张标注图映射到六分类
   的前四类；`tank`、`red_cross` 无样本，不补造标签。
3. 每张原图生成一份局部模糊、一份标注区域内部分擦除、一份围绕标注框的局部裁切，
   共新增 720 张。局部操作依据 YOLO 标注框，不再把擦除/模糊随机施加到整幅背景。
4. 扩展集使用既有 v5 验证集，不随机拆分旋转近重复图，暂不替换默认权重。

### 验证结果

- `file model-refine/Hjiangluo.png` 和 PX4 外部贴图均显示 `PNG image data`。
- 两处 H 贴图 SHA256 一致。
- 无 GUI world-only 启动后，`/gazebo/get_world_properties` 返回模型 `landing_h`；日志未出现 H 贴图/材质错误。
- 区域增强脚本在 `rl_drone` 环境中成功运行：原图 240、区域模糊 240、区域擦除 240、
  区域裁切 240，训练目录共 960 张图像及 960 个标签。
- 生成 `data.yaml` 维持 `bridge/panzer/pillbox/tent/tank/red_cross` 六分类顺序，
  验证路径指向既有 v5 `images/val`。

### 遗留问题

- 当前仅完成资产加载和静态数据增强验证，尚未用 GUI 重新目视确认 H 贴图外观，也未重训
  模型；用户若仍看到旧贴图，需要先停止旧 `gzserver/gzclient` 再启动。
- 区域擦除比例和局部裁切尺度仍需用真实飞行视频做召回/误检消融，不能直接假设增强一定提升效果。

### 下一步

先抽检三类增强图和标签叠加效果，再用官方扩展集做基线对照训练；随后在真实视频上比较
标准靶标召回、圆环关联和起降阶段误检，决定是否将扩展集纳入默认训练权重。

## 2026-07-14（六分类增强读取修复与 v5 合并集重建）

### 改动范围

- `vision_ws/scripts/augment_yolo_region_focus.py`
- `vision_ws/scripts/augment_yolo_region_focus_dataset.py`
- `vision_ws/scripts/merge_official_into_v5.py`
- `vision_ws/scripts/run_region_focus_aug_20260714.sh`
- `vision_ws/scripts/validate_yolo_dataset.py`
- `vision_ws/test_data/yolo_dataset_v5_6cls_redcross_standard_20260713/`
- `vision_ws/test_data/yolo_dataset_v5_region_focus_aug_20260714/`
- `vision_ws/README.md`
- `README.md`
- `docs/OFFICIAL_DATASET_REGION_AUG_20260714.md`

### 具体改动

1. 修正区域增强脚本复用四分类读取函数导致 `got 5` 的错误：官方 zip 导入仍限制
   `0..3`，合并后的 v5 增强读取允许六分类 `0..5`。
2. 清理错误的独立官方增强目录，将官方 240 张原图直接加入 v5 标准训练集；v5
   训练集由 923 张增至 1163 张，原 232 张验证集保持不变。
3. 从合并后的 v5 全集生成区域聚焦增强集：1162 个有标注训练样本分别生成局部模糊、
   局部擦除、局部裁切；1 个空标签 tank hard negative 仅保留原图，不伪造增强区域。
4. 新增 YOLO 数据集配对、类别 ID、归一化范围和空标签校验脚本；使用 tmux 承载批量
   离线处理，避免单次 shell 时限中断。

### 验证结果

- 合并 v5：训练 `1163 images/1163 labels`，验证 `232 images/232 labels`，无孤立文件、
  无非法标签；训练集仅记录 1 个空标签 hard negative。
- 区域增强集：训练 `4649 images/4649 labels`，验证 `232 images/232 labels`，无孤立文件、
  无非法类别或越界归一化值。
- 增强 manifest：`region_blur=1162`、`region_erase=1162`、`region_crop=1162`，
  总增强图 3486，训练总图 4649。
- 已抽检模糊、擦除和裁切图像，变换集中于标注目标区域；未训练模型，未启动仿真。

### 遗留问题

- 空标签 tank 样本需要人工确认是有意 hard negative 还是漏标；在确认前保持原图、不做
  区域增强是安全策略。
- 增强策略尚未通过真实视频召回率/误检率消融，不能直接替换默认权重。

### 下一步

人工确认空标签样本后，使用合并 v5 与区域增强集做不训练的标签/图像抽检，再决定训练
对照实验；训练完成前不修改 dev/sim 默认模型。

## 2026-07-14（v5 标准集/区域增强集训练与真实视频双模型完整视觉链回放）

### 改动范围

- `vision_ws/runs/liftrace_6cls_v5_merged_standard_20260714/`
- `vision_ws/runs/liftrace_6cls_v5_region_focus_aug_20260714/`
- `vision_ws/test_data/real_target_infer_v5_merged_standard_20260714/`
- `vision_ws/test_data/real_target_infer_v5_region_focus_aug_20260714/`
- `vision_ws/test_data/real_target_full_chain_v5_merged_standard_20260714_stride4/`
- `vision_ws/test_data/real_target_full_chain_v5_region_focus_aug_20260714_stride4/`

### 具体改动

1. 使用合并后的 v5 六分类标准集
   `yolo_dataset_v5_6cls_redcross_standard_20260713/data.yaml` 训练 YOLO11n；类别顺序
   固定为 `bridge/panzer/pillbox/tent/tank/red_cross`。
2. 使用区域重点增强集
   `yolo_dataset_v5_region_focus_aug_20260714/data.yaml` 进行同配置对照训练。两次训练
   均使用 `epochs=100`、`patience=20`、`batch=16`、`imgsz=640`、GPU `0`，未覆盖旧
   权重，未修改当前 dev/sim 默认模型配置。
3. 在同一段真实视频 `vision_ws/test_data/real_target.mp4` 上分别完成：
   - YOLO 单独逐帧推理报告；
   - 当前完整视觉链离线等效回放：YOLO + `red_cross` 几何链 + `landing_pad(H)` 几何链。
   两次完整链均使用 `frame_stride=4`、`conf=0.25`、`imgsz=640`，并关闭 bridge 抑制。

### 训练结果

| 实验 | 训练停止轮次 | 最佳轮次 | 独立重验验证集 P/R/mAP50/mAP50-95 |
|---|---:|---:|---|
| v5 合并标准集 | 98 | 78 | `0.998/1.000/0.995/0.980` |
| v5 区域重点增强集 | 33 | 13 | `0.990/0.999/0.995/0.979` |

两次训练都触发了早停。当前 232 张验证集同源且每图基本只有一个目标，不能据此证明
区域增强模型在真实飞行条件下必然更好。两模型 `red_cross` 的验证 mAP50-95 均约为
`0.911`，该类验证实例只有 19 个，仍需真实视频和压力集复核。

### 真实视频 YOLO 单独推理结果

| 模型 | 有检测帧 | 覆盖率 | 类别计数（bridge/panzer/pillbox/tank/red_cross） |
|---|---:|---:|---|
| 合并标准集 | `3474/4623` | `75.15%` | `831/717/469/499/1238` |
| 区域增强集 | `3687/4623` | `79.75%` | `666/500/628/732/1326` |

两模型在该视频中均没有输出 `tent`；这只能说明本段视频未产生该类别输出，不能直接
推断 `tent` 召回率。区域增强模型覆盖率提高约 4.6 个百分点，但类别计数发生明显
变化，尤其 `panzer/bridge` 减少而 `tank/pillbox/red_cross` 增加，可能包含真实召回
改善，也可能包含类别边界变化，必须抽查差异帧。

### 完整视觉链回放结果

| 模型 | 完整链有输出帧 | YOLO 输出帧 | `red_cross` 几何帧 | `landing_pad(H)` 几何帧 | bridge+red_cross | bridge+landing_pad |
|---|---:|---:|---:|---:|---:|---:|
| 合并标准集 | `940/1156` | 939 | 515 | 359 | 181 | 69 |
| 区域增强集 | `983/1156` | 963 | 515 | 359 | 163 | 59 |

`red_cross` 与 `landing_pad` 两列完全一致，符合它们由独立传统几何链产生、未使用
YOLO 模型的实现事实。两次均为 `suppressed_bridge_frames=0`，保留了真实视频中的原始
共现统计；v3 中因复用桥梁底座产生的历史冲突不作为正式比赛规则依据。

### 验证结果

- 两个模型的 YOLO 单独推理均完成 4623 帧，并生成 `summary.json`、CSV、抽样帧和标注视频。
- 两个模型的完整视觉链均完成 1156 帧，并生成完整链标注视频、逐帧摘要、检测明细和
  事件样本。
- 回放仅为离线视觉链等效评测，不包含 ROS 实时时间同步、CameraInfo/TF、地图投影、
  `target_memory` 和主控状态机，不能替代实机或仿真验收。

### 遗留问题

- 当前真实视频没有逐帧真值，`frames_with_detection`、类别计数和几何触发帧不是精确率/召回率。
- 区域增强模型虽然覆盖率更高，但类别分布变化明显，尚未证明其对投递靶心、圆环关联或
  运动模糊场景更可靠。
- `red_cross`/`H` 几何链仍可能与 YOLO `bridge` 同帧共现；当前不做 bridge 抑制，后续由
  任务阶段门控决定控制所有权。
- 尚未将任一新权重切换为默认运行权重，也未进行 RKNN 导出或 OrangePi 性能验收。

### 下一步

先对两组完整链 CSV 做差异帧对齐和人工抽检，重点检查 `panzer/bridge/tank/red_cross`
类别变化、低空模糊和 H 阶段误报；随后补建独立压力测试集并重新比较。模型选定后再接入
`target_refiner` 的圆环中心、地图投影和 `target_memory`，最后才切换默认权重并进行 RKNN
验证。

## 2026-07-14（独立压力验证集与双模型条件评估）

### 改动范围
视觉数据验证脚本、压力集产物、两款 v5 模型评估，以及规划/进度文档同步。

### 具体改动
- 新增 build_yolo_stress_validation.py：从 v5 合并标准集 val 的 232 张图生成 8 类压力条件；
- 新增 eval_yolo_stress_validation.py：按 condition YAML 调用 Ultralytics 验证；
- 生成 yolo_stress_val_v5_20260714，共 1856 张图，images/train 为空，图像/标签配对和标签范围通过检查；
- 条件覆盖运动模糊、旋转、局部裁切、目标变小、强光、暗光、遮挡、多目标同帧；
- 使用 merged_standard 和 region_focus_aug 两款 20260714 模型完成逐条件 GPU 评估；
- 同步训练评估、数据集计划、进度评估、总体开发计划、OrangePi 执行计划、vision_ws README 和根 README。

### 验证结果
两个脚本均通过 python -m py_compile；压力集为 232 x 8 = 1856，图像/标签 1856 对，非法标签 0，训练图像 0。标准模型压力 mAP50-95 顺序为运动模糊 0.977、旋转 0.242、局部裁切 0.953、目标变小 0.958、强光 0.979、暗光 0.981、遮挡 0.980、多目标同帧 0.950；区域增强模型为 0.971、0.234、0.959、0.940、0.976、0.973、0.976、0.938。

### 遗留问题
压力集由 held-out val 派生，是合成压力验证集，不是新的真实独立测试集；旋转条件两款模型高 IoU 定位均弱；区域增强没有全条件优势；多目标为四图拼接，不能替代真实多目标/遮挡视频。

### 下一步
先抽查压力集旋转、小目标、多目标的标注与预测可视化，再用真实视频标注代表帧复核；随后继续圆环中心/类别关联、CameraInfo+TF 地图投影、目标记忆和 H/投递阶段门控，最后再进入 RKNN 候选冻结与板端验证。

## 2026-07-14（控制流审计、双 ONNX 导出与视觉主线收口）

### 改动范围

旧机载控制流、LIO/Planner 边界、视觉组下一阶段规划、两款 v5 候选 ONNX 导出及相关文档同步。

### 具体改动

- 核对 `patrol_control.cpp`：确认旧链是 YAML 固定航点任务，红十字/tank 为巡航机会式中断，标准靶主要在预设 Detect_point 处理；当前没有全场自主搜索管理器；
- 核对 `LandDetectDone()`：15 秒超时分支直接返回成功，记录为控制侧高风险语义，不在本次直接修改执行机构逻辑；
- 核对 `selectedTargetCallback()`：当前主要更新类别目标和 selected 状态，没有使用 `TargetCandidate.map_point` 生成接近目标；
- 核对 FAST-LIO/Fast-Planner 接线：FAST-LIO 发布 `/Odometry` 和 `camera_init → body`，主控/规划仍主要消费 MAVROS local pose；FAST-LIO 配置存在 `pcd_save_en=true、interval=-1`，部分发布队列为 100000；
- 导出两款六分类候选 ONNX：`merged_standard` 与 `region_focus_aug`，均为 opset 12、batch=1、640 输入、关闭 NMS；
- 同步总体开发规划、OrangePi 执行规划、视觉进度评估、问题与责任边界、训练评估、vision_ws README 和根 README。

### 验证结果

- ONNX 文件：
  - `vision_ws/runs/liftrace_6cls_v5_merged_standard_20260714/weights/best.onnx`
  - `vision_ws/runs/liftrace_6cls_v5_region_focus_aug_20260714/weights/best.onnx`
- 两个文件均约 10.1 MB；输入为 `1×3×640×640`，输出为 `1×10×8400`；
- `onnx.checker.check_model` 和 ONNX Runtime CPU 加载均通过；metadata 六分类顺序为 `bridge/panzer/pillbox/tent/tank/red_cross`；
- 未执行 RKNN 导出、板端运行或飞控/执行机构动作。

### 遗留问题

- ONNX 只是 RKNN 转换候选，后处理一致性、量化误差和 OrangePi 性能尚未验证；
- 全场搜索、候选接近、搜索恢复、剩余时间调度和最终释放/降落互锁仍属控制/规划侧缺口；
- 视觉侧仍需真实 CameraInfo/TF/odom 回放、圆环实例关联、地图投影误差和 H 语义证据。

### 下一步

视觉组先完成地图点可消费契约、类别—圆环关联、H 语义拒绝逻辑、`drop_ready/release_permission` 分离和旧接口回归；控制/规划组并行实现 Search/Mission Manager、唯一位姿源和安全任务状态机。

## 2026-07-14（执行规划顺序收口）

### 改动范围

OrangePi 5 Plus 执行规划中的当前建议顺序。

### 具体改动

- 将视觉组当前顺序统一为：真实视频/CameraInfo/TF/odom 地图投影回放、圆环实例关联、H 语义与阶段门控、`drop_ready → release_permission`、旧接口回归和 ONNX 后处理一致性；
- 明确控制/规划组并行承担 Search/Mission Manager、固定航点恢复、LIO→PX4 唯一位姿和局部规划稳定性；
- 明确完整仿真是接口与整机联调工具，不再作为视觉组当前开发的前置条件。

### 验证结果

已同步 `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`，与总体规划、视觉进度评估和责任边界文档的最新结论一致。

### 遗留问题

视觉地图投影误差、圆环关联、H 语义拒绝和释放许可接口仍未完成真实回放验收；ONNX 尚未进行 RKNN 转换和 OrangePi 性能测试。

### 下一步

先建立可重复的真实视频 + CameraInfo/TF/odom 回放样本，并以地图点、圆环实例和阶段门控作为视觉组下一轮验收入口。

## 2026-07-14（当前规划口径清理）

### 改动范围

总体开发规划和视觉责任边界文档中的当前模型、板端和检测器表述。

### 具体改动

- 将当前视觉候选明确为六分类；v3 五分类仅保留为历史离线对照；
- 将 RKNN 状态明确为“尚未配置六分类默认产物、尚未完成板端验证”，并记录两款 ONNX 只是转换候选；
- 将 B01 从“冻结五分类模型”改为记录六分类候选、metadata、校验和及后处理/板端门禁，不以二选一阻塞视觉主线；
- 保留历史文档和评测中的“五分类”措辞，避免篡改历史结果，同时修正当前结论段落。

### 验证结果

已检查总体规划、OrangePi 执行规划、进度评估和责任边界文档；当前执行口径统一为六分类候选 + ONNX 已导出 + RKNN 未验收。

### 遗留问题

历史文档仍会出现五分类字样，但均属于 v3 基线、旧链回放或历史台账；后续若重用这些段落，需明确其时间和模型范围。

### 下一步

继续视觉组真实回放闭环，不追加无证据的模型训练；先完成地图投影、圆环关联、H 语义门控和释放许可接口。

## 2026-07-15（视觉文档收口与仿真验收主路线）

### 改动范围

项目总览、agent 接手规则、视觉架构与任务路线、仿真分层、工作区职责、迁移 Gate、OrangePi 部署、`uav_vision` 包说明、toudi3 操作说明和历史文档定位。

### 具体改动

- 新增 `VISION_2026_ROADMAP.md`，作为视觉组唯一任务优先级来源，统一目标架构、业务边界、L0-L3 仿真验证金字塔、Gazebo 独立真值设计、首轮指标 Gate、30-seed 场景方向和 V-SIM/V-ALG/V-REAL/V-DEPLOY 任务顺序；
- 删除与执行计划、迁移清单和进度评估大量重复的 `VISION_2026_COMPLETE_DEVELOPMENT_PLAN.md`，其有效架构、门禁和任务内容已收敛到新主路线；
- 重写 `README.md`，只保留项目状态、当前缺口、最近里程碑、入口选择和文档导航；
- 重写 `SIMULATION_GUIDE_NOETIC_PX4_GAZEBO_QGC.md`，明确现有新视觉 GUI 入口只是连通烟测，正式算法验收必须具备独立真值、自动报告、固定 seed 和 shadow 隔离；
- 重写 `VISION_WORKSPACE_GUIDE.md`，划分在线运行包 `uav_vision` 与规划中的评测包 `uav_vision_eval`；
- 将 `VISION_MIGRATION_CHECKLIST.md` 改为 M0-M7 发布 Gate，将 `VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md` 收口为 RKNN/OrangePi 部署 Gate；
- 重写 `vision_ws/README.md`、`vision_ws/src/uav_vision/README.md` 和 `docs/当前问题与责任边界.md`，明确 `selected_target` 是候选建议、`drop_ready` 是视觉对准观测、最终 `release_permission` 属于任务/安全层；
- 更新 `AGENTS.md` 的接手顺序、当前卡点和建议优先级，视觉组先建立可量化仿真，再修圆环、投影/记忆、H 和释放证据；
- 为新旧链对照、数据计划、进度评估和规则适配文档增加“历史/专题文档”定位，避免它们继续维护冲突的下一步；
- 修正 `VISION_CHAIN_COMPARISON.md` 和规则适配文档中已过时的 `priority_bridge=0` 口径；当前 `target_memory.yaml` 实际为 `priority_bridge=2.0`；
- 在 `docs/TOUDI3_FULL_SIM_GUI_GUIDE.md` 增加视觉算法结论边界，禁止把 GUI 框、topic 有输出或一次固定航线完成当作精度验收。

### 验证结果

- 已确认新主路线存在、旧冗余规划已删除；
- 已确认文档引用的现有 mock launch、toudi3 新视觉 launch 和 GUI 启动脚本文件均存在；
- `roslaunch --files` 在显式加入 `vision_ws/src`、主工作区 `src` 和 PX4 Gazebo package path 后可解析两个 mock launch 与 toudi3 新视觉 launch，GUI 脚本通过 `bash -n`；
- 进一步发现当前两个独立 catkin `setup.bash` 会相互覆盖：vision→main 后找不到 `uav_vision`，main→vision 后找不到 `patrol_control`；因此文档已把 V-SIM-00 联合环境修复列为首个任务，并撤回“新入口可直接运行”的表述；
- 扫描本轮文档中的 ASCII 绝对 Markdown 路径，未发现缺失目标；
- 扫描除历史变更台账外的旧完整规划引用，只保留新主路线中的删除说明；
- 复核 `vision_ws/src/uav_vision/config/target_memory.yaml`，当前 `priority_bridge=2.0`，相关活文档口径已一致；
- 本轮只修订文档，没有修改代码、launch、yaml 或模型，没有编译、启动仿真、连接硬件或执行飞控/投递动作。

### 遗留问题

- `uav_vision_eval`、Gazebo target catalog、真值投影器、自动 recorder/report 和 headless shadow 入口仍是规划交付物，尚未实现；
- 双 catkin 工作区联合环境尚未修复；手工补齐 `ROS_PACKAGE_PATH` 只证明 launch 文件能解析，未证明生成消息、Python/动态库和运行时环境完整；
- 首轮召回率、中心误差、地图误差、时延等阈值属于工程门禁，需要首版固定场景基线后用变更记录有依据地调整；
- 现有 toudi3 新视觉入口仍会与旧主控接线，未完成 shadow 隔离前只用于烟测；
- 圆环实拍真值、地图投影误差、H 结构、观测新鲜度、释放证据和 OrangePi RKNN 实测仍未通过对应 Gate。

### 下一步

先执行 `VISION_2026_ROADMAP.md` 的 V-SIM-00：建立并验证双 catkin 工作区联合 overlay/环境入口，保证两个包可同时发现、`uav_vision.msg` 可由实际 Python 导入、三个关键 launch 可解析；随后执行 V-SIM-01，建立最小 `uav_vision_eval`、target catalog 和 3 个固定场景。

## 2026-07-15（V-SIM-00～03 落地、视觉业务 Gate 基线与笔记本/板端边界收口）

### 改动范围

联合 catkin 环境、`uav_vision_eval`、固定 Gazebo 评测、shadow 隔离、检测融合/精修、
地图记忆、H 结构、释放证据、圆环实拍回放、PT/ONNX 对照、L0 回归和视觉活文档。

### 具体改动

- 新增/完善 `toudi3_combined_env.sh`、联合构建与只解析验证，解决 vision/main 两个 catkin
  setup 相互覆盖；
- 建立 `uav_vision_eval`：target catalog、固定标准靶/红十字/H/背景场景、Gazebo
  CameraInfo/TF 真值投影、CSV/JSON/report、固定视觉-only runner 和 shadow contract；
- shadow 将所有新视觉、legacy 和 release evidence 输出移到 `/uav_vision_shadow/*`，保护
  全局旧控制话题；默认不启动 PX4/MAVROS/arming/control/`actuator_pwm` 的静态 suite；
- `detection_fusion` 按源图像时间聚合，增加同类去重、阶段门控和 red_cross YOLO+几何
  双确认；`target_refiner` 改为全局一对一类别—圆环关联；
- 标准靶只有完成蓝环关联且中心精修后才进入 memory、旧接口和有效释放证据；
- `target_memory` 使用源 `last_seen` 做控制新鲜度门禁并保留 stable ID/reset；
- H 检测加入内部 H 结构验证，融合/记忆按 `landing` 阶段门控；
- 新增 `ReleaseEvidence.msg`，`drop_aligner` 发布身份、结构、中心、新鲜度、稳定帧和
  拒绝原因；最终 `release_permission` 明确保留给任务/安全层；
- 笔记本 dev/sim 默认六分类模型切换为 `merged_standard`，板端 launch 仍保留独立配置；
- `target_detector.py` 不再内置绝对模型路径，默认选择只由 launch/yaml 参数提供；
- 修复四个 launch 中 `optenv` 空默认被解析成字面量 `''`、覆盖 YAML 模型路径的问题；
  活文档同时补齐联合环境脚本 source 后必须调用 setup/assert 两个函数的真实用法；
- 圆环 C++ 和离线回放使用等比例 letterbox；修正奇数 padding 的半像素逆映射偏差；
- `eval_ring_map_video.py` 使用运行时等价的全局一对一关联，输出按类已关联/未关联统计；
- 新增 `compare_yolo_backends.py`，逐图比较 PT/ONNX 类别、IoU、置信度和框坐标；
- 新增 `run_visual_mock_regressions.sh`，检查 assertion PASS marker，避免 ROS1 `roslaunch`
  外层返回 0 掩盖 required assertion 失败；同步修复 freshness mock 缺少几何有效标志，
  并让 circle/map assertion 显式返回成功或失败码；
- 评测 runner 新增 `smoke/formal` profile；formal 使用主路线 precision/recall/FP/时延阈值，
  recorder/report 新增 P95 像素与 P95 地图误差的实际 Gate，兼容读取旧 summary；
- 修正 recorder 时延语义：使用同源检测消息实际到达时刻，不把 recorder 自身聚合等待
  计入算法时延；fusion 增加完整正样本提前发布并把不完整帧兜底从 0.8 s 收紧到 0.15 s；
- 笔记本 dev/sim detector 新增 yaml `device` 参数并显式使用 GPU；OrangePi/RKNN 路径不变；
- 新增 `docs/VISION_LAPTOP_SIM_BASELINE_20260715.md`，并更新路线图、迁移 Gate、包/工作区
  README、仿真/GUI 手册、责任边界、模型历史文档、OrangePi 计划和 AGENTS；全部明确
  当前推理/识别在笔记本运行，OrangePi 尚未运行统一六分类链。

### 验证结果

- 视觉工作区清空白名单后完整 `catkin_make -j1` 通过，5 个包均遍历；主集成工作区以
  vision 为 underlay 完整 `catkin_make -j1` 通过；
- `verify_toudi3_combined_env.sh` 输出 `V-SIM-00 PASS`；视觉 suite/eval/L0 脚本通过
  `bash -n`，相关 Python 脚本通过 `py_compile`；
- 无 `UAV_VISION_MODEL_PATH` 环境变量时 `phase_d.launch --dump-params` 正确保留
  `target_detector.yaml` 的 merged_standard 路径；static eval/full shadow launch 在联合环境可解析；
- L0 runner 连续 3 次通过：圆环原图坐标恢复、全局一对一关联、地图长期记忆/当前
  观测新鲜度、地图投影、stable ID、`drop_ready` 和 `release_evidence`；
- 固定 Gazebo actionable 基线：五类标准靶 precision=1、FP=0，recall 为
  pillbox 0.808、bridge 0.809、tank 0.765、tent 0.935、panzer 0.874；red_cross
  precision=1/recall=0.646；H precision/recall=1；背景 0 FP；平均像素误差约
  0.67～4.39 px，平均地图误差约 0.0026～0.0143 m；
- 上述场景 P95 延迟约 0.825～0.840 s，主要包含 dev/sim 0.8 s 同源融合等待；因此
  最小 runner 报告通过，但主路线 0.95 召回与 200 ms 正式 Gate 未通过；
- 18 s tent formal profile 正确返回 FAIL：precision=1、recall=0.730、P95 像素误差
  4.36 px、P95 地图误差 0.0142 m、P95 延迟 829 ms；失败项为 recall 与 latency；
- 0.15 s 有界融合后代表结果：tent formal recall=0.709/P95=172 ms（只剩 recall 失败）；
  tank smoke recall=0.675/P95=176 ms、red_cross smoke recall=0.716/P95=182 ms，均 0 FP；
  时延进入 200 ms 内，但正式 0.95 召回仍未达到；
- 融合改动后的 L0 runner 重新连续 3 次通过；
- 实拍 `real_target.mp4` stride=4 共处理 1156 帧；等比例 letterbox 后标准类检测 631、
  圆环候选 546、关联 371，自洽率从旧拉伸版本 1.74% 提升到 58.80%；
- merged PT/ONNX 八图对照失败：matched=8、missing=1、extra=1、min IoU=0.954、
  最大置信度差=0.536、最大框差=2.92 px；重新导出临时 ONNX 结果相同；
- 未连接真实飞控、未启动真实执行机构、未执行实机解锁/起飞/投递；未运行 OrangePi。

### 遗留问题

- 30-seed L1/L2 和 10 min shadow 未完成；低召回类别与笔记本延迟未达到正式 Gate；
- 实拍 MP4 缺圆环实例/中心人工真值，无法计算真实关联召回和绝对中心误差；缺同步
  CameraInfo/TF/pose，无法计算实拍地图误差；
- H、普通黑圈、残圈和 red_cross 实拍负样本仍不足；`TargetDetection` 尚缺显式
  `center_source/association_valid/reject_reason` 字段；
- PT/ONNX 负样本边缘差异未定位，统一六分类 ONNX 尚不能冻结；
- 六分类 RKNN 尚未转换/逐样本对照，OrangePi 5 Plus 的 5-10 Hz、资源、温度和
  10 min 稳定性均无证据；
- 全场自主搜索、候选接近/恢复和最终 `release_permission` 仍属控制/规划/安全侧任务。

### 下一步

先执行 V-SIM-04：扩为 30-seed、在 0.15 s 时延预算内提高低召回类并完成 10 min
shadow；并行补实拍圆环/H/红十字人工真值和同步 pose 采集规范。随后修复
PT/ONNX 差异；获得 OrangePi 和匹配相机后再开始统一六分类 RKNN/板端验收。

## 2026-07-15（V-SIM-04 首轮 30-seed、来源同步与墙钟延迟口径）

### 改动范围

固定 30-seed L1 矩阵、检测分支同步契约、评测覆盖率/延迟口径、静态 TF、H 阶段入口、
本机 ROS 通信隔离和矩阵聚合报告。全部为笔记本 WSL2 纯视觉仿真，未运行 OrangePi。

### 具体改动

- 新增 30 行固定 seed 矩阵、批量 runner 和矩阵聚合器；标准靶复用 toudi3 world，红十字
  仅在对应场景动态生成；每个 seed 使用独立 ROS/Gazebo 端口并记录位姿、seed 和 master URI；
- runner 增加相机 spawn、Gazebo 进程和基础设施日志检查；聚合器将基础设施健康纳入 seed
  判定，避免 spawn 超时仍被当作算法结果；
- `TargetDetectionArray` 增加 `source/completed_sources`，各检测器声明该帧是否实际处理；
  recorder 只在指定分支实际处理的帧上计算识别 precision/recall，并把输入覆盖率作为独立
  formal Gate（最低 0.15）；
- `detection_fusion` 从盲目同时间戳等待改为来源完成契约，并支持 50 ms 近似时间同步；
  正常分支齐全即发布，0.8 s 只用于缺分支兜底。板端入口沿用契约，但时序尚未上板验证；
- 静态评测相机允许受 0.10 s 年龄约束的 latest-TF 回退；动态/实机默认仍禁止；H 评测
  直接设置 `landing` 默认阶段，消除模式话题启动竞争；
- 本机视觉 runner/L0 强制 ROS loopback，避免 WSL 主机名 TCP 握手偶发失败；
- 将延迟改为 `perf_counter` 墙钟：从 recorder 收到源图像到收到 mapped 输出，仿真 `/clock`
  只用于真值时间配对，不再用于算法性能；报告增加分层 stage audit 和来源组合；
- 修正 camera spawn 节点在 roslaunch 收尾被终止时的假阳性健康检查，并保留明确 spawn
  成功标记与服务失败检查。

### 验证结果

- Python `py_compile`、shell `bash -n` 和视觉工作区 `catkin_make -j1` 通过；消息重新生成，
  C++ 圆环/红十字/H 节点重新编译通过；
- 强制 loopback 后完整 L0 通过：圆环坐标、全局一对一关联、记忆新鲜度、地图投影、
  stable ID、`drop_ready` 和 `release_evidence`；此前出现过一次 WSL ROS TCP 握手瞬态，
  loopback 后未复现；
- 首轮完整 30/30 矩阵 aggregate precision=0.9312、recall=0.8595，仅 6 seed 通过；该轮
  同时发现 1 个 camera spawn 超时、仿真时钟延迟污染和独立队列抽帧不同步，因此不作为
  最终性能基线；
- 最新 loopback + 墙钟 7-seed 诊断全部产出报告：H P95=125.3 ms 且识别 precision/recall=1，
  其余代表 seed 中 6/7 P95 仍超过 200 ms；135° tent 为 precision=0.6275、recall=0.5、
  FP=19，属于真实旋转域错分证据；该子集因缺少 23 seed，矩阵总体按设计 FAIL；
- 未启动 PX4/MAVROS/控制/`actuator_pwm`，未执行解锁、起飞、舵机或投递；未在 OrangePi
  运行 PyTorch、ONNX 或 RKNN。

### 遗留问题

- 修订口径后的完整 30-seed 尚未复跑，V-SIM-04 仍未通过；10 min shadow 尚未执行；
- P95 墙钟延迟仍需拆分模型、几何、融合兜底和 TF 投影阶段；
- 135° tent→panzer 错分、部分红十字/旋转视角以及 H 地图 TF N/A 需要固定复现；
- 实拍圆环/H/红十字人工真值、同步 CameraInfo/pose、PT/ONNX 差异和 OrangePi RKNN 均未完成。

### 下一步

先补阶段墙钟延迟统计并复跑干净 30-seed；基础设施零失败后，只针对稳定复现的旋转错分
和关联失败制定数据/算法修复。随后执行 10 min shadow，再进入实拍真值与 OrangePi 准备。

## 2026-07-15（纯视觉安全 GUI 观察入口）

### 改动范围

toudi3 当前视觉状态的人工 GUI 观察脚本与操作手册。

### 具体改动

- 新增 `top_level_scripts/run_toudi3_visual_gui_safe.sh`，只启动 Gazebo 固定评测相机、
  独立真值和笔记本 Phase-D 视觉链；显式排除 PX4、MAVROS、`patrol_control`、arming 和
  `actuator_pwm`；
- 新增 `run_toudi3_visual_viewer_safe.sh`，连接同一隔离 ROS master 并用 WSLg/rqt 显示
  原始相机或用户指定的视觉调试图话题；
- 为非交互 `wsl -e` 补 WSLg 环境和本机 ROS loopback，默认展示当前已知的 135° tent
  压力位姿，并允许通过环境变量覆盖相机位姿、seed 和 master URI；
- 在 `docs/TOUDI3_FULL_SIM_GUI_GUIDE.md` 增加视觉组推荐入口及结论边界。

### 验证结果

- 首次直接向 tmux 发送长 roslaunch 命令时被宿主 shell 截断，仅留下空 tmux 会话，
  Gazebo/ROS 未启动，未误记为成功；
- 安全 GUI 脚本已在 tmux 中实际启动；日志确认 camera spawn 成功、笔记本六分类模型
  加载、`gzclient/gzserver` 持续运行，节点清单不含 PX4/MAVROS/控制/执行机构；
- 首次向第二个 tmux pane 发送长 rqt 命令仍被 shell 截断，因此改用独立 viewer 脚本，
  待紧接着实际启动验证。

### 遗留问题

- Gazebo 窗口是否被当前 WSLg 会话正常拉起仍需本次实际启动确认；GUI 观察不替代算法 Gate。

### 下一步

在独立 tmux 会话运行安全 GUI 脚本，确认 `gzclient`、相机图像和 `/uav_vision/*` 话题；
用户观察结束后显式停止该会话和对应 ROS/Gazebo 进程。
## 2026-07-15（视觉驱动控制流 GUI 观察入口）

- 改动范围：`top_level_scripts/run_toudi3_control_flow_gui_safe.sh`、`top_level_scripts/run_toudi3_visual_viewer_safe.sh`。
- 具体改动：新增笔记本全控制流 SITL GUI 入口，保留 Phase-D 视觉、旧接口兼容、`patrol_control`/planner、PX4/MAVROS 与 Gazebo 飞机响应；默认关闭本次观察非必需且高负载的 FAST-LIO/FreeDOM 和 RViz。修正相机查看器在 ROS setup 阶段受 `set -u` 影响的问题。
- 验证结果：旧的全量入口可启动 Gazebo/PX4/视觉/控制链，但高负载下 PX4 曾报 `Arming denied: Resolve system health failures first`。清理旧 ROS master 后，减载入口已稳定启动，MAVROS 实测 `connected=True`、`armed=True`、`mode=OFFBOARD`、`system_status=4`；起点阶段视觉为 `Class:Nothing`，尚未把后续靶标触发状态转移记为已验证。
- 遗留问题：该入口只用于笔记本 WSL2 SITL，不代表 OrangePi 板端性能，也不代表正式 30-seed/10 min Gate 通过；默认直航点模式下 planner 节点运行但不拥有最终 setpoint。
- 下一步：保持 GUI 会话运行，观察飞机到达旧路线检测点后的识别、兼容话题和控制状态转移；另行量化位姿变化与目标触发反应。

## 2026-07-16（V-CL-00A 任务层投放许可与旧 Servo 安全边界）

### 改动范围

新增主集成包 `uav_mission`，建立视觉释放证据到原有 `patrol_control/Servo` 服务之间的
任务/安全边界；同步路线图、迁移清单、责任边界、README、AGENTS 和一键回归入口。

### 具体改动

- 新增 `ReleasePermission` 和 `ReleaseResult`：许可包含顺序载荷槽位、任务对准模式、视觉
  目标、证据时间、有效截止时间和拒绝原因；结果记录 raw 执行 ACK；
- `release_permission_arbiter.py` 组合 `release_evidence`、唯一 `align_mode`、飞机位姿、
  载荷顺序和目标防重放状态；仅 `drop_circle/drop_cross`、新鲜有效视觉证据和近地位姿
  同时满足时发布短时许可；
- `guarded_servo_proxy.py` 独占旧 `/Servo` 接口，保持旧控制 `req=1/2/3` 及 `res` 语义，
  仅在许可槽位匹配且未过期/未消费时转发到 `/legacy/Servo_raw`；
- 新增纯软件 `mock_raw_servo_server.py`、全分支 assertion、`release_guard_sim.launch` 和
  `toudi3_visual_delivery_guarded.launch`；仿真不启动 `actuator_pwm`；
- 增加 `top_level_scripts/run_release_guard_regression.sh`；
- 按用户要求未修改、清理或替换旧 `patrol_control.cpp`、`actuator_pwm`、`Servo.srv` 和
  原 launch。AGENTS 新增规则：未来确需修改旧链时先保存非编译快照、清单和 SHA256。

### 验证结果

- `uav_mission` 消息和脚本在主工作区 `catkin_make --pkg uav_mission -j1` 编译通过；
- Python `py_compile`、shell/launch XML 基础检查通过；在完整 combined overlay 下，
  `toudi3_visual_delivery_guarded.launch` 可解析全部 include；
- `run_release_guard_regression.sh` 实际 PASS：raw mock 仅收到 `[1, 2, 3]`；无任务阶段、
  错误高度、错误槽位、已用槽位、同目标重复、陈旧证据和载荷耗尽共 7 类拒绝均未到达
  raw 服务；同时验证视觉 stable ID `0` 是合法目标 ID；
- 未启动 `actuator_pwm`、真实 MAVROS、实机解锁/起飞/PWM/投放，也未宣称 OrangePi 结果。

### 遗留问题

- 尚未运行完整旧控制固定三检测点三投 SITL；当前只证明任务许可和旧服务边界；
- 许可暂按视觉证据、任务模式、位姿高度、顺序槽位和防重放仲裁，仍需加入速度、任务目标
  身份绑定、机构健康/在位和 Mission Manager 状态；
- 原旧控制的超时分支源码仍原样保留；新受控入口会因缺少新鲜许可拒绝旁路调用，但后续
  仍需在原包可审计快照后评估是否做最小语义修复；
- mission frame、地图投影质量、物理 stable ID/类别投票、候选接近与搜索恢复未完成。

### 下一步

执行 V-CL-00B：统一 mission frame、源图像时间和 TF/地图质量契约；随后用 guarded
`/Servo` 运行新视觉+旧控制的固定三投 SITL，并把每次 permission、raw ACK、失败原因和
目标 stable ID 写入闭环事件报告。

## 2026-07-16（V-CL-00B/01 地图契约、连续帧确认与物理目标记忆）

### 改动范围

仅修改新视觉消息、投影/记忆节点、Phase D/板端 launch、确定性 mock 与项目状态文档；
未修改、删除或清理旧 `patrol_control`、`actuator_pwm`、`Servo.srv` 和原始投递逻辑。

### 具体改动

- `TargetDetection/TargetCandidate` 追加 `center_source`、`association_valid`、
  `reject_reason`、`transform_age_sec`；候选另保留累计 `observe_count` 并新增
  `consecutive_observe_count`，旧字段和旧话题不变；
- detector/圆环/红十字/H/refiner 全部填充中心来源和关联语义；地图投影将缺内参、未精修、
  缺 TF、TF 过旧、光线平行和交点在相机后方转换为稳定原因码，成功投影清空拒绝原因；
- Phase D 和板端 mission frame 默认改为旧 Fast-Planner 使用的 `camera_init`；Gazebo
  独立真值入口继续显式覆盖为 `world`，没有修改旧 planner 源码；
- Phase D/板端开启 `require_map_for_candidates=true`：无效或陈旧 TF 不创建候选，也不以
  新回调时间刷新旧地图候选；Phase B/C 纯像素兼容入口保持 false；
- `target_memory` 在地图域将五类标准靶视为同一物理身份组，按地图距离和 frame 匹配；
  无地图时仍只做同类别像素匹配；两个有效地图点未命中时禁止退回像素合并；
- 地图点按 `map_quality` 加权融合；类别切换需连续 2 帧、置信度不低于 0.70 且累计
  `class_confidence × geometry_confidence` 投票胜出；候选确认改为真正连续帧，漏检清零
  streak，但 TTL 内已确认地图记忆仍保留；
- 新增 `target_memory_physical_mock.launch` 和 `map_rejection_mock.launch`，并纳入统一 L0；
  map mock 增加未关联标准靶，验证 `circle_association_missing` 不进入 operational 链。

### 验证结果

- TDD RED 证据：旧实现把同一地图位置 tent→panzer 拆为 ID 0/1；缺 TF 场景连续 59 帧
  产生 `tf_unavailable`，但仍错误创建 panzer 候选；两项回归均先按预期失败；
- GREEN 后物理记忆回归 PASS：低于 0.70 的类别证据不切类，连续 2 帧高置信才切为
  panzer 且 ID 不变；2 帧命中—漏 1 帧—再命中 1 帧不确认，重新连续 3 帧才确认；相距
  2 m、像素中心相同的两个目标保持两个 ID；
- 缺 TF 回归 PASS：精修/关联仍有效，但 `map_valid=false`、原因 `tf_unavailable`、
  `transform_age_sec<0`，且 `/uav_vision/targets` 不产生 panzer 候选；
- `phase_d_map_mock` PASS：有效目标带 `circle_geometry`、有效关联和 TF 年龄，未关联 tent
  带明确拒绝原因；地图、stable ID、`drop_ready/release_evidence` 仍通过；
- 视觉工作区 `catkin_make -j1`、主工作区 `catkin_make --pkg patrol_control uav_mission -j1`
  通过；完整 L0（新增后 6 项）通过；`run_release_guard_regression.sh` 继续 PASS，raw mock
  只收到顺序 `[1,2,3]`；
- 全部验证仍在笔记本 WSL2；未启动真实执行机构、实机解锁/起飞/PWM/投递，未运行 OrangePi。

### 遗留问题

- `camera_init` 是根据旧 Fast-Planner/旧控制实际 frame 契约选定的默认值，但尚未在完整
  toudi3 SITL 动态 TF 树中验证相机投影点与 planner 到达点一致；
- 物理 ID/类别投票目前只有确定性 L0，尚缺运动相机、遮挡、重捕获和 30-seed 重复 ID 率；
- 累计类别投票尚未加入长任务时间衰减；先用固定回归观察，只有真实长效抖动证据出现时
  再增加衰减，避免无依据扩大参数面；
- 完整旧控制固定三投、候选接近/恢复搜索和闭环事件报告仍未完成。

### 下一步

执行 V-CL-02：先只读核对完整 SITL 的 `camera_init -> camera` TF 和三处固定检测阶段，
然后使用现有 guarded `/Servo` 与 raw mock 跑通三次 permission→ACK；若必须修改旧链源码，
先按 AGENTS 规则建立 `legacy_baseline/<date>/` 清单和 SHA256 快照。

## 2026-07-16（V-CL-02 连续视觉门控复核、旧控制状态互锁与三投事件审计）

### 改动范围

仅修改新增的 `uav_mission` 任务安全包、集成 launch、回归和项目状态文档；复核既有
`target_memory` 连续帧/置信度机制。未修改、删除或清理旧 `patrol_control`、`actuator_pwm`、
`Servo.srv`、旧路线参数和原始投递状态机，因此本轮无需建立旧链源码备份。

### 具体改动

- `release_permission_arbiter.py` 新增旧控制状态输入 `/detect/point_class`，只接受新鲜的
  `Aligning=2`；即使视觉、对准模式和高度满足，旧控制处于 `Run_point` 仍以
  `control_not_aligning` 失败关闭；
- `release_guard_assertion.py` 增加非 Aligning 拒绝分支，并保持错误高度、错槽、重放、
  重复目标、证据过期和载荷耗尽测试；
- 新增只读 `visual_delivery_audit.py`，记录控制状态、对准模式、候选、视觉证据、许可、
  raw mock 调用和结果，原子写入 JSON；只有槽位 `[1,2,3]`、不同目标、连续视觉证据、
  近地位姿、Aligning 和许可上下文全部一致才发布 `PASS`；
- `toudi3_visual_delivery_guarded.launch` 接入审计节点，默认报告为
  `/tmp/toudi3_visual_delivery_audit.json`；真实执行机构仍不启动；
- 复核 `target_memory`：候选确认默认连续 3 个合格帧；类别切换默认连续 2 帧且
  `class_confidence>=0.70`，漏帧清零 streak，标准靶地图身份跨类别保持同一 ID。

### 验证结果

- TDD RED：加入旧控制状态断言而仲裁器尚未订阅时，回归按预期在
  `control_not_aligning` 等待处失败；实现后 GREEN；
- `target_memory_physical_mock.launch` PASS：低置信类别不切换、连续高置信才切换、漏帧
  清零连续计数且物理 ID 不变；
- `uav_mission` Python `py_compile` 和 `catkin_make --pkg uav_mission -j1` 通过；释放回归
  PASS，raw mock 恰为 `[1,2,3]`，JSON 状态 `PASS/three_drop_chain_complete`；
- 在首个旧检测点 `(-0.602009,-1.04125,1.5)` 运行独立静态 pillbox 评测：130/130 TP，
  precision/recall 均 1.0，平均像素误差 2.773 px，平均地图 XY 误差 0.009 m；本结果仅证明
  笔记本静态视觉输入可识别，不代表完整飞行链或 OrangePi；
- 240 s headless guarded SITL 中，旧控制两次进入 `Aligning/drop_circle`（约
  794.44--798.49、807.23--811.24 仿真秒），但 `selected_target` 始终为空，许可稳定为
  `visual_evidence_invalid`，raw 调用和 release result 均为 0；审计报告状态 `WAITING`；
- 未启动真实 `actuator_pwm`、实机解锁/起飞/PWM/投放；SITL 结束后无 ROS/Gazebo/PX4
  残留进程。

### 遗留问题

- 同一坐标静态视觉 100% 命中而飞行检测点无候选，根因已缩小到机载相机实际视场/姿态、
  航点到达偏差、检测点短时间窗或相机到地图 TF 接线；目前还没有检测点触发截图和独立
  Gazebo 投影视场真值，不能继续猜测是哪一项；
- 旧路线三个坐标实际对应 `pillbox/bridge/tent`，配置注释把后两处写成 `tanke/`
  `zhuangjiache`；本轮只核查未改旧配置，需在不影响逻辑的文档修订中更正；
- 尚未取得三次 permission→raw mock ACK；速度、机构健康/在位和 Mission Manager 互锁
  仍未实现。

### 下一步

继续 V-CL-02，但不调整识别阈值：在完整飞行 launch 中加入只读的检测点触发截图和
Gazebo camera-to-target 独立真值，比较实际 ROI、飞机 XY/Z/yaw 与静态通过场景；定位并
修正相机姿态、航点或 TF 中的首个真实差异后，再复跑同一三投 JSON Gate。

## 2026-07-16（V-CL-02 下视相机链复核、地图候选恢复与阶段暂停）

### 改动范围

修改仓库内 `uav_mission` 仿真模型/launch/相机外参清单、`uav_vision` 对准候选新鲜度逻辑、
`uav_vision_eval` 相机视场与 Aligning 取证入口，以及两份当前状态文档。原 PX4 外部
`iris_mid360` 双相机模型、旧 `patrol_control`/`actuator_pwm` 源码和旧路线均未删除或覆盖；
修改旧 launch 的参数透传前已建立 `legacy_baseline/20260716_vcl02_downward_camera/` 快照和
SHA256 清单。本轮未启动真实执行机构。

### 具体改动

- 核查确认原 `iris_mid360` 同时包含前视 D435i 和下视 FPV；此前新视觉接到前视相机，导致
  Aligning 画面主要为墙体/天空。独立 FPV 投影回归又发现其 320×240 `CameraInfo` 主点约为
  `(320.5,240.5)`，不满足当前米制投影链的帧内条件；
- 新增仓库内 `iris_mid360_downward` 集成模型：保留 MID360，只使用单个下视 D435i；原外部
  模型不改。上层 launch 新增可选 `vehicle_sdf` 透传，默认仍指向原模型，保证旧入口兼容；
- 新增/修订下视 D435i、下视 FPV 独立视场断言和 Aligning 触发同步取证：只在旧控制
  `Aligning=2` 且 drop 模式时记录图像与独立 Gazebo 真值；
- 完整链补充仿真专用 `camera_init -> D435i::camera_color_frame` 静态外参，数值由仓库 SDF
  组合得到并参数化；新增 `camera_extrinsic_profiles.yaml`，登记旧工程主/备外参及相互冲突
  的 CameraInfo 证据。旧值仅保留为实机复标候选，没有覆盖仿真值；
- `drop_aligner.py` 改为先按 `target_max_age` 过滤已确认候选，再按质量排序，避免高质量历史
  候选遮蔽当前新鲜候选；新增确定性回归验证选择新鲜目标；
- 状态文档将当前断点从“飞行中无 `selected_target`”更新为“地图候选已恢复，但旧控制不消费
  新视觉地图点”。现有像素 Pose 兼容输出与旧 `/detect/waypoint_mark_point` 的地图坐标语义
  不一致，继续默认关闭，未用错误接口强行驱动飞机。

### 验证结果

- 下视 D435i 在实际 `toudi3.world` 的独立投影视场断言 5/5 PASS，pillbox 投影约为
  `(330.032,240.393)` 且完整位于图像内；下视 FPV 断言按预期 FAIL，记录了不合法主点证据；
- 补齐仿真外参后，完整 toudi3/PX4/MAVROS/MID360/FAST-LIO/旧路线运行中，新视觉可发布
  `map_valid=true` 的 `camera_init` 地图候选；首个 pillbox 地图点收敛到约
  `(-0.62,-1.006)`，与场景真值约 `(-0.602,-1.041)` 定性一致；
- 新鲜度回归 PASS；最新完整诊断运行 360 s 墙钟时间，旧控制两次进入
  `Aligning/drop_circle`，记录 2 个 episode、10 帧同步证据。失败主因已前移为
  `offset_exceeds_limit`，仍偶发过期/未确认目标；
- 完整运行审计最终为 `WAITING`：`raw_calls=[]`、投递结果为空。旧控制超时时出现两次
  `Invalid servo ID: 0`；没有 mock raw 释放，更没有真实 PWM/投递；
- 相机/launch XML、视觉工作区编译和确定性新鲜度回归已通过；运行结束后无 ROS/Gazebo/PX4
  残留进程。

### 遗留问题

- 新视觉目前已完成检测、圆环精修、地图投影、连续帧确认、稳定记忆、候选选择和对准证据，
  但旧控制没有消费候选 `map_point`，因此尚未形成“视觉偏差带动飞机横向修正”的闭环；
- 完整 SITL 尚未产生一次 raw mock ACK，更未完成三次投递；旧路线的 servo ID 0 映射仍需
  单独核对；
- 尚无全场覆盖搜索、候选优先队列、接近/重捕获/恢复搜索；OrangePi/RKNN、真实相机外参、
  实拍域差和实机执行机构均未验证；
- Fast-Planner 进程在本次限时结束阶段出现 exit -9，暂不能据此判定为运行期算法故障，仍按
  非视觉稳定性问题保留。

### 下一步

按用户要求暂停下一期推进，不新增控制接口、不调整旧路线、不继续追逐仿真延迟指标。恢复
推进时，从语义正确的“新视觉地图候选 → 旧控制地图目标/独立任务接口”开始，而不是启用
像素 Pose 兼容输出或直接连接真实舵机。

## 2026-07-16（OrangePi 板端 RKNN 评测目录检查与 USB 相机实时推理查看器）

### 改动范围

仅新增两个笔记本侧辅助脚本并向板端推送一个查看器脚本；未修改 `uav_vision`、
`uav_mission`、旧控制、launch 或任何配置；未启动 ROS、执行机构或飞行相关进程。

### 具体改动

- 新增 `top_level_scripts/ssh_orangepi_run.py`：pexpect 密码方式在
  `orangepi@192.168.3.15` 上执行只读/受控命令（本机无 sshpass，密码为明文，仅内网调试）；
- 新增 `top_level_scripts/board_realtime_rknn_viewer.py` 并 base64 推送到板端
  `/home/orangepi/liftrace_board_eval_20260716/`：USB 相机 MJPG 1920x1080@30 采集，
  letterbox 640 + RGB + f32/255（与 `board_real_target_eval_fp32.json` 同口径），
  RKNNLite 加载 `merged_standard_fp32.rknn`，conf 0.5 + NMS 0.45，`DISPLAY=:0` 在板载
  显示器开窗实时显示框/类别/FPS；
- 检查板端评测目录：6 个 RKNN（merged_standard/region_focus_aug × fp32/int8_8img/
  int8_50img）、8 图与 50 图量化集、`real_target.mp4` 与唯一落盘记录
  `board_real_target_eval_fp32.json`。

### 验证结果

- fp32 视频评测记录（386 采样帧，无 NMS、无真值）：新 merged fp32 推理 P50 66.7 ms、
  中位数 9 个过阈框、最高分中位数 0.945；新 region fp32 类似但首帧漏检；两个 2025 旧
  RKNN 同口径下分数接近 0（不能排除预处理契约不匹配）；int8 六个模型只有转换产物，
  无落盘评测记录；
- 板端相机确认为 `HD USB Camera /dev/video0`（MJPG 1080p@30）；实时查看器已在板端
  X 会话运行：`CAMERA_READY 1920x1080@30`，运行约 800 帧，显示帧率约 10.5 FPS，
  推理 54-63 ms，无崩溃。

### 遗留问题

- int8_8img/int8_50img 的视频评测未落盘，量化损失未量化对比；
- 旧模型零分未定位（预处理契约 vs 模型本身）；
- 查看器 conf/NMS 为演示值，非任何验收阈值；显示帧率受 imshow 与单核 NPU 限制约
  10 FPS，不代表 `phase_d_board` 链路性能。

### 下一步

如需继续板端评测：用同一口径补齐 4 个 int8 模型的视频评测 JSON 并拷回
`vision_ws/test_data/`；实时查看器退出方式为窗口按 q/ESC 或
`pkill -f board_realtime_rknn_viewer`。

## 2026-07-16（板端实时查看器接入机上标定内参去畸变）

### 改动范围

仅修改 `top_level_scripts/board_realtime_rknn_viewer.py` 并重新推送到板端；未改
`uav_vision`/`uav_mission`/旧链/launch/配置；未启动 ROS 或执行机构。

### 具体改动

- 查看器接入板上机载标定去畸变：内嵌
  `detect_ws/src/camera_sdk/param/camera_param.yaml` 的 1920x1080 plumb_bob K/D
  （fx≈581.26, cx=1043.5, D=[0.0349,-0.0426,0,0,0.0076]），启动时
  `getOptimalNewCameraMatrix(alpha=0)+initUndistortRectifyMap` 预生成 remap，
  每帧先 `cv2.remap` 再 letterbox 推理；`--no-rectify` 可关闭；
- 同脚本新增 `--video/--json/--stride/--warmup` 离线视频评测模式（与
  `board_real_target_eval_fp32.json` 同口径，另含 rectify/prep/infer/post 分段时延），
  为后续带标定重测 `real_target.mp4` 与 int8 对比预留入口；
- 核查发现板上存在三份互相冲突的相机标定：`camera_param.yaml`(1080p, 弱畸变广角)、
  `jdland.yaml`(1080p, k1=-0.42 强桶形)、`video0.yaml`(640x480 理想针孔占位)，与
  `uav_mission/config/camera_extrinsic_profiles.yaml` 记录的 CameraInfo 冲突证据一致；
  本轮选用 `camera_param.yaml`，未删改任何标定文件。

### 验证结果

- 板端日志 `CALIBRATION_READY source=camera_param.yaml size=1920x1080`；
  运行 600+ 帧无崩溃：rectify 16-21 ms(CPU)、infer 54-57 ms、总链 ~80 ms、
  显示帧率 ~9.1 FPS（未去畸变时 ~10.5 FPS）；dets 由 0 恢复到 1-2。

### 遗留问题

- `camera_param.yaml` 与 `jdland.yaml` 哪份对应当前 HD USB Camera 未最终确认，
  需在屏幕上核对直线是否被拉直；如仍畸变明显应切换 `jdland.yaml` 重新验证；
- 去畸变在 CPU 上耗时 ~18 ms，若接入正式板端链需评估分辨率下采样或 RGA 加速；
- 带标定的 `real_target.mp4` 重测与 int8 对比仍未执行。

### 下一步

用户在板端显示器确认去畸变效果与检出率变化；确认标定归属后，用 `--video` 模式
带同一标定重测新旧/量化模型并落盘 JSON。

## 2026-07-16（原始视频带框回放与 v5merge 全模型 P/R/mAP 评测）

### 改动范围

本轮继续使用 `orangepi@192.168.3.15` 的板载 NPU，但按用户最新口径将“视频文件回放”和
“实时相机”分开：`real_target.mp4` 直接读取原始帧，不做相机去畸变；只有 `/dev/video0`
实时相机查看器默认使用固定 1920x1080 内参。未启动 ROS、PX4、Gazebo、舵机或飞行控制。

### 代码与评测工具

- `top_level_scripts/board_realtime_rknn_viewer.py`：视频模式新增原始帧默认、旋转元数据关闭、
  `--output-video` 带框输出和性能 JSON；实时相机仍默认 `remap` 去畸变，可用 `--no-rectify`
  关闭。视频可用 `--rectify-video` 显式开启标定对照。
- `top_level_scripts/board_rknn_dataset_eval.py`：板端读取完整 YOLO 数据集，支持新六分类
  单模型和旧 standard+tank 双模型的类别偏移合并，输出 P/R/F1、mAP50、mAP50-95、逐类
  TP/FP/FN 与单图 P50/P95。
- 新增回归：`vision_ws/scripts/test_board_viewer_calibration.py`、
  `vision_ws/scripts/test_board_rknn_dataset_metrics.py`，均通过；两个脚本均已 `py_compile`。
- v5merge 评测全集已传到板端临时目录 `v5merge_eval`：1395 张 train+val 图像、约 862 MB。
  评测缓存不属于机载工程资产。

### 板端 v5merge 全集结果

统一使用原始图片、letterbox 640、RGB、float32/255、conf=0.25、IoU=0.5；全集包含训练图，
因此用于覆盖/拟合审计，不能替代独立泛化验证。P/R/F1 为 IoU=0.5 下的微平均，mAP50 和
mAP50-95 按六分类计算。

| 模型 | P | R | F1 | mAP50 | mAP50-95 | 单图总耗时 P50/P95 |
|---|---:|---:|---:|---:|---:|---:|
| `merged_standard_fp32.rknn` | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 0.9859 | 57.9/106.9 ms |
| `region_focus_aug_fp32.rknn` | 0.9979 | 0.9993 | 0.9986 | 0.9984 | 0.9744 | 63.1/99.6 ms |
| merged INT8（8图校准） | 0 | 0 | 0 | 0 | 0 | 49.6/73.9 ms |
| merged INT8（50图校准） | 0 | 0 | 0 | 0 | 0 | 45.8/56.4 ms |
| region INT8（8图校准） | 0 | 0 | 0 | 0 | 0 | 45.9/53.1 ms |
| region INT8（50图校准） | 0 | 0 | 0 | 0 | 0 | 47.2/54.6 ms |
| 旧 standard+tank（双模型） | 0 | 0 | 0 | 0 | 0 | 123.2 ms（两次 P50） |

旧模型单独运行也均为 P=R=mAP=0；两模型在整套 2026 六分类数据上没有形成有效候选，且
旧 standard 不含 `red_cross`。INT8 虽然 NPU 时延更低，但在视频和全集上均无 conf=0.25
有效检测，不能部署。

### 本机 PT 对照

- 独立 v5merge val（232 张）：merged PT `P=0.9975, R=1.0000, mAP50=0.9950,
  mAP50-95=0.9804`；region PT `P=0.9897, R=0.9987, mAP50=0.9950,
  mAP50-95=0.9790`。
- v5merge 全集（1395 张，包含训练集，仅作覆盖审计）：merged PT `P=1.0000,
  R=1.0000, mAP50=1.0000, mAP50-95=0.9855`；region PT `P=0.9979, R=1.0000,
  mAP50=1.0000, mAP50-95=0.9758`。
- 同一 `real_target` 原始视频的桌面 PT 带框视频已生成；视频回放的旋转元数据策略与
  板端不同，视频用于观察框稳定性，速度/模型选型以数据集报告为准。

### 带框视频与报告资产

本机已回传的板端原始视频带框结果：

- [merged FP32 视频](/home/xhj/liftrace/vision_ws/test_data/board_eval_20260716/videos_raw_merged_fp32.mp4)
- [region FP32 视频](/home/xhj/liftrace/vision_ws/test_data/board_eval_20260716/videos_raw_region_fp32.mp4)
- [merged INT8 8图视频](/home/xhj/liftrace/vision_ws/test_data/board_eval_20260716/videos_raw_merged_int8_8img.mp4)
- [merged INT8 50图视频](/home/xhj/liftrace/vision_ws/test_data/board_eval_20260716/videos_raw_merged_int8_50img.mp4)
- [region INT8 8图视频](/home/xhj/liftrace/vision_ws/test_data/board_eval_20260716/region_int8_8img.mp4)
- [region INT8 50图视频](/home/xhj/liftrace/vision_ws/test_data/board_eval_20260716/region_int8_50img.mp4)
- PT 两款完整视频位于 `vision_ws/test_data/board_eval_20260716/pt_videos/{merged_pt,region_pt}/annotated.mp4`。

板端视频输出为 1280×540、约 5 FPS 的采样回放，不代表原视频 60 FPS 实时处理；对应 JSON
中保留 NPU、后处理、总时延和类别计数。FP32 原始视频总链 P50 约 63.9/61.9 ms，INT8
约 48–50 ms，但 INT8 视频没有有效框。

### 结论与遗留问题

- 当前板端推荐 `merged_standard_fp32.rknn`；region FP32 作为 A/B 备选；不推荐旧双模型或
  当前 INT8。该结论仅适用于离线推理证据，正式机载还需解决 `librknnrt 1.5.2` 与
  Toolkit 2.3.2 版本警告。
- v5merge 全集 P/R/mAP 很高且包含训练图，不能替代独立 val、压力集和真实飞行域真值；
  下一步应补板端压力验证和相机实时窗口 10 分钟稳定性。
- 数据集板端缓存可在评测结束后清理；原始模型、旧工程和正式数据集均未删除。

## 2026-07-16（最终补充：实时窗口验证与评测资产核验）

### 改动范围

补记本轮新增的板端带框视频输出、v5merge 指标工具、计划文档同步和实时窗口试运行；未
修改 ROS 控制链、执行机构或飞控。

### 验证结果

- 6 个板端 RKNN 采样带框视频均能在本机 OpenCV 打开并读取首帧，规格为 1280×540、约
  5 FPS；4 个视频回传在 `videos_raw_*.mp4`，另外两个保留在板端评测目录并另存于本机
  `region_int8_8img.mp4`、`region_int8_50img.mp4`。
- 两款本机 PT 完整带框视频均已生成，源视频为同一 `real_target_board_eval_20260716.mp4`。
- 合并后的旧 standard+tank 全集报告确认 P/R/F1/mAP 全为 0；两旧模型分开评测以避免双
  runtime 同时运行时中断。
- 按用户要求启动一次 `DISPLAY=:0` 板载实时窗口：成功处理约 100 帧，日志记录
  `CAMERA_READY 1920x1080@30`、去畸变约 29 ms、推理约 52.5 ms、约 9.6 FPS；随后
  `/dev/video0` 连续取帧失败，已主动停止进程。该异常是实时 USB 视频流稳定性问题，不能
  归因于模型精度，也不影响文件视频评测。
- `test_board_viewer_calibration.py`、`test_board_rknn_dataset_metrics.py` 和
  `test_target_detector_rknn_contract.py` 均通过（后者在加载 ROS Noetic 与 vision_ws
  overlay 后运行）。

### 文档同步

已同步：`VISION_2026_ROADMAP.md`、`VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md`、
`vision_ws/src/uav_vision/README.md`，并在本台账补录 RKNN 转换、性能、P/R/mAP、视频资产、
内参/旋转口径和遗留问题。

### 遗留问题与下一步

- 优先排查 `/dev/video0` 连续取帧失败（USB 相机占用、V4L2 自动重连、供电或 MJPG 流稳定性），
  再进行实时 10 分钟稳定性；不把约 100 帧窗口烟测当成通过。
- `merged_standard_fp32.rknn` 作为当前板端主候选；INT8 四款需重新检查量化输入/校准契约，
  在 P/R 恢复前禁止部署。
- 1395 张全集指标含训练图，只作覆盖审计；后续视觉质量判断回到独立 val、压力集和真实
  视频人工真值，继续推进圆环中心、地图记忆和旧控制接口闭环。

## 2026-07-16（补录：OrangePi RKNN 转换、旧新模型板端评测与本机 PT 对照）

### 改动范围

本条补录上一轮已经实际完成、但原记录没有展开的板端工作。评测对象为
`orangepi@192.168.3.15:/home/orangepi/liftrace_board_eval_20260716`，没有启动 ROS、
PX4、Gazebo、执行机构或飞行控制；视频推理只读取 `real_target.mp4`。

### 模型与转换结果

- 旧运行时模型：`Visual/src/yolov5_detect/best_rknn_model/best-rk3588.rknn`（标准
  4 类，输出 `(1,8,8400)`）和 `tank_rknn_model/best-rk3588.rknn`（tank 单类，输出
  `(1,5,8400)`）。两者不含 `red_cross`，不是 2026 六分类部署模型。
- 新模型统一类别顺序：`bridge/panzer/pillbox/tent/tank/red_cross`；输入固定
  `1×3×640×640`，输出原始 `1×10×8400`，板端脚本负责转置、置信度筛选和 NMS。
- 新模型 FP32 RKNN（当前主评测对象）已由两份 ONNX 转换：
  `merged_standard_fp32.rknn`、`region_focus_aug_fp32.rknn`，文件大小均约 7.54 MB。
- 新模型 INT8 RKNN 已完成转换但未作为当前部署结论：两模型各有 8 图、50 图校准
  产物，文件大小约 4.80 MB；尚未完成同一视频的精度/性能验收，因此不能写成已通过。
- 转换工具为 RKNN Toolkit2/compiler 2.3.2；板端 `rknnlite` 为 2.3.2，运行库
  `librknnrt` 为 1.5.2，加载时出现 compiler/runtime 版本不一致警告。模型目前能加载
  和推理，但正式部署前应统一或复核运行时版本。

### 板端硬件与视频口径

- 板端为 RK3588、8 核、约 15.6 GiB 内存；NPU 推理通过 RKNNLite 执行。
- `real_target.mp4` 约 77.08 s、约 60 FPS；评测每 12 帧取 1 帧，共 386 个样本，前
  5 个样本热身，不含人工真值，因此下面是吞吐、输出一致性和类别分布对照，不是准确率。
- 推理前处理为 letterbox 640、BGR→RGB、`float32/255`；后处理为置信度 0.5、类别内
  NMS IoU 0.45。

### 板端旧模型与新模型结果（未加入去畸变前处理的基线）

| 模型 | 输入/类别 | NPU 推理 P50/P95 | 推理后总链 P50/P95 | 最高分中位数 | 最终检测中位数 | 判断 |
|---|---|---:|---:|---:|---:|---|
| 旧 standard | 4 类 | 69.77/74.36 ms | 78.14/83.35 ms | 0.00098 | 0 | 本视频下不可用；还缺 red_cross |
| 旧 tank | 1 类 | 69.71/74.34 ms | 77.95/83.10 ms | 0.00021 | 0 | 本视频下不可用；只能补充 tank |
| merged FP32 | 6 类 | 65.68/70.52 ms | 75.27/80.83 ms | 0.9448 | 1 | 当前首选候选 |
| region-focus FP32 | 6 类 | 66.02/69.91 ms | 75.55/79.73 ms | 0.8984 | 1 | 可作 A/B 候选 |

新模型 NMS 后累计类别数（顺序为 `bridge,panzer,pillbox,tent,tank,red_cross`）为：
`merged=[54,49,45,0,33,95]`，`region-focus=[48,37,50,0,53,91]`。这表明两款新
模型在板端都能走通六分类输出；类别计数不同不是准确率结论，仍需人工标注真值验证。

### 本机 PT 对照

在同一个下载到本机的板端原始视频上，RTX 使用 Ultralytics、`imgsz=640`、`conf=0.5`、
同样每 12 帧采样：merged PT 总链 P50/P95 为 `18.31/41.76 ms`、约 `54.61 FPS`；
region-focus PT 为 `19.37/44.49 ms`、约 `51.63 FPS`。PT 与 RKNN 的累计类别数接近但
不完全相同，说明存在转换/数值舍入差异；桌面 PT 速度不能外推为板端速度。

### 部署判断

- 当前建议以 `merged_standard_fp32.rknn` 作为六分类板端主模型，理由是板端最高分中位数
  更高、与本机 PT 类别分布较接近、无需依赖旧的 standard+tank 双模型拼接；不代表已完成
  规则准确率验收。
- `region_focus_aug_fp32.rknn` 保留为同场景 A/B 备选；当前吞吐几乎相同，但没有真值证据
  证明它优于 merged 版本。
- 旧 standard+tank 不推荐作为 2026 主部署：组合推理约需两次 NPU 推理、没有红十字，且
  在本段 2026 实拍视频中均未形成有效候选。
- INT8 暂不推荐直接切换。虽然转换产物已生成，但需要先解决版本警告并完成 INT8 与 FP32
  的同视频类别/置信度/漏检对照。

### 遗留问题与下一步

- 本条记录的上述结果为未去畸变基线；新的固定相机内参去畸变回放正在补测，结果将以独立
  JSON 追加，不能与本基线混写。
- 板载显示器实时窗口已停止，后续重新启动时使用 `DISPLAY=:0` 和现有窗口脚本，直接在
  香橙派显示器显示，不需要把窗口回传到本机。
- 仍需记录去畸变后的 `rectify/prep/infer/post/total` 分段时延，并观察校正是否改善圆环
  形状和类别稳定性；不以“窗口能显示”代替视觉准确率验收。

## 2026-07-16（最终汇总：全模型图片/视频资产、旧双模型状态与板端负载边界）

### 改动范围

本轮只整理既有评测资产和部署结论，并补充旧 standard+tank 回放工具；按用户要求，
发现香橙派存在整机高负载死机风险后，暂缓实际执行旧双模型视频回放，不继续增加板端
负载。未启动 ROS、PX4、Gazebo、相机实时窗口、执行机构或飞行控制。

### 具体改动

- 新增完整汇总报告：
  `docs/BOARD_MODEL_COMPLETE_EVALUATION_20260716.md`，统一收录两款 PT、两款 FP32
  RKNN、四款 INT8 RKNN、旧 standard、旧 tank 及旧双模型合并结果的 P/R/F1/mAP、
  图片性能、视频带框资产和部署建议。
- 新增 `top_level_scripts/board_legacy_rknn_dual_viewer.py`：按旧链可确认的
  standard 4 类 + tank 1 类拆分加载，将 tank 偏移为六分类 class id=4，保留 standard
  三帧投票语义，并说明旧源码中 tank 原本通过 `/visual/service` 发框中心。
- 新增 `vision_ws/scripts/test_legacy_rknn_dual_mapping.py`，验证旧双模型类别偏移和
  三帧投票契约；与已有板端标定/数据集评测测试均通过。
- `ssh_orangepi_run.py` 支持长命令超时和多 argv 拼接，`ssh_orangepi_copy.py` 用于
  评测脚本传输；本轮仅传输了脚本，没有启动回放。
- 同步 `VISION_2026_ROADMAP.md`、`VISION_2026_ORANGEPI5PLUS_EXECUTION_PLAN.md` 和
  `vision_ws/src/uav_vision/README.md`：新增完整报告入口，明确旧双模型视频暂缓、旧双
  模型图片全集为全零，以及死机按整机负载风险处理。

### 结果摘要

- v5merge 全集为 1395 张图、1394 个目标，统一 `conf=0.25`、IoU=0.5；merged FP32
  RKNN 为 P/R/F1/M50=`1/1/1/1`、M50-95=`0.98587`，总耗时 P50/P95=`57.92/106.86 ms`；
  region FP32 为 `0.99785/0.99928/0.99857/0.99835`、M50-95=`0.97438`。
- merged/region 两款 FP32 已有真实视频带框回放；四款 INT8 也有视频和性能 JSON，但
  图片全集与视频均为零有效框，不能部署。
- 旧 standard+tank 图片合并结果为 TP=0、FP=0、FN=1394，P/R/F1/M50/M50-95 全为 0；
  两次 NPU 推理合并 P50/P95=`123.18/163.40 ms`。旧双模型视频没有在本轮产生，报告
  明确标注为未完成，不将低分诊断候选冒充有效识别。
- 完整结果与所有已生成视频见：
  [BOARD_MODEL_COMPLETE_EVALUATION_20260716.md](/home/xhj/liftrace/docs/BOARD_MODEL_COMPLETE_EVALUATION_20260716.md)。

### 验证结果

```text
python3 -m py_compile top_level_scripts/board_legacy_rknn_dual_viewer.py \
  vision_ws/scripts/test_legacy_rknn_dual_mapping.py
python3 vision_ws/scripts/test_legacy_rknn_dual_mapping.py
python3 vision_ws/scripts/test_board_viewer_calibration.py
python3 vision_ws/scripts/test_board_rknn_dataset_metrics.py
```

结果：四项均通过。已核对现有 6 个板端六分类采样视频和 2 个本机 PT 带框视频可以在
本机读取首帧；旧双模型视频未执行。

### 遗留问题

- 旧工程没有旧 RKNN runner 源码，只有旧 standard/tank RKNN 二进制；旧视频只能按可审计
  的模型拆分、类别映射和语义投票复现，不能声称逐行复原隐藏实现。
- 香橙派死机原因暂按整机高负载处理，尚无 CPU/NPU/RSS/温度/频率曲线；下一次上板必须
  先低并发短时监控，禁止直接长时间双模型回放。
- INT8 仍需检查量化输入 dtype、校准集和输出 scale/zero-point；FP32 仍未接入 ROS
  视觉链和 CameraInfo/TF。

### 下一步

暂停板端压力扩展，回到视觉组主线：基于 merged FP32 继续真实视频圆环中心、地图投影
误差、stable ID、新鲜度和 `release_evidence` 回放；板端稳定性基线建立后再补旧双模型
视频，且单独记录其诊断阈值与正式阈值结果。

## 2026-07-17（视觉组向导航/控制组 Alpha 1 纯视觉交付包）

### 改动范围

本轮按“控制组仍持有未经改造的旧工程”这一边界，整理了只包含完整新视觉链路的独立
Catkin 交付包。未复制、修改或打包 `patrol_control`、`uav_mission`、`actuator_pwm`、
Fast-Planner、MAVROS 控制或舵机代码；未启动 PX4、飞控、执行机构或任何实机动作。

### 具体改动

- 新增中文交付说明 `docs/VISION_CONTROL_HANDOFF_20260717.md` 与包内
  `vision_ws/src/uav_vision/docs/CONTROL_GROUP_HANDOFF.md`，说明单目地图投影公式、输入
  前提、输出话题、控制消费约束、已验证能力和未验证边界。
- 新增纯视觉入口 `control_handoff_dev.launch`、`control_handoff_board.launch`；默认关闭
  `detect_compat_bridge`，不会把新视觉结果写入旧 `/detect/*` 控制兼容接口。
- 将 `phase_d_board.launch` 的图像、CameraInfo、模型、metadata、地图 frame、地面高度、
  TF 策略、对准模式和调试开关参数化；`phase_d.launch` 增加可选旧接口兼容开关。
- 清除交付运行配置中的本机绝对模型路径；新增六分类 metadata 和包内 `models/README.md`。
  仓库自身 toudi3 联调脚本显式传入原模型路径，因此不改变既有仿真模型选择。
- `uav_vision` 版本更新为 `0.2.1`，安装规则包含 `launch/config/docs/models`；RKNN 节点
  默认不再隐式依赖 `/home/xhj/...` 的旧拆分模型。
- 新增 `top_level_scripts/build_vision_control_handoff_zip.sh` 和
  `top_level_scripts/verify_vision_control_handoff.sh`。构建脚本仅收集 `uav_vision`、有效的
  六分类 PT/FP32 RKNN、metadata、中文说明与证据快照，并排除控制包、历史工程、数据集、
  视频、bag、build/devel、缓存和控制专用 mock launch。
- 按交付要求将包内主链源码的说明性 docstring/注释统一为中文；ROS 话题、消息字段、类别
  名、TF、RKNN 等接口标识保持原名。本项只修改注释，不改变算法行为。
- 同步 `README.md`、`VISION_2026_ROADMAP.md`、`VISION_WORKSPACE_GUIDE.md`、
  `VISION_MIGRATION_CHECKLIST.md` 和 `docs/当前问题与责任边界.md`，明确当前是 Alpha 接口
  联调能力，OrangePi 仅完成离线 FP32 模型证据，尚未完成板端 ROS 相机/TF 长稳闭环。

### 交付物

```text
/home/xhj/liftrace/deliverables/uav_vision_control_handoff_20260717_alpha1.zip
SHA256: c657c63e9c23f210b3c37552625df2e159168aa0a2399391abb7398cd5439948
压缩包：90 个条目，约 13.29 MB 解压内容，约 9.6 MB ZIP
```

包内主要内容为 `uav_vision` 源码/消息/配置/纯视觉 launch、六分类 PT、有效 FP32 RKNN、
中文首读说明、详细控制联调说明、评测证据快照和逐文件 `MANIFEST.sha256`。

### 验证结果

- `verify_vision_control_handoff.sh` 最终通过：逐文件 SHA256 全部一致；无
  `patrol_control/uav_mission/actuator_pwm` 目录或依赖；运行配置、launch 和脚本中无
  `/home/xhj/` 硬编码；在临时目录仅用该 ZIP 解压后，`catkin_make --pkg uav_vision -j1`
  独立编译成功，dev/board 两个 launch 均可解析。
- 新视觉 L0 回归通过：圆环几何、全局实例关联、记忆新鲜度、物理地图 stable ID、无效
  TF 拒绝、地图/释放证据均为 PASS。
- RKNN 输入契约五项测试通过：NCHW/NHWC、解码入口、uint8 保持和非重复 `/255`。
- 六分类 PT 权重在 `rl_drone` 环境实际加载成功，类别顺序为
  `bridge, panzer, pillbox, tent, tank, red_cross`。
- 验证脚本首次运行曾因 `set -u` 与 ROS setup 环境变量不兼容失败，已在 source 前后局部
  切换 nounset 后复跑通过；一次 PT 检查仅因命令行引号失败，改用标准输入执行后模型加载
  通过，均非算法或模型失败。

### 当前能力边界

- 普通下视单目相机不需要深度图：精修像素经 CameraInfo 反投影为射线，再用源图像时刻
  的 TF 变换到 `camera_init` 等导航 frame，与 `z=ground_z` 平面求交生成地图点。
- MID360/FAST-LIO 的职责是提供定位/TF，不是给相机提供逐像素深度；当前尚未利用 MID360
  在线拟合局部地面。地面不平或目标不在已知平面时必须补雷达局部平面、测距、深度或多
  视角约束，不能继续把固定 `ground_z` 当真值。
- 当前可输出六分类检测、红十字/蓝环/H 几何证据、同帧关联、连续帧确认、置信度与类别
  投票、地图点质量融合、stable ID、长期记忆、候选排序、像素偏差和释放证据。
- `selected_target` 是视觉排序建议，`drop_ready/release_evidence` 是视觉证据；它们都不是
  导航命令或舵机许可。控制组必须消费合法且新鲜的 `map_point`，并自行完成任务仲裁、
  可达性、避障、重复投递保护和最终动作许可。

### 遗留问题与下一步

- 尚未在控制组原始工程中做真实 overlay 编译和话题联调；尚未用地图候选驱动飞机闭环。
- 尚缺真实单下视相机内外参归属、CameraInfo/TF 时间契约、跨视角地图误差、30-seed 和
  10 分钟 OrangePi ROS 长稳验证。
- 后续建议由导航/控制组先在 mock/仿真中只消费 `map_valid=true` 且 frame、新鲜度、关联
  和拒绝原因均合法的地图点；在接口契约通过前不连接真实投递动作。

## 2026-07-31（规则确认：tank 靶标得分权重 = 5）

### 改动范围

赛委会补充确认坦克靶标得分权重为 `tank=5`，解除此前"规则书未明确 tank 权重"的标注。
同步更新运行配置、代码默认值、契约测试和活文档；未启动 ROS、PX4、Gazebo、执行机构或
任何实机动作，未改动旧控制/执行源码。

### 具体改动

- `vision_ws/src/uav_vision/config/target_memory.yaml`：`priority_tank` 由工程临时值
  `1.0` 改为规则确认值 `5.0`，注释更新为"赛委会已确认 tank 正式得分权重为 5"。候选
  排序中 tank 现在高于 panzer(2.5) 与 bridge(2.0)，仍低于 red_cross(10)。
- `vision_ws/src/uav_vision/scripts/target_memory.py`：`~priority_tank` 默认值同步
  `1.0` → `5.0`，与 yaml 一致。
- `vision_ws/scripts/test_target_memory.py`：Test 4.4 排序测试构造数据 `tank: 1.0` →
  `5.0`（镜像代码默认值）；顺带修正既有过期断言 Test 3 中 `priority_bridge == 0.0`
  → `2.0`（该断言在 2026-07-16 恢复 `priority_bridge=2.0` 后未同步，属既有失败）。
- `AGENTS.md`：规则 4 权重表补充 `tank=5`；删除"tank 权重表述存在缺口"注意段并注明已
  确认；规则 10 示例改为泛化表述（tank 不再作为不确定项示例）。
- `VISION_CHAIN_COMPARISON.md`：当前优先级口径 `tank=1.0` → `5.0`。
- `vision_ws/src/uav_vision/README.md` 与
  `vision_ws/src/uav_vision/docs/CONTROL_GROUP_HANDOFF.md`：更新权重列表与"tank 正式
  权重仍待确认"表述。

### 验证结果

- `test_target_memory.py` 重跑：**27 passed, 0 failed**（基线为 26 passed / 1 failed，
  唯一失败为上述过期 bridge 断言）。
- 排序断言保持成立：red_cross 最高、bridge 最低。

### 遗留问题

- 历史日期文档（`VISION_CONTROL_RULE_ADAPTATION_20260707.md` 等）中"tank 权重未明确"
  的记录按"日期文档为历史快照、不滚动改写"规则未改动，以本条记录作为新规则取代。
- 规则权重只影响 `selected_target` 候选排序；任务层最终是否优先投递 tank 由
  Mission Manager/安全仲裁决定，视觉组不擅自改变释放许可逻辑。

### 下一步

- 恢复 V-CL-02 推进时，把 tank=5 一并纳入三投优先级验证；无其他代码行为变更。

## 2026-08-01（旧链全量仿真打通——两轮运行）

### 改动范围

在本机 WSL2 上启动旧工程完整 SITL 链路（PX4 + Gazebo toudi3.world +
MAVROS + FAST-LIO + FreeDOM + Fast-Planner + patrol_control + 旧视觉三检测器），
验证从自动起飞到航点导航、对准尝试、超时保护到自动降落的旧控制链端到端可行性。
未修改旧控制/执行源码，未启动执行机构，未做任何实机动作。

### 第 1 轮：基础无 YOLO 仿真

- `patrol_full_competition_sim.launch` 原样启动，环境变量修复
  `ROS_PACKAGE_PATH` 包含 PX4 gazebo-classic 路径
- 8 个航点（1 起飞 + 3 投递 + 3 返航 + 1 降落），`patrol_toudi3.yaml`
- 对齐 15s 超时自动跳下一个点（无 YOLO 类别验证），auto_land 兜底降落
- 结果：✅ 完整链路 114s 飞行时间，19,925 行日志，状态机
  Takeoff→Run_point→Aligning(×3)→Land 全流程正确流转
- 新增包装启动脚本 `top_level_scripts/launch_toudi3_full_sim.sh`，
  固化正确环境变量（solve `mavlink_sitl_gazebo` 包查找失败）

### 第 2 轮：带 YOLO 节点（模型加载成功，图像解码失败）

- 在 `vision_ws/src/yolov5_detect` 基础上尝试启动旧 YOLO 双模型
  （best.pt 4 类 + tank.pt 1 类）
- 创建 `top_level_scripts/yolo_detect_sim.launch`：launch-prefix 通过
  `bash -c 'source conda && conda activate rl_drone && exec python3 -u $0'`
  使 yolo_detect.py 运行在 rl_drone 环境
- 修复 `yolo_detect.py` 执行权限（chmod +x）
- 结果：
  - ✅ `load model successfully`（ultralytics 8.4.33 via conda rl_drone）
  - ❌ `image_callback` 崩溃：ROS 系统 Python cv_bridge（OpenCV 4.2.0）与 conda
    rl_drone OpenCV 4.13.0 的 numpy 数组内存布局不兼容，`imgmsg_to_cv2()` 失败
  - ❌ 后期 `libp11-kit.so: undefined symbol ffi_type_pointer`（conda libffi 冲突）
  - 其余链路正常运行（Aligning 超时→NextPoint→Land），94,708 行日志

### 验证结果

- 两轮均完成 Takeoff → 3×Detect_point（Aligning 超时）→ Landing 全流程
- PX4+Gazebo+MAVROS 在 WSL2 中稳定运行（含 MID360 仿真点云 + FAST-LIO + FreeDOM）
- 节点全部在线：px4、mavros_node、uav_arming_node、patrol_control、traj_server、
  waypoint_generator、circle_detector_node、cross_detect、landing_detector_node、
  fastlio_mapping、freedom_node（第 2 轮另有 yolo_detect、image_process、sim_helpers）
- `mavlink_sitl_gazebo` 包查找问题已通过 `ROS_PACKAGE_PATH` 修复

### 遗留问题

- 第 2 轮 YOLO 图像解码崩溃（cv_bridge 版本冲突）；板端同样存在系统 ROS + conda
  环境分离问题，2026 新链 `uav_vision` 通过全部在 conda 内运行避开了此问题
- 两轮均无实际投递：Aligning 阶段因 YOLO 崩溃导致 `align_ok` 始终为 false，
  无 Servo 服务端（actuator_pwm 未启动）
- 圆环检测（HSV 颜色分割）在 Gazebo 中的有效性未单独验证
- 缺少 Servo mock 来验证完整的检测→对准→投递→下一目标闭环

### 下一步

- 为仿真补齐 Servo mock 和 YOLO 类别输出（`sim_helpers.py`），跑通完整投递闭环；
  需用系统 Python 手动 numpy 解码绕过 cv_bridge 冲突
- 验证 Gazebo 中圆环检测是否工作；若不工作需一并 mock
- 完成后更新本台账

## 2026-08-01（旧链全量仿真投递闭环首次完整跑通）

### 改动范围

在旧工程全量仿真基础上，创建 `top_level_scripts/sim_helpers.py` 补充
YOLO 类别 mock + 圆环检测 mock + Servo mock，使 patrol_control 的
投递状态机（ClassCallback → align_ok → WayPointDetectDone → executeDropAction）
能够在 Gazebo 仿真中完整走完起飞→对准→投递→下一目标→降落的闭环。
未修改旧控制/执行源码。

### 关键技术细节

1. **YOLO mock**：`/yolo_detect` 发布 `"panzer"`（匹配 `goal = {"panzer"}` 默认列表）
2. **圆环 mock**：`/detect/waypoint_mark_point` 发布当前位置（GPS 误差 0，
   `CrossDetectionDone dis_to_next_position: 0.00`）
3. **Servo mock**：提供 `/Servo` 服务，任意 req 返回 `res=true`
4. **timer 驱动**：10 Hz ROS Timer（不依赖相机图像回调，彻底避免 cv_bridge 问题）
5. `sim_helpers.py` 使用系统 Python 3 + rospy（无需 conda），手动 numpy 解码
   仅作为备选——实际因 timer 驱动已不再需要图像处理

### 验证结果（第 3 轮仿真）

| 指标 | 数值 |
|------|------|
| 飞行链 | Takeoff → 3×Aligning → 3×Drop → Landing ✅ |
| Drop action completed | **3/3**（826s, 851s, 875s） |
| CrossDetectionDone | 2,040 次（距离误差 0.00m） |
| YOLO Class:panzer | 76 次 |
| Servo 调用 | 6 次（servo_complete.data: 1） |
| 日志行数 | 34,344 |

### 文件变更清单

- 新增 `top_level_scripts/sim_helpers.py`：timer 驱动的仿真辅助节点
- 新增 `top_level_scripts/launch_toudi3_full_sim.sh`：固化环境的启动脚本
  （ROS_PACKAGE_PATH 含 PX4 gazebo-classic、vision_ws source、
   等待 roscore 后自动启动 sim_helpers）
- 新增 `top_level_scripts/yolo_detect_sim.launch`：YOLO 仿真 launch
  （launch-prefix conda rl_drone；因 cv_bridge 冲突未被最终方案采用）
- 新增 `top_level_scripts/toudi3_full_competition_with_yolo.launch`：
  包装 launch（未被最终方案采用）
- 修复 `vision_ws/src/yolov5_detect/scripts/yolo_detect.py` 执行权限

### 遗留问题

- 圆环 HSV 检测在 Gazebo 渲染下不工作（"no valid circle"），由 sim_helpers
  mock 补齐；实机不同，需单独标定 HSV 参数
- YOLO 图像处理（cv_bridge）在 conda/系统 Python 混合环境下崩溃
  （libp11-kit.so libffi 冲突），2026 新链已通过纯 conda 运行规避
- 仿真中无 actuator_pwm 实体，Servo 动作为纯软件 mock
- 三个投递点中第 3 个的 Drop 日志不完全（仅 servo_complete.data: 1，
  无 "Drop action 3 completed" 显式日志），需进一步查 patrol_control
  DropSystem 计数器逻辑
- 走廊穿越/避障未在 toudi3.yaml 简化路线中体现（无走廊段航点）

### 下一步

- 如需完整比赛路线（含走廊穿越），需在 toudi3.world 中补齐走廊模型，
  并在 patrol_toudi3.yaml 中补充对应航点
- 可将 sim_helpers 的圆环 mock 改为基于 world 坐标系真值坐标发布
  （当前发布 drone 位置会在早期帧产生零误差，不够严格）
- 恢复 V-CL-02 推进：把新旧链联调，让新视觉 `selected_target.map_point`
  驱动旧控制完成真实视觉引导的投递

## 2026-08-01（补录：走廊仿真 v1-v4、sim_helpers 迭代与监控工具）

### 改动范围

本条补录 02:09-03:57 时段的工作：走廊配置创建与三次迭代仿真、sim_helpers
发布语义迭代、监控与几何分析工具。未修改任何旧源码/旧 launch（patrol_control.cpp
等 mtime 未变，legacy_baseline 快照有效）。

### 具体改动

1. `patrol_toudi3_real.yaml`（新建，含后续修改）：15 航点完整路线
   （4 投递 + 走廊穿越 + 降高 + 返航 + 降落），`waypoint_skipping_index: 4`
2. `sim_helpers.py` 第二次迭代：circle mock 发布语义从"当前位置"
   改为"`/mavros/setpoint_position/local` 目标点"
3. 走廊仿真四轮：
   - v1（发布当前位置版）：Drop 1-2 成功，走廊出口卡住
   - v2（发布 setpoint 版）：Drop 1-3 全成功，出口 (0.5,7.5) 被北墙阻挡
   - v3（出口移 0.3,7.2）：Drop 1-3 全成功，仍被北墙阻挡（Fast-Planner 正确拒穿墙）
   - v4（出口移 0.3,6.5）：Drop 1-2 成功，投递 3 后 PX4 HEARTBEAT 断连
4. `inspect_world.py`（新建）：解析 world 模型/link pose
5. `sim_monitor.py`（新建）：实时/离线状态监视器（起飞/对准/投递/航点切换时间线）

### 验证结果与真实结论（经几何核查 + 日志复核）

- **走廊段从未真正执行**：patrol_control.cpp L1038 `detect_point_counter>=3` 时
  `waypoint_next = size - waypoint_skipping_index`（15-4=索引11），投递后直接跳
  降高点，跳过走廊航点（索引 5-10）。v3 日志实锤：Next Point 序列 1→2→3→11。
- **toudi3.world 真实几何**（子代理复核）：场地封闭无门，北墙 Wall_9 连续 8m
  无开口；西侧通道宽仅 0.59m（x=-4.04 中心线），且 y∈[2.05,2.85] 被 big_box3
  封堵；yaml 注释把 Wall_4/Wall_13 坐标写错（张冠李戴），x=-4.5 走廊线实际在
  墙西缘外 1.5cm。
- **"投递成功 3/3"可信度受限**：servo_complete.data:1 仅代表 mock 服务 ACK
  （无条件返回 true），不代表机构动作；且第二轮/第三轮验证日志已不在 /tmp
  （未归档）。
- **v4 高度异常澄清**：所有日志中 z 均正常（0.10-1.30m），无 z=15.4 位置数据；
  之前观察到的 "15.4" 是 sim 时间戳子串（815.422 等）。真实异常是投递降高
  z=0.10 贴地飞行判定与 PX4 断连。
- 降高航点 (1.5,5.0) z=0.15 与 Big box 4（x∈[0.29,1.49], y∈[4.71,5.51],
  顶高 1.25m）碰撞，必撞。

### 遗留问题与下一步

- **立即**：补录完成（本条）；按 review 修正走廊方案前必须先改跳转逻辑或引入
  corridor 模式开关，否则走廊航点永远不会被执行
- 修正 real.yaml：删除第 4 个重复投递点、降高航点移出 Big box 4 包络、
  按真实几何重绘走廊（x≈-4.04 通道中心线）
- sim_helpers 落地 world 真值版（固定靶标坐标），Servo mock 加最小状态校验
- 建立 git 基线（当前 /home/xhj/liftrace 无 .git），日志统一归档不再只留 /tmp
- executeDropAction 服务调用失败静默问题需补 ROS_ERROR（实机风险）

## 2026-08-01（git 基线建立：GitHub 远程仓库 + 凭据配置）

### 改动范围

为 /home/xhj/liftrace 建立 git 版本控制基线并推送到 GitHub 远程仓库
（Qinling-Melon-Farmers/liftrace-visionwork）。未修改任何代码。

### 具体改动

- 新建 `.gitignore`：排除 build/devel/install、日志、bag、大文件
  （vision_ws/runs 156M、test_data 25G、*.pt/*.onnx/*.zip/*.pdf/*.pcd）、
  凭证文件（*token*/*.credentials/.git-credentials）、历史副本目录
  （Desktop_patrol_uav_ws-patrol_planner）、垃圾文件（根目录 1/=、runs/）
- `git init` + main 分支，分 4 块提交：
  1. `docs:` 项目文档与顶层仿真工具（81 文件）
  2. `chore:` 旧集成工作区源码基线（1614 文件，含 toudi3.world）
  3. `chore:` 视觉工作区源码基线（567 文件，含 uav_vision 全链）
  4. `chore:` legacy 基线快照与历史对照资产（413 文件）
- 凭据：`~/.git-credentials`（chmod 600，x-access-token 格式），
  `credential.helper store`；remote origin = https 地址；该文件在
  ~/ 目录不在仓库内，且 .gitignore 含 *.credentials/.git-credentials 双保险
- 推送：`git push -u origin main` 成功（4 个提交全部上传）

### 验证结果

- `git log --oneline`：4 个提交（docs + chore ×3），中文 conventional 标记
- `git status`：仅剩 3 个不入库垃圾文件（1、=、runs/）
- 推送成功：origin/main 已建立跟踪

### 遗留问题

- 根目录垃圾文件（1、=、runs/detect）未入库也未删除，待用户确认后清理
- 后续每阶段提交约定见 AGENTS.md 新规则 15

### 下一步

- AGENTS.md 增补 git 工作流规则 15（中文 commit、token 管理、禁止入库项）
- 按计划推进：legacy 备份 → ffmpeg → 补丁 A → mock 动态化 → 走廊仿真

## 2026-08-01（git 提交身份修正：改用 GitHub 账号 + ffmpeg 安装）

### 改动范围

- git 提交身份从"肖怀瑾 <xhj@liftrace.local>"改为 GitHub 账号
  "Qinling-Melon-Farmers <xiaohuaijin@mail.nwpu.edu.cn>"（邮箱为账号绑定邮箱，
  GitHub 凭此关联账号头像），并重写全部 5 个历史提交的作者/提交者身份
- ffmpeg 4.2.7 安装成功（WSL 无密码 sudo）

### 具体改动

- `git config --global user.name "Qinling-Melon-Farmers"`、email 绑定邮箱
- `git filter-branch --env-filter` 重写历史提交（xhj@liftrace.local → 新身份），
  修正一次 COMMITTER_NAME 拼写（Qinglin→Qinling）
- `git push --force origin main` 强制更新远程（历史未被他处 clone，安全）
- `sudo apt-get install -y ffmpeg` → 4.2.7，录屏方案就绪

### 验证结果

- `git log`：5 个提交全部为 Qinling-Melon-Farmers <xiaohuaijin@mail.nwpu.edu.cn>
- 远程 origin/main 已强制更新（+ e29b3cb...c5f1bc2）
- ffmpeg -version：4.2.7-0ubuntu0.1

### 遗留问题

- 历史提交 SHA 全部变化（filter-branch 重写），无外部 clone 依赖，可接受
- 根目录垃圾文件（1、=、runs/）仍未入库

### 下一步

- 阶段 5：patrol_control 补丁 A（goal 参数化）→ 编译
- 阶段 6：sim_helpers mock 动态化

## 2026-08-01（补丁 A：goal 类别列表参数化）

### 改动范围

patrol_control 的目标类别匹配列表 goal 由硬编码 {"panzer"} 改为可从
~goal_list 参数加载（XmlRpc 数组），无参数时 fallback {"panzer"} 保持旧行为。
为 sim_helpers mock 动态发布类别与后续新视觉链多类别接入做准备。

### 具体改动

- `src/patrol_control/include/patrol_control/patrol_control.h`：goal 去掉硬编码初值
- `src/patrol_control/src/patrol_control.cpp` 构造函数：~goal_list 参数读取
  （TypeArray 校验，逐项 push_back），无参数 fallback {"panzer"}，
  ROS_INFO 打印加载数量

### 验证结果

- `catkin_make --pkg patrol_control -j1` 编译通过，devel 二进制更新（05:11）
- 旧配置（patrol_toudi3.yaml 无 goal_list 键）行为不变

### 遗留问题

- 待阶段 7 在 real.yaml 中加入 goal_list 全类别验证

### 下一步

- 阶段 6：sim_helpers mock 动态化（TARGET_TABLE + 就近类别）

## 2026-08-01（sim_helpers mock 动态化）

### 改动范围

sim_helpers.py 的 YOLO mock 从写死 "panzer" 改为按就近靶标动态发布类别，
全部参数经 ~ 私有参数读取（不硬编码，遵守规则 7）。

### 具体改动

- 新增 DEFAULT_TARGET_TABLE（toudi3.world 靶标真值 → 6cls 类别）：
  dibao→pillbox、qiaoliang→bridge、zhangpeng→tent、zhuangjiache→panzer、
  tanke→tank（坐标与 compute_free_waypoints.py 一致）
- 类别选择：按参考点（setpoint 目标或 drone 位置）到各靶标平面距离取最近，
  距离 ≤ class_radius（默认 1.2m）发布该类别，否则发布 "Nothing"
- 新参数：~target_table（可覆盖）、~class_radius、~class_reference
  （setpoint|drone）、~circle_mode（setpoint|target_true 严格模式）、
  ~servo_mock_enable（guarded 链关闭用）

### 验证结果

- py_compile 通过
- 就近逻辑单测：zhuangjiache→panzer、dibao→pillbox、场地中心→Nothing
  （最近 tent 1.05m 在半径内，符合预期——中心其实离靶近，半径参数可调）
- 补丁 A 的 goal_list 支持使动态类别能被 ClassCallback 匹配

### 遗留问题

- 场地中心 (0,0) 距离 tent 仅 1.05m < 半径 1.2m，降落/起飞阶段可能误发 tent；
  实际由 detect_control 门控（非 Detect_point 不发），仿真联验确认
- target_true 严格模式（circle mock 发真值坐标）留作进阶验证，默认 setpoint

### 下一步

- 阶段 7：走廊改墙 + 补丁 B + real.yaml 修正 + 全量仿真

## 2026-08-01（走廊墙几何多轮修正：门洞恢复原始位置 + 1.4m 加宽）

### 改动范围

toudi3.world 走廊墙几何经多轮修正，最终确定方案：两堵墙（Wall_15/Wall_16）
**全部恢复到原始位置**，门洞通过段（V1/V2/V3）修改保持加宽 1.4m 且东边界
与 v4 及以前一致（-2.480）。本轮修正了"只恢复 Wall_15、Wall_16 仍东移 0.61m"
的遗漏（用户指出）。

### 具体改动（toudi3.world，相对原始 diff 12 处 pose）

| 项 | 原始 | 当前 | 说明 |
|---|---|---|---|
| Wall_16 link | -3.18386 | -3.18386 | 恢复原始（此前误留 -2.57386） |
| Wall_16 state | -3.66886 | -3.66886 | 恢复原始（此前误留 -3.05886） |
| Wall_15 link | -2.38386 | -2.38386 | 恢复原始（此前 -2.37386） |
| Wall_15 state | -2.86886 | -2.86886 | 恢复原始（此前 -2.85886） |
| V2 段（西半高） | 0.643127 | **1.234** | 西端贴 Wall_6(-4.335)，东端 -3.871 |
| V1 段（底部） | 0.243127 | -0.356873 | 东移 0.6（覆盖门洞底部） |
| V3 段（顶部） | 0.011255 | -0.588745 | 东移 0.6（门洞顶部横梁） |
| V0 段（东全高） | -0.631873 | -0.631873 | 恢复原始（东边界 -2.480） |

最终几何（compute_free_waypoints.py 验证）：
- Wall_16: x[-3.744,-3.594]（原始）
- V2: x[-4.335,-3.871]（西端贴 Wall_6）
- V0: x[-2.480,-1.994]（东边界 -2.480 = 原始）
- **门洞: x∈[-3.871,-2.480]，宽 1.4m，z∈[0.5,1.3] 可穿** ✓

### 过程教训（记录避免重犯）

1. 方案 A 挪动了两堵墙（Wall_15+Wall_16），后续修正只动了 Wall_15，Wall_16
   遗留东移状态——**每次改墙必须 git diff 全量核对**
2. 段 pose 在 yaw=π 的 link 下方向反转（段 +0.4 = 世界 -0.4），曾改反方向
3. V2 段计算时把"中心"当"东端"，导致门洞 0.94m 而非 1.4m——**必须用工具输出验证**
4. 工具"门洞判定"部分有硬编码文本（子代理遗留），只信动态段输出

### 遗留问题

- 走廊（Wall_6 到 Wall_16）恢复原始 0.59m 窄通道，飞机不再走走廊、
  从主场地直穿门洞进北区（航点已适配）
- v8 仿真运行中（未验证门洞）

### 下一步

- 重启 v9 仿真验证门洞穿越（航点 5→6: -2.3,4.0 → -2.3,4.45）
- 每轮改墙后立即跑工具 + git diff + 更新本台账

## 2026-08-01（v9 走廊仿真：门洞穿越成功 + 北区点云退化分析 + patrol_uav_ws.zip 定位源差异）

### v9 仿真结果（world 恢复原始墙位 + 门洞 1.4m）

- 投递 3/3 完成（825/849/917s），动态类别随航点变化（pillbox→tent→panzer）
- **门洞穿越成功**：航点 1→2→3→4→5→6→7→8→9 全部推进（无 3→11 跳变），
  飞机穿过 Wall_15 门洞（x∈[-3.871,-2.480]，z=1.0）进入北区
- 日志 78199 行；录屏 262KB（ffmpeg 被强杀，moov 缺失不可播放）

### v9 最后阶段乱飞根因分析

- "No Effective Points!" 警告自 993s（走廊段）持续出现（FAST-LIO 地图构建）
- 最后飞机停在北区 (-2.29, 5.23) 附近徘徊，航点 9 后不再推进
- **根因**：MID360 在狭窄走廊（0.59m）/门洞（1.4m）/门框（0.9m）区域点云退化，
  有效点不足 → 地图不更新 → Fast-Planner 失去可靠障碍信息 → 规划失效
- 激光雷达在窄空间的退化是仿真暴露的**实机同样存在的风险**（需板端验证）

### patrol_uav_ws.zip（2026-07-08 打包）关键差异：定位源

zip 工程 `mapping_mid360.launch` 含 **`fastlio_to_px4_vision` 节点**
（patrol_control 包内，`/Odometry` → `/mavros/vision_pose/pose`，frame=map）：
- **zip 版**：PX4 定位来自 FAST-LIO 雷达位姿（vision_pose_estimate 融合）——
  "雷达发位置"工程
- **当前版**：该节点被移除，PX4 只用 IMU（/mavros/imu/data）——"IMU 发位置"
- mid360.yaml 两版一致（imu_topic=/mavros/imu/data）
- **相关性**：当前版 PX4 位姿（IMU EKF）与 FAST-LIO/FreeDOM 位姿不同源，
  坐标漂移不一致可能是历次 setpoint z 异常（-0.38/15.4）与乱飞的根因之一

### 遗留问题与下一步

1. **移植 fastlio_to_px4_vision**：把 zip 版节点（cpp+CMake+launch）移植到当前
   工程，统一 PX4 与 FAST-LIO 定位源，验证 z 异常是否消失（需备份后最小补丁）
2. **点云退化**：走廊窄空间点云失效需实测确认；考虑降低 FAST-LIO
   filter_size 或增加 point_filter_num 调整
3. **录屏完整性**：ffmpeg 必须优雅停止（SIGINT 写 moov）再归档；
   强杀（pkill -9）导致 mp4 不可播放
4. v9 录屏文件留待修复（可用 ffmpeg -i 修复或重录）

## 2026-08-01（toudi3.world 恢复最早期原始版本）

### 改动范围

按用户指示，将 toudi3.world 的墙体模型全部恢复到最早期原始版本
（git 首次提交 0ddc845 中的样式），撤销此前所有走廊墙改动
（Wall_15/Wall_16 link+state+段 pose 共 14 处差异）。

### 具体改动

- `git show 0ddc845:patrol_uav_ws-patrol_planner/toudi3.world` 覆盖恢复
- 验证：md5 302d1e2118dbdc7641421c863be9c4bc（与原始一致）；
  git diff 0 差异；几何工具输出 Wall_15 回到原始
  （V2 [-3.744,-3.280]、V0 [-2.480,-1.994]、门洞 0.8m 原始宽度）

### 验证结果

- 恢复后与 git 基线 100% 一致
- 几何工具输出与原始 world 完全一致（门洞回到 0.8m 原始宽度）

### 遗留问题

- patrol_toudi3_real.yaml 仍为走廊航点配置（基于改墙后几何），
  与原始 world（门洞 0.8m、走廊 0.59m）不匹配——后续若用原始 world
  跑仿真需同步调整 yaml（或回退到 patrol_toudi3.yaml）
- sim_helpers/补丁 A/B 不受影响（与 world 几何无关）

### 下一步

- 待用户确定基于原始 world 的走廊方案（改 yaml 航点或重新设计）

## 2026-08-02（原 2025 链完整仿真阻塞归档与 world 分级改造方案）

### 改动范围

对最新原 2025 链运行日志、当前 ROS 参数、`patrol_toudi3_real.yaml` 航点语义、
`toudi3.world` Wall_15 原始门洞、PX4 `iris.sdf` 碰撞包络和 Fast-Planner 仿真参数进行
只读复核；新增阻塞专题和门区几何示意图，同步路线图、仿真指南、责任边界与 README。
未修改原始 `toudi3.world`，未修改用户当前未提交的 `patrol_toudi3_real.yaml`，未启动
实机、执行机构或新的飞行仿真。

### 具体改动

1. 新增
   `docs/2025原始链完整仿真阻塞与WORLD改造方案_20260802.md`：
   - 记录自动起飞、三次 mock 投递、第 4 个 Detect 触发 Servo 4 错误、门南侧到达、
     穿原始门失败和 PX4 heartbeat 断开的时间线；
   - 将阻塞拆成 B1～B10，并区分“已确认现象”和“待验证假设”；
   - 明确 `flag_planner_px4=true` 为直达模式，不能将其结果写成 Fast-Planner 避障；
   - 建立 R0～R6 分层 Gate，避免每轮先跑三投再定位门区问题；
   - 规定原始 world 不动，后续另建 1.20 m 开发门和 0.80 m 规则门派生 world；
   - 给出门中心线、航点顺序、world collision/visual/state 一致性和报告 manifest 要求。
2. 新增 `docs/assets/toudi3_original_gate_geometry.svg`：
   - 可视化原始门宽 0.8 m、垂直开口 0.5～1.3 m；
   - 记录 `iris` 旋翼碰撞包络约 `0.516 × 0.696 m`；
   - 航向对正时每侧理论余量约 0.142 m，另一方向接近横门时仅约 0.052 m。
3. 更新 `docs/TOUDI3_FULL_SIM_GUI_GUIDE.md`：
   - 增加直达模式与 planner 模式的控制所有权判定；
   - 修正五类靶标名称与 world 坐标的旧错配；
   - 明确第 4 个观察点不得继续配置为三舵机状态机的 `Detect_point`；
   - 补充 real.yaml/派生 world 显式传参和 Gazebo Show Collisions 截图要求。
4. 更新 `docs/当前问题与责任边界.md`：
   - 增加 2025 载体前置的控制/规划/仿真责任；
   - 明确视觉组对无效 TF/pose 失败关闭，不以窄门失败计算视觉完成度。
5. 更新 `SIMULATION_GUIDE_NOETIC_PX4_GAZEBO_QGC.md`、`VISION_2026_ROADMAP.md`
   和 `README.md`，统一分层 Gate、视觉业务边界和文档入口。
6. 对此前 v9 台账因果口径作后续纠正：
   - `No Effective Points!` 确认点云退化，但在直达模式下不能单独证明它导致飞机乱飞；
   - `fastlio_to_px4_vision` 缺失与 PX4 定位源差异保持为待测假设，须结合 EKF 参数和输入
     话题确认。

### 验证结果

- `toudi3.world` 仍为原始 MD5 `302d1e2118dbdc7641421c863be9c4bc`；
- PX4 `iris.sdf` 机身 collision 为 `0.47 × 0.47 × 0.11 m`，四旋翼圆柱 collision
  半径 `0.128 m`，包络计算与 SVG 口径一致；
- Fast-Planner 仿真参数复核为地图分辨率 `0.10 m`、障碍膨胀 `0.15 m`、
  `clearance_threshold=0.20 m`、`optimization/dist0=0.40 m`；
- `xmllint --noout docs/assets/toudi3_original_gate_geometry.svg` 通过；
- `git diff --check` 通过；
- 文档入口均指向存在的专题文档和 SVG；
- Git 状态仍保留用户原有未提交的 `patrol_toudi3_real.yaml`，本轮未将其纳入修改。

### 遗留问题

- 尚未实际创建 `toudi3_corridor_dev_120.world` 和门区专项 YAML；
- 尚未执行 R0 10 min 进程稳定性、R2 开发门直达、R3 planner 控制或 R4 原始门验收；
- PX4 EKF 实际定位输入、`camera_init`/MAVROS local/FAST-LIO 坐标一致性仍待实测；
- SVG 已通过 XML 结构校验，本轮未启动高负载 Gazebo GUI；后续实际创建派生 world 后再按
  俯视、门正视、侧视和 Show Collisions 四类视角留图。

### 下一步

1. 不动原始 world，先创建 1.20 m 派生开发门和最小门区专项航线；
2. 先做 R0 悬停稳定性，再做 R2 直达双向穿门；
3. R2 通过后切 `waypoint_mode=false` 做 R3 planner 所有权和轨迹验证；
4. 最后回归 0.80 m 原始门，并将稳定载体交给新视觉 shadow/任务闭环。

## 2026-08-04（阶段0：仿真统一 run 目录规范与日志基建）

### 改动范围

建立"一次仿真 = 一个 run 目录"的统一规范，解决日志散落 /tmp、ffmpeg 强杀
moov 缺失、无自动归档/命名/时间线的问题。未修改任何旧链源码/launch/world。

### 具体改动

- 新增 `top_level_scripts/sim_run.sh`：统一仿真入口。生成
  `logs/<场景名>_<日期>_<时间>/`（含 manifest.yaml 头部：场景/git HEAD/
  启动命令/ffmpeg PID）、run.log、可选 ffmpeg 录屏（SIM_NO_RECORD=1 关闭）；
  前台运行 roslaunch；结束后自动：ffmpeg SIGINT 优雅停止（写 moov）→
  ffprobe 校验（损坏则 -movflags +faststart 重封）→ 归档 ~/.ros/log/latest/
  → sim_monitor.py 生成 timeline.txt → manifest 补尾部（退出码/结束时间/
  录屏状态）
- `AGENTS.md` 增补规则 16：仿真必须走 sim_run.sh，禁止裸 roslaunch 重定向
- 现有 `stop_toudi3_sim.sh`（TERM→2s→KILL 优雅停止）核对后保留
- 冒烟测试：`sim_run.sh smoke_test echo hello` 生成完整 run 目录
  （manifest/run.log/timeline 正常）

### 验证结果

- bash -n 语法通过；冒烟测试 run 目录产物齐全、退出码 0
- roslog 归档修正为 cp -aL（解引用软链接）

### 遗留问题

- 录屏在无 GUI 显示环境下可能无画面（脚本已容错，仅告警不阻塞）
- 后续每轮仿真必须使用 sim_run.sh，变更记录不再出现"日志放哪了"类问题

### 下一步

- 阶段 1：旧链完整复现（R0 进程稳定 → R1 直达三投 → 门区专项）

## 2026-08-04（阶段1 R1：旧链完整流程首次跑通 2/3 投递）

### 改动范围

按 8-02 文档 R1 Gate 要求，新增 patrol_toudi3_r1.yaml（3 Detect + 第 4 观察点
Nothing + 删 z=0.15 贴地段改 0.50），在原始 toudi3.world 上 headless 跑完整流程
（sim_run.sh 规范）。顺带修复 sim_run.sh 环境问题（首轮失败）。

### 具体改动

- 新增 `config/patrol_toudi3_r1.yaml`：3 投递（dibao/qiaoliang/zhangpeng，
  servo 1/2/3）+ zhuangjiache 观察点（Nothing）+ 门洞往返 + 降高（z≥0.50）+
  降落；goal_list 5 类、detect_skip_enable: false、直达模式
- `sim_run.sh` 修复：内置 ROS 环境 source（nounset 兼容）+ PX4/Gazebo 环境
  （ROS_PACKAGE_PATH/GAZEBO_MODEL_PATH/PLUGIN/LD）+ SIM_HELPERS 自动启动
  sim_helpers + SIM_NO_RECORD headless 模式
- 首轮启动失败根因：sim_run.sh 无环境 source → roslaunch 找不到；修复后正常

### 验证结果（corridor_r1_20260804_041640，headless）

- 起飞 → 投递 1（826s）/投递 2（929s）**Drop action completed**（动态类别
  pillbox/bridge 正确）
- 投递 3 超时（886s Time up，servo_complete=0）——未投递
- 航点推进 1→8（穿门洞后北区），随后 **No Effective Points 13159 次**
  （996s 起）点云退化卡在西墙 (-4.33,1.11)——与 v9 相同问题（8-02 B7）
- 无 Invalid servo ID: 0（goal_list 修复生效，对比 2025 版全败）

### 遗留问题

- 投递 3 超时根因待查（zhangpeng 对准曾 align_ok:1 + Class:tent，但 Time up）
- 北区点云退化（B7）仍是北区/走廊段最大阻塞
- 第 4 观察点（Nothing）执行正常与否未确认（卡在北区前）

### 下一步

- 定位投递 3 超时（检查 drop_system 高度/稳定判定与 servo 时序）
- B7 点云退化专项（MID360 在窄空间的有效点统计与缓解）

## 2026-08-04（新增板端旧工程启动手册）

### 改动范围

新增 docs/板端旧工程启动手册.md：完整记录板端旧工程（含 YOLO）的启动方式
（location.sh/vision.sh/1-3_launch.sh/record.sh 四组 tmux 脚本）、各节点 launch
细节、话题订阅关系全图（patrol_control 订阅/发布 26 项 + 视觉检测节点 + 定位
规划链）以及开发机仿真替代对照。纯文档，无代码改动。

### 验证结果

- 手册中话题列表与 patrol_control.cpp 源码（L75-124）核对一致
- 相机/camera_sdk、视觉节点话题与 launch 文件核对一致

### 遗留问题

- 无（文档性质）

### 下一步

- 继续阶段 1 R1 排障（投递 3 超时 + B7 点云退化）

## 2026-08-06（新视觉自主搜索改造前旧控制基线快照）

### 改动范围

在修改旧 `patrol_control` 投递与任务接口前，按 AGENTS.md 规则 14 保存完整源码快照。
本提交不改变运行行为，不启动飞行仿真或执行机构。

### 具体改动

- 新增 `legacy_baseline/20260806/patrol_control/`，保存当前旧控制包完整副本。
- 新增 `legacy_baseline/20260806/FILELIST.txt`，记录 63 个快照文件。
- 新增 `legacy_baseline/20260806/SHA256SUMS`，记录规则 14 要求的快照校验值。

### 验证结果

- `FILELIST.txt` 与 `SHA256SUMS` 均包含 63 条记录。
- 功能分支创建前工作区干净；视觉 L0 回归通过。
- 主工作区重新叠加 `vision_ws` 编译成功，释放安全回归通过，raw mock 顺序为 1/2/3。

### 遗留问题

- 尚未修改第三投失败处理、外部任务模式或覆盖搜索逻辑。
- R1 北区持续 `No Effective Points` 仍待后续覆盖导航 Gate 复核。

### 下一步

- 为 Servo 服务失败/拒绝路径增加回归测试，再最小修改旧控制投递完成判定。

## 2026-08-04（AGENTS.md 新增规则 17：普通开发者定位，不做过度审计）

### 改动范围

用户反馈工作方式：此前存在过多哈希校验与审计性核对（sha256/md5 反复验证、
过度证据链检查），偏离无人机工程开发本职。在 AGENTS.md 增补规则 17 约束。

### 具体改动

- AGENTS.md 新增规则 17：以普通无人机开发者身份工作——
  1. 禁止无意义哈希校验（除非规则 14 legacy 快照或确有必要排查文件改动）
  2. 不做过度审计（不反复核对证据链、不写审计报告、不查无关敏感信息）
  3. 保留安全底线：token 不入仓库、不执行实机危险动作、仿真实机分离
- git diff/git status 作为变更追踪的默认手段

### 验证结果

- 规则 17 已写入 AGENTS.md（规则 16 之后）

### 遗留问题

- 无

### 下一步

- 继续阶段 1 R1 排障（投递 3 超时 + B7 点云退化）

## 2026-08-06（修复统一仿真入口的宿主 Python 环境污染）

### 改动范围

修复 `top_level_scripts/sim_run.sh` 在 WSL 非交互启动时继承 Windows/Anaconda
路径，导致 ROS 系统 Python 导入标准库失败的问题。

### 具体改动

- 在 source ROS 和工作区 overlay 前固定 Ubuntu 系统 `PATH`。
- 清除继承的 `PYTHONHOME`、`PYTHONPATH`，避免宿主 Python 包污染
  `/usr/bin/python3` 与 `roslaunch`。
- 保留失败运行 `logs/drop_fix_fixed3_20260806_010103/`，未删除或覆盖。

### 验证结果

- 失败样本 `drop_fix_fixed3_20260806_010103` 在 `roslaunch` 导入阶段退出 1，
  manifest、timeline 和 ROS 日志归档均保留。
- 统一入口 smoke `logs/release_guard_smoke_20260806_010229/` 正常退出 0。
- smoke 中 guarded raw mock 顺序为 1/2/3，`ReleaseGuardAssertion` 与
  `VisualDeliveryAudit` 均 PASS；manifest、timeline、ROS 日志归档齐全。

### 遗留问题

- 本记录只验证统一入口环境和软件释放回归，固定三投完整 PX4/Gazebo SITL
  仍需继续执行。

### 下一步

- 重新运行 `drop_fix_fixed3` 无 GUI Gate，验证旧控制实际完成三次 guarded ACK。

## 2026-08-06（Task 1：固定三投超时与舵机反馈收尾）

### 改动范围

收尾固定三投投递判定、guarded Servo ACK 日志契约和无 GUI SITL Gate；不启动真实
`actuator_pwm` 或任何实机执行机构。

### 具体改动

- 保留 `DropActionResult` 的无效槽位、服务失败、舵机拒绝和正 ACK 分类；只有正 ACK
  才设置 `drop_complete` / `drop_completed[]`。
- 删除三处旧 `servo status false` 误导文案，改为节流的
  `Awaiting positive Servo ACK; release remains blocked` 等待日志。
- 保持超时路径不再强制调用舵机，并修正 `stopDropAction(servo_id-1)` 为真实槽位。
- 新增源码日志契约测试，防止旧文案回归。

### 验证结果

- 文案测试通过；DropAction gtest 4/4 通过，catkin test summary 8 tests、0 errors、0 failures。
- 主工作区取消包白名单后 30 包串行 `catkin_make -DROS_EDITION=ROS1 -DCATKIN_WHITELIST_PACKAGES= -j1` 通过。
- `release_guard_regression` 统一入口退出 0。
- `drop_fix_fixed3_20260806_014815` 无 GUI SITL：manifest `exit_code: 0`，timeline/roslog
  齐全；raw mock 严格 `slot=1,2,3 result=True`，`FixedThreeDropAssertion PASS`。
- Gate 针对性检查无 `servo status false`、无 `Invalid servo ID: 0`、无 `Time up`、无重复释放或失败 ACK。

### 遗留问题

- 本次 SITL 使用仿真 `sim_helpers` 提供检测标记；日志仍有既存 Gazebo 模型重复生成和
  PX4 健康检查告警，不影响 guarded 三投 Gate。

### 下一步

- 继续 V-CL-02 完整旧控制与新视觉闭环，单独处理 Fast-Planner/点云退化问题。

## 2026-08-06（Task 1 review fix：新视觉槽位零偏移配置）

### 改动范围

修正任务复核发现的新视觉动态投递偏移问题；旧固定链配置保持原有槽位补偿不变。

### 具体改动

- 新增 `patrol_toudi3_new_vision.yaml`，保留标准固定槽位偏移，并将三个
  `dynamic_slot_offsets` 明确设为零。
- `toudi3_full_competition_sim_new_vision.launch` 显式选择该配置，避免新视觉地图候选
  继承旧动态目标的 ±0.10 m 补偿。
- 新增静态回归断言，锁定零偏移配置和 launch 接线。

### 验证结果

- 日志契约与新视觉零偏移静态测试通过。
- 原始 `patrol_toudi3.yaml`、`patrol_toudi3_r1.yaml`、`patrol_toudi3_real.yaml` 未改写，
  其旧动态偏移保持原值。

### 遗留问题

- 新视觉完整固定三投 SITL 属于阶段 2 Gate，待本配置接线后单独执行。

### 下一步

- 推进 `newvision_fixed3` 无 GUI SITL，确认新视觉候选、许可和三次 guarded ACK。

## 2026-08-06（阶段 2 排障设计：最终下降投递承诺）

### 改动范围

记录新视觉固定三投中“高位已稳定对准、最终下降后视觉变陈旧导致无法投递”的根因和
修复边界；本条仅为设计与失败日志归档，不修改运行时代码。

### 具体改动

- 新增 `docs/superpowers/specs/2026-08-06-drop-commitment-design.md`。
- 明确视觉新鲜性检查保持不变，由任务层锁存目标、槽位、对准模式、锁定位置和时间。
- 明确控制端实际调用 guarded `/Servo` 时仍需当前短时许可，不能仅凭历史 `drop_ready`。

### 验证结果

- 保留失败运行 `logs/newvision_fixed3_20260806_041050/`。
- 运行中在仿真时间 806.626 秒出现 `drop_ready=true reason=evidence_valid`；最终下降后持续
  `drop_ready=false reason=stale_observation`，且无 raw Servo 调用。
- 统一入口已收尾，manifest `exit_code: 1`，ROS 日志已归档，timeline 已生成。

### 遗留问题

- 尚未实现投递承诺和控制端许可消费，阶段 2 Gate 仍未通过。

### 下一步

- 先添加失败测试，再实现受超时、水平漂移、模式和槽位约束的投递承诺。

## 2026-08-06（阶段 2 失败复盘：新视觉槽位零偏移与下降承诺验证）

### 改动范围

修正新视觉固定三投配置的实际槽位偏移，并归档 `newvision_fixed3` 无 GUI SITL
失败结果；确认最终下降时视觉陈旧不再阻止已经锁定的投递承诺。

### 具体改动

- `patrol_toudi3_new_vision.yaml` 的 `drop_system.slot_offsets` 和
  `dynamic_slot_offsets` 三个槽位全部设为零；旧 `patrol_toudi3*.yaml` 配置保持不变。
- `test_new_vision_config.py` 改用 YAML 结构化断言，防止 `slot_offsets` 与
  `dynamic_slot_offsets` 的字符串前缀误匹配。
- 保留 `logs/newvision_fixed3_20260806_050108/`，其中包含 `run.log`、
  `manifest.yaml`、`timeline.txt`、ROS 日志归档、Gate 报告和视觉审计报告。

### 验证结果

- 新视觉配置静态测试由预期失败转为 `4/4` 通过。
- 本轮完整 SITL `manifest.exit_code: 1`，Gate 失败：`raw_calls=[1,2]`、
  `success_slots=[1,2]`；前两投均在高位承诺后下降到 `stale_observation` 时完成 ACK，
  证明承诺路径生效。
- 第三投未进入 `drop_circle`，从仿真时间约 858 s 起停留在第三航点导航，非视觉陈旧
  或强制候选投递问题。

### 遗留问题

- 第三航点从 `(-1.903,-0.023,1.5)` 到 `(1.016,0.256,1.5)` 的旧直线路径未在本轮
  完成，需单独验证树障碍/导航控制因素；本次不标记阶段 2 Gate 完成。
- 工作区仍保留阶段 2 承诺实现及其待提交变更，尚未推送阶段完成提交。

### 下一步

- 清理残留 SITL 节点后，用独立导航诊断复现第三航点；确认是否需要仅对新视觉配置启用
  Fast-Planner 或增加不依赖靶标坐标的避障/覆盖控制，再重跑完整 Gate。

## 2026-08-06（阶段 2 分片 1：最终下降投递承诺与控制门控）

### 改动范围

修复高位对准后因最终下降拍不全靶标而撤销投递的问题；只增加参数化任务许可和相机轴
映射，旧模式默认不要求新许可，`actuator_pwm` 与真实执行机构均未启动或修改。

### 具体改动

- 新增 `ReleaseCommitmentPolicy`：高位稳定视觉证据锁存目标、槽位、模式、位置和原始
  证据时间；最终下降后即使当前观测变为 `stale_observation`，仍可在 30 s、0.20 m
  漂移、控制状态、槽位和重复目标互锁内继续授权。
- `patrol_control` 在新视觉模式下只有收到新鲜
  `/mission/release_permission_active` 才调用 guarded `/Servo`；候选或历史
  `drop_ready` 本身不能直接触发投递。像素轴映射改为参数矩阵，新视觉三个槽位偏移为零。
- 任务层审计绑定原始锁定证据时间戳；承诺过期、水平漂移、模式/槽位变化、重复目标或
  缺少新鲜任务许可时继续 fail closed。

### 验证结果

- 主工作区 30 包串行 Catkin 编译通过；`run_tests_patrol_control` 汇总 14 tests、
  0 errors、0 failures；投递承诺策略测试 4/4 通过。
- `logs/newvision_fixed3_20260806_050108/` 的前两投在当前视觉已为
  `observation_fresh=false` 时，仍凭锁定证据获得
  `permission_granted_from_commitment` 并完成 guarded mock ACK。

### 遗留问题

- 本分片是笔记本 PT 仿真，不是 OrangePi/RKNN 或实机验收；实机必须
  重新标定相机外参、中心、下降速度、承诺超时和最大水平漂移。
- 阶段 2 三投 Gate 尚需解决第三航点导航和规划节点内存问题后再完整验收。

### 下一步

- 启用新视觉固定路线 Fast-Planner，并限制 toudi3 规划地图后重跑完整三投 Gate。

## 2026-08-06（阶段 2 分片 2：限制新视觉仿真规划地图）

### 改动范围

修复新视觉固定路线第三航点绕过避障和 Fast-Planner 规划地图过大导致节点被系统终止的
问题；只调整仿真 launch 参数，新视觉搜索仍不读取任何新增靶标坐标。

### 具体改动

- 新视觉固定路线将 `flag_planner_px4` 设为 0，交由 Fast-Planner 生成避障轨迹。
- 为 PX4 仿真 planner 增加可覆盖的地图尺寸参数链；toudi3 新视觉入口使用
  `14 x 14 x 6 m`，通用/旧入口继续保留 `100 x 100 x 50 m` 默认。
- 新增静态回归，检查新视觉地图限制、旧入口默认值、五类配置、零槽位偏移和相机轴映射。

### 验证结果

- `logs/newvision_fixed3_planner_20260806_054250/` 定位到旧 `100 x 100 x 50 m`、
  0.1 m 栅格会创建约 5000 万体素，`fast_planner_node` 随后以 `-9` 退出。
- launch 展开确认 `/fast_planner_node/sdf_map/map_size_x/y/z` 分别为
  `14.0/14.0/6.0`；新视觉配置静态测试 4/4 通过。
- 后续完整 Gate 中 planner RSS 约 137 MiB，进程持续存活并完成第三航点，不再出现启动后
  OOM `SIGKILL`。

### 遗留问题

- 本分片只解决固定路线导航资源配置；新视觉三投 launch、自动 Gate 和状态文档尚未提交。
- 北区持续 `No Effective Points` 仍留待后续覆盖导航阶段按独立 Gate 复核。

### 下一步

- 接入完整相机/模型/mission frame 参数和 `newvision_fixed3` 自动 Gate，提交阶段 2 收尾。

## 2026-08-06（阶段 2 分片 3：新视觉固定三投 Gate 与状态收尾）

### 改动范围

接通笔记本 WSL2/RTX PT 新视觉链与旧固定三投控制的最终无 GUI Gate，并同步 Roadmap 与
迁移清单；原始 world、旧视觉默认入口和真实 `actuator_pwm` 均未修改或启动。

### 具体改动

- 新增 `newvision_fixed3.launch` 和自动 Gate，完整透传相机图像、CameraInfo、PT 模型、
  mission frame、相机外参与稳定帧参数；旧检测器和 `sim_helpers` 不启动。
- `sim_run.sh` 导出 run 目录给结构化报告，并以 Gate 报告修正最终退出码，失败 Gate 不再
  被 roslaunch 的零退出码误记为成功。
- 更新 `VISION_2026_ROADMAP.md` 与 `VISION_MIGRATION_CHECKLIST.md`：V-CL-02 标记完成，
  V-CL-03 按用户要求暂停，未开始外部任务模式或全图覆盖搜索。

### 验证结果

- 30 包串行 Catkin 编译通过；`run_tests_patrol_control` 汇总 14 tests、0 errors、
  0 failures；新视觉配置 5/5、投递承诺策略 4/4 通过。
- 保留失败运行 `logs/newvision_fixed3_20260806_061155/`：旧孤立 Gazebo/ROS 节点导致
  `/clock` 不发布，统一入口记录 `exit_code: 1`，manifest、timeline、roslog 齐全。
- 干净重跑 `logs/newvision_fixed3_20260806_062124/`：Gate/审计均 `PASS`，候选为
  `tent/pillbox/bridge`，`raw_calls=[1,2,3]`、`success_slots=[1,2,3]`、
  `denied_results=[]`，无重复或未许可 Servo 调用；manifest `exit_code: 0`，timeline 与
  roslog 齐全。

### 遗留问题

- 本 Gate 是固定航点上的笔记本 PT 仿真，不是 OrangePi/RKNN 或实机验收；实机部署需
  重新标定相机外参、下降速度、承诺超时、水平漂移和机构反馈。
- `stop_toudi3_sim.sh` 异常退出后可能残留仓库节点，后续需单独增强清理范围。
- 外部任务模式、全场覆盖搜索、候选恢复与 R6 均未开始。

### 下一步

- 按用户要求暂停并保留功能分支；恢复任务时从 V-CL-03 外部任务模式开始，不直接跳到
  全图覆盖搜索。

## 2026-08-07（阶段 3：旧控制外部任务模式与单候选闭环）

### 改动范围

在保留旧状态机和默认固定航点行为的前提下，增加独立 Mission Manager 驱动的单候选
接近、对准、投递和恢复；原始 `toudi3.world`、真实 `actuator_pwm` 和实机执行链均未修改
或启动。

### 具体改动

- 新增 `patrol_control/MissionCommand`，以及默认 `false` 的
  `external_mission_mode`；外部模式禁用旧 `/fastplanner/goal` 发布和 `NextPoint()`，仍复用
  旧控制的 PX4 轨迹转发、视觉对准和 guarded Servo 调用。
- 新增 `external_candidate_manager.py`：只接受已确认、`camera_init` 地图有效、关联有效、
  无拒绝原因且年龄不超过 0.5 s 的标准候选；完成 `SEARCH→APPROACH→ALIGN→RESUME`，
  每个规划目标最多两次重试，连续 20 s 仿真时间不可达时失败关闭。
- 新增外部闭环和旧模式自动 Gate；持续检查 `/fastplanner/goal` 发布者所有权和命令/ACK
  顺序。
- 修正 `flag_planner_px4` 以整数默认值读取 bool launch 参数的问题；该错误会使
  `waypoint_mode=false` 回退到直接 MAVROS 分支。默认 `true` 的旧行为保持不变。
- 外部轨迹增加新鲜度、有限值、正高度、四元数和当前位姿距离门控；候选或历史
  `drop_ready` 本身仍不能触发投递。

### 验证结果

- 30 包串行 Catkin 编译通过；外部任务接口静态测试 4/4、旧控制测试 14/14 通过。
- `logs/external_candidate_20260807_041914/`：Gate `PASS`，候选 `pillbox id=0`，命令序列
  `[SEARCH, APPROACH, ALIGN, RESUME]`，`release_slots=[1]`；`/fastplanner/goal` 全程只有
  `/external_candidate_manager` 一个发布者，接近最小误差 0.298 m。
- `logs/legacy_mode_regression_20260807_042403/`：Gate `PASS`，零外部命令，唯一目标发布者
  为 `/patrol_control`，首个旧目标仍为 `(-0.602,-1.041,1.5)`。
- 保留 `external_candidate_20260807_030114` 至 `040628` 等失败运行；均有最终
  `manifest.exit_code: 1`、timeline 和 ROS 日志，用于记录直接分支误用、早期 wall-time
  超时和规划参数类型根因。

### 遗留问题

- 本阶段是笔记本 PT + PX4/Gazebo + guarded mock 验收，不是 OrangePi/RKNN 或实机验收。
- 覆盖航线、障碍净空检查、候选队列、恢复索引、北区点云稳定性和三目标任务尚未验收。

### 下一步

- 推进阶段 4 `coverage_navigation`：关闭实际投递，先完成障碍感知全场覆盖和北区恢复 Gate。

## 2026-08-07（阶段 4 前置：Fast-Planner 旧包基线快照）

### 改动范围

在调整覆盖模式的规划安全参数前，保存 `plan_manage` 包的不可编译审计快照；本提交不修改
运行时代码、原始 `toudi3.world` 或旧任务默认参数。

### 具体改动

- 新增 `legacy_baseline/20260807/plan_manage/` 完整包快照。
- 新增 30 项文件清单和对应 SHA256，仅用于满足旧链修改前的可回滚要求。

### 验证结果

- `FILELIST.txt` 与 `SHA256SUMS` 均为 30 项。
- 该提交为纯管理基线，不要求 SITL；阶段 4 的运行时代码和失败仿真未纳入本提交。

### 遗留问题

- `coverage_navigation_20260807_051047` 在中区出现 Fast-Planner
  `current traj in collision`，覆盖模式的规划膨胀与净空参数仍需最小修正并重新验收。

### 下一步

- 通过参数透传仅提高覆盖模式的障碍膨胀和轨迹净空，保持旧模式默认值不变。

## 2026-08-07（阶段 4：靶标区域覆盖搜索导航 Gate）

### 改动范围

按当前任务范围只验证原始 `toudi3.world` 的靶标区域，不把北区走廊避障纳入本 Gate；
增加不读取靶标坐标的覆盖航线、候选记忆、超时恢复和自动验收。真实 `actuator_pwm`、
原始 world 和旧模式默认参数均未修改或启动。

### 具体改动

- 新增可配置靶标搜索区 `x[-2.5,1.5]、y[-1.5,4.5]`，以 0.5 m 安全边距、1 m 行距和
  2 m 高度生成 12 个蛇形端点；从当前位置选择最近的等价入口，但航线不读取任何靶标坐标。
- Mission Manager 独占 `/fastplanner/goal`，仅在规划连续无进展时重试，每个目标最多
  重试两次，20 s 不可达时跳过；覆盖完成后安全返航。
- 只记忆已确认、`camera_init` 地图有效、关联有效、无拒绝原因且年龄不超过 0.5 s 的候选；
  按地图距离合并重复物理 ID，并保留最早 stable ID。
- 新增关闭实际投递的 `coverage_navigation.launch` 和自动 Gate，持续检查覆盖进度、五类
  唯一 ID、目标发布者所有权、碰撞、越界和 Servo 调用。

### 验证结果

- 主工作区 30 包串行编译通过，视觉工作区编译通过；`uav_mission` Python 回归 21/21、
  `patrol_control` 回归 14/14 通过。
- `logs/target_memory_merge_mock_20260807_190054/`：物理目标合并断言明确 `PASS`，manifest
  `exit_code: 0`，timeline 和 ROS 日志完整。
- `logs/target_area_navigation_20260807_190817/`：Gate `PASS`，12/12 端点全部到达，发现
  `tent/pillbox/bridge/panzer/tank` 恰好五个 stable ID，返航完成；Mission Manager 为唯一
  goal 发布者，`raw_calls=[]`，零碰撞、零越界，任务仿真时间 246.6 s。
- 该运行 manifest `exit_code: 0`、timeline 已生成、ROS 日志已归档；无 GUI 模式按约定
  不生成 `screenrecord.mp4`。

### 遗留问题

- 本结果是笔记本 WSL2/RTX PT 仿真，不是 OrangePi/RKNN 或实机验收。
- 北区走廊避障按用户要求后置，本 Gate 不能作为北区或 2026 双门规则验收证据。
- 权重三投、投递后恢复、返航降落仍需通过独立 `coverage_r6` Gate。

### 下一步

- 运行 `coverage_r6`，验证 `tank→panzer→bridge`、槽位 `1→2→3`、三次 guarded mock ACK、
  投递后恢复、返航降落和 600 s 任务时限。

## 2026-08-07（导航组新视觉链 beta1 联调包）

### 改动范围

参照 `deliverables/uav_vision_control_handoff_20260717_alpha1.zip`，建立面向导航组的新视觉
链 beta1 打包和独立验证流程；不修改视觉运行算法、原始 world 或任何实机执行路径。

### 具体改动

- 新增导航组交付总说明、视觉话题/候选字段契约和阶段 4 坐标误差证据摘要。
- 可编译主体保持纯 `uav_vision`，包含 PT、FP32 RKNN、消息、配置和纯视觉 launch。
- ZIP 根目录增加隔离的 `reference_integration/`，提供阶段 4 已验证的
  `MissionCommand.msg`、coverage manager/policy/config/launch 和 Gate 参考；明确这些文件
  不属于视觉正式 API、不能从 ZIP 独立运行、是否采用由导航组自主决定。
- 新增 beta1 构建和验证脚本；验证视觉包无任务/控制编译依赖、参考代码未伪装成 ROS 包、
  manifest 完整，并从解压目录独立编译和解析两个纯视觉入口。

### 验证结果

- beta1 预验证 ZIP 共 108 个文件、约 9.6 MiB；内部 manifest 逐文件校验通过。
- 验证脚本从临时解压目录完成独立 `uav_vision` Catkin 构建，三个 C++ 节点和七类消息均
  生成成功，`control_handoff_dev.launch` 与 `control_handoff_board.launch` 均可解析。
- 参考 Python 通过语法编译，视觉包无 `patrol_control/uav_mission` 编译依赖，参考目录不含
  `package.xml/CMakeLists.txt`，未被误装成 ROS 包。
- 最终包内 `SOURCE_REVISION` 指向 `d13037f` 且视觉树为 clean；外部 `.sha256` 使用相对
  ZIP 文件名，便于导航组在任意目录直接校验。

### 遗留问题

- 阶段 4 `pillbox` 地图点存在 22.97 cm 离群误差，搜索后的只对准、不投递 Gate 尚未执行。
- OrangePi ROS 相机/TF、30-seed、10 分钟稳定性和权重三投仍未验收。

### 下一步

- 生成并验证 `uav_vision_navigation_handoff_20260807_beta1.zip`，记录最终源码提交和校验文件。

## 2026-08-07（beta1 补充原工程接入与仿真依赖）

### 改动范围

补充导航组仅持有板端原始工程时的目录放置、依赖、编译、运行和仿真说明；不修改视觉
运行算法、任务状态机或仿真场景。

### 具体改动

- 新增 ZIP 根目录 `INSTALL_AND_SIMULATION.md`，区分板端 RKNN、纯视觉 mock、接入已有
  仿真相机和完整 toudi3 SITL 四种路径。
- 将“笔记本 PT 联调开发和整机仿真通过后，再上 OrangePi 切换 RKNN”设为推荐 Gate 顺序，
  并列出上板前最小检查清单。
- 说明独立视觉工作区与原 Catkin 工程的 underlay/overlay 顺序，以及只通过 ROS 话题联调
  时无需重编原工程的方式。
- 列出 Image、CameraInfo、TF、`ground_z`、RKNNLite、消息依赖和运行后检查命令。
- 明确完整阶段 4 SITL 不能由 ZIP 单独启动，仍需功能分支、PX4/Gazebo、world/model、
  `patrol_control/uav_mission/Fast-Planner` 和统一日志入口。
- 打包和验证脚本增加新手册，并自动检查 README 中存在完整仿真的额外依赖边界。

### 验证结果

- 预验证包更新为 109 个文件；外部 ZIP 和内部 manifest 校验通过，新手册已进入包根目录。
- 验证器确认 README 明确区分“接入已有仿真相机”和“完整 toudi3 需额外功能分支”；独立
  `uav_vision` Catkin 构建及 PT/RKNN 两个纯视觉 launch 解析继续通过。
- 最终包 `SOURCE_REVISION=476ab6a`、视觉树 clean；外部 SHA256 为
  `7290d5337dbc35766fae096892d9b8d2556d7ecf86125d80b35635a0e69355ba`。

### 遗留问题

- 文档不能替代导航组实际板端相机、CameraInfo、TF 和 RKNNLite 环境验收。

### 下一步

- 用新提交重建 beta1，确认包内源码版本和便携校验后推送功能分支。

## 2026-08-10（板端推理测评：新旧链 RKNN 性能 + 相机修复 + fp16 评估）

### 改动范围

SSH（pexpect 带密码，orangepi@192.168.3.15）对板端做只读盘点与推理测评；
修复 KS2A543 相机（换 USB 接口解决 UVC -110 超时）；新旧视觉链板端视频推理
性能测试；尝试 fp16 RKNN 转换（因环境冲突暂停）。未做任何飞行/执行机构操作。

### 具体改动

1. 新增 `top_level_scripts/ssh_board_cmd.py`：pexpect SSH 执行 + --upload/--download
2. 板端盘点：KS2A543 相机（MJPG 1280x720@100fps）、旧链 RKNN 与本地同份
   （md5 一致）、新链 fp32/int8 板端原缺失（已上传）、板端无 uav_vision 代码
3. 相机修复：原接口 UVC probe/commit -110 超时 → 换 USB 接口后 cv2 抓帧
   24fps 正常（1280x720 MJPG）
4. 板端推理测试（real_target.mp4 4617 帧）：
   - 旧链双 RKNN（best+tank）：total p50 163.7ms（~6fps），检测正常
   - 新链 fp32：infer p50 54.8ms，total 62.1ms（~16fps），六类检测正常
   - 新链 int8：infer p50 38.3ms（最快），**class_hist 全 0（零检测不可用）**
   - 本地 RTX PT 基准：新链 18.1ms（55fps）、旧链双模型 25.0ms（40fps）
5. fp16 转换尝试：rl_drone 安装 rknn-toolkit2 2.3.2 导致 torch 降级
   2.5.1→2.4.0、numpy→1.26、cv2→4.11、torchvision 0.20 与 torch 2.4 冲突
   （torchvision::nms 不存在）——修复需动 torch 生态，用户决定暂停 fp16

### 验证结果

- 相机：换接口后 24fps 稳定；测试图已拉回（board_cam_ks2a543.jpg）
- 板端测评数据全部拉回 logs/board_eval_20260810/（perf_*.json + log + 视频）
- 视频输出为 mpeg4 未压缩（350-380MB/77s），Windows 播放器兼容差，待转码

### 遗留问题

- fp16 RKNN 暂停：如需恢复需对齐 torch/torchvision 生态（或独立转换环境）
- 视频需转 h264 可播版本
- 完整对比报告待编制（板端 RKNN 各模型 + 本地 PT）
- 任务 C（摄像头 GUI 稳定性/时延）未执行（X11 未启动）

### 下一步

- 视频拉完转码 → 编制完整各模型对比报告 → 评估板端运行方式（推荐 fp32）
- 板端 X11 启动 + 摄像头 GUI 稳定性测试

## 2026-08-10（板端推理测评完成：完整对比报告归档）

### 改动范围

板端测评数据与视频全部拉回本地，编制完整对比报告
（docs/板端推理性能对比报告_20260810.md）；视频转码 h264 可播版。

### 具体改动

- 新增 docs/板端推理性能对比报告_20260810.md：五路推理性能总表
  （旧链双RKNN/新链fp32/int8/本地PT×2）、检测质量直方图、
  板端运行方式评估（推荐 fp32 16fps）
- 视频转码：5 个板端输出视频 mpeg4 → h264（crf 28）

### 验证结果

- 全部 json/video 拉回 logs/board_eval_20260810/（5 视频 + 7 json/log）
- 对比结论：板端新链 fp32 16fps 可用；int8 零检测不可用；
  fp16 因环境冲突暂停

### 遗留问题

- fp16/INT8 恢复需独立环境与量化研究
- 任务 C（板端 X11 + 摄像头 GUI 稳定性）待执行

### 下一步

- 板端 X11 启动 + 摄像头 GUI 稳定性/时延测试
- 板端完整 ROS 视觉链（camera_sdk → fp32 RKNN）上板验证

## 2026-08-10（rl_drone 环境损坏记录：本地 PT 全量检测质量对比推迟）

### 问题

rknn-toolkit2 2.3.2 安装降级 torch 2.5.1→2.4.0 并破坏 torch 库文件
（libtorch_global_deps.so 缺失），torchvision 0.20 与 torch 2.4 冲突。
fp16 转换暂停后尝试恢复原环境（torch 2.5.1+cu121 + torchvision 0.20.1 +
torchaudio 2.5.1 + numpy 2.0.2 + cv2 4.13）：
- PyTorch 官方源下载 2.4GB 过慢（>30min 未完成）
- 清华镜像无 cu121 后缀版本（仅 CPU wheel）
- 按用户指示暂停，晚点恢复

### 影响

- 本地 PT 全量（4617 帧）检测质量对比（class_hist）推迟
- 报告 docs/板端推理性能对比报告_20260810.md 第二节暂缺本地 PT 两行
- 后续本地 PT 相关评测均需先恢复 rl_drone

### 恢复计划（晚点执行）

- 网络条件好时：`pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121
  torchaudio==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121`
  后补 numpy==2.0.2 + opencv-python-headless==4.13.0.92
- 然后重跑 local_pt_bench.py 全量（新链 + 旧链双模型）补 class_hist

### 下一步

- 任务 C（板端 X11 + 摄像头 GUI）不依赖本地环境，可继续

## 2026-08-11（rl_drone 环境恢复完成 + 本地 PT 全量检测质量对比补齐）

### 改动范围

修复 rknn-toolkit2 安装造成的 rl_drone 环境损坏（torch 2.5.1+cu121 恢复），
完成本地 PT 全量（4617 帧）检测质量对比，更新板端推理性能对比报告第二节。

### 具体改动

- 环境恢复：官方源重装 torch 2.5.1+cu121/torchvision 0.20.1/torchaudio 2.5.1
  （排除 Windows HTTP_PROXY 环境变量干扰——根因是 Windows 环境变量传入 WSL，
  pip 连 7890 坏代理；numpy 2.0.2 恢复；pillow 降级 10.4 修复 py3.9 enum 冲突；
  cv2 保持 4.11 可用）
- 本地 PT 全量（4617 帧）class_hist：
  - 新链 merged_standard：bridge 1012/panzer 639/pillbox 572/tent 41/
    tank 660/red_cross 1125
  - 旧链双模型：bridge 195/panzer 1146/pillbox 1079/tent 257/tank 522
- 报告第二节加入本地 PT 两行对比（板端 fp32 vs 本地 PT 分布一致，
  旧链无 red_cross）

### 验证结果

- torch 2.5.1+cu121/numpy 2.0.2/ultralytics 8.4.33 全部可用
- 本地 PT 与板端 RKNN 检测分布一致（新链六类；旧链无 red_cross）

### 遗留问题

- rknn-toolkit2 仍装于 rl_drone（与 torch 生态共存但版本非最优），
  fp16 转换如需恢复建议独立 conda 环境
- cv2 4.11（原 4.13）差异无功能影响

### 下一步

- 报告最终版提交推送；继续阶段 1/2/3 或按用户安排

## 2026-08-12（默认仿真切换到 toudi4 + 单下视相机 MID360 机架）

### 改动范围

仿真 world、Gazebo 机架、下视相机接口、toudi4 固定航点/覆盖搜索配置、独立
真值目录、统一仿真入口和配套中文说明。未修改旧 `patrol_control` 状态机源码，
未启动 PX4 解锁、飞行或任何执行机构。

### 具体改动

1. 从根目录 `iris_mid360_downward_camera.zip` 提取并仓库化新机架：
   `patrol_control/models/iris_mid360_downward_camera/`；保留 PX4
   `vehicle:=iris_mid360`，仅用 `vehicle_sdf` 覆盖 Gazebo 实际模型。
2. `patrol_world.launch`、完整比赛入口和新视觉入口默认 world 改为根目录
   `toudi4_copy.world`，相机话题改为 `/downward_camera/image_raw` 与
   `/downward_camera/camera_info`，图像 frame 为
   `downward_camera_optical_frame`。
3. 新增 `patrol_toudi4.yaml`、`patrol_toudi4_new_vision.yaml`、
   `coverage_toudi4.yaml`，避免替换 world 后仍使用 toudi3 旧坐标；新视觉兼容
   像素轴映射更新为 `[0,-1,-1,0]`。
4. 新增 `sim_target_catalog_toudi4.yaml`，同步五类标准靶与主 H 真值；30-seed
   相机位置同步到 toudi4。旧 toudi3 catalog、航点和 SDF 均保留。
5. 历史 `legacy_mode_regression.launch` 与 `drop_fix_fixed3.launch` 显式锁回旧
   toudi3 world/旧 PX4 SDF。改动前 `patrol_control`、`uav_mission` 原包保存在
   `legacy_baseline/20260812/`，带文件清单与 SHA256。
6. GUI/控制流及完整比赛包装脚本接入 `sim_run.sh` 统一 run 目录；新增中文说明
   `docs/TOUDI4仿真机架与世界切换说明_20260812.md`。

### 验证结果

- XML/launch 解析通过；新旧相关 Python 静态回归通过（6 + 4 + 13 项）；主集成
  工作区 30 包 `catkin_make -j1` 全量编译通过。
- `logs/toudi4_rack_smoke2_20260812_225408/` 安全烟测通过：
  - Gazebo model 清单包含 toudi4 五类标准靶、`landing_h`、
    `landing_h_clone` 和 `iris_mid360`；
  - 新相机发布 640×480 Image/CameraInfo，frame 为
    `downward_camera_optical_frame`；
  - `/livox/lidar`、`/livox/imu` 存在；
  - 机架成功生成。
- `gz sdf -k` 对含 MID360 的组合模型在 20 秒内未返回（exit 124），因此不作为
  失败依据；实际 Gazebo 加载与 ROS 话题烟测已经通过。

### 遗留问题

- 本次只完成 world/机架层安全烟测，未执行 PX4 自动起飞、新视觉推理、固定三投、
  走廊避障和降落闭环。
- toudi4 内有两个 H，当前主降落/真值仅使用
  `landing_h=(-0.493412,-1.77269)`，clone 暂不参与任务。
- 旧文件名中仍含 `toudi3`，为兼容既有脚本暂未批量重命名；以默认参数和新增说明
  判断实际场景。

### 下一步

- 在当前 toudi4 + 新机架上运行一次完整新视觉固定三投 SITL，先核对地图投影方向、
  toudi4 航点可达性与 H 降落点，再恢复覆盖搜索/走廊联调。

## 2026-08-13（toudi4 首轮闭环实跑与外部候选投递契约修正）

### 改动范围

按统一 `sim_run.sh` 在 `toudi4_copy.world`、单下视相机 MID360 机架上实跑新视觉
固定三投和无靶标坐标覆盖搜索；保留失败日志，并针对首轮暴露的外部 ALIGN 地图点
覆盖、语义候选与圆环证据身份错配做最小修正。旧固定路线分支和实机执行机构未启动。

### 具体改动

1. 固定三投首轮 `logs/toudi4_newvision_fixed3_20260812_232317/`：新视觉地图候选、
   旧控制对准和安全 mock Servo 均实际工作，但仅完成槽 1/2；首个 pillbox 承诺在下降
   前过期，H 检测为 0，最终 Gate FAIL。
2. 覆盖搜索首轮 `logs/toudi4_coverage_r6_20260812_234508/`：无任何靶标坐标输入的
   12 点蛇形航线全部完成，五类标准靶均被发现，0 碰撞、0 越界；按
   tank→panzer→bridge 排队。tank 对准 60 s 超时，panzer 完成槽 1，bridge 阶段
   旧 `WayPointDetectDone()` 又用固定航点覆盖外部候选地图点，随后错误圆环 ACK 被
   `selected_semantic_target_mismatch` 硬拒绝，Gate FAIL。
3. `patrol_control` 只在 `external_mission_mode=true` 时保留 Mission Manager 下发的
   ALIGN 地图点；旧固定航线仍使用 `waypoint_list`。新增外部对准超时参数，默认 75 s。
4. Coverage Manager 不再要求远距标准图案语义 ID 等于近地 circle ID；改为校验顺序
   payload 槽、`drop_circle/circle` 释放证据及飞机距锁定语义地图点不超过 0.75 m。
   对准超时由配置设为 90 s。
5. release commitment 由 30 s 调至 45 s，覆盖旧控制 70 帧稳定和下降时间；目标地图
   匹配/合并距离由 0.5/0.6 m 调为 0.7 m，以吸收首轮约 0.65 m 最大投影漂移。
6. 修改旧控制源码前已保存 `legacy_baseline/20260813/` 包快照、74 文件清单和
   SHA256；未使用散落 `.bak`。

### 验证结果

- `git diff --check` 通过；Coverage/释放策略 Python 单元测试 19/19 通过。
- `target_memory_physical_mock.launch` 通过，类别抖动、连续帧、分离目标和重复 ID
  融合回归无退化。
- `vision_ws catkin_make -j2` 与主集成工作区 `catkin_make -DROS_EDITION=ROS1 -j1`
  均通过；仅保留历史 VTK/PCL/CMake 警告。

### 遗留问题

- 最小补丁尚需重新执行完整 Coverage R6，不能把编译/mock 结果写成闭环通过。
- 首轮 pillbox 出现两个 stable ID；0.7 m 匹配阈值需由复跑确认是否恰好消除，若仍
  分裂应优先检查地图投影/TF，而不是继续放宽。
- toudi4 原始 0.8 m 门洞、北区往返和 H 降落仍未验收；中央 12 点覆盖不等于走廊通过。

### 下一步

- 以当前提交重跑 `coverage_r6.launch`；通过后再建立独立走廊 Gate，复用
  `patrol_toudi3_r1.yaml` 的门洞语义并按 toudi4 世界坐标逐点核查。

## 2026-08-13（toudi4 Coverage R6 复跑与走廊/H 边界核查）

### 改动范围

复跑无固定靶标坐标的覆盖搜索/权重投递 Gate，核对 toudi4 走廊和 H 的实际几何/验收
边界，更新路线图、问题边界和 toudi4 专项纪录。未启动真实执行机构，未改写旧固定路线状态机。

### 具体改动

1. 按 `sim_run.sh` 执行 `toudi4_coverage_r6_retry`，保留 run.log、manifest、ROS log、
   coverage/gate/audit JSON 和 timeline。
2. 新增 `TOUDI4新视觉闭环实跑与当前阻塞_20260813.md`，分开记录固定三投、
   Coverage R6、走廊几何和 H 降落，不把部分结果写成整体 PASS。
3. 同步 `VISION_2026_ROADMAP.md`、`当前问题与责任边界.md` 和 toudi4 切换说明；
   将主阻塞从“旧控制不消费新地图点”更新为“近地 circle 再识别 + 后备候选可达性”。
4. 核查 world 源文本：toudi4 有五类靶标和两个 H，但无旧门洞 `Wall_15`、
   无北区侧墙 `Wall_16`；因此未对缺失障碍的空场运行旧门洞坐标并伪报避障 PASS。

### 验证结果

- 覆盖到达 11/12；五类标准靶标形成五个物理 ID，上轮 pillbox 重复 ID 未再出现。
- 权重顺序为 tank→panzer→bridge→pillbox→tent；tank 槽 1、panzer 槽 2 完成
  commitment→permission→raw mock ACK。
- bridge 高位语义识别持续有效，进入 ALIGN 后长时间 `stale_observation`，无槽 3 circle
  commitment，90 s 超时；pillbox/tent 后备接近均 20 s 不可达。
- 0 碰撞、0 越界，最小记录净空 0.884 m，任务仿真时长 557.3 s；最终 Gate FAIL，
  `reason=coverage_complete_insufficient_candidates`。
- manifest 记录提交 `0869c37`、工作区干净、统一收尾退出码 1。

### 遗留问题

- V-CL-04 仍是部分完成，不能以两投代替 3/3 Gate。
- 走廊 Gate 被 toudi4 几何缺失阻塞；须保留原 world，另建可审查 corridor 派生 world。
- Coverage R6 的普通返航/落地未消费 H 视觉证据，不是 H 降落 Gate。
- `.gitattributes` 为本轮开始后出现的未跟踪用户文件，已保留且不纳入本次改动。

### 下一步

- 先对 bridge ALIGN 前后的 debug image、circle refine 拒绝原因、TF 年龄和地图落点误差做
  5～10 s 定点回放；根据真实失败项修正后再跑 3/3，不继续盲目放宽超时。
- 并行从 toudi3 恢复 `Wall_15/Wall_16` 到 toudi4 corridor 派生 world，再建立双向穿门与 H 视觉
  降落的独立 assertion。

## 2026-08-13（toudi4 H 材质名修复与 GUI 展示）

### 改动范围

`toudi4_copy.world` 内主 H 与 H clone 的 Gazebo 材质引用，以及安全 GUI 展示日志。
未启动 PX4、自动解锁、控制状态机或执行机构。

### 具体改动

1. 对比 toudi3 的 `model://landing_h` 模型与 toudi4 内联 H，确认实际
   `landing_h.material` 只定义 `Mark/LandingH`。
2. toudi4 主 H 与 `landing_h_clone` 原引用不存在的
   `Toudi3/LandingHCorrected`，Gazebo 因此回退为纯白材质；两处均改为
   `Mark/LandingH`。
3. H 位置、1×1 m 尺寸、collision、贴图文件和旧 toudi3 world 均保持不变。

### 验证结果

- 贴图 `Hjiangluo.png` 为 2268×2268 RGB PNG，可正常读取。
- world 中主 H/clone 两处材质名均与 `landing_h.material` 定义一致。
- GUI 使用 `sim_run.sh` 统一入口，日志位于
  `logs/toudi4_gui_h_fixed_20260813_013831/`；实际加载 toudi4、单下视相机 + MID360
  新机架、五类靶标、主 H 和 H clone，用户已在 GUI 中人工确认 H 显示正常。

### 遗留问题

- 本次只修复 H 渲染；H 视觉确认→降落许可→落地 Gate 仍未验收。
- `toudi4_copy.world` 仍缺少旧走廊 `Wall_15/Wall_16`，与本次 H 材质问题无关。

### 下一步

- 后续将飞机 spawn/home 成对迁移到主 H `(-0.493412,-1.77269)`，同步任务起点、
  返航点、真值与 assertion；本轮仅记录决策，未改变现有 spawn pose。

## 2026-08-13（toudi4 默认出生点迁移到主 H）

### 改动范围

toudi4 默认 PX4/Gazebo 出生点、固定三投路线、覆盖搜索边界及历史 toudi3 回归入口。
未修改旧 `patrol_control` 状态机源码，未执行自动解锁、起飞、投递或实机动作。

### 具体改动

1. 将 `patrol_world.launch`、完整控制入口与新视觉入口的默认 spawn 改为主 H 世界坐标
   `(-0.493412,-1.772690,0.06)`，并保留可配置参数。
2. 明确 PX4/MAVROS/`camera_init` 在 spawn 处建立局部原点；将 toudi4 固定三投航点、
   返航点和覆盖区域整体转换到以主 H 为原点的任务局部坐标。
3. `drop_fix_fixed3.launch` 与 `legacy_mode_regression.launch` 显式传入旧 toudi3
   `spawn=(0,0,0.06)`，避免新默认值污染历史回归。
4. 增加静态回归，锁定 toudi4 主 H spawn、局部固定航点和覆盖边界。

### 验证结果

- 旧控制配置测试 7/7 通过；覆盖策略测试 16/16 通过。
- 统一 `sim_run.sh` 默认启动烟测生成
  `logs/toudi4_spawn_default_h_smoke_20260813_200407/`，Gazebo 机体位置为
  `(-0.493411,-1.77269,0.059991)`，与主 H 和设定高度一致，退出码 0。
- 迁移前独立 frame 探针确认：当 Gazebo spawn 位于主 H 时，MAVROS 本地位置仍约为
  `(0,0)`，因此返航/LAND 使用局部 `(0,0)` 而不是重复使用 H 的 world 坐标。

### 遗留问题

- 本次只验证生成位置与坐标契约，尚未以新局部坐标复跑固定三投和覆盖搜索 Gate。
- H 视觉确认到自主降落的闭环仍未验收；toudi4 走廊实体缺失问题不在本次范围内。

### 下一步

- 后续需要运行任务时，先复跑一次 toudi4 固定路线或覆盖 Gate，重点检查 world 真值、
  `camera_init` 地图点和 Fast-Planner 目标是否处在同一局部坐标系。

## 2026-08-13（重建 toudi3/toudi4 完整 Gazebo 资产 ZIP）

### 改动范围

toudi3/toudi4 world 外部模型、新单下视相机 + MID360 机架依赖、路线/真值参考以及
可重复执行的 ZIP 构建脚本。保留旧 `toudi3_gazebo_assets_20260714.tar.gz`，未删除或覆盖。

### 具体改动

1. 新增 `build_toudi4_gazebo_assets_zip.sh`，从当前 PX4 模型目录和仓库模型目录收集
   big_box、树木、五类标准靶、红十字、完整 landing_h、iris、gps、mid360 与
   `iris_mid360_downward_camera`。
2. ZIP 同时收录 toudi3/toudi4 world、固定路线、无目标坐标覆盖策略、toudi4 真值目录
   和当前 launch 参考；构建后自动检查 H 模型/材质/贴图并执行 `unzip -tq`。
3. 新增包内中文 `README.md` 来源文档，说明安装方法、插件边界和主 H 局部坐标约定。
4. 生成可交付文件 `toudi4_gazebo_assets_complete_20260813.zip`；ZIP 为可再生大文件，
   按 `.gitignore` 不进入 git，构建脚本和说明进入版本库。

### 验证结果

- `bash -n` 检查构建脚本通过；脚本成功生成约 15 MB ZIP。
- `unzip -tq` 返回 `No errors detected in compressed data`。
- 包内已逐项核对 `landing_h/model.sdf`、`landing_h.material`、`Hjiangluo.png`、
  新机架 SDF、toudi4 world、覆盖 YAML 和中文 README。

### 遗留问题

- 包内不含 Astra/PX4 已编译 Gazebo 插件 `.so`，目标机仍需准备兼容的插件构建产物。
- 红十字作为评测动态插入模型被纳入 ZIP，但当前基础 toudi4 world 未静态放置红十字。

### 下一步

- 在目标机解压到独立目录并设置 `GAZEBO_MODEL_PATH`，先执行 world-only 加载，再接入
  与目标机版本匹配的 PX4/Astra 插件；不要直接覆盖目标机已有模型目录。

## 2026-08-13（仓库行尾统一与 AGENTS.md 状态同步）

### 改动范围

`.gitattributes` 行尾策略、仓库级 git 配置、`AGENTS.md` 第 7 节过时状态表述。

### 具体改动

1. 新增 `.gitattributes`：`* text=auto eol=lf` 统一文本文件行尾为 LF；上游原样拷入的
   第三方库（`patrol_uav_ws-patrol_planner/src/tool/**`、`src/FAST_LIO/**`）标记
   `-text` 保持行尾原样，避免污染与上游 diff 对比。
2. 仓库级配置 `core.autocrlf=false`、`core.filemode=false`（仅本仓库，不动全局）。
   背景：Windows 全局 `autocrlf=true` 访问 WSL 内仓库时，git status 显示约 186 个
   0 行差异"变动"并刷屏 LF→CRLF 警告；renormalize 后另发现 189 个文件模式
   755→644 的 staged 假变化（Windows 无法可靠读取 WSL 执行位），均已通过
   `filemode=false` + `git reset` 消除。
3. `AGENTS.md` 第 7 节同步 5 处状态：节首增加"以 ROADMAP 为准"指针声明；7.1-8 板端
   FP32 RKNN 已离线实测（16fps，ROS 实时链未验收）；7.1-9 旧控制三投 SITL（V-CL-02）
   已于 08-06 通过、0869c37 已打外部任务模式最小补丁（快照 `legacy_baseline/20260813/`）；
   7.3-1 主线更新为 V-CL-04 及其当前阻塞（bridge 第三投）；7.3-6 三投状态同步；
   7.1-4 补默认世界已切换 toudi4。

### 验证结果

- `git diff --numstat HEAD` 全部为 0 行差异，确认工作区无内容级误改动。
- `git status --short` 仅剩本次新增 `.gitattributes` 与上述文档，无 CRLF 假阳性 M。
- 25 个 index 中 CRLF 文件（livox 驱动、FastCRC、IKFoM S2.hpp）经 `-text` 豁免后
  renormalize 零内容变化，提交不含第三方文件改动。

### 遗留问题

- Windows 侧若有编辑器/工具以 CRLF 保存文件，会显示为真实行尾差异，编辑 WSL 仓库
  文件时需保持 LF。
- `.gitattributes` 生效后新 clone 仓库自动继承该策略，无需额外配置。

### 下一步

- 观察 Windows 侧 git 操作是否不再出现 LF→CRLF 警告与假阳性变动。
- 若仍有假阳性，核查是否有未覆盖的第三方目录或未跟踪文件混入。

## 2026-08-13（feature 分支合并回 main 与协作分支策略）

### 改动范围

两条 feature 分支合并回 `main`；AGENTS.md 新增规则 18 多人协作分支策略。

### 具体改动

1. `feat/new-vision-coverage-search`（V-CL-02 新视觉固定三投）与
   `feat/external-mission-coverage`（V-CL-03 外部任务模式 + V-CL-04 覆盖实跑基线）
   依次以 `git merge --no-ff` 合并回 `main`，保留分叉痕迹；两条 feature 分支
   按约定保留不删除（供溯源）。
2. AGENTS.md 新增**规则 18 多人协作分支策略（2-4 人小团队 + agent 辅助）**：
   主干模型（main 只经合并进入、禁止直 push）、分支命名（沿用现有语义风格或
   ROADMAP 阶段号）、分支生命周期（一个分支一个 Gate/里程碑、合完保留）、
   按包划分所有权分治（视觉/控制规划/板端三组）、共享文档冲突约定（双方条目
   都保留按日期排序）、仿真资源独占、agent 使用边界（合并/推送 main 须人工
   确认）、合并前检查清单。
3. 规则 15 末条同步：`main` 只经合并进入（引用规则 18）。

### 验证结果

- 合并无冲突；`main` 领先 origin/main 29 个提交（9+18 提交与 2 个 merge commit），
  拓扑为纯线性包含，无交叉分叉。
- 两条 feature 分支与各自远程分支保持同步，未删除。

### 遗留问题

- 规则 18 尚未在真实多人场景验证（当前仍为单人+agent 模式），后续 2-4 人
  协作首周应回顾策略是否过重/过轻。
- GitHub 分支保护（禁止直 push main）需仓库管理员在 GitHub 设置中开启，
  本地规则仅约束 agent 与成员行为。

### 下一步

- push main 到远程，并视需要开启 GitHub main 分支保护。
- V-CL-04 剩余工作（bridge 第三投、返航降落验收）从最新 main 拉出
  `feat/vcl04-r6-bridge` 新分支继续。


## 2026-08-13（bridge 圆环证据高位捕获与 H/圆环地图点兼容输出）

### 改动范围

针对 V-CL-04 bridge 第三投失败与 H 降落通路，从代码与 `logs/toudi4_coverage_r6_retry_20260813_003915/`
日志取证后做的最小修复：视觉兼容桥恢复旧链地图点语义，任务层增加下降前圆环证据捕获阶段。
未修改 `patrol_control` 源码、未启动真实执行机构、未修改 toudi4.world。

### 具体改动

1. `vision_ws/src/uav_vision/scripts/detect_compat_bridge.py`：新增
   `publish_circle_mark_compat`/`publish_landing_mark_compat`（默认 true）。订阅旧控制自有的
   `/detect/control` 与 `/detect/landing_control`，开关打开时分别把 `detections_mapped` 中
   `map_valid` 的 circle/H 地图点发布到 `/detect/waypoint_mark_point`、`/detect/land_mark_point`
   （发布真实 map_point，不发布像素；像素 Pose 调试路径保持默认关闭）。
2. `patrol_uav_ws-patrol_planner/src/uav_mission/scripts/coverage_search_manager.py`：候选到达
   接近点后新增 `CANDIDATE_CAPTURE` 阶段——在 2 m 高度等待新鲜 circle 证据出现在语义候选
   `capture_radius` 邻域内才下发 ALIGN；超时（默认 20 s）按 `capture_timeout_no_circle` 快速
   失败并记录 `capture_observations`（位置/年龄/置信度/到候选距离）供复盘。避免旧控制在无
   任何 circle 证据时盲降至负高度再等 90 s 超时。
3. `patrol_uav_ws-patrol_planner/src/uav_mission/config/coverage_toudi4.yaml`：candidate 下新增
   `capture_timeout: 20.0`、`capture_fresh_age: 1.0`、`capture_radius: 0.75`。

### 验证结果

- 离线取证（不改仓库）：新链 circle 检测算法对 qiaoliang/tanke/dibao/zhangpeng 贴图圆环
  quality≈0.85（阈值 0.70）全部通过；对 30-seed 历史渲染图 bridge 场景 quality=0.926；
  新链 H 判定对 `Hjiangluo.png` 贴图 4 个尺度全通过，对 `seed_26/27_landing_h` 渲染图 PASS
  （seed_25 距离过远、H 字母亚像素为合理失败）。复跑日志取证：slot3 bridge 期间仲裁器从未
  锁第三次 commitment（全程 `visual_evidence_invalid`），旧控制无证据下降至 z=-0.32。
- `python3 -m py_compile` 两处脚本通过；uav_mission 单测 24/24 通过；
  `run_visual_mock_regressions.sh` 六项 L0 PASS；`phase_d_mock_patrol_regression.launch` PASS。

### 遗留问题

- capture 阶段与 circle 地图点跟随尚未在 toudi4 SITL 实跑验证；bridge 在高位仍不可见圆环的
  根因（疑似边缘裁切/遮挡）需下一次实跑用 debug image 确认。
- H 闭环仅打通 `land_mark_point` 地图点通路；H 视觉降落 Gate（landing 阶段 + 结构证据 + 落地）
  仍未验收。

### 下一步

- 在 `feat/vcl04-r6-bridge` 分支上以 `enable_debug_image:=true` 复跑 `coverage_r6.launch`，
  先看 bridge capture 观测记录与 ALIGN 前后 debug image，再决定是否调整检测/接近参数。


## 2026-08-13（V-CL-04 三投 PASS 验证、Gate 全程事实评估与编码边界约定）

### 改动范围

`feat/vcl04-r6-bridge` 分支的实跑验证与收尾：`coverage_r6.launch` 增加 debug 录制开关，
R6 Gate 断言改为按全程累计事实评估，`coverage_search_manager` 任务收尾后冻结发现记录，
AGENTS.md 增加 Windows→WSL argv 编码边界规则并提供中文提交辅助脚本。未修改
`patrol_control`、未启动真实执行机构、未修改 toudi4.world。

### 具体改动

1. `coverage_r6.launch`：新增 `enable_debug_image`/`record_debug` 参数；`record_debug:=true`
   时以 rosbag(lz4) 录制下视原始图像与各检测器 debug 图到 run 目录，供失败复盘。
2. `coverage_r6_assertion.py`：discovered/selection 改为按每个 manager 状态快照累计合并
   （全程事实），其余检查仍用最终快照；修复"任务成功进入 landing 模式后标准靶被
   target_memory 模式过滤清空 → 最终快照 discovered 为空 → Gate 误判"的数据流缺陷。
3. `coverage_search_manager.py`：`RETURN_HOME/LAND/COMPLETE` 状态下不再按当前观测清空
   已发现候选，保证最终状态快照保留五类发现证据。
4. `AGENTS.md` 规则 13/15：新增编码边界约定——中文内容禁止内联在 `wsl -e bash -c`
   命令行（Windows ANSI/GBK → WSL UTF-8 边界导致乱码/丢字），commit message 一律经
   UTF-8 文件 + `git commit -F`；动机：本次会话提交信息多次因编码边界丢字，需机制化防复发。
5. 新增 `top_level_scripts/git_commit_cn.sh`：中文提交辅助脚本（去 BOM、统一 LF、
   `git commit -F`）。

### 验证结果

- `logs/toudi4_coverage_r6_v2_20260813_221737/`（v2 实跑，含 Gate 修复前状态）：
  覆盖 12/12 全到（对比上轮 11/12），tank/panzer/bridge 三投全部完成（槽 1/2/3，
  raw mock ACK 顺序正确），0 碰撞、0 越界、0 拒绝、audit PASS、任务时长 377.6 s 后降落；
  管理器状态 `three_deliveries_landed`。Gate 仅因上述 discovered 数据流缺陷 FAIL
  （`coverage_r6_contract_failed`），修复后 v3 复跑验证中。
- bridge 投递成功证据：仲裁器三次 commitment 锁定（含 bridge 槽 3），旧控制
  `waypointMarkCallback` 持续收到 circle 地图点（2524 次），capture 阶段对三候选均在
  到达接近点时立即满足（高位圆环可见性成立，未触发等待分支）。
- 单测 24/24、py_compile、`git diff --check` 通过；AGENTS.md 规则变更已在本文记录动机。

### 遗留问题

- v3 复跑（Gate 修复后）结果待收；H 视觉降落 Gate（landing 阶段 + H 结构证据 + 落地）
  仍未单独验收，当前 Gate 的降落只验证 z<=0.15 m。
- 分支上 9549e8d 的提交信息曾因编码边界乱码，已 amend 修复并 force-push（远端已同步
  de89441）；main 合并仍需人工确认。
- `sim_run.sh` 丢失执行位（行尾统一时 filemode=false 导致），当前用 `bash sim_run.sh`
  调用，待人工恢复执行位。

### 下一步

- 收 v3 实跑结果：Gate PASS 后更新 ROADMAP/迁移清单，按规则 18 由人工确认合并 main。


## 2026-08-13（V-CL-04 收尾：断言接线、单测、实跑归档与 Gate 审查结论）

### 改动范围

`feat/vcl04-r6-bridge` 收尾：断言接线到共享累计函数并补单测，归档 v2/v3 实跑结果与
Gate 设计审查结论，同步 ROADMAP/迁移清单。未改 `patrol_control` 与 toudi4.world。

### 具体改动

1. `coverage_r6_assertion.py`：删除内联累计逻辑与本地 `STANDARD_CLASSES`，改为从
   `coverage_policy` 导入 `STANDARD_CLASSES`/`accumulate_run_facts` 并调用；最终检查
   语义不变（发现/选择看全程累计，其余看最终快照）。
2. `test_coverage_policy.py`：新增 `accumulate_run_facts` 三类用例——多快照合并且最终
   快照为空仍保留早期事实、过滤非标准类/无效 ID、ID 覆盖与选择去重（27 项全过）。
3. `VISION_2026_ROADMAP.md`：V-CL-04 行更新为"3/3 投递已实跑、Gate 修复待干净复跑"，
   新增 V-CL-05（搜索-投递策略：高权重中断 + red_cross 统一 + 随机红十字），一页结论
   第 11 条与 Gate E 段落同步；后续行顺延重编号。
4. `VISION_MIGRATION_CHECKLIST.md`：M6 `coverage_r6` 行更新为 [~] 并引用 v2 日志。

### 验证结果

- 单测 27/27；`run_visual_mock_regressions.sh` 六项 L0 PASS；patrol mock 回归 PASS；
  `git diff --check` 干净。
- v2 实跑归档：`logs/toudi4_coverage_r6_v2_20260813_221737/`——12/12 覆盖、五类五 ID、
  tank/panzer/bridge 3/3 投递、槽序 [1,2,3]、0 碰撞、0 越界、0 拒绝、audit PASS、
  377.6 s 降落；Gate 唯一失败项为最终快照 discovered 清空（数据流产物，本次已修）。
  bridge 成功关键证据：旧实跑 waypoint 跟随 0 次 → 修复后 2524 次。
- v3 实跑归档：`logs/toudi4_coverage_r6_v3_20260813_224011/`——Fast-Planner 可达性退化
  （6 端点跳点、tank 接近不可达、返航不可达），0 碰撞；判定为规划侧已知问题，不阻塞
  视觉闭环验收，不在本分支迭代。
- Gate 审查结论（供人工裁决是否放宽）：偏严检查有 `count(1/2/3)==3` 钉死三次候选尝试、
  `selected==[tank,panzer,bridge]` 钉死策略、`not denied` 零拒绝；安全类检查（碰撞/越界/
  槽序/独占发布者/审计/时长）建议保留。V-CL-05 将把硬编码期望改为按已发现候选权重排序
  的动态期望。

### 遗留问题

- Gate PASS 尚缺一次规划器正常时的干净复跑（可在 V-CL-05 实跑中一并取得）。
- H 视觉降落 Gate（landing 阶段 + H 结构证据 + 落地）未验收；北区走廊实体缺失未处理。
- `sim_run.sh` 执行位丢失，当前以 `bash sim_run.sh` 调用，待人工恢复。

### 下一步

- 合入 main 后从最新 main 拉 `feat/vcl05-search-delivery-strategy`，实施高权重中断投递、
  red_cross 统一入队与随机红十字布设（见 ROADMAP V-CL-05）。


## 2026-08-13（合并后打 tag 复盘约定）

### 改动范围

git 复盘节点管理：为历史合并补 annotated tag，并把"合并后打 tag"写入 AGENTS.md 规则 18。

### 具体改动

1. 为 main 上三次历史合并补 tag 并推送远端：
   `gate/vcl02-newvision-fixed3`（d1f6436，V-CL-02 固定三投 PASS）、
   `gate/vcl03-external-coverage`（0ad8606，V-CL-03 外部任务模式 + V-CL-04 覆盖基线）、
   `gate/vcl04-r6-bridge`（30bf6fb，V-CL-04 三投闭环与 H 通路）。
2. AGENTS.md 规则 18 新增"合并后打 tag"条款：每次 `--no-ff` 合并回 main 后立即打
   annotated tag 并推送；Gate 合并用 `gate/<Gate-ID>-<简述>`，非 Gate 合并用
   `chore/<简述>`；tag 消息说明对应 Gate/通过证据。动机：未来复盘需要按 Gate 快速
   定位合并节点，避免每次人工翻 `git log --merges`。

### 验证结果

- 三个 tag 本地与远端同步（`git tag -n1` 消息为 UTF-8 中文，无乱码）。
- `git diff --check` 干净。

### 遗留问题

- 当前 main 上 `git log --merges` 共 3 个节点，已全覆盖；更早历史无合并节点。
- 后续 Gate 合并（如 V-CL-05）须按新约定打 `gate/vcl05-search-delivery-strategy`。

### 下一步

- V-CL-05 干净复跑通过后合并 main 并按约定打 tag。


## 2026-08-13（全库文档进度同步）

### 改动范围

README、迁移清单、责任边界、AGENTS.md 7.3 状态同步到 V-CL-04/V-CL-05 实跑后的
最新口径；未改任何代码。

### 具体改动

1. `README.md`：更新时间 2026-08-13；"已具备"补 V-CL-04 三投闭环与 V-CL-05 中断
   策略条目；"尚未完成"删除过期结论（无全场自主搜索/raw Servo 为 0/三投未验收/
   旧控制不消费 map_point），改为当前真实缺口（Gate 干净复跑、H/走廊 Gate、
   召回、30-seed、实拍真值、板端）；里程碑与 SITL 入口表同步。
2. `VISION_MIGRATION_CHECKLIST.md`：更新时间 2026-08-13；M6 增加 V-CL-05 状态行
   （中断机制实跑验证、red_cross 统一入队、随机十字摆放待复跑）。
3. `docs/当前问题与责任边界.md`：首断点段落更新——视觉业务闭环已成立，当前剩余
   阻断归属规划组（低空下降/返航可达性）、任务/仿真联合（H 降落与走廊）、
   量化/部署组（30-seed/实拍/板端）。
4. `AGENTS.md` 7.3-1：卡点更新为 V-CL-04/V-CL-05 实跑结论与按组归属的剩余项。

### 验证结果

- 四处文档与本会话实跑日志/提交内容一致；`git diff --check` 干净。

### 遗留问题

- 其余日期型历史文档（如 TOUDI4 专项、VISION_LAPTOP_SIM_BASELINE 等）按约定
  作历史快照不滚动改写。

### 下一步

- V-CL-05 干净复跑后再次滚动同步 ROADMAP/清单状态并合并 main。


## 2026-08-13（按官方三组责任划分重排剩余阻塞与全随机场地口径）

### 改动范围

依据《基地无人机备赛流程(1).pdf》官方分工（飞控组/导航组/识别组，比赛假设 8.20）
重排剩余阻塞归组，并把"场地全随机布设（4 标准 + 1 红十字）"纳入 V-CL-05 完成定义。
未改代码。

### 具体改动

1. `docs/当前问题与责任边界.md`：5.1 跨组分工表改为官方三组口径（导航组=原
   控制/规划组，识别组=视觉组）；5 节与 4.1 尾段修正——识别组自身的量化/部署项
   （召回 0.95、30-seed/10 min、实拍人工真值、板端 RKNN ROS 链、PT/ONNX 一致）
   尚未验收，不能只等实机部署与调参；飞控组负责机架/结构/PX4/相机标定。
2. `VISION_2026_ROADMAP.md`：V-CL-05 行由"计划中"更新为"部分完成/待干净复跑"，
   完成定义补充场地全随机布设（4 个 1 m 标准靶 + 1 个 0.35 m 红十字全部随机、
   H 固定为起降点、真值仅落盘）。

### 验证结果

- `git diff --check` 干净；归组与 PDF 三组职责逐项对应。

### 遗留问题

- vcl05b 中 tank/panzer 低空下降超时发生于沉降门控修复之前；"高权重中断→投递"
  的完整闭环（含随机红十字）仍需一次干净复跑。
- 全随机靶布设需将现有随机红十字 spawner 泛化为四标准靶 + 红十字联合摆放（导航组
  仿真环境职责，识别组提供场景需求与动态期望 Gate）。

### 下一步

- 干净复跑 V-CL-05（随机十字 + 沉降门控）；随后与导航组对齐实机 CameraInfo/TF、
  mission frame 与释放接口，并按 8.20 窗口安排板端 RKNN ROS 链验收。

## 2026-08-13（V-CL-05 搜索-投递策略：高权重中断、red_cross 统一、随机十字布设）

### 改动范围

`feat/vcl05-search-delivery-strategy` 分支：使命层搜索-投递策略优化与随机投放区红十字。
按人工决策：red_cross 不设独立任务模式，作为普通高权重候选统一入队（内部
drop_cross/drop_circle 仅表示对准几何来源）；规划器失败不重点处理。

### 具体改动

1. `coverage_policy.py`：`RULE_WEIGHTS` 增加 `red_cross: 10.0`；新增 `DISCOVERY_CLASSES`
   （五类标准靶 + 红十字）供累计口径；新增纯函数 `interrupt_eligible`（队列头部权重 ≥
   阈值）与 `expected_delivery_classes`（按已发现候选权重排序的动态期望投递序列）。
2. `coverage_search_manager.py`：SEARCH 态支持高权重中断（默认阈值 4.0，red_cross/tank
   触发）——中断后复用 `_resume_index` 恢复搜索；搜索超时改为按 SEARCH/CANDIDATE_APPROACH
   实际时长累计（中断投递不再吞噬搜索预算）；`_release_matches_candidate` 接受
   (drop_circle,circle) 与 (drop_cross,red_cross) 双模式归因；报告新增 interrupt 字段。
3. `coverage_r6_assertion.py`：期望投递序列改为动态（按已发现候选权重 top-3）；随机十字
   摆放成功（真值文件存在）时要求发现 red_cross；修复重构遗留的两个潜在崩溃点
   （`discovered` 未定义、`selected` 已为类名列表仍按字典取字段）。
4. `patrol_control`（规则 14 快照 `legacy_baseline/20260813_vcl05/` 后最小补丁）：
   外部 ALIGN 按 `target_class=="red_cross"` 设 `CROSS_MISSION`；`externalMissionTick`
   按任务类型分派 `CrossDetectionDone`/`WayPointDetectDone`。旧固定路线与默认模式不变。
5. 新增 `random_red_cross_spawner.py`：仿真启动后在搜索区域内随机摆放 red_cross
   （避开既有模型、保证边界净空，seed 可配）；真值写入 run 目录
   `red_cross_truth.yaml` 仅作复盘，不进入控制链。`coverage_r6.launch` 集成
   `spawn_red_cross`/`red_cross_seed` 参数并给断言传真值路径。
6. `coverage_toudi4.yaml`：新增 `interrupt.enabled/min_weight` 参数块。

### 验证结果

- 单测 34/34（新增 interrupt_eligible 边界、动态期望含/不含十字、red_cross 累计；
  更新释放归因契约测试为双模式）。
- 主集成工作区 `catkin_make -DROS_EDITION=ROS1 -j1` 全量编译通过（含 patrol_control 补丁）。
- `git diff --check` 干净；提交 44bf781 已推远端。

### 遗留问题

- SITL 实跑验收未做：随机十字 + 中断投递 + 动态期望 Gate 需 `coverage_r6.launch` 复跑；
  red_cross 0.35×0.35 在 2 m 搜索高度发现召回风险（发现才参与权重序，未发现不阻塞 Gate）。
- 规划器可达性波动仍外置；H 视觉降落 Gate、北区走廊 Gate 未动。

### 下一步

- 按 `sim_run.sh` 实跑 `coverage_r6.launch`（默认随机十字）：验收 tank/red_cross 中断投递、
  动态期望 Gate、10 min 预算；规划器退化时只记录事实。


## 2026-08-13（V-CL-05 实跑验证：中断机制成立、低空下降规划器波动外置）

### 改动范围

`feat/vcl05-search-delivery-strategy` 实跑与启动链路修复：断言 relay 导入引导、
spawner 安装登记与 Pose 构造、中断前高度沉降门控。规划器失败只记录不迭代。

### 具体改动

1. `coverage_r6_assertion.py`：catkin devel 下经 relay 执行时 sys.path[0] 是 relay
   目录，`from coverage_policy import ...` 会命中空壳 relay 导致 ImportError；加入与
   manager 相同的源码目录引导（`_SCRIPT_DIR` 插入 sys.path）。
2. `uav_mission/CMakeLists.txt`：登记 `random_red_cross_spawner.py` 生成 devel relay；
   脚本统一补执行位。
3. `random_red_cross_spawner.py`：`SpawnModel.initial_pose` 改为 `geometry_msgs/Pose`
   构造（原元组导致序列化 AttributeError）。
4. `coverage_search_manager.py`：capture 阶段新增高度沉降门控——飞机在航段中途被
   中断时可能仍在接近高度之上/未稳定，要求 z 回到 `height+0.15m` 且稳定 2 s 后才
   下发 ALIGN（新参数 `capture_settle_height_margin/capture_settle_duration`），
   避免旧控制从未沉降状态以 ~1 cm/s 慢降导致 75 s 外部对准超时。

### 验证结果

- 单测 34/34；主工作区全量编译通过；`git diff --check` 干净；提交已推远端。
- `logs/toudi4_coverage_r6_vcl05b_20260813_234314/`（修复前代码实跑）：
  - 中断机制成立：tank 在第 9 个覆盖端点前被中断执行（命令序列 9×SEARCH→APPROACH→
    ALIGN→RESUME→恢复搜索），覆盖最终 12/12、0 跳点、0 碰撞、0 越界；
  - tank/panzer 均产生有效 circle 证据（drop_ready=true、仲裁器两次锁定 tank
    circle/7），但低空下降极慢（0.92→0.28 m 用 60 s）导致 75 s 外部对准超时；
    pillbox 完成端到端投递（槽 1，raw mock ACK），bridge/tent 接近不可达、返航
    不可达——与 v3 同类的规划器低空/可达性波动；
  - 该波动为规划侧已知问题，按人工决策外置，不阻塞视觉业务验收。
- `logs/toudi4_coverage_r6_vcl05_20260813_233333/`（首跑）：暴露上述启动链路缺陷
  （断言 ImportError、spawner 未登记/元组 Pose），已全部修复。

### 遗留问题

- 沉降门控与修复后的 spawner 尚未在同一轮实跑中验证；需一次完整复跑观察
  tank/red_cross 中断投递与 Gate PASS（随机十字摆放成功时 Gate 要求发现十字）。
- 低空下降速率与返航可达性仍受 Fast-Planner 影响；V-CL-05 的使命层策略逻辑
  已闭环（中断/恢复/队列/释放归因），完整 PASS 待规划器正常轮次。
- H 视觉降落 Gate、北区走廊 Gate 未动。

### 下一步

- 用户确认后复跑 `coverage_r6.launch`（默认随机十字）：验收沉降门控 + 随机十字
  发现/投递 + 动态期望 Gate；规划器退化时按既定口径只记录事实。


## 2026-08-14（V-CL-05 中断逻辑实跑验证与 capture 阈值修复）

### 改动范围

`feat/vcl05-search-delivery-strategy` 分支：随机十字摆放净空放宽、capture 沉降余量与
圆环几何阈值放宽，并完成中断投递逻辑三轮实跑验证。未改 toudi4.world，规划器失败只记录。

### 具体改动

1. `random_red_cross_spawner.py`：min_clearance 1.2→0.6 m、max_attempts 200→1000
   （搜索区标准靶/树较密，1.2 m 净空导致采样 200 次无解；0.6 m 已保证 0.35 m 十字
   不与其他板重叠）。vcl05e 已成功摆放（0.732, 4.470）并落盘真值。
2. `config/target_memory.yaml`：aux_geometry_confidence 0.85→0.80。取证：2.2 m 搜索
   高度圆环实际质量 0.842-0.857，卡在 0.85 边缘导致 memory 连续帧确认不稳、圆环不进
   入 targets（原始帧用检测器复刻验证圆环清晰可检）。
3. `config/coverage_toudi4.yaml`：candidate 下新增 `capture_settle_height_margin: 0.30`
   （规划器在 2.0 目标上空约 2.22 m 悬停，0.15 余量过紧导致 capture 恒定超时）。

### 验证结果（三轮实跑）

- `logs/toudi4_coverage_r6_vcl05d_.../`（修复前）：tank/panzer `capture_timeout_no_circle`
  ——定位为沉降门控 z 阈值（飞机稳定悬停 2.22 m）与 aux_geom 0.85 边缘双重卡点；
  bridge/pillbox/tent 3 投成功并降落（manager PASS，Gate 因期望序列不符 FAIL）。
- `logs/toudi4_coverage_r6_vcl05e_.../`（修复后）：随机十字成功摆放；tank 中断后
  capture 通过并进入 ALIGN（验证沉降+阈值修复生效），但 ALIGN 低空下降 90 s 超时
  （北墙区规划器慢降，与 vcl05b 同类）；panzer 同因超时，bridge/pillbox/tent 接近
  不可达，返航不可达；覆盖仍 12/12、0 碰撞、0 越界。red_cross 未被发现（0.35 m
  靶 + 红十字召回 0.646）。
- **中断投递-恢复搜索逻辑验证结论**：中断（tank 提前执行）→capture→ALIGN→RESUME
  恢复搜索→覆盖补全，机制链路成立；ALIGN 完成（下降+释放）与候选接近可达性受
  Fast-Planner 影响，属导航组职责，本轮不迭代。

### 遗留问题

- 完整"高权重中断→投递"绿跑仍待规划器正常轮次；随机红十字独立发现/投递受召回限制。
- 全随机场地（4 标准 + 1 红十字）由导航组仿真环境实现，识别组已备好动态期望 Gate。

### 下一步

- 导航组处置 Fast-Planner 低空下降/可达性后，复用本分支复跑即可拿 Gate PASS；
  识别组侧按 8.20 窗口优先推进板端 RKNN ROS 链与召回/负样本。


## 2026-08-14（红十字多高度离线识别测试与“发现门控/航线”根因结论）

### 改动范围

无代码改动。用离线复刻测试定位红十字实跑未被发现的根因，并明确后续处置口径。

### 验证结果（离线多高度识别）

用当前下视机架内参（fx≈554，640×480，60° FOV）、`merged_standard` best.pt YOLO 与
`cross_detector` 几何逻辑复刻，在正下方俯视纯背景纹理上扫 0.8–3.0 m：

| 高度(m) | 十字像素≈ | YOLO red_cross 置信度 | cross_detector 几何 |
|---|---|---|---|
| 0.8 | 208 | 0.97 | PASS |
| 1.0 | 166 | 0.97 | PASS |
| 1.2 | 139 | 0.96 | PASS |
| 1.5 | 111 | 0.93 | PASS |
| 2.0 | 83 | 0.94 | PASS |
| 2.5 | 66 | 0.94 | PASS |
| 3.0 | 55 | 0.89 | PASS |

并在真实渲染场景（seed_23_red_cross，相机 2.0 m）复验：YOLO `red_cross 0.93` +
cross_replica 几何 `PASS` 同帧命中，排除“离线纹理干净才灵”的顾虑。

脚本：`logs/toudi4_coverage_r6_vcl05e_20260814_011723/cross_sweep.py`（多高度）、
`cross_scene_check.py`（真实渲染复验）。

### 结论（实跑未发现的两条链路叠加，高度非瓶颈）

1. **发现门控过严（识别组）**：融合 `require_red_cross_dual_confirmation=True` 要求
   “YOLO red_cross + cross_detector 几何”在同一时间桶同时成立，再叠加 target_memory
   连续 N 帧确认；巡航 1.5 m/s 下十字入画到出画仅约 1 s，两路节拍不同、命中帧错开，
   同桶命中稀碎 → 达不到连续确认。
2. **覆盖航线掠飞（导航组）**：vcl05e 十字在 y=4.47，serpentine 行 y=4.77 只是擦边
   掠过，十字处于画面边缘/斜视，几何形变把 crosslike 判据打掉。

### 处置口径

- 导航航线加密/降速问题**暂时搁置**（导航组职责，与 Fast-Planner 低空下降同批外置）。
- 识别组**采纳“疑似红十字→抵近确认”方向**：发现阶段放宽 red_cross 门控，一旦出现
  疑似红十字候选（YOLO 或几何单路即可），引导低空 1.0–1.5 m 精搜/悬停确认；交付阈值
  仍保持严格（投递释放前必须双确认）。后续作为 V-CL-05 之外的新增子项排期。

### 遗留问题

- “疑似红→抵近确认”尚未落地实现（需在 coverage_search_manager 候选层加 red_cross
  专属 APPROACH 触发，或由融合层输出低置信疑似候选），排期后另开分支。
- 板端 RKNN ROS 链、召回/负样本按既定优先级不变。

### 下一步

- 视用户排期，实现 red_cross 疑似候选的抵近确认闭环；否则按 8.20 窗口继续板端
  RKNN ROS 链与召回/负样本。


## 2026-08-14（V-CL-05 主干同步与红十字近场捕获修正）

### 改动范围

在 `feat/vcl05-search-delivery-strategy` 上合入最新 `main` 文档规则，并修正 Mission
Manager 对红十字近场几何证据的判断。未修改旧固定路线、Servo 服务和执行机构代码。

### 具体改动

1. 以 `--no-ff` 合入 `main`：ROADMAP 采用主干的全随机场地完成定义，AGENTS/README/
   迁移清单/责任边界保留主干最新内容；联调变更记录同时保留主干三条文档治理记录和
   V-CL-05 四条开发记录，没有回退主干文档。
2. `coverage_policy.py` 新增按候选类别选择近场证据的纯策略：五类标准靶要求 `circle`，
   `red_cross` 要求 `red_cross` 自身几何证据。
3. `coverage_search_manager.py` 将原先仅维护圆环的捕获表扩展为模式化捕获证据；红十字只
   接受已确认、地图有效、来源为 `red_cross_geometry` 的新鲜候选。红十字外围黑环不按
   标准投放区蓝环处理；超时原因同步区分 `no_circle` 与 `no_red_cross`。
4. `coverage_toudi4.yaml` 同步上述几何口径；`test_coverage_policy.py` 增加标准靶/红十字
   双向排他测试。

### 验证结果

- `uav_mission` Python 全量单测 36/36 PASS；新增断言确认标准靶不能由红十字证据放行，
  红十字不能由 circle 放行，且陈旧红十字证据无效。
- 三处 Python 文件 `py_compile` 通过，`git diff --check` 通过。
- 本轮未启动新的 ROS/Gazebo；保留现有 `toudi4_gui_redcross` GUI 仿真，不干扰用户进程。

### 遗留问题

- 红十字从 `CANDIDATE_CAPTURE` 进入 ALIGN 的逻辑缺口已补，但尚未做随机红十字在线投递
  实跑；当前仅有逻辑单测和既有随机布设/离线发现证据。
- Fast-Planner 低空下降/返航可达性、全场四标准靶加红十字联合随机布设仍按既定分工推进。

### 下一步

- 完成本分支提交与远端同步后，按人工指令 `--no-ff` 合并回 `main`，保留功能分支并打
  `gate/vcl05-search-delivery-strategy` annotated tag。


## 2026-08-23（冻结 V-EXP-01，回归单下视视觉主线）

### 改动范围

从最新 `main@dc89e18` 建立文档分支 `chore/freeze-oblique-camera-exploration`。本轮只调整
项目决策、唯一 ROADMAP、README 和冻结说明；未修改视觉、任务、控制、规划、仿真模型或
执行机构代码，未启动 ROS/Gazebo，也未操作实机。

### 具体改动

1. 冻结斜下辅助相机 `V-EXP-01`，保留
   `feat/oblique-camera-search-feasibility@b60a4d9`、
   `feat/oblique-active-search-stability@ab6e447` 和
   `feat/oblique-aux-proposal-provider@3f2e55a`，不删除、不合入生产主线。
2. 明确 `main` 从未包含辅助相机提交，生产架构继续为单个下视 RGB 相机 + MID360，不需要
   回退代码；禁止继续辅助 YOLO、单 runtime 双输入和双相机随机世界 A/B，除非单下视无法
   满足比赛时限有量化证据。
3. 新增 `docs/VEXP01辅助相机探索冻结说明_20260823.md`，分别给出辅助方向里程碑完成度
   2/5（40%）、证据边界、冻结项，以及单下视软件链/仿真闭环/比赛交付的分层完成度。
4. 单下视下一任务冻结为：全随机五靶闭环、红十字搜索阶段疑似候选与投递前严格复核、
   运动相机地图记忆 Gate、H 降落、实拍真值、30-seed/10 min 和 OrangePi ROS 链。
5. 修复 `VISION_2026_ROADMAP.md` 任务表中重复出现的 V-SIM-04/V-REAL-01/V-DEPLOY-01
   三行，并将冻结项加入唯一任务表；同时清理“10 min shadow 是当前首要/接入 Mission
   Manager 前置”的过时口径，补齐 V-CL-05 单下视完成定义；README 同步最新里程碑和
   文档入口。

### 验证结果

- `git fetch --all --prune` 后确认 `origin/main=dc89e18`，辅助相机一组堆叠实验提交只存在于远端
  feature 分支，未进入主干；工作区在切分支前干净。
- 本轮为文档/优先级变更，不新增仿真 Gate；以链接、任务表唯一性、分支提交引用和
  `git diff --check` 为验证项。

### 遗留问题

- 单下视主线仍缺全随机五靶实跑、随机红十字独立发现/投递、H 视觉降落、跨视角地图记忆
  量化、实拍人工真值和 OrangePi ROS 10 min Gate。
- Fast-Planner 低空下降/返航可达性和北区走廊仍属联合/导航 Gate；视觉组只记录失败阶段，
  不通过恢复辅助相机或更换 LIO/planner 绕开。

### 下一步

- 在新的单一 Gate 功能分支中先完成“单下视 + 全随机五靶 + 红十字疑似候选抵近复核”的
  无 GUI 多 seed 闭环；本冻结文档分支只提交和推送，不由 agent 合并 `main` 或打 tag。


## 2026-08-23（仿真 SDF 地图收敛为 20×20×5 与正赛类目适配复核）

### 改动范围

在 `feat/planner-map-20x20x5-sim` 分支修改 Fast-Planner 仿真 launch/XML、比赛仿真入口、
覆盖搜索入口、对应静态回归和仿真/ROADMAP 文档。分支基于尚未合入 `main` 的冻结说明提交
`9889591`，后续合并时须先处理 `chore/freeze-oblique-camera-exploration`，避免文档依赖倒置。
未修改 `patrol_planner_real.xml`、旧控制状态机、视觉算法、世界资产、实机链路或执行机构。

### 具体改动

1. 正式 PX4/Gazebo、新视觉完整仿真、旧完整仿真和内部 Fast-Planner 仿真默认地图统一为
   `20 × 20 × 5 m`；分辨率 `0.1 m` 下约 200 万体素。仓库原正式仿真默认实际为
   `100 × 100 × 50 m`（约 5 亿体素），理论体素数减少 250 倍。
2. 仿真 SDF 膨胀统一为水平 `0.25 m`、向上 `0.20 m`、向下 `0.10 m`；同步把
   `local_update_range_z` 从 `20.5 m` 收敛到 `4.5 m`。覆盖入口原 `0.30 m` 显式覆盖改为
   `0.25 m`，clearance threshold 仍保持覆盖链原 `0.35 m`。
3. 补齐 `patrol_planner_sim.launch` 传入 XML 所必需的 `obstacles_inflation` 和
   `clearance_threshold` 参数；保留 `legacy_mode_regression.launch` 的历史
   `14 × 14 × 6 m` 显式覆盖及独立 `topo_replan` 示例。
4. 更新静态回归，锁定仿真地图/膨胀完整参数，并断言实机 XML 仍为原水平 `0.21 m`、
   上/下 `0.14 m`，防止仿真调参污染实机。
5. 文档明确单下视架构能承载“四标准无 tank + 随机 red_cross”三投，但当前默认策略/Gate
   仍硬编码五类标准靶并接受 tank 候选，随机红十字也缺在线完整绿跑；V-CL-05 增加可配置
   比赛类目 profile、排除 tank 假阳性抢队列和随机十字在线投递完成定义。

### 验证结果

- `roslaunch --dump-params` 展开完整新视觉比赛入口，确认最终参数为地图
  `20.0/20.0/5.0`、膨胀 `0.25/0.20/0.10`、局部 z 更新范围 `4.5`。
- 通过统一 `sim_run.sh` 完成两次 planner-only 无 GUI 烟测：
  `logs/planner_map_20x20x5_smoke_20260823_233020/` 确认 Fast-Planner、轨迹服务器和航点
  生成器成功启动并在定时 SIGINT 后无残留；
  `logs/planner_map_20x20x5_rss_smoke_20260823_233119/` 记录 15 s 进程族最大 RSS
  `184064 kB`（约 180 MiB）、0 swap。未接 odom 时保持 `INIT/no odom` 属预期。
- 9 个改动 launch/XML 均通过 `xmllint --noout`；`git diff --check` 通过。
- `test_new_vision_config.py` 8/8 PASS，`test_coverage_policy.py` 28/28 PASS。
- 尝试运行 Catkin 包级测试时，现有 `uav_mission` 没有注册 `run_tests_uav_mission` 目标；
  已注册的 `patrol_control` C++ 测试又被既有 `uav_utils` 链接错误
  `cannot find -luav_utils` 阻断。该错误与本轮 launch/文档变更无关，本轮未扩散修复。
- 未启动完整 PX4/Gazebo 飞行；上述 RSS 只代表 planner-only 进程族，不能作为完整系统
  内存结论。

### 遗留问题

- 需要一次 headless 完整入口实跑，分别记录 PX4/Gazebo/规划/视觉的 RSS、地图边界、
  起飞/覆盖/返航和规划可达性，确认 5 m 高度范围在实际状态树中无越界。
- 正赛类目 profile 尚未代码化；当前 Gate 无 tank 时会因硬编码
  `STANDARD_CLASSES` 误判失败，tank 假阳性仍可能抢占候选队列。
- 随机红十字仍缺在线“搜索发现→抵近确认→drop_cross 对准→许可/ACK”完整绿跑。

### 下一步

- 后续在本分支做完整 PX4/Gazebo 无 GUI 地图边界与内存实跑；视觉主线另开单一 Gate 分支
  实现比赛类目 profile 与随机红十字在线投递，不在本地图参数分支修改视觉/任务业务逻辑。


## 2026-08-26（导航组原始搜索 manager 接入新视觉并完成单轮 headless）

### 改动范围

从 `main@7a0b612` 创建 `feat/navigation-mission-visual-delivery`，读取导航组
`https://github.com/sakelier/liftrace-controlwork.git` 的 `main@5144aa8` 和根目录
`近期工作说明_2026-08-25.md`。接入导航组 Python manager/策略、外围视觉投递适配器、
headless Gate、RViz 下视图像显示和中文 handoff。未修改导航组 manager 状态机、搜索策略、
候选准入逻辑、Fast-Planner 源码、旧控制投递源码或真实执行机构。

### 具体改动

1. 纳入导航组 `target_search_manager_py.py`、`SearchPolicy`、`CandidatePolicy`、
   `SearchContext/MissionState`、`sim.yaml` 和原 launch/setup，并启用
   `catkin_python_setup()`；核心 manager 与上游文件一致，策略无逻辑差异。
2. 新增 `navigation_search_delivery_toudi4.launch`：导航 manager 的
   `/fastplanner/goal` 重映射为 `/navigation/goal_raw`；外围 adapter 独占最终
   `/fastplanner/goal`。原临时 `coverage_search_manager` 不启动。
3. 新增 `navigation_visual_delivery_adapter.py`：只在起飞和地图可用后转发导航原始目标；
   候选抵近后要求标准靶 circle/红十字几何新鲜证据，再进入旧控制 ALIGN 和 guarded
   software mock 投递；成功后恢复导航组中断索引，三投后才返航降落。
4. 适配器不周期重发导航目标；航段失败由“固定总时长”改为“持续无实质进展”，默认
   120 仿真秒/3 cm，避免误杀仍在接近的长航段。导航组 0.3 m 三维到达判据保持不变。
5. 新增 `target_search_delivery_assertion.py` 和导航纯策略单测；Gate 检查 goal 所有权、
   上游来源标记、旧临时 manager 缺席、三不同 ID/三槽/三 positive ACK 和场地边界。
6. 文档同步 README、唯一 ROADMAP、迁移 Gate、仿真手册、责任边界、`uav_mission`
   README；新增 `docs/导航组任务链与新视觉联调HANDOFF_20260826.md`。导航组原工作说明
   保持原文纳入交接。

### 验证结果

- `uav_mission` 单包 Catkin 编译通过；主工作区清空 whitelist 后全量 30 包串行编译通过；
  `uav_mission` 四组纯策略单测 40/40 PASS；视觉 L0 全套 PASS；三个新增 Python 节点
  `py_compile`、两个 launch 的 XML 解析和完整 launch 32 节点展开均通过。
- 话题所有权实跑确认：`target_search_manager_py` 是 `/navigation/goal_raw` 唯一发布者，
  adapter 是 `/fastplanner/goal` 唯一发布者；旧临时 manager 全程不存在。
- 正式模型确认加载
  `vision_ws/runs/liftrace_6cls_v5_merged_standard_20260714/weights/best.pt`，类别为六分类。
- 正式 headless：
  `logs/navigation_upstream_visual_delivery_headless_model_20260826_023411/`。600.0 s 到达
  覆盖索引 0～8（16 点中的 9 点），发现 pillbox/tent/bridge；tent 槽 1、bridge 槽 2
  均完成 `APPROACH→CAPTURE→ALIGN→guarded ACK→RESUME`；pillbox 因
  `capture_timeout` 安全拒绝；0 平面越界；第三投、返航和落地未进入，最终
  `mission_timeout`，Gate FAIL。
- 随机红十字真值 world `(0.5723,4.5496)`，换算本地约 `(1.066,6.322)`；超时时最大
  本地 y 约 `4.875`，航线尚未覆盖其视场，因此本轮不评价红十字召回。
- 导航日志记录搜索长航段高度短时约 4.7 m，超过规则 4 m；需规划/轨迹执行侧继续分析。
- 两次前置诊断仅用于定位，不作为 Gate：
  `navigation_upstream_visual_delivery_headless_20260826_020744` 暴露固定 45 s 总时长误判；
  `navigation_upstream_visual_delivery_headless_progress_20260826_021802` 暴露模型路径为空并
  在索引 6 无进展。问题均未通过修改导航组源码处理。
- 用户在本轮完成后明确暂缓 GUI/RViz 第二轮；所有 ROS/Gazebo/PX4 进程已由统一 runner
  自动收尾，无仿真残留。

### 候选/权重结论

- `target_memory` 只在当前新鲜（默认 `last_seen <= 0.5 s`）候选中按
  `red_cross=10, tank=5, panzer=2.5, bridge=2, pillbox=1.5, tent=1` 选
  `selected_target`；同一时间窗内权重有效。
- 导航组 `CandidatePolicy` 只做地图合法性、状态、有限坐标和新 ID 准入。manager 在
  SEARCH 收到首个合法候选就立即 APPROACH，非 SEARCH 阶段的新目标被忽略；接受 ID 在
  失败后也不会重试。因此当前是局部权重 + 发现时序，不是全图持久权重队列。

### 遗留问题

- 600 s 内只完成两投，V-CL-06 保持 Gate FAIL；第三投、返航、H 降落均未验收。
- 正式任务层缺持久候选队列、场景类目 profile、投递结果回写、失败冷却/重试和剩余时间
  调度；该项属导航/任务组，不在视觉包内实现。
- 视觉组需复盘 pillbox 抵近阶段圆环捕获，并实现红十字“搜索疑似可抵近、投递前严格
  双确认”；随机红十字当前 `seed=0` 实际使用系统随机种子，正式多 seed 前需修正契约。
- 搜索航段高度上冲、长航段耗时和 20×20×5 z 边界由导航/规划侧联合复核。

### 下一步

- 先由双方评审 handoff，冻结 `/targets` 持久候选、`selected_target` 实时候选和投递结果
  接口；导航组在自有分支实现全局队列/重试/时间调度，视觉组不改其已验证 manager。
- 同 world/模型/seed 复跑 600 s headless；只有三投、返航、落地和安全断言全部通过后，
  才恢复 GUI/RViz 二轮。


## 2026-08-27（明确完整工程各链路仓库来源与回流方向）

### 改动范围

仅修订 `AGENTS.md`、导航联调 handoff 和本联调记录，不修改视觉、导航、控制、规划或仿真
运行代码。动机是原 `AGENTS.md` 虽已有包所有权和分支规则，却没有显式登记视觉组主仓
GitHub 地址，也没有区分导航组权威仓、2025 旧链基线和第三方仿真依赖的回流方向。

### 具体改动

1. 在 `AGENTS.md` 新增“代码仓库、链路来源与回流方向”，明确视觉组主仓兼完整集成仓为
   `https://github.com/Qinling-Melon-Farmers/liftrace-visionwork.git`，本地落点为
   `/home/xhj/liftrace`。
2. 明确导航组权威开发仓为 `https://github.com/sakelier/liftrace-controlwork.git`，本地
   `/home/xhj/liftrace-controlwork-nav` 只作为同步参考；导航组 manager/策略及后续任务、
   规划改动应优先回到其 feature 分支，再以明确 revision 接入视觉集成仓。
3. 补充 2025 香橙派旧链基线、PX4、AstraDroneOpen、ROS 系统依赖、仿真日志/模型/数据的
   来源、当前落点和是否进入本仓 Git 的边界，并写出整机链路关系图。
4. 在导航联调 handoff 的 Git 接续章节同时列出两个团队仓库及本轮导航上游 revision，避免
   交接者只看到本地目录而不知道代码应回流何处。

### 验证结果

- 使用各仓 `git remote -v` 核对：视觉组 `origin`、导航组参考 clone、PX4 和
  AstraDroneOpen 的远端地址均与新增登记一致。
- 仅文档变更，未启动仿真；Markdown 表格、链接、分支名和 revision 已静态核对。

### 遗留问题

- 2025 香橙派拷回基线目前没有登记独立权威远端，只能以本仓历史和 legacy snapshot 规则
  保持可追溯；若控制组另有正式仓库，后续应补充 URL 和首个对齐 revision。
- 两个团队仓存在部分同名目录，后续同步仍需逐文件确认，不能把“仓库来源已登记”理解为
  可以互相覆盖工作区。

### 下一步

- 由视觉组和导航组共同确认仓库所有权表；以后每次跨仓联调在 handoff 中同时记录 source
  URL、branch、commit 和本仓接入提交。
## 2026-08-28（V-CL-04 干净复跑收尾：任务侧全指标达标，tank 规划侧下降异常经裁定视为通过）

### 改动范围

分支 `feat/vcl04-clean-rerun`（基于 `main@7a0b612`，含 20×20×5 地图收敛与 V-CL-05 断言语义）。
无源码改动；仅本文档与 `VISION_2026_ROADMAP.md` 状态更新。两轮 headless 实跑均经
`bash sim_run.sh`（执行位缺失问题同 L6543）调用 `coverage_r6.launch gui:=false rviz:=false
spawn_red_cross:=false enable_debug_image:=true record_debug:=true`，spawn_red_cross 显式
关闭以保持 V-CL-04 固定五类标准靶口径。

### 具体改动

1. 第一轮实跑 `logs/toudi4_coverage_r6_vcl04_rerun_20260828_220809/` FAIL。根因：复跑
   shell 未带 `UAV_VISION_MODEL_PATH`，`coverage_r6.launch` 的 `$(optenv
   UAV_VISION_MODEL_PATH)` 解析为空串，`[TargetDetector] model unavailable` 按既有设计
   退化为空检测（launch 照常拉起，L1120-1122 既有结论复现），12/12 覆盖全程 0 候选，
   管理器最终 `return_home_unreachable`。v2 达标跑当时 shell 带有该变量
   （`top_level_scripts/toudi3_combined_env.sh:66` 导出的即 v2 所用 merged_standard 模型；
   `docs/VISION_NAVIGATION_INSTALL_AND_SIMULATION_20260807.md:306` 有记载）。
2. 两轮 run.log 的 roslaunch 参数摘要全量 diff 核对：除模型路径外，差异仅 V-CL-05 新参数
   （red_cross_truth_path、capture_settle、interrupt）、20×20×5 地图收敛
   （map_size/膨胀/local_update_range_z）和 `target_memory/aux_geometry_confidence`
   0.85→0.80（来自 `dec2ff3` 有意修复），无未解释漂移。
3. 第二轮实跑 `logs/toudi4_coverage_r6_vcl04_rerun2_20260828_222644/`：改为命令行显式
   `target_model_path:=/home/xhj/liftrace/vision_ws/runs/liftrace_6cls_v5_merged_standard_20260714/weights/best.pt`
   （manifest 可审计），早检确认模型加载（`TargetDetector ready`，无 model unavailable）。

### 验证结果

- 第二轮任务侧全指标达标：12/12 覆盖、五类五物理 ID（tank=8/panzer=6/bridge=2/
  pillbox=0/tent=4）、三投 panzer→bridge→pillbox 槽序 [1,2,3]、raw ACK [1,2,3]、
  0 碰撞、0 越界、零拒绝、audit PASS、405.5 s（≤600 s）完成三投+返航+落地
  （manager `three_deliveries_landed`/`COMPLETE`）。
- Gate `coverage_r6` 27 项断言仅 4 项不满足且同源：`delivered_classes==expected_classes`
  （[panzer,bridge,pillbox] vs 动态期望 [tank,panzer,bridge]）与 command 1/2/3 计数各
  4 次（要求 3 次）——全部由 tank 一次中断对准失败连锁造成；其余 23 项全过。
  `gate_status.json` status=FAIL（`coverage_r6_contract_failed`）为客观记录，保留不改。
- tank 失败取证（run.log/coverage_status/visual_delivery_audit）：视觉与对准链健康——
  ALIGN 正确进入（`External ALIGN target=8`、`AlignMode->drop_circle`、`align_ok:1`
  持续、circle 地图点跟随 1079 次），`drop_ready=true reason=evidence_valid` 多次出现；
  失败模式为 Fast-Planner 下降段执行异常：z 自 1.74 m 穿过指令对准高度
  `align_height=1.20`（`patrol_toudi4.yaml:19`）继续降至 0.08–0.30 m 悬停约 70 s，
  期间 `No Effective Points!` 1339 次，相机过近致圆环出视场（stale_observation 48 次）、
  水平偏移超限（offset_exceeds_limit 16 次、目标修正 0.5 m 限幅 18 次），释放许可互锁
  正确保持 false，89.98 s 触发 90 s 超时。同场成功对照：panzer/bridge/pillbox 对准均
  稳定在 z=1.14–1.20 m 完成投放；v2 参照 1.18 m。与 vcl05e tank 低空下降异常同签名，
  判定为规划侧问题（视觉无关）。
- 处置裁定（用户确认）：tank 失败为视觉无关的规划器异常，按既定“规划器波动外置、
  不阻塞视觉验收”口径，V-CL-04 视为通过，不再复跑。

### 遗留问题

- Fast-Planner 低空下降/可达性波动（本轮新增证据：穿透对准高度至 0.1–0.3 m 悬停 +
  低空重规划 `No Effective Points`）仍归导航组处置。
- 动态期望断言在“最高权重候选对准失败”场景会连锁 FAIL（delivered==expected 与命令计数
  钉死 3 次均未含失败重试语义），与 V-CL-05 中断/失败恢复口径存在缺口，移交 V-CL-05/06
  Gate 定义时收敛；不得为凑 PASS 临时改断言。
- `coverage_r6.launch` 模型路径依赖 `UAV_VISION_MODEL_PATH` 环境变量，脱离组合环境脚本
  启动时必须显式传 `target_model_path:=`（建议写入复跑 SOP）。

### 下一步

- 推进 V-CL-05 全随机布设（4 个 1 m 标准靶 + 1 个 0.35 m 红十字全随机、`red_cross_seed>0`
  固定种子）与类目 profile 代码化；本分支不再承载其他 Gate，合并 `main` 由人工确认。


## 2026-08-28（V-CL-05 启动：类目 profile 代码化 + 全随机场地基建）

### 改动范围

分支 `feat/vcl05-full-random-deploy`（基于合并后 `main@7e626ad`）。改 `uav_mission` 的
`coverage_policy.py`、`coverage_search_manager.py`、`coverage_r6_assertion.py`、
`CMakeLists.txt`、`test/test_coverage_policy.py`；新增 `scripts/random_field_spawner.py`、
`config/coverage_toudi3_random.yaml`、`launch/coverage_random_field.launch` 与仓库根
`toudi3_random.world`。未改视觉链（`uav_vision`）、旧控制状态机与任何实机路径。

### 场地决策（经用户确认）

1. 全随机场地基座选 **toudi3 场地结构**：其 `toudi2` 场地模型含规则书障碍门特征
   （0.8 m 门洞 `Wall_15`、北区侧墙 `Wall_16`），toudi4 缺这两面墙；V-CL-05 完成定义
   为“按规则场地”全随机布设。
2. **双 H 必须**且起飞 H 与 toudi4 同位（world `(-0.493412,-1.77269)`, yaw 1.57018），
   不得放 (0,0)；第二 H 用 toudi4 clone 位 `(2.78088,6.28453, yaw -3.09751)`。走廊
   （含门洞）与双 H 坐标视为赛会给定值，投递区内不给坐标、全随机。
3. 资产核查：五类标准靶在 PX4 sitl_gazebo-classic 均有独立 28 行静态 `model.sdf`
   （拼音名 zhangpeng/dibao/qiaoliang/zhuangjiache/tanke），可经
   `/gazebo/spawn_sdf_model` 动态摆放，机制与既有 red_cross 相同。

### 具体改动

1. **类目 profile 代码化**：`coverage_policy.py` 新增 `CLASS_PROFILES`（`full`=五类
   历史回归口径；`r2026`=规则书四类标准靶无 tank）、`profile_standard_classes`、
   `profile_allowed_classes`（标准靶 + red_cross）；`candidate_valid` 与
   `CandidateQueue.update` 增 `allowed_classes` 准入参数。管理器新增 `~class_profile`
   参数（默认 full），候选 retain/update 全部按允许集合过滤，状态 payload 上报
   `class_profile/allowed_classes`；断言脚本同步按 profile 判定“发现完整”与候选计数，
   payload 落盘 profile。tank 假阳性从队列与中断阈值两侧被隔离（r2026 下 weight 序
   red_cross=10 > panzer=2.5 > bridge=2.0 > pillbox=1.5 > tent=1.0，仅红十字触发中断）。
2. **随机场地摆放**：新增 `random_field_spawner.py`——按参数化搜索域随机摆放标准靶
   列表（默认 panzer,bridge,pillbox,tent）+ 红十字，避开既有模型、pairwise 净空取
   双方较大值（标准板 1.2 m、红十字 0.6 m），真值写 `random_field_truth.yaml` 并
   向后兼容写 `red_cross_truth.yaml`（保持断言“摆放即要求发现”门控）；seed>0 固定
   随机可复现。
3. **派生世界 `toudi3_random.world`**：由 `toudi3.world` 手术生成——剥离嵌套分组内
   5 个标准靶子模型（保留 4 棵树/箱/墙/场地）、整体删除 `<state>` 快照段、主 H include
   平移至 toudi4 主 H 位并新增 `landing_h_clone` include；1482 行，xmllint 通过。
4. **配置与入口**：`coverage_toudi3_random.yaml`（含 class_profile=r2026；搜索域
   x[-2.007,1.993] y[0.273,6.273] 恰为 toudi3 原覆盖区 x[-2.5,1.5] y[-1.5,4.5] 平移
   到新 H 的结果，与 V-CL-04 航线直接可比）、`coverage_random_field.launch`
   （world 指向派生世界、spawner/管理器/断言三处接线、`required` 不误用于摆放后
   即退出的 spawner）。`CMakeLists` 登记 `random_field_spawner.py`。

### 验证结果

- 单测：`test_coverage_policy.py` 32/32 PASS（新增 4 例：profile 定义与回退、
  candidate_valid 准入、队列过滤丢弃、r2026 发现集动态期望），同包
  `test_external_mission_contract.py`、`test_release_commitment_policy.py` 全 OK。
- `toudi3_random.world` 与新 launch `xmllint --noout` 通过。
- `roslaunch --dump-params`（按 AGENTS 8.5 顺序补 PX4 包路径）644 行参数展开干净：
  manager/断言 `class_profile=r2026`、搜索域四值正确、`/random_field_spawner/seed=0`
  与 `standard_classes=panzer,bridge,pillbox,tent` 到位。
- `catkin_make -DROS_EDITION=ROS1 -DCATKIN_WHITELIST_PACKAGES="uav_mission" -j1`
  定向编译通过，随后白名单清空重跑；`devel/lib/uav_mission/random_field_spawner.py`
  relay 生成，roslaunch 可启动。

### 遗留问题

- 派生世界未实飞：toudi3 场地实际边界与配置 field bounds 的一致性、spawner 在树/箱
  环境下 1.2 m 净空采样可行性、随机摆位与覆盖航线的可检测性，均待 headless SITL
  实跑验证（下一步）。
- 红十字“疑似（单路命中）→抵近 1.0–1.5 m 低空确认”闭环仍未实现（8-14 结论的落地项），
  本轮随机十字在线发现召回尚无保障。
- 动态期望断言与对准失败重试的口径缺口（V-CL-04 收尾移交项）在本 Gate 实跑前需收敛，
  不得为凑 PASS 临时改断言。
- 走廊穿越与走廊 Gate 完全独立，本入口不含北区走廊任务段。

### 下一步

- headless SITL 实跑 `coverage_random_field.launch`（field_seed>0 固定种子），先验
  摆放成功率与覆盖航线可检测性，再验三投闭环；随后补红十字抵近确认链路。


## 2026-08-29（随机场地首轮 SITL：坐标系 bug 修复 + 4/4 标准靶发现验证 + 环境事故记录）

### 改动范围

分支 `feat/vcl05-full-random-deploy`（bd28c8f 之后）。改
`scripts/random_field_spawner.py`、`scripts/random_red_cross_spawner.py`、
`launch/coverage_random_field.launch`、`launch/coverage_r6.launch`。未改视觉算法、
旧控制状态机与实机路径。

### 环境事故与恢复（写明以防复发）

1. 首轮 SITL（smoke1）全节点启动即死：`uav_vision` 包/节点全部找不到 + PX4 startup
   返回 2。根因是**在未 source `vision_ws/devel` 的 shell 里执行了
   `catkin_make -DCATKIN_WHITELIST_PACKAGES="uav_mission"`**，patrol devel 的 overlay
   记录被重写为仅含本工作区（L2427 既有教训复现）；随后一次带正确环境的修复构建因
   CMake 缓存命中未重新生成 devel 配置，断链仍在。最终 `rm -rf build devel` 全量重建。
2. 重建期两个次生坑：① 工具 shell 继承 conda base python3（3.13，无 em）导致 cmake
   `Unable to find either executable 'empy' or Python module 'em'`——旧 build 缓存中的
   EMPY_SCRIPT 一直掩盖该问题，清空后暴露；修法为固定 Ubuntu PATH、清除
   PYTHONHOME/PYTHONPATH 并显式 `-DPYTHON_EXECUTABLE=/usr/bin/python3`（L5492 教训）。
   ② `-j4` 出现 FAST_LIO 等 `fast_lio/Pose6D.h` 消息生成抢跑（L8.2 已知），串行通过
   消息生成阶段后按用户确认改回全核心并行完成，全程 0 错误。
3. 重建后验证：`rospack find uav_vision` 在仅 source 三层 setup 后可达；
   `devel/lib/uav_mission/random_field_spawner.py` relay 生成。
4. **sim_run.sh 隐患（待板端组确认修复）**：smoke1 中 roslaunch 早期失败、
   `gate_status.json` 从未生成，sim_run.sh 仍 exit 0（缺 gate 文件被当作成功）。
   建议补一条"gate 文件缺失视为 FAIL"的守卫。本轮 Gate 证据一律以
   run 目录内 gate_status.json 实际存在且 PASS 为准。

### 坐标系 bug（本轮最重要的算法侧发现）

1. `random_red_cross_spawner.py` 与新 `random_field_spawner.py` 把 `search_region`
   （mission_frame 局部坐标）的采样值**直接当 Gazebo 世界坐标**用于 SpawnModel。
   smoke2 实测标定（4 个已发现靶的地图点 vs 摆放真值差值）：`local = world +
   (0.493, 1.773)`，即世界 = 局部 + 起飞 H 世界位姿（toudi4 主 H
   `(-0.493412,-1.77269)`，yaw≈0 纯平移）。
2. 后果：随机靶标整体北偏约 1.8 m——panzer 实际落在局部 (−0.43, 6.71)，超出航线
   北端（5.773）约 1 m；飞机对准该点后陷入低空受困，后续候选全部
   `approach_unreachable_20s` 且 `return_home_unreachable`（Fast-Planner 全程
   `No Effective Points` 6260 次，北排覆盖点 9/11 被跳过）。
3. **此发现同时重解释 8-14 的"覆盖航线掠飞擦边"结论**：当时十字真值（世界系）与
   serpentine 行（局部系）是跨坐标系比较，1.77 m 坐标系差是主因；发现门控双确认
   偏严仍是独立存在的次要因素。
4. 修法：两个 spawner 均新增 `~spawn_offset/x|y`（默认 0 保持旧行为），采样在局部系、
   净空检查与生成在世界系，真值文件同时记录局部/世界双坐标；两个 launch 显式传入
   toudi4 主 H 世界位姿。`red_cross_truth.yaml` 的 x/y 语义仍为世界坐标并加注释。

### 验证结果（smoke2 → smoke3c 四轮）

- smoke2（seed=1，修复前）：世界加载、摆放 5/5、双真值落盘、覆盖 10/12（北排 2 点
  `planner_unreachable_20s`）、四类标准靶全部发现（panzer/bridge/pillbox/tent）、
  r2026 下 tank 假阳性零出现、0 碰撞 0 越界；投递阶段 panzer
  `alignment_timeout_90s`（与 V-CL-04 tank 同签名）后级联全不可达，0 投递，Gate FAIL。
- smoke3：启动期 `landing_detector.yaml` "invalid YAML" 一次性报错，文件完好且
  正确环境下 `rosparam.load_file` 正常，判定为环境抖动未复现。
- smoke3b：PX4 `simulator_mavlink` 等待 Gazebo 连接 TCP 4560 超时（L3173 已知启动
  竞态），飞机不 spawn/无 odom，停跑重试即恢复。
- smoke3c（seed=1，修复后）：**坐标系修复验证成立**——同 seed 下 5 靶局部坐标与
  航线带一致（panzer local=(-1.031,4.941) → world=(-1.524,3.168)），覆盖 11/12、
  0 跳过（对比修复前同阶段已跳 2 点）、四类标准靶全部发现、0 碰撞 0 越界；
  sim t=206 s MAVROS `HEARTBEAT timed out` + 时间跳变（机器负载下 lockstep 崩溃，
  当时负载 4.91）致任务冻结，按外置口径记录后停止本轮。
- 红十字（local (0.938,3.544)，距 3.773 行 0.23 m）仍未被发现——与"双确认门控偏严 +
  抵近确认未实现"的既有结论一致，为下一实现项。

### 遗留问题

- 完整三投闭环未在随机场地取得：投递阶段受 Fast-Planner 低空下降/接近不可达
  （planner 侧，已有 V-CL-04 tank 与本轮 panzer 两次同签名证据）与 SITL 传输层
  竞态（4560 连接、心跳超时）双重阻碍；建议安静机器窗口重跑。
- 红十字"疑似→抵近确认"链路未实现，Gate 的 red_cross_required 仍会 FAIL。
- 动态期望断言与对准失败口径缺口未收敛（V-CL-04 移交项）。

### 下一步

- 安静窗口重跑 `coverage_random_field.launch`（seed=1）争取完整三投+落地证据；
  实现红十字抵近确认；与导航组对齐 Fast-Planner 低空下降问题的处置计划。


## 2026-08-29（红十字未发现根因闭环：识别正常、几何中心正常、确认识别门过严——已放宽）

### 改动范围

`vision_ws/src/uav_vision/config/target_memory.yaml`（cross_geometry_confidence
0.85→0.75）与 `scripts/target_memory.py`（同名参数代码默认值同步）。仅阈值调整，
未改任何检测/融合/记忆算法逻辑。

### 根因链（smoke3c 全证据闭环，回答“红十字是没有识别出来过吗”）

1. **识别正常**：YOLO 对在线相机帧离线复跑 conf=0.956/0.964（peak 帧
   `cross_frames/peak_00140.7.png`：白板黑圈红十字清晰无遮挡）；debug_image 显示
   在线 YOLO 同样触发（red_cross 标签 + 中心标记），drop_aligner 连像素偏移
   （Offset -206.7,-152.6）都算了。
2. **几何中心正常**：cross_debug 显示 cross_detector 同帧触发（轮廓 + 中心点，
   Score 0.770 / Solidity 0.770 / Area 2211）。
3. **可见时长充足**：对全 bag 做红色像素扫描，十字累计入画约 14 s
   （t=126-127、136.7-149.0 连续 12 s、176.7-183.9），坐标系修复后不存在
   “入画仅 1 s”问题。
4. **卡点在确认识别门**：`target_memory._pass_threshold` 要求 red_cross
   class ≥0.70 且 geometry ≥0.85 同帧同时满足；在线几何分数 0.770 < 0.85，
   12 s 可见期一次都未放行 → 无候选 → 无发现。注意 `/yolo_detect` 的
   "Class:X" 日志流按设计只含标准靶类别（detect_compat_bridge 明确“不混入
   cross/circle/landing”），不能作为红十字识别证据。
5. 8-14 结论修订：“入画仅约 1 s”是坐标系 bug 的伴生症状（已修复）；真正
   长期存在的独立缺陷是几何分数门槛过严。

### 修法与原则

按“YOLO 是识别权威、几何通道职责是中心精修”的原则放宽：
`cross_geometry_confidence 0.85 → 0.75`（yaml 生效值 + 代码默认值，注释含实测
证据）。geometry_verified 已含形状校验（contour/solidity 等几何判定），分数
门槛只防退化几何；0.75 高于 cross_detector 自身合格线、低于在线实测 0.770，
误报风险可忽略（还需 YOLO class ≥0.70 同帧共证）。后续可用参数继续调，不动代码。

### 验证结果

- `target_memory.py` 语法通过；yaml 解析正常且生效值 0.75。
- 阈值证据链完整：离线复跑（merged_standard 模型、同一帧）conf 0.956/0.964、
  在线几何 0.770、双通道同帧触发均有帧级证据。
- 未做实跑：随机场地完整闭环（含红十字发现→中断→drop_cross 投递）留待下一次
  headless 验证。

### 遗留问题

- 红十字“疑似→抵近确认”主动链路仍未实现（本轮十字是航线直接看见的被动发现）；
  若未来随机摆位把十字放在航线带边缘，仍需抵近确认兜底。
- Fast-Planner 低空下降/接近不可达与 SITL 传输层竞态仍为完整闭环的主要阻碍
  （导航组/环境侧）。

### 下一步

- 安静窗口重跑随机场地（seed=1）：预期红十字被被动发现后以权重 10 触发中断、
  走 drop_cross 对准投递；据实跑结果决定是否还需要抵近确认链路。


## 2026-08-29（red_cross 确认阈值配比再平衡：class 0.80 / geometry 0.70 + 安静重跑）

### 改动范围

`vision_ws/src/uav_vision/config/target_memory.yaml` 与
`scripts/target_memory.py` 的同名参数（上一条目把 cross_geometry_confidence
放宽到 0.75；本条按用户裁定改为“类别从严、几何从宽”的最终配比）。

### 具体改动

red_cross 双确认配比调整为 `cross_class_confidence 0.70 → 0.80`、
`cross_geometry_confidence 0.75 → 0.70`：识别权威在 YOLO（在线同帧 0.89-0.96），
类别门槛从严压误报仍有裕量；几何通道职责是中心精修（geometry_verified 已含
形状校验），分数门槛只防退化几何，与标准靶 std_geometry_confidence 一致取 0.70。

### 验证结果

py 语法与 yaml 解析通过（生效值 0.80/0.70）。安静窗口 headless 重跑
（seed=1）另起验证，见后续条目。

### 遗留问题

同上一条目（Fast-Planner 低空下降、SITL 传输层竞态、抵近确认链路）。

### 安静窗口重跑结果补充（smoke4，seed=1，03:21 起飞，负载 0.18）

- 覆盖 7/12、**5 个目标 `planner_unreachable_20s` 被跳过**（rows 1/2/3/4/5 都有）；
  四类标准靶全部发现；红十字所在 row 3 的目标 6 被跳过，十字本轮未获有效入画，
  阈值修复未得到在飞检验。
- 投递：**panzer 槽 1 投递成功**（随机场地首个完整候选链证据：随机摆位发现→
  接近→对准→guarded ACK）；bridge/pillbox `approach_unreachable_20s`；tent
  `capture_timeout_no_circle`；任务 598 s 超时、返航不可达，0 碰撞 0 越界，
  Gate FAIL（manager_return_home_unreachable）。
- 结论：视觉链四类发现与单候选完整投递链在随机场地成立；**Fast-Planner 可达性
  间歇失效已累计四轮证据（v3、V-CL-04 rerun2 的 tank、smoke2 级联、smoke4），
  是完整闭环的唯一主阻碍**，归导航组处置。red_cross 阈值修复的在飞检验待
  规划器正常轮次。


## 2026-08-29（V-CL-05 全随机场进展同步至导航正式联调分支）

### 改动范围

在独立 worktree 的 `feat/navigation-mission-visual-delivery@2ccd8a0` 上，以
`--no-ff` 合入 `feat/vcl05-full-random-deploy@93d5c5c`。本次只同步已验证的
V-CL-05 类目 profile、随机场资产/脚本、断言与红十字阈值进展；未修改导航组上游
`target_search_manager_py.py`、搜索策略、正式视觉投递 adapter 或真实执行机构。

### 具体改动

1. 完整保留导航正式 manager/adapter/assertion 和固定场联调入口，同时纳入
   `random_field_spawner.py`、`coverage_random_field.launch`、随机场配置及
   `toudi3_random.world`；`CMakeLists.txt` 同时安装两条链所需脚本。
2. 共享联调记录的追加冲突采用保留双方内容处理：保留 2026-08-26/27 导航接入记录，
   并串接 2026-08-28/29 V-CL-04 收尾与 V-CL-05 随机场实跑记录。
3. `VISION_2026_ROADMAP.md` 同时保留 V-CL-04/05 最新状态与 V-CL-06
   `接口已接入/Gate FAIL` 状态；不把随机场单投证据误写成正式导航三投 Gate 通过。

### 验证结果

- `git diff --check` 与 staged diff 检查通过，无冲突标记或空白错误。
- `uav_mission` 四组纯策略/接口契约单测共 44/44 PASS。
- `navigation_search_delivery_toudi4.launch`、`coverage_random_field.launch`、
  `coverage_r6.launch` 与 `toudi3_random.world` 均通过 `xmllint --noout`。
- 对合并文件清单定向核对：导航上游 manager 与正式 adapter 无内容差异；未启动
  ROS/Gazebo/PX4，也未执行任何实机动作。

### 遗留问题

- V-CL-06 仍缺正式导航 manager + 全随机场入口，三投、返航、落地 Gate 保持 FAIL。
- 随机场 footprint 净空、field-ready 启动屏障与本届 profile 的严格失败语义仍需在
  后续独立提交实现，不能通过修改导航上游 manager 绕过。

### 下一步

- 基于本同步点新增正式随机场导航 launch 和外围 ready/profile 门控，再按固定 seed
  执行短预检与联合 smoke；导航队列、重试和剩余时间调度缺口继续回流导航组权威仓。


## 2026-08-29（V-CL-06 随机场生成与任务候选严格准入）

### 改动范围

`uav_mission` 随机场生成策略、`random_field_spawner.py`、类目 profile、
`profile_candidate_selector.py`、随机场配置及对应纯策略单测。未修改导航组上游
`target_search_manager_py.py`，未引入候选队列、失败重试或时间调度。

### 具体改动

1. 未知 `class_profile` 由静默回退 `full` 改为启动失败；`r2026` 固定只允许
   `tent/pillbox/bridge/panzer/red_cross`，保留 `full` 的 tank 资产回归入口。
2. 新增无状态 profile selector：候选必须为当前 `CONFIRMED` 状态、连续命中达到
   3 帧、地图/坐标系/关联/年龄/有限值/边界均有效且无拒绝原因；随机场 READY 前
   不向正式导航 manager 发布，输出保持原始 `TargetCandidate` 的时间语义。
3. 随机场 seed 必须为固定正整数；按标准板半对角线 0.707 m、红十字半对角线
   0.247 m 做 footprint、场界、两两净空检查。显式加入 ModelStates 无法展开的
   4 棵 nested tree exclusion，模型根目录改为参数/`GAZEBO_MODEL_PATH`。
4. 全部模型 spawn 后再次通过 `/gazebo/model_states` 核对模型名与平面位姿，随后
   原子写入真值；只有这两步均成功才持续发布 latched JSON `READY`，失败持续发布
   `FAIL`。旧 V-CL-05 随机场入口默认 seed 改为 11，仍保留作回归。

### 验证结果

- `test_coverage_policy.py` 35/35 PASS，覆盖未知 profile、严格 streak/profile、
  REJECTED/EXPIRED、非有限/越界/高目标点及完整 footprint 几何。
- 本提交未启动 ROS/Gazebo/PX4；READY 的在线 ModelStates 复核留待正式 launch
  30 秒预检。

### 遗留问题

- selector、正式 manager 与 adapter 的话题重映射、field/anchor 双 READY 屏障及
  正式 Gate 尚待下一提交接线。
- 飞机最大高度必须由 adapter 的 MAVROS pose 统计；selector 的目标地图点上限不能
  代替飞行高度 Gate。

### 下一步

- 增加可切换规划 anchor profile，并让 adapter 在双 READY、起飞和地图就绪前只缓存
  raw goal；随后新增正式随机场 launch 与专用断言。


## 2026-08-29（V-CL-06 正式导航随机场链与规划特征 A/B 入口）

### 改动范围

新增正式随机场导航 launch、规划 anchor profile/spawner、正式随机场 Gate；扩展外围
`navigation_visual_delivery_adapter.py` 的启动屏障、profile 再校验、事件和安全遥测。
导航组上游 `target_search_manager_py.py` 保持未改，临时 coverage manager 未进入正式链。

### 具体改动

1. 新增 `navigation_search_delivery_random_field.launch`：正式 manager 的 planner goal
   重映射为 `/navigation/goal_raw`，adapter 是唯一 `/fastplanner/goal` 发布者；manager
   与 adapter 同时把原视觉 selected remap 到严格的
   `/mission/profile_selected_target`，避免任一侧旁路 profile。
2. 修复 raw-goal 启动竞态：route 与 candidate raw goal 分开缓存；双 READY、起飞和地图
   就绪后必先转发 route，只有 adapter 已处于 SEARCH 且 candidate raw goal 能在 0.6 m
   内绑定新鲜 profile 候选时才允许实际 `SEARCH→APPROACH`。上游 PoseStamped 不含 ID，
   状态明确记录 `target_id_binding=spatial_inference`。
3. 新增 `nav_feature_profile=baseline|a68925d`。baseline 不生成特征且保持默认；候选
   profile 参数化生成导航提交 `a68925d` 的场外 6 棵 Oak tree 与 3 座 radio tower，
   支持 `model.sdf`、`model.config` 指定文件及 `<model_name>.sdf`，ModelStates 复核后
   持续发布 latched READY，并在状态中标注外部高大特征依赖。
4. adapter 状态追加 class/profile/source、field/anchor READY、当前候选、单调事件序号、
   实际 command 前后状态、profile selection、最大飞行高度、最小点云净空和碰撞计数；
   候选再检查当前 CONFIRMED/streak/年龄/地图/边界，近场 evidence 只接受 CONFIRMED。
5. 新正式 Gate 累计 confirm→profile selected→实际 APPROACH 同 ID 事件，检查三不同 ID
   槽位 1/2/3、返航/降落、r2026 下 tank selected/accepted=0、双 READY、唯一 goal
   发布者、0 碰撞/越界、最大高度不超过 4 m、事件序号和必需产物；任何缺项写 FAIL。

### 验证结果

- `uav_mission` 纯策略/接口测试 51/51 PASS；新增覆盖 SEARCH→APPROACH 事件、anchor
  profile、radio_tower 非标准 SDF 文件解析及正式 launch 的双 selected remap/无临时
  coverage manager。
- 新增/修改 Python 通过 `py_compile`，正式 launch 通过 `xmllint --noout`，
  `git diff --check` 通过。
- 未启动 ROS/Gazebo/PX4，未执行 30 秒预检、90 秒 A/B 或 600 秒 Gate。

### 遗留问题

- 正式 manager 仍只有上游的单目标即时接受语义，没有持久权重队列、失败重试或总时限
  调度；若 Gate 因此 FAIL，按同 seed 日志回流导航组，不在 adapter 复制策略。
- `a68925d` 仍只是候选 profile，未通过同 seed A/B，默认保持 baseline。

### 下一步

- 先用 `SIM_REQUIRE_GATE=1` 做 fixed seed=11 的 30 秒资产/话题/READY/Gate 预检；再按
  baseline→a68925d 顺序做 90 秒固定路线 A/B，只有候选满足验收条件才进入 600 秒联合
  Gate。


## 2026-08-29（V-CL-06 运行入口与正式 Gate 收口）

### 改动范围

正式随机场 launch 的模型根/profile 透传、adapter 与 Gate 终态一致性、统一
`sim_run.sh` Gate 强制开关和 `uav_mission` 运行说明。旧固定场入口保持默认 `full` 且不
要求随机场/anchor READY，避免本轮正式链增强破坏历史回归。

### 具体改动

1. 正式 launch 新增参数化 `px4_model_root` 与 `astra_model_root`（本机默认分别指向
   PX4 Gazebo models 和 Astra `simulation/astra_gazebo_models`），并把环境
   `GAZEBO_MODEL_PATH` 作为追加来源；干净 shell 不再因随机靶模型根为空而启动失败。
2. `class_profile=r2026` 经 guarded wrapper 和 patrol visual wrapper 透传到 Phase-D
   target memory 参数位；旧入口默认 `full`。tank 仍保留在 `/targets` 供误检诊断，
   实际不得 selected/accepted 的约束继续由正式 selector、adapter 和 Gate 执行。
3. Gate 的 confirmed 事实补齐 `last_seen<=0.5 s`，新增任务时间 `<=600 s`；adapter
   终态改为先原子落盘 `target_search_status.json` 再发布 latched 状态，消除 assertion
   抢先检查必需产物的竞态。近场 capture evidence 同样要求当前连续 3 帧确认。
4. `sim_run.sh` 新增兼容开关 `SIM_REQUIRE_GATE`（默认 0）：设为 1 时缺少
   `gate_status.json` 必定返回 1；文件不可解析或状态非 PASS 仍返回 1。正式 README
   命令固定使用该开关、seed=11、r2026、baseline 和 debug-off。

### 验证结果

- `uav_mission` 单测 52/52 PASS；4 个相关 launch 均通过 `xmllint`，正式 launch 在补齐
  PX4 ROS package path 后通过 `roslaunch --files` 全量展开。
- `uav_vision` 定向 catkin 编译通过；`uav_mission` 在 worktree 视觉 overlay 与既有
  `patrol_control` overlay 上定向 catkin 编译通过。
- 无仿真命令 `/bin/true` 验证：`SIM_REQUIRE_GATE=1` 且 Gate 缺失返回 1；默认兼容
  `SIM_REQUIRE_GATE=0` 返回 0。两轮均经统一 run 目录，未启动 ROS/Gazebo/PX4。
- Python `py_compile`、`git diff --check` 通过；导航上游 manager 仍与同步点无差异。

### 遗留问题

- 本轮没有做 30 秒在线 READY 预检、90 秒 A/B 或 600 秒任务实跑，因此正式 Gate 状态
  仍为待验证，不能声明 V-CL-06 PASS。
- Phase-D 内部 profile-aware selected 收紧将由 V-SIM-04 分支统一合入；当前正式链已经
  通过外围 selector 达到 r2026 任务准入，且不删除 tank 诊断事实。

### 下一步

- 合入 V-SIM-04 视觉评测提交后先做 seed=11 baseline 的 30 秒预检；若 READY/唯一发布者/
  profile/Gate 产物均正确，再排队做 90 秒 A/B，最后才运行 600 秒三投硬 Gate。

## 2026-08-29（V-CL-06 随机场节点安装态导入修复）

### 改动范围

`uav_mission` 随机场生成节点的 catkin 安装态 Python 模块解析。

### 具体改动

1. `random_field_spawner.py` 在导入同目录 `coverage_policy.py` 前，显式把源脚本目录
   加入 `sys.path`，兼容 catkin 生成的 relay wrapper 从 `devel/lib` 执行的情况。
2. 新增静态回归测试，确保路径准备一定早于同级策略模块导入。

### 验证结果

- `uav_mission` 策略单测通过；Python `py_compile` 与定向 catkin 编译通过。
- `git diff --check` 通过；未启动 ROS/Gazebo/PX4，未执行任何实机动作。

### 遗留问题

- 本修复仅消除节点安装态启动前的模块搜索风险，不替代 30 秒在线 READY 预检。

### 下一步

- 保持 baseline 与 seed=11，在统一 `sim_run.sh` 入口中执行首轮 30 秒正式链预检。

## 2026-08-29（V-CL-06 候选接收屏障与任务时钟修复）

### 改动范围

正式 profile selector、导航外围 adapter、随机场硬 Gate 和对应纯策略/接线测试；导航上游
`target_search_manager_py.py` 保持未改。

### 具体改动

1. adapter 状态新增持续心跳、`operational_ready` 与 `candidate_accepting`；只有双 READY、
   起飞、pose/control/map 当前有效且 adapter 已处于 `SEARCH` 时才允许候选中断。
2. selector 订阅 adapter 的 latched 状态并默认 fail-closed；field READY 先到、anchor/map/
   起飞未就绪或 adapter 非 SEARCH 时均不发布，消除上游 manager 提前切 APPROACH 的竞态。
3. selector、adapter 与 Gate 统一要求 `last_seen` 和置信度/地图质量为有限值，年龄严格满足
   `0 <= age <= 0.5 s`，拒绝未来时间戳；近场证据使用同样的非负年龄语义。
4. 上游 manager 没有失败结果回流接口，APPROACH stall 不再伪装成恢复搜索，而是明确以
   `upstream_manager_approach_unreachable_no_result_interface` 结束并留存状态。
5. 任务计时从最早 raw goal 或离地 pose 开始，不再等首个搜索航点；状态同时记录 ROS 时间
   与 monotonic 墙钟，硬 Gate 分别要求二者不超过 600 s。

### 验证结果

- `uav_mission` 定向纯策略/接线测试 44/44 PASS，新增乱序 READY、未来时间戳、非有限质量、
  APPROACH fail-closed 和正式 launch 门控断言。
- 相关 Python `py_compile`、正式 launch `xmllint --noout`、`git diff --check` 均通过。
- 未启动 ROS/Gazebo/PX4，未执行任何实机动作。

### 遗留问题

- adapter/selector 心跳与实际 ROS 回调顺序仍需 30 秒预检确认；导航上游持久队列、失败结果
  接口、冷却重试和剩余时间调度仍归导航组实现。
- 随机场复合墙 footprint、Gazebo contact 事实和 A/B runner 在后续独立提交处理。

### 下一步

- 补齐实际障碍几何与 contact monitor 后执行静态测试，再新增 30 秒预检和 90 秒 A/B 入口。

## 2026-08-29（V-CL-06 随机场实际净空与 Gazebo 接触事实）

### 改动范围

随机场 footprint 配置/生成器、仿真专用单下视机架、Gazebo contact monitor、正式 adapter/
Gate、ROS 依赖与对应测试；未改导航上游 manager，未触碰实机执行链。

### 具体改动

1. 随机场按模型名支持保守 footprint 半径；4 个 `1.2 x 0.8 m` 大箱体使用不小于实际
   半对角线的 `0.722 m`，不再套用低估的通用 `0.55 m`。
2. `toudi2` 的子 link 不出现在 ModelStates，故把 Wall_15 四段碰撞盒变换到 world 后以
   保守外接圆显式纳入 exclusion；READY 状态记录实际使用的模型半径和复合排除项。
3. 仿真机架增加固定于 `iris::base_link` 的 0.30 m 半径桨保护接触包络和
   `libgazebo_ros_bumper.so` contact sensor，仅发布仿真接触事实，不访问飞控或执行机构。
4. 新增 `gazebo_contact_monitor.py`：过滤正常 ground contact，按接触开始沿去重计数，持续
   发布 latched JSON 并原子写 `gazebo_contact_status.json`。正式 adapter 在 contact 流有效前
   保持 operational false。
5. 正式 Gate 要求 contact monitor 存在、采样新鲜且 actual collision=0；原点云中心净空
   改名为 `pointcloud_*` 近距诊断，不再冒充实际碰撞。Gate 同时监听原始与 profile-filtered
   selected，任一路出现 tank 均失败。

### 验证结果

- `uav_mission` 定向测试 48/48 PASS，覆盖大箱体半径、Wall_15 排除、contact 去重/ground
  过滤、仿真 SDF 插件、依赖与 Gate 事实来源。
- 新增/修改 Python 通过 `py_compile`；正式 launch、机架 SDF 与 package XML 通过
  `xmllint --noout`；`git diff --check` 通过。
- `uav_mission` 在当前 vision overlay 上定向 `catkin_make -j1` 编译通过，新增 contact
  monitor 已生成 devel-space wrapper。
- 未启动 ROS/Gazebo/PX4，因此 bumper 空状态流、实际接触事件和对飞行模型的影响仍待短预检。

### 遗留问题

- 当前主分支 target memory 尚未消费 `class_profile`；原始 selected 的 tank=0 要等
  V-SIM-04 profile-aware 改动合入后才能通过硬 Gate。
- Wall_15 已按实际碰撞盒保守覆盖；若后续 world 修改复合墙，必须同步更新配置或升级为
  world SDF 自动解析，不能继续依赖 ModelStates 父模型中心。

### 下一步

- 用 30 秒预检验证 contact 话题、CameraInfo/TF/READY 与唯一发布者，再执行固定路线 A/B。

## 2026-08-29（V-CL-06 30 秒预检与 90 秒导航特征 A/B 工具）

### 改动范围

正式随机场 launch 的验证模式开关、30 秒基础设施预检、90 秒固定路线观测/比较、统一
`sim_run.sh` 编排入口、`uav_mission` 测试与 README，以及 Roadmap/导航 handoff。导航上游
`target_search_manager_py.py` 保持未改，默认导航特征 profile 仍为 baseline。

### 具体改动

1. 正式 launch 增加独立的 candidate selector 与 600 秒硬 Gate 开关，并参数化相机图像、
   CameraInfo、mission frame 和 optical frame；正常正式入口两者仍默认开启，预检/A-B 才显式
   关闭候选中断和长 Gate。
2. 新增 `navigation_random_field_preflight.py/.launch`：资产全部 READY 后连续运行 30 秒，
   检查同 seed 的随机真值与 target footprint、Gazebo 实际模型 pose、anchor、模型/world、
   CameraInfo 内参、camera optical 到 mission frame TF、图像/地图/pose/ModelStates 新鲜度、
   contact monitor、必要话题 publisher、raw/planner goal 唯一所有权、场界和最大高度；不以
   短窗内路线尚未到点判失败。
3. 新增 `navigation_ab_observer.py/.launch`：禁用 selector 后执行导航组固定搜索路线 90 秒，
   从首个有效 pose 累计场界/最大高度，记录 pose 最大断流、实际 goal/到达顺序、规划响应与
   超时失败、replan、高度漂移、实际接触、ROS 时间和 monotonic 墙钟；循环不用 ROS Rate，
   `/clock` 暂停时仍能按墙钟超时。
4. 新增纯比较策略与命令：只比较同 seed/world/真值/模型/路线的 baseline 与 a68925d；双方
   必须资产有效、0 实际碰撞/越界、有限 pose、最大高度不超过 4 m、发布者唯一，候选还须让
   规划失败或高度漂移至少一项改善，且共同路线进度墙钟退化不超过 10%。缺失/非有限指标
   fail-closed，报告中的 `promote_candidate` 不自动修改配置。
5. 新增 `top_level_scripts/run_navigation_ab.sh`，严格按 baseline 预检/90 秒、a68925d 预检/
   90 秒、比较器顺序调用 `sim_run.sh`，每步强制 `SIM_REQUIRE_GATE=1` 且 debug/录屏关闭。
   本机无 `rosrun`，纯比较器改由统一 run 环境中的系统 Python 直接调用，不依赖 ROS master。
6. README、Roadmap 和导航 handoff 明确两个来源 revision、验证口径、工具已落地但未实跑、
   baseline 不自动切换，以及队列/重试/剩余时间调度仍回流导航组。

### 验证结果

- `uav_mission` 纯策略/接线测试 65/65 PASS，新增 A/B 通过/拒绝、30/90 launch 模式、
  CameraInfo/TF/footprint/发布者/场界口径和 runner 不自动推广断言。
- 新增/修改 Python 通过 `py_compile`；三个相关 launch 通过 `xmllint --noout`，runner 通过
  `bash -n`，`git diff --check` 通过。
- 在当前 worktree 的 vision overlay 上定向 `catkin_make --force-cmake -j1` 编译通过；正式、
  30 秒预检和 90 秒 A/B 三个 launch 均通过 `roslaunch --files` 全量展开；比较器 `--help`
  通过系统 Python 与 devel-space 包导入。
- 未启动 ROS master/Gazebo/PX4，未执行 30 秒、90 秒或 600 秒仿真，也未执行任何实机动作。

### 遗留问题

- 30 秒预检尚未验证 bumper 空状态流、实际 CameraInfo/TF/地图 frame 和 READY 心跳；90 秒
  指标尚无 baseline/a68925d 实际样本，不能据此推广候选 profile。
- 600 秒硬 Gate 仍可能因上游 manager 缺持久队列、失败结果/重试和时间调度而 FAIL；保持
  同 seed 失败事实回流，不在 adapter 补第二套策略。
- 当前 V-CL 分支的原始 `/uav_vision/selected_target` profile 收紧依赖 V-SIM-04 合入；合入
  前硬 Gate 的 raw tank=0 断言仍可能暴露该缺口。

### 下一步

- 合入 V-SIM-04 后设置正式模型路径，按统一 runner 先执行 seed=11 baseline/a68925d 的
  30 秒预检与 90 秒固定路线 A/B；只有比较报告 PASS 才另提默认 profile 变更，随后排队做
  600 秒三投硬 Gate。
## 2026-08-29（目标选择当前准入与比赛类目 profile）

### 改动范围

在 `feat/vsim04-stability` 分支修改 `uav_vision` 的目标选择策略、dev/board 纯视觉入口及
对应确定性回归；未修改 ROS 消息、导航 manager、旧控制、规划器或执行机构。

### 具体改动

1. 保留候选长期 `CONFIRMED` 记忆状态，但 `selected_target` 额外要求当前连续命中达到
   `confirm_frames`、地图和实例关联有效、拒绝原因为空、观测年龄不超过门限。
2. 新增参数化 `class_profile=full|r2026`：`full` 保留六分类资产和 tank 选择兼容；
   `r2026` 允许 tent/pillbox/bridge/panzer/red_cross，tank 仍进入诊断候选记忆但不能成为
   `selected_target`；未知 profile 直接抛错并使节点非零退出。
3. 把准入逻辑收敛为无 ROS 副作用的 Python 策略模块，增加 profile、sticky 状态、连续帧、
   地图、关联、拒绝原因和年龄的确定性断言；同步修正 freshness/physical mock 的合法关联字段。
4. `phase_d`、`phase_d_board` 和两套控制组纯视觉 handoff launch 均暴露 `class_profile`，
   默认 `full` 保持旧入口兼容。

### 验证结果

- 纯策略断言经源码入口和 Catkin devel wrapper 各运行一次，均 PASS；未知 profile、
  full/r2026 对 tank 的差异和五类当前准入失败分支均有覆盖。
- `uav_vision` 与 `uav_vision_eval` 分别定向 `catkin_make -j2` 通过。
- `target_memory_freshness_mock.launch` 与 `target_memory_physical_mock.launch` 均出现 PASS marker；
  四个修改 launch 通过 `xmllint --noout`，相关 Python 通过 `py_compile`，`git diff --check` 通过。

### 遗留问题

- 本提交只建立视觉候选选择边界；导航任务接受、失败重试和时间调度仍归导航组 manager，
  不在 adapter 或 target memory 中复制。
- 未运行完整 Gazebo 飞行或实机；V-SIM-04 单会话 trial 和阶段性能产物在下一独立提交实现。

### 下一步

- 用固定 seed=11 的 15 个静态 + 8 个动态 trial 建立 P_confirm/P_selected、耗时和地图无效率
  评测框架，并在 visual-only 报告中明确 `P_interrupt=null`。


## 2026-08-29（V-SIM-04 单会话阶段性能最小框架）

### 改动范围

在 `feat/vsim04-stability` 分支新增 `uav_vision_eval` 的 V-SIM-04 固定矩阵、纯指标核心、
dry-run、ROS recorder、单 Gazebo 会话 runner 和 visual-only launch；同步更新 CMake、包依赖、
README 与唯一 Roadmap。未修改导航 manager、旧控制、规划器、PX4/MAVROS 或执行机构。

### 具体改动

1. 固定 `seed=11`、`class_profile=r2026`：静态
   `tent/pillbox/bridge/panzer/red_cross × {1.2,2.4,3.6} m` 共 15 trial，动态
   `panzer/red_cross × {1.8,3.0} m × {0.5,1.5} m/s` 共 8 trial；tank 资产不删除，
   仅不进入本轮矩阵。
2. `P_confirm` 按目标完整入画至离开窗口内的当前完整候选准入评分；`P_selected` 必须关联
   同一 stable ID；visual-only `P_interrupt` 固定为 `null`，禁止用 selected 冒充导航接受。
3. 确认耗时拆为图像源时间 `confirmation_exposure_sec` 与 monotonic 墙钟
   `confirmation_processing_ms`；地图无效率、全机会 map unavailable、TF 失败率和地图误差
   分开报告，避免只对 map-valid 幸存样本统计。
4. 每次输出 `manifest.json/frames.csv/events.csv/summary.json/report.md/`
   `vision_search_performance.csv`；manifest 记录 profile、模型、运行时阈值、CameraInfo、外参
   profile、视觉/导航 revision 和实际图像 FPS。空模型路径在视觉运行时直接失败，避免浪费
   整轮矩阵时间。
5. 单会话 runner 只通过 Gazebo `set_model_state` 移动评测相机，每个 trial 调用 memory reset
   并留出 Phase-D 兜底回调余量；launch 节点清单不含 PX4、MAVROS、控制、规划和执行机构。
6. 修正上一提交的状态判定：当前准入严格要求 `state == CONFIRMED(2)`，明确拒绝
   `REJECTED(3)` 与 `EXPIRED(4)`，并增加两条确定性断言。

### 验证结果

- 源码与 Catkin devel wrapper 下的 target-selection、V-SIM-04 matrix/artifact assertion 均
  PASS；断言确认矩阵为 15+8、无 tank、六产物齐全、dry-run 为 `0/23` 且
  `P_interrupt=null`，并验证 map-invalid/map-unavailable/TF-failure 聚合公式。
- `uav_vision`、`uav_vision_eval` 分别定向 `catkin_make -j2` 通过；新增 Python 全部通过
  `py_compile`，五个相关 launch 通过 `xmllint --noout`，`git diff --check` 通过。
- `roslaunch --nodes` 成功展开 V-SIM-04 的 Gazebo/真值/视觉/recorder/runner 节点，未出现
  PX4、MAVROS、旧控制或 actuator；`--dump-params` 确认 `r2026`、confirm=3、age=0.5 s 和
  `require_model_path=true`。
- dry-run 证据位于 `/tmp/uav_vision_eval/vsim04_readme_dry_run_20260829/`，六个文件均生成，
  报告明确写明不是 Gate PASS。

### 遗留问题

- 本轮没有占用 Gazebo 完成 23 trial，尚无实际 P_confirm/P_selected、地图误差或 FPS 数值；
  dry-run 不能当作 V-SIM-04 Gate 通过，也未执行 30-seed/10 min。
- 本 worktree 从 main 建立时仍带旧 red_cross 阈值 0.70/0.85，本提交没有修改这两行；后续
  合入 VCL05/VCL06 时必须保留联调分支已验证的 class=0.80、geometry=0.70。recorder 会从
  运行时参数服务器快照最终阈值，禁止用本分支旧 YAML 覆盖。
- `P_interrupt` 仍需 V-CL-06 中由导航 adapter 的同 ID `SEARCH→APPROACH` 接受事件评分。

### 下一步

- 在独占仿真窗口显式提供模型路径和两个仓 revision，经 `sim_run.sh` 跑完 seed11 的 23 trial；
  按失败高度/速度/类别决定是否扩展 30-seed，失败时只用同 seed 最小 debug 窗口复现。


## 2026-08-29（V-SIM-04 正式实跑握手与终态收紧）

### 改动范围

在 `feat/vsim04-stability` 分支收紧 `uav_vision` 当前候选准入和 `uav_vision_eval` 的
V-SIM-04 recorder/runner/产物契约；未修改导航 manager、旧控制、规划器、PX4/MAVROS、
执行机构或实机入口。

### 具体改动

1. `TargetMemory` 把历史融合地图记忆与当前观测投影有效性分开：stable ID、融合点和
   `CONFIRMED` 可保留，但消息 `map_valid` 只代表当前帧；selected 同时 fail-closed 要求
   `0 <= age <= 0.5 s`、置信度/地图质量/三维坐标有限且 map frame 非空，未来时间戳和
   NaN/Inf 不得发布。
2. recorder/runner 新增 latched JSON READY/RUNNING/FAIL/FINALIZED 握手。开跑前要求有效
   CameraInfo、连续图像、完整真值、mapped/targets 心跳、detector `/perf` 为 OK 且 backend/
   模型路径一致、mapped `completed_sources` 含 target/circle/cross 三个正式分支；trial 内
   任一恢复性 heartbeat gap 也写入 events/manifest 并使终态 INVALID。
3. trial 使用源 stamp 窗口和 monotonic receipt 双时钟缓存 truth/detections/targets/selected，
   支持 detections-before-truth、selected-before-targets 和 image-after-candidate；trial_end
   等 image/truth/mapped/targets/perf watermark 越过离场帧并完成真正 quiet drain 后才 ACK。
4. runner 的 trial handshake 严格大于 recorder drain+quiet+status period+write margin，四项
   均由同一 launch 参数传入并在运行时复核；Gazebo set-model/reset 服务使用 daemon worker
   monotonic deadline，ROS 时间等待同时有停钟 watchdog 和总墙钟上限，abort 等 recorder
   写盘状态 ACK 后再退出。
5. 终态固定要求 23/23 completed、每项真实 enter+leave、CameraInfo/FPS/轨迹和六产物完整，
   manifest 的模型、world、场景、内嵌 target model/anchor catalog、相机 RPY、外参来源、
   TF fallback/age、优先级和视觉/导航 git revision 均须为实值；失败终态明确发布 FAIL，
   visual-only `P_interrupt` 始终为 null，不能用 selected 冒充导航 `SEARCH→APPROACH`。
6. 产物按锁内快照、锁外串行 I/O 写入，并使用进程级唯一临时文件原子替换；summary/report
   同时报告 map-invalid、map-unavailable、TF failure、地图误差、exposure/processing 分母、
   receipt/source 双 FPS 和 receipt reorder 样本。五类靶共存明确为 clutter，本轮 FP=false。
7. 正式 launch 默认把六产物写入 `$SIM_RUN_DIR/vsim04`，由 `sim_run.sh` 统一归档；未设置
   `SIM_RUN_DIR` 时才回退 `/tmp/vsim04`。

### 验证结果

- target selection policy、physical target memory、freshness 和 V-SIM-04 matrix/schema 断言
  均 PASS；覆盖未来/NaN/Inf/空 frame、当前 map-invalid 粘性记忆、23 固定计数、enter/leave、
  selected/targets 回调反序、image receipt 反序、watermark、detector diagnostic、完整 sources、
  service deadline 与 handshake 边界。
- `uav_vision`、`uav_vision_eval` 分别定向 `catkin_make -j2` 通过；修改 Python 通过
  `py_compile`，V-SIM-04 launch 通过 `xmllint` 与 `roslaunch --nodes` 展开，节点清单不含
  PX4/MAVROS/旧控制/执行机构。`--dump-params` 确认 recorder drain=10.0 s、quiet=0.25 s、
  status period=0.25 s，runner handshake=16.0 s，且 `$SIM_RUN_DIR` 正确展开。
- dry-run 六产物位于 `/tmp/vsim04_dry_run_final.i4rVIs/`，状态为 `DRY_RUN`、0/23，明确不是
  MEASURED 或 Gate PASS；`git diff --check` 通过。

### 遗留问题

- 本提交未运行完整 Gazebo 23-trial，因此没有实际 P_confirm/P_selected、地图误差、动态速度
  或 FPS 结论；不得把 dry-run 或 mock 写成 V-SIM-04 MEASURED。
- 本分支仍不实现导航持久队列、失败重试、时间调度或 P_interrupt；后者只在 V-CL-06 联调中
  由 adapter 的实际同 ID `SEARCH→APPROACH` 接受事件评分。
- 合入 VCL05/VCL06 时仍须保留联调分支已验证的 red_cross class=0.80、geometry=0.70，避免
  本 worktree 的旧基线 YAML 覆盖。

### 下一步

- 在独占仿真窗口通过 `sim_run.sh`、真实模型路径和两个仓 revision 跑完 seed=11 的 23 trial；
  只有 recorder 最终 latched `FINALIZED/MEASURED` 且六产物齐全才进入结果分析。


## 2026-08-29（V-SIM-04 首轮实跑基础设施竞态修复）

### 改动范围

修复 `feat/vsim04-stability` 首轮 23-trial 实跑暴露的 detection fusion 并发竞态和评测器
完整分支判定；同步收紧统一仿真入口的 V-SIM-04 终态检查。未修改模型、trial 矩阵、导航
manager、旧控制、规划器或执行机构。

### 具体改动

1. 首轮证据 `logs/vsim04_seed11_20260829_190453/vsim04/summary.json` 为 `INVALID`：23 个
   trial 均完成，但 `detection_fusion` 的 Subscriber/Timer 并发遍历和修改同一
   `OrderedDict`，触发 `OrderedDict mutated during iteration`；该轮 P_confirm/P_selected
   诊断值均为 0.4348，不作为有效量化结论。
2. fusion 使用同一 `RLock` 原子保护 buckets/flushed keys 的判定、遍历和所有权转移，锁外
   publish，并快照转移时的 align mode；新增确定性并发断言覆盖迭代冲突、同桶单次发布、
   锁外发布和 mode 快照。
3. target detector 采用 `queue_size=1`，实际 14.4 FPS 低于图像源 17.8 FPS，部分帧只有
   circle/cross 等几何分支属于预期抽帧。recorder 不再把每个 partial bucket 直接记为 fatal，
   仅 complete bucket 推进 mapped heartbeat/watermark；partial 数量和最近 sources 写入
   manifest，完整链路超过 2 s 无心跳仍然硬失败。
4. `sim_run.sh` 对 `vsim04*` 场景强制核对六项产物及 `summary.status=MEASURED`，修复
   required runner 失败后 roslaunch 正常关停返回 0 导致统一入口误报成功的问题。

### 验证结果

- detection fusion 并发断言连续 10 次 PASS；V-SIM-04 matrix/schema 与 profile/current
  admission 断言 PASS。
- `target_memory_physical_mock.launch` 和 `target_memory_freshness_mock.launch` 均干净退出，
  freshness 明确报告 PASS；`uav_vision`、`uav_vision_eval` 分别定向 catkin 编译通过。
- 修改 Python 通过 `py_compile`，`sim_run.sh` 通过 `bash -n`，V-SIM-04 launch 节点展开
  正常且不含 PX4/MAVROS/旧控制/执行机构，`git diff --check` 通过。

### 遗留问题

- 本条只登记首轮无效运行及基础设施修复；修复后的同 seed=11 复跑尚未完成，V-SIM-04
  仍不得标为 MEASURED。
- 首轮观察到的高空漏检、地图无效率和动态表现需等有效复跑后再分析，不能与基础设施
  INVALID 混为模型结论。

### 下一步

- 以本提交 revision 复跑完全相同的 seed=11、23-trial 矩阵；只有统一入口返回 0、summary
  为 MEASURED 且六项产物齐全时，才把数值写入 Roadmap 并进入 VCL06 合入。


## 2026-08-29（V-SIM-04 seed11 最小矩阵有效量化）

### 改动范围

以视觉 revision `28c9221f0fab1fa9ab43992a736e979e43c7b271`、导航/地图 revision
`a68925d15293e5510e2b4351c6b3d9bc5aa136ab` 和固定模型
`vision_ws/runs/liftrace_6cls_v5_merged_standard_20260714/weights/best.pt` 复跑 V-SIM-04
seed=11 最小矩阵；更新 Roadmap 和评测入口说明。未修改算法阈值、模型、矩阵、导航 manager
或任何实机链路。

### 具体改动

1. 统一入口 `sim_run.sh` 完整运行单 Gazebo 会话 23 trial，证据目录为
   `logs/vsim04_seed11_20260829_192515/`；六项规定产物全部存在，summary 为 `MEASURED`，
   terminal validation 无错误、infrastructure gaps 为空，入口退出码为 0。
2. 有效结果：P_confirm=P_selected=`9/23=0.3913`，visual-only P_interrupt=`null`；确认
   processing P95=`184.4 ms`，exposure 中位/P95=`0.183/0.399 s`，图像接收/源 FPS=
   `15.90/16.70`。
3. 地图结果：map-invalid=`177/672=26.34%`，map-unavailable=`428/923=46.37%`，TF failure=
   `0/672`，地图误差 mean/P95=`0.0233/0.0777 m`（n=495）。partial fusion bucket 770 帧被
   正确记录但未推进完整 heartbeat；最近 complete bucket 含 target/circle/cross。
4. trial 分解显示：tent/pillbox/bridge/panzer 在 1.2/2.4 m 静态均确认；全部 3.6 m 静态、
   全部 red_cross 均未确认；动态仅 panzer 1.8 m、0.5 m/s 确认，其余 7 项失败。

### 验证结果

- recorder 明确报告 `finalized MEASURED (23/23)`，`sim_run.sh` 二次核对后报告
  `V-SIM-04 status: MEASURED` 和 `Main cmd exited: 0`。
- run.log 未出现 Traceback、OrderedDict 异常或 INVALID；manifest 记录有效 CameraInfo、外参、
  模型、阈值、两个 revision、实际 FPS 和空 infrastructure gaps。
- `report.md`、`summary.json` 与 `vision_search_performance.csv` 的总数和 trial 分解一致。

### 遗留问题

- `MEASURED` 只证明矩阵与产物有效，不代表算法 Gate PASS；9/23 的当前召回不足以直接扩展
  30-seed 或宣称搜索稳定。
- red_cross 全失败、3.6 m 全失败和较快动态失败需要按 detector/geometry/map-current 准入阶段
  分解；P_interrupt 仍只能由 V-CL-06 的 adapter 实际 `SEARCH→APPROACH` 事件评分。

### 下一步

- 将本 V-SIM-04 分支以 `--no-ff` 合入 VCL06 正式联调分支，保留 VCL05 已验证的 red_cross
  class=0.80/geometry=0.70；随后执行导航 baseline/a68925d 的同 seed 30 s + 90 s A/B。


## 2026-08-29（V-SIM-04 合入 V-CL-06 正式联调分支）

### 改动范围

在 `feat/navigation-mission-visual-delivery` 以 `--no-ff` 合入
`feat/vsim04-stability@0deeb1e`；保留 VCL05/VCL06 正式随机场链、导航 A/B 工具和全部追加型
记录，同时纳入当前候选严格准入、V-SIM-04 单会话评测与有效 seed11 量化结果。未合并 main，
未修改导航组上游 manager，未启动任何实机链路。

### 具体改动

1. Roadmap 保留 V-CL-06 编号 16 与 Gate FAIL 状态，并把 V-SIM-04 顺延为编号 18，记录
   23/23 MEASURED 及 9/23 当前性能；V-EXP/V-REAL/V-DEPLOY 编号与 V-CL-06 同时保留。
2. 共享变更记录按追加语义保留双方全部条目；README 日期统一为 2026-08-29。
3. 自动合并保留 VCL 已验证的 red_cross `class=0.80/geometry=0.70`，r2026 只准入
   tent/pillbox/bridge/panzer/red_cross，tank 资产和 full 回归入口不删除。
4. 修复自动合并在 `phase_d.launch` 产生的重复 `class_profile` arg；V-SIM-04 的 fusion 并发
   修复、partial/complete heartbeat 语义和 `sim_run.sh` MEASURED 二次检查一并纳入。
5. 导航 Handoff 新增 V-SIM-04 视觉能力边界，明确 P_interrupt 只能来自 adapter 的实际
   SEARCH→APPROACH，视觉性能缺口与导航队列/重试/调度职责不混淆。

### 验证结果

- `git diff --check` 与 conflict marker 检查通过；target memory 运行参数确认 red_cross
  `class=0.80/geometry=0.70`，导航上游 `target_search_manager_py.py` staged diff 为空。
- `uav_mission` 65/65 单测通过；profile/current-admission、fusion 并发和 V-SIM matrix/schema
  断言均 PASS，physical/freshness 两个 target-memory ROS mock 干净退出。
- `uav_vision`、`uav_vision_eval` 和 `uav_mission` 分别定向 Catkin 编译通过；修改 Python
  通过 `py_compile`，`sim_run.sh` 通过 `bash -n`。
- phase_d/dev/board、V-SIM-04、正式随机场、30 s 预检和 90 s A/B launch 均通过 XML；
  formal/preflight/A-B/V-SIM 四个入口成功展开，正式入口不含临时 coverage manager。

### 遗留问题

- 导航 30 s baseline/a68925d 预检和 90 s 固定路线 A/B 尚未运行，默认必须保持 baseline。
- V-SIM-04 的 red_cross/高空/动态召回仍未达标；导航上游仍无持久队列、失败结果、重试或
  剩余时间调度接口，600 s Gate 保持 FAIL。

### 下一步

- 完成合并态静态验证后提交；随后用统一 A/B 脚本按 baseline 30/90 → a68925d 30/90 顺序
  实跑，只有候选安全且满足改善/退化阈值才允许改默认并进入 600 s Gate。


## 2026-08-29（V-SIM-04 合并态连续确认与 reset 隔离修复）

### 改动范围

修复 `feat/navigation-mission-visual-delivery` 合并态复核发现的目标连续确认误清零、
TargetMemory reset 并发和 detection fusion 安装态断言失效；未修改模型、置信度阈值、
trial 矩阵、导航 manager、规划器、旧控制或执行机构。

### 具体改动

1. 按 align mode 定义正式检测分支集合。融合输出已携带 `completed_sources` 时，缺少任一必要
   分支的 timeout bucket 只保留诊断，不再推进或清零候选连续帧；Phase D、板端与物理 mock
   要求元数据必须存在，旧直连输入在字段为空时保持兼容。
2. TargetMemory 用同一 `RLock` 串行化 detection、align-mode 与 reset 对候选表的访问；reset
   建立源时间戳 cutoff，拒绝在服务调用前已排队或无时间戳的旧帧，避免 trial 间候选复活。
3. 物理身份回归新增 aux-only partial 不清 streak、并发发布/reset 及最终 cutoff 隔离断言；
   纯策略断言新增 profile 对应分支完整性和 reset 时间戳边界。
4. 将 `DetectionFusion` 实现移入可安装的 `uav_vision` Python 包，ROS 入口改为薄包装，断言
   不再依赖同目录脚本导入；同时让空 buckets 状态下也清理过期 `flushed_keys`，避免长时运行
   的已发布键集合持续增长。

### 验证结果

- `uav_vision` 定向 Catkin 编译和 install 均通过；源码、devel relay、install-space 三种入口
  的 fusion 并发断言与 profile/current-admission 断言均 PASS。
- `target_memory_physical_mock.launch` 干净退出并报告 PASS，覆盖不完整帧不发布、不清连续计数、
  detection/reset 并发以及 reset 后旧帧不复活；`git diff --check` 和 Python 语法检查通过。

### 遗留问题

- 已有 `logs/vsim04_seed11_20260829_192515/` 使用的是合并前 red_cross class/geometry
  `0.70/0.85`，只能作为旧配置基线，不能代表当前集成阈值 `0.80/0.70`；本提交尚未产生新的
  23-trial 量化结果。
- 本修复不补导航持久队列、失败重试、剩余时间调度或真实 `P_interrupt`，V-CL-06 仍保持 FAIL。

### 下一步

- 先提交本修复，使 manifest 能记录确定 revision；再以相同 seed=11、模型和 23-trial 矩阵
  重跑当前合并态，随后执行 baseline/a68925d 的 30 s 预检与 90 s A/B。


## 2026-08-29（检测来源严格门槛的模式与旧链兼容收口）

### 改动范围

补齐上一提交的初始 align mode、Phase C 兼容与 ROS time=0 reset 边界；同步加强 freshness 与
物理身份回归。未调整模型、类别/几何阈值、导航接口或仿真矩阵。

### 具体改动

1. TargetMemory 从 `default_align_mode` 初始化，Phase D 与板端 launch 将同一参数同时传给
   fusion 和 memory，避免首次 `/align_mode` 消息到达前按错误的 required sources 判定。
2. `require_complete_detection_sources=false` 明确为完全旁路，保留只有 circle/cross 的旧
   Phase C；正式 Phase D/板端保持 true，继续拒绝带缺失分支的 partial bucket。
3. 未 reset 状态改用 `None` 表示，使 ROS time=0 执行过 reset 与“尚未 reset”可区分；reset
   之后零时间戳或不晚于 cutoff 的消息一律拒绝。
4. 回归新增 drop-circle/未知模式、strict=false 旁路、time=0 reset、确定旧 stamp 先生成再
   reset 后发布的断言；freshness mock 改为严格来源元数据，并继续验证完整空帧会使当前地图
   无效而长期 CONFIRMED 记忆仍保留。

### 验证结果

- `uav_vision;uav_vision_eval` 定向 Catkin 编译通过；策略断言、physical memory 和 freshness
  ROS mock 均 PASS，Python 语法、launch XML 与 `git diff --check` 通过。
- `roslaunch --dump-params` 证明 Phase D 的 fusion/memory 均为 drop_cross、板端均为 landing，
  两者 strict=true；Phase C strict=false。

### 遗留问题

- 当前修改发生在 `3606413` 的 seed11 实跑之后，属于模式初始化与旧链兼容修订；该实跑的
  `disabled/r2026` 数据流未受这些分支影响，但 manifest revision 仍应如实保留为 `3606413`。

### 下一步

- 独立提交本兼容修复；随后只更新 `3606413` 实跑的量化结论，不改写 manifest revision，
  再进入导航特征地图 30 s/90 s A/B。


## 2026-08-29（V-SIM-04 当前合并阈值 seed11 复跑）

### 改动范围

以视觉 revision `36064135c7eda302cbfa667bdd50565f3a3095ec`、导航/地图 revision
`a68925d15293e5510e2b4351c6b3d9bc5aa136ab`、r2026 profile 和固定 seed=11 重跑 V-SIM-04
23-trial 最小矩阵；更新 Roadmap、评测 README 与跨组 Handoff。未调整模型或阈值。

### 具体改动

1. 统一入口证据为 `logs/vsim04_seed11_current_20260829_195506/`，23/23 完成，六项产物齐全，
   `summary.status=MEASURED`、terminal validation PASS、infrastructure gaps 为空，退出码 0。
2. 当前结果 P_confirm=P_selected=`13/23=0.5652`，visual-only P_interrupt=`null`；P95 processing
   `123.0 ms`，exposure 中位/P95=`0.178/0.430 s`，图像接收/源 FPS=`15.64/16.56`。
3. map-invalid=`168/681=24.67%`，map-unavailable=`294/807=36.43%`，TF failure=`0/681`，地图
   误差 mean/P95=`0.0335/0.0789 m`（n=513）；partial mapped frame 315 帧被记录但不再清零
   TargetMemory 连续计数。
4. 相对合并前旧阈值/旧 partial 语义的 9/23 增加 4 项：red_cross 通过静态 2.4 m 与动态
   3.0 m 两档，动态 panzer 1.8 m 两档均通过。四类标准靶 3.6 m、panzer 动态 3.0 m、
   red_cross 静态 1.2/3.6 m 与动态 1.8 m 仍失败。

### 验证结果

- manifest 明确记录 profile、模型、CameraInfo、外参、当前 `0.80/0.70` red_cross 阈值、两个
  revision 和实际 FPS；run.log 无 Traceback、fusion 竞态或 INVALID。
- 失败 red_cross trial 仍分别有 13 至 45 个有效检测/地图帧，但 current_confirmed 始终为 0，
  说明剩余问题优先位于双路重叠/完整帧连续性，而不是 TF 或地图投影完全不可用。

### 遗留问题

- `MEASURED` 不是算法 Gate PASS；10/23 仍未确认，当前不扩 30-seed/10 min。
- P_interrupt 仍须在 V-CL-06 中由 adapter 实际 SEARCH→APPROACH 事件产生；视觉结果不能替代。

### 下一步

- 进入导航 `baseline|a68925d` 同 seed 的 30 s 预检与 90 s 固定路线 A/B；视觉侧另开最小
  red_cross 连续性重放，不在本轮放宽地图、profile 或 selected 当前性门槛。


## 2026-08-29（随机场完整布局确定性重采样）

### 改动范围

修复 seed=11 正式随机场预检在顺序贪心摆放时可能留下不可完成布局的问题；不改变场地边界、
模型 footprint、目标类别/profile、导航 manager 或 adapter 策略。

### 具体改动

1. 新增纯策略 `plan_footprint_layout`：每次先在内存中规划全部 4 个标准靶和红十字；任一目标
   在采样上限内无合法位置时，丢弃本轮完整布局并从同一确定性随机流继续重试。
2. spawner 只有在完整布局通过边界、障碍物和目标间 footprint 净空校验后才写入 Gazebo，
   避免失败时留下部分模型；新增可配置且写入状态的 `spawn/layout_attempts=64`。
3. 单测用可控随机序列复现“第一轮顺序布局死路、第二轮完整布局成功”，同时保留固定 seed
   与既有 footprint/profile/正式 launch 契约回归。

### 验证结果

- `test_coverage_policy.py` 54 项全部 PASS，新增重采样用例通过；两个修改脚本 Python 语法检查
  与 `git diff --check` 通过。
- 主集成工作区执行全量 `catkin_make -DROS_EDITION=ROS1 -DCATKIN_WHITELIST_PACKAGES='' -j1`
  成功到 100%，补齐此前仅定向编译导致的 manager、patrol_control 消息和 Fast-Planner 可执行文件。
- 首次 baseline 预检 `logs/vcl06_preflight_seed11_baseline_20260829_200651/` 保持 FAIL 证据：
  一项原因为 bridge 在 4000 次顺序采样后无安全位姿，另一项为当时主工作区未完成全量编译；
  该结果不用于评价 baseline 或 `a68925d` 地图。

### 遗留问题

- 修复后尚需用同 world/seed/model/profile 重跑 baseline 30 s 预检；预检通过前不进入 90 s A/B。
- 本改动不解决导航持久队列、失败重试和剩余时间调度，也不放宽任何安全 Gate。

### 下一步

- 独立提交本修复，重跑 seed=11 baseline 预检；若结构化 Gate PASS，再按顺序执行
  baseline 与 `a68925d` 的 90 s 固定路线 A/B。


## 2026-08-29（随机靶区净空可行性修正）

### 改动范围

修正完整 footprint 校验启用后，原中央随机靶区在既有树、箱体和墙体约束下不存在四标准靶
安全布局的问题；只调整随机摆放与对应覆盖路线的横向边界，不改 world、障碍物、靶标资产、
15 cm 净空、profile 或导航接口。

### 具体改动

1. `coverage_toudi3_random.yaml` 的 search region 从 world x=`[-2.5,1.5]` 横向各扩 0.5 m
   至 `[-3.0,2.0]`（mission x=`[-2.507,2.493]`）；新边界仍完整位于 field 内，北部走廊
   继续不纳入本阶段。
2. 新增 seed=11 可行性回归：使用 world 中四个箱体、起飞位车辆、四棵复合树、四段复合墙
   与实际 footprint/净空参数，断言能够确定性规划 4 个标准靶加红十字的完整布局。

### 验证结果

- 修复前 baseline retry `logs/vcl06_preflight_seed11_baseline_retry_20260829_202707/` 的地图、
  CameraInfo/TF、图像、pose、单 planner-goal 发布者和正式 manager/raw-goal 发布者均已有效；
  唯一启动链根因是原区域 64 次完整重采样仍无可行布局，Gate 按 `startup_timeout` 保持 FAIL。
- 离散可行性检查表明原区域在保守圆 footprint 下最多放置 3 个标准靶；旧 smoke 中可放置 4 个
  是因为当时未检查树/箱体/复合墙完整 footprint，已有真值可见靶面与树重叠，不能作为安全证据。
- `test_coverage_policy.py` 55 项全部 PASS（含 seed=11 完整布局）；`git diff --check` 通过。

### 遗留问题

- 扩展后的区域尚需再次通过真实 Gazebo 30 s baseline 预检；其结果通过前仍不运行 90 s A/B。
- 横向扩大 1 m 会增加覆盖航线跨度，A/B 必须对两种 profile 使用完全相同的新路线和靶位。

### 下一步

- 提交边界与回归后重跑相同 world/seed/model/profile 的 baseline 预检；仅在结构化 Gate PASS
  后进入有序的 baseline→`a68925d` 90 s A/B。


## 2026-08-29（按树模型碰撞尺寸恢复原随机靶区）

### 改动范围

复核上一提交的“扩大随机靶区”假设后，改为修正四棵 juniper tree 的过度保守 footprint，
并恢复原投递区与覆盖路线语义；不修改 target、箱体、复合墙、world 或 15 cm 额外净空。

### 具体改动

1. PX4 `juniper_Tree` 的 DAE node scale=`0.0254`、SDF XY scale=`0.2`，网格最大平面碰撞
   半径约 0.3174 m；静态排除半径从通用 0.55 m 改为 0.40 m，仍保留约 8.3 cm 模型余量，
   footprint 校验还会在此之外叠加 0.15 m pair gap。
2. 撤回上一提交临时扩大的 search region，恢复 mission x=`[-2.007,1.993]`（world
   x=`[-2.5,1.5]`），避免无必要地改变正式覆盖路线宽度和投递区语义。
3. 可行性回归从固定 seed=11 扩为 seed=1..30，继续使用四箱体、起飞位车辆、四树和四段
   复合墙的实际世界坐标及各自 footprint，要求每个 seed 都生成 4 标准靶加红十字完整布局。

### 验证结果

- `test_coverage_policy.py` 55 项全部 PASS，其中 seed=1..30 完整布局回归连续通过；
  Python 语法与 `git diff --check` 通过。
- 离线对照确认：原边界+树半径 0.55 m 时 seed=11 无解；按模型尺寸采用 0.40 m 后相同
  seed/profile/边界可解，无需增加重采样区域或削减 15 cm 净空。

### 遗留问题

- 仍需在真实 Gazebo 预检中确认 seed=11 的 READY、真值落盘和 ModelStates footprint 断言；
  离线 30-seed 只证明布局策略可行，不替代联合 Gate。

### 下一步

- 独立提交 footprint 修正；另行修复 baseline 空 anchor profile 的启动竞态，再重跑 30 s 预检。


## 2026-08-29（baseline 空锚点 profile 启动竞态修复）

### 改动范围

修复导航地图 A/B 的 baseline profile 在没有任何外部 anchor 时仍等待 Gazebo ModelStates，
并在 Gazebo 冷启动超过 30 s 时错误进入 FAIL 的竞态；不改变 `a68925d` 候选 profile 的模型
解析、spawn 或位姿核验。

### 具体改动

1. baseline 的 anchors 列表为空时，`_spawn` 直接返回空期望集，`_verify` 对空集立即成功；
   profile 校验、来源 revision 与 READY 状态仍照常发布。
2. 非空候选 profile 仍必须等待 `/gazebo/spawn_sdf_model`、逐一载入 9 个模型，并在
   `/gazebo/model_states` 中完成位置容差校验后才能 READY。
3. 新增回归断言，禁止空 profile 再调用 `_model_states`，同时确认 spawn/verify 两层均有
   fail-fast 空集分支。

### 验证结果

- 上一轮 `logs/vcl06_preflight_seed11_baseline_retry2_20260829_203940/` 中随机场已 READY、
  5 靶模型已验证且真值落盘；地图、相机、TF、图像、pose 和话题所有权也有效。Gate FAIL
  由 baseline anchor 在 Gazebo 冷启动阶段超时导致，adapter 未进入 SEARCH 是其级联结果。
- `test_coverage_policy.py` 56 项全部 PASS；脚本 Python 语法与 `git diff --check` 通过。

### 遗留问题

- 修改后仍需第三次运行同一 seed=11 baseline 30 s 预检，以结构化 Gate 证明竞态已消除。
- 候选 `a68925d` 的 9 个外部特征必须在候选预检中实际 spawn/verify，本修复不对其短路。

### 下一步

- 独立提交启动竞态修复并重跑 baseline 预检；PASS 后才启动 90 s 有序 A/B。


## 2026-08-29（预检 readiness 掉线归因明细）

### 改动范围

为 30 s 导航随机场预检补充运行期 readiness 掉线的逐断言计数与状态转换时间线；不改变
任何 PASS 条件、允许抖动次数、话题新鲜度阈值或导航/视觉运行逻辑。

### 具体改动

1. Gate 从测量开始后对每次 failed check 分别累计 `dropout_check_counts`，并在失败断言集合
   变化时记录 monotonic wall/ROS elapsed 与 `failed_checks`，最多保留 64 条状态转换。
2. 同一循环只计算一次 checks，确保决定 ready 与记录失败原因使用完全相同的快照；恢复全绿
   后清除上一个失败签名，以便再次掉线时保留新事件。
3. 报告继续要求 `no_readiness_dropout=true`，本改动只让下一次 FAIL 可直接归因，避免反复
   完整重启再猜测是 adapter、TF、地图、话题所有权还是 artifact 抖动。

### 验证结果

- 第三轮 `logs/vcl06_preflight_seed11_baseline_retry3_20260829_204948/` 已证明 field/anchor、
  5 靶真值与 footprint、地图/图像/CameraInfo/TF/pose、正式 manager/adapter 话题所有权、
  0 碰撞/0 越界和 30 s measurement 均通过；唯一失败项为 67 次 readiness dropout。
- `test_coverage_policy.py` 56 项全部 PASS；预检脚本 Python 语法与 `git diff --check` 通过。

### 遗留问题

- 上一份报告生成于明细字段加入前，无法反推 67 次掉线对应的具体 check；必须以同 seed
  再跑一次 30 s 预检获得确定归因。

### 下一步

- 独立提交纯诊断增强并重跑 baseline；若掉线属于真实地图/adapter readiness，则保持 FAIL
  并按责任边界修复或回流，不能删除 `no_readiness_dropout` 硬条件。


## 2026-08-29（预检流新鲜度与publisher缺口展开）

### 改动范围

继续细化 30 s 预检的结构化失败报告，把聚合的 live-stream/publisher 失败展开为具体流年龄与
缺失话题；不改变启动超时、消息新鲜度、必需话题集合或 PASS 条件。

### 具体改动

1. 报告九路输入的 monotonic wall age，并显式记录普通流 1 s、field/anchor/map 2 s 的现行
   阈值，使相机、pose、map、ModelStates、barrier、contact、adapter 可逐项归因。
2. 报告所有必需话题到 ROS publisher 节点的快照，同时单列 `missing_required_publishers`；
   `_required_publishers_present` 复用同一快照逻辑，避免诊断口径与 Gate 判定漂移。

### 验证结果

- 第四轮 `logs/vcl06_preflight_seed11_baseline_retry4_20260829_205559/` 保持结构化 FAIL：
  未进入 measurement，最终聚合阻断仅为 `live_streams_fresh=false` 与
  `required_topic_publishers=false`；field/anchor/adapter、真值、TF/CameraInfo、地图 frame、
  0 碰撞/0 越界均有效。
- `test_coverage_policy.py` 56 项全部 PASS；预检脚本 Python 语法与 `git diff --check` 通过。

### 遗留问题

- 第四轮报告产生于本明细加入前，尚不知道具体陈旧流和缺失 publisher；需一次缩短的诊断窗
  获取名称，修复后再恢复正式 30 s 时长。

### 下一步

- 先以同 seed/profile 运行 5 s measurement、120 s startup 的诊断预检；根据明细做最小修复，
  再执行正式 30 s baseline，避免每次定位都支付完整测量成本。


## 2026-08-29（latched barrier 与实时流新鲜度语义分离）

### 改动范围

修复 30 s 预检把 field/anchor 的 latched READY 当作实时流按墙钟 2 s 判陈旧的问题；保持
barrier 内容、publisher 存活、地图/图像/CameraInfo/pose/ModelStates/contact/adapter 新鲜度
以及零掉线硬条件不变。

### 具体改动

1. `live_streams_fresh` 只覆盖实际连续数据流；field 与 anchor 仍在每个循环分别执行完整
   READY/profile/seed/模型集合契约，并由 required publisher 检查其节点是否仍存活。
2. 报告显式写入 `latched_barriers_freshness_exempt=[field,anchor]` 和独立 `map_age_limit_sec=2`，
   避免以后把不可变启动屏障与连续传感器数据再次混用。

### 验证结果

- 缩短诊断窗 `logs/vcl06_preflight_diag_seed11_baseline_20260829_210137/` 精确显示 10 次掉线
  全部来自 `live_streams_fresh`；结束时仅 anchor wall age=`2.137 s` 超过旧 2 s，而图像、
  CameraInfo、pose、map、ModelStates、field、contact、adapter 均新鲜，必需 publisher 0 缺失。
- 同一报告 wall 5.06 s 仅推进 ROS 2.24 s（实时因子约 0.44），证明 ROS-time 1 s 的 latched
  heartbeat 会自然拉伸到约 2.25 s 墙钟；这不是 readiness 内容掉线。
- `test_coverage_policy.py` 56 项全部 PASS；预检脚本 Python 语法与 `git diff --check` 通过。

### 遗留问题

- 必须恢复 30 s 正式时长重跑 baseline；短诊断窗只能证明根因，不能作为 A/B 前置 PASS。

### 下一步

- 独立提交 Gate 语义修复，重跑正式 seed=11 baseline 30 s；通过后进入有序地图 A/B。


## 2026-08-29（接触监视器状态写入并发修复）

### 改动范围

修复仿真接触回调与 2 Hz 心跳同时写 `gazebo_contact_status.json.tmp` 时的竞态崩溃；不改变
碰撞对过滤、碰撞 episode 计数、0 碰撞 Gate 或真实运行链。

### 具体改动

1. `GazeboContactMonitor` 新增 `threading.RLock`，把回调中的状态更新与发布视为一个事务；
   心跳发布也使用同一把锁，避免读取一半更新的事件状态。
2. 状态 JSON 的临时文件写入、`os.replace` 与 latched ROS 发布在锁内串行执行，保留原子落盘
   语义并消除两个线程争用同名 `.tmp` 文件。
3. 回归断言要求 callback/publish 两层均持有同一 RLock，防止以后重新引入固定临时文件竞态。

### 验证结果

- 正式 run `logs/vcl06_preflight_seed11_baseline_final_20260829_210454/` 的接触监视器日志捕获
  `FileNotFoundError: gazebo_contact_status.json.tmp -> gazebo_contact_status.json`；节点在启动约
  18 s 后退出，最终 contact 流年龄 162.77 s、publisher 缺失，Gate 正确保持 FAIL。
- 同一 run 还观测到最大高度 4.237 m，超过 4 m 硬限制；本提交不掩盖该事实。接触节点修复后
  正式 30 s 窗若仍复现高度越界，必须按导航/规划问题继续保持 FAIL。
- `test_coverage_policy.py` 57 项全部 PASS；脚本 Python 语法与 `git diff --check` 通过。

### 遗留问题

- 需重跑正式 baseline 验证 contact monitor 在高频回调+心跳下持续存活，并确认 30 s 窗是否
  仍发生高度超过 4 m；两项都通过前不进入 A/B。

### 下一步

- 独立提交并发修复，再跑相同 seed/profile 的正式 30 s baseline；若只剩高度越界则回流
  导航/规划，不在 adapter 内复制高度调度策略。


## 2026-08-29（导航 A/B runner fail-fast 与目录选择修复）

### 改动范围

修复有序 A/B runner 在 preflight/sample Gate 失败后仍继续下一 profile，以及相似场景名前缀
可能选到 retry/final/diagnostic 旧目录的问题；不改 A/B 指标、阈值或 profile 默认值。

### 具体改动

1. preflight 与 90 s sample 的 `sim_run.sh` 非零退出现在由函数原样返回，使 `set -e` 在首个
   Gate FAIL 立即停止，不再用随后成功的 `latest_run_dir` 覆盖失败状态。
2. 最新 run 只匹配标准 `scene_YYYY...` 数字时间戳后缀，排除 `scene_retry_*`、`scene_final_*`
   等人工诊断变体，避免比较器读取错误 revision 或旧指标。
3. 回归断言覆盖 profile 执行顺序、fail-fast 提示、数字后缀匹配，以及 runner 不自动提交或
   改写 `planner_anchor_profiles.yaml`。

### 验证结果

- baseline 正式预检 `logs/vcl06_preflight_seed11_baseline_contactfix_20260829_211009/` PASS：
  全断言通过，30.06 s、0 readiness dropout、0 碰撞、0 越界，最大高度 3.163 m。
- 实际 A/B run 暴露旧 runner 缺口：baseline 90 s 因最大高度 4.798 m FAIL 后仍继续候选；
  修复后相同情况会立刻停止。`bash -n`、57 项策略回归和 `git diff --check` 均通过。

### 遗留问题

- 本轮候选数据已在旧 runner 继续执行后完整生成，可用于诊断比较；新的 fail-fast 行为尚未
  再跑一轮（无需为验证 shell 控制流重复支付四次 Gazebo 冷启动）。

### 下一步

- 独立提交 runner 修复；记录本轮 A/B 的候选改善与不推广结论，baseline 保持默认且不进入
  600 s Gate。


## 2026-08-29（V-CL-06 seed11 地图 A/B 结论与需求完成度回填）

### 改动范围

把正式 baseline 预检、baseline/a68925d 90 s A/B 与导航组视觉需求完成度写回唯一 Roadmap
和跨组 Handoff；不改 profile 默认值、模型、阈值、导航上游或 Gate 结论。

### 具体改动

1. Roadmap 将 V-CL-06 从“工具已落地未实跑”更新为实跑事实：baseline 30 s PASS，90 s
   baseline 高度 FAIL，候选虽安全且高度改善但耗时退化超限，比较不推广，600 s 未启动。
2. Handoff 固定五个证据目录，分别记录双方预检、双方 90 s 和比较结果；继续分开 manager
   来源 `5144aa8` 与地图实验来源 `a68925d`，并明确候选依赖 6 树+3 塔场外特征。
3. Handoff 将导航需求完成度拆为两个口径：冻结的 23-trial 首轮工程交付 100%，原始完整
   A+B 矩阵 23/125=18.4%；同步列出 C/D、多 seed、实拍/板端、实际 P_interrupt 与字段缺口。

### 验证结果

- baseline 预检 `logs/vcl06_preflight_seed11_baseline_20260829_211245/` PASS；baseline 90 s
  `logs/vcl06_ab90_seed11_baseline_20260829_211440/` 0 碰撞/0 越界/0 规划失败，但最大高度
  4.798 m、RMS 漂移 1.106 m，终态 FAIL。
- 候选预检 `logs/vcl06_preflight_seed11_a68925d_20260829_211733/` PASS；候选 90 s
  `logs/vcl06_ab90_seed11_a68925d_20260829_211952/` 最大高度 3.254 m、RMS 漂移 0.513 m，
  终态 PASS。
- 比较 `logs/vcl06_ab_compare_seed11_20260829_212259/` 验证同 seed/world/真值/模型/路线且
  高度改善，但共同首段 60.01→71.67 s、比值 1.1943，`baseline_safe=false`、
  `wall_time_regression_le_10pct=false`、`promote_candidate=false`，终态 FAIL。

### 遗留问题

- baseline 高度越过 4 m 与候选 19.43% 进度退化均归导航/规划组；baseline 仍为默认仅表示
  未满足切换合同，不表示其 90 s Gate 安全通过。
- 视觉首轮评测框架完成但性能仅 13/23；完整性能曲面、实际 interrupt、实拍与板端仍未完成。

### 下一步

- 不跑 600 s、不切换默认；把同 seed 指标回流导航组，先收敛高度与耗时，再重做安全 A/B。
  视觉侧只针对 10 个失败 trial 做最小连续性重放，随后再扩横偏/偏航/多目标矩阵。


## 2026-08-29（导航视觉双向需求文档与新相机标定口径）

### 改动范围

基于 `feat/navigation-mission-visual-delivery@d9f0bd9` 的现行接口、V-SIM-04 seed11 量化和
导航地图 A/B 实跑证据，形成视觉对导航的阶段回复与反向联调需求；不修改视觉算法、导航
上游、launch、配置或 Gate 结论。

### 具体改动

1. 新增 `docs/视觉组对导航组V-SIM-04扩展需求阶段回复_20260829.md`，先按原文十节复述导航
   需求，再逐项区分完成、部分完成和未开始；明确首轮工程 100%、算法 13/23 和完整 A+B
   23/125 是三个不同分母，visual-only 的 selected 不能替代实际 `P_interrupt`。
2. 新增 `docs/视觉组对导航组联调需求_20260829.md`，按现行
   `targets -> profile selector -> manager -> raw goal -> adapter` 链划分双方职责；把持久队列、
   显式 stable ID、任务结果、失败重试、剩余时间与高度/耗时收敛列为导航侧 P0，同时保留
   adapter 对 planner goal 的唯一所有权。
3. 在阶段回复中纠正相机标定口径：内参走棋盘格/ChArUco 图片与离线脚本，GUI 仅是可选
   采集辅助；CameraInfo 与相机安装外参分开交付，当前仿真 CameraInfo 不冒充新实机标定。
4. 两份文档同时保留 r2026 暂不准入 tank、tank 资产/权重/`full` 回归不删除的 profile
   约定，并列出需要导航组共同冻结的接口与验收决定。

### 验证结果

- 逐项复核 `视觉组需求.md`、V-SIM-04 六项产物、正式随机场 launch、manager/adapter/selector
  源码和导航 Handoff；文档中的话题所有权、stable ID 空间推断风险与完成度分母与当前实现
  一致。
- 复核 seed11 A/B 原始 JSON：baseline 最大高度 4.798 m；`a68925d` 最大高度 3.254 m，
  共同进度耗时比 1.1943，比较保持 FAIL 且 `promote_candidate=false`。
- 两份 Markdown 相对链接目标存在，`git diff --check` 通过；本轮只改文档，未重复运行 Gazebo
  或 catkin 编译。

### 遗留问题

- 反向需求中的带 ID raw decision 与生命周期事件是待双方冻结的合同，尚未由导航上游实现；
  当前 adapter 仍使用 0.6 m 空间邻近绑定 raw goal 与 stable ID。
- V-SIM-04 性能仍为 13/23，完整 A+B/C/D、联合 `P_interrupt`、30-seed、实拍和 OrangePi
  尚未完成；A/B 未通过，因此 600 s Gate 仍不启动。
- 新相机尚未拍摄标定图片，分辨率、裁剪、对焦/曝光、内参模型和安装外参仍需现场冻结与
  实测。

### 下一步

- 先把两份文档发导航组确认 profile、显式 ID、结果回流和任务队列合同；导航侧收敛高度/耗时
  的同时，视觉侧按同 seed 最小重放修复 10 个失败 trial。新相机到位后按离线图片标定流程
  生成并独立复核 CameraInfo，再接入实拍和板端 ROS 链。


## 2026-08-29（导航联调 Handoff 登记双向文档入口）

### 改动范围

在现有导航联调 Handoff 中登记本轮阶段回复和反向需求的入口，保证后续接手者能从统一
Handoff 找到组间口径；不改历史条目、源码、配置或 Gate 状态。

### 具体改动

1. 在 `docs/导航组任务链与新视觉联调HANDOFF_20260826.md` 追加“双向组间文档入口”，分别
   说明阶段回复与视觉对导航需求的用途。
2. 重申三项不可混淆的当前事实：地图 A/B 比较 FAIL、600 s 未启动、V-SIM-04 产物完整但
   算法只有 13/23，避免新文档入口被误读为 Gate PASS。

### 验证结果

- 两个相对链接目标均已纳入同一 feature 分支；`git diff --check` 通过。
- 本轮只追加共享文档条目，未运行 Gazebo 或 catkin 编译。

### 遗留问题

- 导航组尚未确认显式 stable ID、生命周期事件、持久队列和重试/时间调度合同；当前所有
  Gate 结论保持不变。

### 下一步

- 导航组按 Handoff 入口审阅两份文档并回填接口决定；确认后再拆分导航上游与视觉评测的
  独立实现提交。


## 2026-08-30（V-SIM-04 红十字双证据置信度语义修复）

### 改动范围

修复 `detection_fusion` 在红十字分类与几何双确认后丢失分类器置信度的问题，并补充确定性
回归；不降低 target memory 阈值，不改相机内参、导航上游或 Gate 结论。

### 具体改动

1. 将红十字双证据确认提取为独立融合函数：重叠成立时以几何分支拥有精修中心、几何质量
   和关联状态，以目标分类器拥有类别置信度，生成新的融合消息且不修改输入消息。
2. 保持无重叠分类/几何证据直接拒绝，避免用单路结果放宽红十字准入。
3. 扩展 `detection_fusion_concurrency_assertion.py`，覆盖置信度所有权、几何中心保留、输入
   不变和非重叠拒绝，同时保留原有并发与 mode 快照回归。

### 验证结果

- `git diff --check` 通过。
- 定向 `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision -j2` 通过。
- `detection_fusion_concurrency_assertion.py` 输出
  `detection_fusion concurrency/evidence PASS`。
- Python 语法检查和 `target_selection_policy_assertion.py` 通过。

### 遗留问题

- 现有 seed11 中 5 个红十字 trial 的历史结果不会自动重算；需同 seed 定向重放确认本修复
  对连续 3 帧确认的实际改善。
- 标准靶 3.6 m 失败主要停在圆环关联，pillbox 3.6 m 为分类器漏检，均不是本次修复范围。

### 下一步

- 补齐 raw/resolved/refined/mapped/confirmed 分阶段字段后，仅重放现有 10 个失败 trial，先
  验证红十字修复，再依据圆环阶段证据处理 3.6 m 标准靶，不盲目放宽全局阈值。


## 2026-08-30（V-SIM-04 逐帧流水线断点与首阻断归因）

### 改动范围

扩展 V-SIM-04 评测产物，使一次失败 trial 能直接定位 raw 分类、raw 几何、融合、精修、
关联、地图或记忆准入断点；不改变视觉算法阈值、trial 运动轨迹、内参或导航上游。

### 具体改动

1. Recorder 同时订阅 raw、resolved、refined、mapped 四级检测话题，在 `frames.csv` 追加各级
   是否出现、分类/几何置信度、几何验证、中心精修、关联与分级拒绝原因。
2. `vision_search_performance.csv` 为每个 trial 增加各阶段有效帧数和 `failure_stage`；失败时
   从真值可见性开始，按分类器、几何、融合、精修/关联、地图、记忆顺序报告首个阻断点。
3. `summary.json`/`report.md` 增加阶段帧覆盖率和失败阶段计数，并明确帧覆盖只用于诊断，
   不是独立概率样本。
4. 正式 launch 显式传入四级检测话题；schema 回归覆盖阶段聚合、首阻断分类和新增 CSV
   字段，同时保持 `P_interrupt=null` 的视觉侧边界。

### 验证结果

- `vsim04_metrics_assertion.py` 输出 `V-SIM-04 matrix/artifact schema PASS`。
- Python 语法检查、launch `xmllint --noout` 和 `git diff --check` 通过。
- 定向 `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j2` 通过。

### 遗留问题

- 阶段字段尚未经过新的 Gazebo 产物实测；旧 seed11 CSV 不含新增列，不能反向补齐 raw
  分支证据。
- 标准靶的 `raw_geometry_present` 表示同帧存在合格圆环几何；最终一一关联仍以 refined
  阶段的 `association_valid` 为准。

### 下一步

- 增加显式 diagnostic trial selector，只重放历史 10 个失败条件并输出 `DIAGNOSTIC`，避免
  每次算法修复都重复跑完整 23 trial；字段实测通过后再扩静态 1.8/3.0 m。


## 2026-08-30（V-SIM-04 失败条件定向复测选择器）

### 改动范围

为单 Gazebo 会话增加显式 trial 子集选择，缩短算法修复后的同 seed 定向复测；正式 23 项
Gate 口径保持不变，子集产物不得冒充 `MEASURED`。

### 具体改动

1. 新增逗号分隔 `trial_selector`：空值保持完整 23 trial 与 `full` scope，指定 ID 时按给定
   顺序选择已冻结矩阵中的 trial，并拒绝重复或未知 ID。
2. Recorder、runner 和 manifest 共用同一选择结果与动态计数；正式 full scope 仍硬性要求
   23/23，diagnostic scope 至少一项且终态固定为 `DIAGNOSTIC`。
3. Runner 的终态握手按 scope 区分 `MEASURED`/`DIAGNOSTIC`，两者都必须完成所选全部 trial
   和原有基础设施/产物校验；失败或子集缺项仍 fail closed。
4. Launch 明示“空选择=正式、非空=诊断”；报告写入 scope，并声明 dry-run 与 diagnostic
   都不是 Gate PASS。

### 验证结果

- schema 回归覆盖选择顺序、输入矩阵不变、重复/未知 ID 拒绝、2 项 diagnostic 终态及原
  23 项 `MEASURED` 终态，测试输出 PASS。
- Python 语法检查、launch XML、`git diff --check` 通过。
- 定向 `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j2` 通过。

### 遗留问题

- 尚未启动 Gazebo 验证真实 recorder/runner 握手；首次使用应先选 1 个已知成功 trial 做
  短 smoke，再重放历史 10 个失败项。
- 子集只减少重复试验，不改变每个 trial 的 settle、完整入画定义或输出排空时限。

### 下一步

- 先用选择器跑一项成功 smoke，随后同 seed 重放 5 个圆环失败、4 个红十字失败和 1 个
  pillbox 分类失败；依据新增 `failure_stage` 决定是否推广算法参数。


## 2026-08-30（高空薄蓝环形态学尺度修复）

### 改动范围

修复当前 640x512 检测输入在 3.0-3.6 m 尺度下蓝色薄环被形态学开运算清空的问题；只调整
圆环 morphology kernel 和对应回归，不改圆环质量/半径/关联阈值，不处理 pillbox 模型漏检。

### 具体改动

1. `circle_morphology_kernel_size` 从 15 调整为 7，并同步 C++ 参数缺省值；现有资产投影探针
   显示 15 px 会清空 tent/bridge/panzer 高空蓝 mask，7 px 可恢复质量约 0.81-0.89 的候选。
2. 圆环 mock 新增一个原图厚 20 px、缩放后约 10 px 的远距薄环，覆盖原测试两个厚环未能
   覆盖的高空尺度。
3. 圆环断言从两个中心扩为三个中心，并校验薄环反变换后的半径范围，防止后续再次用过大
   kernel 获得虚假的近距回归 PASS。

### 验证结果

- 新薄环回归在旧 15 px 配置下明确超时 FAIL；改为 7 px 后检出 3 个圆环并输出
  `CircleGeometryAssertion success circles=3`。
- 定向 `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision -j2` 通过。
- 全局圆环一一关联 mock 输出 `V-ALG global ring association PASS`；红十字融合、profile
  准入回归、Python 语法和 `git diff --check` 均通过。

### 遗留问题

- 5 个历史 `circle_association_missing` trial 尚未在 Gazebo 同 seed 重放，不能仅凭合成薄环
  回归宣称 V-SIM-04 已恢复。
- `static_pillbox_h3p6` 仍是 640 输入分类器边界漏检，应补高空/小目标训练数据；不因这一例
  全局降低 detector 或 memory 阈值。

### 下一步

- 通过 diagnostic selector 先重放 5 个圆环失败项和低空已成功标准靶对照；确认没有新增
  误环/错误关联后，再决定是否把 7 px 推入完整 23 trial 基线。


## 2026-08-30（统一仿真收尾区分正式与诊断评测）

### 改动范围

让统一 `sim_run.sh` 正确验收 V-SIM-04 diagnostic 子集，同时继续阻止子集状态冒充正式
`MEASURED`；不修改 trial、视觉算法或 Gazebo 配置。

### 具体改动

1. `vsim04_diag*` 场景的六产物终态必须为 `DIAGNOSTIC`。
2. 其他 `vsim04*` 场景继续严格要求 `MEASURED`；状态或六项产物任一不符仍返回非零。
3. `run.log` 同时打印实际与期望状态，便于快速识别场景命名/selector 口径错配。

### 验证结果

- `bash -n top_level_scripts/sim_run.sh` 与 `git diff --check` 通过。

### 遗留问题

- 尚待一项真实 diagnostic smoke 验证 runner、recorder 与收尾脚本三方终态一致。

### 下一步

- 以 `vsim04_diag_smoke` 场景跑一个历史成功 trial；只有六项产物完整且终态
  `DIAGNOSTIC` 后，才启动 9 项算法修复定向重放。


## 2026-08-30（导航算法要求评估与 V-SIM-04 修复后实测）

### 改动范围

复核导航组 V-SIM-04 性能曲面要求的合理性，以正式/诊断同 seed 运行验证本轮视觉算法修复，
并同步 Roadmap、阶段回复、视觉对导航需求和联调 Handoff；不修改导航上游 manager，不推进
新相机内参，不执行实机动作。

### 具体改动

1. 正式 23 项运行验证红十字双证据置信度修复、7 px 圆环 morphology 和逐帧失败归因；用
   diagnostic selector 复测 9 个受影响项，并对高速 panzer 再独立重复两次。
2. 将导航要求分为必须保留的实验合同和可压缩的执行矩阵：独立真值、stable ID、逐阶段断点
   和实际 `P_interrupt` 定义保留；A/B/C/D 与 30 seed 改为静态 25 格、每类 6 点稀疏筛选、
   横偏优先、pairwise 压力集和最终工作域 paired seeds 的序贯 DoE。
3. 明确导航扩大矩阵前需冻结航高/航速/路线、横偏/yaw/速度口径、采用阈值、capture 半径、
   可中断 SEARCH、accepted/result/retry stable ID 事件和 seed 扰动语义。
4. 更新 Roadmap、导航需求阶段回复、视觉对导航联调需求与 Handoff，保留 2026-08-29 的
   13/23 历史快照，并追加 2026-08-30 最新证据；明确 `MEASURED`、20/23 和 diagnostic 均不
   等于 Gate PASS，`P_interrupt` 在 visual-only 中仍为 `null`。
5. 远程复核 `liftrace-controlwork` 仍只有 `main@a68925d`，没有新的 branch/tag；正式 manager
   来源仍为 `5144aa8`，V-CL-06 A/B FAIL、候选 profile 不推广和 600 s 未启动的结论不变。

### 验证结果

- `logs/vsim04_diag_smoke_seed11_20260830_192043/`：1/1，六产物完整，终态 `DIAGNOSTIC`。
- `logs/vsim04_diag_algofix_seed11_20260830_192144/`：9 项中 8 项恢复，终态
  `DIAGNOSTIC`；地图误差 P95=0.0856 m。
- `logs/vsim04_seed11_algofix_20260830_192604/`：正式 23/23 和六产物完整，终态
  `MEASURED`；`P_confirm=P_selected=20/23=0.8696`，较旧 13/23 净增 7 项。
- 高速 panzer 正式运行失败一次，随后
  `vsim04_diag_panzer_fast_retry_seed11_20260830_192948` 与
  `vsim04_diag_panzer_fast_retry2_seed11_20260830_193047` 两次 diagnostic 均通过。
- 正式运行记录处理 P95=816.9 ms、地图误差 P95=0.602 m、TF failure rate=1.53%；与旧运行
  的 recorder/负载非受控一致，当前标记为完整运行性能异常，尚不直接归因本轮算法修改。
- 导航远程 `git ls-remote --heads --tags` 仅返回
  `a68925d15293e5510e2b4351c6b3d9bc5aa136ab refs/heads/main`。

### 遗留问题

- `pillbox@3.6 m` 仍阻断于 raw classifier；`red_cross@3.6 m` 阻断于 memory admission，
  strict/relaxed 几何质量尚未统一量纲且缺负样本，不通过全局降阈值消除。
- 完整长会话吞吐、partial fusion、TF 失败和动态地图误差需以相同 recorder/负载做受控复跑；
  高速 panzer 的短窗口抖动也尚未证明稳定成功概率。
- 导航仍缺持久候选队列、显式接受/结果、失败冷却重试、剩余时间调度和最终运营包络输入；
  V-CL-06 仍未进入 600 s Gate。

### 下一步

- 先做代表成功/失败条件的短会话与完整会话受控对照，定位处理 P95、partial fusion 和动态
  地图误差来源；稳定后补静态 1.8/3.0 m，再运行每类 6 点的高度×速度稀疏筛选。新相机内参
  继续暂停。


## 2026-08-30（V-SIM 相机位姿单一时间源）

### 改动范围

修复 V-SIM 动态条件下 TF 与独立真值各自读取“最新 LinkStates pose”再冒用图像时间戳造成的
评测伪差；只修改 `uav_vision_eval` 仿真基建，不改变生产 map projector、视觉阈值、导航或
相机内参。

### 具体改动

1. 新增 eval-only `gazebo_camera_pose_stamper`，把无 Header 的 Gazebo camera LinkStates 在
   单一进程中盖上当前仿真时间并发布 latched `PoseStamped`。
2. 新增线程安全的零阶保持 pose history；同 stamp 覆盖、仿真时间回退时清空旧历史，并只
   返回不晚于请求图像时间的 pose。
3. TF broadcaster 与独立真值 projector 默认共同订阅该 stamped pose，并按每张图像时间戳
   选择同一历史 pose；保留关闭开关和原 LinkStates 入口用于历史场景回退。
4. 增加历史选择、未来 pose 拒绝和仿真时间回退回归；安装新增 ROS 脚本。

### 验证结果

- Python 语法、launch XML 和 `git diff --check` 通过。
- `vsim04_metrics_assertion.py` 输出 `V-SIM-04 matrix/artifact schema PASS`，新增 pose history
  断言通过。
- `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j2` 通过。

### 遗留问题

- LinkStates 本身没有生成时间，stamper 使用轻量回调的 `/clock` 当前值；这消除了 TF/真值
  两个消费者的独立错位，但仍需动态同路线实跑量化零阶保持残差。
- 当前 map P95 异常运行还混有 detector/Gazebo 负载变化，不能仅凭本补丁宣称吞吐恢复。

### 下一步

- 在提交态用同一高速动态 selector 重跑，比较修复前约 0.98 m 的 map P95；随后再处理逐帧
  detector 时延和 complete/partial 分母。


## 2026-08-30（V-SIM 逐帧时延与完整融合分母）

### 改动范围

把“recorder 图像回调到候选回调”的传输混合时延与 detector 自身处理、同 host pipeline 时延
分开，并为 partial fusion 增加 active-trial、source-stamp 去重后的正式分母；不改变检测模型、
融合 slop、memory 门槛或试验轨迹。

### 具体改动

1. dev/sim detector 在每帧 perf 中增加 callback start/end monotonic 时间；recorder 按同一
   source stamp 记录 detector inference/processing 和候选 receipt，新增真正的同机
   `confirmation_pipeline_ms`，同时保留旧 receipt-to-receipt 指标作传输诊断。
2. `frames.csv` 增加逐帧 detector 时延字段；trial/summary/report 增加 detector P95、pipeline
   P95 及对应样本分母。
3. mapped bucket 只在 active trial 内按 `(trial_id, source_stamp)` 去重；同 stamp 后续形成完整
   bucket 时不再同时计为 partial，输出 complete/partial-only 数量、完整率和 partial source-set
   分布。旧全局 partial message count仅保留兼容诊断。
4. 增加 `enable_stage_trace`，正式默认开启；关闭时只用于受控 A/B recorder 订阅负载，并把
   未确认项首阻断显式写为 `stage_trace_disabled`，不能冒充正式阶段归因。

### 验证结果

- Python 语法、launch XML、schema 回归和 `git diff --check` 通过。
- schema 断言覆盖 same-stamp pipeline 计时、detector P95、complete=1/partial=1 的 0.5 完整率
  和 stage trace 关闭语义，输出 `V-SIM-04 matrix/artifact schema PASS`。
- `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j2` 通过。

### 遗留问题

- monotonic callback start 仅可在 recorder 与 detector 同一主机时计算；跨机部署必须使用
  单独时钟同步合同，本字段为空而不是混用 ROS 时间。
- 尚需提交态 diagnostic 验证新增 perf 字段、完整融合分母和 formal 默认 stage trace；旧日志
  不含这些字段，不能反向补齐。

### 下一步

- 对同一四到五项 selector 运行 stage trace on/off 受控 A/B；若 detector/完整率改善超过
  10%，再把逐阶段遥测改为低负载计数器，否则保留正式默认并优先处理 Gazebo/GPU竞争。


## 2026-08-30（搜索评测停用无关 H 检测器）

### 改动范围

在 V-SIM-04 搜索性能评测中停止不参与 disabled/search 准入的 landing detector，去除一条
灰度、自适应阈值和双 morphology 的全图 CPU 路径；默认 Phase D 仍启动 H 检测器，降落业务
和旧入口行为不变。

### 具体改动

1. `phase_d.launch` 新增 `start_landing_detector`，默认 `true`，并用条件 group 包裹原 H 节点。
2. `vsim04_stability.launch` 显式设为 `false`；搜索模式 fusion 的 required sources 仍为
   target/circle/cross，不改变完整 bucket、候选或 profile 语义。

### 验证结果

- 两个 launch XML 与 `git diff --check` 通过。

### 遗留问题

- 该优化只适用于没有 landing/H trial 的搜索评测；任何 H Gate 必须显式保留节点并单独量化。
- 仍需提交态对照 detector P95 和 complete mapped rate，不能仅由少一个节点推定性能改善。

### 下一步

- 在相同五项 selector 下复测，与 landing detector 仍运行的 `trace_on` 证据做同版本指标对照；
  若收益不稳定，仍以完整运行实测为准。


## 2026-08-30（V-SIM-04 运营域序贯评测切片）

### 改动范围

补齐高度、速度运营域的确定性试验设计与可重复入口，用命名诊断切片先定位边界，再保留正式
23-trial Gate 作最终验收；不改变检测阈值、模型、相机内参或导航接口。

### 具体改动

1. 新增 85 点完整运营域矩阵：5 类静态目标覆盖 5 档高度，5 类动态目标覆盖 3 档高度和
   4 档速度，继续排除 tank。
2. 在矩阵中定义 `static25` 与 `sparse30` 两个命名诊断切片；loader 校验切片非空、无重复且
   只引用已定义 trial，selector 与 slice 互斥。
3. runner/recorder/manifest 透传并记录切片名，任何命名切片均保持 `DIAGNOSTIC`，不能产出
   `MEASURED` 结论。
4. 新增 `run_vsim04_surface.sh`，只允许上述两个切片并通过统一 `sim_run.sh` 生成运行产物；
   启动前强制提供模型路径和导航 revision。

### 验证结果

- Python 语法、shell 语法、launch XML 与 `git diff --check` 通过。
- schema 回归覆盖 85 点展开、25/30 点切片、互斥和未知切片拒绝，输出
  `V-SIM-04 matrix/artifact schema PASS`。
- `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j2` 通过。

### 遗留问题

- `static25` 与 `sparse30` 是运营域诊断证据，不替代正式 23-trial Gate；尚未在本提交态实跑。
- 完整 85 点矩阵成本较高，本轮只按序运行两个切片，边界失败应先最小 selector 重放。

### 下一步

- 先整合 pillbox 失败帧采集与 red-cross 统一质量，再运行 `static25`；根据失败分布决定是否
  进入 `sparse30`，最后回到正式 23-trial Gate。


## 2026-08-30（红十字几何质量统一与负样本回归）

### 改动范围

统一 `cross_detector` strict/relaxed 入口的几何质量量纲，补齐可生成的红十字正负样本
ROS 回归；不修改 target memory 0.70 门槛、ROS 消息、`center_source`、fusion 类别置信度
归属、相机内参或导航逻辑。

### 具体改动

1. strict 与 relaxed 形状预选通过后，共同执行 64×64 归一化十字模板 IoU、水平/垂直对称性和
   归一化凹角拓扑约束，拒绝只凭 relaxed 离散得分进入几何证据链。
2. 几何置信度统一为模板 IoU、双轴对称性和 solidity 合理度的连续 `[0,1]` 加权值；
   不再让 strict 直接输出原始 solidity、relaxed 输出离散总分归一化。
3. 新增 `cross_geometry_regression` rostest，在内存中生成正常尺度和约 3.6 m/50 px 尺度
   正十字，以及红方块、圆、条、T、L、X、断裂十字和边缘裁切负样本；负样本若
   发布 `geometry_verified` 则测试硬失败。

### 验证结果

- `catkin_make --pkg uav_vision -j2` 通过。
- `rostest uav_vision cross_geometry_regression.test` 通过：正常与 3.6 m 尺度正样本质量分别为
  0.956/0.934，8 类负样本全部拒绝，无 `geometry_verified` 误发布。
- `detection_fusion_concurrency_assertion.py` 保持 PASS；Python 语法、launch XML 与 `git diff --check`
  通过。
- 本轮未启动 Gazebo，未执行任何实机动作。

### 遗留问题

- 当前正负样本是确定性合成图；真实俯视透视、阴影、反光、模糊和遮挡尚未覆盖，因此
  新的模板/对称门不应在缺少实拍回放时进一步收紧。
- 任务链中 red_cross 仍需 YOLO 同帧双证据；本改动只修复 geometry quality 量纲和负样本
  边界，不代替类别识别。

### 下一步

- 合入 V-SIM 性能分支后，先用 `static_red_cross_h3p6` diagnostic 验证 memory admission 恢复，
  再加入俯视透视/模糊/部分遮挡实拍负样本；未完成这两项前不改 0.70 门槛。


## 2026-08-30（V-SIM 小目标失败帧诊断采集）

### 改动范围

为 `static_pillbox_h3p6` 等显式选中的 V-SIM diagnostic trial 增加有界原始帧采集能力；不改变
正式 23 项运行的默认进程图、视觉模型、算法阈值、导航链或相机内参。

### 具体改动

1. 新增默认不启动的 `vsim04_failure_capture`；只有 `enable_failure_capture:=true` 且
   `trial_selector` 非空时才能运行，并由 `failure_capture_max_frames` 和独立输出目录约束写盘。
2. 原图与独立 Gazebo 真值只按 ROS header 的 `(secs,nsecs)` 精确配对；每帧保存无叠加
   lossless PNG 和同名 JSON，记录 trial、类别、高度、真值 ROI、CameraInfo、源图编码及时间戳。
3. 输出 `dataset_manifest.json` 并标记 `sim-small-target`；空 selector、非法上限、非空输出目录、
   CameraInfo 尺寸/矩阵不一致、run abort 或选中 trial 零有效配对均 fail closed。
4. 新增无 ROS master/Gazebo 的纯 mock/schema 断言，覆盖双向乱序精确配对、错 stamp 拒绝、
   歧义/无效真值拒绝、配置保护及逐帧 schema；README 补充统一 `sim_run.sh` 诊断入口。

### 验证结果

- `vsim04_failure_capture_assertion.py` 输出
  `V-SIM-04 failure capture mock/schema PASS`。
- Python 语法检查、launch XML、`git diff --check` 通过。
- 默认 `roslaunch --nodes` 不含采集器；显式 diagnostic 参数下只增加
  `/vsim04_failure_capture`。
- `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j2` 通过；既有
  `vsim04_metrics_assertion.py` 输出 `V-SIM-04 matrix/artifact schema PASS`。

### 遗留问题

- 按任务约束未运行 Gazebo，因此当前只证明采集合同、默认零负载和构建有效；尚未生成实际
  `static_pillbox_h3p6` 图像集。
- 仿真小目标数据不能替代实拍数据，也不能单独作为降低分类阈值或修改模型的依据。

### 下一步

- 由统一仿真入口对 `static_pillbox_h3p6` 运行一次最多 30 帧的 diagnostic 采集，人工检查
  ROI 覆盖与图像内容后再决定数据增强方案；新相机内参继续暂停。


## 2026-08-30（V-SIM 多 trial 采集预算与失败透传）

### 改动范围

修复失败帧采集器在多 selector 下由首项耗尽总预算、后项零样本，以及 callback 致命错误仍以
进程码 0 退出导致统一入口误报成功的问题；不改变正式运行默认节点图或视觉算法。

### 具体改动

1. 将总帧数上限按已选 trial 顺序确定性均分，余数分给靠前 trial；总预算小于 trial 数时
   启动即拒绝，保证每个已选项至少有一个保留名额。
2. image/truth callback 达到当前 trial 配额后停止写盘，把各 trial quota/count 写入 dataset
   manifest；总写盘上限保持不变。
3. callback fatal 后 `rospy.spin()` 返回时以非零进程码退出；`sim_run.sh` 识别显式采集参数并
   强制检查 capture manifest 必须为 `DIAGNOSTIC`，缺失、`FAIL` 或 `INCOMPLETE` 均返回非零。
4. mock/schema 回归增加配额均分、余数、预算不足和重复 ID 拒绝。

### 验证结果

- Python/shell 语法、launch XML、mock/schema、`git diff --check` 及
  `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j2` 通过。
- `logs/vsim04_diag_capture_quota_seed11_20260830_202715/` 为 `DIAGNOSTIC`：30 帧按
  pillbox/red_cross 各 15 帧写盘，两个 trial 均有精确 stamp 真值配对，capture manifest 无错误。
- `logs/vsim04_diag_capture_failclosed_seed11_20260830_202756/` 用两项/一帧非法预算验证硬失败：
  V-SIM 为 `INCOMPLETE`、capture manifest 缺失，统一入口退出码为 1。
- 同一成功运行中 3.6 m red_cross 完成 confirm/selected，地图误差 P95 约 0.006 m；3.6 m
  pillbox 仍明确停在 `raw_classifier`，不是几何或 TF 问题。

### 遗留问题

- 采集开启后 complete mapped rate 降至约 0.84，符合额外图像订阅和 PNG 写盘带来的诊断负载；
  因此采集运行不得用于正式吞吐或 Gate 计分。
- 当前 pillbox 15 帧仍是同一静态位姿，只适合确认尺度/域差异，不能直接训练或宣称召回改善。

### 下一步

- 提交该 fail-closed 修复后，在无采集负载下运行 `static25`；再按其失败分布选择少量
  pillbox 高度/动态 selector 采样，形成独立 held-out 的 sim-small-target 候选数据集。


## 2026-08-30（Pillbox 小目标输入尺度诊断）

### 改动范围

为 dev/sim YOLO 增加可记录、可覆盖的输入尺寸参数，用相同单 trial 对照确认 3.6 m pillbox
首阻断是否来自网络特征尺度；默认 640、板端 RKNN 输入、阈值、内参和正式 Gate 均不改变。

### 具体改动

1. `phase_d.launch` 与 V-SIM launch 显式透传 `target_detector_imgsz`，默认 640；该入口只服务
   dev/sim 诊断，不修改 `phase_d_board.launch`。
2. target detector 强制把 imgsz 解析为整数并写入 `/uav_vision/perf`；V-SIM manifest 阈值区
   同步记录实际 `/target_detector/imgsz`，避免不同输入尺度产物混淆。
3. 对 15 张精确真值 ROI 的高空 pillbox 采集帧做离线 640/960/1280 对照，并用同一模型、
   同一 trial、无采集负载分别实跑 640 与 1280。

### 验证结果

- Python 语法、两个 launch XML、V-SIM schema 和 `git diff --check` 通过。
- 离线 ROI：640/960 的 14/15 有框帧均把目标判成 panzer；1280 的 14/15 均判为 pillbox，
  置信度中位数约 0.723，14/15 均大于 0.5，说明不能靠降低类别阈值修复。
- `logs/vsim04_diag_pillbox_imgsz640_seed11_20260830_203128/`：36 个 eligible frame 的
  raw class 为 0，`P_confirm=false`，detector inference P95 约 64.4 ms。
- `logs/vsim04_diag_pillbox_imgsz1280_seed11_20260830_203035/`：35/35 raw class/geometry/map
  全有效，`P_confirm=true`，地图 P95 约 0.012 m，detector inference P95 约 59.6 ms；manifest
  与 perf 均记录 `imgsz=1280`。

### 遗留问题

- 1280 是笔记本 dev/sim 上采样诊断，不代表当前 640 固定输入 RKNN 可直接部署；单次本机 P95
  未退化也不能外推 OrangePi 吞吐。
- 1280 运行的同 ID `P_selected=false`，表示 clutter 下另有候选排序，不否定本次 raw classifier
  恢复；仍需在正式矩阵中按同 ID 统计。
- 当前高空采集帧同质性高，不足以训练或宣称泛化改善。

### 下一步

- 正式/板端路径继续使用 640；采集跨高度与横向运动的 pillbox 小目标，按 trial 隔离 held-out，
  在完整共视标签前提下微调 640 候选模型，再以同一 23-trial Gate 决定是否替换模型。


## 2026-08-30（Landing detector 按任务阶段静默门控）

### 改动范围

把 V-SIM 中“搜索时不启动 H 节点”的性能收益下沉为 dev/board 共用的生产阶段门控：非
`landing` 模式跳过全图 H 处理，进入 `landing` 后恢复；不改变 H 算法、阈值或导航状态机。

### 具体改动

1. landing detector 订阅参数化 `/uav_vision/align_mode`，由 `default_align_mode` 初始化；
   `process_only_in_landing_mode=true` 时只有 `landing` 模式执行 cv_bridge、阈值和 morphology。
2. 实测发现 inactive 空 heartbeat 会进入 fusion bucket 并破坏完整率，因此最终门控在非
   landing 模式完全静默；搜索 fusion 本来不要求 landing source，业务合同不受影响。
3. dev 与 board Phase D 都把既有 `default_align_mode` 传给 H 节点；V-SIM 增加默认关闭的
   `start_landing_detector` A/B 参数，正式搜索 Gate 节点图保持不变。
4. 新增 rostest：disabled/drop_circle 阶段必须无输出和 debug，landing 阶段必须恢复输出和
   debug，再退出 landing 后重新静默。

### 验证结果

- Python 语法、launch/test XML、`git diff --check` 和
  `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision -j2` 通过。
- `rostest uav_vision landing_mode_gate.test` 1/1 PASS。
- 反例 `logs/vsim04_diag_landing_gated_seed11_20260830_203728/` 证明空 heartbeat 不可用：
  complete mapped rate 仅 0.590；该实现未保留。
- 最终静默门控 `logs/vsim04_diag_landing_silent_seed11_20260830_203903/`：同五项 selector
  pipeline P95 约 79.5 ms、detector inference P95 约 54.2 ms、complete mapped rate 0.986。
  对照完全停 H 节点约 77.4 ms/73.8 ms/0.936，门控节点存在时 pipeline 退化约 2.8%，没有
  完整率退化；对照旧无门控 H 路径约 97.4 ms，pipeline 改善约 18%。

### 遗留问题

- mock 证明模式切换会恢复处理，但本提交未重新跑真实 H 正样本 Gate；历史 H 检测结果仍需在
  最终合并态复核。
- align mode 若上游发送未知字符串会安全进入静默；模式发布的可靠性仍属于任务接口联调。

### 下一步

- 合入 red-cross yaw 修复后先跑 H 正样本/普通黑圈负样本，再进入无 H 节点负载的
  `static25` 与 `sparse30`。


## 2026-08-30（红十字多 yaw 几何质量鲁棒性修复）

### 改动范围

修复红十字几何质量绑定图像水平/垂直轴、合法旋转正十字被当作 X 拒绝的问题；
保持 strict/relaxed 共用连续 `[0,1]` 质量、target memory 0.70 门槛和双证据接口不变。

### 具体改动

1. 将单一轴对齐模板替换为预计算的 0–90°、5°间隔、多臂宽等比正十字模板库；
   观测轮廓和旋转模板使用同样的支持域裁剪与 64×64 归一化，以最大 IoU 评分。
2. 将仅在特殊 yaw 成立的水平/垂直镜像对称替换为 90°/180°/270°四重旋转对称，
   保持原有模板、对称和凹角门槛数值不变。
3. 扩展 `cross_geometry_regression` rostest，在 0/15/30/45/60/75/90° 同时覆盖
   96 px 和约 50 px 正样本；将原本与合法 45°正十字等价的 X 负样本改为臂宽约 8%
   的细臂 X，并保留方块、圆、条、T、L、断裂和边界裁切负样本。
4. 扩展 fusion 回归，覆盖 0.70/0.83/1.0 几何质量，确认双证据融合不量化或改写
   几何质量、精修中心和类别置信度归属。

### 验证结果

- `catkin_make --pkg uav_vision -j2` 通过；Python 语法、launch XML 与 `git diff --check` 通过。
- `rostest uav_vision cross_geometry_regression.test` 通过：14 个多 yaw/双尺度正样本全部
  发布 `geometry_verified`，96 px 质量为 0.943–0.954，50 px 质量为 0.922–0.936；
  方块、圆、条、T、L、细臂 X、断裂和裁切 8 类负样本全部拒绝。
- `detection_fusion_concurrency_assertion.py` 输出
  `detection_fusion concurrency/evidence PASS`。
- 本轮未运行 Gazebo，未执行实机、导航、内参或阈值改动。

### 遗留问题

- 多角度模板已预计算，单帧仍需计算多个 64×64 二值 IoU；当前合成 ROS 回归满足
  搜索阶段负载，但仍需在无采集负载的 V-SIM 中量化实际 callback 延迟。
- 当前是等比、无透视畸变的确定性合成图；不代替倾斜透视、模糊、阴影、反光和
  遮挡回放。

### 下一步

- 合入 V-SIM 性能分支后用无采集负载的 `static25` 验证多 yaw 红十字与整体吞吐；
  若存在边界失败，仅对同 seed 最小 selector 补透视/模糊回放，不下调 0.70 门槛。


## 2026-08-30（视觉 L0 回归入口收敛）

### 改动范围

修正统一视觉 mock 回归脚本误测固定 `/home/xhj/liftrace` 的问题，并把当前必须长期成立的
视觉侧合同纳入一个 fail-closed 入口；不启动 Gazebo、导航、飞控或执行机构。

### 具体改动

1. 从脚本自身绝对路径推导项目根目录，直接调用当前 worktree 的 devel-space 包装器；移除
   固定原工作区路径和本机缺失的 `rosrun` 假设。
2. 每个 roslaunch 使用独立临时 ROS 日志目录，只在本次日志中查 PASS marker，避免历史
   `latest` 残留造成假通过；临时目录删除前校验固定 `/tmp/uav_vision_l0_roslog.*` 前缀。
3. 增加 drop freshness、disabled/drop_circle/drop_cross/landing 四模式、红十字多 yaw、
   H 阶段门控、profile/当前帧准入、fusion 并发、V-SIM schema 与失败采集回归。
4. 四模式 mock 不再把无地图的标准目标误当成可 selected；目标选择的地图、年龄和 r2026
   准入由 policy 与地图链专项回归断言。

### 验证结果

- `bash -n`、`git diff --check` 通过。
- 扩展后的 `run_visual_mock_regressions.sh` 连续运行 3 次均完整 PASS；每次覆盖 11 个
  roslaunch/rostest 场景与 4 个纯断言入口。
- 首轮旧期望曾因无地图 panzer 未 selected 正确失败，更新为现行 fail-closed 合同后稳定通过；
  未通过篡改视觉准入规则规避测试。

### 遗留问题

- 该入口是视觉 L0 确定性回归，不替代 Gazebo operating surface、真实视频或板端 ROS Gate。
- roslaunch 对 required 断言正常退出会打印红色“process has died”，最终结果仍由独立日志
  PASS marker 与脚本退出码共同判定。

### 下一步

- 在此回归基线上继续加入 V-SIM 错误/禁用候选硬审计和算法性能 verdict，再运行无采集负载
  的 red-cross 3.6 m、H 正负样本与 `static25`。


## 2026-08-30（V-SIM 失败样本采集合同加固）

### 改动范围

加固 diagnostic failure capture 的选择、相机 profile、轨迹采样、图像编码、数据集完整性和
统一入口失败传播；不修改正式视觉阈值、模型、相机内参、导航代码，也不启动 Gazebo/实机。

### 具体改动

1. failure capture 与 recorder/runner 共用 `select_trial_matrix`，支持 `trial_selector` 或命名
   `trial_slice` 且二者严格互斥；运营域 wrapper 显式透传后续 `name:=value`，拒绝静默参数和
   覆盖其 matrix/slice 所有权。
2. 固定仿真相机冻结首个合法 CameraInfo profile：frame/尺寸须与图像一致，K/P 完整有限且
   fx/fy 为正，D/K/R/P、畸变模型、binning/ROI 后续变化全部 fail closed；CameraInfo 作为
   latched profile，不要求 stamp 与逐帧图像相同。
3. 每个 trial 的配额按图像源时间采样：单样本位于预计时长 45%，多样本覆盖 0%～90%；
   static 使用 dwell，dynamic 使用 `2*path_half_length/speed`。manifest 与逐帧 JSON 记录计划、
   实际偏移和 trial-end fallback，不再只抢最前 N 帧。
4. 图片显式转换为 `bgr8` 后保存，metadata 同时声明源/保存编码；保留 active truth，并按
   target_id/class/ROI 确定性记录 exact-stamp truth 中全部 pose/projection 有效、ROI 有效且
   完整入画的 `scene_targets`，避免共视靶标在 YOLO 标签转换中被误作背景。
5. schema 升级为 v2；成功必须同时满足 `run_complete=true`、每 trial count=quota、总数等于
   max_frames、连续 capture index，以及 manifest 声明的 PNG/JSON 全部存在且逐帧 JSON 与
   manifest record 一致。新增独立 manifest checker，`sim_run.sh` 识别 true/True/1，尊重
   run 内相对采集子目录并拒绝绝对路径、父目录逃逸和 V-SIM output 根目录覆盖。
6. callback fatal 用 `try/finally` 保证 shutdown，异步失败在 main 返回非零；纯逻辑断言注册为
   catkin nosetest，覆盖 slice 冲突、多 trial quota、源时间采样、CameraInfo 非法/漂移、RGB
   通道哨兵、共视标签以及 manifest/files/schema 失败。

### 验证结果

- `vsim04_failure_capture_assertion.py` 输出 `V-SIM-04 failure capture mock/schema PASS`。
- `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision -j2` 与随后
  `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j2` 通过。
- `catkin_make run_tests_uav_vision_eval ...` 实际执行 1 项 nosetest，1/1 PASS，并生成 xUnit。
- Python 语法、launch XML、两个 shell 入口语法和 `git diff --check` 通过。
- 未启动 Gazebo 的 shell 合同验证中，`enable_failure_capture:=True` 被识别且缺少完整
  manifest 必须非零；自定义子目录用于预期检查路径，`../escape`、V-SIM `output_dir` 覆盖和
  wrapper 非 `name:=value` 参数均在启动前拒绝。

### 遗留问题

- 本轮按任务约束未启动 Gazebo；源时间 0%～90% 采样、动态横向偏移覆盖和首个真实 v2
  dataset manifest 仍需下一次 diagnostic 实跑确认。
- 动态末端目标已离开可投影范围时允许用本 trial 最后一帧有效 exact-stamp pair 补最后一个
  采样点，并在 metadata 显式标记；若仍无法填满 quota，运行保持硬失败。
- CameraInfo 漂移比较对固定仿真 profile 使用精确值；未来切换会逐帧更新内参的真实相机驱动时，
  应另立动态 profile 合同，不能直接复用本采集器放宽判断。

### 下一步

- 合入性能分支后，用动态 pillbox 小集合和 run 内独立子目录运行一次 v2 capture，检查横向偏移、
  `scene_targets` 去背景标签和 quota 完整性；通过后才进入候选小目标数据集转换/训练。


## 2026-08-30（H 外圈残缺误触发修复）

### 改动范围

把 landing 阶段门控回归扩为 H 正负几何哨兵，并修复“完整内部 H + 半圈残弧”仍发布
`landing_pad` 的安全缺口；不修改导航降落状态机、内参或实机接口。

### 具体改动

1. 合成回归在 landing 激活阶段精确覆盖完整外圈+H 正样本，以及纯背景、普通黑圈、仅两根
   竖条的残 H、半圈残弧+完整 H 四类负样本；跨话题使用图像 stamp 而非会被发布端重写的
   Header seq 关联结果。
2. landing detector 增加参数化 `landing_min_ellipse_fill_ratio=0.70`：以轮廓面积除以拟合椭圆
   面积验证外圈完整度，拒绝仅凭局部弧段得到的近圆拟合；内部 H 结构阈值保持不变。
3. 非 landing 阶段继续完全静默，landing 阶段即使负样本也只发空 heartbeat，不发布
   `landing_pad`。

### 验证结果

- 修复前扩展 rostest 稳定复现 `broken_ring_h` 被错误发布为 `landing_pad`。
- 修复后 `catkin_make --pkg uav_vision -j2` 通过，`rostest uav_vision
  landing_mode_gate.test` 1/1 PASS：完整 H 连续 3 帧为正，四类负样本各连续 3 帧为负，
  disabled/drop_circle 仍保持静默。
- Python 语法与 `git diff --check` 通过；未启动 Gazebo 或实机。

### 遗留问题

- 当前负样本是无透视、无反光的确定性合成图；门槛仍需用真实 H、普通黑圈、残圈和遮挡
  回放复核，不能把合成 PASS 写成真实降落 Gate。
- 新外圈完整度会拒绝严重遮挡 H；对最终降落而言这是 fail-closed 取舍，后续只能基于标注数据
  调整，不能为了单个漏检直接降低门槛。

### 下一步

- 用统一 `sim_run.sh` 运行 Gazebo `landing_h_eval` 正样本和 background 负样本，再把真实视频
  中的黑圈/残圈帧加入离线 held-out Gate。


## 2026-08-30（短视觉评测纳入统一 run 目录）

### 改动范围

修正 bounded visual evaluator 只能依赖绝对调用路径、产物默认落到 `/tmp` 的问题，使 H、背景和
单靶短评测可以由 `sim_run.sh` 统一归档；不改变场景、视觉算法或 Gate 阈值。

### 具体改动

1. `run_toudi3_visual_eval.sh` 使用 `${BASH_SOURCE[0]%/*}` 后立即解析为绝对目录，从仓库根或
   任意当前目录调用均能正确找到环境 helper。
2. 外层存在 `SIM_RUN_DIR` 且调用者未显式给 output_dir 时，评测六类产物、scene 截图、
   roslog 和内部 launch.log 默认写入 `${SIM_RUN_DIR}/visual_eval`；独立调用仍保留 `/tmp` 回退。

### 验证结果

- `bash -n top_level_scripts/run_toudi3_visual_eval.sh` 与 `git diff --check` 通过。
- 本提交只整理入口，实际 H/background Gazebo 结果在后续独立 run 记录。

### 遗留问题

- 该 wrapper 仍是每个场景一次 Gazebo 冷启动；V-SIM operating surface 继续使用单会话 runner，
  不应退回本入口逐 trial 启动。

### 下一步

- 从仓库根通过 `sim_run.sh` 分别调用 landing_h/background bounded evaluator，确认产物确实位于
  对应 run 的 `visual_eval/`，并记录正负 Gate 结果。


## 2026-08-30（仿真 feature worktree 视觉 overlay 置顶）

### 改动范围

修复独立 worktree 复用主集成 devel 时，roslaunch 文件来自 feature 分支但视觉节点可执行体仍
解析到 `/home/xhj/liftrace/vision_ws` 的问题；该问题会让仿真产生“测了旧代码”的无效证据。

### 具体改动

1. `sim_run.sh` 显式导出当前 `PROJECT_ROOT/VISION_WS`；worktree 没有 patrol build/devel 时仅把
   可配置的 `LIFTRACE_INTEGRATION_WS`（本机默认主集成 workspace）作为编译 underlay。
2. 环境加载顺序改为先 source 集成 workspace，再以 `vision_ws/devel/setup.bash --extend` 把
   当前 feature worktree 置顶，同时保留 patrol 消息、库和 Gazebo 包。
3. bounded visual wrapper 把自身解析出的 worktree 根、视觉 workspace 和编译 underlay 显式
   传给组合环境 helper，避免子 shell 丢失未导出的 shell 变量而回退主仓。

### 验证结果

- 首次 `visual_landing_h_sentinel_20260830_205910` 判为无效 FAIL：launch.log 明确显示
  detection_fusion/landing detector 来自主仓旧路径，并复现旧 `OrderedDict mutated during
  iteration`；该 run 不作为 H 算法结论。
- 修复后环境静态解析全部指向当前 worktree：`rospack find uav_vision/uav_vision_eval`、
  `roslaunch --files ... landing_h_eval.launch` 和 `catkin_find --libexec ... landing_detector_node`
  均返回 `/home/xhj/liftrace-worktrees/vsim04-performance/...`。
- 三个 shell 入口 `bash -n`、组合环境自检和 `git diff --check` 通过。

### 遗留问题

- 主集成 devel 仍是编译 underlay；若 feature 分支修改 patrol 消息/C++ ABI，必须在独立集成
  workspace 重编译，不能只靠路径置顶。本轮只修改视觉包，不存在该类跨仓 ABI 变化。

### 下一步

- 丢弃首次旧 overlay run 的算法结论，以修复后的统一入口重跑 H/background/red-cross 定向项；
  每次从 launch.log 复核关键节点 ready 字段来自当前实现。


## 2026-08-30（H 评测相机对齐 toudi4 主标记）

### 改动范围

修复 `landing_h_eval` 与 bounded wrapper 仍沿用 `(0,0)` 旧坐标、没有拍到 toudi4 主 H 的问题；
只改评测相机默认位姿，不改世界、真值 catalog、相机内参或算法阈值。

### 具体改动

1. `landing_h_eval.launch` 与 `run_toudi3_visual_eval.sh landing_h` 的默认相机 XY 改为真值 catalog
   中 `landing_h_1=(-0.493412,-1.77269)`，高度仍为 1.8 m、下视姿态不变。
2. launch 注释明确默认值来源，防止 toudi3 历史 `(0,0)` 再次覆盖 toudi4 入口。

### 验证结果

- 修正前当前 worktree run `visual_landing_h_sentinel_current_20260830_210241` 为 FAIL：
  landing detector 处理 329 帧但 landing_pad=0，TP=FP=FN=0，像素/地图误差均 N/A；真值 catalog
  明确 H 位于 `(-0.493412,-1.77269)`，故该 run 证明入口未拍到目标，不用于算法召回结论。
- launch XML、shell 语法与 `git diff --check` 通过；修正坐标后的实际 Gate 留给下一次独立 run。

### 遗留问题

- 固定正上方相机只验证 H 正样本基本链；真实返航仍有横偏、yaw、模糊、遮挡和高度变化，需联合
  landing 路径或离线矩阵覆盖。

### 下一步

- 以修正后的默认坐标重跑 35 s landing_h smoke；要求 landing_pad 真值、像素误差与地图误差
  实际有样本，而不是依赖空集合 precision/recall。


## 2026-08-30（Landing 模式背景负样本入口）

### 改动范围

增加 H detector 真正处于 landing 激活态时的无目标背景评测入口，避免因搜索阶段静默而把
“未运行 H 算法”误报成背景零误检；不改变默认 background 行为和算法阈值。

### 具体改动

1. `background_eval.launch` 参数化 `required_scoring_source` 与 `vision_default_align_mode`，默认仍为
   `target_detector/disabled`，保持既有通用背景回归兼容。
2. bounded wrapper 新增 `background_landing` 场景别名：复用同一 background-only 真值，显式以
   `landing_detector` 计覆盖率并以 `landing` 模式运行 H 链。

### 验证结果

- 前置 H 正样本 run `visual_landing_h_at_target_20260830_210442` 为 PASS：458 TP、0 FP/FN，
  input coverage=1.0，mean/p95 pixel error=0.665/0.670 px，mean/p95 map XY error=0.0026 m，
  P95 latency=30.2 ms；launch.log 的新 ready 字段确认节点来自当前 worktree。
- `background_eval.launch` XML、wrapper shell 语法与 `git diff --check` 通过；landing 背景实跑留给
  下一次独立 run。

### 遗留问题

- 固定 background-only 视点不是普通黑圈/残圈实拍全集；它只用于发现自然场景误触发与验证
  landing detector 在负场景确实被调度。

### 下一步

- 运行 35 s `background_landing`；要求 landing_detector 有非零 processed frames、FP=0，且报告
  不依赖像素/地图误差（无目标场景按定义不可计算）。


## 2026-08-30（V-SIM-04 动态位姿遥测与分层统计）

### 改动范围

补齐 V-SIM-04 动态 operating surface 的逐帧相机运动遥测，并让 summary、Markdown 报告和
既有一 trial 一行性能表可直接按类别、高度、请求速度比较；不修改导航、实机、内参、阈值或模型。

### 具体改动

1. recorder 复用真值投影的 stamped camera pose 话题和 `at_or_before` 零阶保持规则，在
   `frames.csv` 追加 world 坐标、ZYX yaw、pose 来源时间/年龄及明确的无效原因。
2. 动态线速度按相邻有效 pose 的三维位移/严格递增 pose 来源时间差计算，yaw 角速度使用最短
   有符号角差；静态、首个有效样本、缺 pose、重复或倒退时间戳均保持空值，不伪造为 0。
3. 动态横向偏移定义为水平面相对计划起终点直线的有符号垂距，归一化值为垂距/整段路径长度；
   runner 只追加起终点 y 元数据，不改变轨迹执行方式。
4. `summary.json` 和 `report.md` 增加 class/height/requested-speed 三套分层统计；
   `vision_search_performance.csv` 保留原列前缀和一 trial 一行，仅在尾部追加逐 trial 运动汇总及
   所属分组指标。动态 trial 缺位姿、速度或横偏样本时终态 fail closed。
5. 扩展 `vsim04_metrics_assertion.py`，覆盖速度、跨 ±π yaw、横偏公式，静态/首帧空值，缺位姿、
   时间非单调、三维分组聚合、CSV 列顺序和报告分层表。

### 验证结果

- Python 语法、launch XML、`git diff --check` 通过。
- 当前 worktree devel-space 的 `vsim04_metrics_assertion.py` 通过，输出
  `V-SIM-04 matrix/artifact schema PASS`。
- `catkin_make --pkg uav_vision -j2` 与 `catkin_make --pkg uav_vision_eval -j2` 通过。
- 本轮未运行 Gazebo、导航、飞控、执行机构或相机内参标定。

### 遗留问题

- 当前 yaw 是相机模型姿态的标准 ZYX yaw；下视相机接近俯仰奇异位形时仍应结合后续实跑数据
  解释，不将低 yaw rate 单独当成姿态稳定结论。
- 需在合入其他 V-SIM recorder/runner 改动后的最终状态重跑 `sparse30`，确认 pose 来源时间
  更新率足以产生非零有效样本，并用分层表定位速度/高度退化。

### 下一步

- 合入 V-SIM 主联调分支后先跑最小动态 selector 校验字段，再运行 `sparse30`；若出现大量
  `non_monotonic_stamp` 或 `camera_pose_missing`，只修评测位姿同步链，不调整视觉阈值。


## 2026-08-30（V-SIM-04 全类别候选审计与性能结论分层）

### 改动范围

在 `feat/vsim04-hard-audit` 独立分支补齐 V-SIM-04 视觉侧错误候选审计和机器可读性能
结论；不修改或启动导航 manager、控制链、PX4/MAVROS、执行机构或实机相机。

### 具体改动

1. recorder 不再只查看当前 trial 的期望类别：对 `/targets` 中所有 `CONFIRMED` 候选和
   `/selected_target` 的每次发布记录实际类别、stable ID、期望类别、profile 准入及当前
   policy 结果；原 P_confirm/P_selected 仍只按期望类别且必须关联同一 stable ID。
2. `summary.json` 按 run、类别和 trial 汇总 confirmed/selected 的 observation、unexpected、
   disallowed、tank 和 unique target 数；events.csv 保留每次原始审计事件，性能 CSV 追加
   trial 级审计列。禁用类 confirmed 仅作误检诊断，任何 profile 禁用类 selected 都标记硬
   失败；`r2026` 额外明示 tank selected 必须为 0；允许类 selected 若不满足当前完整准入
   policy 同样硬失败，避免陈旧/无地图/无关联候选绕过审计。
3. 将 artifact set、trial measurement completeness 和 algorithm performance verdict 分为
   独立机器字段及报告条目：`MEASURED` 只表示正式测量完整，diagnostic 固定为
   `DIAGNOSTIC_ONLY`，不再把完整性写成算法 PASS。
4. 性能合同仅固化 Roadmap/历史 formal 已有的 confirmation processing P95 `<=200 ms` 与
   map error P95 `<=0.25 m`；P_confirm、P_selected 和 TF failure 门槛显式列为未冻结，已知
   指标通过时仍写 `NOT_GATED`，不臆造跨组阈值。
5. `sim_run.sh` 除原六产物/终态检查外读取 performance verdict；普通性能 FAIL 仍保留有效
   测量产物但 formal 入口非零，未冻结合同导致的 `NOT_GATED` 也不得冒充 PASS；diagnostic
   仅接受 `DIAGNOSTIC_ONLY`，任何 selected hard failure 强制非零退出。
6. 扩展纯 schema assertion，覆盖允许但 unexpected 候选、禁用 confirmed、r2026 tank
   selected、同 ID 关联语义、formal 完整性与性能结论分离、diagnostic 不冒充 Gate，以及
   summary/report/性能 CSV 的审计字段。

### 验证结果

- Python `py_compile`、`bash -n top_level_scripts/sim_run.sh` 和 `git diff --check` 通过。
- 源码路径及本 worktree devel-space 的 `vsim04_metrics_assertion.py` 均输出
  `V-SIM-04 matrix/artifact schema PASS`。
- `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j2` 定向编译通过。
- 本轮未运行 Gazebo；未执行导航、飞控、投递、实机或新相机内参标定。

### 遗留问题

- P_confirm/P_selected 与 TF failure 的正式最低门槛仍需导航组冻结；在此之前完整正式运行
  最多给出 `NOT_GATED` 或已知阈值 FAIL，不会给出算法 PASS。
- 新审计只对后续运行生效；旧 V-SIM-04 产物未记录被原 recorder 过滤掉的其他类别，不能
  事后补算完整的 unexpected/disallowed 计数。

### 下一步

- 合入当前 V-SIM 性能分支后先运行 L0 统一回归，再用最小 diagnostic selector 验证真实
  ROS 话题下全类别审计，随后执行 red-cross 3.6 m、H 正负样本和 operating surface。


## 2026-08-30（V-SIM 产物完整性按落盘事实收口）

### 改动范围

收口全类别硬审计与动态遥测合并后的产物写入事务，避免性能 CSV 和 summary 在文件真正落盘前
自报完整；不修改视觉算法、阈值、模型、导航、内参或实机接口。

### 具体改动

1. 合并全类别候选审计与动态位姿/速度/横偏分层统计：性能表同时保留 trial 级审计列、运动列和
   class/height/requested-speed 分组列，summary/report 同时呈现性能结论、错误候选和动态遥测。
2. 六项产物先以原子写生成 provisional summary/report，再按磁盘上实际存在且非空的文件计算
   `artifact_completeness`；性能 CSV 的 `artifact_set_complete` 随实测结果二次刷新，不再预置 true。
3. 最终写入后再次核对 inventory；若写事务期间状态变化，同步刷新 summary、性能 CSV 和 report，
   保证三份结论一致。
4. schema assertion 增加 required 集合、missing 空集、非空文件以及落盘 summary 与返回值一致性断言。

### 验证结果

- `vsim04_metrics_assertion.py` 输出 `V-SIM-04 matrix/artifact schema PASS`。
- Python 语法、`sim_run.sh` shell 语法及 `git diff --check` 通过。
- 本项只验证写入合同，未启动 Gazebo、导航、飞控、投递或新相机标定。

### 遗留问题

- 真实 ROS 运行仍需确认 diagnostic 返回 `DIAGNOSTIC_ONLY`，formal 在未冻结三项门槛时按设计
  返回 `NOT_GATED`/非零，而不是把完整测量误写为 Gate PASS。

### 下一步

- 重编当前 worktree 后运行 pillbox 多高度 schema v2 capture 与最短动态 red-cross selector，
  同时检查候选审计、产物 inventory 和统一入口退出码。


## 2026-08-30（V-SIM 采集集按 trial 安全转换为 YOLO 数据）

### 改动范围

增加 schema-v2 diagnostic capture 到 YOLO 数据目录的确定性转换工具，并用首个真实多高度
pillbox 采集集验证共视标签；不启动训练、不修改现有模型/阈值，也不把 9 帧诊断集宣称为可训练集。

### 具体改动

1. 新增 `vision_ws/scripts/build_vsim04_yolo_dataset.py`，先复用 capture 的严格 manifest/files
   校验，再从 detector metadata 读取连续 class ID，避免在工具中另建不一致类别表。
2. 每帧标签取 active truth 与全部完整共视 `scene_targets` 的并集，以物理 `target_id` 去重；
   active truth 可保留裁剪后的部分入画框，`landing_pad` 等模型外类别只记录忽略原因，不伪造 ID。
3. 图片按 trial 整体分配 train/val/test，同一运动序列绝不跨 split；分配由
   `sha256(seed:trial_id)` 决定，输出 `dataset.yaml` 与逐帧/逐类/忽略项 conversion manifest。
4. 输出目录非空时拒绝覆盖；转换结果固定写 `training_readiness.ready=false`，要求先完成人工多样性
   与 held-out 审核，防止“转换成功”被误写成“可以微调”。

### 验证结果

- conda `rl_drone` 下 Python 语法与 `git diff --check` 通过。
- 真实采集 `vsim04_diag_pillbox_multih_v2_seed11_20260830_211944` 的 9 帧全部转换：train 6、
  val 3；生成 pillbox 9、bridge 3、tent 2 个框，证明共视 bridge/tent 未被错误当作背景。
- 三个高度 trial 均只落入单一 split；所有归一化框位于 `[0,1]`，输出明确保持
  `training_ready=false`。

### 遗留问题

- 9 帧仅用于验证采集/标签合同；首个低空样本存在较多部分入画，高空只覆盖单场景单 seed，
  不能据此训练或评价泛化。
- 需要先修复首采样点迟到，再扩充不同 seed、相位、背景和高空小目标 held-out 数据；之后才能
  决定是否做候选微调。

### 下一步

- 在采集时序修复后重跑多高度 pillbox，小规模人工抽查标签与 split，再决定是否组装候选训练集；
  原 `merged_standard` 权重继续作为正式基线。


## 2026-08-30（PT/ONNX 一致性改为同输入几何核验）

### 改动范围

修正笔记本 PT/ONNX 导出一致性工具的预处理口径，复核标准靶、红十字、H 和纯背景；不修改
模型权重、推理阈值、RKNN、导航、实机或相机内参。

### 具体改动

1. `compare_yolo_backends.py` 默认先生成同一 `640x640` fixed-letterbox 图像，再交给 PT 与
   固定形状 ONNX，避免 PT 动态矩形和 ONNX 方形输入的几何差异被误写成模型数值差异；保留
   `backend-native` 仅用于复现历史口径。
2. 支持重复 `--source` 聚合多组样本；结果补充预处理口径、输入尺寸、双方置信度以及未匹配框
   详情，便于区分真正的导出误差与前后处理差异。
3. 更新 Roadmap、笔记本基线和视觉包说明：笔记本 PT→ONNX 一致性已关闭，但不外推为 RKNN、
   NPU 性能或 OrangePi ROS 链通过。

### 验证结果

- `backend-native` 在 9 张多高度 pillbox 采集上复现 15 匹配、PT 缺失 1、ONNX 新增 2，
  证明旧口径确会因预处理不同产生类别/置信度差异。
- 同一 fixed-letterbox 下，标准靶采集 9 图、red_cross、landing H、纯背景共 12 图全部通过：
  19 个框，缺失/新增均为 0，最低 IoU 0.9999991、最大置信度差 `5.19e-6`、最大坐标差
  `6.11e-5 px`。
- 同一手工 letterbox 的原始 PT/ONNX 输出张量最大绝对差约 `9.46e-4`；Python 语法与
  `git diff --check` 通过。

### 遗留问题

- 当前集合仅 12 张受控图，后续仍需独立 held-out 实拍/仿真样本防回归。
- 尚未执行同样本 RKNN 逐框对照、板端 ROS CameraInfo/TF、5–10 Hz 与 10 min 稳定性 Gate。

### 下一步

- 合入动态遥测与采集时序修复，重跑对应 Gazebo diagnostic；PT/ONNX 回归继续固定同一输入
  几何，板端可用后再做 PT→ONNX→RKNN 三后端对照。
## 2026-08-30（V-SIM-04 动态遥测时序与轨迹重置隔离）

### 改动范围

修复 V-SIM-04 recorder 因多话题回调乱序和动态 trial 起点重置而污染逐帧速度统计的问题；
不修改视觉模型、检测阈值、相机内参、导航、飞控、投递或实机接口。

### 具体改动

1. 动态 frame 在计算遥测前按 matched camera-pose source stamp、image stamp 和稳定字段逐 trial
   确定性排序，落盘的 `frames.csv` 同步采用 trial 顺序内的相同时间顺序。
2. runner 为动态轨迹记录 ROS-time 起止边界；recorder 将起点重置前、轨迹完成后的 pose 与路径内
   突发回跳标为明确无效原因，不把 offscreen 到 path-start 的瞬移计入 motion sample。
3. 真实静止且时间递增的零线速度/零 yaw rate 仍是有效样本；路线距离/总耗时的
   `actual_speed_mps` 与未加权 frame-delta 瞬时分布保持独立字段，并在报告语义中明确区分。
4. 扩展纯 schema assertion，覆盖回调乱序、轨迹重置、后续正常速度、真实零速度/零 yaw、
   重复 pose source stamp 以及静态空值合同。

### 验证结果

- `vsim04_metrics.py`、recorder、runner 与 schema assertion 的 Python `py_compile` 通过。
- 源码路径及本 worktree devel-space 的 `vsim04_metrics_assertion.py` 均输出
  `V-SIM-04 matrix/artifact schema PASS`。
- `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j2` 定向编译通过；
  `git diff --check` 通过。
- 本轮不启动 Gazebo，不执行导航、飞控、投递、实机或新相机内参标定。

### 遗留问题

- 本修复只隔离评测相机的轨迹边界与确定性排序；Gazebo `SetModelState` 的离散更新仍会使
  frame-delta 速度分布出现零阶保持抖动，因此 P95 只作诊断，不替代路线级平均速度。

### 下一步

- 合入 V-SIM 性能分支后复跑同一 `dynamic_red_cross_h1p2_v2p0` selector，确认 reset frame
  原因、路线级速度与 frame-delta 分布均按新语义落盘，再决定是否运行更大的 sparse30。


## 2026-08-30（V-SIM 预检失败的 abort ACK 空值兼容）

### 改动范围

修复 V-SIM runner 在 recorder 预检失败后等待 abort ACK 时对尚未填充事件序号强制转整数的
二次异常；不改变正常试验、视觉算法、模型、阈值、导航、实机或相机内参。

### 具体改动

1. `abort_event_seq` 为 `null` 时继续等待 recorder 对本次 `run_abort` 的确认，不再执行
   `int(None)`；非空但格式非法时同样保持原始失败原因并按超时路径失败关闭。
2. 正常整数/数字字符串 ACK 仍与 runner 发布的事件序号严格相等才算确认，不放宽终态合同。

### 验证结果

- 动态 red-cross 修复后真实 diagnostic 完成 1/1：路线实际速度 1.984 m/s（请求 2.0），
  frame-delta 均值 1.957 m/s，原 130–155 m/s 重置假峰消失；P_confirm/P_selected=1/1，
  TF failure=0，地图误差 P95=0.104 m，六产物与 `DIAGNOSTIC_ONLY` 退出语义通过。
- 缺 `VSIM04_VISION_REVISION` 的故障注入仍按 `vision_revision_missing` 非零退出，六产物记录
  `INCOMPLETE/NOT_EVALUATED`；runner 保留原始预检原因，未再出现 `int(None)`/abort 发布二次异常。

### 遗留问题

- Gazebo 离散 SetModelState 的 frame-delta P95 仍受零阶保持影响，只作诊断；路线级平均速度是
  轨迹执行的主统计。

### 下一步

- 合入 capture 采样时序修复并重跑多高度 pillbox；失败路径继续保留缺 revision 的最小故障注入
  作为 fail-closed 回归。
## 2026-08-30（V-SIM-04 失败采集就绪握手与源时间采样修复）

### 改动范围

在独立 `feat/vsim04-capture-sampling-sync` 分支修复多高度 pillbox 真实采集中首个计划样本延迟、
低高度前两帧挤在一起的问题；只修改 V-SIM-04 失败采集、runner 的采集握手、launch 接线、
schema/assertion 和说明文档，不修改视觉模型、阈值、recorder/metrics、导航、内参或实机接口。

### 具体改动

1. 根据 `logs/vsim04_diag_pillbox_multih_v2_seed11_20260830_211944` 定位根因：旧 0% 样本锚在
   目标尚未入画的全路径起点；h1.2 首次可投影约为 3.0 s，已经逼近旧 45%=3.15 s，因此随后
   一帧立即满足第二计划点。首帧延迟随高度变化，不是采集进程中途上线。
2. 启用采集时 runner 必须先等待合法 CameraInfo 与一组 exact-stamp 图像/真值使采集器 READY；
   trial_start 逐项 ACK 后，再通过与 recorder 事件隔离的 capture control 话题预告未来的
   source-time `sampling_start`。采集器 ACK 后 runner 才启动相机轨迹，topic 冲突直接失败。
3. runner 将场界裁剪后的实际动态路径时长和目标中心时刻交给采集器；采样计划以首个有效投影为
   可见窗口入口，动态出口按实际中心时刻镜像并受轨迹末端约束。h1.2 回归计划由旧近邻采样改为
   约 3.0/3.45/3.9 s，0%/45%/90% 真正分散。
4. 非末端 fallback 样本增加默认 0.25 s 最大源时间迟到约束；末端 fallback 只允许最后一个样本
   且显式豁免。逐帧记录计划/实际偏移、迟到、窗口和 sampling_start，manifest 复算图像 stamp
   与实际偏移、窗口几何、严格递增时间、配额和全部 PNG/JSON 后才发布 FINALIZED。
5. capture schema 升至 v3，旧 v2 不再被新 checker 冒充通过；FAIL 状态与 manifest 在 shutdown
   前 fail closed 落盘，JSON 禁止 NaN，输出路径同时防父目录和符号链接逃逸。
6. 扩展已注册 catkin assertion，覆盖真实低高度回归、裁剪动态路径、READY/RUNNING 状态语义、
   最大迟到与 fallback、sampling_start/source stamp、错误窗口、CameraInfo、schema/files 和路径逃逸。

### 验证结果

- Python `py_compile`、`git diff --check`、launch XML 通过；`roslaunch --nodes` 正确列出唯一
  failure capture 节点，未启动任何进程。
- `vsim04_failure_capture_assertion.py` 输出 `V-SIM-04 failure capture mock/schema PASS`；
  `vsim04_metrics_assertion.py` 输出 `V-SIM-04 matrix/artifact schema PASS`。
- `uav_vision` 与 `uav_vision_eval` 定向 catkin 编译通过；注册测试 1/1 PASS，0 failure。
- 旧 schema v2 实跑 manifest 被 v3 checker 按预期拒绝。本轮未启动 Gazebo、导航、飞控、
  投递、实机或相机内参标定。

### 遗留问题

- 新 source-time 握手与可见窗口需要在合入当前含动态遥测的 V-SIM 分支后，用同一 pillbox 多高度
  selector 做一次真实复跑；本提交按要求不运行 Gazebo，因此不把纯测试写成实跑 PASS。
- 单 trial 请求极高 quota 时仍受真实相机 FPS 和短可见窗口限制；配额不足会保持 fail closed，
  应按诊断目的降低总帧数，而不是放宽迟到上限或抢前 N 帧。

### 下一步

- 合入后用固定 seed=11、pillbox 三高度、每 trial 三帧复跑，验收 sampling_start 之前 READY、
  计划约 0%/45%/90% 分散、非 fallback 迟到不超过 0.25 s，并由 schema v3 checker 终验产物。


## 2026-08-30（schema-v3 采集实跑与 YOLO 转换入口同步）

### 改动范围

用合并后的采集/动态遥测代码实跑多高度 pillbox，并将已提交的 YOLO 安全转换工具同步到新的
schema-v3；不训练模型、不修改阈值、导航、实机或相机内参。

### 具体改动

1. 固定 seed=11，对 pillbox `1.2/2.4/3.6 m`、`1.0 m/s` 各采 3 帧；runner 在 capture READY
   与逐 trial ACK 后发布未来 source-time `sampling_start`，采集器按可见窗口 0/45/90% 落盘。
2. `build_vsim04_yolo_dataset.py` 改为只接受当前 schema-v3；继续先调用统一 manifest/files
   validator，旧 v2 不能绕过新 READY、时间窗、迟到和 source stamp 合同进入训练目录。

### 验证结果

- `vsim04_diag_pillbox_multih_v3_seed11_20260830_215821` 完成 3/3 trial、9/9 帧，主评测为
  `DIAGNOSTIC_ONLY`，capture 为 `DIAGNOSTIC`，统一 manifest/files checker PASS，进程退出 0。
- 三高度计划/实际偏移分别约为 `2.929/3.445/3.998 s`、`2.386/3.443/4.428 s`、
  `1.698/3.322/4.963 s`；最大非 fallback 迟到 0.0544 s，低于 0.25 s；无 trial-end fallback。
- schema-v3 记录 CameraInfo、精确图像/真值 stamp、可见窗口与 sampling_start，可复算；人工查看
  低空三帧已从部分入画到居中再到另一侧，未再挤在同一相位。
- 新转换入口对该 v3 集成功生成 train 6/val 3，标签为 pillbox 9、bridge 3、tent 1，且仍明确
  `training_ready=false`；Python 语法和 `git diff --check` 通过。

### 遗留问题

- 该 9 帧仍是单 world、单 seed、单类的合同验证集，不足以训练或声称泛化提升；转换输出继续
  强制 `training_ready=false`。
- 3.6 m pillbox 本轮仍未进入 target memory，首要算法边界未因采集修复而被掩盖。

### 下一步

- 先运行最新正式 23 trial、static25 和 sparse30，确认当前工作包络；再按多高度/尺度 A/B 和
  held-out 需求扩充数据，满足多样性审核前不启动训练。


## 2026-08-30（V-SIM 长矩阵 ROS 服务瞬断有界恢复）

### 改动范围

加固 V-SIM trial runner 在长 operating surface 中对一次 ROS service transport 瞬断的处理；
只覆盖幂等的 Gazebo 相机位姿写入和视觉 memory reset，不修改算法、阈值、导航、实机或内参。

### 具体改动

1. `set_model_state/reset_memory` 首次抛出 `rospy.ServiceException` 时，在原 5 s monotonic 总时限
   内等待 50 ms 后仅重试一次；没有无上限循环，也不跳过当前 trial。
2. 第二次仍失败、ROS shutdown、其他异常或总时限到期时继续失败关闭；错误增加具体 service
   名，避免后续只看到无来源的 `Connection reset by peer`。

### 验证结果

- 最新正式 23 trial 完成 23/23、六产物完整：P_confirm=P_selected=22/23=0.9565，唯一失败为
  `pillbox@3.6 m/raw_classifier`；processing P95=195.3 ms、地图 P95=0.0792 m、TF failure=0，
  tank/disallowed/policy-rejected selected 均为 0。因三项门槛未冻结，结论正确为 `NOT_GATED`。
- static25 完成 25/25，P_confirm=P_selected=24/25=0.96；1.8/3.0 m 五类全通过，唯一失败仍为
  pillbox 3.6 m。processing P95=117.6 ms、地图 P95=0.0145 m、TF failure=0。
- 首次 sparse30 在完成 20/30 后遇到单次 service `Connection reset by peer`，Gazebo 随后仍有
  数据；recorder 正确落盘 `INCOMPLETE/NOT_EVALUATED`，证明原失败语义有效。本补丁待整批复跑。

### 遗留问题

- 若 Gazebo 进程真正退出或 service 连续两次失败，整批仍必须 FAIL；不通过重试掩盖持续故障。

### 下一步

- 重跑完整 sparse30；若有一次 transport retry 但最终完成，记录恢复次数，若连续失败则保留
  service 名和同 seed 产物继续定位 Gazebo 稳定性。


## 2026-08-30（V-SIM 动态遥测首采样时间感知判定）

### 改动范围

修复 sparse30 首个低速 trial 因图像/位姿首采样晚于轨迹起点而被整段误判为遥测缺失的问题；
只修改离线动态遥测标注及其确定性断言，不修改视觉算法、阈值、trial 轨迹、导航、实机或内参。

### 具体改动

1. 根据 `logs/vsim04_diag_sparse30_seed11_20260830_221558` 定位：30/30 trial 已全部执行，但
   `dynamic_tent_h1p2_v0p5` 的首个运动窗内 pose 在轨迹开始约 0.3 s 后到达，已按 0.5 m/s
   合理前进约 0.15 m；旧固定 0.05 m 起点容差将其及后续所有帧都标成
   `trajectory_start_pose_not_ready`，终验因此按设计报遥测样本缺失。
2. 首个有效 pose 改为与 `start + path_unit * expected_speed * source_time_elapsed` 对应的计划
   位置比较；sampling slack 保持至少 0.05 m 或 1.5 个轨迹更新步，更新率无效时回退路径离散步，
   时间或速度元数据无效时仍使用起点与 0.05 m fail-closed 下限。
3. 新增 0.5 m/s、延迟 0.3 s、前进 0.15 m 的正例，并用相同 pose 源时间下 7 m offscreen
   reset 和“晚到但仍停在起点”作反例；同时验证图像 stamp 不参与计划位置计算、拒绝 reset 后
   可由后续在轨 pose 重新建立速度基准。

### 验证结果

- Python `py_compile` 通过；`vsim04_metrics_assertion.py` 输出
  `V-SIM-04 matrix/artifact schema PASS`。
- 新正例产出首个有效 pose 和后续 0.5 m/s motion delta；offscreen 与晚到停滞反例保持
  `trajectory_start_pose_not_ready`，offscreen 后回到计划轨迹可正常恢复采样。
- `uav_vision_eval` 定向 catkin 编译通过；完整 `run_visual_mock_regressions.sh` 通过几何、融合、
  四种 align mode、新鲜度、profile policy、地图/释放证据、V-SIM schema 与 failure capture。
- 本提交尚未重跑 Gazebo；上一批 30/30 完成但终验 INVALID 的事实保留，不改写为算法 FAIL。

### 遗留问题

- 需要用相同 `sparse30/seed=11` 实跑确认首个低速 trial 恢复 motion/lateral 样本，且其他
  29 个 trial 的重置隔离、速度、偏航和横向偏差统计不回退。

### 下一步

- 先做定向编译和全套视觉 mock 回归，再重跑完整 sparse30；整批产物通过严格终验后才整理
  operating surface 结论。


## 2026-08-30（V-SIM 当前入口与 operating surface 结论同步）

### 改动范围

同步 README、唯一 Roadmap、迁移 Gate 和 `uav_vision_eval` 入口说明中的最新视觉侧实跑结论；
只修改文档，不修改视觉算法、模型、阈值、导航、相机内参、飞控、投递或实机接口。

### 具体改动

1. 保留 9/23、13/23、20/23 和长会话异常为历史阶段证据，把当前摘要更新为 formal23
   22/23、static25 24/25 与 sparse30 25/30；明确 formal 的测量终态为 `MEASURED`、性能
   verdict 为 `NOT_GATED`，diagnostic 和单 seed 覆盖不能冒充概率 Gate PASS。
2. 记录 sparse30 最终 30/30：processing P95=153.9 ms、地图 P95=0.1103 m、TF failure=0，
   motion/lateral 有效样本 4045/4075、terminal errors=0；五个失败均为
   `target_memory_admission`，tank/disallowed/policy-rejected selected 均为 0。
3. 记录 pillbox 3.6 m 的 1280 A/B：P_confirm 恢复但共视 bridge 被选中，P_selected=0，
   processing P95=449.3 ms，超过 200 ms；保持 640 默认，不推广 1280。
4. 同步 failure capture schema-v3 实跑、同 fixed-letterbox 的 PT/ONNX 一致性和 H 固定
   Gazebo 正例/landing-active 背景负例；继续保留 30-seed、10 min、实拍和 OrangePi ROS
   未验收边界。
5. 把 V-CL-04 已关闭、V-CL-06 baseline 30 s PASS/A-B FAIL/600 s 未启动写入当前入口；导航
   manager 仍来自 `5144aa8`，`a68925d` 仍只作为地图实验，视觉文档不代替导航上游实现。

### 验证结果

- 四份入口文档中的 13/23、20/23、22/23、static25、sparse30 和 1280 表述已按历史/当前
  分层复核；r2026 下 tank 资产保留但 selected/accepted 禁止的口径未改变。
- `git diff --check` 通过；仅 README、Roadmap、迁移清单、评测包 README 和本追加记录发生
  变更。

### 遗留问题

- P_confirm、P_selected、TF failure 的导航采用门槛和有效重复次数仍未冻结，当前不能由
  22/23、24/25 或 25/30 推导比赛漏失概率。
- 1280 不满足当前延迟和选择合同；pillbox raw classifier 与五个 memory admission 边界仍需
  在 640 默认下做受控重复和尺度归因。
- 当前 operational chain 的 10 min、30-seed、实拍人工真值、OrangePi ROS 和联合
  P_interrupt/600 s 三投 Gate 均未完成；新相机内参继续暂停。

### 下一步

- 先在 640 下复测 formal/sparse 的标称与边界点并完成 10 min，再由导航冻结工作点和采用门槛；
  不在这些前置条件完成前扩 30-seed、切换 1280 或启动无失败证据驱动的训练。


## 2026-08-30（r2026 禁用类不再抢占蓝环关联）

### 改动范围

修复 `target_refiner` 不感知 class profile、导致 r2026 已禁用的 tank 仍可能先占用唯一蓝环、
允许的 pillbox 无法进入 operational 链的问题；保留 tank 原始/诊断资产，不修改类别权重、模型、
导航、内参、飞控、投递或实机接口。

### 具体改动

1. 用 schema-v3 捕获 `pillbox@3.6 m` 五帧并查看无叠加原图：中央 pillbox 完整入画，底部另有
   一个边缘裁切但真实存在的 bridge；768/960/1280 选择 bridge 符合现有高权重优先策略，不能
   误当成同一靶跨类错误去硬抑制。
2. 640 单项重复仍为 `raw_classifier` 失败；全程产生 tank 诊断确认但 tank selected=0。离线检查
   同一中央框可同时出现 tank 与 pillbox 候选，暴露 refiner 的一对一贪心关联在 profile 过滤前
   就可能让 tank 抢走蓝环。
3. `target_refiner` 复用统一 `resolve_class_profile`；只让当前 profile 可选标准类参与蓝环分配。
   r2026 下 tank 检测继续透传，但保持未精修、`association_valid=false`，拒绝原因显式为
   `class_profile_disallowed`；full profile 仍允许 tank 正常关联，未知 profile 继续 fail closed。
4. `phase_d.launch` 将既有 `class_profile` 同时传给 refiner 与 memory，配置默认仍为 full。
5. 扩展全局一对一关联 ROS 断言：把 tank 放在 pillbox 之前竞争同一圆环，要求 r2026 下
   pillbox 获得精修、tank 保留诊断但不得关联；原 tent/panzer 全局最优关联同时保持。

### 验证结果

- Python `py_compile`、launch XML 静态检查和 `git diff --check` 均通过。
- 定向 `target_refiner_association_mock.launch` 已输出 `V-ALG global ring association PASS`，日志
  明示 `profile=r2026 selectable_standard=bridge,panzer,pillbox,tent`。
- `uav_vision` + `uav_vision_eval` 定向串行 Catkin 编译通过；
  `top_level_scripts/run_visual_mock_regressions.sh` 全套 L0 回归通过。
- 全包 `catkin_make -j2` 另外暴露历史 `yolov5_detect/image_process` 对生成头
  `image2center.h` 缺少显式构建依赖；该问题与本次 refiner 修改无关，将独立修复和验证。

### 遗留问题

- 需要在补丁提交后用相同 seed、640 输入重跑 `static_pillbox_h3p6`；若模型没有连续输出
  pillbox 候选，profile 关联修复不会凭空创造分类结果，仍应保持 raw classifier FAIL。
- 768 虽把 pillbox P_confirm 提升到 1 且 processing P95=188.1 ms，但同画面真实 bridge 按权重
  优先被 selected；该 clutter 语义不能冒充 pillbox P_selected 改善，640 默认暂不改变。

### 下一步

- 完成定向编译和全套视觉 L0，再复跑 640 边界；依据实际连续帧结果决定继续优化 profile-aware
  后处理，还是只将 3.6 m pillbox 归入训练/模型改进数据集。


## 2026-08-30（yolov5_detect 生成头并行构建依赖）

### 改动范围

修复历史 `yolov5_detect/image_process` 在并行 Catkin 构建时可早于本包服务头
`yolov5_detect/image2center.h` 生成而编译失败的目标依赖竞态；不修改历史检测逻辑、消息接口或
当前 `uav_vision` 主链。

### 具体改动

1. 在 `image_process` executable 上按 Catkin 标准写法显式依赖本包与上游导出的生成目标，
   保证服务头先生成、C++ 源码后编译。
2. 保持 `package.xml`、旧 `image_process.cpp` 和与本竞态无关的历史依赖原样，避免扩大改动面。

### 验证结果

- 两套全新 out-of-tree build/devel 中，`image_process -- -j8` 与 `all -- -j8` 均通过；构建日志
  显示先完成 `yolov5_detect_generate_messages_cpp`、再编译 `image_process.cpp`。
- 两套干净 devel 均实际生成 `include/yolov5_detect/image2center.h` 和可执行
  `lib/yolov5_detect/image_process`。
- 现有 `vision_ws` 清空 whitelist 后全包 `catkin_make -j2` 通过，旧竞态未再出现。

### 遗留问题

- `detect_pkg` 存在另一类历史问题：包内 msg 文件存在，但消息生成配置整体被注释。
  该问题不是本次并行竞态的根因，本条不顺带修改。

### 下一步

- 全新构建通过后提交独立修复，再恢复视觉全包并行编译作为收口检查。


## 2026-08-30（板端 profile 接线与 full/r2026 兼容回归）

### 改动范围

补齐 `phase_d_board.launch` 的 refiner profile 接线，并将现有关联 mock 扩展为
`full`/`r2026` 双 profile 回归；不运行板端、相机、飞控或执行机构。

### 具体改动

1. 板端 Phase D 将已有 `class_profile` 参数同时传给 refiner 和 memory，避免
   `r2026` 启动时 refiner 仍默认读取 `full`。
2. 关联 mock 同时启动 `r2026` 和 `full` 两个隔离节点：
   - `r2026` 要求 pillbox 获得共享蓝环、tank 保留但 profile 拒绝，red_cross 字段原样透传；
   - `full` 给 tank 独立蓝环，要求仍可正常关联和精修，保留历史资产回归能力。

### 验证结果

- Python `py_compile` 与两份 launch XML 检查通过。
- 定向 ROS mock 通过，日志同时明示 `full` 可选标准类包含 tank、`r2026` 不包含 tank，
  并输出 `V-ALG global ring association PASS (profile-aware r2026/full)`。
- `top_level_scripts/run_visual_mock_regressions.sh` 全套 L0 通过；新 marker 保留原有回归脚本所需
  的兼容前缀。

### 遗留问题

- 新相机内参和 OrangePi ROS 实时链仍未验收；本条只验证配置传递与节点合同。

### 下一步

- 在不改变 640 默认工作点的前提下，将 `pillbox@3.6 m` 漏检与动态连续帧准入边界分别
  保留为模型/训练数据和 memory 时序问题。


## 2026-08-30（V-SIM-04 导航 Handoff 当前快照同步）

### 改动范围

同步四份视觉↔导航组间文档的当前实测结论、profile 边界和导航任务层下一交付；
保留 13/23 和 20/23 作为历史阶段快照，不改算法门槛、不启动联合仿真、不冻结最终
性能分支 hash。

### 具体改动

1. 在导航需求阶段回复中追加 formal 22/23、static25 24/25、sparse30 25/30，显式区分
   `MEASURED/NOT_GATED`、`DIAGNOSTIC_ONLY` 和 `P_interrupt=null`。
2. 记录 640 下 `pillbox@3.6 m` profile 修正后仍为 `raw_classifier`、
   `P_confirm=P_selected=0`；保持 640 默认，不将 profile 合同修正写成模型召回修复。
3. 明确 `r2026` 保留 tank 资产、权重、`full` 回归与 raw 诊断；refiner 禁止 tank
   抢占蓝环关联，operational selected/accepted 必须为 0。
4. 同步 2026-08-30 再次 fetch 结论：导航远程 `main` 仍为 `a68925d`且只含地图实验，
   正式 manager 仍为 `5144aa8`；V-CL-06 A/B 仍 FAIL，600 s Gate 未启动。
5. 将导航下一交付收敛为带 stable ID 的 accepted/result/retry 事件、持久队列摘要和剩余
   时间调度；强调 `P_selected` 不能替代 actual interrupt，这些任务策略不在 adapter 复制。

### 验证结果

- 已对照四组实跑 `report.md` 复核 trial 数、终态、首阻断阶段、处理/地图误差与
  tank selected audit。
- 四份 Handoff 已通过关键口径检索；13/23、20/23 历史快照与 22/23、24/25、25/30 当前
  补充结果分层保留，`P_interrupt=null`、导航 revision、A/B FAIL 和 600 s 未启动口径一致。
- 2026-08-30 实际执行导航仓 `fetch --prune`，复核仅有 `origin/main@a68925d` 且无 tag；
  `git diff --check` 通过，改动范围仅为四份 Handoff 与本追加记录。

### 遗留问题

- formal 采用门槛和有效重复数尚未冻结；static25/sparse30 仍是诊断子集，不是性能 Gate。
- `P_interrupt` 仍为 `null`；导航 manager 尚未交付 accepted/result/retry、持久队列与时间调度。
- V-CL-06 安全 A/B 尚未通过，600 s 三投未启动；新相机内参、OrangePi ROS 与实机仍未验收。

### 下一步

- 等导航组在自有 feature revision 交付可重放的 stable-ID 生命周期和收敛的安全 A/B；
  双方前置通过后先做带 actual interrupt 的 90 s smoke，再进入 600 s 三投。


## 2026-08-30（导航交付 beta1 优先级与正式链澄清）

### 改动范围

澄清 `uav_vision` 导航组接口文档中 2026-08-30 补充与旧 `20260807-beta1` 的阅读优先级，
补齐正式 r2026 任务链和历史 coverage manager 能力边界；不改代码、launch、算法门槛或
导航上游。

### 具体改动

1. 在文档顶部显式规定：正式任务链、profile、实测与职责表述冲突时，2026-08-30 补充
   优先于旧 beta1。
2. 补齐正式链：`/uav_vision/targets -> profile_candidate_selector ->
   /mission/profile_selected_target -> manager /navigation/goal_raw -> adapter /fastplanner/goal`，
   并写清 manager/adapter 的唯一发布者边界。
3. 明确 `/uav_vision/selected_target` 只作视觉 `P_selected` 统计与绕过 profile 审计，不是正式
   r2026 任务入口。
4. 将第 5 节标明为历史 coverage manager 参考；其权重队列、去重、重试和恢复能力不代表
   `manager@5144aa8` 已实现 accepted/result/retry、持久队列或时间调度。

### 验证结果

- 关键话题/节点检索通过：优先级声明、selector、profile-selected 话题、原始 selected 审计边界和
  `manager@5144aa8` 历史能力声明均可定位。
- `git diff --check` 通过；改动范围仅为 `NAVIGATION_GROUP_HANDOFF.md` 与本追加记录。

### 遗留问题

- 正式 manager 的 accepted/result/retry、持久队列和剩余时间调度仍待导航组交付；本文澄清
  不替代代码实现或联合 Gate。

### 下一步

- 导航上游交付新 revision 后，按正式链复核发布者所有权、stable-ID 生命周期与 actual
  interrupt，不再引用历史 coverage manager 结论代替验收。

## 2026-08-30（V-SIM-04 独立重复运行与跨 run 聚合）

### 改动范围

新增 V-SIM-04 边界 trial 的独立重复运行 wrapper 和跨 run 聚合器，用于减少手工拼接命令、
查找 run 目录与逐份抄录指标的时间；不修改视觉算法阈值、导航代码、相机内参或硬件链。

### 具体改动

1. 新增 `top_level_scripts/run_vsim04_repeats.sh` 和可安装的 repeat runner，显式接收 repeats、
   trial selector、matrix、imgsz、模型路径及视觉/导航 revision；每次重复均调用统一
   `sim_run.sh`，使用唯一 scene，在独立 Gazebo 会话中串行完成。`--dry-run` 只打印完整命令和
   环境，不启动 ROS/Gazebo。wrapper 自行 source ROS Noetic 和当前 worktree 的视觉 overlay；
   overlay 未构建时在仿真前明确非零退出，不依赖调用者 shell 的隐式环境。
2. 新增跨 run 聚合模块与 CLI，再次核验每个源 run 的六项产物、V-SIM-04 evaluation ID、
   完成 trial 数、`run_complete`、终态、visual-only `P_interrupt=null` 和 wrapper 实际退出码；
   重复 run 路径或 trial 集不一致同样 fail closed。
3. 聚合固定输出 `repeat_summary.json`、`repeat_trials.csv`、`repeat_report.md`，包含每个源 run
   verdict，以及逐 trial 完成数、P_confirm/P_selected、failure_stage 分布、processing/map P95
   样本；`P_interrupt` 保持 `null`。
4. 聚合只在所有源 run 均为正式 `MEASURED + PASS` 时返回成功；`DIAGNOSTIC_ONLY`、
   `NOT_GATED`、缺产物、非终态、硬失败或非零 `sim_run` 退出码都不会被提升为 PASS。
5. 多 trial selector 的 scene token 固定限制为 48 字符并追加 8 位 SHA-256 短哈希，避免
   6 个边界 ID 导致路径过长，也避免两个相同长前缀在截断后碰撞。
6. 增加 7 个无 Gazebo 单测并接入 Catkin nosetest，覆盖参数拒绝、唯一 scene、长 selector
   有界哈希、强制 `sim_run.sh`、正式 PASS 聚合、diagnostic/缺产物/非终态/重复目录和执行失败。

### 验证结果

- Python `py_compile`、`git diff --check` 和 7 个 repeat 单测通过。
- `uav_vision + uav_vision_eval` 定向 `catkin_make -j1` 通过；
  `run_tests_uav_vision_eval` 汇总 8 tests、0 errors、0 failures。
- `--dry-run` 实测生成 3 个唯一 scene，三条命令均直接调用本 worktree 的 `sim_run.sh`，参数与
  revision 均完整透传，未启动 Gazebo；复制到无 devel overlay 的临时根目录时按约定退出 2，
  并明确报告缺失的 `vision_ws/devel/setup.bash`。
- 对既有 1280/960/768 三份 `static_pillbox_h3p6` diagnostic run 做只读聚合，三源均保留
  `DIAGNOSTIC_ONLY`，总判定为 FAIL、进程退出 1；同时正确汇总 3/3 完成、P_confirm=1、
  P_selected=0、P_interrupt=null，并生成 JSON/CSV/Markdown 三产物。

### 遗留问题

- 工具只压缩独立重复和报告整理时间，不改变单次 Gazebo 启停耗时，也不冻结尚未冻结的
  P_confirm/P_selected/TF 门槛；diagnostic 重复按合同仍不是 Gate PASS。
- 本条未启动新的 Gazebo 复测；应在集成分支合入后用最新视觉 revision 对选定边界点执行实际
  repeats，避免在旧 commit 上产生新证据。

### 下一步

- 在最新视觉集成 revision 上先对 `static_pillbox_h3p6@640` 做 3 次独立重复，再按同一入口复测
  已知动态 target-memory admission 边界；依据聚合 failure_stage 决定进入数据补充还是时序调整。

## 2026-08-30（V-SIM-04 camera-only 600 秒稳定性入口）

### 改动范围

新增视觉侧 10 min 连续稳定性入口及纯函数审计，不启动 PX4、MAVROS、导航/control、
`actuator_pwm` 或实机设备；不把本次无 Gazebo 验证写成 600 秒通过。

### 具体改动

1. 新增 `vsim04_camera_soak.launch`：复用 V-SIM-04 单 Gazebo、评测相机、独立真值与 Phase-D
   视觉链，默认以 monotonic 墙钟连续 600 秒；ROS 源时间驱动固定椭圆并执行倒退/停滞检查，
   每圈只发布一次唯一
   `soak_loop_NNNN` ID，启动时只 reset memory 一次。
2. 新增 soak runner 与纯 accounting 模块：持续检查预期 ROS 节点、image/truth/camera pose/
   complete mapped/targets/perf 心跳和源时间单调，记录最大 heartbeat gap、输入/完整映射吞吐，
   对连续三组 10 秒窗口的低吞吐、源时间积压和 partial-only 高占比 fail closed。
3. selected 按 r2026 当前完整准入复核；tank、禁用类、陈旧、地图/关联无效、拒绝原因非空或
   连续帧不足均硬失败。visual-only `P_interrupt` 始终为 `null`，没有导航接受事件。
4. 启动必须取得有效 CameraInfo；manifest 保存 width/height、distortion_model、K/D/R/P 和
   frame_id，并拒绝运行中 profile 变化。CameraInfo 按 latched/低频配置处理，不伪装成周期心跳。
5. 输出 `manifest.json`、`frames.csv`、`events.csv`、`summary.json`、`report.md`、
   `vision_search_performance.csv`；缺产物、未跑满或终态错误返回非零。短运行明确写
   `SOAK_MEASURED/SMOKE_ONLY/soak_600s_pass=false`，只有实际跑满 600 秒才可写
   `SOAK_600S_MEASURED`。
6. `sim_run.sh` 识别 `vsim04_soak*`，复核六产物和 soak 终态；新增纯函数 assertion 覆盖
   唯一圈 ID、健康 600 秒语义、短 smoke 降级、源时间倒退、持续吞吐/积压/partial 失败、
   tank/陈旧 selected 与未跑满失败。

### 验证结果

- Python `py_compile`、launch XML、`roslaunch --files` 静态展开、shell 语法、`git diff --check`
  和无 Gazebo soak assertion 均通过；pure assertion 覆盖 600 秒墙钟资格与独立 ROS 源时长。
- `uav_vision + uav_vision_eval` 定向串行 Catkin 编译通过；`uav_vision_eval` 两项 nosetest
  共 2 tests、0 errors、0 failures。
- 按同机仿真资源独占要求，本条暂不启动 Gazebo；短时 `<=30 s` smoke 和正式 600 秒均无实跑
  结论。

### 遗留问题

- 需要在仿真资源空闲后先用 `duration_sec<=30` 验证真实进程名、话题心跳和吞吐阈值，再运行
  默认 600 秒；短 smoke 无论成功与否都不能写成 10 min PASS。
- 本入口只评估连续视觉链，不提供导航 `SEARCH→APPROACH` 接受事件，也不替代 V-CL-06 的
  三投、返航和落地 600 秒联合 Gate。

### 下一步

- 资源空闲后通过 `SIM_NO_RECORD=1 top_level_scripts/sim_run.sh` 运行短 smoke，依据真实产物
  修正阈值或节点名；短 smoke 通过后再排队执行完整 600 秒。

## 2026-08-30（V-SIM-04 camera-only 稳定性入口实跑前硬化）

### 改动范围

修复 600 秒视觉稳定性入口在实际轨迹、预热计数、长跑调用方式和终态产物上的误判风险；
不修改视觉识别阈值、相机内参、导航上游、控制链或实机设备。

### 具体改动

1. 相机循环更新改为 latest-only `/gazebo/set_model_state` topic，仅初始定位和 memory reset 使用
   有 monotonic deadline 的 service；节点列表 RPC 移出全局回调锁并使用独立超时，避免 10 Hz
   长跑累计创建约 6000 个线程或 master 卡顿冻结写盘。
2. 缓存 `/uav_vision_eval/camera_pose` 的 Gazebo 实际 camera link 位姿；`frames.csv` 同时记录命令/
   实际位置、位姿年龄和三维跟随误差，并按 `0.5 s/0.25 m` fail closed，不再用命令轨迹代替
   实际执行。
3. 从 scenario 读取精确 active target 集；启动及运行期要求 ground truth 的 scenario 匹配、目标
   不缺失/不重复/不多出且全部 `pose_valid`。detector perf 同时核验 level、backend 和模型路径。
4. 启动改为“完整预热 → reset → 全流 post-reset 新鲜 → 计时前二次 reset”；正式 epoch 原子清除
   warm-up counts、mapped buckets、错误、窗口、selected 和事件，并拒绝 epoch 前 selected。
5. 正常结束补最终节点、心跳、实际位姿、真值、perf 和 route progress 检查；随后冻结回调，在锁内
   深拷贝终态快照；终态还要求每个 required stream、有效真值和实际相机位姿在正式测量 epoch
   至少出现一次，不能仅凭二次 reset 前缓存通过。六产物先写其余五项，最后原子写
   `artifact_set_complete=true` 的 summary，避免回调竞态和半提交 summary。
6. `sim_run.sh` 将 `vsim04_soak_smoke*` 与 `vsim04_soak600*` 分开验收；额外核验非空六产物、
   `artifact_set_complete=true`、空 errors、`P_interrupt=null`、qualification/pass 和 requested/actual
   墙钟一致性，其他 soak scene 名直接失败。

### 验证结果

- Python `py_compile`、launch XML、shell `bash -n`、`git diff --check` 与扩展 soak pure assertion
  通过；新增覆盖 warm-up 清零、实际位姿误差和精确真值集合。
- `uav_vision + uav_vision_eval` 定向串行 Catkin 编译通过；`run_tests_uav_vision_eval` 汇总
  9 tests、0 errors、0 failures。
- 本条仍未启动 Gazebo；以上是实跑前合同和构建验证，不写成 smoke 或 600 秒通过。

### 遗留问题

- 需要用 35 秒 smoke 验证 Gazebo static camera 是否持续接受 set-state topic、实际 link 位姿误差、
  ground truth 完整性及真实吞吐阈值；通过后才可运行 600 秒。
- camera-only 入口仍不产生导航 accepted/`SEARCH→APPROACH`，`P_interrupt` 必须保持 `null`。

### 下一步

- 运行 `vsim04_soak_smoke35_seed11`，检查六产物与 fail-closed 指标；若无真实合同问题，再运行
  `vsim04_soak600_seed11`。

## 2026-08-30（V-SIM-04 重复运行证据绑定与中断收口）

### 改动范围

加固独立重复 runner 的批次路径、源 run 绑定、配置一致性和中断证据，保证边界点复测在失败时
仍保留有效测量、但不会误判 Gate；不修改视觉算法阈值、导航代码、相机内参或硬件链。

### 具体改动

1. 将 `batch-id` 净化为最长 48 字符且带短哈希的 token；默认及显式输出目录经 realpath 解析后
   必须严格位于当前项目 `logs/` 下，已有批次目录默认拒绝，避免路径逃逸和证据覆盖。
2. 每次执行前后分别采集对应 scene 的 run 目录集合，只接受集合差中唯一的新目录；零个或多个
   新目录均 fail closed，不再按 mtime 猜测来源。
3. runner 在启动首个 Gazebo 前加载 matrix，并用正式选择合同拒绝空、未知或重复 trial；同时
   校验 repeat 数、imgsz、模型文件和视觉/导航 revision 格式。
4. 聚合分离 `measurement_eligible` 与 `source_pass_eligible`：完整终态但算法硬失败或
   `sim_run` 非零的 diagnostic 仍贡献 trial 指标，源 run 和批次 verdict 继续保持 FAIL。
5. 聚合从 manifest 核对视觉/导航 revision、模型路径、imgsz、profile、matrix、seed 和 selector
   配置元组；不一致的源 run 禁止合并为同批测量。报告明确 repeats 复用同一固定 seed，只表示
   同设计点运行时重复，不是多 seed 样本。
6. 启动前及每个 repeat 返回后原子替换 `batch_checkpoint.json`；中断或编排异常时为未执行项写
   `UNFINISHED` 失败占位，仍聚合已经完成的 run，并将批次标为 `INTERRUPTED`。

### 验证结果

- Python `py_compile`、`git diff --check` 和 14 个 repeat 无 Gazebo 单测通过。
- `catkin_make run_tests_uav_vision_eval -j1` 通过，汇总 15 tests、0 errors、0 failures。
- 单测覆盖批次路径越界/复用拒绝、长 token、重复 selector 预检、集合差唯一绑定、失败 diagnostic
  指标保留、配置不一致、原子 checkpoint 和未执行占位；本条未启动 Gazebo。
- 既有完整 diagnostic run 的只读聚合仍输出 trial 指标且返回 FAIL，符合“不丢测量、不升格 PASS”。

### 遗留问题

- 当前未实现自动 `--resume` 和编排层单次运行墙钟超时；checkpoint 已保留命令、环境、源 run 与
  未完成状态，可作为后续恢复入口。

### 下一步

- 在最新视觉集成 revision 上用新 batch ID 执行首组 3 次边界重复；若人工中断，先依据 checkpoint
  核对已完成 run，再决定补跑，禁止复用原批次目录覆盖证据。

## 2026-08-31（V-SIM-04 稳定性视觉刺激与姿态闭环）

### 改动范围

补齐 camera-only soak 对相机姿态和有效视觉刺激的真实性约束；不调整检测阈值、模型、相机内参、
导航/控制代码或硬件链。

### 具体改动

1. 实际相机位置不再与“当前回调时刻命令”直接比较，而是用实际 pose 的 ROS source stamp 回算
   对应椭圆路线命令，降低仿真倍率和回调延迟造成的假失败。
2. 启动准入后冻结 Gazebo camera link 的实际四元数基线，运行期按符号不变的 quaternion 角距离
   检查姿态漂移，默认阈值 `0.05 rad`，并记录最大漂移。
3. 正式测量期分别计数至少一个目标 `projection_valid` 和 `fully_in_frame` 的 truth 消息；终态两项
   都必须大于 0，防止相机方向错误或全程看不到目标时，仅凭空 detections 的 completed source
   心跳误报稳定性通过。
4. 扩展 frames、summary、report 和 performance CSV 的姿态/视觉刺激字段，并增加纯函数回归。

### 验证结果

- Python `py_compile`、soak pure assertion、launch XML、shell `bash -n` 和 `git diff --check` 通过；
  pure assertion 覆盖 quaternion 正负号等价、姿态漂移、缺少/具备投影与完整入画两侧。
- 本条不预写 Gazebo smoke 结论。

### 遗留问题

- 实际位姿按 source stamp 对齐后的 `0.25 m` 跟随阈值、姿态 `0.05 rad` 阈值及 35 秒内是否具有
  完整入画刺激仍需 smoke 用真实数据验证；若失败应依据实际误差/真值定位，不直接放宽门槛。

### 下一步

- 完成静态/构建测试后运行 `vsim04_soak_smoke35_seed11`，确认实际位姿、姿态和有效投影/入画字段。

## 2026-08-31（V-SIM-04 首次 soak smoke 与派生流乱序口径修正）

### 改动范围

执行首个 35 秒 camera-only smoke，并修正 fusion 派生消息跨 reset/并行完成时的 source stamp
记账口径；不修改检测算法阈值、模型、导航/控制或实机链。

### 具体改动

1. 通过统一入口运行 `vsim04_soak_smoke35_seed11`，证据目录：
   `logs/vsim04_soak_smoke35_seed11_20260831_000729/`。
2. 运行在正式计时 `0.316 s` 时按约定 FAIL，六产物完整；错误为
   `mapped_source_time_regressed`、对应 runtime exception 与 duration incomplete。实际相机已有
   70 个 pose 样本、最大位置误差 `0.0325 m`、姿态漂移 `0`，说明 fail-closed 本身有效。
3. 定位到 `/detections_mapped` 是由多个 fusion 分支异步完成的派生流，receipt 顺序不保证严格按
   图像 source stamp；二次 reset 后还可能收到 reset 前已经在途、且会被 target memory 明确拒绝的
   派生消息。它不同于 image/truth/camera pose 的源时钟倒退，不能用同一硬失败口径。
4. 正式 epoch 开始时清空 source stamp baseline；各 callback 丢弃并计数 epoch 前消息，终态 presence
   Gate 继续要求 epoch 内新数据。mapped/complete-mapped 的 receipt reorder 改为计数并报告，仍由
   `max_source_lag_sec`、持续 backlog、心跳、完整映射吞吐和 target-memory cutoff 约束；image、truth
   和实际 camera pose 的源时间倒退仍是硬失败。
5. summary/report 增加 `source_reorder_counts` 与 `pre_epoch_ignored`，纯函数测试覆盖源流倒退硬失败、
   mapped 派生乱序可观测但不误杀两侧。

### 验证结果

- 首次 smoke 的 FAIL 与产物已保留，不覆盖、不写成通过；`sim_run` 退出 1。
- 修正后的 Python `py_compile`、pure assertion 与 `git diff --check` 通过；
  `run_tests_uav_vision_eval` 汇总 16 tests、0 errors、0 failures。二次 smoke 待新提交后执行。

### 遗留问题

- 首次运行不足 1 秒，尚未验证 35 秒内有效投影/完整入画、持续吞吐窗口和最终 SMOKE_ONLY；
  必须使用新 scene/run 重跑。

### 下一步

- 完成静态测试后运行 `vsim04_soak_smoke35b_seed11`；若仍失败，依据新 summary 的具体流、lag、
  实际位姿和 truth 刺激定位，不覆盖首次证据。

## 2026-08-31（V-SIM-04 模型串行生成与 ready 准入）

### 改动范围

修复 soak launch 中 camera/red-cross 并发 spawn 与 runner 抢跑的启动竞态；不改视觉算法、模型、
内参、导航/控制或硬件接口。

### 具体改动

1. 第二次独立 smoke `logs/vsim04_soak_smoke35b_seed11_20260831_001442/` 在正式计时前 FAIL；
   六产物完整，错误为 `set_model_state failed: model does not exist` 与 duration incomplete。
   run.log 同时记录 camera spawn service 无响应，确认是仿真启动竞态，不是算法或阈值失败。
2. 新增 `vsim04_soak_model_spawner.py`，在同一节点内依次生成 camera、red-cross；每次调用前后均
   用 `/gazebo/get_model_state` 核验，服务异常可在 deadline 内重试，全部存在后发布 latched
   `/uav_vision_eval/soak_models_ready=true` 并保持节点存活。
3. launch 删除两个并发 `gazebo_ros/spawn_model` 进程，改用串行 spawner；runner 订阅 ready，未
   ready 时不得调用 set-state、reset 或进入预热，expected-node 清单同步加入 spawner。
4. spawner 仍使用参数化 SDF、模型名、位姿、service/topic 与超时；不硬编码本仓以外的新资产。

### 验证结果

- 第二次 smoke FAIL 证据完整保留，`sim_run` 退出 1；未覆盖首次 run。
- 新 spawner 与 runner 的 Python `py_compile`、launch XML/`roslaunch --files`、定向 Catkin 编译和
  `git diff --check` 通过；第三次 smoke 待新提交后执行。

### 遗留问题

- 尚需第三次独立 run 证明串行 ready 能稳定消除启动竞态，并继续暴露后续真实 Gate 指标。

### 下一步

- 构建后运行 `vsim04_soak_smoke35c_seed11`；只在完整跑满 35 秒且终态为 SMOKE_ONLY 后进入
  600 秒。

## 2026-08-31（V-SIM-04 spawn 超时不可取消与唯一模型校验）

### 改动范围

修复串行 spawner 在不可取消的 Gazebo spawn 超时后重试所产生的重复相机，并澄清测量未开始时
的统计口径；不改视觉算法或硬件链。

### 具体改动

1. 第三次 smoke `logs/vsim04_soak_smoke35c_seed11_20260831_002006/` 越过 model ready 后在预热
   阶段 FAIL；错误为 camera pose stale 与 duration incomplete。run.log 明确出现 RealSense 同时
   挂到 `vision_eval_camera`、`vision_eval_camera_0`，pose stamper 因 camera link 不唯一持续拒绝。
2. 根因是 5 秒 deadline 只终止等待，不能取消 Gazebo 内仍运行的 spawn worker；旧 spawner 随后
   重试，第一请求完成后第二请求生成 `_0` 副本。现在每个模型最多提交一次 spawn 请求，并允许
   该唯一请求使用完整 startup deadline；超时后只轮询存在性，禁止再次 spawn。
3. ready 前新增 `/gazebo/get_world_properties` 校验：每个期望模型必须恰好以原名出现，任何
   `model_name_0` 等同名前缀副本都 fail closed。
4. runner 若在正式 measurement start 前失败，会先清空 warm-up accounting，再生成 FAIL 产物；
   不再用预热 counts 除以零墙钟而报告虚假的超高 FPS，并显式写 `measurement_started=false`。

### 验证结果

- 第三次 FAIL 六产物完整、`sim_run` 退出 1，且前两次证据未覆盖。
- 单请求/唯一性修正的 Python `py_compile`、soak pure assertion、launch XML 与
  `git diff --check` 通过；第四次 smoke 待新提交后执行。

### 遗留问题

- 需要第四次独立 run 确认世界中只生成一套 camera，并进入正式 35 秒测量。

### 下一步

- 验证后运行 `vsim04_soak_smoke35d_seed11`，检查 model uniqueness、pose/truth 与完整窗口。

## 2026-08-31（V-SIM-04 健康检查单调时钟竞态修复）

### 改动范围

修复 soak runner 在 ROS 节点查询和并发回调之间复用旧单调时钟造成的相机位姿假失败；不改
视觉算法、阈值、模型、内参、导航/控制或硬件接口。

### 具体改动

1. 第四次独立 smoke `logs/vsim04_soak_smoke35d_seed11_20260831_002504/` 已确认世界中只有一套
   camera，完成正式 measurement start，并在约 5 秒的首次节点检查后以
   `camera_pose_invalid` FAIL；六项 V-SIM-04 产物完整且未覆盖前三次运行。
2. 失败前图像 29 帧、完整 mapped 22 帧、有效真值 28 条、有效投影 16 条，实际相机位姿
   826 条，最大位置误差 0.0325 m、姿态漂移 0，说明 camera/truth/视觉主链均已进入测量。
3. 根因是主循环先采样 `now_wall`，再执行可能阻塞的 ROS Master 节点查询；查询期间 camera
   callback 继续更新 `receipt_monotonic`，随后旧 `now_wall` 小于新 receipt，被纯函数正确地判为
   future receipt。启动、测量和终态健康检查现均在取得共享状态锁后重新采样单调时钟；节点检查
   的下一 deadline 也从 RPC 完成时重新计算。
4. frame 健康快照同步使用锁内新时钟，避免普通 callback 抢占也产生同类负年龄；源时钟倒退、
   真正陈旧位姿和非有限数的 fail-closed 规则保持不变。

### 验证结果

- 第四次 smoke 仍按真实结果保留为 FAIL；输入 5.78 FPS、完整 mapped 4.39 FPS、partial-only 3，
  tank/disallowed/stale selected 均为 0，未将工具竞态误写成算法通过。
- 修正后的 Python `py_compile`、pure assertion、launch XML、`git diff --check` 均通过；
  `run_tests_uav_vision_eval` 汇总 16 tests、0 errors、0 failures。

### 遗留问题

- 仍需新的独立 35 秒 run 证明锁内时钟消除假失败，并验证一个完整目标入画窗口；第四次运行仅
  5 秒，尚无 fully-in-frame 真值。

### 下一步

- 提交后运行 `vsim04_soak_smoke35e_seed11`；只有完整跑满且获得 `SMOKE_ONLY` 才进入 camera-only
  600 秒稳定性 Gate。

## 2026-08-31（V-SIM-04 首轮 600 秒失败定位与相机位姿降载）

### 改动范围

保留首轮 camera-only 600 秒硬失败证据，并降低只承载最新状态的 Gazebo 相机位姿派生流冗余
负载；不改视觉模型/算法、CameraInfo、3 秒心跳门槛、导航/控制或硬件接口。

### 具体改动

1. 修复时钟竞态后的独立 smoke
   `logs/vsim04_soak_smoke35e_seed11_20260831_004108/` 完整跑满 35.018 秒，结果为
   `SOAK_MEASURED / SMOKE_ONLY`、退出 0、六产物完整且 errors 为空；输入 6.03 FPS、完整 mapped
   4.94 FPS、最大源滞后 0.487 秒、有效投影 99 条、完整入画 55 条，tank/disallowed/stale
   selected 均为 0。
2. 首轮正式运行 `logs/vsim04_soak600_seed11_20260831_004358/` 在 127.207 秒按硬规则 FAIL，
   错误为 `image_heartbeat_gap_exceeded`、对应 runtime exception 和 duration incomplete；六产物
   仍完整，未自动重试或覆盖。原始图像最大空窗 3.282 秒，真实超过 3 秒门槛，不是旧单调时钟
   假失败。
3. 失败前累计图像 709 帧、完整 mapped 589 帧、有效真值 708 条、完整入画 140 条；整体输入
   5.57 FPS、完整 mapped 4.63 FPS，12 个完整健康窗口未形成持续吞吐/积压/partial 失败，
   tank/disallowed/stale selected 为 0。该证据把缺口定位为单次 Gazebo 原始相机长停顿。
4. 同轮相机位姿从 `LinkStates` 派生 19490 条（约 153 Hz），远高于 10 Hz 路线/图像需求。
   `gazebo_camera_pose_stamper.py` 新增可选 `max_publish_rate_hz`，0 保持历史不限频；soak 显式使用
   20 Hz，发布与 runner 订阅队列均降为 1，并将实际限频写入 manifest。源时间回退时仍立即
   重新发布，避免 reset 后锁死。
5. 通用 `ground_truth.launch` 只新增透传参数且默认 0，既有矩阵行为不变；所有心跳、实际 pose
   误差、吞吐、源积压和 selected Gate 保持原阈值。

### 验证结果

- 35 秒通过证据与 127 秒失败证据均保留；首轮 600 秒未被写成 PASS，`P_interrupt=null`。
- 根盘尚有 850 GiB 可用、内存约 14 GiB 可用；`.ros/log` 1.2 GiB 仅触发启动告警，未发现
  OOM、节点崩溃或磁盘不足，因此未以清日志替代根因修复。
- 限频修订的 Python `py_compile`、launch XML/`roslaunch --files`、pure assertion 与
  `git diff --check` 通过；`run_tests_uav_vision_eval` 汇总 16 tests、0 errors、0 failures。

### 遗留问题

- 首次空窗与运行期间额外启动 WSL 只读 shell 时间强相关但无法证明因果；下一轮禁止叠加新的
  `wsl.exe`，仅轮询原 PTY。若降载后仍复现，再评估移除评测相机未消费的深度/双红外/IMU
  传感器，仍不得直接放宽 3 秒门槛。

### 下一步

- 静态验证后提交限频改动，运行全新 35 秒 smoke；通过后使用同 revision 重新执行 600 秒，
  失败证据继续按独立 run 保留。

## 2026-08-31（V-SIM-04 selected 最新状态队列收口）

### 改动范围

修复 soak 评测器自身的 `/selected_target` 历史消息排队造成的陈旧误判，并增强事件可诊断性；
不改 target-memory 选择算法、0.5 秒门槛、导航接口或硬件链。

### 具体改动

1. 位姿限频 revision 的独立 smoke
   `logs/vsim04_soak_smoke35f_seed11_20260831_010504/` 在 28.879 秒以 `selected_stale`
   FAIL，六产物完整且未覆盖既有运行；唯一陈旧 selected 对应 header source 1007.657，runner
   abort 时 source 已为 1008.180，跨过 0.5 秒硬门槛。
2. 同轮输入 16.00 FPS、完整 mapped 14.61 FPS，相机位姿 504 条（约 17.5 wall Hz）、最大位姿
   年龄 0.094 秒、误差 0.087 m，图像最大心跳空窗仅 0.122 秒；证明 20 Hz source-time 限频
   显著降低冗余且没有破坏相机/真值/视觉主链。
3. `target_memory` 的 selected publisher 原本已是 queue 1；soak runner 却订阅 queue 10，在高吞吐
   时会依次消费已被更新替代的历史状态并用当前 source time 复核，从而制造本地队列陈旧。
   runner 现改为 queue 1，与“只消费当前 selected”接口语义一致，0.5 秒校验保持不变。
4. selected 事件新增 `last_seen_sec`、`receipt_source_sec` 和 `selected_age_sec`，后续真实传输陈旧可
   直接由产物定位，不再只留下原因字符串。

### 验证结果

- 本轮 FAIL 按真实结果保留，未把 evaluator backlog 解释为算法通过。
- Python `py_compile`、pure assertion、launch XML、`git diff --check` 均通过；
  `run_tests_uav_vision_eval` 汇总 16 tests、0 errors、0 failures。

### 遗留问题

- 仍需新 run 证明 queue 1 消除本地 selected backlog；若新消息本身到达时已超过 0.5 秒，仍应
  保持硬失败并定位真实视觉处理/传输延迟。

### 下一步

- 提交后运行 `vsim04_soak_smoke35g_seed11`，通过后才重新进入 600 秒正式稳定性 Gate。

## 2026-08-31（V-SIM-04 最终视觉证据与组间文档收口）

### 改动范围

只更新视觉 Roadmap、迁移清单、V-SIM-04 使用说明、导航需求/阶段回复和两份 Handoff；不改
算法、模型、阈值、launch、导航/控制代码或硬件接口。

### 具体改动

1. 归档 camera-only PASS：`logs/vsim04_soak600b_seed11_20260831_011055/` 在 vision
   `8b3b88cd321469e3b61b6127ec2574d770848109` 上连续 wall 600.024 秒，ROS source
   564.863 秒，六产物完整、errors 为空、`P_interrupt=null`；输入/完整 mapped 平均
   15.019/13.336 FPS，59 个完整健康窗口坏 streak 均为 0。
2. 记录运行诊断：partial bucket 1003；实际相机位姿 10289 条、最大跟随误差 0.0782 m、
   姿态漂移 0；有效真值/投影/完整入画 9010/3668/1756；selected 1702 次为状态重复发布，
   最大年龄 0.467 秒，tank/disallowed/stale selected 均为 0。
3. 归档六点固定 seed 三次重复聚合
   `logs/vsim04_repeat_aggregate_boundary6-seed11-r3-final-307ac5c4/`：三次源 run 均 exit 0、
   配置一致、完整 `DIAGNOSTIC_ONLY/FINALIZED`。bridge/panzer 低空 2 m/s 均 0/3；pillbox
   低空 2 m/s 2/3、高空 0.5 m/s 1/3、高空 2 m/s 0/3；静态 pillbox 3.6 m 0/3。
4. 在所有当前文档统一边界：重复聚合 FAIL 是诊断语义且不是多 seed；默认 640 保持，2 m/s
   不作跨类别通用工作点、3.6 m 不承诺。算法/模型/阈值未变，所以不重跑 formal23/
   static25/sparse30，继续引用历史 run 并保留 revision 边界。
5. 明确 camera-only PASS 仅覆盖固定五目标、不含 tank 的 Gazebo 合成 D435i + 笔记本
   Ultralytics；run 名含 seed11 但 manifest 无 seed 字段，不覆盖随机场、OrangePi/RKNN、新
   实物相机或导航三投。tank=0 只证明该路线未选中，不能外推为随机场 accepted 审计。
6. 同步导航冻结点：2026-08-31 外部远程仍仅
   `main@a68925d15293e5510e2b4351c6b3d9bc5aa136ab`、无 branch/tag；manager 仍为
   `5144aa8f536bdcd214aea2f39ada558383b3bcb0`；联动分支 `8255aa4` 已是当前 HEAD 祖先。
   V-CL-06 A/B 仍 FAIL、导航 600 秒三投未启动，accepted/result/retry、持久队列和时间调度
   继续归导航交付。

### 验证结果

- 600 秒 run 的六产物、空 errors、qualification、吞吐、健康窗口、位姿、真值和 selected
  指标已与归档 summary/manifest 交叉核对；重复聚合的三次 exit、配置一致性、固定 seed 语义和
  六个 trial 结果已与 `repeat_summary.json` 核对。
- 最终静态回归：`uav_mission/test/test_coverage_policy.py` 57/57 PASS，
  `test_external_mission_contract.py` 4/4 PASS；`navigation_search_delivery_random_field.launch`
  通过 xmllint，`run_vsim04_repeats.sh` 与 `run_vsim04_camera_soak.sh` 通过 `bash -n`。
- `VISION_2026_ROADMAP.md` 只关闭 camera-only 10 min，V-CL-06 仍明确为 FAIL；
  `VISION_MIGRATION_CHECKLIST.md` 仍把 30-seed、OrangePi ROS 和实拍列为未完成。
- 全部文档将 `P_interrupt` 保持为 `null`，没有用 selected 替代导航接受/状态中断。

### 遗留问题

- 导航需交付 accepted/result/retry、持久队列和剩余时间调度，并以更保守的航高/航速重新通过
  90 秒 A/B，之后才能启动联合 600 秒三投。
- 视觉仍缺工作点邻域多 seed、横偏/转弯压力、OrangePi RKNN ROS、新实物相机和实拍真值；
  新相机内参按当前决定继续暂停。

### 下一步

- 导航冻结可采用工作点和任务生命周期 revision 后，视觉只在其邻域补真正多 seed/横偏，并
  联合测同一 stable ID 的 accepted→`SEARCH→APPROACH`→APPROACH command，即真实
  `P_interrupt`；不在 adapter 复制导航策略。

## 2026-08-31（V-CL-06 导航任务核心与规划遥测隔离导入）

### 改动范围

在独立 worktree `/home/xhj/liftrace-worktrees/vcl06-next-integration`、分支
`feat/vcl06-next-integration` 上，以视觉稳定性收口点
`d6f4cc372c030558755703464a545e6140821cde` 为基线，导入本地导航仓 clean 集成节点
`4de028f77b545964fd337945159fb0b8b26984e5`。该节点只汇合导航 manager runtime
`a65a616f209bfc7dd4d788ebb36609589cea5418` 与 planner telemetry
`022b7636b1661304ce2d47e9368c392adc67997d`；未导入 execution bridge `c12ee0c` 或其工作树内容。

### 具体改动

1. 源仓与本仓历史没有共同 merge-base，故没有使用 `--allow-unrelated-histories` 把导航仓整棵
   历史合入；改为先审核并应用可审计差异
   `a68925d15293e5510e2b4351c6b3d9bc5aa136ab..4de028f77b545964fd337945159fb0b8b26984e5`。
   路径清单只有 VCL06 manager/runtime、规划遥测、对应测试和文档，无大范围删除或 execution
   bridge 文件。
2. `uav_mission/CMakeLists.txt` 冲突同时保留视觉侧 `ReleasePermission/ReleaseResult` 与导航侧
   `NavigationDecision/NavigationResult`，保留全部既有视觉脚本安装项并新增
   `navigation_mission_manager.py` 与五个导航纯测试；没有引用不在本次差异中的旧导航 C++ manager
   源文件。
3. `uav_mission/package.xml` 合并双方消息、Gazebo、视觉、任务链和 `std_srvs`/YAML 测试依赖；
   `search_policy.py` 保留视觉现有覆盖路线、恢复和索引逻辑，同时保留比赛高度 `altitude <= 4 m`
   硬限制。
4. 导航 `competition_profiles.yaml` 的 `r2026` 仅准入
   `tent/pillbox/bridge/panzer/red_cross`；本仓已有 full/tank 资产、随机场 profile 选择器、旧释放消息
   与全部回归入口均未删除。导航侧 `docs/导航工程变更记录.md` 与本视觉联调台账同时保留。
5. 规划侧新增 `PlannerStatus.msg`、tracker、FSM 遥测发布与 schema/lifecycle gtest；任务侧新增
   competition profile、coverage route、确定性 mission core、runtime manager、ROS manager launch
   和对应消息合同。

### 验证结果

- `git diff --cached --check` 通过；导航新增脚本、模块和测试 `py_compile` 通过；三个变更 XML
  （两份 `package.xml` 与 `navigation_mission_manager.launch`）通过 `xmllint`。
- 导航新增纯 Python 回归 77/77 PASS；视觉既有 coverage、外部任务、释放 commitment、搜索策略
  回归 69/69 PASS，合计 146/146 PASS。
- 定向 `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_mission -j1` 与
  `catkin_make -DCATKIN_WHITELIST_PACKAGES=plan_manage -j1` 均 100% PASS；四种任务消息和两种规划
  消息均成功生成，`fast_planner_node`、`traj_server` 均成功链接。
- `planner_status_schema_test` 实际运行 4/4 PASS，覆盖状态值稳定、序列化、目标替换取消顺序与完成
  闭合；编译仅保留历史 PCL/VTK、unused-variable 警告，无新增 error。
- 本次未启动仿真、ROS 运行节点、实机或执行机构，也未 push。

### 遗留问题

- execution bridge 按边界明确未导入；manager decision 到 planner 接受/完成结果的实际运行链仍未
  在本分支闭合，不能据静态测试宣称 V-CL-06 联合 Gate 通过。
- 新 manager/runtime 与视觉现有 adapter、随机场 ready、selected/accepted 审计仍需在后续正式
  launch 中做单一 goal publisher 接线和 90 秒 smoke；失败重试、持久队列、剩余时间调度继续由
  导航侧实现，不复制进视觉 adapter。

### 下一步

- 由集成负责人复核本提交后，再从导航侧独立 clean 来源引入 execution bridge；随后先做 launch
  静态单发布者断言，再按同 seed 执行 30 秒预检和 90 秒联合 smoke，不直接进入 600 秒三投。

## 2026-08-31（V-CL-06 随机场一次性启动门控）

### 改动范围

在独立 worktree `/home/xhj/liftrace-worktrees/vcl06-start-gate`、分支
`feat/vcl06-random-start-gate` 上，以导航任务核心集成点
`7dd2c49acf8eb6f8a58a333195b77096810ad285` 为基线，为新
`navigation_mission_manager.py` 增加随机场一次性启动门控；不修改 manager、旧随机场正式入口、
视觉算法、execution bridge、planner、实机或执行机构。

### 具体改动

1. 新增无 ROS 依赖的 `mission_start_gate.py`，冻结 `r2026` 五类
   `tent/pillbox/bridge/panzer/red_cross`，逐项验证 field 的 READY/seed/profile、
   allowed/expected/spawned/verified 五目标清单、`footprint_valid`，以及本 run 目录中原子落盘
   truth 的 seed/profile/类/model/正 footprint 清单。
2. 同一 policy 还验证 latched planner anchor 的 READY/profile 与
   expected=spawned=verified 清单，以及 manager 的 `phase=IDLE/profile=r2026`。pose/map
   readiness 不在 gate 重复实现，继续由 manager 的启动服务作为第二道门。
3. 新增 `navigation_mission_start_gate.py` ROS shell；只有所有纯合同同时成立才探测并调用
   `/navigation/start_mission`。READY 前实际 Trigger 调用计数保持 0；服务未注册不计为调用并进入
   退避，服务拒绝后同样使用 0.5--5 s 封顶指数退避；首次 success 后永久闭锁，manager active
   时不调用。解析/运行错误通过 latched JSON `/navigation/mission_start_gate_status` 显式输出并
   fail-closed。
4. 新增独立 `navigation_mission_start_gate.launch`，默认 `enabled=false`，不启动任何 manager、
   planner adapter 或执行机构。现有正式随机场 launch 仍使用旧 `target_search_manager_py.py`，
   未把新旧 manager 混在同一入口；待 execution bridge 接入后再由新的 VCL06 launch 显式 include
   manager 与本 gate。
5. CMake 安装新节点并注册 15 项纯状态机/静态合同测试；README 记录接线条件、状态话题、退避和
   当前不得接入旧正式入口的边界。

### 验证结果

- 新增 15/15 测试 PASS，覆盖 READY 前调用为 0、五类/tank 排除、真值/footprint/anchor 严格
  匹配、manager active 不重调、服务不可用与拒绝的有界退避、成功永久闭锁及 launch 不混 manager。
- `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_mission -j1` 100% PASS；
  `run_tests_uav_mission` 汇总 92 tests、0 errors、0 failures、0 skipped。
- 新模块、ROS shell 和测试通过 `python3 -m py_compile`；launch/package XML 通过 `xmllint`，
  `roslaunch --files` 能唯一解析新增 launch；`git diff --check` 通过。
- 本次未启动仿真、ROS 运行任务、实机或执行机构，也未 push。

### 遗留问题

- execution bridge 尚未导入当前基线，新 manager 的 `NavigationDecision` 到 planner、投递结果回流
  仍未闭环；因此本 gate 只作为安全 include 交付，不能接入仍由旧 manager 驱动的正式随机场入口。
- 本轮只验证启动前合同与调用状态机，尚无 ROS 运行态 field READY→manager service accepted 证据，
  更不能据此宣称 90 秒 smoke 或 V-CL-06 Gate 通过。

### 下一步

- execution bridge clean 导入并完成单一 planner goal publisher 静态断言后，在全新的 VCL06 launch
  显式启用本 gate；先用 mock/ROS 合同验证 READY 前 service call=0、READY 后单次 accepted，再按
  30 秒预检和 90 秒联合 smoke 顺序推进。
## 2026-08-31（V-CL-06 视觉冻结对准上下文）

### 改动范围

在独立 worktree `/home/xhj/liftrace-worktrees/vcl06-vision-context`、分支
`feat/vcl06-vision-context` 上，以 VCL06 导航任务核心集成点
`7dd2c49acf8eb6f8a58a333195b77096810ad285` 为基线，新增视觉侧中性冻结上下文与兼容证据面。
本次不修改 `ReleaseEvidence.msg`、`MissionCommand.msg`或 `ReleasePermission.msg`，不改旧
`/uav_vision/release_evidence` topic/MD5，不引入 `uav_mission` 消息依赖。

### 具体改动

1. 新增 `AlignmentTargetContext.msg`：command 常量与 `NavigationDecision v1` 同值，显式携带
   `active/source/header/deadline/profile/align_mode`、完整 decision fence、语义目标首次/最近观测、
   payload 槽位和冻结地图 pose；`has_target` 区分无目标与合法 `target_id=0`。
2. 新增 `ReleaseEvidenceContext.msg`：内嵌旧 `ReleaseEvidence`，并回显 context
   source/header、mission/decision/语义身份、实际几何 ID/first_seen/last_seen/map pose、关联距离与
   `context_valid/context_reason`。
3. `drop_aligner` 继续无条件发布旧证据，并行发布上下文证据；默认
   `require_alignment_context=false` 保持旧行为。严格模式对缺失/失鲜/inactive、错 schema/source/
   profile/command/mode、排他 deadline 到达、decision/fence/冻结内容或几何实例变化全部
   清空 stable frames 并 fail-closed；独立 watchdog 在目标流中断时会分别检查 context 租约与
   当前几何观测年龄，即使 coordinator 仍续租也会主动撤销过期视觉证据。
   默认只接受 `ALIGN=2`，过渡放宽必须由 launch 显式配置。
4. r2026 上下文明确拒绝语义 tank，full 仍保留 tank 资产兼容；标准蓝环几何 ID
   不强制等于语义目标 ID，改按同 frame 地图距离关联；red_cross 按 `id + first_seen`
   精确匹配，防止 stable ID 重用。
5. `phase_d.launch`/`phase_d_board.launch` 暴露 profile、strict 开关和两个新 topic 参数；新增
   `alignment_context_mock.launch`、纯 policy 单测与 ROS assertion，同步更新 README 和导航交付文档。

### 验证结果

- `catkin_make --pkg uav_vision -j1` 100% PASS，9 种视觉消息的 C++/Python/EusLisp/Lisp/NodeJS
  绑定均生成；旧 `ReleaseEvidence` MD5 仍为 `07fbec53d6c6a8bdc19fddf37c081d04`，三个禁改消息
  与基线 `git diff --exit-code` 通过。
- 纯 policy 6/6 PASS；`run_tests_uav_vision` 及 `catkin_test_results --all` 最终汇总 12 tests、
  0 errors、0 failures、0 skipped。新 strict mock 覆盖 source/header freshness、deadline 排他边界、
  错 command/profile/r2026 tank、完整 fence/几何换代清 streak、标准环异 ID 关联、red_cross
  精确匹配与 ID 0。
- 旧 `drop_aligner_freshness_mock.launch`、`phase_d_map_mock.launch` 和
  `phase_d_mode_mock.launch` 均 PASS，旧 `evidence_valid/drop_ready` 默认路径保持。
- 新增/变更 Python 全部 `py_compile` PASS，变更 XML 全部 `xmllint --noout` PASS，
  `git diff --check` PASS。本次未启动仿真、实机、解锁、起飞或投递机构，也未 push。

### 遗留问题

- 导航 coordinator 尚需把冻结 `NavigationDecision v1` 无损映射为本中性 context，并以小于
  `alignment_context_max_age` 的周期只续租 header；在正式联合 launch 启用 strict 前，本提交不宣称
  decision→alignment→release 跨仓闭环已通过。
- 本次只是 ROS mock/静态契约验证，未覆盖飞行速度、高度、机构互锁或真实投递；
  最终 `release_permission` 仍由任务/安全层生成。

### 下一步

- 集成负责人将本提交与导航 coordinator/execution bridge 的独立提交集成，先跑 strict
  context ROS smoke 并核对同一完整 fence 的 ALIGN accepted/result；通过后再进入 90 秒联合
  smoke，不在视觉 adapter 复制导航调度策略。
## 2026-08-31（V-SIM-04 类型化导航事件评测接入）

### 改动范围

在独立 worktree `/home/xhj/liftrace-worktrees/vcl06-eval-typed`、分支
`feat/vsim04-navigation-events` 上，以
`7dd2c49acf8eb6f8a58a333195b77096810ad285` 为基线，只修改 `uav_vision_eval` 的评测器、
launch、纯断言和说明；不修改导航 manager/bridge、视觉算法、模型、阈值或硬件接口。

### 具体改动

1. `vsim04_stability.launch` 新增默认 `visual_only` 的
   `navigation_metrics_mode=visual_only|typed_contract|target_stage` 及 typed decision/result 话题；
   默认模式不导入导航消息且保持历史进程图。typed 模式可选订阅
   `/navigation/mission_command_raw` 与 `/navigation/mission_result`。
2. recorder 原样记录 `NavigationDecision/NavigationResult` typed 字段，并在终态快照时按
   `mission_id + decision_seq + target_id + target_first_seen + attempt + payload_slot` 全键关联；
   `target_id=0` 由 `has_target` 区分且保持合法。decision 还须携带属于该 trial 实际 selected
   观测集合的 `target_observation_stamp`，避免同一 stable ID 跨高度/速度 trial 重复计分。
3. 新增 `P_decision`、`P_dispatch`、`P_planner_arrival`；`P_interrupt` 仅在显式
   `target_stage` capability 下由同键 `STARTED/CAPTURE` 置 true，`visual_only/typed_contract`
   始终为 null。selected、decision、dispatch、planner arrival 均不能替代更强事件。
4. 纯函数校验 schema、profile/allowlist、command、完整键、幂等重传、decision identity 冲突、
   executor `event_seq` 冲突/乱序和排他 deadline。与导航 core 一致，所有用于 dispatch、planner
   arrival、capture 评分的 result 必须严格早于 action deadline；达到或越过边界的事件不参与
   指标。r2026 空显式 allowlist 会从已知 profile 推导，未知 profile 保持 fail closed。
5. typed 模式在 `run_complete` 后增加 0.5 秒 quiet、3 秒上限的 late-result drain；每次新导航
   回调重置 quiet，结束时在同一锁内冻结回调再生成快照。`visual_only` 继续立即终态化，并保留
   历史 `visual_only_no_navigation_acceptance_event` reason token。
6. `manifest.json`、`summary.json`、`report.md` 记录 mode、capability、reason、话题、事件数、
   去重数和合同错误；`vision_search_performance.csv` 的全部历史字段顺序不变，新导航字段只在
   尾部追加，`events.csv` 继续复用历史字段并把 typed 原文放入 details JSON。

### 验证结果

- 新增 typed 纯断言通过，覆盖 target ID 0、完整键、跨 trial 隔离、late callback 回填、三种
  capability、排他 deadline、乱序不评分、幂等/冲突、profile fail closed、终态 drain 边界及
  六产物字段；既有 V-SIM-04 matrix/artifact 纯断言通过。
- `uav_vision;uav_vision_eval` 定向 `catkin_make -j1` 通过；相关 Catkin 测试汇总 21 tests、
  0 errors、0 failures；target selection policy 与 detection fusion concurrency 纯断言通过。
- `patrol_control;uav_mission` 定向组合构建通过，导航 core 回归 77/77 PASS；typed 消息可从组合
  overlay 正常导入。launch 在视觉+任务 overlay 下成功展开 typed 参数。
- 修改脚本 `py_compile`、launch `xmllint`、`git diff --check` 均通过。本次未启动 Gazebo、
  ROS 运行节点、实机或执行机构，也未 push。

### 遗留问题

- 本提交只建立可审计指标，不提供导航事件本身；真实 `P_dispatch/P_planner_arrival/P_interrupt`
  仍取决于 execution bridge 与 planner/target-stage 在联合链中按合同发布 typed result。
- 未执行 V-SIM-04 Gazebo trial 或 V-CL-06 90/600 秒 Gate；late-result drain 只由纯状态边界测试
  验证，需在后续同 seed 联调中确认实际 ROS 调度下的结果事件均落盘。

### 下一步

- 由 V-CL-06 联动分支接入已审核 execution bridge 后，先跑 30 秒话题/schema 预检和 90 秒
  confirm→selected→decision→dispatch→planner arrival smoke；只有 executor 明确提供
  target-stage capability 时才启用 `target_stage` 并验收真实 `P_interrupt`。

## 2026-08-31（VCL06 合并态与视觉剩余项文档同步）

### 改动范围

只更新 Roadmap、迁移清单、导航接口说明、双向联调需求/回复和总 Handoff；不修改视觉、导航、
planner 或执行代码，不启动 ROS/Gazebo、实机或执行机构，不推送远程。

### 具体改动

1. 把导航来源分为三层：外部远程仍为地图实验
   `a68925d15293e5510e2b4351c6b3d9bc5aa136ab`；历史运行 manager 为 `5144aa8`；本地 clean
   manager/runtime `a65a616f209bfc7dd4d788ebb36609589cea5418` 与 planner telemetry
   `022b7636b1661304ce2d47e9368c392adc67997d` 由 `7dd2c49` 导入，并与 start gate、视觉上下文、
   typed evaluator 合并至 `db80dfd`。
2. 将持久候选队列、accepted/result reducer、有限冷却重试、三槽、510 秒硬返航、600 秒任务
   上限和 typed contract 从“待导航编码”改为“已实现且纯测试通过”。同时保持严格边界：live
   execution bridge 尚未合入，真实 `/fastplanner/goal`、planner/target-stage `STARTED`、
   `P_interrupt`、新合并态 90 秒 smoke 和 600 秒三投 Gate 均未验收。
3. 登记随机场一次性 start gate、`AlignmentTargetContext/ReleaseEvidenceContext` 与 typed V-SIM
   evaluator；新增功能默认关闭或保持 visual-only，旧 `ReleaseEvidence` MD5 和默认链不变。
4. Roadmap 顶层 20 项统一统计为 9 项闭合、10 项开放、1 项冻结；开放项中严格视觉主责 5 项，
   把 H 结构/降落联合 Gate 计入视觉参与面时为 6 项，避免把导航执行缺口计入视觉算法欠账。
5. 按用户最新信息把新相机状态更新为“内参已在仓外标定完成，等待 YAML、原始图片、标定板
   规格、运行分辨率和采集设置后复核接入”。明确该状态不等于 CameraInfo 已接线/通过；安装
   外参、ROS CameraInfo/TF、OrangePi 和实拍真值仍分别开放。

### 验证结果

- 文档修改前后执行 `git diff --check` 均通过；修改清单仅包含本条列出的状态文档。
- 交叉核对 `NAV_VCL06_MISSION_CORE.md`、`NAV_VCL06_RUNTIME_MANAGER.md`、
  `NAV_VCL06_CONTRACT.md` 与 `VCL06_PLANNER_TELEMETRY.md`，任务策略完成面和 live bridge 边界一致。
- 本次沿用相应实现提交已记录的纯测试/定向构建结果，不重复运行算法矩阵或联合仿真，也不把
  纯测试写成 V-CL-06 Gate PASS。

### 遗留问题

- execution bridge 未进入 `db80dfd`，新 manager 尚未实际驱动 Fast-Planner；typed
  `P_dispatch/P_planner_arrival/P_interrupt` 没有运行证据。
- 历史 baseline/a68925d A/B 仍不满足推广合同；新合并态 90/600 秒 Gate 未执行。
- 相机标定文件尚未收到，无法复核重投影误差、生成最终 CameraInfo 或开展外参/板端接线。

### 下一步

- 审核并合入 live execution bridge，完成单一发布者静态断言、30 秒 typed/start-gate 预检和
  90 秒 target-stage smoke；通过后再运行 600 秒三槽 Gate。
- 收到相机 YAML/原图后先做离线标定验收，再接 CameraInfo 与 optical→body→mission TF；不在
  文件未验收时改写现有仿真或板端内参。

---

## 2026-08-31：可选对准上下文保持旧稳定帧时序

### 改动范围

- `vision_ws/src/uav_vision/scripts/drop_aligner.py`
- `vision_ws/src/uav_vision/scripts/alignment_context_assertion.py`
- `vision_ws/src/uav_vision/test/alignment_context_optional.test`
- `vision_ws/src/uav_vision/CMakeLists.txt`

### 具体改动

1. `require_alignment_context=false` 时，首次或 fence 变化的上下文仅缓存并用于诊断回显，
   不再清空旧链 `stable_frames`，因此不会改变旧 `drop_ready/release_evidence` 时序。
2. 严格模式保持原 fail-closed 语义：decision、语义身份、attempt、slot 等 fence 变化仍清零。
3. 新增可选模式 ROS 断言：先累计两帧，再分别注入首次上下文和变化的 decision 上下文，
   验证下一帧均按旧路径达到第三帧并生成有效证据；原严格模式反向断言继续保留。

### 验证结果

- `catkin_make --pkg uav_vision -j1` PASS。
- `alignment_context.test` 与 `alignment_context_optional.test` 均 1/1 PASS。
- 旧 `drop_aligner_freshness_mock.launch`、`phase_d_mode_mock.launch` 均 PASS。
- 相关 Python `py_compile`、XML `xmllint --noout`、`git diff --check` 均 PASS。
- 未启动仿真或实机链路，未 push。

### 遗留问题

- 可选模式只用于旧链兼容；正式联调启用严格模式后仍依赖导航 coordinator 持续续租有效上下文。

### 下一步

- 集成分支同时回归 strict 与 optional 两套 launch，再接导航 coordinator 做完整 fence 联调。

---

## 2026-08-31：VCL06 释放许可绑定语义上下文

### 改动范围

- `uav_mission/scripts/release_commitment.py`
- `uav_mission/scripts/release_permission_arbiter.py`
- `uav_mission/config/release_guard.yaml`
- `uav_mission/launch/release_guard*.launch`
- `uav_mission/launch/toudi3_visual_delivery_guarded.launch`
- `patrol_control/launch/toudi3_full_competition_sim_new_vision.launch`
- `uav_mission/test/test_release_commitment_policy.py`

### 具体改动

1. 在现有释放仲裁器增加默认关闭的 `require_evidence_context`；正式 VCL06 可显式只消费
   `ReleaseEvidenceContext`，旧 VCL05、固定三投和裸 `ReleaseEvidence` 路径保持原默认行为。
2. strict 模式验证 context 新鲜度、排他 deadline、profile/mode/slot、语义—几何关联和当前有效
   对准证据；标准靶仍允许语义 ID 与蓝环几何 ID 不同，红十字继续要求 ID 与 first_seen 精确匹配。
3. strict 许可与后续 `ReleaseResult` 改为携带冻结的语义目标 ID/类别，避免继续用 `circle` 几何
   身份弱关联导航事务；没有新增消息、topic 类型或并行 arbiter。
4. 从视觉 Phase D 到 guarded release launch 补齐 strict context 参数透传，所有新开关默认关闭。

### 验证结果

- `test_release_commitment_policy.py`：6/6 PASS，覆盖标准靶语义/圆环几何分离、红十字实例匹配和
  payload slot fence。
- `release_commitment.py`、`release_permission_arbiter.py` Python 编译通过。
- 四个相关 launch 通过 `xmllint --noout`，`git diff --check` 通过。
- 未启动 Gazebo、ROS 控制链、实机或执行机构。

### 遗留问题

- 当前只有 strict 释放身份合同；仍需 typed execution bridge 发布/续租对应
  `AlignmentTargetContext`，并将匹配的 `ReleaseResult` 归约为 `NavigationResult`。
- 尚未运行新 manager 合并态的 ROS smoke 或 90/600 秒 Gate。

### 下一步

- 导入经审查净化的导航 planner motion bridge，在同一 executor 中补齐 capture、alignment、release
  与 recovery reducer；正式 launch 只启用该 bridge 作为 `/fastplanner/goal` 发布者。

---

## 2026-08-31：VCL06 单执行器补齐目标事务与落地事实

### 改动范围

- `uav_mission` 现有 `navigation_planner_bridge`、纯执行 reducer、配置/launch 与定向测试。
- guarded visual-delivery audit 对现有 `ReleaseEvidenceContext` 的 strict 语义审计。
- 不新增跨组消息、服务、任务 manager、planner-goal adapter 或执行机构接口。

### 具体改动

1. 在同一个 planner bridge 和同一 `executor_id/event_seq` 中补齐
   `APPROACH→CAPTURE→ALIGNMENT→RELEASE→RECOVERY`；目标位姿只取完整 fence 匹配的
   `TargetCandidate.map_point`，队列、重试、槽位和剩余时间仍只由导航 manager 管理。
2. `MissionCommand.ALIGN` 发出后等待旧控制实际进入 `Aligning` 才回流
   `STARTED/CAPTURE`；严格视觉证据成立后才进入 ALIGNMENT。所有 handed-off 阶段按原 decision
   deadline 产生 typed timeout，释放已可能在途时允许同一事务一次精确 late commit 回流，
   不在 bridge 发明重试策略。
3. `ReleaseResult` 去重收窄为当前目标事务，结果时间必须晚于 strict evidence；释放成功后的
   RECOVERY 还要求 control state 与 align mode 都在 ACK 后重新到达且保持新鲜。
4. LAND 不再使用默认零点：以返航完成后的新鲜 odom x/y 冻结落点，等待旧控制实际进入
   `Land`，并同时满足水平半径、低高度/低速度 dwell 和只读 MAVROS `ON_GROUND` 事实后才发布
   `SUCCEEDED/LANDING`。正式 live 旧控制路径强制 `mission_frame=camera_init`。
5. visual audit 在 strict 模式按 semantic target ID/class 审计 permission，同时继续从嵌套
   geometry evidence 读取稳定帧、有效性和 evidence stamp；旧模式默认行为不变。

### 验证结果

- `uav_mission` 全量纯测试：208/208 PASS；Catkin 登记测试：137/137 PASS。
- `plan_manage + uav_mission` 定向 `catkin_make -j1` PASS；工作树 `uav_vision` 定向构建 PASS。
- 相关 Python `py_compile`、launch `xmllint --noout`、`git diff --check` 均 PASS。
- 本轮只完成软件接口/事务闭合，未启动 Gazebo、实机、真实舵机或执行机构。

### 遗留问题

- 目标事务和 LAND 尚无 ROS 运行时 smoke；本条不能作为真实 `P_interrupt`、返航或落地证据。
- VCL06 正式随机场入口、只读硬 Gate 及 90/600 秒运行证据另行提交和验收。

### 下一步

- 建立只包含 clean manager、单 planner bridge、strict release 链与只读 Gate 的正式入口；先做
  ROS 图唯一发布者检查和 90 秒同 ID smoke，再决定是否支付 600 秒三投 Gate。

---

## 2026-08-31：建立 VCL06 正式随机场入口与只读硬 Gate

### 改动范围

- `uav_mission/config/vcl06_random_field_runtime.yaml`
- `uav_mission/launch/navigation_search_delivery_vcl06.launch`
- `navigation_mission_manager.launch` 的可选 runtime 配置入口。
- `navigation_vcl06_assertion.py`、纯 reducer 测试与 Catkin 安装/测试登记。
- 现有单 planner bridge/executor、strict visual audit 与 SITL MAVROS mission-frame 配置。

### 具体改动

1. 新正式入口固定 `toudi3_random.world`、`field_seed=11`、`class_profile=r2026` 与
   `nav_feature_profile=baseline`；标准随机类只有 tent/pillbox/bridge/panzer，red_cross 由同一
   spawner 加入。tank 资产/full 回归保持，正式场不生成也不准入 tank。
2. 正式运行只包含一个 clean `navigation_mission_manager` 和一个
   `navigation_planner_bridge`；旧 coverage/search manager、profile selector、visual-delivery
   adapter 均不启动。bridge 是 `/fastplanner/goal` 的唯一预期发布者，并显式启用 strict
   alignment/release context；策略队列仍只存在于 manager/core。
3. random-field runtime 使用已验证的安全搜索包络、2.2 m 搜索高度、1.2 m 接近高度、三槽与
   510/600 秒时限；原 manager launch 保留默认 `vcl06_runtime.yaml`，只有正式入口覆盖配置。
4. 新增纯只读硬 Gate：完整关联 decision/result fence、实际 `MissionCommand.APPROACH`、三次
   release commit 与 recovery、返航/落地、field/anchor/contact/start-gate/manager/bridge 状态、
   tank selected/accepted、碰撞/边界/4 m 高度和 ROS graph 单一 goal 发布者。Gate 不发布控制
   话题、不调用服务，也不实现队列或重试。
5. Gate PASS/FAIL 原子写入 `SIM_RUN_DIR/gate_status.json`；正式 launch 缺少该证据或 Gate 节点
   非零退出时保持失败，不用日志文本推断通过。
6. 修复 ALIGN 控制状态、strict context 与 ReleaseResult 跨话题回调竞态：严格证据可在
   `ALIGN_COMMAND_SENT` 缓存但不提前报阶段，控制接受或 guarded release ACK 到达后按
   CAPTURE→ALIGNMENT→RELEASE 顺序回流；替换决策后只保留一个待对账物理释放 fence，不实现
   重试队列或历史兼容层。
7. ABORT 不再是假 handoff：单桥用新鲜同 frame odom 生成当前位姿 planner goal，并沿同一 typed
   planner 生命周期确认停驻；协议保留但本桥未实现的 HOLD 明确拒绝，不新增控制消息或第二 stop
   adapter。
8. Gate 用 `MissionCommand.header.seq` 精确绑定 decision，避免同目标重试窗口重叠误报；late commit
   继续记作真实已释放事实但单独硬失败，并新增 CAPTURE→RELEASE→RECOVERY 与 RETURN_HOME stage
   顺序断言。strict visual audit 同时核对既有语义—几何匹配、几何目标存在/map_valid 及嵌套 evidence
   身份，不复制 arbiter 策略。
9. SITL MAVROS 本地 pose/odom/setpoint frame 统一标为 `camera_init`，与视觉地图、manager 和
   Fast-Planner 的既有 mission-frame 契约一致；正式 launch 删除半链可配、实际固定的伪
   `mission_frame` 参数。

### 验证结果

- Gate/正式入口定向测试 17/17 PASS；`uav_mission` 全量纯测试 218/218 PASS。
- Catkin 登记测试总计 147/147 PASS；`plan_manage + uav_mission` 和工作树 `uav_vision` 定向构建
  PASS。
- 在只引用当前两个 worktree 的 `CMAKE_PREFIX_PATH/ROS_PACKAGE_PATH` 下，`roslaunch --files
  uav_mission navigation_search_delivery_vcl06.launch` 完整解析 PASS。
- Python 编译、四个相关 launch XML、`git diff --check` PASS；未启动 Gazebo 或实机链路。

### 遗留问题

- 本条只证明正式拓扑可解析、Gate reducer 可执行；尚无真实 ROS graph/目标事务运行证据，
  不能把 `P_interrupt`、三投、返航或落地写成 PASS。
- 历史 baseline/a68925d A/B 结论仍是 FAIL，baseline 默认只表示未推广候选地图 profile。

### 下一步

- 用统一 `sim_run.sh` 先跑固定 seed 的 90 秒 confirm→selected→decision→实际 APPROACH smoke；
  同一 ID 全链成立且无高度/碰撞/发布者问题后，再运行 600 秒硬 Gate。

---

## 2026-08-31：VCL06 worktree 运行叠加修复与首轮硬 Gate

### 改动范围

- `top_level_scripts/sim_run.sh` 的 worktree 编译叠加顺序；
- 现有 `navigation_planner_bridge.py` 的共享 strict 校验模块导入；
- 现有 `navigation_vcl06_assertion.py` 报告语义及对应纯测试；
- 不新增消息、服务、action、任务 manager、执行 bridge、planner goal 入口或兼容节点。

### 具体改动

1. 修复当前 worktree 只有定向 catkin 产物时被误当成完整运行底座的问题：默认 worktree 场景下
   先加载既有 `LIFTRACE_INTEGRATION_WS` 完整 underlay，再以 `--extend` 加载当前 patrol 与
   vision overlay；显式指定其他 `UAV_WS` 时保持原调用者负责的覆盖语义。
2. planner bridge 与既有 release arbiter 使用同一源码目录导入方式，避免 catkin 生成的
   `release_commitment.py` 可执行 wrapper 被 Python 当作普通模块后丢失导出符号；没有复制
   strict 校验实现。
3. Gate 的三项依赖检查不再对空提交集合做 vacuous true：没有三次合法 release commit 时，
   `three_capture_started`、`real_approach_commands`、`committed_targets_were_selected` 明确为
   false，防止 FAIL 报告误显为已观察到目标事务。

### 验证结果

- 当前 patrol worktree 完整 `catkin_make -DROS_EDITION=ROS1 -DCATKIN_WHITELIST_PACKAGES= -j1`
  PASS；首次配置遇到一次系统 Python `re/setuptools` 既有偶发异常，原命令重跑后完整 30 包
  构建通过，未修改系统 Python 或安装依赖。
- Gate/bridge/release 定向纯测试分别 17/17、14/14、6/6 PASS；`uav_mission` 全量纯测试
  218/218 PASS；Catkin 登记测试汇总 147 tests、0 errors、0 failures、0 skipped。
- 45 秒启动预检
  `logs/vcl06_overlay_preflight2_20260831_060405/` 成功保持正式链运行至外部 timeout：seed=11
  随机场五类模型与 truth/footprint READY，baseline anchor READY，接触监视 READY 且 0 碰撞；
  manager、单 bridge、guarded proxy/mock 均正常启动，无缺 executable、消息或模块导入错误。
- 首轮硬 Gate
  `logs/vcl06_typed_smoke90_r2_20260831_060512/gate_status.json` 按合同输出 FAIL，reason 为
  `wall_timeout`。ROS graph 只有 `/navigation/planner_bridge` 一个 `/fastplanner/goal` 发布者；
  1477 个 pose 样本最大高度 2.0753 m，0 碰撞、0 越界、0 超高，tank selected/accepted=0。
  manager 始终未创建 mission，decision/result/selected/release commit 均为 0，因此没有真实
  `P_interrupt`、三投、返航或落地证据。

### 遗留问题

- start gate 的场景、truth、anchor 和 manager-IDLE 条件全部 READY；manager 自身 readiness
  在起飞前拒绝 `pose_altitude_out_of_bounds`，升空后持续拒绝 `map_missing`。本轮
  `/freedom/static_pointcloud` 没有到达 manager；日志同时保留仿真 Livox PointCloud2 缺少
  `intensity/time/ring`、FAST-LIO 跳过 scan 的既有上游现象。
- 该阻断属于仿真 LiDAR→FAST-LIO→FreeDOM/导航地图链，不通过关闭 `require_map`、放宽地图
  新鲜度或在视觉 adapter 伪造地图绕过。历史 baseline/a68925d A/B 仍为 FAIL，600 秒 Gate
  仍未启动。

### 下一步

- 将同 seed、Gate 报告和 `map_missing` 时间线回流导航/建图侧，先恢复带有效时间戳且持续发布
  的 `/freedom/static_pointcloud`；随后原样重跑 90 秒 typed smoke。只有同一 stable ID 的
  selected→decision→实际 APPROACH/target-stage 成立后，才讨论 600 秒三槽 Gate。

---

## 2026-08-31：VCL06 执行面与地图阻断文档同步

### 改动范围

- Roadmap、项目 README、`uav_mission` README；
- VCL06 contract/planner bridge 说明；
- 导航联调总 Handoff、视觉对导航需求及 V-SIM-04 阶段回复；
- 只更新现有权威文档，不新建同义接口说明或改写 `docs/handoff/original/` 原始需求。

### 具体改动

1. 将“live execution bridge 尚未接入”更新为当前事实：导航 PR #6 Draft HEAD `3864a7c` 的
   四个 bridge 提交已等价导入，目标事务/LAND、正式 launch 与只读 Gate 继续使用同一 executor；
   正式图没有新增 msg/srv/action、compat adapter、第二 manager 或第二 planner-goal publisher。
2. 冻结来源映射：导航桥 `c12ee0c/4bef528/5767d95/3864a7c` 分别对应本仓
   `98cb587/83e796b/c7c1d8f/933eb78`，集成目标事务与正式 Gate 为 `e2df599/09d16a8`，运行修复
   为 `09c4c5c`。导航 PR Conversation 仍停在旧 13 commits/`c12ee0c`，明确只需更新描述，
   不为旧描述增设兼容开关。
3. 将 45 秒预检 READY 与首轮硬 Gate `wall_timeout/map_missing` 写入 Roadmap/Handoff/双向回复：
   唯一 goal 发布者及安全零计数成立，但 decision/result=0、`P_interrupt=null`，V-CL-06 继续
   FAIL，历史 A/B 不推广、600 秒不启动。
4. 将下一跨组请求收敛为仿真导航地图：恢复带有效时间戳、`camera_init` frame 且持续新鲜的
   `/freedom/static_pointcloud`；禁止关闭 require-map、放宽 map age 或在视觉 adapter 伪造地图。

### 验证结果

- 只读核对导航 PR #6 commit 页与本地 tracking ref，远端 HEAD 均为 `3864a7c`；后三提交
  patch-id 一致，首提交仅受集成侧 CMake/package/追加台账上下文影响，核心 bridge 源码、配置、
  launch、文档和测试一致；没有尚未导入的新提交。
- 文档引用的预检与 Gate 指标均来自对应 run 目录现有结构化 JSON；未把启动 READY 写成
  target transaction 或任务 PASS。
- `git diff --check` PASS；本条不运行新的 ROS/Gazebo、实机或执行机构。

### 遗留问题

- 导航 PR 描述需由导航组更新；仿真地图链需由导航/建图侧修复并复跑。
- `P_interrupt`、三投、返航、落地、新相机 CameraInfo/外参和 OrangePi ROS 仍分别未验收。

### 下一步

- 导航地图恢复后按当前正式 launch 原样重跑 90 秒，不再扩展接口；通过同 ID target-stage
  smoke 且历史 A/B 安全条件满足后，再支付 600 秒 Gate。

---

## 2026-08-31：新相机 1280×720 内参与 ROS CameraInfo 接入

### 改动范围

- 用户提供的 `vision_ws/calibration.yaml` 原始 OpenCV 标定结果；
- `camera_sdk` 的 ROS CameraInfo profile、相机启动入口、消息 frame 与安装规则；
- 相机 profile 定向测试；不修改 Gazebo 相机内参、导航接口或实机外参。

### 具体改动

1. 原样保存 1280×720、9×6 内角点、方格尺寸 24 的 OpenCV FileStorage 标定结果；内参为
   `fx=725.3510, fy=723.3404, cx=631.6719, cy=397.5664`，五参数 plumb-bob 畸变，RMS
   重投影误差 0.26874 px、平均点误差 0.24506 px。
2. 将同一 K/D 无损转换为 `camera_sdk/param/calibration_1280x720.yaml` ROS CameraInfo 格式，
   补齐 identity R 与 `[K|0]` P；保留旧 1920×1080 profile，不做跨分辨率缩放或覆盖。
3. 新增参数化 `camera_calibrated_1280x720.launch`，固定标定像素域为 1280×720、rotation=0，
   设备列表、话题和 optical frame 仍由 launch 参数配置；安装外参继续通过 TF 单独实标。
4. `camera_sdk.py` 令 Image、CompressedImage、CameraInfo 使用同一 frame_id，并在启动与首路
   图像处理时拒绝标定分辨率、请求分辨率或实际输出不一致；相机参数无法加载时不再继续发布
   缺 CameraInfo 的半链。
5. 补齐 `camera_sdk.py`、launch 和 param 的 Catkin 安装规则及实际运行依赖。

### 验证结果

- OpenCV 原始文件与 ROS YAML 的 K/D 逐值一致；定向 profile 测试 3/3 PASS。
- `camera_sdk` Catkin 白名单构建 PASS；Catkin 登记测试 3 tests、0 errors、0 failures、0 skipped。
- `camera_sdk.py` py_compile PASS；未连接相机、未启动实机飞控或执行机构。

### 遗留问题

- 当前尚缺原始棋盘格图片、标定帧数、方格单位、对焦/焦距档和采集时是否已做软件旋转的记录，
  因此本条只关闭格式转换和数值一致性，不宣称完成实机 CameraInfo/TF Gate。
- 新 profile 只能用于完成本次标定的新相机原生 1280×720 输出；不能套用到 2560×1080 的
  `real_target.mp4`，也不能替代机架安装外参。

### 下一步

- 收到标定原图和采集参数后做边缘覆盖/直线残差复核；连接新相机验证实际输出 1280×720、
  三话题 frame/stamp 一致，再测量 optical-frame 安装外参并接入板端 10 分钟 Gate。

---

## 2026-08-31：视觉检测器缺模型时全链快速失败

### 改动范围

- dev/sim `target_detector.py` 与板端 `target_detector_rknn.py`；
- 既有 Phase-D 两个完整视觉 launch、模型路径纯合同与定向测试；
- 不增加消息、服务、话题、兼容节点或 `require_model` 开关。

### 具体改动

1. 新增唯一的本地模型文件合同：路径必须非空、展开后必须为普通文件；模型仍只能由既有
   launch 参数或 `UAV_VISION_MODEL_PATH` 提供，不把开发机绝对路径写入配置。
2. dev/sim detector 删除“模型或 Ultralytics 不可用时仍发布 completed 空数组”的降级路径；
   缺模型、路径无效或运行时缺失均在建立 publisher/subscriber 前退出。
3. RKNN detector 在没有任何可用 unified/split runtime 时同样退出；有效 runtime 推理得到
   0 个目标时仍正常发布合法空帧，避免把“场景确实无目标”和“根本没有模型”混为一谈。
4. `phase_d.launch` 与 `phase_d_board.launch` 将主检测器标为 required；节点初始化失败会立即
   结束完整视觉 launch。正式 VCL06 因而不可能再进入持续空检测的伪健康状态。

### 验证结果

- 模型合同定向测试 4/4 PASS；Catkin 登记测试 4/4 PASS，连同当前 camera_sdk 结果汇总为
  7 tests、0 errors、0 failures、0 skipped。
- `uav_vision` Catkin 白名单构建、三个 Python 文件 py_compile、launch XML 解析 PASS。
- 实际 unset/空 `UAV_VISION_MODEL_PATH` 启动 `phase_d.launch` 时，detector 以
  `model path is empty` 退出，required 语义立即关闭其余节点；没有再发布空 detection 帧。

### 遗留问题

- 有效 `.pt` 的当前算法/性能证据继续引用 V-SIM-04 camera-only 600 秒 PASS；本条未与正在由
  另一工作进程使用的 Gazebo 资源并发启动新的有效模型 ROS smoke。
- 所有正式运行脚本仍须显式提供模型资产；缺参现在是可见启动失败，不再被空检测掩盖。

### 下一步

- 地图链工作完成后，以现有 merged-standard `.pt` 设置 `UAV_VISION_MODEL_PATH`，先做 30 秒
  detector perf backend=`ultralytics` 预检，再重跑 90 秒 VCL06 typed smoke。

---

## 2026-08-31：实拍回放、人工定量真值与新内参状态同步

### 改动范围

- Roadmap、README、迁移清单、责任边界和导航联调 Handoff；
- 只收敛既有证据口径，不修改算法阈值、模型、导航接口或 Gate 判据。

### 具体改动

1. 明确 `real_target.mp4` 已完成整段 4623 帧模型推理、完整视觉链回放、OrangePi RKNN 抽样
   和人工审片，应计入实拍域功能验证，不再笼统写成“缺实拍”。
2. 将剩余项精确命名为“实拍人工定量真值”：现有 58.80% 是预测框—算法圆环候选自洽率；
   summary 的 `ground_truth.provided=false/matched_rows=0`、待填模板和缺同步 pose 说明它不能
   产生人工召回、错配、中心绝对误差或地图误差。
3. 更新新相机状态为 OpenCV YAML 已入库、ROS profile 已转换且数值测试通过；仍保留标定原图/
   采集参数复核、真实相机 frame/stamp、安装外参、OrangePi ROS 及 10 分钟 Gate。
4. 登记正式 VCL06 首轮 run 的空模型配置错误及当前 fail-fast 修复；下一轮把有效模型 backend
   与新鲜地图并列为预检，不再把 decision=0 单独归因于 manager。

### 验证结果

- 视频规格与既有评测文档/summary/template/板端抽样结果逐项交叉核对；没有重跑或重解释模型
  预测数值。
- Roadmap 20 项状态总数未改变：内参“文件/格式接入”完成，但真实 CameraInfo/TF、人工定量
  真值和板端 Gate 仍属同一开放任务。
- `git diff --check` PASS；本条未启动仿真、相机、飞控或执行机构。

### 遗留问题

- 新相机与旧手机视频不是同一成像域；不能用 1280×720 新内参为 2560×1080 视频补地图真值。
- 若要量化旧视频，至少需人工填实例/中心标注；若要量化地图误差，必须另采同步 pose/TF 数据。

### 下一步

- 优先完成地图+有效模型的 90 秒联合 smoke；随后接真实 1280×720 相机验证 CameraInfo/frame，
  并从现有视频抽取代表帧填第一批人工类别、实例和中心标注。

---

## 2026-08-31：新相机标定验收口径收敛

### 改动范围

- Roadmap、迁移清单、导航联调 Handoff 与两份组间需求/回复文档；
- 1280×720 ROS CameraInfo profile 的说明注释；不修改 K/D/R/P 数值或相机运行代码。

### 具体改动

1. 按用户决策，将 `vision_ws/calibration.yaml` 直接作为当前新相机内参权威输入；已完成的
   ROS profile 接入保持不变，不再安排独立标定正确性复核。
2. 从现行任务和组间依赖中移除标定原图、方格尺寸单位、对焦和拍摄旋转记录；这些材料仅在
   将来重新求解或独立复核内参时有用，不再阻塞本轮视觉、导航或板端推进。
3. 将剩余工作限定为运行接线：真实输出保持 1280×720、Image/CameraInfo frame 与时间戳
   一致、安装外参与 optical→body→mission TF、OrangePi 持续发布和稳定性验收。
4. 本条明确更正本日志前两条中“仍待标定原图/采集参数复核”的遗留项和下一步；历史记录
   保留不改，但该要求自本条起不再有效。

### 验证结果

- 文档口径已统一区分“内参采用完成”和“真实相机/外参/板端接线待验收”。
- CameraInfo 数值未变化；本条不需要重跑标定、算法或仿真。
- `git diff --check` PASS；未连接相机、飞控或执行机构。

### 遗留问题

- 尚未在真实相机上确认 1280×720 输出及 Image/CameraInfo frame、时间戳一致性。
- 机架安装外参、OrangePi ROS 实时链和同步 pose 的新相机实拍定量数据仍未验收。

### 下一步

- 不再回头索取标定采集材料；收到可用硬件窗口后只做运行接线和安装外参验收。当前继续优先
  恢复地图链并以有效模型重跑 V-CL-06 90 秒 typed smoke。

---

## 2026-08-31：V-SIM-04 数据与远程分支移交索引

### 改动范围

- 新增独立的 V-SIM-04 仿真实验数据、需求完成度和远程分支关系移交文档；
- 在导航 Handoff 增加入口并修正顶部 bridge 状态；
- 同步 V-CL-06 README 的模型 fail-fast 口径和显式模型复现参数。

### 具体改动

1. 逐项对照本地主工作区 `视觉组需求.md`，登记 formal23、static25、sparse30、固定 seed 重复、
   camera-only 600 秒和 V-CL-06 联合 FAIL 的物理路径、原始 revision、关键指标和判定边界。
2. 明确 A 静态 25/25 格、B 高度×速度 38/100 格、C 横偏与 D 非零偏航/转弯/多目标未完成；
   visual-only `P_interrupt=null`，不得写成导航中断概率。
3. 登记 `logs/`、test_data、模型和视频均不随 Git push；给出约 30 MB 核心 run、repeat 三源
   run、5.2 MB 模型和可选实拍视频的外部移交边界与接收检查清单。
4. fetch/prune 后将远程分支分为已在 main、活动堆叠、冻结实验三类，说明 execution 严格包含
   VCL05/navigation/performance/next，历史 V-SIM 专项已被修订吸收，不应重复 MR。
5. 明确“可现在开 Draft PR review”和“尚不可合 main”的区别，并给出 stacked review 与
   Gate 后逐层 retarget/合并顺序，避免再增加兼容接口或重复分支。

### 验证结果

- 核心 run 的 report/summary/manifest/六件套存在性、目录大小、模型与 revision 已只读核对；
  repeat 汇总对应的三个源 run 均存在。
- `git fetch --prune origin` 后，10 条远程保留分支已是 main 祖先；5 条活动远程分支严格堆叠
  到 execution；三条 oblique 实验链仍未吸收且按主线决策冻结。
- GitHub 当前无 PR 记录；`git diff --check` PASS。未重跑仿真，未连接相机、飞控或执行机构。

### 遗留问题

- 核心实验数据和模型尚未上传到 Git 之外的共享位置；本文只完成索引，不能替代数据包。
- LiDAR→FAST-LIO→FreeDOM 真正修复仍在另一 worktree 的未提交修改中，V-CL-06 仍为
  `map_missing` FAIL。
- 30-seed、真实 `P_interrupt`、横偏 C、视角/转弯/多目标 D、新相机实采和 OrangePi ROS
  仍未关闭。

### 下一步

- 地图修复提交后，以有效模型先做 30 秒预检并重跑 90 秒 typed smoke；同时由人工决定外部
  数据包上传位置和是否立即创建 Draft stacked PR。

---

## 2026-08-31：恢复 V-CL-06 仿真激光地图链并同步 PR #6 状态

### 改动范围

- 仿真专用 FAST-LIO 点型配置与预处理，不修改实机 `mid360.yaml`；
- `iris_mid360_downward_camera` 的传感器碰撞保护环、Gazebo 模型搜索路径；
- FreeDOM 静态地图消息时间戳；
- `uav_mission` 地图合同回归测试及 V-CL-06 活跃文档；
- 导航仓 PR #6 状态与说明同步；不合并 `main`，不启动实机、飞控动作或执行机构。

### 具体改动

1. 确认 Gazebo MID360 发布的是 XYZ-only `PointCloud2`，将仿真 profile 的 `lidar_type` 改为
   专用类型 4，并在 FAST-LIO 仿真 handler 中按 `pcl::PointXYZ` 解码、将 intensity 置零；实机
   MID360 配置继续保持类型 1。
2. 将曾遮挡射线原点的实心圆柱碰撞体替换为仓内闭合八边形保护环；保护环整体位于射线原点
   上方，保留 `/bumper_states` 接触包络，同时让浅向下激光束通过。
3. 令 `patrol_world.launch` 优先搜索仓内 `patrol_control/models`，避免同名 PX4 模型抢先；
   FreeDOM 发布 `/freedom/static_pointcloud` 时补齐当前 ROS 时间戳。
4. 新增 5 项静态合同回归，覆盖仿真/实机 profile 分离、XYZ 解析、保护环接触插件与几何净空、
   模型路径优先级和地图时间戳，并登记到 `uav_mission` Catkin 测试。
5. 将 README、ROADMAP、CHECKLIST、V-CL-06 合同/桥接/HANDOFF/组间回复统一到最新事实：地图
   阻断已解除，但目标事务和整场 Gate 尚未通过；历史 `map_missing` 记录不改写。
6. 导航 PR #6 保持运动桥范围，更新到 `d95377c` 并从 Draft 转为 Open/Ready；PR 描述明确
   集成层 target transaction、LAND、stop ACK 和系统地图 Gate 不属于该 PR，尚未合并 `main`。

### 验证结果

- 主工作区 30 包完整 Catkin 构建 PASS；`uav_mission` Catkin 测试 152 tests、0 errors、
  0 failures、0 skipped；新增地图合同脚本独立运行 5/5 PASS；`gz sdf -k` PASS。
- `logs/vcl06_map_guard_fix_preflight3_20260831_124042/` 中 `/livox/lidar`、
  `/cloud_registered_body`、`/freedom/static_pointcloud` 分别约为 20000/7900/5600 点；静态地图
  为 `camera_init`、非零时间戳并连续约 10 Hz，接触监视 READY、碰撞计数为 0。
- 最终 90 秒 `logs/vcl06_map_guard_fix_gate90_v3_20260831_125136/` 中注册点保持约 11000，
  地图约 10 Hz、pose 约 30 Hz、延迟约 1 ms；运行中没有 `map_missing`/`map_stale`，
  `contract_errors_zero=true`，碰撞、越界和超高均为 0，最大高度 1.886 m。
- Gate v3 真实结论仍为 FAIL（`wall_timeout`）：只有 3 个 decision/5 个 result，未产生 selected、
  APPROACH、target transaction、投递、返航或 LAND，`P_interrupt=null`；没有把地图恢复写成
  V-CL-06 整体 PASS。

### 遗留问题

- `start_gate_started_once=false` 仍在 evaluator failed checks，尽管运行日志可见 start accepted；
  需核对订阅/锁存时序并与真正的任务启动次数分开判断。
- Gate v2 曾出现一次启动阶段 `pose_stale`，v3 未复现；需在后续复跑继续观测。
- `/bumper_states` 已连续 READY 且本轮零碰撞，但尚未用正向障碍碰撞验证保护环接触响应。
- 目标未进入 selected/APPROACH/同一 stable ID 事务，600 秒三投、返航、LAND 尚未启动；
  新相机实机 CameraInfo/外参、OrangePi ROS 和执行机构均不在本轮验证范围。

### 下一步

- 沿 candidate→manager decision→planner result→target transaction 路径定位 90 秒内为何未进入
  APPROACH；修复并取得同一 stable ID 的 STARTED/CAPTURE 证据后，再启动 600 秒三投 Gate。
- 地图修复以独立、可审阅的跨组 PR 同步到导航仓，避免扩大已 Ready 的 PR #6 运动桥范围。

---

## 2026-08-31：接收视觉仓 PR #1 并生成导航需求群聊移交包

### 改动范围

- 独立复核导航侧提交到视觉仓的 PR #1，并仅合入 V-CL-06 execution feature 分支；
- 更新 V-SIM-04 数据、V-CL-06 修复前后证据和远程分支关系索引；
- 将导航需求原文、双方回复、结构化实验数据、复现入口、相机内参和模型打为 Git 外群聊附件。

### 具体改动

1. 核对 PR #1 只包含仿真 LiDAR→FAST-LIO→FreeDOM 地图恢复、保护环模型和合同回归，未新增
   消息、兼容 adapter 或 planner goal 发布者；以双父 no-ff merge commit `8b3d289` 合入
   `feat/vcl06-execution-integration`，没有触碰 `main`。
2. 在 PR 留下独立验证和整场仍 FAIL 的说明后关闭；GitHub 已识别该 PR 为 merged，避免后续
   再次点击 Merge 重放相同提交。
3. 更新 `docs/V-SIM-04仿真实验数据与分支移交索引_20260831.md`：历史 `map_missing` 与
   post-fix `wall_timeout` 分开登记，分支 tip 更新为 `b83342d/8b3d289`，后续工作改为直接定位
   candidate→decision→result→target transaction，不再复制地图修复或增加兼容接口。
4. 生成 `deliverables/liftrace_nav_to_vision_handoff_20260831.zip`：包含需求/回复/合同、四个核心
   V-SIM run、repeat 聚合及三源 run、八组 V-CL-06/A-B 证据、复现 launch/config、两份相机
   内参、实拍/板端摘要与 `best.pt`；包内外分别提供 SHA256。该 ZIP 由 `.gitignore` 排除，
   只作群聊移交，不进入 Git。
5. 为控制附件体积，排除 bag、录屏、`roslog/`、构建缓存和标记视频；344 MB 原始
   `real_target.mp4` 保持本机原件，需时作为独立附件发送，不冒充人工真值数据。

### 验证结果

- PR 增量 `git diff --check` PASS；地图合同 5/5 PASS；`uav_mission` 152 tests、0 errors、
  0 failures、0 skipped；FAST-LIO、FreeDOM 定向构建和 `gz sdf -k` PASS。
- PR head `b83342d` 是 execution `8b3d289` 的祖先，后者为 `ad93d36+b83342d` 的 no-ff merge；
  远程分支已推送，PR 已收口，`main@7e626ad` 未变化。
- 群聊 ZIP 为单一 ASCII 根目录，大小 6.9 MiB；外层 SHA256、`unzip -tq` 和包内逐文件
  `MANIFEST.sha256` 校验通过；必选文档、六件套、repeat 三源 run、JSON/YAML/CSV 与模型齐全。
  独立验收发现 15 份历史外层 manifest 的 `launch_cmd` 本来就不是合法 YAML；包内将原字节
  保留为 `manifest_legacy.txt`，并以记录原件 SHA256 的可解析 `manifest.yaml` 包装，未回写源 run。
- 修复后 `gate90_v3` 仍原样为 `FAIL/wall_timeout`：decision=3、result=5，
  selected/APPROACH/capture/release=0，0 碰撞、0 越界、0 超高；未将地图恢复写成整场 PASS。

### 遗留问题

- post-fix 联合证据发生在修复提交前的 dirty worktree，尚缺 `8b3d289` 干净 HEAD 的等价 30 秒
  预检和 90 秒同 ID 完整链复跑。
- 真实 `P_interrupt`、600 秒三投/返航/H 落地、横偏 C、偏航/转弯/多目标 D、30-seed、新相机
  实时 ROS 链和 OrangePi ROS 仍未关闭。
- `real_target.mp4` 因体积未进入群聊 ZIP；包内只交付回放摘要和小型 CSV，且明确不具备人工
  实例/中心真值与同步 pose。

### 下一步

- 在 `8b3d289` 干净基线上显式传入模型完成预检，定位为什么 target 未进入
  selected/APPROACH/target transaction；90 秒同 ID 链通过后再启动 600 秒 Gate。

---

## 2026-09-02：新增实拍视频标注入口并准备新相机板端视觉链

### 改动范围

- 为 `real_target.mp4` 增加当前 `uav_vision` 生产检测、几何精修、融合与标注回放入口；
- 仅处理图像像素域，不伪造相机位姿、TF、地图点、stable ID、选靶或控制结果。
- 接入已完成的 1280×720 相机内参，准备 OrangePi USB/RKNN/ROS 纯视觉部署入口；不连接飞控、
  舵机或任务控制。

### 具体改动

1. 新增逐帧 ACK 的 ROS 视频发布器与单文件标注器，保持源媒体帧率和像素坐标系，避免推理较慢时丢帧。
2. 新增 `video_replay_annotation.launch`，复用生产 YOLO、十字、圆环、降落检测、搜索/降落融合和
   `target_refiner`；输出颜色和顶部计数区分 raw、geometry、fused、refined 与 reject 状态。
3. 补充回放合同单测、launch/CMake 安装项和使用说明；模型继续显式传入，缺失时 fail-fast。
4. 相机节点新增采集 FPS 参数并记录实际协商的尺寸/FPS/FOURCC；板载查看器从首个真实帧确认
   分辨率后才宣布 READY，统一使用 `UAV_VISION_RKNN_MODEL_PATH` 和包内相对模型路径。
5. map projector 在 raw 像素投影射线前按 CameraInfo 做畸变校正，并通过 dev/board launch
   显式透传开关；新增相机+RKNN 统一纯视觉入口，debug image 默认关闭。
6. 新增自包含 OrangePi 部署包生成器和实验室清单；SSH 认证改为 key/交互/仓外密码文件，
   移除脚本内旧密码，`ssh_board_cmd.py` 作为后续唯一公开入口，其余仅保留兼容包装。

### 验证结果

- 3 项视频回放合同单测 PASS，launch XML PASS，`uav_vision` 定向 Catkin 构建 PASS。
- 相机标定 profile 3 项单测、板载查看器合同、map projector 畸变校正断言、相关 launch XML 和
  `camera_sdk` 定向 Catkin 构建 PASS；SSH 脚本 `py_compile/--help` PASS。
- 30 帧端到端烟测 PASS；全量运行读取源视频 4622 个可解码帧并逐帧写出 4622 帧，输出为
  2560×1080、59.985 FPS、77.053 s，生产模型正常加载且标注器正常 finalize。
- 产物位于 `vision_ws/test_data/real_target_ros_pixel_chain_20260902.mp4`，另生成 H.264 review 版；
  视频与模型资产均由 `.gitignore` 排除，不进入 Git。

### 遗留问题

- 本轮按用户约定暂时视 YOLO 类别检测为正确；输出供人工观察红十字/圆环中心和各阶段拒绝原因，
  不等价于带人工实例真值的召回率或地图误差验收。
- 文件回放没有同步 pose/TF，因此不运行 map projector、target memory 和任务控制。
- OrangePi 尚未连接；USB 协商、真实相机图像、RKNNLite/NPU、板载显示、ROS CameraInfo/TF 和
  10 分钟稳定性必须等用户到实验室后实测，当前不能写成板端 PASS。

### 下一步

- 用户人工审片后，针对明显中心偏移或关联拒绝做最小复现；相机到实验室后改用真实 CameraInfo/TF
  验证相同视觉链，不从该视频推导实机地图精度。

---

## 2026-09-02：建立 V-SIM-04 横偏 C25 与轨迹/关联 D50 诊断面

### 改动范围

- 落地导航组需求中的 C 横向偏差 25 工况和 D 偏航/转弯/多目标 50 工况设计；
- 复用现有 V-SIM-04 runner、recorder、六件套和生产视觉链，不新增 ROS 消息或组间话题。

### 具体改动

1. C25 固定 seed=11、高度 2.4 m、速度 1.0 m/s，覆盖 5 类 × center/左右 75%/左右部分入画；
   按目标实际尺寸与视场角计算路径，完整入画指标和 partial 可见窗口指标分开报告。
2. frames/performance/summary 追加目标横向米制偏差、归一化像素偏差及 lateral breakdown；旧列顺序
   保持 append-only。formal23 和 C25 使用冻结 design ID，未知/85 工况矩阵自动降为 diagnostic，
   防止任意自报 trial 数冒充完整 MEASURED。
3. D50 冻结 40 条偏航×运动×构图 pairwise 和 10 条定向多目标设计，使用真实 640×480、
   HFOV=1.211 rad 相机模型生成带完整 quaternion 的 source-time 轨迹；D 始终 diagnostic-only。
4. D runtime 只允许经场界、目标四角完整入画 enter→leave 和 framing 预检的 16 条工况；20 条
   edge/partial、1 条越界、3 条观察窗不闭合和 10 条多目标均明确 NOT_RUN。现有世界仍为五靶
   clutter，`single_pairwise` 只表示主评目标，不声称隔离单靶。
5. runner 仅新增 `matrix_kind` loader、runner type 和 `_set_trajectory_start` 三个薄扩展点；
   D 首姿态在采样握手前完整设置，避免固定 RPY 帧污染试验。

### 验证结果

- V-SIM-04 metrics/schema 断言 PASS；C25 dry-run 25/25 并输出六件套，正式成功率阈值未冻结，
  即使数据完整也只能 `NOT_GATED`，不能伪报 Gate PASS。
- D50 9 项单测、Python 编译、launch XML、wrapper shell 语法、roslaunch 参数解析、metrics 断言、
  pairwise 覆盖和 dry-run 50/50 PASS；dry-run 明示 `gazebo_execution_status=NOT_RUN`。
- `git diff --check` PASS；复核确认没有新增视觉—导航接口或第二套评测/控制状态机。

### 遗留问题

- C25 和 D 的 16 条白名单均尚未启动 Gazebo，当前只是可执行设计与 fail-closed runner 预备面。
- D 的 edge/partial 需要独立观察窗合同，多目标需要 spawner/truth/stable-ID 关联接线；在这些完成前
  34 条保持 NOT_RUN，不把设计覆盖写成算法完成。

### 下一步

- 用统一 `sim_run.sh` 先跑 C25 全 25 条，再跑 D 默认 `d_single_01` 和 16 条 supported 诊断面；
  只将真实六件套结果写回状态，失败时按同 seed 最小重放，不扩大视觉—导航接口。

---

## 2026-09-02：修复零畸变 CameraInfo 的地图投影 NaN

### 改动范围

- 修复新 raw 像素校正路径与 Gazebo 零畸变 CameraInfo 的兼容问题；不改变真实相机非零畸变路径。

### 具体改动

1. `target_map_projector` 从 CameraInfo 判断是否存在非零畸变系数；仅在确有畸变时调用
   `rectifyPoint()`，空 `D`/全零 `D` 直接使用原像素射线。
2. 畸变校正断言新增零畸变回归，继续覆盖非零畸变校正和显式关闭两条路径。

### 验证结果

- 首轮 C25 真实 run `logs/vsim04_c25_seed11_20260902_015235/` 六件套完整、25/25 MEASURED，
  但 verdict=FAIL：15 个完整入画 trial 的首阻断均为 map projection，map-invalid=1.0、
  `P_confirm=0`；manifest 显示 CameraInfo `D=[]`，mapped payload 出现 NaN，故保留为失败诊断证据。
- 零/非零畸变定向断言 PASS，`uav_vision` 多核定向 Catkin 构建 PASS，`git diff --check` PASS；
  同 seed C25 仍必须复跑，不能把本次数据当算法性能结论。

### 遗留问题

- 真实相机非零畸变仍需到实验室用已标定 CameraInfo 实测；当前只关闭 Gazebo 空 `D` 退化。

### 下一步

- 提交最小修复后立即复跑 seed=11 C25，并将首轮 FAIL 与修复后结果同时纳入视觉需求提交包。

---

## 2026-09-02：修复统一仿真外层 manifest YAML 序列化

### 改动范围

- 修复 `sim_run.sh` 对 launch 命令参数的 YAML 写法，供后续 C/D 与提交包直接解析。

### 具体改动

1. 用 Python 标准库 JSON 字符串（合法 YAML scalar）逐参数写出 `launch_cmd` 和
   `roslaunch_args` 列表，保留空格、引号和 roslaunch substitution，不再用破坏参数的 `sed`。
2. 历史 run 原始 `manifest.yaml` 不回写；提交包仍将它们标为 legacy，修复后的新 run 使用合法 YAML。

### 验证结果

- 首轮 C25 外层 manifest 复现同一旧缺陷，不能直接作为 YAML 消费；修复后无 ROS 的
  `sim_run` manifest smoke 退出 0，`yaml.safe_load` 精确还原命令列表，shell 语法检查 PASS。

### 遗留问题

- 旧 A/B/C 运行的外层 manifest 仍需在提交包内保留原字节并由 wrapper 描述，内层六件套不受影响。

### 下一步

- 静态验证通过后提交，再启动 C25 retry 和 D，确保新 run 外层 manifest 可直接解析。

---

## 2026-09-02：修复 D50 批量选择器默认值冲突

### 改动范围

- 修复 D50 `supported` 批量入口启动即退出的问题；不改评测矩阵、视觉算法或组间接口。

### 具体改动

1. `run_vsim04_d50.sh` 不再传递会被 ROS Noetic 丢弃的空值 remap；单项只传一个非空
   selector，批量从冻结矩阵取得 16 个 runtime-supported ID 并合成一个非空 selector。
2. `vsim04_d50_single.launch` 保留直接启动时的安全 `d_single_01` 默认值；保持
   `trial_selector` 与 `trial_slice` 互斥校验不变，不增加兼容分支。

### 验证结果

- `d_single_01` 实跑 `logs/vsim04_diag_d50_d_single_01_seed11_20260902_020734/`
  六件套完整，1/1、`P_confirm=P_selected=1.0`、TF failure=0，诊断运行退出 0。
- 首次批量运行 `logs/vsim04_diag_d50_supported_seed11_20260902_020816/` 在 trial 开始前
  fail-closed；runner 日志明确报 `trial_selector and trial_slice are mutually exclusive`。
  Torch 导入阶段的 `KeyboardInterrupt` 是 roslaunch 收尾结果，不是模型推理根因。
- launch XML 解析 PASS；同 seed supported 复跑
  `logs/vsim04_diag_d50_supported_seed11_20260902_021311/` 退出 0，16/16 完成、
  `P_confirm=P_selected=1.0`、TF failure=0、地图误差 P95 0.0833 m、处理 P95 104.62 ms，
  六件套完整且 validation errors 为空；判定按设计为 `DIAGNOSTIC_ONLY`。

### 遗留问题

- D50 仍为 diagnostic-only；34 条未接线/几何预检不通过工况继续保持 NOT_RUN。

### 下一步

- 把 A/B、C25 和 D 的真实六件套更新到面向导航组的提交包；D 剩余 34 条继续逐项保留
  NOT_RUN 原因，不将 16/16 写成 D50 全矩阵完成。

---

## 2026-09-02：更新视觉组 A/B/C/D 导航回复提交包

### 改动范围

- 按导航组《视觉组需求》刷新阶段回复、数据索引、Roadmap 和 Git 外群聊提交包生成入口；
- 复用先前 A/B 真实六件套，新增 C25 与 D50 本轮真实结果，不新增 ROS 接口。

### 具体改动

1. 明确视觉 A/B 与导航地图 profile A/B 是两个实验：A static25 为 25/25 覆盖、24/25 成功；
   B 仅由 formal 动态 8 格和 sparse30 30 格组成，共 38/100 个唯一条件，不宣称完整曲面。
2. C 当前结果只采用修复后 `020233`；首轮 `015235` 仅在 diagnostics 下解释空 `D` 投影修复。
3. D 写入 `021311` supported16 实跑；34 个 edge/partial/panzer/multi-target 设计继续 NOT_RUN，
   visual-only `P_interrupt` 继续为 null。
4. 新增可复现打包脚本，收录原始需求、回复/索引、A/B/C/D 六件套、矩阵/launch/runner、
   新相机 CameraInfo、V-CL-06 边界和本轮 PT 模型；标注视频只记录路径并单独传输。
5. 历史非法外层 manifest 原样保存为 `manifest_legacy.txt` 并附可解析 wrapper；新 run 直接保留
   合法 YAML。包内生成逐文件清单并做 ZIP 完整性检查；Git 忽略 ZIP 的外层 checksum 文件。

### 验证结果

- D 矩阵 9 项单测、两个 wrapper selector 静态展开、shellcheck、shell/launch 语法、
  `git diff --check` 均 PASS；supported 只形成一个含 16 个 ID 的非空 selector。
- 视觉工作区清空 whitelist 后以 `catkin_make -DCATKIN_WHITELIST_PACKAGES= -j8` 完成
  camera_sdk、detect_pkg、uav_vision、uav_vision_eval、yolov5_detect 全五包构建。
- 最终 ZIP 尚待本记录对应提交后生成，确保包内文档和脚本来自冻结 revision。

### 遗留问题

- B 剩余 62/100、D 剩余 34/50、正式 30-seed、多目标逐实例关联和真实导航
  `P_interrupt` 尚未完成；提交包不得把这些空白写成通过。
- 标注后实拍视频约 296 MiB（H.264 review copy），按体量与证据语义独立于结构化 ZIP 传输。

### 下一步

- 提交当前文档和生成器，构建 ZIP，核对包内六件套、摘要口径、checksum 和解压测试；不推送远端。

---

## 2026-09-02：修复 OrangePi 部署包构建与十字准星标定接线

### 改动范围

- 修复 Ubuntu 20.04 上视觉部署包无法生成的问题；
- 让红十字调试准星使用 CameraInfo 主点，不改目标检测、地图投影或组间消息接口。

### 具体改动

1. 部署包脚本不再调用 Ubuntu 20.04 `install(1)` 不支持的 `-L`，改为显式解引用 catkin
   工作区符号链接后复制普通文件。
2. `cross_detector_node` 在合法 CameraInfo 到达后用 `cx/cy` 更新调试与偏移参考点；非法内参
   保持 launch 参数回退值并限频告警。
3. 红十字几何回归新增主点准星断言，并等待同时间戳 debug 图，避免检测回调与 debug 回调
   先后到达造成竞态误报。

### 验证结果

- `test_calibrated_profile.py` 3/3、`test_board_viewer_calibration.py` PASS。
- `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision -j8` PASS。
- `catkin_make run_tests_uav_vision -j8` 后 `catkin_test_results` 汇总 21 tests、0 errors、
  0 failures；`git diff --check` 与部署脚本 `bash -n` PASS。

### 遗留问题

- 本条只保证部署包可构建和新内参调试显示一致；OrangePi 的 RKNN、USB 相机、ROS 实时链与
  10 分钟稳定性仍需在板端实跑。
- 实拍历史视频并非新相机标定源，文件回放不得套用 1280×720 新内参。

### 下一步

- 从本提交构建并部署冻结 ZIP，在 OrangePi 上分别完成历史视频 RKNN 回放、新 USB 相机
  CameraInfo/ROS 链和稳定性验证；全程不启动飞控、舵机或投递节点。

---

## 2026-09-02：接通实拍视频 RKNN ROS 回放证据链

### 改动范围

- 在现有逐帧回放入口增加 RKNN 后端，复用既有检测、融合、精修、投影和性能消息；
- 不新增跨组接口，不启动 target memory、导航、飞控或执行机构。

### 具体改动

1. `video_replay_annotation.launch` 默认继续使用原 PT 像素链；显式选择 `rknn` 时改用
   `target_detector_rknn.py`，并接通 mapped/perf 两个既有输出。
2. 文件视频没有同步位姿和安装外参，因此 RKNN 回放只启动无 TF 的投影器，使用独立不可达
   map frame，并禁止 latest-TF fallback；所有 mapped 结果必须保持 `map_valid=false`。
3. recorder 对同一时间戳等待 raw 四源、search/landing resolved、refined、mapped 和 RKNN perf
   后才确认下一帧；带框视频明示 RKNN backend 及 mapped valid/invalid 计数。
4. 回放契约补充扩展阶段完整性、地图失败关闭和 RKNN perf 校验；PT 默认行为保持不变。
5. OrangePi 清单将 catkin 构建显式固定到系统 OpenCV 4.2，避免历史 `/usr/local` OpenCV 3.4
   与 ROS Noetic `cv_bridge` 的 4.2 ABI 混链。

### 验证结果

- 回放契约测试 6/6、相关 Python `py_compile`、launch XML、PT/RKNN 两套 `roslaunch --nodes`、
  RKNN 参数展开和 `git diff --check` 均 PASS。
- PT 仍为原 9 个节点；RKNN 只替换 detector 并增加 fail-closed projector，共 10 个节点，
  未出现 memory、selected target、导航或控制节点。
- OrangePi 视觉工作区以 OpenCV 4.2、Release、`-j4 -l4` 编译 PASS；板端 FP32 RKNN 视频
  烟测 381 帧，推理总耗时 P50 59.1 ms、P95 62.8 ms、推导吞吐约 16.9 FPS，六类均有输出。

### 遗留问题

- 实拍视频不属于新相机 1280×720 标定源，回放刻意不套用新内参；它只能验证识别和
  ROS 消息/失败关闭链，不能证明地图定位或 stable ID。
- 本提交后的板端 RKNN ROS 逐帧实跑、新相机实时 ROS 链和 10 分钟稳定性仍待完成。

### 下一步

- 重新生成冻结部署包并在 OrangePi 上先跑短帧 RKNN ROS 回放，再跑新相机实时链和稳定性；
  记录 mapped 全部无效和 perf 全部来自 `rknn_unified` 的实际结果。

---

## 2026-09-02：修复板端 RKNN-ROS 日志兼容并压缩相机采集开销

### 改动范围

- 修复 RKNNLite 2.3.2 与 rospy 日志初始化冲突；
- 相机压缩图改为按订阅需求编码，不改话题、图像内容、内参或检测接口。

### 具体改动

1. RKNNLite 导入后恢复 Python 标准日志级别名称，避免其将 `DEBUG/INFO/...` 改成单字母后
   令 `rospy.init_node()` 报 `Unknown level: 'DEBUG'`；保留 RKNN runtime 和模型原配置。
2. 对未配置的旧 split RKNN 路径保持静默，只对显式给出但不存在的文件告警；无检测首帧
   的日志改为“decode/threshold 后无结果”，不再误报输出格式无法解码。
3. `camera_sdk` 仅在 `/camera/image_raw/compressed` 有订阅者时执行 JPEG 编码；raw 图、
   CameraInfo 与有订阅者时的 compressed 图仍共用同一时间戳和 frame_id。

### 验证结果

- 相机 profile 3/3、模型启动契约 4/4、相关 Python `py_compile` 与 `git diff --check` PASS。
- 板端同一 FP32 模型完整逐帧回放 4617 个计入帧：总耗时 P50/P95 为 60.07/63.65 ms，
  推导吞吐 16.65 FPS，六类均有检测，357 MiB 带框视频已拉回本机。
- RKNN ROS 回放 900 帧逐帧 ACK 全部完成，perf=900；前 15 秒得到 689 个 mapped 检测，
  689 个均因无视频同步 TF 而无效，map valid=0，符合失败关闭设计。
- 新 USB 相机 30 秒 ROS 实测：启用 compressed 订阅时 raw/info/compressed 约 21.6 Hz、
  RKNN 333 帧、处理 P95 90.32 ms；取消 compressed 订阅后 raw/info 29.16 Hz、RKNN 401 帧、
  处理 P95 75.94 ms，均为 `rknn_unified`/OK，且无飞控、舵机、PX4 或控制节点。
- 板端 GUI raw 口径约 12.5 FPS；启用新内参整帧去畸变后约 10.9 FPS，去畸变本身约
  9–12 ms。另录制两段 1280×720@30、600 帧、精确 20 秒原始/去畸变视频，读取失败均为 0。
- 删除 32.36 GiB 可再生旧缓存、旧 VS Code Server 和历史带框输出后，板端根盘由 85%
  降至 71%；原视频、FP32 模型、最新 Server、源码、标定和当前部署均保留。

### 遗留问题

- 板端 RKNN runtime 1.5.2 低于模型/Toolkit 2.3.2，当前实推稳定但仍有版本不匹配告警；
  不在已有实跑正常的情况下盲目升级系统运行库。
- 当前相机朝向室内顶棚；实时链验证的是设备、内参、吞吐和失败关闭，不能替代靶标实景、
  安装外参或真实飞行验证。
- OrangePi 新相机完整视觉链的 10 分钟稳定性仍待本提交部署后执行。

### 下一步

- 从本提交重建部署包，确认板端 revision 和源码一致后，在无 compressed 订阅模式运行
  600 秒并记录图像/RKNN 心跳、延迟、USB 错误、温度和节点存活。

---

## 2026-09-02：完成 OrangePi 新相机 RKNN ROS 600 秒稳定性与性能报告

### 改动范围

- 在冻结视觉 revision `abc5103bcd0d4c8d3331f64d9f3bf0cce1bab177` 上完成新 USB 相机、
  CameraInfo、统一 FP32 RKNN 和现有 ROS 视觉链的板端长稳验收；
- 新增全面板端性能报告，并同步 Roadmap 与现场实验清单；不改导航、飞控、控制或执行接口。

### 具体改动

1. 从干净提交重建并部署板端 ZIP，以系统 OpenCV 4.2、Release、`-j4 -l4` 编译通过；部署
   目录的 `VISION_REVISION` 与受测提交完全一致。
2. 先完成一次 600 秒运行。用户确认中途碰过相机线，内核记录一次真实 USB disconnect；
   相机节点约 2.81 秒后自动重开并继续推理。该轮保留为外力断连恢复证据，不作为清洁 Gate。
3. 同 revision、无人触碰完成 rerun1；每 10 秒记录温度、内存、负载和 ROS 存活，后 213 秒
   另以 sysfs/pidstat 记录 NPU、CPU 频率、cooling state 与逐节点 CPU/RSS。
4. 冻结 revision 另做 15 秒 compressed 订阅回归，验证 JPEG 按需编码仍保持 raw、CameraInfo、
   compressed 三话题同 stamp，不破坏远程显示入口。
5. 新增 `docs/OrangePi板端视觉性能报告_20260902.md`，汇总 USB/V4L2、离线视频、ROS 回放、
   JPEG A/B、GUI、20 秒去畸变对比、两轮长稳、逐节点资源、Gate 边界和证据索引，并同步
   README、Roadmap 与现场实验清单中的板端状态。

### 验证结果

- 本地相机 profile 3/3、模型契约 4/4、`uav_vision` 24 项测试、Python `py_compile`、
  `git diff --check` 均 PASS。
- 清洁 rerun1 wall 601 秒、runner exit 0：raw 17,498 条/29.164 Hz，CameraInfo 17,499 条/
  29.163 Hz，RKNN perf 8,223 条/13.705 Hz；processing P50/P95 62.186/73.478 ms，
  inference P50/P95 54.684/63.894 ms，backend 全部 `rknn_unified`、level 全部 OK。
- 60/60 个 health 采样中 roslaunch/rosmaster 均存活，probe `errors=[]`、
  `forbidden_nodes=[]`，新增 USB/UVC 内核日志 0 B；无外参时有效地图检测为 0。
- 后 213 秒 NPU 22/22 个点均为 100%@1 GHz；视觉 10 节点 CPU 均值求和约 3.66 核，
  RSS 均值求和约 952.5 MiB（含共享页重复，非 USS）。最高观察温度 69.307 °C，CPU/DMC/GPU
  cooling state 全部为 0，10 个视觉 PID 全程存活。
- 15 秒 compressed 回归 raw/info/JPEG 为 438/439/439 条、约 29.17 Hz，438 组三话题同
  stamp，RKNN 199 条、processing P95 75.709 ms，错误、禁用节点和新增 USB 日志均为 0。
- 结论：V-DEPLOY-01 的“实物新相机 + CameraInfo + 统一 RKNN + ROS 像素链 10 min”部分
  PASS；完整指标见 `docs/OrangePi板端视觉性能报告_20260902.md`。

### 遗留问题

- NPU 已在统一模型下持续报告 100%，不得再并行第二套全图 RKNN；视觉约占 3.66 个逻辑核，
  叠加 FAST-LIO、点云、导航和控制后的整机资源余量仍需联合复测。
- RKNNLite/Toolkit 2.3.2、runtime 1.5.2 和 driver 0.9.6 仍有代际差异；当前实推稳定，最终
  系统镜像统一版本后需复跑离线、ROS 回放和长稳 Gate。
- 当前现场相机主要朝顶棚且未安装外参；未验收带靶实景准确率、有效地图投影、同步 pose、
  stable ID、飞行或投递。首轮人工碰线也表明实机必须固定线缆并做应力释放。

### 下一步

- 视觉分支先推送并向 V-CL-06 聚合分支提交最小差异 MR；完整 V-CL-06 三投、返航、LAND 和
  导航/LIO 并发资源 Gate 通过后，再由单一聚合分支进入 `main`。
- 安装相机外参后补 `optical→body→mission` TF、带靶实景有效地图投影和整机台架联跑；
  不因本次像素链 PASS 提前执行解锁、起飞或真实投递。

---

## 2026-09-02：补齐导航需求 B100 同版本全矩阵入口

### 改动范围

- 扩展 V-SIM-04 运营域矩阵和统一运行入口；
- 不修改视觉检测算法、ROS 话题、导航接口或 formal23 验收定义。

### 具体改动

1. 将动态高度从 1.2/2.4/3.6 m 补齐为 1.2/1.8/2.4/3.0/3.6 m，运营域总设计由
   25 个静态 trial + 60 个动态 trial 扩为 25 + 100。
2. 新增显式 `b100` 命名切片，严格覆盖五类 × 五高度 × 四速度的 100 个唯一组合；旧
   `static25` 和 `sparse30` 切片保持不变，可继续复现历史结果。
3. 统一包装脚本接受 `b100`，并增加数量、唯一性、类别、高度、速度和动态类型断言。

### 验证结果

- 代码落地后将先执行指标单测、YAML 加载和包装脚本参数回归，再在本提交冻结 revision
  上运行完整 B100；实跑结果不预填，待产物生成后另行追加。

### 遗留问题

- B100 属于视觉运营域诊断，不等同于导航实际中断 `P_interrupt` 或 V-CL-06 联合 Gate。
- D50 中超出当前单目标观察窗口或需要第二目标/H 真值生成器的条目仍按 NOT_RUN 报告，
  不用伪造零值补齐。

### 下一步

- 冻结本提交后顺序运行 B100 和当前可执行 D 子集，将同 revision 产物写入导航移交包。

---

## 2026-09-02：消除板端 RKNN 冷启动首帧竞态

### 改动范围

- 调整 `target_detector_rknn` 的初始化顺序；
- 不改变模型、推理、检测消息、话题或兼容接口。

### 具体改动

1. 将图像 Subscriber 的创建移动到 unified/std/tank RKNN 句柄、回调计数器和模型可用性
   检查全部完成之后，避免回放 publisher 在订阅建立瞬间发送首帧时抢入未初始化回调。
2. 增加初始化顺序回归断言，固定所有回调可见状态必须先于图像订阅建立。

### 验证结果

- 板端冻结 revision `abc5103` 的首次全视频冷启动在 frame 0 稳定复现旧竞态并由 60 秒
  ACK 门控失败关闭；失败目录完整保留，未启动任何飞控、导航、控制或执行节点。
- 本补丁完成本地单测、语法和差异检查后，将增量部署板端并从 frame 0 重跑 4622 帧。

### 遗留问题

- 只有板端完整回放 4622/4622 帧、各阶段 ACK 完整且视频成功拉回后，才算此次竞态闭环。
- 回放无同步位姿和安装外参，地图投影应继续失败关闭。

### 下一步

- 部署本提交并从头执行 ROS publisher→RKNN→OpenCV→fusion/refiner→map projector→recorder。

---

## 2026-09-02：完成 B100、板端全视频链与导航 v2 移交包

### 改动范围

- 补跑导航需求 B 的完整高度×速度曲面，复核 D 的实际可执行边界；
- 完成 OrangePi `real_target.mp4` 正式 ROS+RKNN+OpenCV 全链回放并拉回本机；
- 新增导航组首读 HANDOFF、自动 evidence index 和 Git 外 v2 移交包；同步 Roadmap 与板端报告。

### 具体改动

1. 在冻结视觉 revision `1e48c22` 上以单 Gazebo 会话运行五类×五高度×四速度 B100，
   不再把历史 formal23/sparse30 跨 revision 拼成曲面。
2. 审计 D50 readiness：现有 supported16 已全部实跑；其余 24 个 single 缺有效观察窗口/
   场界 readiness，multi10 缺第二靶/H spawner 和逐目标真值，因此 34 项保持 `NOT_RUN`。
3. OrangePi revision `59c74b6` 先通过真正冷启动 10/10，再顺序处理全部 4622 个 OpenCV
   可解码帧；保留 843,258,683 B 原始 MP4，并生成帧数不变的 269,418,434 B H.264 审片副本。
4. 新增 `docs/handoff/视觉组给导航组_HANDOFF_20260902.md`，明确 A/B/C/D、指标语义、
   板端无外参边界和最新 V-CL-06 dirty-worktree FAIL 上下文。
5. 更新 `build_vision_nav_submission_bundle.sh`：B100/D16 成为必需主结果，历史 run 分区；
   自动生成 `evidence_index.csv`，硬校验板端帧/perf/fail-closed/H.264 帧数，完整 MP4 外置。

### 验证结果

- B100：100/100 完成，P_confirm=P_selected=0.83，地图误差 P95=0.203479 m，processing
  P95=448.181 ms；`validation_errors=[]`、`hard_failure=false`、终态 `DIAGNOSTIC_ONLY`。
- D supported16：既有 run 16/16 confirm/selected，P95 processing/map error=
  104.620 ms/0.083298 m；D34 无可合法直接补跑项。
- 板端全视频：4622 annotated=4622 perf，backend 仅 `rknn_unified`；processing
  P50/P95=63.795/79.924 ms，inference=49.622/57.694 ms；5135 mapped 全部 invalid，
  禁用节点/ERROR/FATAL/竞态/ACK 超时均为 0，输出 ffprobe 为 4622 帧/77.053 秒。
- B100 矩阵断言、RKNN 竞态 targeted 测试与 `uav_vision` 25/25、Python 语法、bash 语法、
  `git diff --check` 均 PASS。
- `liftrace_vision_to_navigation_handoff_20260902_v2.zip` 构建 PASS（9.5 MiB）；外层 checksum、
  `unzip -t`、内部 `MANIFEST.sha256`、必需产物、无占位符和 evidence index 复核 PASS。

### 遗留问题

- B100 是单 seed 诊断面，处理 P95 超过 200 ms；暂行采用约 2.4–3.0 m、优先 ≤1.0 m/s，
  不能宣称性能 Gate PASS。
- 板端视频无人工真值、同步 pose 和安装外参；地图投影保持 `tf_unavailable`，不报告真实
  precision/recall 或地图误差。
- 最新 V-CL-06 r9 仅为 dirty-worktree 调试证据：记录到一次 APPROACH/CAPTURE/release，
  但 Gate 因最大高度 4.002849 m 等条件 FAIL，尚未三投、走廊、返航和 LAND。

### 下一步

- 将 v2 ZIP 与 H.264 审片副本交导航组；导航在冻结 revision 上先完成 90 秒目标事务和
  4 m 高度约束，再运行 600 秒三投联合 Gate。D 后续先补观察窗口，再做 multi10。

---

## 2026-09-02：冻结导航移交包源码版本

### 改动范围

- 收紧视觉到导航材料包的版本可追溯性，不改变任何视觉或导航运行逻辑。

### 具体改动

1. 构建材料包前检查 tracked worktree 必须干净；存在未提交修改时直接失败。
2. 在 ZIP 根目录写入 `SOURCE_REVISION.txt`，记录构建时精确 Git revision，并纳入内部 manifest。

### 验证结果

- dirty-worktree fail-fast 返回码、bash 语法、干净 revision 重建、外层 checksum、ZIP 完整性、
  内部 manifest 均 PASS；`SOURCE_REVISION.txt` 与构建时分支 HEAD 一致。

### 遗留问题

- 仿真日志和完整视频仍按设计位于 Git 外；revision 只冻结构建脚本、文档、配置和索引来源。

### 下一步

- 从干净提交重建最终材料包，推送 feature 分支并更新 PR #4。

---

## 2026-09-02：清理 PR #3 合主干前行尾空白

### 改动范围

- 仅清理 HANDOFF 与历史 Python manager 的行尾空白，不改变运行逻辑或接口。

### 具体改动

1. 移除 `视觉组给导航组_HANDOFF_20260902.md` 两处 Markdown 行尾空格。
2. 移除 `target_search_manager_py.py` 的空行、`return` 与入口行尾空格。

### 验证结果

- `git diff --check origin/main` PASS；122 个 Python、10 个 shell、43 个 XML、26 个 YAML 静态解析 PASS。
- 视觉侧 29 个 Python 单测、任务侧 223 个 Python 单测、板端标定查看脚本检查均 PASS；视觉 Catkin 54 项测试 PASS。
- `uav_vision`、`uav_vision_eval`、`camera_sdk` 定向编译 PASS；主集成工作区全量编译 PASS。
- 主集成工作区并行首编暴露历史消息生成依赖抢跑；串行预生成消息目标后 `-j4` 全量编译通过。
- `uav_mission` 与 `plan_manage` Catkin 定向回归共 160 项：0 error、0 failure、0 skip。

### 遗留问题

- 本提交只处理静态检查阻断；V-CL-06 联合 Gate 的高度约束和三投闭环状态不变。

### 下一步

- 完成 PR #3 聚合复核，通过后按确认合入 `main` 并打非 Gate 集成 tag。

---

## 2026-09-02：登记导航组轻量搜索策略仿真上游

### 改动范围

- 登记导航组新增 `liftrace-sim` 纯 Python 任务级仿真仓库及其与视觉、导航执行仓的边界；
- 更新视觉主线在 PR #3 合并后的状态，不修改任何运行代码、接口或 Gate 判据。

### 具体改动

1. 新增 `docs/导航组轻量策略仿真上游_20260902.md`，冻结本次核对 revision、已建模能力、
   假设边界、视觉数据输入和整机 Gate 关系。
2. 在 Roadmap 中记录 `liftrace-sim` 只负责大规模策略筛选，不能替代 ROS/Gazebo/SITL；
   视觉 `main@1094db5` 进入功能冻结、整机 SITL 优先阶段。
3. 修正 Roadmap 中“live execution bridge 未合入”的过期表述；该接口面已随 PR #3 合入，
   当前缺的是正式随机场 target-stage、三投、返航、走廊和 LAND 联合证据。

### 验证结果

- 已核对上游 `main@18f6ee8219120f1c00279b7fe41d9dda6fbc1377` 的 README、建模大纲、
  假设表、基线配置、视觉数据目录和测试目录；`git diff --check` 与本地链接目标检查 PASS。

### 遗留问题

- 上游模型仍含多项 `ASSUMED` 输入，避障得分、穿门和自主降落未完整建模；其结果不得标记
  为比赛得分预测或 V-CL-06 PASS。
- 视觉主仓仍有 D50 扩展、30-seed、安装外参/TF、真实 H/负样本等非阻塞验证项；“功能冻结”
  不等于所有量化和部署验收均完成。

### 下一步

- 由 `liftrace-sim` 先筛少量搜索策略候选，再用视觉 `main@1094db5` 和导航冻结 revision
  运行正式随机场 90 秒 target-stage smoke；成立后进入 600 秒整机 Gate。

---

## 2026-09-02：修正策略仿真记录文件尾空行

### 改动范围

- 仅移除新建记录文档的多余 EOF 空行，不改变结论。

### 具体改动

1. 清理 `docs/导航组轻量策略仿真上游_20260902.md` 文件末尾空行。

### 验证结果

- `git diff --check origin/main` PASS。

### 遗留问题

- 无。

### 下一步

- 保持 PR #5 为文档独立合并单元。

---

## 2026-09-04：建立实装相机仿真候选基线并拆解 B100 延迟

### 改动范围

- 将 V-SIM-04 独立相机夹具从完整 D435i 收敛为实装 KS2A543 单彩色流候选基线；
- 记录实装相机参数、安装外参测量基准、B100 `448.181 ms` 根因和视觉工程精简边界；
- 安全清理本机冗余视觉 worktree，不动导航组工作树、分支或提交。

### 具体改动

1. 新增 `KS2A543_color_only` Gazebo 模型，仅渲染 `1280x720@30` 彩色流，使用已接受标定的
   `fx/cx/cy/D` 和由 `fx` 推导的 `82.85°` 水平视场；话题和 frame 与板端正式链一致。
2. `vsim04_stability.launch` 和 `vsim04_camera_soak.launch` 默认使用实装相机口径，不增加
   D435i/KS2A543 动态 selector；历史夹具由 Git revision 复现。
3. 新增 `docs/实装相机与安装外参测量标准_20260904.md`，冻结唯一交付外参为
   `body(IMU 感测中心) -> downward_camera_optical_frame(投影中心)`，不复用 2025 未核实值。
4. 新增 `docs/视觉工程精简与B100延迟复盘_20260904.md`，把延迟拆为模型推理、仿真渲染、
   ROS fan-out/recorder 排队、fusion partial 与审计写入，并冻结历史 manager 为 regression-only。
5. 将 24 个 worktree 收敛为 8 个：移除 16 个 clean/已归档视觉工作树，保留根工作区、
   当前视觉工作树和 6 个导航工作树；实验证据已移入根仓 `logs/` 忽略目录。

### 验证结果

- XML 静态解析、KS2A543 内层及组合模型 `gz sdf -k` PASS；视觉工作区 `catkin_make -j4` PASS。
- `uav_vision_eval` 26 项定向测试：0 error、0 failure、0 skipped；补齐主集成 overlay 后两个 launch
  均可完整展开节点列表。
- 纯 YOLO 实拍 100 帧推理/总耗时 P95 为 `15.13/15.23 ms`；完整 D435i 代表 10 格
  `process/pipeline=291.9/94.6 ms`，单彩色 D435i detector processing 降至 `68.8 ms`。
- KS2A543 formal23 完成 23/23，确认 22/23，detector/pipeline P95 为 `117.0/152.5 ms`，地图误差
  P95 `0.2038 m`；但 source/receipt 仅 `12.17/11.08 FPS`，process P95 `222.9 ms` 且一格精确时戳缺样，
  终态按规则保持 INVALID。

### 遗留问题

- 当前机架安装外参尚未实测；内参能确定视场和畸变，不能推导机体到相机的六自由度位姿。
- Gazebo Classic 插件仅提供一个 focal length，仿真用 `fx=fy=725.351 px`，与实机
  `fy=723.340 px` 差约 0.28%；不为该近似增加 relay。
- 当前候选基线 formal23 不是有效 PASS，不得合入 `main` 或替换历史默认结论。

### 下一步

- 实测并冻结 `body -> downward_camera_optical_frame`；随后做一次小范围 partial 隔离和终态事件去重，
  在宿主吞吐可用时重跑 formal23/B100，成立后再申请合入 `main`。

---

## 2026-09-04：冻结 KS2A543 基线下 A/B/C/D 重验证决策并修正轻量策略模型

### 改动范围

- 评估实装相机换型后导航组 A/B/C/D 四类视觉性能表的有效性和重跑优先级；
- 更新 C25/D50 设计参数为 KS2A543 几何，不运行无效的大矩阵；
- 在导航轻量策略仓建立最小相机基线修正，不改 ROS 接口或导航执行链。

### 具体改动

1. 新增 `docs/KS2A543相机基线下ABCD重验证决策_20260904.md`，确认学习模型仍为 0714
   `merged_standard`，变化来自 `1280×720`、约 `82.85°` 视场、畸变和有效处理频率；历史
   D435i A/B/C/D 仅保留作对照。
2. 冻结执行顺序为：先解决评测 exact-stamp/吞吐有效性，再完整运行 A25/B100/C25；D50 先跑
   当前可执行 D11，再补 edge/partial 观察窗口、越界轨迹和 multi-target 真值。
3. C25 合同和水平视场切到 KS2A543；D50 切到 1280×720、`fx=fy=725.351 px`、零夹具
   sensor-link 偏移，并更新复现说明。历史 D435i 运行产物不回写。
4. 导航轻量仓本机分支 `feat/ks2a543-vision-baseline@6b070f5` 切换相机几何和板端有效检测率，
   停用旧 D435i B100 查表，将旧动态阈值候选标为失效，并去除单测对偶然生成的 results 依赖。
5. 更新 Roadmap 和轻量仓上游说明；上游 push 因当前 GitHub 身份无权限返回 HTTP 403，保持
   `sakelier/liftrace-sim main@18f6ee8` 未变，不绕行修改其他远程。

### 验证结果

- KS2A543 D50 dry-run：50/50 schema、pairwise coverage PASS，Gazebo 状态按合同为 NOT_RUN；
  loader 给出 11 个当前可执行 single case，其余 39 项均有结构化 NOT_RUN 原因。
- D50 相关 9 项 Python 单测 PASS；包含 C25 dry-run 25/25 的 V-SIM-04 matrix/schema
  assertion PASS，状态保持 DRY_RUN。
- `liftrace-sim` 全部 49 项单测 PASS，`git diff --check` PASS；命令行相机报告为
  1280×720、HFOV 82.85°，2.4 m 对应横向地面视场约 4.235 m。

### 遗留问题

- KS2A543 formal23 仍因 source/receipt 吞吐、processing P95 和一格 exact-stamp 缺样为 INVALID；
  在该问题收敛前不启动 A25/B100/C25 正式长跑。
- `liftrace-sim` 当前只消费 B 表；C/D 新数据的拟合和策略消费方式尚未定义，不为此新增接口。
- 上游修正提交尚未远程可见，需要导航组授权推送或在本机 cherry-pick `6b070f5`。

### 下一步

- 最小修复评测器跨 subscriber 精确时戳配对和冗余终态记录，先用 3～5 个边界 trial 验证运行
  有效性；成立后按 A25→B100→C25→D11 顺序执行并更新导航移交包。

---

## 2026-09-04：修正确认时延的跨订阅配对合同

### 改动范围

- 修复 KS2A543 评测中候选已确认但 recorder 图像订阅缺同 stamp 样本导致整 run INVALID 的问题；
- 只调整评测指标合同，不改变检测、融合、target memory、导航接口或准入阈值。

### 具体改动

1. 确认缺样来自独立 ROS 图像订阅：分类器允许跳帧时，检测节点处理的帧不保证也被 recorder
   图像订阅收到；这不是 OrderedDict 容量或清理时机问题。
2. 新合同 `vsim04-ks2a543-pipeline-20260904` 使用同 source stamp 的 detector
   callback-start 到 recorder 收到合格候选的 monotonic 时差作为端到端 P95 Gate。
3. 旧 `confirmation_processing_ms` 不删除，继续作为 recorder receipt-to-receipt 传输诊断；
   缺少该可选样本不再令运行失败。`confirmation_pipeline_ms` 缺失仍硬失败。
4. formal、operating-surface 和 C25 三份矩阵统一切换合同，schema assertion 加入“recorder
   未收到图像但 detector exact-stamp 存在”回归。

### 验证结果

- V-SIM-04 matrix/artifact schema assertion PASS，D50 9 项单测 PASS，Python 编译 PASS。
- `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j4` PASS；`git diff --check` PASS。

### 遗留问题

- 尚需实际 Gazebo 边界 trial 证明新合同每个确认项都有 detector exact-stamp，并核对 P95
  不超过 200 ms；旧 run 不回写、不按新合同事后改判。

### 下一步

- 提交冻结该指标修复，在提交态运行 3～5 个边界 trial；有效后依次运行 A25、B100、C25、D11。

---

## 2026-09-04：修复 B100 可见窗口采样混叠与收尾死等

### 改动范围

- 修复 B100 低空高速大目标可能未采到完整入画帧时，recorder 因没有离场时戳而死等的问题；
- 提高 B100 仿真相机轨迹更新频率，只改变评测夹具，不改检测、准入、消息或导航策略。

### 具体改动

1. recorder 优先以真实 visibility leave 作为输出 drain 边界；若 trial 从未采到有效可见窗口，
   则退回 runner 的 trial-end 源时戳，允许全矩阵继续，并由既有终态校验明确报告
   `never_entered/never_left_visibility_window`，不把无效设计伪装成算法失败或 PASS。
2. B100 动态相机运动从 20 Hz 提高到 60 Hz。KS2A543 图像源为 30 FPS，60 Hz 离散位姿可避免
   `bridge@1.2 m` 约 0.15 m 的完整入画区间恰好落在两个图像样本之间。
3. 合同版本更新为 `vsim04-ks2a543-pipeline-60hz-20260904`，并补充 drain 边界优先级和
   B100 轨迹频率断言。

### 验证结果

- `git diff --check`、Python `py_compile`、V-SIM-04 matrix/artifact schema assertion PASS；
  `catkin_make -DCATKIN_WHITELIST_PACKAGES=uav_vision_eval -j4` PASS。
- 故障证据中 `dynamic_bridge_h1p2_v1p5` 的相机确实完成 1.477 m/s 运动，但 20 Hz 位姿样本从
  `x=-2.2575 m` 跳到 `x=-2.0332 m`，跨过此前相邻试次实际观测到的完整入画区间；不是 YOLO、
  TF 或 ROS 输出链停摆。

### 遗留问题

- 尚需在提交态复跑该低空高速边界，确认 60 Hz 下存在 enter/leave 且不再触发 watermark timeout；
- 边界成立后 A25/B100/C25/D11 必须在同一新 revision 重跑，旧半轮 B100 不拼接。

### 下一步

- 先运行 `bridge@1.2 m×1.5/2.0 m/s` 边界，再按 A25→B100→C25→D11 完整执行并更新移交材料。

---

## 2026-09-04：解除高频动态轨迹对 Gazebo 渲染的同步阻塞

### 改动范围

- 修复 60 Hz 动态试次继续经同步 service 设置每个位姿而压低相机源帧率的问题；
- 仅调整 `uav_vision_eval` 夹具内部驱动方式，不新增生产接口。

### 具体改动

1. 试次起点、离场点仍经 `/gazebo/set_model_state` service 确认；运动中的 60 Hz 位姿改经同名
   Gazebo 标准 `ModelState` topic 异步发布，queue 固定为 1，避免 service 往返阻塞传感器渲染。
2. topic 通过 launch 参数 `set_model_state_topic` 配置；runner 复用同一 `_camera_state` 构造，
   不复制姿态语义。
3. 最后一个动态位姿保留一个 command period 的异步送达时间，再由 service 移至离场点。

### 验证结果

- 首轮 60 Hz 同步-service 边界诊断证明 drain 死等已消失，`bridge@1.2 m×1.5 m/s` 正常出现
  enter/leave；但 2.0 m/s 轮次相机 source FPS 降至约 4.90，故该轮保持 INVALID，不作算法结论。
- Python 编译、V-SIM-04 matrix/artifact schema assertion 和 launch XML 解析 PASS。

### 遗留问题

- 需在提交态复跑 1.5/2.0 m/s 边界，确认异步轨迹同时恢复源帧率与完整入/离场事件。

### 下一步

- 边界有效后重跑同 revision 的 A25/B100/C25/D11；若边界仍无完整窗口，按设计 INVALID 报告，
  不再提高频率或延长超时。

---

## 2026-09-04：去除 V-SIM-04 粘性候选重复审计

### 改动范围

- 落实既有过度设计复盘，减少长矩阵中 recorder 对同一候选状态的重复持锁与记录；
- 不改变视觉节点输出、候选状态机或性能准入语义。

### 具体改动

1. confirmed/selected 审计按 `trial + event kind + stable ID + last_seen + class + state + policy`
   去重；同一观察的真实状态或准入变化仍会留下独立记录。
2. 当前可准入和 selected 关联事件按 `trial + stable ID + last_seen` 去重，frame 标志仍按每次回调
   更新，相关性计算仍保留每个唯一视觉观察。
3. recorder 的 image/targets/selected subscriber queue 统一为 1，避免消费已过期的粘性状态和图像，
   与板端 queue-size=1 的正式约束一致。

### 验证结果

- 修复前完整 B100 已 100/100 收尾、无 watermark timeout，确认/选靶 `78/76`，pipeline/map
  P95=`194.264 ms/0.204128 m`；但 recorder source FPS 仅 `5.337`，三个低空高速条件未采到
  visibility window，终态正确保持 INVALID。
- 该轮 confirmed/selected 审计分别达到 `29303/3191` 条，证实重复粘性记录已成为不必要负载。

### 遗留问题

- 需用问题三格和代表长路径复跑，确认去重后 source FPS 与 visibility 完整性实际改善；旧 INVALID
  B100 只保留作根因证据，不进入导航概率表。

### 下一步

- 先跑最小代表集；若仍不足以稳定采到高速窗口，则把运动夹具改为 Gazebo 连续速度模型，而不是
  继续增大 queue、超时或离散发布频率。

---

## 2026-09-04：将 B 表动态零可见采样定义为端到端失败

### 改动范围

- 区分“轨迹/真值设计损坏”和“有效动态条件内没有任何完整入画采样”两种情况；
- 该语义只属于 diagnostic operating-surface B，不放宽 formal23、静态 A、C25 或 D50。

### 具体改动

1. operating-surface 显式声明 `dynamic_zero_visibility_policy: count_as_failure`。几何有效的动态
   试次即使没有完整入画样本，也以 `P_confirm=false`、`failure_stage=truth_visibility` 进入 B100
   分母；不会冒充检测器 raw failure，也不会使整张诊断表结构 INVALID。
2. 默认策略仍为 `invalid`；loader 拒绝未知策略，正式矩阵继续要求 enter/leave 和至少一帧
   eligible truth。
3. manifest、terminal context 写明实际策略，schema assertion 同时覆盖 diagnostic 计失败和
   strict 判 INVALID 两条路径；合同更新为
   `vsim04-ks2a543-pipeline-60hz-sampling-20260904`。

### 验证结果

- 去重后代表 5 格完成 5/5：source FPS `17.212`、pipeline P95 `105.548 ms`、地图 P95
  `0.213324 m`，confirmed 审计仅 `188` 条；相比修复前 B100 的 source FPS `5.337` 和
  `29303` 条 confirmed 重复审计，负载问题已明显收敛。
- 代表集中只有 `panzer@1.2 m×1.5 m/s` 无完整入画采样；相邻真值帧在靶心两侧，说明这是
  低空大靶窄窗口与有效帧率共同形成的零机会条件。matrix/schema assertion、Python 编译、
  `git diff --check` 和 `uav_vision_eval` 定向 `catkin_make -j4` PASS。

### 遗留问题

- B100 需在本策略与去重实现的同一提交上完整重跑；旧 INVALID 100 格不得事后改判或拼接。

### 下一步

- 重跑 A25/B100 后执行 C25、D11；汇总时单列 `truth_visibility`，供导航避开零机会工作域。

---

## 2026-09-04：将链路心跳校验限定在有效观察窗内

### 改动范围

- 修复目标已离开有效观察窗后的短暂图像心跳空档，误将已完整采样的试次和整张 B100 判为
  `INVALID` 的问题。

### 具体改动

1. recorder 只在试次有效观察窗尚未关闭时，把 CameraInfo、图像、真值、mapped、targets 和
   detector diagnostic 心跳缺口记为该 trial 的 infrastructure gap。
2. 目标离开窗口后不再扩大心跳审计；与离场边界相关的输出仍必须全部越过对应 source stamp，
   否则由既有 `output_watermark_timeout` 失败关闭。

### 验证结果

- 修复前同 revision B100 已完成 100/100，仅
  `dynamic_tent_h1p2_v0p5:infra_gap:three_consecutive_images` 使整轮无效；该缺口发生于目标离开观察窗
  约 3.7 s 后，窗内已有连续图像、同 ID confirm/selected 和完整输出水位。
- 该轮 100 个 trial 均有终态、watermark timeout=0，所以保留为根因对照，不进入最终移交表。

### 遗留问题

- 需在新提交上重跑边界条件及 A25/B100/C25/D11，确认窗内心跳缺口仍会正确否决。

### 下一步

- 先运行首个低空慢速格与高速边界，再完成同 revision 的全量矩阵和移交包。

---

## 2026-09-04：对齐仿真 TF 与检测器实际消费帧

### 改动范围

- 修复高负载下 TF 广播器和 YOLO 的独立 `queue_size=1` 订阅丢弃不同原图帧，导致已处理检测帧缺少
  同 source stamp TF 的问题。

### 具体改动

1. `gazebo_camera_tf` 保留原图触发，同时可配置订阅 `TargetDetectionArray`；仅
   `source=target_detector` 的粗检测帧会补发该帧的精确 TF。
2. `ground_truth.launch` 将触发话题和 source 参数化；不改生产视觉话题、不放大原图队列，不把仿真补丁
   带入板端。

### 验证结果

- 修复前一次 A25 合同完整但出现 167 个 frame-level `tf_too_old`，检测帧 TF failure
  67/101，导致 map-invalid=67.33%、confirm/selected=8/25；相邻 revision 曾为 23/25，明确不是分类模型变化。

### 遗留问题

- 需用静态代表集确认 `tf_too_old=0`，再重跑最终 A25/B100/C25/D11。

### 下一步

- 先跑 5 个静态高度边界；若 TF 失败仍存在，保持 INVALID 并停止全量矩阵。

---

## 2026-09-04：统一观察窗的心跳缺口入口

### 改动范围

- 补齐 recorder 定时 readiness 检查与 callback 相邻消息间隔检查的观察窗语义。

### 具体改动

1. 两条心跳审计路径都只在 `entered_visibility_window=true` 且
   `left_visibility_window=false` 时记录 infrastructure gap。
2. 试次启动到目标进窗前由 READY 预检保护；目标离窗后由 source-stamp 输出水位保护。

### 验证结果

- TF 对齐后的 5 类静态门槛试验完成 5/5，TF failure=0/19，地图误差 P95=0.1711 m。
- 随后 A25 的 `static_tent_h1p8` 在离窗后 23–35 ms 由 callback 旁路记下两个约 2.00 s gap；
  已主动终止该次必然无效运行，不作算法结论。

### 遗留问题

- 需重跑 A25 确认 callback 旁路不再将窗外抖动写成 infra gap，且窗内默认 2 s 门槛保持不变。

### 下一步

- A25 有效后连续完成 B100、C25 和 D11。

---

## 2026-09-04：用输出水位唯一裁决下游链路中断

### 改动范围

- 移除 mapped detections/targets 的中途 heartbeat 与试次输出水位之间的重复失败判定。

### 具体改动

1. 试次窗口内继续审计原图、真值和 detector diagnostic，确保输入与粗推理基建持续健康。
2. mapped detections 和 targets 在窗口内延迟或缺席时，由 P_confirm、pipeline latency 和首阻断阶段表达；
   不再把被测链的性能失败改写为整场实验无效。
3. 起跑前 READY 和终态 freshness 仍要求两路输出；trial 收尾时两路必须越过观察窗离场 source stamp，
   否则既有 `output_watermark_timeout` 依然使试验 INVALID。

### 验证结果

- 修复前最终 A25 完成 25/25、TF failure=0、地图误差 P95=0.2036 m，终态有效。
- 最终 B100 完成 100/100、validation errors=[]、watermark timeout=0，confirm/selected=73/100，
  TF failure=0，地图误差 P95=0.2071 m。
- C25 两次分别在不同 trial 出现 mapped/targets 约 2 s 中途空档；原图、真值、detector 和最终水位均正常，
  单格重跑也正常，证实是被测下游时延而非不可测样本。

### 遗留问题

- 修复改变测量终态语义，A25/B100/C25/D11 需在同一新 revision 重跑；旧轮仅作根因对照。

### 下一步

- 完成最终四个矩阵，将下游空档归入性能结论而非从结果中隐去。

---

## 2026-09-04：去除 recorder 原图旁路对测量结果的重复否决

### 改动范围

- 区分相机/推理源健康与 recorder 自身的独立 `queue_size=1` 原图观察通道。

### 具体改动

1. 窗口内原图 recorder callback 的短暂缺席不再直接记为 infrastructure gap；它仍进入实际接收 FPS 和丢帧统计。
2. 窗口内继续必须有由另一原图订阅产生的 truth heartbeat 和 detector diagnostic heartbeat；两者任一中断
   仍会使试次 INVALID。
3. 起跑 READY 仍要求连续 3 帧原图，终态仍要求原图 fresh；长时间断流不会被隐藏。

### 验证结果

- 修复前 A25 运行在两个静态窗口内只记到 recorder `three_consecutive_images` 缺口；同时段 truth 与
  detector diagnostic 均无缺口，因此确认为观察器丢帧，不是相机源或 YOLO 停摆。

### 遗留问题

- 评测语义再次变更，最终 A25/B100/C25/D11 仍需同 revision 重跑。

### 下一步

- 只在 truth 或 detector 也中断、输出水位追不上或终态不 fresh 时将全轮判为 INVALID。

---

## 2026-09-04：补齐 D50 运行矩阵的严格可见性策略

### 改动范围

- 修复 D50 独立 runtime adapter 没有补齐 recorder 新增必填字段，导致 D11 在节点启动期间退出的问题。

### 具体改动

1. `load_d50_runtime_matrix()` 显式写入 `dynamic_zero_visibility_policy=invalid`；D 仍要求完整进窗、离窗和有效帧，
   不继承 B100 诊断表的零可见计失败特例。
2. D50 单测新增该默认值断言，防止再次出现 loader/recorder 合同脱节。

### 验证结果

- 修复前 `run_vsim04_d50.sh supported` 在 0 trial 时失败，根因为
  `KeyError: dynamic_zero_visibility_policy`；未产生六件套，不计为 D 算法失败。

### 遗留问题

- 需重跑 supported D11；A25/B100/C25 不经 D loader，其已完成结果不受此修复影响。

### 下一步

- D11 完成后统一更新 HANDOFF、Roadmap 和群聊移交包。

---

## 2026-09-04：按规划曲线修复 D50 转弯轨迹测量

### 改动范围

- 修复 D50 `turn` trial 被通用直线路径统计误判为没有有效运动/横向样本的问题。

### 具体改动

1. D runner 在内部 trial 事件中附带紧凑的 `[source_time_offset,x,y]` 规划轨迹；不新增跨组 ROS 接口。
2. recorder 对带规划轨迹的试次按 source time 插值首帧期望位置，并将相机位置投影到最近规划线段；直线
   A/B/C 继续沿用原统计路径。
3. D 的中间姿态改走既有 `/gazebo/set_model_state` topic，首姿态和离场仍用 service 确认，避免 20 Hz
   轨迹被逐帧同步服务调用拖慢。
4. schema 自测新增延迟首帧的折线路径样例，确保曲线不会退化成起终点弦线。

### 验证结果

- `git diff --check`、Python 编译、`vsim04_metrics_assertion.py` 均通过；D50 单测 9/9 通过。
- 将失败运行的原始 D22/D38 帧离线套用修复口径，运动/横向样本分别由 `0/1`、`0/0` 恢复为
  `65/66`、`64/65`，规划路径 P95 横向误差均为 0；没有修改检测或准入结果。

### 遗留问题

- 旧 D11 运行保持 `INVALID` 作为根因对照；必须在本提交上重新实跑，不能事后改判。

### 下一步

- 重跑 supported D11，随后冻结 A25/B100/C25/D11 的同轮结论并更新导航移交包。

---

## 2026-09-04：完成 KS2A543 A/B/C/D 当前可测面并更新导航移交

### 改动范围

- 固化评测合同修复后的 A25/B100/C25/D11 结果，更新 Roadmap、导航回复、HANDOFF、实验索引和
  群聊移交包构建器。

### 具体改动

1. HANDOFF 只把 2026-09-04 的四批 KS2A543 run 作为当前权威数据；D435i formal23/sparse30/
   C25/D16 保留为历史对照，不再拼入当前曲面。
2. `evidence_index.csv` 改用 `pipeline_p95_ms`，不再把 recorder 双 subscriber 排队诊断误写成
   算法 processing 合同指标。
3. v3 builder 移除已清理旧工作树日志的硬依赖，只打包当前 A25/B100/C25/D11 六件套、D50
   设计/复现、相机/板端报告、模型和 V-CL-06 上下文；大视频继续不重复打包。
4. Roadmap 与评测 README 明示保守工作点约 2.4 m、`<=1.0 m/s`，以及 C 左右不对称、D39
   NOT_RUN 和 visual-only `P_interrupt=null` 边界。

### 验证结果

- A25：25/25，confirm/selected=23/25，pipeline/map P95=164.813 ms/0.203769 m。
- B100：100/100，confirm=87/100、selected=86/100，pipeline/detector process/map P95=
  181.990 ms/145.724 ms/0.203484 m。
- C25：25/25，完整入画 confirm=15/15、selected=12/15，pipeline/map P95=
  158.690 ms/0.216720 m。
- D11：11/11 全部 confirm/selected，运动/横向样本 792/811，pipeline/map P95=
  265.232 ms/0.152282 m；TF failure、终态错误和硬失败均为 0。

### 遗留问题

- D 其余 39 项仍因观察窗口、完整入画/离画预检、场界或 multi-target/H 真值接线而 NOT_RUN；
  D11 pipeline 超过 200 ms 参考线，需在整机负载下复核。
- 四批均为单 seed visual-only 数据，不能替代多 seed 概率或导航实际 `P_interrupt`。

### 下一步

- 生成并校验 v3 ZIP；将新 B100 以明确的单 seed 诊断 provenance 导入 `liftrace-sim`，只重调少量
  搜索策略候选后开始整机 90 秒 smoke。

---

## 2026-09-04：将 KS2A543 实测面导入导航轻量策略仓

### 改动范围

- 用当前视觉移交包更新本机 `liftrace-sim`，并把策略扫描结论和上游权限边界写回视觉移交材料。

### 具体改动

1. `liftrace-sim@ee5d2bd` 导入 B100/C25/D11；仅 B100 驱动精确格点，C/D 保持归档，不新增
   ROS 话题、执行器或跨组接口。
2. 搜索基线由 2.2 m 调整为 2.4 m；动态阈值 `p=0.70, gamma=0.50` 仅登记为未启用研究候选，
   `active_policy` 继续为 `first_seen`。
3. 修复轻量仓 Pareto 绘图器不能读取自身 sweep JSON 的问题，并补回归测试。
4. Roadmap、HANDOFF、导航回复和专门说明统一记录本机提交与 HTTP 403 权限边界；移交包构建器
   增补轻量策略仓说明文档。

### 验证结果

- 轻量仓 50 项单测通过；B/C/D 导入计数为 100/25/11，交付 manifest 75 个文件校验通过。
- 120 点搜索曲面 Pareto 包含 3.6 m/2.0 m/s、2.4 m/1.5 m/s 和 2.4 m/1.0 m/s；保守采用
  2.4 m/1.0 m/s。1000 次基线模拟 mean Cue rate=0.881，P(all)=0.441，仅作任务模型输出。
- 36 点阈值扫描后以独立 seed 验证 2000 次；当前候选相对 `reserve_red_cross` 平均
  `+1.595` 分，配对 95% CI=`[1.425,1.765]`。
- 视觉热力图、Pareto 图、动态阈值图均成功生成；`git diff --check` 通过。

### 遗留问题

- 当前 GitHub 身份对 `sakelier/liftrace-sim` 无写权限，feature 分支 push 返回 HTTP 403；
  上游仍为 `main@18f6ee8`。
- 单 seed B100 和含假设任务模型不能替代整机 `P_interrupt`、安全/时限或投递 Gate。

### 下一步

- 用 2.4 m/1.0 m/s 作为首个整机候选，开始 V-CL-06 90 秒同 ID smoke；通过后再做 600 秒 Gate。

---

## 2026-09-04：同步 V-CL-06 r11 导航进展与联合责任边界

### 改动范围

- 根据导航上游 `feat/vcl06-local-full-mission@c1be1d0` 的最新 HANDOFF、变更记录和 r11 原始
  Gate，更新视觉 Roadmap、视觉给导航 HANDOFF、责任边界及移交包构建入口；未修改导航源码。

### 具体改动

1. 将 V-CL-06 最新上下文从 r9 切换到
   `vcl06_p0_seed11_r11_20260904_030218`：记录 panzer/bridge/red_cross 三个目标事务、三槽投递和
   恢复 3/3、Wall_15、最高 3.147812 m 及零碰撞/越界/超高。
2. 依据投递子审计 PASS 将 V-CL-05 的随机场、前三权重中断、red_cross 和三投子 Gate 关闭；
   V-CL-06 继续保持 FAIL，未把 Wall_20、Wall_22、H 或 LAND 外推为已通过。
3. 把当前首阻塞收敛为 Fast-Planner `<0.2 m` `Close goal` 与执行桥
   `TRAJECTORY_FINISHED` 门控之间的近目标终态死锁，明确不得用增加重试、全局放宽容差或视觉
   adapter 伪造到达掩盖。
4. 移交包默认更新为 v4 并指向 r11 Gate；证据索引使用实际导航代码 `919e438`，同时标注 r11
   顶层 manifest 没有冻结双仓 revision 的复现缺口。
5. 核对当前视觉 `a1bfca9`：target memory、drop aligner、兼容桥和两份 typed 上下文消息与 r11
   overlay `34ce0c3` 逐 blob 一致，当前分支仅增加 raw 像素去畸变投影修正；同时记录 r11 仍使用
   640×480、HFOV 1.047 rad 旧仿真相机，不能替代 KS2A543 新几何验收。

### 验证结果

- 导航工作树 clean，HEAD 与上游同名 feature 均为 `c1be1d0`；r11 `gate_status.json` 可解析，
  三投/恢复为 3/3、唯一 planner goal 发布者成立，整场终态为 `FAIL/manager_failed`。
- 组合当前视觉与导航工作树执行 `roslaunch --files/--nodes` 静态解析 PASS；解析到当前视觉
  `phase_d.launch`，并包含 target detector、target memory、drop aligner 和唯一 planner bridge。
- `git diff --check` 与移交包构建脚本 `bash -n` PASS；未启动 ROS master、Gazebo、PX4、MAVROS、
  执行机构或任何实机动作。

### 遗留问题

- r11 尚未通过整场 Gate；Wall_20、Wall_22、H 新鲜度对齐、AUTO.LAND、落地与解锁均未触达。
- r11 顶层 manifest 错记旧根工作区 revision 与 r10 脚本名，下一轮必须显式冻结双仓 revision、
  相机模型和实际 launch 参数。
- 新相机模型、2.4 m 搜索候选和近目标修复不能在同一首轮复跑中同时切换，否则无法归因。

### 下一步

- 导航侧有界修复近 effective goal 终态；先保持 seed=11、2.2 m 与旧仿真相机复跑 r12，首次整场
  Gate PASS 后再单独切 KS2A543 几何并比较 2.2/2.4 m。

---

## 2026-09-05：仿真显式授权、单实例与强制零残留收尾

### 改动范围

- 修订 `top_level_scripts/sim_run.sh`、`top_level_scripts/stop_toudi3_sim.sh`、`AGENTS.md` 和
  仿真指南；新增 VCL06 分阶段验收、流程、时间、阈值及巡点坐标总报告。
- 本次只修改本机仿真编排和文档，不修改视觉算法、导航状态机、Planner、飞控、舵机或实机配置，
  不启动任何仿真和实机节点。

### 具体改动

1. `sim_run.sh` 新增单命令 `SIM_RUN_AUTHORIZED=1` 显式授权；没有当前用户启动请求时包装器以 64
   拒绝，不允许“恢复任务”、诊断、汇报或自动 continuation 隐式重启 Gazebo。
2. 新增 `/tmp/liftrace_sim_run.lock` 非阻塞互斥锁，并在启动前按精确进程名检查 ROS、Gazebo、PX4、
   MAVROS 和 RViz；已有本机 SITL 或残留时以 73 拒绝叠加启动。
3. 主命令改为可等待的子进程，并为正常退出、Gate FAIL、命令失败及 HUP/INT/TERM 统一安装 EXIT
   trap；trap 停辅助节点和录屏、调用停止脚本，再复查受管进程是否为零。清理失败会令整轮非零退出。
4. `stop_toudi3_sim.sh` 补入 `roscore` 和 `rviz`，TERM/KILL 后最多三次复查；从“已请求停止”改为
   “确认零残留”，仍不发送任何飞控或执行机构命令。
5. AGENTS 新增规则 19，明确仿真授权不能跨请求继承、不得直接 `roslaunch/gzserver`、失败先诊断
   再复跑；仿真指南示例同步显式授权变量。
6. 新增 `docs/VCL06完整竞赛分阶段验收流程时间与参数报告_20260905.md`：冻结 r11、r33、r35b
   证据边界，记录真实 ROS 用时、600 s 估算、任务/Planner/投递/H/视觉阈值、12 点搜索、3 个动态
   目标、8 点走廊和异常返航逻辑，并附 09-04～09-05 连续失败归因。

### 验证结果

- `bash -n top_level_scripts/sim_run.sh top_level_scripts/stop_toudi3_sim.sh` PASS。
- `shellcheck` 未发现本次新增逻辑错误；仅保留 ROS 动态 source 和原录屏轮询变量的既有提示。
- 未授权执行 `top_level_scripts/sim_run.sh dry_run true` 返回 64，且前后均无 `gzserver`；证明拒绝
  发生在创建 run 目录和启动任何命令之前。
- `git diff --check` PASS；报告中的最新完整竞赛状态保持 r35b 1/3 投 FAIL，未把分段 PASS 外推为
  整场 PASS。

### 遗留问题

- SIGKILL 和宿主机强制关机无法由 shell trap 捕获；后续首次人工启动前的残留拒绝仍作为第二道保护。
- 最新完整竞赛的首阻塞仍是外部模式中 release_guard 与旧 `patrol_control` 本地几何 Gate 重复；
  本条不修改该导航业务逻辑。

### 下一步

- 只有用户再次明确要求启动仿真时，才使用单命令
  `SIM_RUN_AUTHORIZED=1 SIM_NO_RECORD=1 bash top_level_scripts/sim_run.sh ...`；先修复单一投递许可权威，
  再安排一轮完整 headless Gate，不因本次脚本验证自动续跑。

---

## 2026-09-05：进程核查只读化，禁止检查命令误启 Gazebo

### 改动范围

- 新增 `top_level_scripts/check_sim_processes.sh`，修订 `AGENTS.md` 与仿真指南；不修改视觉、导航、
  Planner、飞控、舵机或实机配置。

### 具体改动

1. 一次人工收尾核查把带 `|` 的 `pgrep` 表达式经 Windows→WSL 传递；引号在边界处丢失后，bash
   将进程名解释成管道阶段，误启动了孤立的 `gzserver`/`gzclient`。当时 ROS、PX4、MAVROS 均未启动；
   发现后立即调用统一停止脚本，复查为零残留。
2. 新增只读检查脚本：固定列举 ROS、Gazebo、PX4、MAVROS 与 RViz 的精确进程名，逐项调用
   `pgrep -x`；不存在任何动态正则、管道或启动仿真程序的代码。
3. AGENTS 规则 19 与仿真指南明确禁止 `pgrep -af 'a|b|...'` 及等价跨 WSL 写法；启动前、结束后和
   人工诊断均统一走新脚本，避免“核查残留”本身产生残留。

### 验证结果

- `bash -n`、`shellcheck top_level_scripts/check_sim_processes.sh` 与 `git diff --check` 均通过；执行新脚本
  返回“无本机 ROS/Gazebo/PX4/RViz 进程残留”。本次修改过程不启动仿真。

### 遗留问题

- 只读脚本只覆盖本项目约定的本机仿真进程名；新增仿真组件时需同步扩充固定名单。

### 下一步

- 完成静态检查并提交后，按用户本轮明确授权运行一次完整竞赛 headless Gate；无论结果如何都先读取
  本轮产物、记录首个失败或完整时序，不对同版本连续重跑。

---

## 2026-09-05：联合仿真 overlay 只服从显式双工作区

### 改动范围

- 修订 `top_level_scripts/sim_run.sh`、`AGENTS.md` 与仿真指南；不修改视觉算法、导航业务、Planner、
  飞控、舵机或实机配置。

### 具体改动

1. 盘查确认包装器虽已 source 调用者传入的 `UAV_WS`/`VISION_WS`，随后仍把 `PROJECT_ROOT` 下旧导航
   与旧视觉源码置于 `ROS_PACKAGE_PATH` 前部；这会让同一命令混入根仓旧包，也是此前多轮错误 overlay
   和手工长路径补丁的根因。
2. `UAV_WS/src` 与 `VISION_WS/src` 改为唯一显式源码权威，视觉 source 优先于导航仓内仅用于消息生成的
   兼容副本；两个目录缺失时在创建 run 目录前以 66 拒绝。
3. 若显式工作区提供 `uav_mission`/`uav_vision`，包装器用 `rospack find` 和规范化绝对路径核对归属；
   解析到其他根仓或旧工作树即拒绝启动，不允许再靠人工拼接整条 `ROS_PACKAGE_PATH`。
4. 每轮 manifest 新增双工作区路径、双仓 HEAD、各自 `git status` 和两个核心包实际解析路径，避免只
   冻结视觉仓 HEAD 却无法复现导航版本。

### 验证结果

- `bash -n` 与排除既有动态 source/旧循环提示后的 `shellcheck` 通过，未产生本次新增告警；显式
  overlay 下 `rospack find` 分别解析到导航工作树的 `uav_mission` 和视觉工作树的 `uav_vision`；
  零残留检查及 `git diff --check` 均通过。本条修改不启动仿真。

### 遗留问题

- 包归属检查只对当前明确存在的 `uav_mission` 与 `uav_vision` 生效；其他包仍由正常 Catkin overlay
  规则解析，后续若成为跨仓同名包再纳入固定检查。

### 下一步

- 静态验证、提交并同步同一视觉集成分支后，使用仅含 `UAV_WS`/`VISION_WS` 的短命令运行一轮最终
  完整竞赛 Gate；轻量策略仓探索和整机上板仍只进入规划。

---

## 2026-09-05：冻结 r41 结论并制定双仓交付收口计划

### 改动范围

- 新增双仓最终交付、远端合并、坐标/旧接口减法、轻量策略仿真、视觉运动模糊/MJPEG 和实机
  部署计划；同步 `VISION_2026_ROADMAP.md` 的 r41 当前状态。
- 本条只读取既有 r41 产物和三个仓库远端状态，不修改视觉算法、导航业务、Planner、飞控、舵机
  或实机配置，不启动 ROS/Gazebo/PX4 或实机节点。

### 具体改动

1. 复核 r41 manifest 和 Gate：视觉 `8e53bd0`、导航 `3557215`、KS2A543、seed=11；完成三目标
   三槽、三次恢复、9 段、三门、H、AUTO.LAND、ON_GROUND 和 disarm，0 碰撞/越界/超高，
   任务时钟 `429.875 s`。原报告唯一 FAIL 为主机墙钟 `1761.101 s` 被错误纳入比赛时限。
2. 区分 Gate 口径修复、passive audit 去除重复高度裁决和 LAND deadline 简化：前两项由 r41
   直接支持；LAND 实际仅用 `32.73 s`，deadline 简化不得描述成降落超时修复。
3. `git fetch --prune` 后冻结远端关系：视觉 main `44359e8` 尚缺含 r41 报告的相机基线
   22 个提交和外参 2 个提交；导航上游 main `a182ca9` 尚缺完整任务分支 38 个提交；轻量仓 PR #1 已合入
   `main@99cfa33`。
4. 将最终交付定义为两个 tagged 源码 revision + 单一 integration manifest + 精简板端 runtime，
   明确导航仓不再携带视觉运行副本、正式 H 使用已有 typed mapped detection、宽兼容桥仅留 legacy。
5. 实机投影基准冻结为 MID360 IMU 到相机正下方 `0.21 m`；飞控盒到相机 `0.16 m` 仅作另一机械
   链口径。仿真 r41 的 `-0.05 m` SDF 保留，真实机架另建 profile，不静默覆盖历史几何。
6. 轻量仓先导入 r41 阶段耗时和 toudi3 几何，再用配对场景筛出唯一策略候选；Gazebo 只验最终
   候选，不承担网格扫参。
7. 运动模糊/MJPEG 设计为视觉评测侧单一图像退化适配器：检测器仍消费 raw ROS Image，MJPEG
   模拟 encode/decode，运动模糊按曝光和 pose 估算，默认 off 且不新增导航接口。
8. 盘点 worktree：根树、两个在制视觉树和已推送导航候选树保留；历史 clean checkout 在确认远端
   引用后移除；含未提交修改的工作树禁止自动清理。
9. 纳入导航 `vcl06-local-full-mission@6c59e17` 的本地 HANDOFF，明确其 2026-09-05 顶部更新
   覆盖 r3～r11 正文中的旧阻断结论，最终移交页面不再并列两套“当前状态”。

### 验证结果

- r41 `gate_status.json` 共 38 项检查，其中 37 项为 true；唯一 false 为
  `mission_wall_within_limit`，且唯一
  `/fastplanner/goal` 发布者为 `/navigation/planner_bridge`。
- 视觉、导航和轻量仓远端均完成 fetch；候选与 main 的祖先/提交计数通过 Git 验证。
- 新文档和 Roadmap 通过 `git diff --check`；未重跑仿真或 ABCD。

### 遗留问题

- 导航 r41 口径修订已形成 clean/pushed `6c59e17`，但尚未经过上游 main 合并评审。
- 视觉 `56f0667` 与外参 `f0b1b8b`、导航 `6c59e17` 均未合入各自 main。
- typed H、导航仓视觉副本、MID360 权威 TF、运动模糊/MJPEG 和板端整机共载仍是计划项。

### 下一步

- 按计划先合并视觉两分支，再提交导航完整任务 PR，冻结
  integration manifest，完成板端 A–C Gate，最后只跑一次最终双仓完整 SITL。

---

## 2026-09-05：r41 完整竞赛功能 PASS 与最终参数报告更新

### 改动范围

- 更新 `docs/VCL06完整竞赛分阶段验收流程时间与参数报告_20260905.md`；不修改视觉算法、相机、
  导航飞行逻辑、飞控、舵机或实机配置。

### 具体改动

1. 归档唯一最终整场 r41：导航 `3557215`、视觉 `8e53bd0`、KS2A543、seed 11 完成
   panzer/bridge/red_cross 三投与恢复、9 段路线、三门、H、AUTO.LAND、ON_GROUND、Manager
   COMPLETE 和 disarm；零碰撞、零越界、零超高，最高 `3.696886 m`。
2. 按原始 bag 修正完整时序：首个任务决策到 LAND `SUCCEEDED` 为 `429.875 s`；三投阶段
   `278.271 s`、九段到 LAND 指令 `118.878 s`、降落 `32.726 s`，比 600 s 留 `170.125 s`。
3. 明确原始 Gate 唯一红项是 WSL/Gazebo `1761.101 s` 主机墙钟，不是 ROS/比赛飞行时钟；导航侧
   已把墙钟降为卡死 watchdog。三次投递高度 `0.291/0.293/0.290 m` 均由 release_guard 的
   `0.30 m` 权威上限授权；导航侧同时删除只读审计的旧 `0.25 m` 重复裁决。
4. 报告把过期 r35b、8 段、60 s 航段、LAND 90 s 和双投递高度口径替换为 r41 最终事实、9 段
   r4 路线、90 s 运动段、LAND 全场剩余预算及单一 release_guard 权威，并列出 25 个离散目标上限。
5. 降落提速按实测分解：首 H 等待 `18.874 s` 是第一优化项，锁定后到 AUTO.LAND 为
   `6.667 s`；预热 H 检测和提高仿真交接高度只进入后续专项规划，本轮不改飞行参数。

### 验证结果

- r41 原始功能/合同检查除旧墙钟项外全部为 true；导航修正口径离线重判为 PASS；三次投递上下文
  用删除重复高度裁决后的审计逻辑重算为 `three_drop_chain_complete`。
- 导航侧 `uav_mission` 全量 220 项测试 PASS；报告 `git diff --check` PASS。
- 统一停止脚本确认 r41 收尾后无本机 ROS/Gazebo/PX4/RViz 残留；未启动第二轮仿真或实机节点。

### 遗留问题

- 当前是固定 seed 11 的 SITL 功能基线；GUI/RViz 展示、多 seed 统计、新相机实机外参、视觉与
  LIO/规划同机负载、QGC/遥控触发、真实舵机和实机 LAND 仍须分别验收。

### 下一步

- 停止同 seed 重复仿真。后续先规划 `sakelier/liftrace-sim` 的搜索策略统计，再规划整机上机；
  H/AUTO.LAND 提速仅做独立小范围 Gate，不回头改已通过的走廊策略。

---

## 2026-09-05：部署前事实复核与视觉 worktree 二次收口

### 改动范围

- 复核视觉、导航和轻量仿真三个仓库的最新远端、分支、worktree 与 r41 证据现状；修订最终交付
  计划和 Roadmap；移除两个已确认可重建的视觉 checkout。
- 不修改视觉算法、导航业务、Planner、飞控、舵机或实机配置，不启动 ROS/Gazebo/PX4/RViz。

### 具体改动

1. 以视觉组已完成的上一轮清理为新基线，不再沿用包含 `pr3-premerge-hygiene` 等目录的旧清单。
   复核后视觉仓实际只剩根树、`vcl06-execution-integration`、`vdeploy-camera-extrinsic` 和本文树。
2. `vcl06-execution-integration` 相对最新 `origin/main` 无独有提交；唯一 tracked 修改为
   `mid360_sim.yaml` 的 simulator parser 修正，功能已由 main 中更准确的 `lidar_type: 4` 实现。
   其约 12 MiB 旧 Gate 日志与未提交 diff 已迁至
   `logs/worktree_archive_20260905/vcl06-execution-integration/`，随后移除 checkout。
3. `vdeploy-camera-extrinsic@f0b1b8b` clean、已推送且无独有运行产物，移除 checkout；上述两条
   本地分支和远端分支均保留，不删除 branch/ref，不 push 或合并 main。
4. 纠正文档中的三处超前口径：`f0b1b8b` 当前实际发布 `body/-0.16 m`，MID360 `-0.21 m`
   仍是测量元数据；r41 的 H 控制仍依赖兼容桥；当前 WSL 工程树和既有归档中未找到 r41 完整
   结构化 run，现仅有报告和部分 ROS 日志，不能继续称为本机完整可复现证据。
5. 最终唯一整场 Gate 冻结 r41 已验证策略、H 适配和无退化图像链；typed H、导航仓视觉副本
   物理删除、轻量策略替换及 MJPEG/运动模糊均移到首个部署基线之后，避免再次引入跨仓变量。
6. 比较最终候选与 r41：视觉 `8e53bd0 -> 8de8444` 仅有文档增量；外参分支不改仿真 SDF 或
   视觉算法；导航 `3557215 -> 6c59e17` 只改 LAND/审计/Gate 口径，没有改搜索、走廊或 Planner
   执行路径。最终风险因而收敛到合并/overlay、实机 parent frame 和新 Gate 证据保存。

### 验证结果

- 清理前两候选 checkout 约 634 MiB 与 94 MiB；保留 12 MiB 归档后，净释放约 716 MiB。
- `git worktree list --porcelain` 确认视觉仓最终只剩根工作树和
  `vdeploy-final-closeout-plan`；导航三个 worktree 和轻量仓单一 checkout 未改变。
- 导航仓管理 checkout 的本地 `main@a68925d` 落后上游 `origin/main@a182ca9` 26 个提交；本轮只
  fetch 核对，不把该目录当最终构建源，也不代替人工更新/合并 main。
- `feat/vcl06-execution-integration` 与 `feat/vdeploy-camera-extrinsic` 本地分支引用仍可查询；后者
  远端仍为 `f0b1b8b`。归档中确认保留旧 `gate_status.json` 及 66 个文件。
- 复核 `patrol_control` 直接订阅 `/detect/land_mark_point`、视觉兼容桥从
  `/uav_vision/detections_mapped` 生成该话题；外参 launch 默认值和 profile 元数据与文档修订一致。
- 文档 `git diff --check` 通过；仿真进程固定名单为空。
- 导航候选 `uav_mission` 纯 Python 回归重新执行：220/220 PASS。

### 遗留问题

- 视觉 `56f0667 + f0b1b8b`、导航 `6c59e17` 尚未进入各自 main/tag；本轮不代替人工合并确认。
- r41 的原始结构化证据目录当前在 WSL 工程树和既有归档中不可用；若团队无外部备份，最终双仓
  revision 需用一次完整 SITL 重新产出 manifest、Gate、时序和必要运行日志。
- 导航 `vcl06-planner-stop-ack` 仍是含未提交协议实验的 dirty worktree，继续保留且不纳入最终链。

### 下一步

- 复用当前 closeout 分支合入 `f0b1b8b`，不再新增同义视觉分支；与导航 `6c59e17` 分别冻结
  候选后，先做 clean install/package 归属和外参静态验证，再以 r41 运行口径执行唯一一次最终
  联合 SITL。结构化证据迁出临时 worktree且 P3 PASS 后，才由人工将两仓 PR 非快进合入 main
  并打 tag；合并内容变化则重新验收。
---

## 2026-09-05：接入 2026 实机相机安装平移基线

### 改动范围

- 分支：`feat/vdeploy-camera-extrinsic`。
- 参数化接入新相机安装平移，更新视觉、部署和导航移交口径；不修改 2025 投递机构逻辑，
  不启动仿真、实机飞控或执行机构。

### 具体改动

1. 根据机械组汇报登记两套不可混用的物理基准：雷达 IMU 到相机光心正下方 `0.21 m`；
   飞控盒到相机正下方 `0.16 m`。群聊“约 21 cm”经项目负责人最终确认采用
   `0.21 m` 正式值。
2. 新增 `real_2026_ks2a543_body_runtime` profile。由于当前 FAST-LIO 的 `body` 由
   `/mavros/imu/data` 驱动且雷达 IMU frame 尚未接入 TF 树，运行接线暂用
   `body→downward_camera_optical_frame: [0,0,-0.16] m`；下视旋转沿用既有约定并标记待验。
3. `board_camera_vision.launch` 参数化发布上述静态 TF；增加
   `publish_camera_extrinsic` 开关，供整机顶层成为唯一 TF 发布者时关闭，未增加自定义
   消息、话题或兼容层。
4. 保留 2025 `z=-0.15 m` profile 作为历史证据，不修改旧投递对准补偿、舵机、PWM 或
   释放逻辑；补充安装基准文档、Roadmap、板端清单和导航 handoff。
5. 增加 launch 契约单测，锁定父子 frame、平移、旋转、发布开关和标准 TF 节点接线。

### 验证结果

- `test_detector_model_contract.py`：6/6 PASS。
- `xmllint`：`board_camera_vision.launch` PASS。
- Ruby/Psych 解析 `camera_extrinsic_profiles.yaml` PASS。
- `roslaunch --nodes uav_vision board_camera_vision.launch` 静态展开 PASS，且仅出现一个
  `/aircraft_camera_extrinsic`。
- `git diff --check` PASS。

### 遗留问题

- 飞控侧 `0.16 m` 是“飞控盒”口径，尚未区分盒体基准与内部 IMU 原点；雷达 IMU
  `0.21 m` 是后续精测主基准。
- 本次只新增机械平移；下视旋转、非零横向偏移、完整
  `camera_init→body→downward_camera_optical_frame` 和有效地图投影仍需静止实机验证。
- 导航当前 KS2A543/MID360 仿真 SDF 仍是旧机架几何；需先明确各 frame 原点再同步，
  不能把雷达基准 `-0.21 m` 直接写成 `body→camera`。
- 投递机构按用户要求沿用 2025 安装关系，但尚未重新量测或实机动作验收。

### 下一步

- 整机静止上电时检查唯一 TF 发布者、轴向和已知目标地图投影；随后由导航组同步仿真
  机架 frame 几何，再进入导航/LIO/视觉并发 Gate。

---

## 2026-09-05：修正雷达 IMU 相机距离为 0.21 m

### 改动范围

- 根据项目负责人最终确认，修正同日安装外参记录；不改变当前 `body→camera` 运行 TF、
  投递机构或任何控制逻辑。

### 具体改动

1. 雷达 IMU 到相机光心的正式垂直距离由 `0.20 m` 修正为 `0.21 m`。
2. 同步外参 profile、安装基线、Roadmap、板端清单、责任边界和导航 HANDOFF。
3. 飞控盒到相机的 `0.16 m` 与当前 `body→optical z=-0.16 m` 保持不变。

### 验证结果

- 外参 YAML 解析、launch XML、契约单测及 `git diff --check` PASS。

### 遗留问题

- 雷达 IMU frame 尚未接入当前 TF 树；`-0.21 m` 仍不能直接替换当前 `body` 口径的
  `-0.16 m`。

### 下一步

- 按修正后的 `0.21 m` 基准完成整机静止 TF 与仿真机架核对。

---

## 2026-09-05：合入实机相机外参分支并统一收口基线

### 改动范围

- 在 `feat/vdeploy-final-closeout-plan` 以非快进方式合入
  `feat/vdeploy-camera-extrinsic@f0b1b8b`；不合并或推送 `main`，不启动仿真或实机节点。
- 解决 Roadmap、导航移交、责任边界和追加型变更记录冲突。

### 具体改动

1. 保留 r41 后形成的最新 A/B/C/D、整场任务和 worktree 清理事实，同时完整保留外参分支的
   两条历史变更记录。
2. 接入 `real_2026_ks2a543_body_runtime` 和板端静态 TF 参数：当前运行父 frame 为 `body`，
   平移采用飞控盒口径 `z=-0.16 m`；雷达 IMU 到相机光心 `0.21 m` 仅作为机械测量元数据。
3. 更新视觉给导航组的下一步，冻结已跑通基线为视觉 `8e53bd0` + 导航 `3557215`，并明确
   首个最终 Gate 保留 r41 的 H 兼容通路，接口替换不得与外参/最终 Gate 同轮进行。

### 验证结果

- 外参契约单测 6/6 PASS；`board_camera_vision.launch` XML 解析 PASS。
- 外参 YAML 解析 PASS；`roslaunch --nodes` 静态展开 PASS，且只有一个
  `/aircraft_camera_extrinsic`。
- `git diff --check` PASS；未运行 ROS/Gazebo/PX4 或实机节点。

### 遗留问题

- `body` 到相机的 `-0.16 m` 仍是盒体工程口径；下视旋转、雷达 IMU frame、真实 LIO
  有效地图投影和唯一 TF 发布者仍待整机静态验收。
- 本次合入后尚未执行最终整场 SITL，不能把候选 revision 写成已验收交付 revision。

### 下一步

- 在同一收口分支缩小视觉正式链的旧兼容话题面，只保留 r41 首次部署所需 H 通路；随后完成
  静态回归和交付文档更新，再推送 feature 分支供人工评审。

---

## 2026-09-05：移除视觉兼容桥无效订阅并对齐 typed H 边界

### 改动范围

- 精简 `detect_compat_bridge.py`；只读核对导航侧最新提交和在制收口，不修改导航工作树、
  检测/融合/地图投影、target memory、投递许可、导航策略或飞控逻辑。

### 具体改动

1. 删除兼容桥对 `/uav_vision/drop_ready` 的无效订阅、`DropReady` 导入和空回调。该 typed
   话题由任务/控制链直接消费，原本不存在任何旧接口映射。
2. 保持 legacy bridge 的其他旧话题语义不变，不再为它增加“部分兼容”模式或第二个开关。
3. 将 `phase_d.launch` 和 `phase_d_board.launch` 的 legacy bridge 默认值改为关闭；历史
   `phase_d_patrol_internal.launch` 显式启用，避免 typed 默认入口无意发布 `/detect/*`，同时保留
   旧链回归能力。
4. 核对导航远端已前进到文档提交 `49ce16c`；其 dirty 在制改动拟让正式 external 模式直接
   消费 `/uav_vision/detections_mapped` 的 H，并用已有 `start_legacy_compat:=false` 整体关闭
   bridge。该方向比视觉侧再造一个 H-only compat mode 更干净，正式接线继续归导航权威仓。
5. README 明确：兼容桥只属于旧路线/显式 legacy 回归；正式链直接消费 typed 视觉接口。

### 验证结果

- `detect_compat_bridge.py` Python 编译 PASS；三份改动 launch 的 XML/静态节点展开 PASS。
- 默认 Phase D/board 展开不含 `detect_compat_bridge`，legacy patrol 包装器展开包含且仅包含一个；
  外参/模型契约测试 6/6 PASS。
- `uav_vision` 定向 `catkin_make -j4` PASS；本次未增加测试框架或新 ROS 参数。
- `git diff --check` PASS；ROS/Gazebo/PX4/RViz 固定进程名单为空，未启动仿真或实机节点。

### 遗留问题

- 导航 typed H/关闭 bridge 的改动当前仍未提交；远端 `49ce16c` 只是删除前基线文档，不能把
  dirty 工作树状态写成已交付能力。
- legacy 回归仍保留完整旧话题；正式链完全关闭 bridge 后必须在最终双仓 SITL 重新验证 H/LAND。

### 下一步

- 提交并冻结视觉候选；待导航侧将当前 typed H/关闭 bridge 改动提交后，以两仓精确 revision
  更新最终 HANDOFF，并在唯一一次最终 Gate 核对正式链中不存在 `detect_compat_bridge`。

---

## 2026-09-05：冻结视觉收口基线并更新双仓交接

### 改动范围

- 更新 Roadmap、视觉给导航 HANDOFF、双仓收口计划和责任边界；清理当前视觉临时 worktree
  的可再生构建产物。不修改导航 dirty 工作树，不启动仿真或实机节点。

### 具体改动

1. 将版本拆成三层记录：r41 跑通组合为视觉 `8e53bd0` + 导航 `3557215`；视觉运行代码冻结点
   为 `9324745`；导航清理前远端基线为 `49ce16c`，其 typed H/关闭 bridge/删除视觉副本仍明确
   标为未提交，避免把工作树内容借名给已有 revision。
2. 最终接口口径改为复用 `/uav_vision/detections_mapped` 的 `landing_pad`，正式链整体关闭
   `detect_compat_bridge`；旧 `/detect/*` 与 `/yolo_detect` 只保留给显式 legacy 回归，不新增
   H-only 模式、消息或 coordinator。
3. 更新 P1/P2/P3、Stage B、完成定义和当前责任：视觉 P1 已冻结；导航 P2 仍在制；两仓 clean
   联合构建后只跑一次最终 Gate，并增加单一视觉包来源和正式链零 legacy 话题断言。
4. 删除当前 closeout worktree 中约 `13.1 MiB` 的 `vision_ws/build`、`vision_ws/devel` 和测试生成
   `__pycache__`；均可由 catkin/Python 重建，源码、模型资产、证据归档和分支未删除。

### 验证结果

- detector/外参合同测试 6/6 PASS；三份 launch XML PASS。
- `phase_d.launch` 与 `phase_d_board.launch` 默认静态展开均为 0 个 `detect_compat_bridge`；历史
  `phase_d_patrol_internal.launch` 展开为且仅为 1 个。
- `detect_compat_bridge.py` Python 编译、`git diff --check` PASS；固定 ROS/Gazebo/PX4/RViz
  进程名单为空。

### 遗留问题

- 导航 typed H、正式关闭 bridge 和删除视觉副本尚未形成 clean commit；最终导航 revision 未冻结。
- 本轮没有运行 SITL；`9324745 + 导航待提交 revision` 仍需唯一一次完整 Gate 才能替代 r41
  作为最终可交付证据。

### 下一步

- 导航组提交并推送接口减法后回填精确 revision；随后 clean install 联合构建、静态节点/话题
  核对，再由用户明确授权最终一次 seed 11 SITL。

---

## 2026-09-06：回填导航接口减法提交并校准旧话题断言

### 改动范围

- 只读审阅导航新提交并更新视觉 Roadmap/HANDOFF/责任边界/双仓计划；不修改导航仓代码，
  不启动仿真或实机节点。

### 具体改动

1. 回填导航运行候选 `be85c423f13d840519f2c0af739394fcd557dcb1`：该提交已 clean 推送，
   直接消费既有 typed H、关闭正式 bridge、删除导航仓 78 个视觉副本文件，未新增消息或状态机。
2. 记录导航自报验证：空 build/devel 全量 `catkin_make -j1` PASS、`uav_mission` 222 项 PASS，
   正式 launch 包含 `/uav_vision/detections_mapped` 且不含旧 landing control/mark。
3. 将验收从笼统“正式链零 `/detect/*`”改为精确禁止 legacy 视觉桥话题。
   `/detect/point_class` 仍由 release arbiter 和 Planner Bridge 实际消费，不是冗余；当前不为改名
   新增别名，后续如迁移须一次性同时修改生产者和全部消费者。
4. 冻结最终待验组合为视觉运行代码 `9324745` + 导航 `be85c42`；两仓后续文档 tip 与运行代码
   revision 分开记录。
5. 记录导航后继文档 tip `b74b918`：只回填 Handoff、约 522 MiB 构建产物清理和视觉接力清单，
   不改变 `be85c42` 的运行代码。

### 验证结果

- 导航 HEAD 与远端同为文档 tip `b74b918`，工作树 clean；运行提交 `be85c42` 的 diff 确认
  external/legacy 分支隔离、`start_legacy_compat=false`、typed landing selector 和视觉副本删除。
- 静态检索确认 `/detect/point_class` 有一个生产者，并被 release arbiter 与 Planner Bridge 消费；
  不按“名字旧”误删有效控制状态。
- 视觉文档 `git diff --check` PASS；未宣称继承 r41 飞行结果。

### 遗留问题

- `be85c42` 的全量构建使用较早视觉 overlay，`9324745 + be85c42` 仍需 clean 联合静态复核。
- 导航 `uav_mission/README.md` 尾部仍保留“全量构建因 cv_bridge/local_sensing 失败”的旧段落，
  与 `be85c42` 同提交已修复并全量构建 PASS 的记录矛盾；该文档修订归导航仓负责人。
- 导航 `b74b918` Handoff 的视觉候选表仍停在 `e5fdc68 + f0b1b8b`，尚未回填已经合并后的视觉
  `9324745`，且有一行重复“没有被删”；不影响运行代码，但导航文档需在最终 manifest 前校正。

### 下一步

- 视觉侧保持冻结并把 `9324745` 回传；导航侧修正文档残留后，联合复核精确双仓组合。最终 SITL
  仍须用户当轮明确授权。

---

## 2026-09-06：实采 VCL06 整机 Nodes-only 拓扑并复核接口减法

### 改动范围

- 在视觉评测包增加 ROS Master 运行拓扑采集工具，使用 rqt_graph 自身的 `Nodes only` 后端；
  保存本次正式 VCL06 联合入口的图、原始清单和结构化关系，并形成接口清理复核文档。
- 不修改视觉运行算法和导航源码，不连接真实投递机构；仿真仅用于启动节点和采集注册关系，
  不作为整场 Gate。

### 具体改动

1. 新增 `ros_topology_snapshot.py`：直接采集 ROS Master system state、`rosnode list`、
   `rostopic list -v`、服务提供者和源码根；输出 nodes/topics/edges/services/one-sided CSV 与 JSON。
2. 调用 rqt_graph 的 `NODE_NODE_GRAPH` 生成核心链和全量链 DOT/SVG/PNG；严格采用椭圆节点、
   话题边标签和 publisher → subscriber 箭头，不再另造二部图语义。
3. 增加正式接口断言：旧视觉桥话题、旧 coverage manager、单一 planner goal 发布者、typed
   detections、`/detect/point_class` 真实合同、guarded Servo 服务、双仓源码根，以及已由服务 ACK
   取代的 `/control1~3`、`/servo/complete`。
4. 在 `docs/topology/vcl06_final_20260906/` 保存 2026-09-06 00:48 现场快照；新增
   `docs/整机ROS节点话题拓扑与接口清理复核_20260906.md`，明确视觉侧旧桥已清、整机侧尚余
   4 个导航/控制旧执行话题，以及应在 formal external/headless 模式关闭的
   `waypoint_generator`、`static_pointcloud_viz`。
5. 清理首版采集器曾额外生成的全量/任务二部图，仅保留用户指定的 rqt_graph Nodes-only 图和
   机器可读数据，避免图口径和维护面重复。

### 验证结果

- 正式入口现场采集 33 个节点、239 个话题、227 条发布边、187 条订阅边和 152 个服务。
- `detect_compat_bridge`、`coverage_search_manager` 与规定的旧 `/detect/*` 控制/结果话题均未
  注册；`/fastplanner/goal` 唯一发布者为 `/navigation/planner_bridge`；typed
  `/uav_vision/detections_mapped` 和 `/detect/point_class` 均有真实生产者、消费者。
- 接口总审计预期为 FAIL，唯一未通过项是旧执行话题：`/control1~3` 仅由
  `/patrol_control` 发布且无人订阅，`/servo/complete` 仅由 `/patrol_control` 订阅且无人发布。
- 最终拓扑运行目录为 `logs/ros_topology_rqt_nodes_only_20260906_004739/`；统一包装器收尾 PASS，
  无 ROS、PX4、Gazebo 或 RViz 残留。该运行主动在采图后结束，不是任务 Gate。
- 视觉定向 `catkin_make -j4` PASS；导航在先串行生成 FAST_LIO/Livox 消息 target 后并行 `make -j4`
  PASS。脚本 Python 编译、package XML、DOT/SVG/PNG 和 `git diff --check` 均通过。

### 遗留问题

- 导航/控制侧需让 external mission mode 不注册 `/control1~3` 和 `/servo/complete`，同时保留
  explicit legacy 模式；正式入口还应关闭无消费者的 `waypoint_generator` 和 headless 下的
  `static_pointcloud_viz`。
- 导航空 build 直接 `catkin_make -j4` 仍会在 FAST_LIO/Livox 生成头上发生依赖抢跑；需补齐
  CMake target dependency，不能把“串行预生成后成功”写成 clean parallel build 已根治。

### 下一步

- 导航侧按模式完成上述三项减法后，只重采一次 Nodes-only 拓扑；接口审计通过后，再由用户明确
  授权运行一次最终整场 Gate。无需重跑 ABCD 视觉矩阵，也不新增消息、兼容桥或 coordinator。

---

## 2026-09-06：复核最新工作树、规划波动和低空水平绕障搜索方案

### 改动范围

- 新增 `docs/规划波动与低空水平搜索评估_20260906.md`，本轮仅形成工程评估与修复方案。
- 读取视觉与导航候选、历史根 checkout、stop-ack 实验树及轻量策略仓；不改飞行源码、配置或场景。

### 具体改动

1. 核对最新正式组合为视觉 `0700c34` 与导航 `b69ba72`（运行代码 `1928f75`），保留 r41 完整功能成功记录，并区分 r42 首段超时和 r43 第一门碰撞。
2. 回读 r42 同轮拓扑 13/13 PASS，明确上一条记录中待清理的旧执行话题/辅助节点已由导航后续提交关闭；不再重复按旧快照要求清理。
3. 离线定位旧目标 effective-goal 校验发生在 retired 分流之前、从而错误终止新目标；r43 7 次偏移拒绝中存在旧代消息连锁误伤。记录确定性复现及最小修复边界。
4. 指出固定 0.1 m/s 重规划起速、全曲线空间选点、按时间碰撞前视之间的不一致；区分替代目标到达与原始门点到达，以及当前点云入口未应用虚拟顶棚。
5. 依据用户提供的水平绕障、树高约 1.5 m、无 bigbox 条件，提出今年场地 profile、1.3/1.4 m 低空候选、0.6～0.8 m 航带及 0.5 m/s 初始速度，并给出相机离地高度、完整圆环观察窗口和时间预算的限制。

### 验证结果

- r42/r43 的 Gate、manifest、投递 audit、碰撞记录和 debug bag 均已只读复核；两轮投递 audit PASS，整场 Gate FAIL。
- r42 投后首段按 bag 切片：实际累计位移约 9.39 m、结束时距目标约 0.958 m；r43 碰撞 ROS 时刻 445.849 s，命中 Wall_15/Wall_16。
- 纯 Python 复现旧 goal 8 的迟到 CANCELLED 导致新 goal 20 FAILED；现有 planner execution 25 项单测全部通过，该交错场景原测试未覆盖。
- KS2A543 B100 条件 CSV 中 1.2 m 与 1.8 m 各自在 0.5/1.0 m/s 的五类组合均 10/10；保留单 seed、夹具高度和完整过视边界，不写成新低空全场 Gate。
- 本轮没有启动任何仿真，精确进程检查为零残留。文档提交前执行 `git diff --check`；未改 CMake 或编译源码，未执行 catkin 构建。

### 遗留问题

- 上述逻辑/规划问题尚未修复；当前候选不能仅因拓扑 PASS 就声明鲁棒。
- 今年专用树/垫座和无 bigbox 世界、低空遮挡/横向覆盖、多轮整场稳定性尚未验证。
- 旧根目录用户改动与 dirty stop-ack 实验树原样保留；r41 完整结构化原始目录仍不可用。

### 下一步

- 在导航仓先做旧代消息隔离和原门点到达语义的最小修复，再修轨迹连续性并建立今年低空场景；视觉侧保持现有候选/释放主链，按新高度做针对性观察窗口验证。后续 SITL 仍需当轮明确启动授权。


---

## 2026-09-06：修复旧规划事件误伤后继任务及门点到达语义

### 改动范围

- 导航 uav_mission/planner_execution.py 与定向测试；当前用户已授权整套修复和 SITL 验证。

### 具体改动

- 已结束或交给视觉执行的旧目标事件先分流，不再用旧目标偏移/时效使当前新任务 FAILED；保留活动目标检查和旧 CANCELLED 的观测。
- RETURN_HOME/ABORT 按原任务目标检查实际到达，门点被调整后不能以到达替代点推进路线。

### 验证结果

- planner_execution 定向单测 27 项 PASS，新增旧目标 REPLANNING/READY/CANCELLED 迟到交错及原门点到达回归。
- 本提交尚未启动仿真，后续与 Planner 连续性、今年场地和安装外参组合后专项实跑。

### 遗留问题

- 轨迹进度、重规划初始状态、点云高度边界与今年场景仍待修复；本提交不代表整场 PASS。

### 下一步

- 修复 Planner 连续性并构建，随后运行用户已授权的专项和整场 Gate。

导航独立提交：`3ea0c6f`；视觉算法本阶段未修改。


---

## 2026-09-06：统一轨迹进度并准备低空水平避障实跑配置

### 改动范围

- 导航 Fast-Planner 的 plan_manage、plan_env、uav_mission 配置/launch；视觉 uav_vision_eval 的独立场景和机架。
- 旧规划包修改前快照：导航 `legacy_baseline/20260906_planner_progress/`，含文件清单与 SHA256；不参与编译。

### 具体改动

- 轨迹发送、执行 FSM、碰撞前视统一使用实际位置投影到连续前段的进度；取消全曲线搜索后跳末段的行为。
- 重规划使用实际速度；安全且完整的轨迹继续执行；失败优化不替换已有轨迹；优化结果复查膨胀占用与距离。
- 点云建图补齐高度上限，并可将搜索区域内观测障碍沿 z 延伸，约束水平绕障；场地边界外的门区保持三维建图。
- 新 `navigation_horizontal_search_vcl06.launch`：1.4 m 搜索、0.7 m 条带、1.6 m 取景接近、无槽位补偿；适当加快已有渐进下降参数。
- 独立今年世界去掉四个 bigbox，四棵树加 0.3 m 底座，总高约 1.5 m；保留旧世界用于回归。
- 相机 SDF/TF 同为 body z=-0.16 m，MID360 IMU 位于 z=+0.05 m；考虑模型内部传感器偏移后，FAST-LIO 的 MAVROS IMU 到雷达外参为 [0.011,0.02329,0.03588] m。
- 使用原有消息、任务入口和释放链，无新增协调器、ACK 协议或槽位补偿。

### 验证结果

- 主工作区 catkin_make 完整构建通过；plan_manage 的 catkin 测试汇总 16 项、0 错误/失败，planner_execution 27 项通过。
- XmlLoader 离线展开 33 节点，参数覆盖生效，零解析错误；确认两个槽位补偿数组均为零。
- 这是实跑前提交，尚不代表新场景整场 PASS。

### 遗留问题

- 窄门膨胀余量、实际导航稳定性、低空识别覆盖与取景下降仍须 SITL 实跑判定。
- 相机姿态暂沿用垂直下视；真实安装角如有偏差，应修改同一 SDF/TF 配置对。FCU 安装基准按 iris base_link，内部仿真 IMU 偏移单独处理。

### 下一步

- 按当前用户授权先运行 post_delivery 专项，分析首个失败后修复，再做搜索三投整场。


---

## 2026-09-06：修复实跑发现的回弯前视跳变和拟合端点漂移

### 改动范围

- 导航 plan_manage 轨迹选择、参数化与时间分配；沿用已有规划包快照。

### 具体改动

- 前视同时受沿曲线弧长与飞机距离约束，防止整个小回弯落在 0.4 m 球内时指令跳向另一侧。
- 三次 B 样条拟合后固定起始位置/速度/加速度控制点，完整路径固定终点；统一伸缩时间保持曲线几何及端点位置。

### 验证结果

- r44 专项 `logs/vcl06_horizontal_r44_post_20260906_185843/` FAIL：首航段 90 s 超时，零碰撞，包装器收尾零残留。
- bag 回放定位：成功样条起点距规划实际起点约 0.424 m；50–96 s 前视在约 1.4 s 与 4.7 s 两段间切换，导致来回追逐。不是门宽不足或通信适配器失效。
- 增加“整个回弯在球内”的定向回归；构建与 plan_manage 测试通过后提交。
- 相机原图已留在 r44/snapshots，安装位置影响已在 Gazebo 成像生效。

### 遗留问题

- 新修复尚待同场景实跑；首轮未进入三门及 H 阶段。

### 下一步

- 原 seed、原门点、原高度配置重跑 post_delivery 专项，仍使用原 Gate。


---

## 2026-09-06：按护圈位置修正门区垂直余量并保留逐靶取景高度

### 改动范围

- 导航 uav_mission 场景配置；patrol_control 外部 ALIGN 的高度初始化。
- 旧控制快照 `legacy_baseline/20260906_camera_alignment/`，含文件清单与 SHA256。

### 具体改动

- 护圈位于机体上方约 0.2 m，因此顶上障碍应向下膨胀 0.3 m；地面障碍向上膨胀 0.1 m。此前两方向误配，已纠正。
- 第一扇 0.5–1.3 m 竖向开口的两端门点从 0.9 降到 0.75 m，给顶部护圈与高度跟踪留余量；水平坐标、接触实体和 Gate 不变。
- 每次外部 ALIGN 恢复配置的取景高度，避免第一投后旧流程的 1.2 m 恢复值覆盖后续取景；十字分支同步处理。
- 新比赛配置去掉正式链不消费的 progressive_descent 参数块。实际下降仍由 drop_ready 后的原控制流程触发，launch 的位置前视从原 0.2 增至 0.4 m；保留零槽位补偿。
- 调试 bag 增加 Gazebo 模型状态与 LIO 里程计，仅用于核对估计坐标和碰撞真值，不进入任务控制。

### 验证结果

- r45 `logs/vcl06_horizontal_r45_post_20260906_191127/`：首段 18.55 s 完成、第二段 6.67 s 完成，约 47.95 ROS s 接触 Wall_15_Collision_3 上门梁，整轮 FAIL、收尾零残留。
- bag 显示穿门曲线 x 约 [-2.417,-2.362]，靠近门心；先前“曲线向左”的初判不准确，实际飞机被阻后向左偏移。原图、轨迹图与 trajectory_review.json 已归档。
- 实时 ROS 拓扑检查 PASS（32 节点、231 话题），未引入额外桥接链。
- 控制程序构建和相关测试通过后提交；本次门区高度修正尚待实跑。

### 遗留问题

- 仍需核对 Gazebo 真值相对 MAVROS/LIO 的高度差，并完成门区及低空三投整场。

### 下一步

- 保留旧成功路线及旧 Planner 有效实现；重跑专项，再进入联合整场验证。用户已要求推远端、保留分支合入 main，并交付精简整机比赛分支。


---

## 2026-09-06：保留原规划动态包络并按用户要求直接进入整场

### 改动范围

- navigation_horizontal_search_vcl06.launch：搜索动态上限恢复旧成功链的 1.0 m/s；实际位置跟踪继续使用 0.4 m 前视。

### 具体改动

- 取消低空配置额外将 Kinodynamic A* 速度包络压到 0.5 的设置；实际飞行仍低空水平绕障，预算速度按 0.5 m/s。
- 用户明确要求停止继续堆叠 post_delivery 专项，下一轮直接运行全任务 full 入口。

### 验证结果

- r46 `logs/vcl06_horizontal_r46_post_20260906_192454/` 起步大量 NO_PATH 后才生成路径，后已到达第一过渡点；按用户转整场要求主动终止，不能计作专项 PASS。
- 样本显示 MAVROS 高度 1.405、Gazebo body 高度 1.516、LIO 高度 1.862，约 0.11 m 的物理高度基准差说明上门梁余量必须考虑机体与护圈。LIO 高度漂移仍需后续结合真值跟踪。
- 对照旧规划器保留 Kino A*、ESDF 与样条优化；使用旧动态上限和已经成功的门前/门后航点方式，水平路线不新增虚拟门廊限制。
- 参数级修改无需重新编译；主程序及控制程序最近构建/回归通过。

### 遗留问题

- 起点不可达是否完全消除及完整低空闭环仍待本轮实跑。此前九段 post_delivery 是新场景验证入口，不是旧 8/8 或 9/9 原配置复跑。

### 下一步

- 单实例 full 仿真；结果归档，形成 liftrace-visionwork 的整机分支并保留分支合入 main。

## 2026-09-06：修正全场定位源分离与 MID360 仿真接线

- 日期：2026-09-06
- 改动范围：新低空场景定位配置、LIO 外部定位输出、SITL 起飞前参数设置、调试 bag。
- 具体改动：r47 完成三次投递、三次恢复、搜索途中零碰撞，门前 Wall_15 上梁碰撞 FAIL，已零残留收尾。199 秒前 MAVROS 高度约 0.90 m、Gazebo body 约 1.41 m，LIO 同时漂到约 -4.63 m。旧链把 LIO 地图和 GPS/气压计位姿都命名 camera_init，却没有外部定位融合，不能通过继续调低航点解决。
- 新配置使用 MID360 自带 IMU；按实际 ray/IMU 安装关系设置 IMU→雷达外参 [0.011,0.02329,0.00588]，地图体素细化到 0.15 m。LIO 位姿扣除旋转后的 IMU→机体安装平移，经标准 MAVROS vision_pose 输入 PX4；不订阅真值、不增加任务协议。SITL arming 节点在请求 OFFBOARD/解锁前设置外部位置/高度/航向融合，关闭 GPS 和气压计定位融合；参数只在新场景且 /use_sim_time 下生效。原实机入口不启用此节点。
- 验证结果：主工作区 catkin_make 全量编译通过；安装外参平移、非水平姿态旋转、无效四元数三项测试通过。r48 将直接执行整场，当前不宣称定位或整场通过。增加原始雷达、两路 IMU、动态 TF 与外部位姿入 bag，便于区分估计误差和规划问题。
- 遗留问题：新 LIO 闭环是否稳定须实跑确认，不能把历史 r33/r41 或 r47 三投等同于新场景全量 PASS。
- 下一步：整场实跑，核对 LIO/PX4/Gazebo 高度与门框点云，并继续完成集成分支和远端交付。

## 2026-09-06：修复仿真机体遮挡雷达并保留气压计融合

- 日期：2026-09-06
- 改动范围：新机架 MID360 安装、定位融合、物理 Gate。
- 具体改动：r48 全场在初始搜索阶段定位失真、越界 FAIL，已零残留停止。原始点云表明地面回波在机架加载后消失；模型射线原点 z=0.05588 m 仅比 iris 实体碰撞盒顶 z=0.055 m 高 0.88 mm，向下扫描撞自身盒体后被最小量程滤掉。恢复 MID360 0.08 m 支架安装，使 ray z=0.13、IMU z=0.12412，机体→相机 z=-0.16 不变；LIO 输出同步更新 IMU→机体外参。该尺寸是当前仿真机架值，实机仍需按安装测量配置。
- 根据用户补充，实际硬件保留雷达 IMU、飞控 IMU 和气压计。恢复 EKF2_BARO_CTRL=1，LIO 为外部高度参考，气压计继续融合；不是替换为额外测距硬件。新场景 Gate 用独立 Gazebo 实体位姿判物理高度、边界和过门，避免估计漂移掩盖真实超高；真值不接入飞行控制。
- PX4 使用官方 -w 参数把参数存储和飞控日志放到本轮 run/px4，避免某轮调参残留影响后续历史场景。
- 验证结果：Gate 40 项回归通过，含独立真值发现超高的新增测试；r48 的估计高度上限约 1.59 m，却发生实体超高，旧报告高度指标不足，不能当作真实高度合格。
- 遗留问题：LIO 正确地面回波和融合稳定性待 r49 全场验证；r48 导航启动时暂存修复尚未提交，运行代码对应随后 8009a7c，除文件末尾空白外无运行代码变化。
- 下一步：继续整场，检查原始下向回波、实体/估计高度、投递和走廊。

## 2026-09-06：按实机正装和 21 cm 相机外参统一融合建图

- 日期：2026-09-06
- 改动范围：新机架外参、PX4 融合选择、FreeDOM TF。
- 用户确认：MID360 正装在机顶，相机位于雷达 IMU 正下方 21 cm；飞控 IMU 和气压计均保留。
- 具体改动：雷达 IMU 机体 z=0.12412，因此相机光心机体 z=-0.08588；Gazebo 相机传感器 pose 与 ROS optical TF 同步修改，精确满足 0.21 m。r49 已证明抬高雷达后地面静止时恢复约 1347 个地面点，但正装飞行后垂直可观测性不足，LIO 高度仍漂移；机体最终到地面而估计仍显示巡航，主动停止并收尾，不标记 PASS。
- PX4 改为只融合 LIO 水平位置和航向，继续以飞控 IMU/气压计估计高度（EKF2_EV_CTRL=9，HGT_REF=0，BARO_CTRL=1）。FreeDOM 仍使用 LIO 输出的 IMU 坐标点云，但通过与控制/相机同源的融合机体 TF 加安装平移建图，去掉两个不同高度源共用 camera_init 名称的错误。真值只进 Gate。
- 验证结果：参数和 SDF/TF 安装几何检查通过；前一阶段 Gate 40 项测试通过。新高度接线待 r50 整场验证。
- 遗留问题：气压计绝对高度偏差在窄门处仍须以实际点云净空约束处理，不能只依赖固定 z 航点；板端需按真实标定载入安装参数。
- 下一步：继续整场并检查融合建图后的门框与实际飞行高度；完成独立集成构建和远端交付。

## 2026-09-06：在 PX4 估计器启动前加载传感器融合配置

- 日期：2026-09-06
- 改动范围：SITL 启动参数入口、新场景气压计测量噪声。
- 具体改动：本机 PX4 参数元数据明确 EKF2_HGT_REF 需要重启；旧做法在 MAVROS 连通后设置，不能保证已启动 EKF 切换到期望参考。改用原生 px4-rc.params 扩展入口：启动前在 run/px4/profile 生成只含参数的文件，先加载 PX4 原默认参数脚本，再加载场景参数，随后原生 rcS 才启动传感器和 EKF。启动前脚本通过 exec 替换为 PX4，不留下辅助 ROS 节点。
- 移除本轮新增的 arming 节点运行时参数设置逻辑，恢复原起飞辅助节点。气压计插件源代码为 1 Pa RMS（约 0.09 m），而默认 EKF2_BARO_NOISE=3.5 m；仅 SITL 将其配置为 0.15 m，保留余量，实机气压计噪声仍需实际测量。雷达 IMU、飞控 IMU、气压计都保留，不引入额外测距硬件。
- 验证结果：原生默认参数顺序、非法数值/命令拒绝两项测试通过；导航全量 catkin_make 编译通过。r50 正式启动前有一次 launch 文件名笔误，未启动飞行链且已收尾；修正后的 r50 飞行中仍有约 0.8–0.9 m 高度偏差，已分析后停止并确认零残留，不标记 PASS。
- 遗留问题：新的启动时序与噪声配置须在 r51 整场验证，不把参数设置成功当作飞行验收。
- 下一步：整场观察机体/估计高度、实际水平避障、三投和走廊，完成独立集成分支。

## 2026-09-06：外部任务自主降落复用于实机入口

- 改动范围：导航 `patrol_control/src/patrol_control.cpp`；视觉联调记录。
- 具体改动：外部任务完成 H 对齐后的 AUTO.LAND 使用既有 `switch/auto_land` 配置，不再额外要求 `simulation/enable_auto_land`。保留旧固定任务的原分支行为；没有新增任务握手或自动解锁。机载应用可复用同一 H 对齐、飞控 AUTO.LAND 和 MAVROS 落地确认链。
- 验证结果：导航全量 catkin_make（-j1、空白名单）通过，日志 `/tmp/liftrace_r52_build.log`。精简分支的硬件入口只展开 XML 验证：22 个节点，无 Gazebo、PX4 SITL、自动解锁、舵机 mock 节点；未启动任何实机节点。正在运行的 r51 仍是修改前 afad81a 二进制，本修复将在整机分支下一轮整场验证。
- 遗留问题：板端实时链、真实飞控参数和现场 H 降落仍须实机单独验收，不能以笔记本仿真代替。
- 下一步：整机独立编译和联合整场仿真通过后合入 main；保留功能分支并推送。

## 2026-09-06：近目标短连接失败继续避障搜索

- 改动范围：Fast-Planner `path_searching`、`plan_manage`、`plan_env` 测试访问声明；整场调试 bag 话题。
- 具体改动：原 Kinodynamic A* 在起点已位于目标三体素范围时，直接连接被占据格挡住就返回 NO_PATH；从其他节点到此范围则提前返回 NEAR_END。改为连接成功才终止，失败继续正常展开，允许在门前或树旁寻找侧向绕行。搜索阶段与最终轨迹验证一致地拒绝地图范围外点；短连接逐点检查包含确切末端。失败重试复用已有 0.75 s 重规划间隔，避免 100 Hz 重复同一失败。调试 bag 增加 FreeDOM 静态点云及规划膨胀图。
- 验证结果：导航全量编译通过。真实搜索器 + 内存占据图 3 个回归通过：无遮挡近目标、短连接被墙阻挡但可水平绕过、封闭墙不能逃出地图。测试 run：`logs/kino_near_goal_regression_20260906_212439/`，3/3、零错误，收尾零残留；该测试没有飞行模拟或硬件服务。初次无 ROS master 的 gtest 启动因 NodeHandle 等待参数服务而中止，已改成 rostest 包装运行。
- r51 整场记录：`logs/vcl06_horizontal_r51_full_baro_20260906_204726/`，三投及三次恢复完成，实际最高高度 1.7748 m、零碰撞/越界/超高；第 2 个门前航段 90 s 超时 FAIL。近目标连接失败累计 6745 次，未放宽到达阈值或把 FAIL 改成 PASS。高度参考修复有效，但新搜索补丁仍需完整飞行验证。
- 保护快照：改动前 `path_searching` 已保存到 `legacy_baseline/20260906_planner_progress/path_searching/`（含清单与 SHA256）；其他规划包已有同批保护快照。
- 遗留问题：新场景完整三门与 H 降落仍待实跑；板端链和实机另行验收。
- 下一步：将修复导入精简整机分支，从该分支独立编译并继续整场。

## 2026-09-06：2026 导航与视觉精简整机分支

- 改动范围：`feat/r2026-competition-integrated` 的导航/视觉编译工作区、运行入口、配置、说明文档与 AGENTS.md。
- 具体改动：导入导航 fbaf58b 与视觉 e2214ec 对应的当前工程，保留 FAST_LIO、FreeDOM、Fast-Planner、patrol_control 执行器、Mission Manager、uav_vision、评测及实际编译依赖。按用户要求移出本分支中的旧工作区、冻结快照、迁移参考、旧搜索/视觉节点和机械 PWM 实现；原始资产在来源分支保留。外部控制配置只保留起飞种子，搜索、靶标和返航均由当前 Mission Manager 下发。相机安装关系精确为雷达 IMU 正下方 21 cm，不启用槽位偏差补偿。
- 运行入口：`build_competition.sh` 独立构建两个工作区；`run_competition_sim.sh` 经统一 sim_run 单实例启动，去掉自动借用其他 worktree 编译产物的回退。调试 bag 包含实际点云、膨胀图、IMU、飞控位姿、相机压缩图与全任务状态。`competition_hardware.launch` 连接 RKNN 与当前导航控制链，使用真实雷达自身 IMU；设备驱动/机械服务由机载设备提供，不启动模拟器、mock 或自动解锁。
- 文档：重写分支 README、ROADMAP、环境/外参/接口/硬件/验证/来源说明。AGENTS.md 因分支按用户要求精简而改为本分支目录和工作流程约定；原始资产保护仍适用于来源 worktree，仿真单实例及实机动作单独授权不变。
- 验证结果：导航和视觉在本 checkout 独立全量编译 PASS；任务/控制/轨迹检查 284 项、零失败、零跳过；导航近目标真实搜索 3/3 回归已在来源分支通过。SITL 与硬件入口 XML 展开通过（35/22 节点）；硬件入口无 SITL/arming/mock 节点，未执行实机操作。日志 `/tmp/liftrace_integrated_final_build.log`、`/tmp/liftrace_integrated_checks.log`。
- 遗留问题：本分支尚未完整飞行通过；r51 为三投、零碰撞但门前超时 FAIL。板端 ROS 实时、真实飞控参数与实机飞行未验收。
- 下一步：从干净提交启动本分支整场；通过后推送联合日志。main 通过 feature 的 --no-ff 合并接收同版运行代码并保留原始资产，精简分支独立保留。

## 2026-09-06：精简分支保留来源分支历史

- 改动范围：精简整机分支合并历史与联调记录。
- 具体改动：以 --no-ff 合并视觉来源分支 e2214ec；共用追加日志的冲突保留全部来源条目及整机分支条目，运行代码未新增变化。
- 验证结果：合并前整机 1ef24a2 独立编译、284 项检查通过；确认来源日志是当前整机日志完整前缀。
- 遗留问题：联合整场仍待执行，未标记 PASS。
- 下一步：以合并后的干净提交启动整场并保留原始产物。


### 2026-09-06｜修复绕树轨迹拟合与重复规划停滞

- 改动范围：Fast-Planner 搜路采样、曲线选择、水平搜索配置及离线现场复现工具。
- 具体改动：运动基元检查间隔上限 0.02 s，与最终曲线检查一致；当前场景控制点间距由 0.5 m 改为 0.2 m；优化曲线失败时仅保留通过同一完整碰撞/净空检查的初始拟合曲线。机体膨胀、净空阈值及任务接口不变。
- 验证结果：导航完整编译通过（/tmp/liftrace_dense_search_build.log）。r52 在精简整机分支完成 1 投后搜索超时，已主动停止，收尾零残留。冻结该轮真实地图和停滞位置，原方案连续无法下发；修复后 frozen_search_map_dense_20260906_221040 输出完整终点曲线，planner_replay_result.json=PASS，收尾 PASS。此为 ROS 规划复现，不是走廊或整场通过记录。
- 日志位置：r52 原始记录在 r2026-competition-integrated/logs/r2026_integrated_r52_full_20260906_213503；复现记录在 vdeploy-final-closeout-plan/logs/。测试工具接收地图文件、目标和完整规划参数，保留原始结果。
- 遗留问题：尚不能宣称稳定三投；正装雷达/气压计新场景须继续整场验证，首次冻结复现使用静止速度近似。
- 下一步：同步精简整机与 main 集成分支，运行三投、走廊、H 降落完整链条；通过前不合入 main。

- 整机分支追加验证：独立完整编译通过 /tmp/liftrace_integrated_curve_build.log；显式构建测试二进制后 kino_dense_regression_built_20260906_221555 搜路三例 3/3、清理 PASS。首次测试因二进制尚未构建未执行任何用例，补齐构建后通过。


### 2026-09-06｜R53 三投实跑收尾与整机拓扑交付

- 改动范围：验收记录、rqt_graph Nodes only 图及脚本 Git 执行位；无新增飞行算法修改。
- 具体改动：记录 R53 三投/三次恢复 3/3、零碰撞与门前超时 FAIL；保存全量原始拓扑、23 节点阅读版、SVG/DOT/交互 HTML 与结构化 ROS 连接关系。修正由 Windows 侧文件模式跟踪造成的脚本 Git 执行位缺失，便于新机器直接执行启动入口。
- 验证结果：R53 源码 133e512；实体最高 1.753 m，无越界/超高；后段第二航点搜路 47.915 s 后出现大幅绕行，90 s 超时中止；Gate FAIL；完整收尾并复查零残留。图的节点形状已核对均为椭圆，标签保留实际 ROS 话题及发布到订阅方向。
- 日志：r2026-competition-integrated/logs/r2026_integrated_r53_full_20260906_221720；6 个 bag 共 5.55 GB，报告 docs/verification/r53/REPORT.md，拓扑 docs/topology/r53/；大日志经预发布归档提供，不进入 Git。
- 遗留问题：当前只确认本轮三投成功；门前规划阻塞与绕行尚未解决，不能宣称稳定三投或整场通过。main 待验收，未合并。
- 下一步：遵照用户“本轮后不论成败均收尾”的要求，停止后续修复与仿真；推送记录和保留的功能分支，后续开发等待用户新任务。


### 2026-09-07｜R54 走廊入口航点适配（仅一轮验证）

- 改动范围：水平场景 runtime YAML、对应 Gate 航段编号和本轮记录。
- 具体改动：参考旧成功链 3557215 的门洞中心线与前后引导点，适配 1.4 m 搜索高度；在原南侧过渡点先降至 1.05 m，再前进 0.5 m 降至 0.75 m，形成门外低位观察点；保留原门前坐标，门后清空点向北延伸 0.2 m 后再升高。路线 9 段变为 11 段，三门评分索引同步为 5/8/10，几何边界和到达标准不变。
- 验证结果：YAML 解析、三门过平面方向和索引、搜索及任务其他配置一致性检查通过；没有编译项变化。R53 既有地图离线检查显示降高/低位接近区有空间，但门洞附近已被膨胀占据，因此本方案依赖低位靠近后的正常地图更新，不能凭离线检查宣称过门成功。
- 遗留问题：本次只尝试航点引导，不改变规划算法、地图膨胀、超时或通信链；门洞可达性须由本轮实际结果判断。
- 下一步：仅启动一次整场 SITL；无论成败都收尾、记录并推送，不再重跑。


### 2026-09-07｜R54 唯一正式测试启动超时并收口

- 改动范围：本轮结果、任务状态及日志归档记录；无追加算法或参数改动。
- 具体改动：记录已推送的 11 段入口引导方案，标注实际尚未执行，保持 main 待验收。
- 验证结果：正式 launch 静态展开通过；唯一正式 SITL 在 180 s 启动期限内仅推进至 run.log 最后 ROS 时间戳 6.178 s，Gate FAIL/startup_wall_timeout，未解锁、未起飞、无任务决策。统一收尾 PASS，零残留，无第二轮。
- 运行说明：预检查引号错误误起空 Gazebo 后已清理；正式仿真随后仅启动一次。记录见精简整机分支 docs/verification/r54/REPORT.md，完整日志 r2026_integrated_r54_guided_entry_20260907_023929。
- 遗留问题：新航路尚未获得飞行验证，本次无法判断入口方案效果；不得称其通过或宣称稳定三投。
- 下一步：按用户要求收口，推送功能分支和记录，不继续修复、不重跑。


### 2026-09-07｜R54 原配置同轮复跑，三投成功、门前超时并收口

- 改动范围：R54 复跑结果、任务状态、拓扑图和原始日志索引；无新增运行代码/配置改动。
- 具体改动：依用户追加授权，以同一 R54 轮号原配置复跑一次，保留首次启动超时目录；补充前三个引导段及第 4 段失败时间线，记录 rqt_graph Nodes only 拓扑。修正首次 bag 缺失 /clock 被误记为 0.000 s 的摘要，run.log 末个 ROS 时间戳为 6.178 s。
- 验证结果：panzer/bridge/red_cross 三投及恢复 3/3；前三引导段 17.438 ROS 秒内完成；第 4 段等待 36.602 秒出首条轨迹，随后绕行并在 90 秒期限耗尽时 FAIL。碰撞/越界/超高均 0，实体最高 1.712 m，门洞 0，未自主降落。统一收尾 PASS，零残留。
- 记录：整机分支 docs/verification/r54/REPORT.md；复跑 logs/r2026_integrated_r54_guided_entry_20260907_025104；首轮启动失败继续保留，同属 R54。
- 遗留问题：航点调整未解决门前最后约 0.5 m 的可达性与绕行；首次启动超时的硬件根因未确定。本轮三投成功不代表稳定性验收，main 不合入。
- 下一步：按用户要求收口，仅推送记录和归档，不继续修复或重跑。


### 2026-09-07｜R54 门前失败离线根因与走廊模式方案

- 改动范围：R54 诊断文档、结构化分析和示意图；无运行代码、参数或接口修改。
- 具体改动：对照 r41 导航 3557215 与当前 planner 历史；确认门前目标格被门柱膨胀占据，近目标继续扩展后接受距目标 7.32 m 的 horizon 轨迹；记录同步搜索阻塞地图更新、旧轨迹继续执行导致新轨迹起点陈旧。提出复用现有 POST_DELIVERY_ROUTE 的走廊模式。
- 验证结果：读取 R54 既有 bag；地图发布间隔 36.659/33.864 s，而上游点云最大间隔 0.205 s、位姿 0.062 s。保留 0.30 m 护圈包络、5 cm 格的离线局部重建使 0.75 m 门前/门心查询为空闲；不是整条路线或实飞 PASS。
- 遗留问题：走廊精细地图、无进展局部轨迹处理及非阻塞搜索尚未实现/飞行验证；不能直接回退 20 cm 膨胀或宣称复用旧版即可通过。
- 下一步：本次按诊断请求提交并推送报告；未启动仿真。后续实施按报告方案处理，实跑须有新的明确启动授权。

- 诊断报告：https://github.com/Qinling-Melon-Farmers/liftrace-visionwork/blob/feat/r2026-competition-integrated/docs/verification/r54/DIAGNOSIS.md


### 2026-09-07｜核正 r41 垂向膨胀与离线对照

- 改动范围：R54 诊断中的旧参数对比；无运行代码或参数改动。
- 具体改动：按 3557215 的 patrol_planner_sim.xml 核实 r41 向上膨胀 0.20 m、向下 0.10 m，修正此前分析表暂按对称 0.20 m 建模的错误，并更新分析包。
- 验证结果：纯离线同一点云重建中，r41 实际参数下 0.75/0.90 m 门前与门心四个查询均空闲；当前 30 cm 包络、5 cm 格下 0.75 m 两个查询也仍为空闲。没有执行 ROS/planner/SITL。
- 遗留问题：有限格点空闲不等于整段动态穿门通过；旧版完整净空检查、当前护圈/姿态及定位配置的差异仍须结合考虑。
- 下一步：推送更正报告并替换本次刚上传的分析补充包；不改变既有仿真结论。

## 2026-09-07 R55 门洞精度、短目标及搜索阻塞修复

- 改动范围：Fast-Planner 的 path_searching/plan_env、水平绕障参数、离线检查及 R55 说明；导航 launch 复用整机分支的同名 YAML。
- 具体改动：5 cm 有界地图保留 30 cm 水平膨胀；连续体素填充、最新点云；占用目标立即失败，短目标要求完整路径，局部路径要求实际接近目标；短连接优先与 0.25 s 搜索预算。沿用任务状态机及原话题，没有新通信契约。
- 验证结果：精简整机视觉/导航构建通过；7/7 C++ 离线测试通过。R54 冻结点云在粗网格复现阻塞，细网格完整接近/穿门路径通过占用和 0.025 m 净空检查；建图及 ESDF 160 ms。完整说明见 docs/verification/r55/FIX.md。
- 遗留问题：新版本尚未整场实跑；板端地图资源与实时链仍需独立验证，不能继承历史 9/9 为新场景通过。
- 下一步：执行用户授权的单次完整 SITL，无论成败收尾并记录推送；未通过不合入 main。

## 2026-09-07 R55 中断后的记录入口与存储检查修复

- 改动范围：整机仿真包装器、记录器 launch、运行说明；不改飞行算法。
- 具体改动：记录器原订阅 /downward_camera/image_raw/compressed，但 R55 实时 ROS 图中该话题没有发布者，bag 也没有 Image/CompressedImage。改为参数化的真实原始图像话题，并补录检测原始输出及 /fastplanner/goal。整机包装器支持 SIM_LOGS_DIR，WSL 通过 SIM_STORAGE_GUARD_PATH 检查 VHDX 所在宿主盘，默认要求宿主盘和记录目录分别保留 20 GiB；不再只相信 ext4 内部虚拟容量。
- 验证结果：launch XML、bash 语法检查通过；缺少 WSL 宿主盘配置时拒绝、当前 F 盘约 5.5 GiB 时拒绝、充足空间目录检查通过。没有启动 ROS/Gazebo/PX4 或新增飞行。
- 遗留问题：R55 既有画面无法补录，H 无有效检测的视觉根因不能仅靠现有 bag 定论；新录图配置尚未飞行验证。20 GiB 是启动前余量检查，不能替代长时间录制的空间预算。
- 下一步：完成 R55 中断记录和原始数据归档、推送功能分支；本轮不重跑、不标记整场 PASS、不合入 main。

## 2026-09-07 R55 三投三门完成、存储中断与日志恢复收口

- 改动范围：R55 实跑报告、恢复产物、拓扑、任务状态和冗余日志清理记录。
- 具体改动：仅启动一轮完整 SITL；宿主 F 盘耗尽后终止故障 WSL 实例并离线恢复。保留原始中断 bag，在 E 盘副本重建索引；依用户授权清理 47.69 GiB 旧大日志和重复归档，保留当前轮次与历史成功记录。
- 验证结果：3 槽投递、11 航段、3 门完成，碰撞 0；goal 19 首轨迹 0.203 s。原 Gate reducer 离线确认过门顺序且无错误。可恢复记录止于 239.976 ROS s，仍 LAND/OFFBOARD，H mapped 检测 0，未见自主降落；最终 Gate 未写出，整场 INCOMPLETE。仿真资源零残留。
- 记录：docs/verification/r55/REPORT.md、recovery_manifest.json、Nodes only 拓扑及远端 sim/r2026-r55-bounded-corridor 预发布。
- 遗留问题：原压缩图话题无发布者，既有画面缺失；H 漏检根因仍待有效画面复核。磁盘内部删除不等于宿主 VHDX 已缩小。main 尚未合并。
- 下一步：本轮收口，推送代码/文档及归档；不新增仿真，未来另行验证 H 降落。


## 2026-09-07 R56 H 取景几何修正

- 改动范围：水平场景最后 H 航点、外部降落取景高度、离线几何验证及相机缺录复核。
- 具体改动：H 模型外圈直径 1 m，与下移相机在 0.75 m 的取景条件不匹配；最后航点和 capture_height 同步改为 1.60 m。沿用现有 planner、走廊状态机、H 结构/稳定帧和 AUTO.LAND 流程，不放宽检测阈值。
- 验证结果：生产几何函数+当前 YAML 对实际 H 纹理重投影检查 3/3 符合预期，旧高度不检出、新高度检出；配置一致性及 launch XML 检查通过。首段 R55 bag 从启动就没有图像，压缩话题无发布者，不能把缺录全部归因于末段磁盘耗尽。
- 遗留问题：离线图像不包含全部光照/姿态因素；新取景与 R55 收尾后录图修复尚待本轮飞行确认。R55 仍 INCOMPLETE。
- 下一步：在已回收宿主空间、WSL 恢复后，执行用户本次授权的唯一一轮整场，归档完整结果并更新报告。

### 2026-09-07｜R56 按用户要求在 WSL 原生目录同轮重跑与收口

- 改动范围：报告、全部参数、拓扑、环境/接口/来源/路线图、历史阶段状态；视觉只读拓扑脚本去掉旧 worktree 硬编码判据。未再改飞行源码。
- 具体改动：本轮源 f0f7480 保持不变，日志、PX4 工作目录与归档恢复到 WSL 项目 logs，不再写其他盘。报告覆盖 R41—R56、R55 中断/错录图/H 几何、三投与失败根因、全部阈值；35 节点 Nodes only 图来自本轮实时注册。R53 Release 清理保留源码 tag，VHDX 实际回收 68.06 GiB。
- 验证结果：R56 同轮重跑三投/恢复 3/3，前三门外引导段完成，goal 19 在 90.04 s 后运动超时，整场 FAIL；零碰撞、越界、超高，Gazebo 机体原点最大约 1.739 m，H 未执行。原图和 PX4 八项参数读回成功，记录缓冲溢出 0，收尾零残留。三时刻冻结地图证实目标由同一普通膨胀点覆盖，水平绕障列贡献 0。只读接口检查按实际注册重跑；JSON/YAML、文档链接和 git diff 在提交前检查。
- 遗留问题：门前持久地图点观测支持与安全高度窗口仍需处理，H 至落地完整记录、多 seed/板端/标定/扰动接管/机械落点仍需验收；main 不合并失败配置。
- 下一步：提交并推送导航 fork、视觉精简和 main 集成准备分支；上传同轮完整归档、报告和拓扑，保留分支；本轮不再自动仿真。小图表/事实和参数入库，bag/大日志/冻结地图原数据只在 run/Release。

本阶段补充：检查发现此前入库的 H 重投影三张 PNG 与几何 JSON 为零字节；已从现存有效源附件恢复，并验证格式。当前 11 项注册接口检查 PASS；包来源继续由 sim_run 校验。

用户最新修正：失败轮次不全量发布 Release；转为追溯 FreeDOM 持久地图点、修复后再验。当前只提交失败报告和小产物，完整成功后再发布全量记录。

### 2026-09-07｜R56 根因修复：接触代理遮挡与门洞清空尺度

- 改动范围：仿真接触代理 Gazebo 插件及模型、FreeDOM 竞赛参数/输入队列、仿真插件搜索路径；实际巡航/三门/H 航点、碰撞几何和规划膨胀未改。
- 具体改动：接触代理采用 Gazebo 碰撞类别掩码，继续与世界/机体发生物理接触，同时不再挡住激光射线；保持其 z=.20 m、原网格和接触传感器。FreeDOM 空闲体素 .40→.10 m，保留 6 次空闲观测与原连接关系；室内范围 15 m、增强深度上限 12 m，提升近距离深度分辨率。输入队列 100→1，避免旧点云积压。实机与仿真共享竞赛建图 profile，接触代理插件仅用于仿真。
- 验证结果：实际 60 帧雷达数据证实近处自身遮挡；Gazebo 自带 ODE 上，完整网格射线 .254933 m，掩码修正后看到后方 1.45898 m，护圈与墙物理接触仍为 1，PASS。生产 FreeDOM 的离线门洞几何对照中 .40 m 始终保留旧点，.10 m 于第 6 帧清除；门梁有效点保留（最少 59、最终 61）。此为几何/生产代码离线对照，非实际穿门 PASS；接下来构建和新授权整场实跑。
- 遗留问题：新完整闭环仍待实跑。不得仅因单点坐标恒定直接删除地图点；不得关闭接触判据或减少护圈余量。失败轮次不发布全量 Release。
- 下一步：完成构建、记录实跑源码后重跑；通过后发布完整成功记录、同步文档并 --no-ff 合并 main。按仓库规则在导航与 main 集成分支保存原包快照，精简分支不带旧包快照。

本阶段验证补充：视觉与导航完整构建 PASS；完整四边形网格三角化探针 PASS，59/60 帧原始回波与代理求交误差≤1.13 mm。增强深度视场同步为实测对应的 [-7,52]°；重新运行生产 FreeDOM 对照，旧点仍在新尺度第 6 帧清除、门梁点保留。所有源码与证据均在 WSL 内，未发布失败全量 Release。

### 2026-09-07｜R56 根因修复后完整 PASS 与最终交付

- 改动范围：最终报告、全部运行参数、实际相机帧/回放索引、rqt_graph Nodes only、来源与当前文档。飞行源码保持 cc899f2，不再追加仿真。
- 具体改动：归档三投/三恢复、11 航段、三门、H 对齐→AUTO.LAND→ON_GROUND→disarm 的完整结果；统一当前文档为 PASS，并保留所有历史失败/中断原结论。新增成功源标签与 main --no-ff 验收计划，功能分支保留；失败轮次不全量发布。
- 验证结果：运行 Gate 37/37 PASS，182.924 ROS s / 762.126 wall s；碰撞/越界/超高 0，收尾零残留。goal 19 首条 ready 等待 .072 ROS s；机体最大 z≈1.760 m，接触代理最高点约1.982 m，落地中心误差约.162 m。原图4046帧，缓冲溢出0；PX4八项读回成功；注册接口11/11。整机及 main 集成分支独立构建 PASS。
- 遗留问题：一次 SITL 成功不等于多场景/板端/实机验收；仍有多 seed与实时负载、板端ROS10min、安装标定、扰动接管、机械与实际落点五项工作包。
- 下一步：提交推送文档、发布完整成功记录，按 --no-ff 合入 main 并打 annotated tag；保留分支和必要历史小材料，清理已归档的大日志副本。

### 2026-09-07｜R56 工程适用性复核、模型图与 tmp 清理

- 改动范围：报告/实机说明/README的仿真适用性表述，新增模型外观图与来源。未改飞行代码、参数或原PASS结果。
- 具体改动：明确接触代理射线过滤只适用于虚拟余量，不适用于真实护圈；补充初始占据点来源未被直接证明、队列1会丢弃旧帧、板端负载待验收。用实际SDF及Gazebo网格导入器离线绘制当前模型，明确不是飞行截图。
- 验证结果：与R56修复前模型做结构比较，仅新增指定接触插件；本轮归档SDF与当前模型一致，iris/mid360外部SDF与归档一致。图像实际打开检查；原37/37 PASS保留，未新启动仿真。tmp删除849项约1.995GiB，复现摘要先归档约4.46MiB，未删除成功完整归档/源代码。
- 遗留问题：真实护圈/支架几何及地面可见性未完成标定；一次SITL PASS不证明实机鲁棒性。
- 下一步：现场静态安装测量与雷达扫描验证，再按测量补齐实体几何；当前仅同步文档到远端。

### 2026-09-07｜R57 toudi4 错列墙与0.85m通口首轮配置

- 改动范围：2026场景走廊、投后航点与截面几何、随机摆放静态排除项、Gate配置与回归；Planner/机架/相机/释放链未改。
- 具体改动：确认现世界派生自导航toudi3_random；移除旧Wall_14/15/16/20/22，复用导航toudi4.world的Wall_14和两块错列短墙。原版净宽约.598/.562m，用户确认先.85m通过再严格.80m；首轮只缩短短墙侵入走廊的一端，使两处实际净宽均.850000m。航点改为6个走廊点+1个H取景点，走廊1.4m，H1.6m。Gate按入口+两处绕墙口配置顺序，不冒称原三框门评分；保留碰撞/超高/时限/落地判据。
- 验证结果：几何检查PASS，全部引导线段距墙最小.425m，保留.30m规划膨胀及.025m净空；原40项Gate回归与新增4项错列场景检查通过。搜索树/H/外部边界保持原模型；整机构建后按用户授权运行首轮。
- 遗留问题：0.85m与严格0.80m实跑尚未完成；虚拟接触代理的实机适用前提保持原说明。
- 下一步：首轮0.85m完整通过后才切到0.80m验收；各轮独立记录并强制收尾，失败不全量发布Release。

### 2026-09-07｜R57 0.85m整场通过后切换严格0.80m

- 改动范围：两块错列墙的侵入长度、对应引导中心线和Gate截面。
- 具体改动：按用户“先0.85，能通过后严格0.8”要求，在0.85m完整PASS并零残留后，将两处通口净宽精确收至0.800000m；航点仍6个走廊点+1个H，走廊1.4m/H1.6m，Planner与机架包络未变。
- 验证结果：0.85m整场Gate 37/37 PASS，记录在 logs/r2026_r57_toudi4_085_20260907_172823；0.80m静态几何和Gate回归通过，中心线最小距墙0.400m。0.85m结果不计作0.80m通过。
- 遗留问题：严格0.80m整场实跑待验证；实机安装与虚拟接触代理适用条件保持原说明。
- 下一步：按当前用户任务执行最终尺寸整场验证，成功或失败均强制收尾，保留两阶段独立记录。

### 2026-09-07｜R57严格0.80m失败分析并按指令收尾

- 改动范围：两轮结果/参数/实际轨迹、严格0.80m失败分析、去年机载航点对照；后续全场bag默认关闭。未修改Planner或进行新仿真。
- 具体改动：保留0.85m的37/37 PASS；严格0.80m三投/三恢复、前4/7投后点和2/3通行截面完成，第5点持续占据导致90s运动超时FAIL。新增可复核的静态点/体素与估计位姿对照；明确十组seed执行数0，main不接收失败配置。
- 验证结果：三快照重现目标占据，普通点贡献2、水平列0，目标在地图内。两点190.623→276.497s保留；首次附近MAVROS相对真值XY约(-.067,-.121)m，LIO约(.011,-.070)m；墙体位姿未变。两轮记录缓冲溢出0，收尾零残留。离线检查后续record_debug默认false，不再默认记录全场bag。
- 遗留问题：严格0.80m尚未通过，水平估计/地图边界与先验引导点一致性仍需处理；未把误差来源全部归结为单一估计器或硬件。
- 下一步：按用户“未通过则分析报告收尾、不再新仿真”停止；推送报告并保留分支。未来只有严格0.80m通过后，才进入十seed验证；届时主要保留TXT/JSON及必要局部信息。

本阶段收尾补充：静态展开确认全场bag记录节点不再默认启动；提取完成后删除两轮20个bag，约18.539GiB，保留TXT/JSON、轨迹CSV及关键图像/局部地图；未压缩VHDX。没有启动额外仿真或十seed，失败配置不合入main。

### 2026-09-07｜R58按实际1.5m走廊与去年引导方式修正

- 改动范围：错列短墙场景、返程航点、紧凑仿真记录器；Planner/定位/膨胀参数未改。
- 具体改动：按用户明确的走廊总宽1.5m校正原约1.0704m模型，两处通口保持0.8m；保留错列短墙、搜索树和随机靶场。前后引导距离从0.45m改0.70m，沿用现有速度<=0.20m/s、0.5s停稳判定，7个投后点，通行1.4m/H1.6m。记录器只写JSONL、CSV、参数和最多3份失败局部地图，无发布/控制服务，无全场bag。
- 验证结果：1.5m总净宽、两处0.8m通口和0.70m距离检查通过；原失败冻结图中原目标占据，沿纵向退远后的点空闲。44项Gate回归通过，记录器实际ROS消息序列化/解码检查通过；整机Catkin构建通过。
- 遗留问题：这是几何和引导可达性修正，不宣称已经根治LIO/飞控所有位置误差；仍需完整实跑及十seed评估。接触包络的实机适用条件保持原说明。
- 下一步：按当前用户授权先跑seed11整场，通过后冻结版本运行seed1~10，只保留文本/JSON/CSV与必要局部信息，成功失败均收尾。

### 2026-09-07｜R58记录器入口权限

- 范围/改动：为新增紧凑记录脚本补齐可执行权限，便于ROS直接查找与安装。
- 验证：ROS消息解码检查和Catkin生成入口通过；不改变飞行逻辑。
- 遗留/下一步：待首轮实跑，通过后十seed。

### 2026-09-07｜R58紧凑记录器字段校正

- 改动范围：只读记录器，飞行算法与场景参数未变。
- 具体改动：按实际plan_manage/PlannerStatus数值状态和goal_seq触发局部失败快照；非有限标量记录为JSON null。
- 验证：真实NavigationDecision和PlannerStatus消息序列化/回调检查通过，同一目标不重复采集局部快照。
- 遗留/下一步：当前pilot已加载原记录器，任务/规划事件仍完整记录；十seed开始前冻结修正后的记录器。不是新的飞行策略修复。

### 2026-09-07｜R58首轮搜索孤点成柱失败与结构支持修复

- 改动范围：Planner的附加水平绕障柱生成、相关参数与测试；原始三维占据/膨胀、轨迹算法、1.5m走廊和0.8m通口未变。
- 具体改动：首轮seed11在第二靶接近失败，(-1.488894,3.877989,.516418)单点生成全高柱覆盖桥靶上空；普通3D膨胀贡献0、水平柱贡献1。新规则仅在0.15m XY邻域内至少3点、竖向跨度至少0.20m时外推整柱；其余点仍保留普通三维碰撞占据，不按目标类别/ID开白名单，不注入真值、不删点。
- 验证结果：真实冻结图662个高度候选中657有结构支持；目标孤点不再生成柱，四树分别仍有169/183/166/139个支持点。生产C++类与冻结输入对照0差异，平均约0.618ms/次（笔记本、662点，不是板端整机性能）。4项C++回归、整机Catkin构建通过。前后引导0.70m以墙中面计、到实体面约0.625m，文注已澄清。
- 遗留问题：修复后完整闭环与十seed待实跑；LIO/飞控误差没有因此被宣称全部消除。
- 下一步：先按当前请求重跑seed11，完整通过后冻结版本进行seed1~10；失败首轮保留真实结果，不发布失败全量Release。

### 2026-09-07｜R58沿用去年机架相对朝向，补齐实际接触记录

- 改动范围：返程目标航向及到点检查、仿真接触记录、紧凑日志和R58报告；机架几何、相机外参、搜索三投和Planner轨迹/膨胀不变。
- 具体改动：去年沿Y飞且yaw=0，当前沿X飞，配置返程yaw=90度；距开阔引导点0.50m内转向，到点增加0.10rad航向条件并与现有位置/速度共同停稳0.50s。已有PoseStamped传四元数，不增飞行话题/桥接。仿真原话题改由整机碰撞形状观察器输出，补齐桨盘/机身接触，移除该机型重复护圈发布器，物理尺寸和响应不变。记录器从master读取完整参数，补规划与飞控位置指令CSV及曲线JSONL。
- 验证结果：任务/运行时/执行/适配相关140项Python回归通过，实际ROS消息序列化通过；整机Catkin构建通过。失败冻结轨迹显示外侧墙更早桨盘贴近/重叠迹象，PX4波动而LIO水平观测仍连续；未把单一几何回放写成全部因果证明。R57的0.85m仍保留37/37历史PASS，严格0.80m仍为失败。
- 遗留问题：新修复尚待完整实跑；先前R58两轮根参数文件为部分缓存，报告已披露。整机接触统计较旧护圈统计完整，历史口径不冒充新口径。小型JSON分析与飞行图作为报告附件纳入git，大日志/ULog留在logs。
- 下一步：先seed11完整通过，再冻结同一版本跑seed1~10；无全场bag、无失败全量Release，成功失败均收尾，未通过不合main。

### 2026-09-07｜R58完整接触统计的起降垫地面语义

- 改动范围：horizontal仿真评测配置，飞行算法不变。
- 具体改动：完整接触观察在0.627s捕获静置机身与landing_h起降垫接触，起飞前Gate FAIL；原始启动失败记录保留。核对模型为1×1×0.005m的平面标记，属于起降支撑地面，配置与ground_plane同类处理，仍统计所有墙体/障碍接触，不改变碰撞几何或动力学。
- 验证结果：已读实际SDF厚度5mm；原Gate正确反映未配置起降地面类别的启动问题，本轮未进入飞行，收尾零残留。修改仅launch参数，XML展开检查通过。
- 遗留/下一步：航向修复还未进入实飞验证；修正后重跑seed11，通过后十seed。

### 2026-09-07｜R58入廊先停在宽处，再对齐窄通口

- 改动范围：一个投后引导点及报告，飞行代码和其余参数不变。
- 具体改动：航向修复整场完成三投/三恢复与90度转向，181.265s在入廊停点发生护圈-外侧长墙接触。原停点y=8.16769距外墙0.40m，实际北向偏出目标0.1023m后机体距墙只余0.2977m。现把第2点移至1.50m通道中线y=7.81769，外墙距离0.75m；先在宽处停止北向运动，再斜向第一短墙前点，门口和其余6点保持。
- 验证结果：原轮FAIL并零残留；碰撞时MAVROS-真值XY为(-.00885,-.00585)m，返回段XY误差P95约.0308m，区别于上次飞控大波动。目标/真实轨迹/飞控指令已完整记录。修改后静态几何与引导线净空检查通过，无新算法或门宽修改。
- 遗留/下一步：窄通口和降落仍待通过；按授权重跑seed11，完整通过后十seed，保留失败原始记录。

### 2026-09-07｜R58恢复去年目标附近选点，避免地图更新后锁死

- 改动范围：Planner已有目标调整逻辑与比赛配置；机架、场地、航点、轨迹碰撞阈值及控制参数不变。
- 具体改动：中线停点版本完成三投和入廊、首通口截面，但原门后目标被更新的长墙地图占据，最终90s超时FAIL、零碰撞。查明比赛profile关闭了旧Planner的目标附近调整。现启用已有能力，选点始终围绕原请求航点，半径0.15m、步长沿用0.05m地图分辨率、保持z；有效点仍空闲时不重新选点，避免随噪声跳动。不再硬编码在膨胀后另需0.20m净空，使用同一Planner碰撞净空0.025m；未降低轨迹净空或放宽0.18m原航点到达要求。
- 验证结果：R57严格0.8冻结图原目标改为(1.623022,7.897277297,1.4)，R58冻结图改为(.587997,8.11769,1.4)，均只移动0.05m、对应占据栅格净空0.05m，20次重复结果相同。3项目标调整、4项轨迹进展、5项目标状态回归通过，整机Catkin构建通过。修改前在导航和main集成来源分支保存plan_manage完整legacy快照；精简分支不复制旧包。
- 遗留问题：冻结图选点通过不等于整场轨迹与实机通过，仍需实跑；缺少可行点时保持原规划失败，不越障、不忽略占据。
- 下一步：同场景seed11整场验证，完整通过后冻结版本执行seed1~10，不录全场bag，保留所有失败与收尾结果。

### 2026-09-07｜按用户要求停止R58并整理成功版本对照和实机方案

- 改动范围：文档、离线事实/源码对照及未加载的低空方案，飞行源码和现有运行配置不变。
- 具体改动：停止继续调试，十seed暂停且实际0组；完整记录最后59ed04b三投后179.495s外墙护圈接触FAIL。对照R56/R57成功源码，区分13次提交中的报告产物、10个飞行源码文件及配置/仿真/测试变化。给出场地旋转/平移统一换算、当前frame参数不完整的边界、碰撞评测与共享实机代码的区别。查明旧0.65→0.15→0.65两组固定航点，保留低空0.65/实际≤0.70目标为未加载方案，不直接照搬未解释的升降动作。
- 验证结果：最后仿真已收尾，进程检查零残留；本次新仿真0、实机操作0。硬件入口只静态展开，22节点无Gazebo/接触/真值/mock/自动解锁；识别出返程航向覆盖参数与仿真不一致。最后返程真值z-估计z P50=.0964m、P95=.1462m、max=.1668m，无法据0.65命令保证实际≤0.70。低空相机短边视野约.56m，当前完整1m H外圈取景不能直接下移。报告链接、YAML及JSON解析检查通过。
- 遗留问题：窄走廊完整通过、真实控高、任意场地旋转贯通、板端实时视觉/导航、机械与接管仍待验收；R58不合入main。未实施停止前提出的净空优先选点。
- 下一步：本轮收口，推送文档并保留分支和成功标签。后续仅在新明确请求下恢复工作或仿真。

### 2026-09-08｜R59单轮低空走廊与H降落准备

- 改动范围：可选H笔画几何、独立走廊专项配置/入口与报告。全场默认搜索/三投配置、机架、世界、相机外参、Planner核心及旧控制器不变。
- 具体改动：确认去年实际检测为黑色轮廓椭圆中心，无H字形/完整环检查；当前保留完整环路径，增加默认关闭的两平行笔画+连接横画几何补充。专项目标0.45m、H取景0.50m、Planner顶界0.55m、指令顶界0.50m、真值Gate≤0.70m；跟随步长0.15m、到点0.12m/0.20m每秒/0.80s，两墙间增加横向对齐点。现有post_delivery模式不生成投递完成证据。
- 验证结果：整机Catkin构建、3项C++几何与44项Gate回归通过；93有真值正例最大中心误差5.49px，7负例拒绝；1张旧成功轮低H实图可检出，25边界输入仅观察。静态launch展开与墙面净空检查通过，尚未启动本轮仿真。
- 遗留问题：低空动态通行及H闭环待唯一一轮实跑；实机和任意现场坐标适配不在本轮。
- 下一步：按用户已有的一轮明确授权运行走廊-landing；结束即收尾，不追加或十seed，不把专项当全场三投PASS。

### 2026-09-08｜R59唯一一轮完成走廊/H对准，修正触垫评测遗漏并收尾

- 改动范围：终点平面降落标记的仿真接触分类、回归和结果报告；飞行算法/控制/相机/世界不再修改。
- 具体结果：仅一轮，源码e633a9b。8/8航点、两处严格0.80m通口完成；通过高度.5687/.4842m，全程真值最高.6701m，越界/超高0。184.8s完成10帧H对准，185.772s确认AUTO.LAND，186.821s机体触landing_h_clone，距H中心约.116m。
- 首个失败：终点与起点同为5mm平面H，但评测漏配clone名称，正常触垫触发Gate FAIL并提前收尾。原始FAIL22/31保留；ULog末尾仍armed且landed=false，不能改判整链PASS或补造disarm。现补齐终点地面分类，墙体接触仍计碰撞。
- 验证结果：补丁后45项Gate回归通过；无第二轮仿真。包装器与最终进程检查零残留。只读截图因ROS已停超时，没有重新启动任何ROS/Gazebo。
- 遗留/下一步：走廊和低空H阶段已有本轮事实，完整landing末端验收仍缺ON_GROUND/disarm；按一轮约定本次收口，十seed暂停，不合main、不发布失败全量Release，保留分支与两个历史成功标签。

### 2026-09-08｜R60全场低空走廊阶段接入与验证准备

- 改动范围：任务参数切换、既有Planner/控制参数缓存读取、独立走廊高度Gate、全场配置及报告；旧包快照保存于来源worktree的legacy_baseline/20260908_r60_phase_profile。
- 具体改动：保留搜索三投；廊外高位到点后原地下降0.40m/到达误差0.05m，随后切换R59低空顶界和跟随参数。H低空笔画路径启用。全走廊真实高度≤0.70m，不放松原碰撞/落地/disarm。
- 验证结果：完整Catkin构建、239项回归、launch展开通过；本轮完整Gate/十seed尚未启动。
- 遗留问题：动态阶段交接及最终落地待实跑，实机仍未验收。
- 下一步：按最新授权先完整Gate，PASS后冻结版本十seed；附R56后修订、飞行记录分析及rqt完整/核心/仅飞行三版图后推送。

### 2026-09-08｜R60完整PASS先导、十seed与联合收口

- 改动范围：验收报告/逐组关键记录、R56后修订说明、当前文档、rqt三版图及离线复现工具；飞行源码冻结9cfb3e5未再修改。
- 具体改动：全场seed11先导完整PASS37/37后，固定同版本依次执行seed1–10；三版图由先导真实ROS注册快照经rqt_graph Nodes only后端生成。清理R54失败/R55中断旧全量Release，小附件及Git报告/标签/分支保留。
- 验证结果：先导269.748 ROS秒、三投/三恢复、9/9投后点、两处严格0.80m通口、H对准、ON_GROUND/disarm，走廊最高0.66333m、落地偏差0.03977m、零碰撞/越界/超高；十seed仅5/7两组完整PASS（2/10），8/10三投、6/10全投后路线，全部0bag/清零进程。构建及239项回归已通过；本次离线工具编译、rqt三SVG及本地报告链接校验通过。
- 遗留问题：2组窄口护圈擦墙，4组H阶段桨盘擦墙，2组投递事务失败；确认外部对齐丢标误退旧(0,0)航点漏洞，另有搜索端点占据/目标可达性及seed6落地估计异常。详见docs/verification/r60_full_matrix/FAILURE_ANALYSIS.md。
- 下一步：本轮收口，不追加仿真，不把2/10合入main；main保留R56历史验收。后续软件修复、板端/机械/现场与实机验收待新任务。日志继续仅留WSL项目logs，未再次压缩VHDX。

### 2026-09-08｜R61实测外参、会议规则、旧回退修复与大场地准备

- 改动范围：外部对齐丢标回退、相机/雷达外参与地面参考、保守机架模型、9.6m场地/出生范围/名义航点、规则校正、静态截图工具。
- 具体改动：最终采用16/21/6cm与50×50×37cm，FC落地高0.22m、IMU0.27m、保守包络55×55×40cm；视觉ground_z及AGL检查一致。起飞H居中贴边，终点H边缘留0.10m。目标出生扩到77.76m²，无旧中央24m²限制。固定门偏移只是开发场景，会议要求随机开口识别仍有缺口。外部丢标保持锁定目标，旧独立模式不变；旧包快照保存在来源分支legacy_baseline/20260908_r61_external_alignment。
- 验证结果：完整构建、240项任务/Gate回归、生产分支丢标回归（旧失败/新通过）、SDF及外参/场地静态校验通过。历史R60先导PASS/十seed2/10不覆盖这些新改动。
- 遗留问题：新模型通用动力学未标定/未全场实跑；随机门开口定位、赛前调整后的缓存/触发、机械及板端全链仍待验收。轻量结果仅几何预筛。
- 下一步：按用户截图请求只启动seed11静态Gazebo展示，截图后清零进程，补齐离线策略及完整报告并推送，不执行飞行/投递、不合main。

### 2026-09-08｜R61机体/雷达/相机装配纠正

- 改动范围：通用机体模型、MID360外壳局部装配、可见相机支架/起落架，以及静态预览读回检查。
- 具体改动：此前通用机架缩放后仍直接使用原雷达CAD原点，导致外壳与机体相交、相机无可见连接。将chassis参考置于FC下方0.04m，原MID360外壳visual/collision局部上移0.04m；IMU/ray传感器坐标、相机FC下16cm/IMU下21cm不变。增加FC盒、支架、起落架示意，明确不是实机CAD。预览增加侧视图和Gazebo真实link位姿读回，检查16/21/6cm。
- 验证结果：已核对原网格包围盒，准备重新静态截图；前两次静态预览已收尾，未启动任何飞行或投递。外层55×55×40cm保守碰撞体保留。
- 遗留问题：通用动力学和机械内构未实测标定，新组合未全场飞行验证。
- 下一步：仅静态渲染检查装配，完成报告并推送。

### 2026-09-08｜R61补齐可见支撑连接

- 改动范围：相机支杆、起落架竖杆及上横梁的示意visual。
- 具体改动：侧视检查发现支杆终点没有进入通用机体外形，延长支杆并加入横梁，避免视觉悬空。相机/IMU/FC坐标、物理保守碰撞包络、控制代码不变。
- 验证结果：上一静态预览Gazebo实时link坐标验证16/21/6cm全部成立；继续渲染最终连接外观。
- 遗留问题：支架仍为示意，不是实机CAD；本轮无飞行验证。
- 下一步：最终静态截图后收尾，整理报告并推送。


### 2026-09-09｜R61静态装配收尾、完整报告及部署资源

- 改动范围：当前README/环境/参数/验收/部署文档、R61布局/规则/策略报告、最终原生截图、仿真模型依赖与打包工具。
- 具体改动：完成seed11地图/机体/侧视三张原图和16/21/6cm实时位姿记录；保留R60实际11布局，另区分新9.6m离线样本；轻量仓比较四组假设条件，未替换在线策略。将必要12个外部模型/材质（最大MID360网格约31MiB）作为用户要求的仿真资产例外纳入git，保留来源/许可；运行入口加入包内模型路径。新增机载/仿真源码打包工具，权重只进入忽略的本地deliverables，不入git。来源AGENTS过时tank/强制桨保条款按会议修正，其余流程要求不改。
- 验证结果：整机构建、240项任务/Gate、轻量仓53项测试通过；最终静态预览CAPTURED、实时安装距离检查通过、收尾零残留；当前world/飞机/五靶共13份SDF及25个资源引用闭合。打包脚本语法检查通过，归档在本次提交后生成并读回检查。
- 遗留问题：没有新飞行PASS。通用动力学未标定；随机门开口感知、赛前调整缓存/启动、板端并发与机械实投仍待验收；硬件旧任务高度和新零点需整体配套，不能只替换单一参数。
- 下一步：生成两份本地包并核查；推送现有feature分支，main保留R56，不发布失败全场Release、不再启动仿真。

### 2026-09-09｜补全机载包文档并正确读取中文文件名

- 改动范围：源码打包器的文档选择。
- 具体改动：机载包同时纳入deployment目录，保证HARDWARE/BUNDLES文档的相对链接在解包后有效；git ls-files改用NUL分隔原始UTF-8路径，避免中文文件名被转义后找不到。
- 验证结果：初次归档暴露git中文路径转义问题，修复后重新生成两类包并逐项读回；没有修改运行代码或参数。
- 遗留问题：硬件与新场景的飞行验收仍待完成。
- 下一步：生成并检查两份本地归档，推送feature。


### 2026-09-09｜双包生成、读回与解包构建收尾

- 改动范围：BUNDLES/R61报告、bundle_validation.json及本地deliverables索引。
- 具体改动：生成fd147c72版机载与仿真源码包，分别17,473,765/38,927,840 bytes；附各自RKNN/PT权重，权重和压缩包只保留本地。清除本次中文路径修复前的无用半成品包。
- 验证结果：两包1225/1417文件全部读回；无build/devel、机械PWM或旧参考副本；仿真当前资源引用闭合；机载解包后视觉+导航在WSL x86_64完整重编译退出0。未执行硬件、ARM板端或飞行。
- 遗留问题：外部ROS/SDK/PX4/plugin等依赖见使用说明；新几何/随机开口/板端整机和机械带载仍待验收。
- 下一步：推送各现有feature并核对远端HEAD，保留main的历史验收，不新增仿真或失败全场Release。


### 2026-09-09｜R60未闭环问题与轻量姿态模型复核

- 范围：工程复核报告、README与任务优先级；飞行源码/场地/包未变。
- 改动：逐项区分回退源码已修与窄门/H/落地估计/可达性尚未闭环；读取已有seed11姿态记录；轻量仓增加刚性安装四元数投影与三组姿态敏感性，57项测试通过。优先固定当前seed11，门/树箱独立随机化仅设计未启用。
- 验证：原水平seed11五条路线结果不变；实际记录运动样本约62.86%机头与航迹夹角>30°；这些都不是新飞行结果。
- 遗留：seed11新场景尚未飞行PASS，R60全部问题未根治；ARM部署与实机验收结论不变。fd147c72双包仍为此前冻结版本，本次不重新打包。
- 下一步：推送轻量fork并向原仓提PR，之后经明确启动请求验证seed11全场基线。

- 用户补充确认：门只左/右两种，净宽固定0.80m；已修正规则说明和随机化设计，删除居中/连续横移假设。现有seed11 world未变。


### 2026-09-09｜登记障碍柱、自适应覆盖与分线路返航计划

- 改动范围：ROADMAP、会议规则校正、SEARCH_RETURN_PLAN、四张规则原页与既有R60返程时间JSON；运行代码/阈值未变。
- 具体改动：确认Planner已有有限顶界障碍柱，不能以此规避“全程高于障碍不得分”；按“完成所有投放后”的流程前提，未完成投放安全返航选起飞H。设计基于实际相机姿态/高度与地图的覆盖记忆、可达剩余航带及稳定更新；分别预算起飞H和走廊终点H线路。用户优先级固定为seed11通过→随机化完成→三项优化。
- 验证结果：实际查看规则书第11–13/15页并交叉核对另一版文本；读取R60 seed11/5/7已有日志，投后路线至disarm约129–149s，LAND交接约9.5–11.7s。当前实际420/180秒核对一致；起飞H450/150秒仅为待验收工程候选。
- 遗留问题：新场景未实跑；起飞H分支缺对应多轮数据，不能用三条旧成功记录定可靠尾部阈值。没有新仿真或实机动作。
- 下一步：推送计划和规则事实；优先等明确启动请求验收固定seed11，后续再推进随机化与优化。


### 2026-09-09｜R62 seed11完整Gate准备与相机录像

- 改动范围：完整Gate场地边界、可选机载相机视频记录器、对应回归与构建。
- 具体改动：发现完整Gate仍继承旧8m场地边界，按当前world内墙面修正为X[-4.8,4.8]/Y[-0.5,9.1]，保持物理碰撞/限高标准。新增默认关闭的图像订阅MP4记录器，seed11启用10fps，并保存每帧源时间戳CSV；不录bag。没有修改航线、动力学或飞行控制；不启用新安全返航设计，420/180秒现有行为保留。
- 验证结果：241项任务/Gate回归通过；视觉catkin构建通过；独立合成图像录像烟测30帧输入/10帧写入，ffprobe确认1280×720 MPEG4可读。
- 遗留问题：新模型与地图尚无全场PASS，R60未闭环问题仍需本轮观察。
- 下一步：按本次用户授权仅运行seed11；FAIL则分析并停止；PASS后同一版本静默完成seed1–10，再集中分析。只有seed11录机载相机视频，不新增全场bag，不提前启用随机门/树箱或新返航方案。


### 2026-09-09｜R62首轮失败后按新授权修复并重试

- 范围：仿真MID360消息配置、保守包络的刚体归属、自身雷达外壳射线设置、可关闭的任务提前返航预算。
- 事实：首轮run r2026_r62_full_pilot_seed11_20260909_013457在8.726 ROS秒触碰Wall_12，未解锁、0任务决策/0投递，已自动收尾；视频75帧可读。发现新装配MID360误用CustomMsg而SITL LIO等待PointCloud2，系复制模型时遗漏旧type=1。静态展示没有运行该链。未解锁滑移指向新动态支撑/约束问题，不能归为Planner重规划失败。
- 修复：雷达恢复type=1；同尺寸55×55×40cm碰撞体直接归主base_link，消除1g独立支撑刚体并保留总质量；仅自身雷达封闭外壳对激光透明，实体碰撞不删。相机16/21cm和环境几何不变。按用户明确答复，本批次early_return_enabled=false，关闭420秒/返航预留对搜索与选靶的限制，仍保留600秒总限时、动作超时和异常结束。硬件入口默认true。
- 验证：244项任务/Gate回归通过，含超过420仍可搜索/接近且截止600；插件实际构建通过；包络世界坐标范围仍z[-0.22,+0.18]，SITL雷达类型与LIO一致。实际生成24个覆盖航点，X[-4.3,4.3]/Y[0,7.1]，0.7m间距、1.4m AGL，匹配9.6m新场地。
- 遗留：动态效果、遮挡和完整搜索投递仍须重试验证。旧fd147c72仿真包含本次暴露问题，不能作为当前可运行版本；机载包不包含这套Gazebo模型/插件，但仍未取得实机验收。
- 下一步：用户已重新授权修复后从seed11开始；通过才冻结版本静默完成seed1–10，未通过则分析收尾停止，不提前推进后续场景/算法。


### 2026-09-09｜R62两次seed11启动失败收尾

- 范围：R62报告/JSON/场景快照、当前README/ROADMAP与本地包状态提示。
- 结果：初次未解锁滑移撞Wall_12；重新授权后的重试机体停稳，但插件link名称与Gazebo include展开名不匹配，插件加载失败，接触心跳缺失、雷达近点被盲区过滤，180s启动超时。两次均0任务决策/0投递，无seed1–10，视频均可读、进程清零。
- 验证与界限：消息类型修正、244项回归和构建通过；动态闭环未通过，不能视作搜索/投递算法试飞失败或成功。记录确切Gazebo错误，不把缺接触数据写成无碰撞验收。
- 部署影响：Gazebo组件不进入机载硬件入口；原fd147c72仿真包存在回归，当前重试版本也未可用；均未实机验收。关闭提前返航只用于本批次全场仿真配置，硬件默认保留。
- 下一步：本次按失败停止，不再启动仿真；推送源码/失败报告，不发布全量失败Release，不合main。后续首先修正实际展开link引用并验证传感器/接触，再申请新的seed11运行。


### 2026-09-09｜按持续任务运行目标修复展开名并补外围模型

- 用户新目标：持续修复直到seed11能建图、起飞、巡航和投递，不追求整场PASS。外围仅补充简单模型增加点云，不做人群拟真；420秒提前返航保持关闭、600秒总限时保留。
- 改动：插件link引用改为实际展开的iris::base_link和mid360::lidar_link；用gz sdf -p实际展开后验证两处链接和碰撞体均存在。FC IMU使用固定关节，符合刚性安装。场地外新增简单墙/柱/箱体，不改场内树箱、门、seed11靶位；布设器忽略indoor_context整体外壳的圆形足迹近似，实际几何仍参与激光与碰撞。
- 验证：SDF展开引用检查通过、外围17个静态几何link，未模拟人群；原244项任务/Gate回归仍为本批次软件验证，动态运行待本次确认。
- 运行范围：保留原完整Gate的异常检查；另从实际LIO/任务启动、解锁/真值高度、搜索位移、至少一次释放并恢复搜索的记录验证本次运行目标。达到后收尾，不宣称完整竞赛PASS，不自动起seed1–10。
- 遗留/下一步：按当前持续授权进行seed11实跑，若未达运行目标则先分析修复再继续；未完成前不标已验收。原两轮失败报告作为历史记录保留。


### 2026-09-09｜主刚体FC采样与无穿透生成位姿

- 上轮证据：022748运行已恢复20000点有效场景点云、LIO/FreeDOM建图及接触心跳；但PX4静止加速度周期波动、位置发散并拒绝解锁，启动超时。未达起飞/巡航/投递目标，持续修复而非宣告完成。
- 改动：将主base_link参考原点放到FC位置，外形/碰撞姿态平移补偿后保持不变；将人工IMU小刚体的质量和惯量合并进主刚体，Gazebo IMU直接采样该刚体，避免固定关节约束力作为传感器信号。保持原IMU噪声、动力参数与健康检查。spawn_z改为0.225，与0.22m支撑高度+5mm H垫一致，不在初始时穿透垫面。
- 验证：XML结构、总质量/形状位姿静态核对，等待实跑确认惯性信号与位置融合；没有关闭健康检查或改用真值控制。
- 范围：仅仿真模型/生成高度，实机硬件配置不改；seed11场内布设/24航点和600秒限制不改。
- 下一步：按活动目标继续seed11实跑，直至观察到建图、起飞、搜索巡航、投递并恢复搜索。


### 2026-09-09｜将原主机体接触设置用于新的接地包络

- 证据：024028运行中主刚体与雷达IMU静止时仍出现约数m/s²周期加速度，物理位置仅微米级变化；点云/建图正常，但PX4初始化倾角和位置发散，健康检查拒绝解锁。
- 差异：原成功iris主机体接地碰撞具有ODE min_depth=0.001/max_vel=0；新实体包络成为接地面后缺少这组接触设置，使用默认穿透速度修正，产生高频接触抖动。
- 改动：将原主机体同一surface原样复制到新接地包络，保持几何、噪声、质量、动力和健康检查；目标是消除数值接触抖动，而不是过滤传感器或放宽解锁。
- 验证：XML已核对与已有surface一致；需实跑观察静止加速度和任务启动。活动目标仍未完成，继续seed11。


### 2026-09-09｜垂直起飞固定XY独立于Z限幅

- 上轮进展：024628已验证接地抖动消失、LIO/FreeDOM与接触流正常、飞控位置毫米级且成功解锁。随后起飞初段向南侧贴边墙漂移接触，未达到运行目标。
- 直接代码问题：通用三维距离限幅把固定起飞XY拉向当时已漂移的当前位置，输出只保留很小横向误差，削弱贴边起飞的横向保持。
- 修复：仅Takeoff模式在距离限幅后恢复起飞点XY；Z继续原限幅，搜索/Planner轨迹分支保持原行为。修改前patrol_control完整快照与清单/SHA256保存在main集成与导航来源legacy_baseline/20260909_r62_takeoff_xy。
- 验证：直接抽取生产限幅代码的C++14/UBSan回归通过，验证起飞固定XY及非起飞原0.4m向量限幅；导航实际catkin构建通过。
- 遗留/下一步：新控制行为待本轮seed11观察，继续直到建图/起飞/搜索/投递目标达成，保留600秒限制与健康/接触检查。


### 2026-09-09｜匹配SITL悬停推力初值

- 上轮证据：025758已按固定XY发送起飞目标，但机体长时间贴地；PX4悬停初值0.5，电机差动随姿态积分持续增大，刚离地即侧倾向墙。此前XY修复没有根治此动力初值不匹配。
- 计算：当前装配总质量1.577kg、4电机k=5.84e-6、转速映射omega=100+1000*u，mg平衡对应u约0.7134，取0.713作为MPC_THR_HOVER初值；HTE自适应保留。
- 范围：仅/simulation/px4_parameters的SITL启动参数，不改真实飞控配置，不调整碰撞/健康/限高标准。记录精确计算于本轮artifact/hover_thrust_model.json。
- 验证/下一步：模型静态质量与参数核对完成；继续实际seed11验证离地与任务链，不能仅凭推力计算宣称可飞。


### 2026-09-09｜seed11建图起飞巡航首投运行目标达成

- 运行：r2026_r62_operational_seed11_20260909_030841，飞行源码8bedcc0。地图就绪、正常解锁，ROS22.110s真值AGL超过1m，22.848s任务开始；105.570s panzer第1槽raw_actuator_ack成功且仅一次调用；143.001s确认恢复，发布RESUME继续搜索。
- 验证：7198个接触样本、实际碰撞0；最高FC AGL1.580m；原生相机1197帧可读，源图像时间与编码播放时间分别保留；达到本次目标后主动SIGINT收尾、进程0残留。退出130及三槽audit WAITING如实保留，不改写整场PASS。
- 回归与交付：244项任务/Gate、1项生产起飞限幅、4项实际SDF/模型契约通过；相关实际构建通过。补完整报告、轨迹/高度图、首投证据和源参数。打包器增加版本标签，准备替换已知含回归的旧fd仿真包；权重仍只进入本地包。
- 遗留：未稳定三投、未走廊/降落、未随机矩阵或实机。投后恢复37.431s与高度误差需后续梳理，不扩为本次验收结论。
- 下一步：提交推送现有feature、生成本地更新包并读回；完成活动目标，不启动新仿真、不合main或发布全场PASS Release。


### 2026-09-09｜更新本地R62双包并完成交付

- 从干净提交3ed8cdd7导出机载/仿真包，实际运行代码对应8bedcc0；两者之间无飞行行为改动。
- 机载包19,591,146 bytes/1310文件，仿真包41,048,816 bytes/1504文件，逐文件读回与包内容检查通过，包含首投运行报告；权重/压缩包只在本地deliverables。
- 视频保留原run目录，当前包索引已更新，旧fd包标为历史。没有新仿真/ARM运行/机械动作，不声称完整Gate或实机验收。
- 下一步：推送现有feature并确认远端一致，完成本次“建图、起飞、巡航、投递”目标。


## 2026-09-09：完整 seed11 与条件矩阵观察模式

- 范围：只读 Gate、仿真 launch、矩阵脚本。
- 改动：显式 observe_full_trial 模式保留所有原判据及错误，非终局错误不立即关闭仿真，等待 600 秒、落地解除武装或碰撞；启动/墙钟看门狗仍处理基础设施停滞。默认模式不变。矩阵可逐轮启用下视相机录像。飞行算法、模型与飞行参数未改。
- 验证：执行前进行 Python/launch 静态检查及终止条件测试；本次用户授权完整 seed11，PASS 才执行 seed1–10。
- 遗留：本次运行结果待报告，提前返航保持关闭。
- 下一步：实跑结束后更新搜索返航计划；失败不追加新轮次。


## 2026-09-09：完整 seed11 碰靶收尾及搜索返航计划更新

- 范围：R62完整seed11报告、小型记录、搜索返航计划、ROADMAP；不修改飞行行为。
- 结果：代码aedba01，三投回执成功；ROS485.561秒（任务462.893秒）第三投恢复下沉碰红十字靶，完整Gate FAIL；未进入走廊，未启动seed1–10，未追加重跑。
- 分析：控制恢复交接0.73local早于桥0.95local，随后旧规划的当前位置保持指令接管并随下沉漂移。bridge到达后严格视觉对准等待98.706秒。根因链及未确定的动力/融合贡献已在报告区分。
- 验证：完整观察模式6分支检查、Python/launch解析通过；相机4043帧，ffprobe及完整ffmpeg解码通过，零仿真进程残留。无全场bag，无失败全量release，不合main。
- 遗留与下一步：恢复闭环、旧轨迹接管、捕获耗时待修复；原部署包不标为可放飞验收包。安全返航保持关闭。新的实跑需新的明确授权。


## 2026-09-09：R63 投后恢复与固定位置保持修复

- 范围：patrol_control、轨迹服务器、完整低走廊控制配置。旧控制包在来源分支legacy_baseline/20260909_r63_recovery保护。
- 改动：控制/桥恢复高度统一0.95local；控制交接后维持已有恢复目标高度及固定XY，直到新任务运动命令；轨迹偏离时锁存保持位置，新轨迹到来重置。无新消息/节点，未更改相机、模型、地图、释放许可及搜索路线。
- 验证：生产代码回归和实际Catkin构建随后执行；用户授权修复后完整seed11，保留相机录像，安全返航仍禁用。
- 遗留：bridge捕获耗时继续观测，不通过放宽许可解决；本轮结果待写报告。
- 下一步：完成编译回归、提交后只运行本次完整seed11。

验证补充：实际视觉/导航Catkin构建通过；244项ROS任务/Gate回归、2项生产恢复/保持分支回归及1项起飞限幅回归通过。观察模式ROS时钟初始化仅在显式启用时执行，保持旧纯逻辑测试入口兼容。


## 2026-09-09：R63恢复修复实跑与最终降落FAIL

- 范围：本轮报告、计划、验收状态，飞行代码b7c0dd7冻结。
- 结果：一次完整seed11，三投/三恢复、9投后航点、两门通过，ROS447.203秒（任务424.375秒）AUTO.LAND后碰Wall_13，Gate FAIL 28/37。没有追加实跑或矩阵。
- 修复验证：恢复耗时2.747/2.139/2.314秒，原第三投下沉碰靶未复现；244项任务/Gate+两项保持回归+起飞回归、实际构建通过。
- 新发现：AUTO.LAND激活后内部航向约跳90度，与无旧FlightTask时历史reset counters重复应用路径吻合；近地定位/高度分离和落地检测仍待分解，未修改PX4源码。
- 录像：3804帧，完整解码通过，0bag，零进程残留；失败不全量release、不合main，旧包未重建。
- 下一步：修复并验证AUTO.LAND初始化与近地落地问题，新的实跑需明确授权；保持已验证恢复/搜索/走廊。安全返航仍关闭。


## 2026-09-09：R64修复PX4自动任务历史重置与无GUI整场录像

- 范围：部署目录PX4最小补丁/说明/回归、仿真launch、只读俯视相机模型及布设忽略项。
- 改动：无旧FlightTask时从当前估计初始化重置计数，已有任务继承与之后的新重置仍处理；本机PX4 99c4040实际应用并构建。俯视相机无碰撞，只输出录像，默认关闭，不参与飞行；seed11开启，失败只删除overview.mp4，机载相机原始录像保留。
- 验证：PX4 SITL实际构建通过，生产计数交接分支回归通过，SDF/launch解析通过；随后授权实跑验证。未实施20cm空中停机，不关闭真实碰撞、落地/解除武装判据。
- 遗留：近地估计/接地因素需动态验证；本次用户授权持续排障至seed11 PASS，随后打包、静默十seed并汇总、再推进随机地图。
- 下一步：先验证修复后完整seed11及两个视频源。当前安全返航仍关闭。


## 2026-09-09：R64 seed11完整PASS及打包说明

- 实跑：1983b2f+PX4基线99c4040和所附补丁，37/37 PASS，422.712秒，三投/三恢复/两门/正常落地解除武装，零碰撞。最终中心误差0.065m，55cm包络角点最大半径0.451m。
- 录像：机载3694帧、俯视3214帧均全量解码通过；俯视投后移至走廊观测位置，保存动作时刻，不改变飞行或物理障碍。
- 文档：更新包内说明，明确R63机载控制修复及PX4固件依赖，未刷写实机。
- 下一步：当前飞行版本冻结，重打R64两包后静默十seed，再集中分析与随机化。

## 2026-09-09：USB 相机与 YOLO 实时显示说明及补全

- 范围：uav_vision 独立显示节点、launch、安装依赖及使用说明；导航和控制业务代码未改。
- 改动：新增 yolo_live_view，按时间戳和 frame_id 匹配原图及 YOLO 结果，发布类别/置信度/框，支持 RKNN 与 Ultralytics；有界缓存、无订阅者跳过绘图。
- 验证：人工核对两个 detector 的 source、header、ROI 消息契约与显示入口。遵照用户要求未编译；未运行 ROS、仿真或实机。
- 遗留：需真实 USB/NPU 环境确认帧率与显示；当前目录 git status 报 not a git repository，无法进行版本提交。
- 下一步：按 vision_ws/src/uav_vision/docs/USB_YOLO_LIVE.md 完成相机及像素检测显示联调；实机部署另需配套设备链、现场参数与机械执行服务。
## 2026-09-11：实机入口可选相机与 YOLO 实时显示

- 范围：仅调整 `uav_mission/competition_hardware.launch` 的实机视觉接入参数。
- 改动：显式传递 RKNN metadata；增加可选 UVC 相机入口和 YOLO 标注显示入口，默认均关闭，避免与设备侧已有相机或无桌面板端冲突。
- 验证：检查 launch XML 结构与参数传递；按用户要求未编译、未启动设备或飞行。
- 遗留：相机标定、RKNN 模型和板端显示帧率仍需在实际设备上确认。
- 下一步：先以 `start_camera:=true start_yolo_live_view:=true` 做静态视觉联调，再进入导航链检查。

## 2026-09-11：更新实机启动 README

- 范围：patrol_uav_ws-patrol_planner/新readme.md。
- 改动：保留 livox_ros_driver2 与当前 FAST-LIO 消息适配的待办说明；删除独立视觉启动描述；补充 competition_hardware.launch 的相机和 YOLO 显示参数、Linux 检查命令及旧流程差异。
- 验证：人工对照当前 launch、FAST-LIO 消息类型和实机文档；未编译、未启动仿真或实机。
- 遗留：Driver2 消息适配、板端实时性和实机飞行仍需单独验证。

## 2026-09-12：启用可控的 FAST-LIO 地图点云可视化发布

- 范围：FAST_LIO/src/laserMapping.cpp、uav_mission/config/mid360_hardware.yaml。
- 改动：移除固定 if(0) 地图导出块，将导出移至已有 publish_map 函数（地图增量更新之后）；增加 publish/map_publish_en 与 publish/map_publish_hz。通用默认关闭，MID360 硬件配置开启、1 Hz；没有订阅者时不遍历地图。只改变可视化快照发布，不改点云预处理、定位估计、内部地图更新或规划控制。
- 验证：静态对照发布顺序、参数、订阅者判断和频率保护；上传后逐字节核对远端文件。按用户要求未编译，未启动雷达/飞控或飞行任务。现有二进制尚不包含本次改动，不能宣称新 /Laser_map 已实测生效。
- 说明：/cloud_registered 是经过预处理和去畸变的当前扫描；point_filter_num=3、blind=0.5，dense_publish_en=true 不会恢复预处理已丢弃的点。/Laser_map 是 ikd-tree 的地图点云，不是占据栅格或规划轨迹；查看体素可用 /sdf_map/occupancy_inflate，轨迹 Marker 用 /planning_vis/trajectory。当前 publishMap() 也读取膨胀占据缓存，因此 /sdf_map/occupancy 不能称为原始未膨胀占据图。
- 遗留：完整地图导出仍会产生额外 CPU/内存/传输开销，实际延迟需用户编译后观察。原文件保留 .before_map_publish_20260912 备份；未执行提交（板端部署目录无 .git）。
- 下一步：用户重新编译并启动后，RViz Fixed Frame=camera_init，订阅 /Laser_map 验证非空及低频发布；需要时在硬件配置中关闭 map_publish_en。

## 2026-09-12：新增实机只读命令行导航状态监视器

- 范围：新增 uav_mission/scripts/navigation_status_monitor.py、launch/navigation_status_monitor.launch、docs/NAVIGATION_STATUS_MONITOR.md。
- 改动：每 5 秒打印 LIO/飞控位置、坐标系、航向、飞控状态、任务/规划事件、目标与轨迹控制点数量。周期性短暂订阅地图，仅解析 PointCloud2 头部和点数；占据体素默认只检查发布者，可选采样。不发布目标、不调用控制服务、不修改业务逻辑。
- 验证：SSH 启动 MAVROS、MID360、competition_hardware；默认监视模式连续输出三次，sample_occupancy=true 连续输出两次。扫描约 5000 点，FreeDOM 约 3.6 万点，膨胀占据体素约 187 万点；定位持续更新，飞控连接且未解锁、MANUAL，任务 IDLE 等待手动开始，规划器等待目标。TF camera_init 到 body/vision_body/mapping_imu 可用，到 map 不连通。按用户要求未编译，未进行飞行或规划目标注入。
- 更正前一记录：用户随后要求撤销 /Laser_map 发布改动，源码及硬件配置已恢复修改前状态（if(0)），本次实测 /Laser_map 为空；不需要执行前一记录中的启用步骤。
- 收尾：本次启动的三组进程和监视器已退出；雷达恢复 SDK WakeUp(2)，设备 ACK 成功。原始运行日志目录 /tmp/nav_monitor_check_m3p1ri33/。
- 遗留：规划节点存在并不证明实际规划/飞行成功；本次未产生规划轨迹。占据体素仍为大消息，采样也会产生传输开销；监视摘要不代替空间地图可视化。板端无 .git，未提交。
- 下一步：用户启动业务后运行 roslaunch uav_mission navigation_status_monitor.launch 查看状态；需要更低刷新频率可传 interval:=10。

## 2026-09-13：新增 4×4 轻量三维障碍方块与规划路径视图

- 范围：uav_mission 新增 obstacle_view_node、obstacle_4x4_view.launch、config/obstacle_4x4/view.rviz、test/verify_obstacle_view.py 及 OBSTACLE_VIEW.md；更新 CMakeLists.txt、package.xml。
- 改动：订阅 /sdf_map/occupancy_inflate 并按 0.10m 起步聚合为最多 12000 个外表面 CUBE_LIST，输出 /navigation/obstacle_blocks；订阅 /planning_vis/trajectory 的最终 BSPLINE=300，转换为最多 1000 点 LINE_STRIP，输出 /navigation/planned_path。专用 RViz 仅显示方块、路径、位姿和网格，Fixed Frame=camera_init，10 FPS，不显示 PointCloud2/Image；RViz 进程使用 LP_NUM_THREADS=2 限制 llvmpipe 线程。
- 验证：板端 catkin_make obstacle_view_node -j2 -l2 通过；4 项 gtest 通过；隔离 ROS 合成地图/轨迹与 RViz 连续 600 秒 PASS，RViz 订阅无点云/图像，内存 194.7–205.4MB（测试采样），方块/路径/删除/锁存/坐标系检查通过；截图确认三维方块和橙色规划路径显示。view.rviz 与 launch 契约检查通过。
- 遗留：MID360 当前未接通（eth0 DOWN；配置为雷达 192.168.1.175、主机 192.168.1.100），尚未完成真实雷达地图地面检查；llvmpipe 仍是 CPU 软件渲染，5 FPS 左右是预期上限之外的实际表现。未启动飞行任务或控制输出。
- 下一步：接通 MID360 后，关闭驱动自带 RViz（rviz_enable:=false），先启动业务链，再运行 `roslaunch uav_mission obstacle_4x4_view.launch` 做地面地图检查；必要时用 `display_resolution:=0.20 max_blocks:=6000` 降低图形负载。


## 2026-09-14 标定舵机包复制部署

用户指定仅复制，不编译。新增 patrol_uav_ws-patrol_planner/src/actuator_pwm：右仓 Pin7/chip5，左仓 Pin16/chip0，后仓暂定 Pin11/chip4（待接线确认）。保留标定脉宽，校验硬件地址，初始化不输出脉冲。完成传输内容核对和脚本静态检查；未编译、未运行 PWM 或舵机。下一步按包 README 编译并逐只验证。

## 2026-09-18：4×4 实机地图关闭虚拟顶棚

- 范围：仅 `uav_mission/launch/obstacle_4x4_hardware.launch` 的 Fast-Planner 虚拟顶棚参数；不修改竖直障碍列扩展逻辑。
- 改动：将 `/fast_planner_node/sdf_map/virtual_ceil_height` 显式设为 `-0.1`，按现有源码的非正值契约关闭“指定高度以上全 XY 体素置为占据”；显式覆盖下层 planner launch 的正值默认，避免仅删除顶层参数后功能仍然启用。
- 验证：launch XML 解析通过；以 `ground_z=-0.2` 静态展开参数，确认 `virtual_ceil_height=-0.1`、`horizontal_avoidance/enabled=true`；`plan_env` 与 `uav_mission` 实际 Catkin 编译通过；竖直障碍支撑 4 项 gtest 通过，汇总 8 项零错误零失败。未启动仿真或实机。
- 遗留：`horizontal_avoidance` 仍会将满足局部点数及竖向跨度条件的障碍列，从 `floor_z` 填充到地图 Z 上边界；本次按要求只检查，不修改。
- 下一步：地面 preview 检查真实点云与膨胀体素；需改变竖直障碍列行为时另行评估低空绕障安全性。

## 2026-09-18：走廊测试目标高度统一为 0.3 米

- 范围：`corridor_waypoint_test.launch`、实际最近使用的 `corridor_waypoint_test1.launch`。
- 改动：增加统一 `flight_height` 参数，默认 0.3；起飞点、搜索、接近、返程及四个任务航点均引用该参数。旧入口移除重复规划器 include，保留控制器内部的单一规划器链。安全高度上限不是目标高度，仍为 0.65；未改变路线顺序。
- 验证：两入口分别在 0.3 默认值及 0.4 参数覆盖下用 ROS XmlLoader 完整展开，目标高度一致且规划器/轨迹服务器各一个；`catkin_make --pkg uav_mission -j2 -l2` 实际编译通过，日志 `/tmp/corridor_height_build.log`。未启动节点、仿真或实机任务。
- 遗留：目标 Z 为任务坐标系数值，不等同实测离地高度。此前日志仅能证明第一个任务未完成并超时；航点重合本身不足以证明根因，须进一步确认桥接里程计与任务 frame 一致性及新鲜度，不能以本次高度统一宣称悬停问题已修复。仓库无 HEAD，已有大量用户暂存文件且本记录已有未提交修改，本次未创建会包含这些内容的首次提交。
- 下一步：用户重新加载所用入口使参数生效；地面只读核对实际高度参数及桥接状态，再独立安排飞行验证。

## 2026-09-18：新增走廊任务只读诊断记录脚本

- 范围：`top_level_scripts/record_corridor_diagnostics.sh`、`docs/CORRIDOR_DIAGNOSTICS.md`。
- 改动：默认 180 秒记录明确列出的定位、任务、规划、控制、TF、rosout 话题；保存开始/结束参数白名单和话题连接。bag 128 MB 分片、64 MB 缓冲，定时/信号退出收尾自己的记录进程，不调用控制服务；不采集点云图像。
- 验证：`bash -n`、帮助/非法时长检查及本机 rosbag 选项检查通过；假 ROS 命令隔离测试中定时结束和 TERM 中断均关闭 bag 并保存结束快照。本次为 Bash 脚本，无编译目标；未启动真实 ROS、硬件、仿真或任务。
- 遗留：尚未采集真实数据；记录可揭示里程计 frame/时间戳与桥接拒绝原因，不预判首点重合为根因。仓库无 HEAD 且已有大量用户暂存及修改，本次未创建混入用户内容的首次提交。
- 下一步：用户任务前启动脚本，将输出目录路径用于后续分析；原始 bag/日志仅在本地归档，不入 git。

## 2026-09-19：走廊任务接入 MAVROS 与任务系双向坐标适配

- 范围：两份 `corridor_waypoint_test*.launch`、uav_mission CMake 安装项、走廊诊断脚本。
- 改动：复用现有 navigation_frame_adapter，将 MAVROS pose/odom 变换到 camera_init，供任务、规划器、轨迹服务器及控制器使用；控制输出改至 /navigation/setpoint_mission，由适配器逆向转换至 map 后发布 MAVROS。适配器置于 remap group 外以防环路。map→任务系变换增加 xyz/xyzw 参数，默认零平移及四元数 (0,0,0,1)，即旋转 0 度。保留 0.3 米目标与完整四航点；诊断脚本补录转换前的控制设定点。补充适配器 Catkin 安装声明。
- 验证：现有 7 项适配器测试通过，包括非零平移/90 度旋转、逆向设定点、时间戳、机体系 twist/参考点与位姿协方差、异常输入拒绝。两入口 ROS XML 完整展开验证反馈/输出 remap、桥接里程计、任务位姿、适配器无 remap 环路、节点唯一性与默认单位 TF/0.3 高度通过；`catkin_make --pkg uav_mission -j2 -l2` 通过，日志 /tmp/corridor_frame_build.log。未启动硬件、ROS 节点或飞行。
- 遗留：当前单位 TF 是配置约定，不是实际坐标对齐标定证明；实际偏移需标定后配置。Odometry twist 保持 child_frame_id 语义及原机体参考点，不直接换用 IMU 原点的 LIO odom。无实飞验收。仓库仍无 HEAD 且用户暂存/修改众多，未创建混入用户内容的首次提交。
- 下一步：在地面重新加载走廊入口，记录 /navigation/local_odom 的 camera_init frame 及桥接完成事件；飞行验证由用户单独安排。

## 2026-09-19：走廊诊断复盘与 sdf_map 可视化降频

- 范围：`uav_mission/config/horizontal_planner.yaml`；只读分析 `logs/corridor_diag_20260919_002249_Ueacwg`。
- 改动：`sdf_map/visualization_rate` 由 5.0 降为 2.0，仅影响 RViz 显示负载，不改变规划行为；分析发现悬停期间规划器对目标 (2.0, 0.37, 0.3) 反复报 `kinodynamic goal occupied or outside map`（35 次）与 `No collision-free goal within requested waypoint neighborhood`（25 次），进入重规划重试循环，期间 `/navigation/local_pose` 的 z 在 -0.086~0.345 间大幅波动（均值 0.155，目标 0.3），位姿不稳导致占据地图在目标高度层被污染。
- 验证：无编译目标；诊断依据 bag 中 `/planning/goal_status`、`/rosout_agg`、`/navigation/local_pose`、`/mavros/extended_state`（空中 317 帧）实测数据。
- 遗留：位姿 z 漂移/振荡的根因（LIO 低空退化或定高控制振荡）尚未分离；点云未入 bag，幻影障碍的确切来源需带 `/sdf_map/occupancy_inflate` 或 `/freedom/static_pointcloud` 复现确认。仓库仍无 HEAD 且用户暂存/修改众多，本次未创建混入用户内容的首次提交。
- 下一步：RViz 中订阅 `/sdf_map/occupancy_inflate` 观察目标点附近的幻影占据；必要时再评估提高飞行高度或放宽 `horizontal_avoidance` 判定。

## 2026-09-19：缩小 sdf_map 局部更新范围

- 范围：`uav_mission/config/horizontal_planner.yaml`。
- 改动：覆盖底层 `patrol_planner_sim.xml` 的 `sdf_map/local_update_range_x/y/z`，由 8.5/8.5/4.5 降为 5.5/5.5/3.0；走廊航线全长 5 m，以机体为中心 ±5.5 m 仍覆盖全部目标。减少每次点云/ESDF 更新体素量与 RViz `update_range` 盒规模，缓解板端与可视化负载；底层共享 xml 未动。
- 验证：YAML 静态展开，键层级与既有 `sdf_map/*` 覆盖一致；参数在底层 xml 之后加载，覆盖生效。未启动节点、仿真或实机。
- 遗留：更新范围外占据会被 reset，正式赛道若目标距离超过 5.5 m 需重新评估；无实飞验收。
- 下一步：地面重载走廊入口，`rosparam get /fast_planner_node/sdf_map/local_update_range_x` 确认 5.5 生效，RViz 观察盒体缩小。

## 2026-09-19：走廊入口抽取集中配置文件

- 范围：新增 `uav_mission/config/corridor_waypoint_test.yaml`；重写 `uav_mission/launch/corridor_waypoint_test1.launch` 的参数装配方式。
- 改动：原 launch 内联的 Fast-LIO 调参、规划器限速/膨胀/阈值、px4_max_distance、Z 上限、起飞航点表、四航点走廊航线、着陆与高度参数全部移入新 YAML；launch 通过 `test_config` arg 分两阶段加载同一文件（控制组前、控制组后，保持原后置覆盖 runtime 档案的语义），include 传参改用 `$(param ...)` 引用 `corridor/*`。节点、TF、remap 结构与参数取值未变。
- 验证：YAML 语法解析通过；临时 roscore 下 `roslaunch --dump-params` 与 `--nodes` 完整展开成功，转储的航线四航点、waypoints、approach/return/search 高度 0.3、px4_max_distance 0.15、max_vel/acc 0.8、route revision、landing_xy、Fast-LIO 调参与 2026-09-19 实跑参数快照逐项一致，节点列表 13 个与原 launch 相同。未启动节点、仿真或实机。
- 遗留：YAML 中高度相关值（flight_height、waypoints z、route z、approach/return_altitude）为写死副本，修改高度须按文件头注释同步；`$(param ...)` 依赖阶段一先加载，调换 launch 标签顺序会破坏解析。仓库仍无 HEAD 且用户暂存/修改众多，本次未创建混入用户内容的首次提交。
- 下一步：用户地面重载入口，`rosparam get /corridor/flight_height` 确认 0.3；飞行验证由用户单独安排。

## 2026-09-19：修正走廊集中配置的装配方式（弃用 \$(param)）

- 范围：`uav_mission/config/corridor_waypoint_test.yaml`、`uav_mission/launch/corridor_waypoint_test1.launch`。
- 改动：ROS1 roslaunch 不支持 `$(param ...)` 替换（合法命令仅 find/env/optenv/dirname/anon/arg），上一版 launch 无法解析。改为：YAML 中参数直接写到最终服务器键（新增 `fast_planner_node/*` 规划器限速段、`px4_max_distance`、`search/altitude`），launch 不再向被包含 launch 传规划器/高度 arg，保持两阶段加载，阶段二在控制组后重新断言使本文件值最终生效。依据：`patrol_control` 对两个全局参数使用 getParamCached 运行时重读；mission manager 后置覆盖与原内联覆盖同机制且已被 2026-09-19 实跑快照证实；fast_planner 参数后置覆盖与现有 horizontal_planner.yaml 加载顺序相同。
- 验证：临时 roscore 下 `roslaunch --dump-params`/`--nodes` 展开成功（上一版在此步骤报 Invalid <arg> tag）；转储最终值 px4_max_distance 0.15、max_vel/acc 0.8（manager/search/optimization/bspline）、clearance 0.025、obstacles_inflation 0.1、search/approach/return 高度 0.3、四航点航线、waypoints、route revision、Fast-LIO 调参与 2026-09-19 实跑快照逐项一致；节点列表 13 个不变。未启动节点、仿真或实机。
- 注意：上一轮"验证通过"实际展开的是被还原的旧版 launch（文件曾于 01:12 被外部覆盖回原版），本次已重新写入并直接读回确认。
- 遗留：fast_planner 参数后置覆盖存在理论上的启动时序窗口（与现有 horizontal_planner.yaml 相同的历史做法）；高度相关值仍为写死副本，修改高度须按 YAML 文件头同步。仓库仍无 HEAD，本次未创建混入用户内容的首次提交。
- 下一步：用户地面重载入口抽查 `rosparam get /px4_max_distance`（0.15）与 `/fast_planner_node/manager/max_vel`（0.8）；飞行验证由用户单独安排。

## 2026-09-19：走廊配置纳入 sdf_map 地图几何键

- 范围：`uav_mission/config/corridor_waypoint_test.yaml`。
- 改动：`fast_planner_node/sdf_map` 段新增 `resolution` 0.05、`map_size_x/y/z` 12/12/4、`local_update_range_x/y/z` 5.5/5.5/3.0、`visualization_rate` 2.0，与共享档案 `horizontal_planner.yaml` 同键；本文件阶段二最后加载，走廊测试以本文件为准。`horizontal_planner.yaml` 被 4 个入口共享，未做改动，其他链路不受影响。
- 验证：临时 roscore 下 `roslaunch --dump-params` 展开，上述 9 项键最终落值与预期一致。未启动节点、仿真或实机。
- 遗留：同键存在于两个文件，修改时以本文件（阶段二后加载）为准；resolution/map_size 与 local_update_range 需配套评估（盒体不超出有界地图）。
- 下一步：用户地面重载入口确认 RViz update_range 盒体与地图尺寸。

## 2026-09-19：实测 FreeDOM 点云范围与一键 RViz 查看入口

- 范围：新增 `uav_mission/launch/view_corridor_map_rviz.launch`、`uav_mission/rviz_config/corridor_map_view.rviz`；uav_mission CMake 安装目录追加 rviz_config。
- 改动：RViz 配置 Fixed Frame 为 camera_init，默认显示 FreeDOM 静态地图（AxisColor-Z）与规划器膨胀占据云（红色），可开关原始占据/ESDF 切片/输入点云，附 update_range 盒、规划轨迹、当前目标箭头；launch 仅启动 rviz，不动参数与管线节点。实测运行中系统 `/freedom/static_pointcloud`（7.2 Hz，约 19.2 万点/帧，frame camera_init）范围：x [-8.435, 8.197]、y [-9.562, 9.309]、z [-4.044, 2.530]。
- 验证：rviz 配置 YAML 解析通过（10 个显示项、fixed frame 正确）；launch XML 解析通过；点云范围为对运行中系统只读订阅 6 秒统计，未改任何状态。
- 遗留：z 下限 -4.0 m 明显低于地面（约 -0.2 m），与此前发现的位姿 z 漂移一致，疑似污染占据地图的噪点来源之一，待复现确认；rviz 配置未经 GUI 实测打开。
- 下一步：用户在桌面执行 `roslaunch uav_mission view_corridor_map_rviz.launch` 或在已开 rviz 中加载该配置，目视核对地图与噪点分布。

## 2026-09-19：走廊配置纳入 FreeDOM 建图范围

- 范围：`uav_mission/config/corridor_waypoint_test.yaml`。
- 改动：新增 `freedom:` 段（最终位于 `/freedom/*`，与 competition_freedom.yaml 同键、阶段二后加载生效）：`sensor/max_range` 15→8.0、`sensor/min_z` -3→-1.0、`sensor/max_z` 5→3.0，`map/raycast_max_range` 15→8.0、raycast_min_z/max_z 同步为 -1.0/3.0。依据：走廊航线全长 5 m、局部更新盒 ±5.5 m，8 m 传感半径足够；z 向（相对雷达本体）-1.0 保留最低约 -0.9 m 的地面回波，同时切掉实测到的 -4 m 地下噪点（疑似位姿漂移污染占据地图的来源之一）。
- 验证：只读 `roslaunch --dump-params`（对运行中 master 解析，不改其参数）确认 `/freedom/sensor/max_range=8.0`、`min_z=-1.0`、`max_z=3.0` 及 raycast 三键最终落值；其余 competition_freedom 键（counts_to_free 等）保持不变。未重启任何节点。
- 遗留：当前运行中的系统仍是旧值，需用户重载入口生效；local_map 滑动窗口仍关闭（短期测试不需要）。`max_raycast_enhancement_range` 保持 12.0，已被 raycast_max_range 8.0 实际截断。
- 下一步：用户重载走廊入口后用点云范围脚本复查 z 下限是否回到地面附近。

## 2026-09-19：顶层"天花板"占据定位与开关、RViz 方块体素显示

- 范围：`uav_mission/config/corridor_waypoint_test.yaml`、`uav_mission/rviz_config/corridor_map_view.rviz`。
- 改动：实测运行中 `/sdf_map/occupancy_inflate` 顶层 z[2.25,2.35) 约 1.05 万点、铺满约 9×9 m，定位为 `horizontal_avoidance` 竖直障碍列挤压的顶面（从 floor_z 填到 virtual_ceil_height=2.30，`sdf_map.cpp:1009-1015`），因 `visualization_truncate_height=2.49` 而可见。走廊配置 `fast_planner_node/sdf_map` 新增三个显式开关：`horizontal_avoidance/enabled`（false 整功能关闭）、`virtual_ceil_height` 2.30（-0.1 关闭虚拟顶棚，此时挤压列填到地图顶 3.8 m）、`visualization_truncate_height` 2.49（纯显示截断，降到 2.20 可只隐藏顶层）。RViz 配置 5 个点云显示全部由 Points 改为 Boxes 体素风格，FreeDOM Map 方块尺寸 0.1 m 对齐体素。
- 验证：dump-params 确认三个新键与 resolution 0.1（用户已自行调整）最终落值；rviz 配置 YAML 解析通过，5 个显示项均为 Boxes。对运行中系统仅只读订阅统计，未改其状态。
- 遗留：顶层以上 z[2.35,2.65] 仍有约 2.5 万点（真实顶部结构/膨胀残留），与本次定位的 2.3 m 挤压层是两回事；Boxes 风格渲染开销高于 Points，板端若卡顿可将 FreeDOM Map 改回 Points。
- 下一步：用户重载入口后在 RViz 目视确认顶层消失条件（开关置 false 或截断 2.20）。

## 2026-09-19：走廊末点取消 AUTO.LAND，悬停等待人工降落

- 范围：`uav_mission/launch/corridor_waypoint_test1.launch`、`config/corridor_waypoint_test.yaml` 注释。
- 依据：19:38:12 诊断 bag 显示末点切 AUTO.LAND 后飞控回传目标出现后退/偏航；用户确认随后 POSCTL 为主动救机接管，并要求末点悬停、人工降落。具体飞控根因仍缺 ULog/遥控输入证据。
- 改动：从走廊 launch 移除 `obstacle_test_auto_land` 节点及其不再使用的 cruise_z/route_revision 参数；保留 `switch/auto_land=false`、`simulation/enable_auto_land=false`、无视觉节点及 capture_height=航线 Z=0.30m。到末点后任务层仍可能显示 LAND，既有控制器在末点附近保持 capture_height，不请求 PX4 AUTO.LAND；飞手切换 POSCTL 等手动模式后自行降落。没有修改共享降落脚本、控制算法或飞控参数。
- 验证：`catkin_make --pkg uav_mission -j2 -l2` 构建通过；离线展开 `roslaunch --dump-params`/`--nodes`，13 个节点中无 auto_land/arming 节点；最终自动降落开关均 false、external_mission_mode=true、各航点 Z 与 capture_height 一致；YAML 参数与修改前完全相同，仅更新注释。控制代码核查：外部任务不调用旧低高度 Lock；LAND watchdog 失败时转当前位置悬停，任务 LAND 超时走 ABORT 保持，不改 AUTO.LAND。不启动实机节点或仿真。
- 遗留：这是取消自动降落的规避方案，不是 PX4 根因修复；任务状态名称仍保留 LAND，长期等待可能显示 watchdog/任务超时。当前无 HEAD 且已有大量用户暂存内容，不创建混入用户资产的首次提交；修改前快照及验证日志保存在 `logs/corridor_diag_20260919_193812_TRiJJu/manual_landing_change/`。
- 下一步：落地后重新启动此 launch 才应用节点移除；飞行验收由用户安排。飞控自身失效保护不由本次修改取消。

## 2026-09-19：新增走廊点云与占据体素录制入口

- 范围：`top_level_scripts/record_corridor_maps.sh`。
- 改动：基于现有诊断录制流程，新增独立只读入口，原频率记录 `/freedom/static_pointcloud`、`/sdf_map/occupancy`、`/sdf_map/occupancy_inflate` 与现有导航诊断话题；启动时兼容用户误拼的 static_pointcloude（只有该实际话题存在才选用）。默认 Ctrl+C 结束，可指定 1–3600 秒和输出父目录；128MB 缓冲、256MB 分包、不删除历史分包；起止参数快照补充 manager 和 FreeDOM sensor/map；结束生成地图话题消息计数，缺失时明确提示。
- 验证：bash -n、--help、非法时长拒绝（exit 2）、内嵌 ROS Python 编译和关键话题检查通过。未启动实机驱动、任务或实际录制，不宣称现场吞吐已验证。
- 遗留：占据话题是受高度/范围可视化过滤的 PointCloud2，并非完整内部 SDF 栅格；完整 FreeDOM 输入仍记录以辅助判断。全频率地图体积较大，运行期间可查 recorder.log 的缓冲告警。无 HEAD 且用户暂存内容众多，未创建混入用户资产的首次提交。
- 下一步：用户在已有 ROS 系统上启动录制，故障重现结束后 Ctrl+C，提供 corridor_map 日志目录。

## 2026-09-19：地图录制脚本移除 rg 依赖

- 范围：`top_level_scripts/record_corridor_maps.sh`。
- 改动：依赖检查与话题名匹配从 rg 改为系统 grep，使用 `grep -Fxq` 精确匹配完整话题名，修复用户环境报 Missing command: rg。
- 验证：bash -n、--help 通过；实际执行话题选择片段，标准名称、误拼名称回退、两者同时存在、尚无发布者四种情况均通过。未启动录制或任何飞行节点。纯 shell 修改无需编译。
- 遗留：仓库无 HEAD 且用户暂存内容众多，未创建混入用户内容的首次提交。
- 下一步：用户重运行原录制命令。
