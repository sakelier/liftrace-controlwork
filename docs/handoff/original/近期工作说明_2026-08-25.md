# `liftrace-controlwork` 近期工作说明

- 编写时间：2026-08-25 22:46:34 CST（Asia/Shanghai）
- 项目仓库：`liftrace-controlwork`
- 当前重点工作区：`patrol_uav_ws-patrol_planner`、`vision_ws`、`patrol_sim_ws`
- 当前核心包：`uav_mission`
- 工作阶段：最小自主目标搜索任务集成与仿真闭环验证

## 1. 工作目标

近期工作面向2026年度无人机识别与快递运输实物赛，目标是在现有 PX4、Gazebo、MAVROS、FAST-LIO、Mid-360、Fast-Planner 与视觉识别系统之上，建立独立的任务规划层 `uav_mission`，实现以下最小自主闭环：

```text
规则覆盖搜索
→ 接收可靠视觉目标
→ 中断并保存搜索进度
→ 飞向目标上方安全位置
→ 执行临时任务处理
→ 恢复被中断的覆盖搜索
```

系统的长期目标不是单纯执行预设航点，而是形成分层粗到精搜索框架：

```text
低—中高度系统覆盖
→ 高空或远距离视觉线索触发主动复核
→ 近距离可靠确认
→ 机会式投递
→ 恢复覆盖
→ 必要时高空补漏
```

当前阶段优先完成可运行、可验证、可恢复的最小任务闭环。Cue管理、主动下降复核、投递执行、高空补漏和时间预算控制仍属于后续工作。

## 2. 当前系统架构

### 2.1 模块职责

```text
Mid-360 / FAST-LIO
  位姿估计、三维点云注册与环境建图

uav_vision
  目标检测、几何精修、地图投影、目标记忆与投递对准证据

uav_mission
  搜索航点生成、视觉目标准入、任务状态切换、中断与恢复

Fast-Planner
  根据阶段性目标生成局部安全轨迹并在线避障

patrol_control / MAVROS / PX4
  轨迹设定值转换、飞控状态估计与机体执行
```

任务控制主链为：

```text
uav_mission
→ /fastplanner/goal
→ Fast-Planner
→ /fastplanner/setpoint_position/local
→ patrol_control
→ /mavros/setpoint_position/local
→ PX4
→ Gazebo无人机
```

视觉事件链为：

```text
下视相机
→ target_detector
→ detection_fusion / target_refiner
→ target_map_projector
→ target_memory
→ /uav_vision/selected_target
→ uav_mission
```

### 2.2 任务层边界

`uav_mission` 只负责“下一阶段应该去哪里以及当前任务处于什么状态”，不直接实现以下功能：

- 不替代 FAST-LIO 建图；
- 不替代视觉目标检测和地图投影；
- 不生成局部避障轨迹；
- 不直接输出电机、姿态或速度控制量；
- 当前不自动切换 OFFBOARD、不自动解锁、不自动降落。

这种分层使搜索策略可以在不修改底层定位、规划和飞控代码的前提下独立迭代。

## 3. `uav_mission` 软件包建设

### 3.1 ROS包结构

新建的软件包位于：

```text
patrol_uav_ws-patrol_planner/src/uav_mission/
├── config/sim.yaml
├── launch/target_search_manager_sim.launch
├── scripts/target_search_manager_py.py
├── src/target_search_manager_node.cpp
├── src/uav_mission/
│   ├── search_types.py
│   ├── candidate_policy.py
│   └── search_policy.py
├── CMakeLists.txt
├── package.xml
└── setup.py
```

包已接入 catkin：

- `catkin_python_setup()` 安装内部Python模块；
- `catkin_install_python()` 安装ROS Python可执行节点；
- C++占位节点 `target_search_manager_cpp` 可以正常编译和运行；
- `geometry_msgs`、`rospy`、`roscpp` 与 `uav_vision` 等依赖已写入构建配置。

当前真正承担任务逻辑的是Python节点；C++文件仅保留后续移植入口，尚未实现等价功能。

### 3.2 模块化设计

原型被拆分为一个ROS节点和三个纯Python策略模块：

```text
target_search_manager_py.py
  ROS发布、订阅、回调、定时器与状态编排

search_types.py
  公共状态和数据结构

candidate_policy.py
  可靠目标检查与ID去重

search_policy.py
  牛耕航点生成、推进、完成度和恢复
```

依赖关系保持为：

```text
target_search_manager_py
    ↓
search_policy / candidate_policy
    ↓
search_types
```

三个策略模块不依赖 `rospy`，便于进行消息无关的单元测试，并为后续C++重写提供清晰边界。

### 3.3 公共状态与数据结构

`search_types.py` 当前定义：

```text
MissionState:
  INIT
  SEARCH
  APPROACH
  TARGET_HOLD
  RESUME_SEARCH
  SEARCH_COMPLETE

Waypoint:
  不可变三维任务航点

SearchContext:
  waypoint_index
  interrupted_goal
  search_altitude
```

`SearchContext` 用于在视觉目标打断搜索时保存主流程上下文，保证临时任务结束后不会跳过或重置原航点。

### 3.4 覆盖搜索策略

`SearchPolicy` 已实现：

- 参数有限性检查；
- 搜索区域边界检查；
- 正航带间距和正飞行高度检查；
- 矩形区域内牛耕式航点生成；
- 当前航点查询；
- 到达后的索引推进；
- 搜索完成判断；
- 基于已完成航点数的覆盖率；
- 使用航点索引恢复中断任务。

当前 `_current_index` 具有一致的双重语义：

```text
当前正在执行的航点序号
=
已经完成的航点数量
```

当 `_current_index == len(waypoints)` 时搜索结束，`current_waypoint` 返回 `None`。

### 3.5 视觉候选目标准入

`CandidatePolicy` 当前仅接受同时满足以下条件的正式地图目标：

```text
map_valid == True
state >= minimum_state
class_name非空
map_point.x/y/z均为有限数
目标ID尚未被接受
```

已接受ID存入集合，避免同一正式目标反复中断搜索。当前策略有意拒绝 `map_valid=False` 的远距离线索，因为该类检测没有可直接用于规划的可靠地图坐标。

### 3.6 ROS任务管理节点

`target_search_manager_py.py` 当前：

- 发布 `/fastplanner/goal`，类型为 `geometry_msgs/PoseStamped`；
- 订阅 `/mavros/local_position/pose`；
- 订阅 `/uav_vision/selected_target`；
- 使用三维距离与阈值判定航点到达；
- 在 `SEARCH` 中接受可靠新目标；
- 保存搜索上下文并切换至 `APPROACH`；
- 将目标地面点转换为目标上方安全航点；
- 到达后进入 `TARGET_HOLD`；
- 定时停留后恢复被中断的航点；
- 路线完成后进入 `SEARCH_COMPLETE`。

当前状态转换为：

```text
SEARCH --到达--> SEARCH（下一航点）
SEARCH --可靠新目标--> APPROACH
APPROACH --到达目标上方--> TARGET_HOLD
TARGET_HOLD --计时结束--> RESUME_SEARCH --> SEARCH
SEARCH --全部航点完成--> SEARCH_COMPLETE
```

## 4. 仿真参数化与规划接口

第一版 `toudi4_copy.world` 任务参数已经从代码中移至：

```text
uav_mission/config/sim.yaml
```

当前参数为：

```yaml
search:
  min_x: -3.6
  max_x: 2.6
  min_y: -2.0
  max_y: 6.0
  lane_spacing: 1.2
  altitude: 2.2

target:
  approach_altitude: 1.2
  hold_duration: 3.0
  minimum_state: 2

arrival:
  threshold: 0.3
```

搜索边界由世界外墙向内收缩约 `0.8 m`，航带间距结合 `2.2 m` 高度下的下视相机覆盖宽度确定。Fast-Planner的仿真最大速度和加速度均由原值降低至 `1.0`，以适配小型场地和下视视觉观测时间。

旧 `patrol_control` 对 `/fastplanner/goal` 的发布已在仿真launch中重映射到：

```text
/patrol_control/goal_muted
```

因此任务目标发布权可以由 `/target_search_manager_py` 独占，避免两个任务源同时控制规划器。

## 5. 已完成验证

### 5.1 构建与运行验证

已处理并验证：

- C++可执行文件缺少 `main()` 导致的链接错误；
- ROS master未启动导致节点无法注册；
- 独立catkin工作区间 `uav_vision` 消息不可见；
- Python包安装和编辑器模块解析；
- launch文件、参数文件及脚本安装；
- `/fastplanner/goal` 目标发布权。

### 5.2 消息级安全干跑

通过话题重映射，将真实接口替换为：

```text
/fastplanner/goal            → /uav_mission/test_goal
/mavros/local_position/pose → /uav_mission/test_pose
/uav_vision/selected_target → /uav_mission/test_target
```

完整验证目标序列：

```text
搜索航点0
→ 搜索航点1
→ 目标上方安全航点
→ 恢复搜索航点1
```

状态转换实际通过：

```text
SEARCH
→ SEARCH
→ APPROACH
→ TARGET_HOLD
→ RESUME_SEARCH
→ SEARCH
```

该测试证明航点推进、候选目标准入、搜索中断、目标接近、定时停留及原航点恢复已经形成消息级最小闭环。

### 5.3 视觉目标链路验证

视觉节点已经能够完成：

```text
下视图像
→ 检测与圆环关联
→ 地图投影
→ target_memory
→ /uav_vision/selected_target
→ uav_mission日志
```

近距离实测曾输出：

```text
id=4
class=panzer
state=2
map_valid=True
```

当前实验事实表明：

- 低空、近距离时可以形成正式地图目标；
- 高空或远距离时通常只能形成 `map_valid=False` 的检测线索；
- 现有 `uav_mission` 只处理前者；
- 后者需要后续Cue管理、图像居中和下降复核机制。

### 5.4 PX4状态估计发散诊断与修复

此前仿真曾反复出现：

```text
AUTO.LOITER中真实机体漂移
→ PX4估计与Gazebo真值分离
→ gps_glitch
→ local/global position失效
→ Navigation failure
→ Failsafe Descend
```

ULog分析发现，故障时参数形成了不一致的混合配置：

```text
EKF2_GPS_CTRL = 7
EKF2_EV_CTRL  = 15
EKF2_HGT_REF  = 1
```

即GNSS与Gazebo/MAVLink External Vision同时参与融合。EV与GNSS/ground truth逐渐冲突，GNSS创新超限后被拒绝，最终导致EKF发散。

将配置改为当前GPS仿真基线：

```text
EKF2_GPS_CTRL = 7
EKF2_EV_CTRL  = 0
EKF2_HGT_REF  = 1
```

完整重启后执行相同起飞—HOLD A/B测试：

- 水平真值—估计误差约 `0.02–0.18 m`；
- `gps_glitch_status_flag=False`；
- 未出现导航失效或自动下降。

原始Mid-360/深度相机视觉机型采用的是另一套内部一致的Vision-only配置：

```text
EKF2_EV_CTRL  = 15
EKF2_GPS_CTRL = 0
EKF2_HGT_REF  = 3
```

因此问题不是“EKF或EV永远不可用”，而是不同airframe定位参数发生了混合。当前带有效GPS的比赛仿真保持EV融合关闭；若未来需要FAST-LIO参与PX4定位，必须另行完成里程计frame、时间戳、协方差与reset语义转换。

### 5.5 真实规划控制链牛耕测试

在状态估计修复后，`uav_mission` 已接入真实 `/fastplanner/goal`，通过QGC起飞并切换OFFBOARD，完成一次完整牛耕覆盖。

测试条件：

```text
区域：x ∈ [-3.6, 2.6]
      y ∈ [-2.0, 6.0]
航带间距：1.2 m
搜索高度：2.2 m
航点数：16
```

测试结果：

- 全部16个航点按牛耕顺序完成；
- 左右方向交替和航带推进正确；
- 最终输出 `Search route completed`；
- 最终位置约为 `(-3.55, 6.00, 2.06)`；
- 最终航点误差约 `0.05–0.08 m`；
- 全程没有GPS glitch、EKF发散或导航失效；
- Fast-Planner能够在障碍物周围生成局部绕行和升高轨迹。

该结果首次证明了以下完整链路可以闭环运行：

```text
SearchPolicy
→ target_search_manager_py
→ Fast-Planner
→ patrol_control
→ MAVROS / PX4
→ Gazebo
→ MAVROS pose反馈
→ 航点推进
```

## 6. 开发与启动环境整理

项目包含多个独立catkin工作区，`uav_mission` 同时依赖规划工作区和视觉工作区生成的消息。已明确运行时加载顺序：

```bash
source /opt/ros/noetic/setup.bash
source /root/astra_workspace/liftrace-controlwork/vision_ws/devel/setup.bash
source /root/astra_workspace/liftrace-controlwork/patrol_uav_ws-patrol_planner/devel/local_setup.bash
```

第二个工作区使用 `local_setup.bash`，以保留视觉工作区环境。完整节点启动顺序已经整理为：

```text
ROS Master
→ PX4/Gazebo/MAVROS
→ FAST-LIO/FreeDOM
→ Fast-Planner/patrol_control
→ uav_vision
→ uav_mission
```

同时已建立容器内统一环境脚本：

```text
/root/astra_workspace/scripts/uav_ros_env.bash
```

用于减少重复source与工作区不可见问题。

## 7. 当前新发现的问题

### 7.1 全局点云可视化与带宽瓶颈

牛耕飞行期间，RViz出现地图卡顿或无法正常刷新。数据检查表明FAST-LIO、TF和点云输入均正常，瓶颈位于FreeDOM累计地图的全量高频发布：

```text
/freedom/static_pointcloud
点数：约750,000
单帧：约12.1 MB
发布频率：约8 Hz
带宽：约74–87 MB/s
```

RViz配置中的 `global Map` 正在显示该话题。随着地图增长，FreeDOM需反复重建和发布全部静态点，Fast-Planner与RViz又同时订阅，导致CPU、序列化、传输和渲染负载持续升高。

相关代码位置：

```text
FreeDOM/src/visualization.cpp
  Visualizer::visualize_map_removal_result()
  Visualizer::visualize_static_pointcloud()

FreeDOM/config/mid360.yaml
  map/sub_voxel_size

patrol_control/rviz_config/patrol_pc.rviz
  global Map → /freedom/static_pointcloud
```

建议增加独立的低频、降采样可视化话题 `/freedom/static_pointcloud_viz`，供RViz显示；规划地图话题与可视化话题应解耦。不能未经评估直接降低原规划话题频率或粗化地图，因为这可能影响Fast-Planner的障碍物更新与安全裕度。

### 7.2 任务完成终态不完整

当前 `SEARCH_COMPLETE` 只停止发布新目标。Fast-Planner与 `patrol_control` 会继续保持最后航点；任务节点不会自动HOLD、返航、降落或解除锁定。

需要后续明确：

```text
SEARCH_COMPLETE
→ HOLD / RETURN / DOOR_CROSSING / LAND
```

### 7.3 到达判定仍较简化

当前仅使用三维位置距离和 `0.3 m` 阈值。尚未结合：

- 飞行速度；
- 连续稳定时间；
- 规划器到达反馈；
- 轨迹失败或目标不可达状态。

局部避障轨迹可能在任务航点附近高速穿越阈值，因此后续应加入低速和持续时间条件。

### 7.4 失败恢复机制未完成

尚未覆盖：

- 规划失败；
- 目标接近超时；
- 目标丢失；
- 目标不可达；
- 投递失败或超时；
- 视觉目标误报后的ID释放与重试；
- 剩余时间不足；
- 定位质量下降和工作区越界。

### 7.5 视觉Cue与投递尚未闭环

当前只接受 `map_valid=True` 的正式目标。尚未实现：

- `map_valid=False` 的连续线索管理；
- bbox居中；
- 分级下降复核；
- 复核失败后的爬升与恢复；
- 目标—货物匹配；
- 视觉对准；
- 释放机构控制；
- 投递成功证据。

### 7.6 工程规范待补充

`uav_mission/package.xml` 当前仍包含：

```text
maintainer: root@todo.todo
license: TODO
version: 0.0.0
```

正式推送和团队交付前应补充维护者、许可证、版本、README与测试说明。C++节点目前是占位实现，不应在文档中描述为可替代Python任务管理器。

## 8. 当前阶段结论

近期工作已经完成三个关键跨越：

1. 从旧巡逻控制中的固定航点逻辑，分离出独立 `uav_mission` 任务层；
2. 从单一目标发布脚本，发展为具有规则覆盖、正式目标准入、中断和恢复能力的最小状态机；
3. 从消息级干跑，推进到PX4/Gazebo真实规划控制链上的16航点完整牛耕验证。

同时，PX4定位发散问题已经通过ULog分析和单变量A/B实验解决，使后续视觉与任务联调建立在稳定飞控基线上。

当前系统可以准确表述为：

> 已具备参数化牛耕覆盖、可靠视觉目标触发、任务中断和搜索恢复的最小原型；纯覆盖任务已在完整仿真控制链上运行成功。视觉目标近距离链路和消息级中断恢复已经分别验证，但“飞行中由真实视觉目标触发中断—接近—恢复”的联合闭环、Cue主动复核和实际投递尚未完成。

## 9. 建议下一步

按风险与依赖顺序建议：

1. 修复FreeDOM全局地图的高带宽可视化问题；
2. 在 `10032` airframe或启动检查中固化GPS基线EKF参数；
3. 增加任务安全监视器：定位有效性、GPS glitch、速度、位姿跳变和工作区边界；
4. 在完整视觉负载下重新执行HOLD和牛耕回归；
5. 验证真实 `/uav_vision/selected_target` 对飞行中搜索的中断与恢复；
6. 为 `SEARCH_COMPLETE` 增加明确的HOLD、返航或降落策略；
7. 增加规划失败、目标超时、不可达和重试机制；
8. 实现Cue连续帧管理、主动居中与下降复核；
9. 接入投递对准、释放执行和成功证据；
10. 使用多随机种子世界统计发现率、任务完成率、航程、耗时和碰撞率。

在上述工程能力稳定后，再评估将Python原型迁移至C++，以及将FAST-LIO里程计规范接入PX4 External Vision融合。
