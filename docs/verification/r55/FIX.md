> 历史阶段记录。后续 R56 根因修复已完整 PASS（37/37）；最新结果见 [最终报告](../r56_final/REPORT.md)。本文件保留当次运行的真实结论。

# R55 规划修复与单轮验证准备

针对 R54 门前目标被膨胀地图占据、无进展 REACH_HORIZON 曲线被执行、同步搜索阻塞 36 秒的问题，继续复用 Fast-Planner 和现有 POST_DELIVERY_ROUTE 状态机。

- 地图分辨率从 10 cm 改为 5 cm；水平膨胀仍为 0.30 m，上膨胀 0.10 m、下膨胀 0.30 m，保留机架保护圈尺寸及搜索区水平绕障柱。地图范围 X [-6,6]、Y [-11,11]、Z [-0.2,2.8]，实际飞行虚拟顶界仍为 2.30 m；6,336,000 个体素，主要 SDF 缓冲约 355 MB。没有将机架缩小来打开门洞。
- 点云先量化到体素，再连续填充 Z 范围；点云/里程计订阅只保留最新消息。可视化降至 5 Hz，建图仍按传感器更新，避免大点云展示占用链路。
- 被占用或地图外目标立即返回失败，等待下一帧地图；短于 horizon 的目标必须找到完整路径。远目标的 horizon 终点必须相对原目标缩短至少 0.20 m。不会再以远离门口的 7 m 曲线替代半米请求。
- 短距离（2 m 内）先尝试完整解析连接，沿程使用同一膨胀地图检查；仍经原有 B-spline 拟合、速度/加速度时间缩放、完整占用/ESDF 检查，保留优化失败时的已验证初始曲线回退。
- 每次搜索有 0.25 s 墙钟预算，内层扩展也检查；原有二次搜索最多约 0.50 s 加优化开销，避免长时间不处理地图和里程计。没有对共享地图直接启用多线程回调。
- 保留 R54 的 11 段引导：门外降高/对正、门前、穿门并清出、下一门、H 降落；同一任务状态机顺序推进，无新控制器、桥接节点、话题或握手协议。取景仍为 1.6 m；今年不做槽位偏移补偿，相机仍位于雷达 IMU 正下方 21 cm。

## 离线与构建结果

精简整机视觉和导航 catkin 构建通过。7 个真实 C++ 搜索/地图检查通过，共 594 ms；直接运行测试二进制，无 ROS master、Gazebo、PX4 或飞行动作。

R54 164.69 s 原始 FreeDOM 点云 28,352 点通过环境变量 LIFTRACE_FROZEN_CLOUD 输入测试：10 cm 网格复现门前占用；5 cm 网格下实际 SDFMap 点云回调及 ESDF 更新 160 ms，门前接近与从 y=5.672270 到 6.672270 的完整路径均以 5 ms 间隔验证占用=0、ESDF>=0.025 m。封闭墙在预算内失败；受阻短连接仍能寻找水平绕行。此结果是冻结地图检查，不替代整场或实机验收。

修复前 path_searching、plan_env 包快照只保存在导航来源分支 legacy_baseline/20260907_r55_planner，含清单及规则要求的 SHA256。精简分支不复制旧参考包。

## 本轮边界

用户已要求“执行修复后重新运行一次仿真”。接下来仅启动一次完整搜索、三投、三门、H 降落 SITL；无论结果均收尾、更新报告并推送功能分支。尚未取得本版本整场结果，不合入 main。
